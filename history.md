# Lupin Project History

> **Archives**: See [history/README.md](history/README.md) for the full chronological index. Most recent: [2026-08-14 to 08-20](history/2026-08-14-to-20-history.md).
>
> **Last archived**: 2026-08-30 by Mr. Radio 🦉 (session 93a8751c) — one cut (08-14 to 08-20), seven entries, 25,267 chars moved verbatim; character accounting balanced (46,822 kept + 25,267 cut = 72,089 before). Cut by DATE, not position — the 08-19 entry sits at the BOTTOM of the file below 08-14, so a tail cut would have archived a newer entry than it kept. Prior: 2026-08-25 by Krishna 🦚 (one cut, 08-10 to 08-12). Prior: 2026-08-21 by Cheech 🌿 (one cut, 08-04 to 08-07).
>
> ⚠️ **This line records PROVENANCE, not health — do not read it as a status.** It says when the file was last archived, not how big it is now. The previous header asserted *"✅ HEALTHY at ~7.9k tokens (31% of 25k)"*; six days later the file was **21.0k — past the 19k CRITICAL threshold** — and the header still said 31%, because nothing re-derives a stamp. **On 2026-07-27 that stale figure was quoted into a commit message as a health claim in the same command that measured the real number.** Same defect as a calibration stamp standing in for its instrument.
>
> **Measure it, never quote this line**: `python3 -c "import io;n=len(io.open('history.md',encoding='utf-8').read());print(f'{n/4/1000:.1f}k tokens')"` · thresholds **17k WARNING · 19k CRITICAL · 25k limit**.

### 2026.08.31 (evening) - Session 1a265076 (Rio ⚡, author #1 for Mr. Radio 🦉; review Tiberius 👑 · Rachel 🕊️) | The checked-hash control went live, and every defect in it was an empty value read as permission

1. **Row `f313fc2d` needed WIRING, not building.** The mechanical control was built on 08-30 by `3688a47b` (row `9b5b97de`) and **nothing called it** — its own R&D banner says the module is inert pending Rick's placement ruling. Rick ruled placement 3+4 (patch prevents, verifier detects), and `src/sitecustomize.py` IS that placement. Merged `184ea9cb`.
2. **🔴 An empty root set was read as UNIVERSAL scope, in a permission check — found by Tiberius 👑.** `sitecustomize` installs in EVERY interpreter and `_default_roots()` returns `()` without `LUPIN_ROOT`, so on any such interpreter the patch owned the whole filesystem. Verified before fixing rather than read off the source: `_in_scope( "/usr/lib/python3.13/json/decoder.py", () )` answered **True**, and a scratch package outside every repo came back **checked-hash**. Now fails closed (`9226f26a`).
3. **🔴 And the test that should have caught it was BLIND — he proved that too, which is the sharper half.** Both scopes excluded `tmp_path` (the explicit `/nonexistent/elsewhere` AND the inherited `$LUPIN_ROOT/src`), so the negative arm alone could not tell an honoured scope from an inherited one: with only that arm, reverting the prologue to its pre-fix form **left the test passing**. I had handed him that gap BY NAME as the thing I could not close, and naming it is what produced the discriminator.
4. **The same shape, three more times, all mine.** `install()` returned a bare `False` for both *"already in place"* (the preventer works) and *"cannot be patched"* (there is no preventer); making them merely falsey was still wrong, because to a caller asking *"is it ACTIVE?"* already-installed is a **success** — so `__bool__` now RAISES and names `is_installed()` (Mr Radio's ruling). Three end-to-end tests took their scope from the **ambient environment**: one child reads `converted_count` **0** with `LUPIN_ROOT` set, **2** with it unset — one test failed outright, two passed **vacuously**. And Rachel 🕊️ measured that the shim caught `Exception`, not `BaseException`: a staged `KeyboardInterrupt` gave **`Fatal Python error: init_import_site`** and the interpreter never started.
5. **🔴 I withdrew my own explanation for the one red I could not place.** Called a `test_bounce_dev_server_busy_guard` timeout *system load*, then tested it: under `systemd-run` at `CPUQuota` 100/25/10/5% it passes every time at 1.15–1.18s. **Not CPU-bound, so the hypothesis failed.** Recorded in-file as **UNEXPLAINED** — not flake, not load — with *"do not wave a red here through"*; ceiling left at 30 because raising it to stop a red switches off the sensor. Also withdrew two of my own numbers: *"9ms and 9ms"* was **integer truncation** hiding a difference, and a re-take with `env -u` misplaced measured **`env` failing** at 0.68 ms/start.
6. **Every fix carries a two-arm proof on one mutated sha, restores verified by sha.** Fail-open mutant `702b3db7` survives the old fixture, killed by the new. Reverting `__bool__` reddens exactly the two new cases. Scope mutant `1828a077` reddens two table rows AND the real-child guard (3 failed / 56 passed), restored 59 passed at `85082d3c`.

**Checkpoint**: merged `184ea9cb` (6 files, 501 insertions) after Tiberius verified `9226f26a` in his own detached worktree — **disclosed in the merge message that it carries `61b2eb93`, one docstring-only commit past the sha he verified**, rather than letting an approval stretch silently. Tier on the merge: **2 failed / 21316 passed**, both the deliberate `secret_scan` rotation hold. ⚠️ **The passed count moved 21216 → 21316 and the extra hundred is NOT mine** — my branch is 4 commits off `904a9b01` while the working branch had advanced 6 to `b67fd52b`; **guessing** at the source, the `kill_guard` tests plus Rachel's merge. Two earlier tiers **void and withdrawn**: both measured trees I edited underneath them. Coverage on the module 100% (149 stmts / 0 miss, 42 branch / 0 partial); `sitecustomize` a declared omit with its reason. Left unstaged, not mine: `src/conf/epic-stories.json` and two peers' `src/rnd/` docs.
**Files**: `src/sitecustomize.py` (new) · `src/cosa/utils/checked_hash_pyc.py` · `src/tests/unit/test_checked_hash_pyc.py` · `src/tests/unit/test_sitecustomize_fails_open.py` (new) · `src/tests/unit/test_bounce_dev_server_busy_guard.py` · `pyproject.toml`

### 2026.08.30 (evening) - Session 93a8751c (Mr. Radio 🦉; crew Rachel 🕊️ · Tiberius 👑 · Pocholo 📣 · Clayton 😎 · Rio ⚡ · Krishna 🦚 · maya 🌻 · chloe 🗼) | Five tools, one shape: a clean exit that means nothing happened

1. **🔴 Five seats found the same defect in five different tools and none of us recognised it until they sat together.** `--cov` at a bad target: exit 0, no table, **never measured**. `--verify` on a fresh tree: exit 0, **nothing there to judge**. `purge-pycache.sh --verify`: exit 0, flag ignored, **purged anyway**. A purge on an already-emptied tree: exit 0, *"nothing to purge"*, **never ran**. `rc == 1` read as a mutation kill on a suite that was **already red at baseline**. In every one the failure and the success print the same thing. Landed `49dba474`, then **rewritten around Rachel's correction** at `e5e68c09`: mine told the reader to *check*, which is a rule requiring vigilance, and this page's own doctrine is that a rule depending on memory is not a control. **The fix is a tool that REFUSES and names what it did not do** — both live examples are ours: Rio's purge declining before the `rm`, and Rachel's harness printing `ANCHOR MATCHED 2x — NOT APPLIED` rather than guessing at placement.
2. **🔴 I published a rule, filed it P1, and retracted both within the hour.** Claimed an ordinary pytest run undoes the checked-hash conversion. **maya disproved it** with a probe module and a two-directional negative control — a source pyc stays checked-hash and the mutation IS seen. Pytest's rewriting writes a **disjoint filename tag** (`…-pytest-8.4.2.pyc`), which is why two honest counts looked contradictory. Measured across three trees: main checkout **2452 checked-hash / 0 timestamp**, a converted worktree 2434/1, an **unconverted worktree 0/724**. Corrected finding at `0a3a2471`, sharpened by Pocholo into **three states** — fresh-unused verifies `0` *vacuously*, fresh-then-one-run verifies `1`, converted-and-populated verifies `0` genuinely. **The first and third print the same code and mean opposite things.**
3. **🔴 The remedy this file prescribed reached into a shared tree.** `purge-pycache.sh` took its root from `$LUPIN_ROOT`, inherited from the shell — so run from a worktree it purged and reconverted the **MAIN REPO**, printed its success banner, and left your tree as poisoned as it found it. **Found and written up by Pocholo at ~17:52 in `TODO.md:671`**; re-derived independently by Rachel five hours later, who **declined the credit and pointed at his entry**; Krishna reached it a third time with the sharpest receipt (worktree kept all 91 dirs, script reported *"Purging 188"*). ⚠️ **It was written down, correctly and in detail, and the whole crew walked into it anyway for five hours** — including a reviewer whose every per-arm isolation went to the wrong tree. *A defect recorded in TODO.md is not a control; only the code is.* Fixed by Rio (`6cb757b3`): root from `BASH_SOURCE` unconditionally, **both scripts** (purge chains to migrate, so fixing one leaves them resolving different trees).
4. **My own stopgap left the exact half-done tree it warned about.** `LUPIN_ROOT="$PWD" purge-pycache.sh` **purges then fails to reconvert** in a venv-less worktree, because `PYTHON` derives from `LUPIN_ROOT`. Two variables, two questions — *which tree do I clean* versus *what runs compileall* — and I had treated them as one knob. Fixed `5e72942c`, verified by Rachel independently in a real worktree. ⚠️ **My original verification tested a hardcoded `PYTHON` while I published a `git-common-dir` expression** — I verified one command and shipped another.
5. **Three seats corrected the record against their own interest, and that is why the rest is trustworthy.** Tiberius **retracted his own approval** after proving the same arm at the same sha read KILLED then SURVIVED, and reported a coverage figure as *unverified* rather than take it under contention. Clayton audited his **own** fixture, found `acked=3` against `recipients=3` (two equal numbers cannot reveal a swap between them), and later **dropped his own duplicate branch** on discovering Pocholo's covered the class. Pocholo **declined to self-merge** though both reviewers told him to.
6. **maya's rule is the strongest thing on the branch and it did not come from me.** *Coverage measures whether a line RAN, never whether the test could have NOTICED it running wrong.* Two files at 100% lines and branches with the defect live in them — the number TRUE and useless, because a fake taking any arguments returns its scripted page whatever is asked. **The mechanical tell**: replace the code under test with a constant; if the fixture still yields the same data, the suite is measuring the fixture.

**Checkpoint**: 7 branches merged (`c91bd1bb` replay user_id · `168e677c` bounce_dev_warn 100% · `3ccc821a` coverage_contention · Chloé, three maya branches, Rio's eleven); ~25 CLAUDE.md commits including **three retractions of my own claims**; 3 seats re-spun on sensor-confirmed readings. **Ends blocked, correctly**: `wt-maya-4f0ced13` held unmerged until Rick rotates the postgres credential — the red is the signal working and greening it changes the signal, not the risk. Left unstaged: `src/conf/epic-stories.json`, a peer's work, unclaimed in `.claude-session.md`.
**Files**: `CLAUDE.md` (five new rules) · `TODO.md` · merges above

### 2026.08.29 (evening wrap-up) - Session 36656467 (Mr. Radio 🦉; crew Krishna 🦚 · Rio ⚡ · maya 🌻) | Three workers reported their own mementos stale, and the reap alarm gave one verdict for two different faults

1. **🔴 Every worker's inherited memento was wrong about merge state, and every one of them caught it by re-deriving rather than quoting.** maya's predecessor claimed all seven of her commits were merged — `git log HEAD..wt-maya-ba6df71e` returned **8, none of them merged**, including `9529abce`, the whole `watch-hook-events.py` coverage ramp. Rio's own memento listed `cp-rio-42f65ee1` as outstanding when `git merge-base --is-ancestor 77240c5b HEAD` returns true; Krishna's said two commits were unmerged and `1f5d872e` made that false **inside a minute**. *A memento written at session end describes a tree that is still moving under it.*
2. **Both branches merged with green receipts I ran myself, not claims I accepted.** Krishna's `e8bfbbe6` + `a90f8fa8` → `1f5d872e`: `test_tfe_to_cc_phase1_smoke_script.py` at **66 pass pre-merge, 68 post**, and Krishna reconciled the delta rather than just reporting it — 64 at `12d6309e`, +2 pins at `a7d23fe4`, then `e8bfbbe6` swapped those 2 pins for 4 agreeing tests. maya's 8 commits → `27f993b9`: **63 pass** on `test_watch_hook_events.py`, `watch-hook-events.py` off the zero set at 149 stmts / 38 branches.
3. **maya flagged a merge hazard before it landed and she was right to.** Four of her eight commits edit `CLAUDE.md`, and HEAD already carried my own 08-30 narrowing of the scoped `--cov` rule; she asked for a diff-before-merge rather than assuming hers superseded. Auto-merge went clean and the narrowing survived intact at line 504 — **checked, not assumed**, which is the only reason that is worth writing down.
4. **`memento_alarm` returns one verdict string for two faults with opposite remedies.** The reap flagged 2 of 3 seats `unparseable_present`; I opened all three as the alarm instructs and **every file was complete and current**. Krishna's was a **timing race** (header stamped 23:31:05, the prober's 45s window had already closed); Rio's is **genuinely header-less** — row `48b5f19e`'s defect, reproduced live. One is a timeout, the other a writer bug, and the verdict cannot tell you which. Receipt amended onto the row.
5. **The header-less memento is a bypassed write path, not a skipped one.** Rio hand-wrote a substantive, current 11,320-byte memento — content fine, header absent. Krishna named the mechanism in the same minute (row `5680c544`): `memento_io write` **refuses on immutability** when a record already exists this session, and a seat reading that refusal as failure falls back to a hand-`Write`, which lands the content without the header.

**Checkpoint**: crew harvested first per the ritual's own §0.3 — 3 seats reaped, 3 bridges deleted, 3 holds cleared, `c89cec9b` reconciled onto my board; all three mementos verified on disk by reading them. Left uncommitted and **deliberately not staged**: `src/conf/epic-stories.json` (+12 lines, valid JSON) — it is a peer's work, unclaimed in `.claude-session.md`, and two of its three new epics are not mine.
**Files**: merges `1f5d872e` · `27f993b9` · row `48b5f19e` (amended, queued)

### 2026.08.29 - Session 36656467 (Mr. Radio 🦉) | Two branches that looked unmerged were already in, and the README told readers to run a deleted file

1. **🔴 An orphan inventory built on commit hashes counted work that had already landed.** Rick's PR broadcast asked for stragglers swept up. `git rev-list HEAD..<branch>` reported five local branches carrying 14 unmerged commits, and I put two of them to him as decisions. **He ruled merge-it-in on the memory cap — and the merge conflicted, which is what caught it.** `git cherry -v` (patch-id, not sha) shows both branches were re-applied to this branch under new hashes: five of six security commits report already-applied, and the sixth landed as `458d1730` with the identical 8-file footprint. **The branch copies are OLDER** — `jwt_service.py` there still uses the deprecated `datetime.utcnow()` where the branch has `datetime.now( timezone.utc )`, so cherry-picking would have regressed it. *A commit hash is an identity, not a content check; rev-list cannot tell re-applied work from absent work, and only the merge conflict made the difference visible.*
2. **The memory-cap merge would have re-created the exact split that made Rick's own ruling fail.** `start-cc-with-tmux.sh:352` reads `CC_MEM_LIMIT="${CC_MEM_LIMIT:-8G}"` — the 8G ruling is live. `test_launcher_memory_ceiling.py` exists *because* there were two implementations and the ruling was applied to `memory_cap.py`, **the one that does not run**, leaving the fleet at 24G for three days; the file names that branch explicitly. Merging re-adds the duplicate and regresses three files to pre-`ab2a321c`. Not merged; 71 tests green on what is already there. **Salvaged the one genuinely-absent piece**: `watch-cc-memory.sh`, a 28-line wrapper over a module that has been on the branch since `216dc749` — so the watcher the fleet depends on had no launcher in the tree. Executed rather than asserted: it names three live sessions with their ids and per-scope memory.
3. **The entry page told a new reader to run a file deleted three days earlier.** README's quick-start still listed `src/scripts/run-lupin-gui.sh`, removed 08-26 with `src/lib` (`71d5efaa`). Its test counts also disagreed with **its own table eleven lines above** — 12,436 against 16,247 unit, 678 against 663 E2E — because the 08-28 refresh re-derived the table and never touched the block below it. Both now match; 663 re-derived today and agrees exactly. `src/rnd/README.md` claimed v0.2.0 held 181 files in 6 subdirs (it holds **210 in 9**) and its *"put new documents here"* pointer still named `v0.1.8/`, current until 08-03. `bbf260f4`.
4. **Three doc-only orphans landed; a fourth finding came out of the debris.** `2905e606` (Pass-B kwarg table), `59c3e6cb` (epic-accordion review + its five probes, kept together so the verdict can be re-run), `833eb31f` (pocholo memento) — merged, no source touched. An untracked `projects-data/lupin-mobile/.respin-boot-receipt-*.json` in the repo root led to row `1facc18e`: **`fleet_data_root()` takes the sibling of the `projects` directory, which is right for lupin and wrong for a repo nested under it** — a lupin-mobile session resolves `…/projects/lupin/projects-data/lupin-mobile`, inside this working tree. Reproduced both directions. `respin_wake_check.py`'s own header warns against that placement (*"never the repo root … no reader looks there"*, row `011f1f90`). A `.gitignore` backstop keeps the debris out of commits and is commented as **not** the fix.
5. **The parked audit fix was verified without being merged, on Rick's word twice.** Row `a01e4e2a` — a caller-supplied reason **replaced** the field delta, so an overwrite recorded only the justification. Rick, 08-28: *"Park this. You will return to it after the merge."* Re-confirmed 08-29 with an addition: *"I don't want this to get lost in the sauce — I want to make sure that there is a bug tracker ID for this."* The previous amendment said **"NOT VERIFIED BY ME … treat the patch as unproven"**; that is now discharged — **88 passed** on the file, **236 passed** across every other `apply_patch` caller. The work stays uncommitted in the tree and the row carries the receipts. **Its four guards had to assert the composition directly**: the old ternary's arms are intra-line, so coverage reports the line covered whether or not the truthy arm ever ran — which is how this survived a 100% gate.

**Files**: `README.md` · `src/rnd/README.md` · `src/scripts/watch-cc-memory.sh` · `.gitignore` · rows `1facc18e` (P2) · `42f65ee1` (P3) · `a01e4e2a` (parked, chase 09-04)

### 2026.08.28 - Session a6c3e87f (Mr. Radio 🦉, one self-respin; peer María 🌸) | A cancelled job that came back to spend money, and a cost estimate that read the wrong side of an if-statement

1. **🔴 Cancelling a queued job never touched the ledger, so a restart re-ran it.** `delete_by_id_hash` mutates the in-memory dict and nothing else; the `job_history` row stayed `pending`, and `get_restorable_jobs()` selects exactly `pending`. María measured it end to end: cancel at 12:34 with the endpoint's own *"No work was lost"*, queue empty at 12:54, `:8000` bounced at 12:58, **job back at 13:00** — a metered ~105-minute eval that had been cancelled *because* its slot was wrong. Fixed at `b46818e2` + `2107f24f` across all three delete doors, with `CANCELLABLE_QUEUES = {"todo"}` so a normal completion is never mislabelled. **My own structural suggestion was wrong and she was right to refuse it**: I proposed moving the write into `delete_by_id_hash`/`clear()` so every door inherited it — all eight call sites in `running_fifo_queue.py` are `_transition_to_done`/`_stalled`/`_dead` and four handler paths, so that design would have stamped `cancelled` on every completed job in the system.
2. **Two venues, two databases, and the empty answer that reads like a fact.** Neither container sets `DB_NAME`, so a host shell inherits the *Development* block and queries **dev** even when chasing a job that ran on `:8000`. Host/dev: 205 rows, zero `ts-` rows. In-container: 4 rows, all same-day, including the one at issue. The host answer reads exactly like *"test_suite jobs are never persisted"*, which is false and nearly cost a correct fix its retraction. `fafb60d5`, plus `7d2e5aab` — going at the DB container and **naming** the database has no default to fall through to, where "run inside the container" still reads *a* database successfully either way.
3. **A cost estimate that pointed at the wrong side of an `if`, and Rick caught it in one sentence.** Row `e2099400` costed job 2 of the `:8000` fold-in as blocked on *"a booting lifespan loads Whisper and the embedding models… the runtime nobody costed."* Rick: *"There is no way we are loading a whisper model. That's a standalone service we consume."* He is right and the code already agrees: `main.py` branches on `speech to text provider`, `lupin-app.ini:569` ships `= model-server`, and **remote mode is the default** — the lifespan calls `declare_remote_only()`, sets `whisper_pipeline = None`, and probes `:7998`. The eager three-model load is the `else` branch no test runs. **Job 2 is plumbing, not a blocker**, and it is the valuable one — those 274 `lifespan` statements are the bulk of the gap keeping a whole-system coverage figure from existing. Job 3 (coverage from inside the container) is now the only real unknown.
4. **A row cited a live specimen and the citation outlived it by 36 minutes.** Row `91ccbc26` pointed at a corrupted `park_reason` sitting in row `2ebe4ccb`; I closed `2ebe4ccb` half an hour later, and leaving `parked` **clears the field by design**. Checking further, the *first* specimen (`9baba1f7`) was already gone the same way. **`park_reason` cannot be used as an evidence store** — any defect whose evidence lives in one evaporates when that row moves, silently, while the citing row still asserts it is there. Both fragments survive only because they had been quoted verbatim into prose first.
5. **A blocked row closed by changing the channel, not the message.** `9baba1f7` sat three days on delivering a `COVERAGE_FILE` notice to a seat that exists on no surface. But `src/conftest.py:218` already raises a `UsageError` carrying the cause, the verbatim remedy and the escape hatch — **the error IS the notice, at the moment of need**; the DM was a courtesy. The durable half (the `fail_under` scope boundary) went into `src/tests/README.md`, which had no coverage section at all, so it also reaches every seat the original commons post missed by being pull-not-push. `2465bf2f`.
6. **The README's headline numbers were three weeks stale.** The test table was an 08-07 snapshot claiming 24,547; re-derived 08-28 by the table's own stated method it is **29,167** (26,743 Python + 2,424 TypeScript), now grouped by directory so anyone can reproduce it with one grep. Highlights stopped at 08-25 with 209 commits after them. Coverage stated for the first time — **98.46% over 62,225 statements**, floor 98 — with the caveat that it covers the unit and CoSA tiers only and **no whole-system figure exists yet**. `73cd7176`.
7. **Rick's correction on ownership, and what it had been doing to the record.** *"Tiffany doesn't own the task. That persona has simply been working on it. Any one of you could pick this up with zero problems."* Row `e2099400`'s park reason had said the ramp step was hers and therefore *"cannot move today by anyone"* — measured absence, false inference, and it converted a scheduling choice into an impossibility a reader cannot argue with. Re-parked on its true reason: the PR sequencing.

### 2026.08.25 (evening) - Session 40b53762 (Mr. Radio 🦉; crew Krishna 🦚 · Sam 🎙️ · Clayton 😎 · Tiberius 👑 · Rio ⚡) | One INI key broke three instruments, and the third one was the number the plan is decided on

1. **🔴 The paired latency gate was comparing two different events, and it passed by 3.15x while doing it.** `v1_eval_arm.py:373` measures client-send → **observed completion**; v2's span stops at reply-in-hand, which under the queued executor is the **enqueue acknowledgement**. Off tonight's pair that renders a delta of **3152.81 ms against a 999.561 ms bar** — a thunderous PASS computed over a mismatched pair. Under `inline` the two coincided, which is exactly why nobody saw it. **One INI flip at `22c85b5b` (08-21 17:03:30) broke three instruments**: the cache family (ninth pin, fixed), the four degradation rates (tenth pin, closed tonight), and the latency instrument — the plan's **primary** criterion and the only one nobody had named. Every other defect on that run makes a number refuse to appear; this one makes a number appear and be **wrong in v2's favour**.
2. **Two figures withdrawn in writing, and the withdrawal is the point.** Krishna published ~2400 ms of deferred work, corrected it to ~1909 ms, then withdrew both: each subtracts across two runs, two days, two server states, n=60 vs n=20. His own reason is the durable one — *correcting 2400 to 1909 made the METHOD look careful while resting on the same mismatched pair.* The sensitivity figure carries the same trap: **≥3415.3 ms is how much deferred work it would TAKE** to drag the gate to its bar; there is **no defensible estimate of what it IS**. Never quote it bare.
3. **The poll was built, and the run was deliberately not made.** 27 minutes of baseline against a one-hour shutdown buys a dead run that spends the calls and leaves no artifact. Wrapper + timeout state committed green at 100% (`bd8a11bb`, `75d3a02e`); `main()` wiring **deliberately owed** — mocked-listener tests would prove the mock, not the JWT-refresh interaction. Run goes in tomorrow inside 10–1 EDT. The timed-out wait now has **its own name and its own count** and does not kill the pass, and its enqueue span is kept out of both span sets — without that filter the enqueue-vs-completion bias walks straight back in through the timeout path.
4. **Coverage can be credited to a background thread that merely ran during the suite.** Sam measured the same 333 tests reporting **15% or 18%** of `session_bridge.py` depending only on **how long the run takes** — 41 statements credited by elapsed time, deterministic both ways. Shipped a thread-attribution detector (`981fda12`) after **building, measuring and deleting** the first one: the clock-based lever reported CLEAN on a scope it had itself proven dirty, because its own subprocess overhead fell inside the poll interval. A detector that reports clean on a known-dirty scope does not merely fail — it **certifies the problem away**.
5. **A second dirty scope nobody knew about, and the discriminator that sorts it.** `src/tests/unit/commons`: 88 thread-credited lines, split **78 defect / 10 earned**. The ten are earned because a test **starts the thread deliberately and asserts on what it did**. That is the whole rule, and it is why the tool names the thread and refuses to decide. Also proves the census boundary: an AST census of thread **starters** is not a bound on **blast radius**.
6. **Every session was writing the same repo-root `.coverage`, and pytest-cov erases it at startup.** A twenty-minute run and a nine-second one shared one mutable file and the short one won — producing a **96.59% GREEN that was false**, with ~28,000 statements silently out of the denominator. Because the vanished files were the worse-than-average ones, **the mean went UP while nothing improved**. From `dfb53168`, `pytest --cov` without `COVERAGE_FILE` is a hard refusal with the remedy printed.
7. **Rick ruled the cloud venue names: Option A.** The file named for a GPU has no GPU; the GPU-bearing one is named `test` and runs on no host. `gcloud compute instances describe lupin-host-test` returns `e2-standard-8` with **no `guestAccelerators` field**. Q1 then decided by measurement rather than argument: **all five references to `deploy-cloud-test.sh` in `lupin-vm.sh` are comments**, and line 272 says outright *"that is not the script this VM is deployed with"* — so it retires rather than gets repointed. Rio priced my "clean" twice and was right both times; three **line-number citations** go stale on retirement and are the real bill.
8. **Pass A's deliverable killed its own claim.** Clayton's inventory said six builders were pass-through with kwargs in four shapes; walking `swe_team` broke it. **The six was really five**, the table lands with eight shapes, and the finding is worth more than the refactor would have been — which is what the row's escape clause was for.
9. **Three times a worker's reason beat mine, recorded that way.** Rio: `e7aa19bf` was already green and I was budgeting a rebuild. Krishna: v1 can afford to raise because it has **already dumped its artifact**; v2 cannot, because the kill costs the whole run. Tiberius: checked ownership of a manager-gated row rather than assuming it, handed me the park quote instead of acting on my board — and refused a pragma for six uncovered statements, covering them instead, because **a pragma is for an unreachable branch, never for six nobody got to**.

**Checkpoint**: closed with receipts I re-ran myself — `e7aa19bf` (44 passed), `ef22c328` (util at 318 stmts / 114 branches / **100%**, tier 18,764 exit 0), `99c05af9`, `759a895b`, `aa41fa66`. Parked with their own decisive sentences quoted: `d13b2812`, `9d654899`, `117ed1b6`. **On Rick's instruction all non-María rows consolidated onto my board** — seven reassignments, so nothing reads as covered by a seat that will not exist tomorrow. Live constraint carried rather than lost: **`5306ac4c` is still unmerged and no session launches with the memory cap until it lands.**
**Files**: `src/rnd/v0.2.0/2026.08.25-trust-criteria-verdict-paired-run-ts-f06f5961.md` · `2026.08.25-coverage-that-moves-with-the-clock.md` · `2026.08.25-coverage-assumptions-that-died-on-measurement.md` · `2026.08.25-phase-5-pass-b-kwarg-source-table.md` · `2026.08.25-cloud-venue-name-reconciliation-plan.md` · `src/scripts/detect_thread_credited_coverage.py` · `src/tests/unit/test_v2_eval_unobserved_error_rates.py`


### 2026.08.25 (afternoon) - Session 40b53762 (Mr. Radio 🦉; crew Clayton 😎 · Tiffany 💍 · maria 🌸 · maya 🌻) | Rick cleared five blocked rows in one sitting, and the R&D rubric turned out to be asking the wrong question

1. **Five rows had been waiting on Rick; he ruled all five in twenty minutes, and two of the rulings were corrections to me.** Asked whether to bounce `:7999`: *"you don't need my permission to bounce a fucking dev server."* Asked whether to push: *"Don't you ever ask me to push God damn it how many times do I have to tell you? We push when I say so."* **Both were already written down** — CLAUDE.md has made the dev bounce standing authority for every seat since 2026-08-01, and Cheech logged the push ruling on row `f7b57d88` on 08-22 with the explicit note *"I do not raise it again."* I asked anyway, three days later, because a walkthrough of blocked rows makes every blocked item look like a question owed. Written to durable memory so the next seat does not repeat it; row `f7b57d88` amended to strike the false bounce-gate it had carried since 08-21.
2. **Epic Board accordion: merged, and green.** `b0b07bb9`, no conflicts, 2,679 insertions. Its 20-check Playwright suite could never run from the branch — `:8000` bind-mounts the *main* tree's `./src` — so Rick ruled merge-then-test. First run 19/20; the red was **not** a product defect. maya diagnosed it as `epic-board.css:135` capitalizing the title so `inner_text()` returned the transformed string, fixed at `d1950f1c` on `text_content()`, and **proved the test could still fail** by mutating the stub title to a string absent from the DOM. Confirming run `ts-b104a4a2`: **20/20, 0 failures, 80.3s**. My own leading hypothesis (a poisoned one-shot memo) was wrong and she killed it with evidence rather than adopting it.
3. **The worktree sweep executed against a population that had already moved.** The census said 36 of 54 removable. Re-derived from scratch: **only 5 worktrees existed** — the sweep had happened days earlier and the census was stale. Removed the 3 that were merged-and-clean (branch refs survive, each recoverable with `git worktree add`); kept 2 holding one unmerged commit each. Rick ran the `stash@{0}` drop himself after the permission classifier refused me; it reported `e26966bd`, **exactly the sha pinned on `rescue/stash-tiffany-postgres-e26966bd`**, so the entry was verified by its own sha rather than by its position. Stack now empty.
4. **🔴 The R&D rubric was measuring the wrong thing, and Rick caught it, not me.** Two passes archived only 10% then 13% of 231 docs. His pushback — *"many of them dedicated to simply doing nothing at all related to R&D"* — exposed the error: I had workers test **whether each doc's mechanism is still true**, and they answered honestly (mostly yes). *A memento can describe a live mechanism perfectly and still be a work log.* Truth was the wrong axis; **form** is the right one. A third pass on form, plus a clause Tiffany forced — *a task record keeps when it is the only place a number lives* — pulled fourteen rows back from a wrong sweep on one slice alone.
5. **51 documents had never been classified at all.** The 231-row manifest only ever globbed the top level; five subdirectories were invisible to it, including `mementos/`, `cascade-resume-artifacts/` and `demo-day/`. **The real population is 284, not 231.** Two of the uncounted were written *after* the manifest was built, one of them mine about the classification itself.
6. **A P1 instrument defect found in the v2 eval, filed as `2ec6ad9c`.** The warm pass reports `cache_hit_rate 0.0` and a §6a table of zeros. That is not a cache that never hits — it is a harness that cannot see. Every warm response carries `status: "waiting"`, `wrote_snapshot: false`, `answer: null`: the agent path is now **asynchronous**, so the cold pass never writes a snapshot before the harness moves on and the warm pass can never take the replay path (warm route distribution: agent 59, receptionist 40, replay **0**). `is_completed_ok` grades `waiting` as completed work — **re-opening the exact hole row `d8d019f6` closed on 08-20**, one step further along. The 08-21 run measured warm cache-hit **0.4586** with replay firing 83 times on the same harness and corpus, which is what makes this an instrument regression rather than a product one. **Second tool with this blind spot**: Cheech logged the identical premise in the cache-dump tool's `--verify-learn-back` on 08-22. Routing accuracy (0.9667) is still valid — routing is synchronous.
7. **The archive executed: 68 of 284 moved to `src/rnd/v0.2.0/_archive/{D,E,F}/`, 21 citing files rewritten, zero verification failures.** Not deleted — git keeps the bytes either way, so deleting reclaims nothing and only removes the undo. Final split **23.9%**, against 10.8% from the truth-based rubric. The workers kept more than their directory names suggested and were right to: `dm-verbosity-reduction` archived **2 of 29** because it is a real study (experiment designs, two independent reviews, cost-and-breakeven, hand-labelled recall sampling), and the only two that moved were a rotation plan carrying its own *"SUPERSEDED, do not build from this document"* banner and an as-built freeze record whose 494 tests live in the suite. `mementos/` archived 6 of 10 — four held measurements with no other home.
8. **One defect of mine, caught by the mover rather than by me**: all 29 rows in Clayton's manifest lacked their subdirectory prefix, and only the two classed *archive* surfaced it, as "absent from disk". A verification step that reports what it could not find is why this did not become a silent skip of two files.

**Checkpoint**: rows closed — `e04f6ba5` (worktrees + stash), `f2eb0e42` (accordion, 20/20), `d212f54b` (eval run). `d62d2d51` in progress. Bug `2ec6ad9c` filed P1. Epic `f7b57d88` amended twice. **Still Rick's and NOT re-asked**: the push.
**Files**: `src/rnd/v0.2.0/_archive/{D,E,F}/` (68 docs) · `src/rnd/README.md` (v0.2.0 count 231→180, subdirs 5→6) · `io/v020-form-*.tsv` (7 classification manifests, 284 rows) · `io/v2-flow/eval-2026-08-25-16-53-36/`


### 2026.08.22–25 - Catch-up entry, written 2026-08-25 by Mr. Radio 🦉 (solo) | Four days, 273 commits, two OOM kills and a quarantine — the weekend that made rules

⚠️ **Provenance first, because it changes how to read this.** Written on 08-25 to close a four-day gap: `grep -cE '^### 2026\.08\.2[2345] '` returned **0** against **273 commits** and **64 R&D docs**. Most of this is other personas' work and **I was not there for it** — it is reconstructed from `git log` and the docs, not witnessed. Attributions come from commit authorship and doc bylines. Where I could not source a claim I left it out rather than smooth it over. The dated rules below are the reason the gap mattered: **a standing rule whose origin is not in history cannot be dated, scoped, or challenged**, which is how a temporary recovery measure becomes permanent by default.

1. **08-22 — the brain-integration epic reached wip, and the push became Rick's alone.** Integration merged at `9b49f04d` onto `wip-v0.2.0-2026.08.03-present-and-demo`; step 13 ran on Rick's GO at 12:05 (both DBs wiped behind `pg_dump` backups, verify-empty pass, 9a/9b proved against the database directly). **Rick ruled the push, 12:10, genuine keypress**: *"We will push when I say that we are ready to push Understood?"* — recorded so no successor re-litigates it. Origin was **230 commits behind** at that point. Alongside it: the Q&A-card lane ran phases 1–4 (Tiberius 👑 implementing, Clayton 😎 reviewing across ten review docs), `display_name` landed as its own field distinct from the spoken label, and the receptionist conflation was measured as **live on voice** — the router is trained on 174 utterances to pick it deliberately, yet chosen and degraded were identical on every field.
2. **08-22's real finding was a shape, not a bug: four checks that could not fail.** A negative control that never read the registry; a gate whose discriminator did not exist in the data; a gate that falsified backwards, so following it literally would have *un-derived* the field it guarded; and a dropdown gate asserting against mode keys that were never commands. Each was caught by someone re-deriving a green rather than trusting it. This is the same shape that runs through the whole weekend — **a green check means clean OR blind, and only a mutation tells you which.**
3. **08-23 — the second OOM, and the quarantine that followed.** Two incidents in two days (08-22 kernel oom-kill; 08-23 systemd-oomd ×2) took roughly two dozen sessions. 🔴 **STANDING RULE, dated here: the JS/TypeScript test tier is QUARANTINED** — `npm test`, `node --test`, `*.test.ts`, the multiplexer. 🔴 **STANDING RULE: a per-session memory ceiling on `cctmx`**, so one runaway no longer takes the host. 🔴 **STANDING RULE: never pass `"all"` to the suite runner — name the components you want**, because `_expand_all()` substitutes every component including the tier that was killing seats. The ban originally named a *command* when it needed to name a *surface*; that correction is itself in the record. ✅ **LIFTED 2026-08-25** (row `92e94cb7`): the allocator was named — a *failing assertion holding a DOM node*, not the tier — the capped `jstest.slice` lane was built, and Rick ruled *"capped lane, then lift the tier ban."* All four doors now reach the cap, `test_types: ["all"]` is safe again, and the tier is a required merge gate. The `cctmx` ceiling rule stands. **The entry above is left as written — it is what was true on 08-23.**
4. **08-23 also ran a vacuity sweep, and it kept finding the same thing.** Nine assertions in `lupin_smoke` that could not fail; six more plus a detector that overcounted them 8×; twenty grep-shaped tests replaced with ones that drive the code instead of reading it; ten filtering smoke tests that had **never executed** now green; a websocket test passing on a timeout; blanket skips removed that were each false for at least one test beneath them — one of which was hiding a real dispatcher bug. Plus a `TIMESTAMPTZ` correctness pass: naive `utcnow()` written into timezone-aware columns, five ISO emitters fixed emitter-side only, and a consumer audit demanded *before* any further swap.
5. **08-24 — the burn-down, and the OOM root cause named at last.** The allocator was identified as `node:assert` deep-inspecting the actual value to build a failure diff: a happy-dom element walks `ownerDocument → defaultView →` the whole `Window` graph. It needs **three conditions at once** — happy-dom registered × a DOM node as operand × an assertion that **fails** — which is exactly why every green run looked innocent. Follow-on measurements corrected each other in public: the kill was never broken (the test leaks ~2 GB *every time it correctly fails*); "no survivors" is satisfiable by killing everything including the killer; CPU seconds cannot bound wall seconds; the suite's memory is per-worker, not per-file (117 files measured); and one file at 452s owns the suite's wall clock — those were never hangs.
6. **08-24's other landings**: the venue-idle check learned to read the lane that was invisible (row `e6b8fe56` — `monopolize_id` answers *which job holds the slot*, never *is this venue free*); `get_required()` so a missing config key names itself instead of surfacing as "debugging failed"; the stash guard hardened after **21 of 22** spellings of `git` walked past it, and eight documented bypasses; the DOM-assert ratchet burned **276 → 16 → 0**; **sixty integration tests found able to disappear without failing anything**; and the branch/worktree purge — **95→16 branches, 51→1 worktrees, 8.4 GB reclaimed, 34 salvage tags** so nothing swept was lost.
7. **08-25 — the final `:8000` gate, and it is green.** E2E **692 / 9F / 8E / 2S** with **0 regressions** against the sanctioned baseline; integration **448 / 1F / 0E / 32S**, the single failure self-declared *"INSTRUMENT … not evidence of a product defect"* (exit 2 = unobserved stages). Gate sha `7c4964dd`, **362 commits** since the last gated head `888754f1`. The five integration modules that can vanish whole all executed — checked by name, not off the summary line. One rig defect fixed on the way: `.claude/worktrees` was `root:root` while the container runs as uid 1001, which is a **sixth entry for the rig's ignored-paths list and the first that is a permission rather than a missing file**. Full receipts: `src/rnd/v0.2.0/2026.08.25-final-8000-gate-receipts.md`.

**Checkpoint**: rows `e04f6ba5` (worktree sweep half closed by fact — 6 worktrees remain, all three ancient forks salvaged as tags; Tiffany's `stash@{0}` ruled DROP by Rick 08-25) and `08fe2977` (this entry) both discharged. Epic `f7b57d88` amended with the gate receipt (event 9236). **Still Rick's and not re-asked**: the `:7999` bounce that makes the 410 tombstones live, and the push to origin.
**Files**: `src/rnd/v0.2.0/2026.08.22-oom-incident-what-we-know.md` · `2026.08.23-typescript-test-runner-oom-hazard.md` · `2026.08.24-oom-allocator-named-happy-dom-assertion-diff.md` · `2026.08.24-ts-oom-addendum-kernel-dump-evidence.md` · `2026.08.24-typescript-suite-memory-measured.md` · `2026.08.23-stray-worktree-census-and-sweep-plan.md` · `2026.08.24-branch-and-worktree-purge-manifest.md` · `2026.08.24-monopolize-as-idleness-caller-audit.md` · `2026.08.24-stash-guard-prefix-position-bypasses.md` · `2026.08.25-final-8000-gate-receipts.md`

### 2026.08.21 - Session 5c04b97c (Cheech 🌿, nine re-spins; crew Krishna 🦚 · Rachel 🕊️ · Pocholo 📣 · maya 🌻; Mr Radio's crew landed B/C/D) | Brain integration built end to end: /api/v2 ask|submit, **twelve** doors retired, three tiers green at `8657cfa9` *(corrected 2026-08-25 by Krishna 🦚: this read "ten" and disagreed with the entry's own cited sha — `_retired_doors.py` at `8657cfa9` already carried **12** paths. Not a record that went stale; a count that was wrong against its own receipt. Re-measured live the same day: 12 in the table, 12/12 answering 410 on :7999, each naming its replacement and the 2026-12-31 removal date, all without a token.)*

1. **The whole plan shipped on `wt-brain-integration-10ef4b64`** (code head `8657cfa9`, tip `7c00c787`, pushed): steps 1–9b of `2026.08.20-brain-integration-cascade-review-plan.md`, all ten retired doors (each: tombstone + 410 naming `/api/v2/{ask|submit}` + callers moved + accordion card deleted per Rick's 20:44 ruling; Re-render button stays), door 8 (voice → ask flow, no 410), step 12 lifespan wiring, 7a20a09d class delete (`FileBasedSolutionManager` gone, −120 cosa tests). Every commit reviewer-PASS (Pocholo 71+ verdicts; Sam for Mr Radio's crew).
2. **Gates**: `:8000` ts-6eaebff8 @`888754f1` = no code regression (integration 424/3F explained, E2E diff vs baseline = 2 flapping visuals); final tiers @`8657cfa9` unit 16,876/0 · cosa 8,622/0 · guard 2/0 (quiet box), independently reproduced by María. Everything after `888754f1` rides the next `:8000` gate before merge-to-wip.
3. **Confirmed + fixed in flight**: on `:8000` the test-suite job IS the monopolizer, so a queued v2 job's drain is unobservable inside any gate run (ce29cd20 → strict xfail + unit with a fake consumer; ff4166d9 teardown deletes own jobs); write-back stored no code (38815328 → 94e03bc9); monopolize-lineage checker armed on a tombstone *comment* (now reads code; message/cancel/resume-from-checkpoint pinned `NOT_A_DOOR`); the 12 `test_bounce_dev_server_*` reds were fixtures, not the script (Mr Radio 14e6b591).
4. **My own defects, on the record**: a "pre-stage the rig" instruction without "never recreate under a live job" killed Tiffany's lane-2 baseline at 19:07 (rule folded); four asks to Rick on the wip merge timed out (20:52–21:29) → decision `83fe98aa` blocked, chase 10:00 EDT — bounce + push his word.
5. **Fleet-shaped findings for tomorrow's rows (no new bugs tonight, Rick 20:47)**: `self_respin` fired `/clear` but the wake prompt never arrived (Pocholo, 20 min, hand-typed via tmux); a successor rehydrated from a stale `~/.claude/mementos` copy (Krishna); order-effect unit reds that pass alone (spoken_char_cap SystemExit, presentation dry-run banner, lancedb closure, arbiter-loop `test_runner_swallows_job_error` — root cause unstubbed `hold_roots_fn`/`live_session_ids_fn`); a fresh worktree needs its gitignored state (keys, terraform cache, cloud-run.env) or 11 tests read as code reds; three concurrent unit tiers on one box contend (free-port + tree-sweep tests).

**Checkpoint**: epic `f7b57d88` (12 amendments) + `io/mementos/cheech-5c04b97c.md`; rows closed today ce29cd20 · ff4166d9 · 25b14d59 · deee5468 · ef62749e · 7a20a09d · b7fe8941 · 38815328 · c84e9313 · 44d91bf5 · 6791ce47; open: 83fe98aa (rick), 0d4bc1ff (rick, probe patch still ARMED uncommitted on main), 1e597a65 (step 13, my GO after the wip merge), a40e462c (Pocholo's sweep → rows). All four crew reaped with verified mementos; `wip-v0.2.0` pushed (123 commits) + `wt-brain-integration-10ef4b64` pushed.
**Files**: `src/rnd/v0.2.0/2026.08.20-brain-integration-cascade-review-plan.md` (11 folds today) · `src/rnd/v0.2.0/2026.08.21-claude-code-doors-external-callers.md` · `src/scripts/dump_solution_cache.py` · `src/cosa/rest/v2/*` · `src/cosa/rest/routers/v2_ask.py` · `src/cosa/rest/_retired_doors.py` · `io/brain-integration-gate/*` · `io/mementos/{cheech-5c04b97c,krishna,rachel-425a6c1a,pocholo-ef84b45b,maya-a19a8d2a}.md`

### 2026.08.21 - Session e9a672a2 (Mr. Radio 🦉, six re-spins; crew Clayton 😎 · Rio ⚡ · Tiberius 👑 · John 🏄🏽 · Sam 🎙️ · Chloé 🗼) | Rick's ☕ — four branches landed, board to zero, receipts not claims

1. **A router-label-fix** merged no-ff into wip `decae6c7` (+doc count fix `c6609829`): ruled **0 corpus lines move** — arithmetic already delegates to MathAgent (`agent.py:175-178`); deliverable is the rule doc + `router_label_audit.py` + red-on-plant lint test, corpus byte-identical. Sam PASS ×2, María verified, Chloé 16,460/0 + 8,801/0.
2. **B claude-code v2 submit** (`14172091`) landed in integration at `1c319db0`, rebased twice as Cheech's head moved; **C expeditor per-call state** (`10c60712`) at `24c3b3ba`; **D xfail-blanket strip** (`82fb9fcb`) at `af9f557d`. Each: Sam PASS re-derived on the rebased base, Chloé tiers green, Cheech fast-forwarded on my relay of word + sha.
3. **D found a live UI defect**: `handleDoneQueueUpdate` read `data.done_jobs`, which the API stopped sending — Replay on every finished job was dead ("Job metadata not found"); fixed red→green (JS 315/315). Sam's correction stands on the row: five of the twelve xfail'd tests were failing-rendered-as-skipped, not "twelve silently xpassing."
4. **Two of my own defects, corrected on the record**: `git merge-tree --write-tree` does not exist in this git — my "both conflict" ruling was the command failing (B merged clean); and I tore down Rio's worktree mid-tier, making his log unreadable (Cheech: maya's full unit at `1c319db0` is the count of record).
5. **Seat deaths**: Chloé's first seat died ~21:22 (replacement inherited her worktrees); I self-re-spun at 52% (#5) and again (#6) via the wake-proof path. All six seats reaped with mementos (`io/mementos/`), all crew worktrees/branches removed.

**Checkpoint**: rows closed 0795b5d8 · 14172091 · 10c60712 · 82fb9fcb · 25ff8360; my own 1e597a65 (Cheech's GO) and cdfedc41 (Cheech 10:00 EDT, María PASS on 1447261b) blocked with chases; Rick's what-next ask timed out → hold until morning; NOT pushed (María/Cheech own tonight's push). Follow-ups, no rows per Rick's ban: FastAPI docs regen after bounce · lupin-mobile submit → 410 · `NotificationsHeaderRenderer.ts` TS coverage gap (pre-existing, Chloé's c8 baseline) · Rachel's monopolize-lineage red + two podcast dry-run reds at the head.
**Files**: `src/rnd/v0.2.0/_archive/D/2026.08.21-mr-radio-cascade-resume-memento.md` · `src/rnd/v0.2.0/2026.08.21-router-label-fix.md` · `src/scripts/router_label_audit.py` · `src/tests/unit/test_router_label_audit.py` · `src/tests/unit/test_router_corpus_consistency.py` · `io/mementos/{john,clayton,rio,sam,chloe-3f3ad005,tiberius}.md`

