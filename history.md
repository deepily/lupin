# Lupin Project History

> **Archives**: See [history/README.md](history/README.md) for the full chronological index. Most recent: [2026-08-21 to 08-29](history/2026-08-21-to-29-history.md).
>
> **Last archived**: 2026-09-06 by Sam 🎙️ (session 07554529) — one cut (08-21 to 08-29), 8 entries, 39,857 chars moved verbatim; character accounting balanced (43,128 kept + 39,857 cut = 82,987 before). Cut by DATE, and the positional cut was VERIFIED to coincide with it in both directions rather than assumed — this time they did. Prior: 2026-08-30 by Mr. Radio 🦉 (session 93a8751c) — one cut (08-14 to 08-20), seven entries, 25,267 chars moved verbatim; character accounting balanced (46,822 kept + 25,267 cut = 72,089 before). Cut by DATE, not position — the 08-19 entry sits at the BOTTOM of the file below 08-14, so a tail cut would have archived a newer entry than it kept. Prior: 2026-08-25 by Krishna 🦚 (one cut, 08-10 to 08-12). Prior: 2026-08-21 by Cheech 🌿 (one cut, 08-04 to 08-07).
>
> ⚠️ **This line records PROVENANCE, not health — do not read it as a status.** It says when the file was last archived, not how big it is now. The previous header asserted *"✅ HEALTHY at ~7.9k tokens (31% of 25k)"*; six days later the file was **21.0k — past the 19k CRITICAL threshold** — and the header still said 31%, because nothing re-derives a stamp. **On 2026-07-27 that stale figure was quoted into a commit message as a health claim in the same command that measured the real number.** Same defect as a calibration stamp standing in for its instrument.
>
> **Measure it, never quote this line**: `python3 -c "import io;n=len(io.open('history.md',encoding='utf-8').read());print(f'{n/4/1000:.1f}k tokens')"` · thresholds **17k WARNING · 19k CRITICAL · 25k limit**.

### 2026.09.05 (night) - Session e97796db (Mr. Radio 🦉, manager, rehydrated after /clear; crew Rio ⚡ · Krishna 🦚 · Tiffany 💍; adjacent María 🌸 · Tiberius 👑) | Three workers refused something that would have been easier to assert, and the hand-check caught a memento five hours stale

**Landed on the working branch**: `6a078e12` (ActionRequiredStore.respond() deleted — it emitted "responded" BEFORE its POST and swallowed rejections in a bare catch, and a named test ENDORSED that) + `5f8c42ea` (María's follow-on 1, the public-surface census guard). Commit-scoped cherry-picks, dry-run in a throwaway worktree first, 41/41 post-land. **Nothing pushed** — Rick's 16:47 keypress stands.

**The merge I withdrew**: I checked *green AND reviewed* at the COMMIT level and asked Rio for a BRANCH action. He measured it would land EIGHT commits on a one-diff review, six not his. Six unreviewed commits would then have read as reviewed to everyone downstream. His counter — *make the action match the gate's granularity* — is what shipped.

**María's review** (non-author, read the diff): APPROVED with two follow-ons, after she withdrew her own first verdict because this row already held the refutation. Her durable line: *"They are NOT one check counted twice. They are two independent checks over a population that ONE filter defines. THE REDUNDANCY IS IN THE DENOMINATOR, NOT THE TEST."* Behaviour axis separated (M2); discovery axis blind together on `/^respond/i` (M3).

**Krishna**: proved the `memento_io` fix GUARDED, not the third state — green baseline 12/12, revert `b8b063c`'s one line gives 3 failed with `assert 11 == 0`, the exact exit code that fix's own message names, 9 of 12 staying green so it discriminates; anchor matched 2× and he narrowed it by reading the diff hunk rather than guessing. Re-framed his four green arms as a RECEIPT for `b8b063c` rather than a refutation of the reporter.

**Tiffany**: pre-registered her kill condition BEFORE running the arm and it survived — arm D reproduces the baseline byte-for-byte. Reported it as "proven sufficient", not as the cause. Also split her finding from María's rather than riding it: hers is a POPULATION limit (the field strips `??` rows), María's is a SPAN limit (point samples cannot certify an interval) — two mechanisms, one conclusion, and folding them would have lost the corroboration.

**The E2E hook is still unexecuted.** María's tier died in 0.81s: a safety fixture at `conftest.py:513` refused a destructive reset because the run resolved to `lupin_db_dev` where `lupin_db_test` was required. The fixture worked; **the harness returned EXIT 0 over three errors and zero tests**. Worse than the TypeScript non-run, which at least titled its own report NOT EXECUTED.

**Six commits on `krishna-seven-onto-live` are green, enumerated, UNREVIEWED and unmerged** — manifest at `9785b12d`, whose TITLE carries UNREVIEWED because a thorough manifest reads like a completed review from the outside. Krishna left the reviewer row honestly empty when filling it was available and nobody would have checked.

**My errors, corrected by workers not by me**: three `task_transition` ReadTimeouts I published as "0-for-3, none landed" — one HAD landed, stamped 01:57:37, earlier than two reads at 01:58:11 and 01:58:55 that did not see it (`updated_ts` is a TRANSACTION START stamp, so an early read is indistinguishable from a failed write). Rio's formulation stands: **a ReadTimeout is a lost RESPONSE, not a failed WRITE.** I also told Rick twice the dirty `lupin-app.ini` was his edit; it is `cc session fleet size cap 9 → 8` on a line the fleet-size slider rewrites in place, and neither I nor Rio had measured it.

**Reaps**: Rio, Tiffany, Krishna — all `verified`. Krishna's first check found NO record at his io slot and a **five-hour-old pointer from a prior seat**; a reap on his word would have returned `prior_holder_present` and completed, losing his evening. His diagnosis: he had written a ROOT-slot record and believed that covered him. The reap reads IO.

**Board hygiene**: re-pointed a row blocked on a persona I had just reaped, and returned a P1 from `in_progress` to `queued` — an `in_progress` row with no live worker misreports in the flattering direction.

### 2026.09.04 (night) - Session 21dff055 (Mr. Radio 🦉, manager, rehydrated after /clear; crew Rachel 🕊️ · Tiberius 👑 · Cheech 🌿 · pocholo 📣; adjacent María 🌸 · Tiffany 💍) | Wrap on Rick's broadcast — and a memento defect four corpus sweeps could not settle and two greps did

**Rick's two asks discharged**: pushed `30a2644a..9e4538d2`, backup executed (333 files, 9.17 G, sync complete). Crew of four coordinated, stood down, mementos verified **by header `session_id`** rather than by placement.

1. **Merged pocholo's two branches, tested before landing.** `931a4e14` memento-slot parity closes a *coincidence* the way CLAUDE.md says one must be — applying the rule to BOTH slots at once rather than repointing one side (root resolves a linked worktree to itself, io collapses it to the main checkout, from one definition). Receipt: 73 passed across the three memento test files. Plus one R&D doc, 222 insertions.
2. **DECLINED a green merge, deliberately.** `wt-tiberius-nonce-prestamp` passes in his tree and edits `self_respin_core.py` + `cosa_voice_mcp.py` — the live re-spin path — while seats were re-spinning through it. Left intact, reason recorded in `.claude-session.md`. **Not merging to clear a list is the same discipline as not dropping to clear one.**
3. **Unit tier at `9e4538d2`**, tracked-dirty=0, run-span=unmoved: **22,295 passed · 2 failed** — both the documented `test_secret_scan.py` rotation holds. Zero new failures from the merges.
4. **`50ac8ed0` closed by its author's own caveat.** Rachel wrote *"if his branch already retires the key in a later commit, this row is already discharged and should be dropped with that sha named."* Named: `a5bab186`, in the NARROW form she specified — `dev-dev` retired, `cloud-gpu` survives. She then caught the half I fixed **without telling her**: her remedy followed literally left a dangling `"see above"` pointing at prose deleted in the same edit. *A retraction must reach the artifact AND the person carrying the instruction forward; I did one.*
5. **A row dropped in error is terminal, so it was reinstated rather than mourned.** Tiffany dropped `3658ec66` acting on a **DM summary while `task_get` had handed her the full ruling seconds earlier** — the condenser drops a negation first, and my ruling's whole content was *"neither of your two words."* Her diagnosis, sharper than the outcome, is in replacement row `0b11bd5a`: **act on the row, not on the summary of the row.** My share: I sent a three-way ruling as three paragraphs of prose on a channel I know condenses.
6. **The memento line-2 defect — four seats, five reversals, and the method is the finding.** The generator freezes the human-readable `**Written**:` line at first write while the header tracks each amend. Established by Tiberius 👑 (observation, then the 59/99 census with 20 unparseable named as a **third state**), Cheech 🌿 (mechanism, then its retraction, then the weakening of his own retraction), and closed when **both read `memento_io.py:1065-1069` independently, within a minute, landing on the same lines**. Under the bug: `restamp_header_written_at` exists **because** a stale header once made a good record read as missing to the reap (`69c3829d`) — the machine-facing field got a careful fix and the human-facing twin two lines below was never considered.
7. **Cheech's keeper, and it outranks the bug**: *"when the question is* what does this program do*, the program is the primary source and the artifacts are hearsay. The method is not the finding — it is the cost."* Four sweeps over 921 files, one hour; **every result compatible with both mechanisms**, because a corpus cannot separate *frozen* from *regenerated to a matching value*. Worse than slow — the sweeps **manufactured** claims that had to be retracted.
8. **Mine, unsoftened: four relay errors, one shape.** Welded Cheech's mechanism into "universal by construction" for Tiberius · relayed Cheech's refutation as settled without checking it against Tiberius's population · speculated María's alarm onto Tiberius's unparseable bucket (she refuted it: hers is a **path** defect, mine was **content**) · told her to check her **sha** when the reap runs in the **MCP server process**. **Every underlying measurement was somebody else's and sound; every join was mine and wrong.** Tiberius's correction is the keeper: **attribution and a scope caveat do not make a lead safe on a durable surface** — a caveat protects the reader of the conversation, only omission protects the reader of the artifact.
9. **Five receipts in one evening for one meta-point: a written rule is not a control.** *"A hit is not a use — read the call sites"* while running four corpus sweeps; stdio-staleness while I aimed a merged fix at a tree. Not missing, not unread, **quoted between us the same evening**.
10. **`931a4e14` is merged and UNREACHED** — measured on two seats (María's process table; Cheech's boot at 20:44:04). The reap runs in the MCP server process and no live subprocess has loaded it. *Fixed on origin* and *fixed in the process that will run tonight* are different facts.

**Checkpoint**: board driven to terminal or bounded-deferred, nothing dropped to look tidy — `50ac8ed0` done with receipts; `96cf5cec` (P2, accepted from María) and `0b11bd5a` (P3) **parked with their own decisive sentences quoted**, chases 09-05 and 09-11; `31142a62` **blocked on Rick** — his ask fired at 22:55 and **timed out**, and **no default was passed on purpose** so nothing could be recorded as his keypress, which is the exact defect that row's own epic exists to close. Hold written via the verb and **honored** through the hook's own reader. **Still owed by Rick**: the `approval_source` decision.
**Files**: `.claude-session.md` · `TODO.md` · `history.md` · (merged, not authored here) `src/lupin_mcp/memento_repo_root.py` · `src/lupin_mcp/memento_slot.py` · `src/tests/unit/test_memento_slot.py` · `src/tests/unit/test_the_slot_decides_which_tree_a_memento_lands_in.py` · `src/tests/unit/test_the_memento_readers_resolve_the_writers_tree.py` · `src/rnd/v0.2.1/2026.09.04-census-seam-reds-orphaning-not-backgrounding.md`

### 2026.09.03 (evening) - Session 2424de1c (Mr. Radio 🦉, manager, self-respun; crew Tiberius 👑 · Rachel 🕊️; adjacent María 🌸) | The post-game's movement, and eleven corrections — five of them people catching their own work

**The deliverable was §5 movement, and the durable output is a correction record.** Every finding below survived a cross-examination that shrank it, and the sections that graduated are smaller than their first drafts.

1. **§5 movement discharged** for `io/post-games/2026.09.03-seat-and-repo-resolution-post-game.md`. Rulings D1–D6 into TODO.md's Decisions Log (`3a30cd41`): Rick's seat-repo ruling with its three shas and why the octopus was deliberate; **"when you say HELD, name what is holding it"**; the coinciding-derivations family shipped **as a QUESTION** because Tiberius refused the weld and I withdrew it; and the `self_respin`-is-per-seat retraction that made an operator gate void.
2. **The store REFUSED six findings** — ticket ratio 0.51 against a gate opening below 0.50, **and the P0 exemption was not used** (declined five times that night by four people). They are in TODO.md under *HELD OUT OF THE STORE*, **H1–H6, each with a named owner** — because this same run's finding is that a finding filed as a state with no owner reads as closed.
3. **A candidate doctrine doc, deliberately not doctrine**: `planning-is-prompting → workflow/the-cheap-check-gets-crowded-out.md`, **v0.2**, three findings each usable alone, with the unifying claim filed as an open question. `verify-the-subject.md` graduated on Rick's explicit word off a comparable one-evening base; **this has not been put to him and may not be cited until he rules.**
4. **Five new CLAUDE.md sections, every one cross-examined**: hand-written fixtures are systematically **better-formed than reality**; a malformed artifact does not announce **which of its content went unread**; retiring a gate does not retire **the thing it gated**; a spawn brief is **the one document a seat cannot notice being wrong**; and a reap reports `prior_holder_present` **and proceeds**.
5. **The sharpest receipt of the night is three-deep and none of it is mine.** A post malformed itself **while warning about malformation**; the correction of it described what was *displaced* rather than what was *present* and was wrong; the report about that correction got the content right and **invented the mechanism**. Tiberius struck my "three layers" framing — the malformed post is a *production* defect, not a claim-construction one, admitted only because it was adjacent in time, **which is the weld I had already been refused on that same evening.** What survives is **two instances, two authors**, with each author's *substitute* named: his came from **his plan for the artifact**, hers from **a plausible explanation** — different substitutes, different guards.
6. **My own, unsoftened.** I handed Rachel a mapping of "Tiberius's three" that was **a different triple** and built an accusation on it against a position nobody took. I collapsed a dropped **gate** into an absent **target**, erasing that his restraint was a real choice. I put a spurious member in a set for the same reason twice in one hour. And I **took all the blame for the brief one message after writing the table that assigns him half** — Tiberius: *a ranking that disfavours you is still a ranking*, and self-disfavouring feels safe precisely because nobody objects to it.
7. **The brief was mine and it cost five hours.** My spawn brief said `self_respin` is down **fleet-wide**, measured on exactly one seat — mine, whose MCP subprocess was five hours stale. cosa-voice is registered **stdio**, so staleness is per-subprocess. ⇒ **Three outcomes measured that night and they must not be collapsed**: `SCHEDULED` (Rachel, 22:15, marker on disk), **DECLINED AT THE HUMAN GATE** (Tiberius, 23:00 — the verb ran and Rick said no), and the stale-module failure on my seat alone. *A retraction must reach every copy, and a spawn brief is a copy.*
8. **María's finding fired on me live, an hour after I filed it.** Her reap of a worker 65 seconds after asking for a memento hit `prior_holder_present` and cost her his hour. **My own reap of Rachel hit the same verdict tonight** — and I had already recorded two of my earlier reaps returning it as *a fact about those seats* rather than a defect. Her mechanism is the keeper: **a silent failure teaches you to look; a failure that explains itself in a field you were not reading teaches you nothing at all.** Rachel's memento was recovered from the root slot into `io/mementos/rachel-1c8db7e3.md`, annotated as manager-recovered.

**Checkpoint**: board empty; `720583da` ruled + reframed (events `10925`/`10926`) and blocked on Pocholo, chase 09-05 11:00 EDT; `9d654899` · `11253df9` · `864e2c28` · `7ad5eba6` closed with receipts; `69f3b917` dropped as void. Crew harvested — Tiberius's memento verified-written, Rachel's recovered. **Written off explicitly, mine**: Rio's and Krishna's mementos were lost to earlier `prior_holder_present` reaps; their code is on the branch, the session narrative is gone, no reconstruction. **Still owed by Rick**: whether the candidate doc graduates, and which way to fix the `CLAUDE.md` auto-include (H4).
**Files**: `TODO.md` · `CLAUDE.md` · `io/post-games/2026.09.03-seat-and-repo-resolution-post-game.md` · planning-is-prompting `workflow/the-cheap-check-gets-crowded-out.md` + `workflow/session-checkpoint.md` · `io/mementos/{tiberius,rachel}.md`

### 2026.09.01 (late) - Session 0e61abe3 (Mr. Radio 🦉, manager; rehydrated after /clear) | Four wrong numbers in one evening, and only one of them was caught by a person

**Managed four seats through end-of-session; the durable output is a defect class, not a feature.** Every finding below is the same shape — *the failure and the success print the same thing* — and it fired four times in ninety minutes across three people.

1. **I sent Rick at a remediation that was already done** (`68106100`). I told him twice to run `./backup.sh --write` on `projects/lupin-mobile` to restore a wiped mirror, quoting "0 deletions, 788 creates" from an amendment stamped 21:28. A `--write` had landed at ~21:42. I repeated a figure without re-running the command that produced it; the live dry run says **0 created / 0 deleted / 2 transferred**.
2. **And my confirming arithmetic would have BACKED THE ERROR UP.** I sized the gap by subtracting the **six** excluded directories the row happens to name from a 22,789-file tree — the exclude file carries **41** entries. That manufactures a ~15,000-file phantom loss. Krishna 🦚 built the identical six-directory filter independently and got 16,820 vs 577, one message from filing it. **rsync's own account, having applied all 41 rules, beat both hand-rolled counts.**
3. **⇒ Correlated error is not corroboration, and Krishna's is the cut to keep.** I first wrote this up as "we caught each other — the crew working." False, and the flattering reading: neither of us found it by reading the other's work. **Two people using the same method are one measurement taken twice**, and it *feels* like validation precisely because it arrived separately — separateness being the thing people check for. Only a different INSTRUMENT validates. ⚠️ And note the direction: both wrong numbers **agreed** with the row's existing five-figure story. A wrong number that contradicts a record gets challenged; one that confirms it gets absorbed.
4. **Neither memento slot is a superset of the other** (`ba4c4d3b`). I told Krishna a ~6k gap between his root and io records was "expected, not drift — the nonce alone would differ." Wrong, and wrong in the direction that tells a peer to **stop looking**: root held 2 entries / 199 lines, io held 3 / 322, and io carried a whole evening root never had. **The slots ACCUMULATE SEPARATELY**, so the verb picks what a successor knows — `self_respin` reads root, `dismiss_sessions` reads io. He then measured it fleet-wide: 18 keys in both slots, **root larger in 7 of 18**, so the divergence is universal rather than benign. Every worker was sent the measured reason with the both-slots instruction.
5. **A fabricated endpoint, laundered by arithmetic — volunteered against himself by Krishna.** He revised a gap from 37s to 45s; I refused it because 45 reconciles with no stamp pair (every pairwise gap among the four amendments is 36.3 / 57.0 / 20.7). **My refusal was right and my diagnosis was charitable and wrong**: it was not a mis-measurement. He had no stamp for one endpoint, typed `03:41:00.000000` into his script as a placeholder, and it printed **`45.2s`** beside the genuine `97.2` and `57.5` — identical format, nothing marking one input as invented. ⇒ **A placeholder is indistinguishable from a real value once it has been through arithmetic**; the subtraction launders it and six significant figures finish the job. The rule is not *avoid placeholders* — a computation over an invented input must say so in its output or refuse. His fix is the structural one: **pass identifiers, not values**, so a missing id is a lookup failure that stops the run.
6. **Three UI decisions owed by Rick, recorded rather than re-fired** (`bc77cd79` → blocked on `user:rick`, chase 09-02 10:30 EDT). Two were asked and timed out twice; the third — María's colourblind glyph — was **never asked**, which looks identical on a board and behaves oppositely. ⚠️ **I fired the second ask on a presence nobody observed**: a `notify` refusal reading *"a user-initiated voice message takes precedence"* is triggered by inbound peer DMs, not by the user. Seen four times.

**Checkpoint**: no source commits of my own — manager seat. **Push done** (Rick's word, broadcast `68fdff5b`): `7d17fb1e..983854a9`, 120 commits, branch level with origin, verified 0/0 after. **Backup done**: dry-run first (183 deletions, all directories, zero regular files), then `--write` — **211 created, 0 deleted, 294 files, 8.59 GB, exit 0**. Row `68106100` closed by Krishna with receipts; `ba4c4d3b` carries five amendments and two open items neither of us touched (does the writer offer to sync both slots; is the divergence normal across personas). No rows created — ticket moratorium. `src/conf/epic-stories.json` dirty since before this session and **not mine** — independently confirmed by Krishna, Tiberius and Rio.
**Files**: `history.md` (this entry + Tiberius's, which was sitting in a scratchpad and would have died with his seat)

### Tiberius 👑 (session `d203e90a`) — the guard-remedy defect class, closed and pinned

**A guard's refusal is tested; its remedy was executable advice nothing checked.** Two hook
guards shipped a message recommending the very hazard the guard exists to prevent (Tiberius
`30ba7976`/`92445956`, Rio `9350649d`). Rio built the shared predicates; Mr Radio ruled one
test serving both guards beats two. It now serves all nine guard messages.

1. **`src/tests/unit/test_guard_remedies_are_checked.py`** (15 tests) wires
   `tests/helpers/guard_remedy.py` to every `_deny_reason_for` / `_notice_for` in `hooks/`.
   Each guard gets the predicate matching what its message CLAIMS — `remedy_carries_its_caveat`
   where it claims safety, `remedy_is_readable` where it claims reviewability — plus negative
   controls, because an empty result is the same output whether the check worked or looked at
   nothing.
2. **The coverage frontier is pinned as a map**, 2 checkable / 7 vacuous. A `[]` from the caveat
   check means either "every hazard qualified" or "no hazard was named"; only the map separates
   them. A message crossing that line in either direction fails and says what to do.
3. **My own map was the defect it exists to catch** — it claimed to BE the frontier with 4 of 9
   entries, missing `commit_scope_guard`'s actual deny in all three scope shapes. Census
   command now lives in the file (`f71ed629`).
4. **Two CLAUDE.md sections, both measured**: a wrapper's exit code is its LAST command's, so
   the `echo "EXIT=$?"` added to REPORT it is what destroys it — `rc=$?` then `exit $rc`
   (`0768a8b2`); and **a hit is not a use**, the mirror of the empty-result rule, after the same
   mention-vs-read error fired three times in one evening across three people (`ba657856`).
5. **One retraction, mine**: I reported `run-span=` as blank and called it a silent failure in
   Pocholo's tool. My `grep -o '…[a-z-]*'` had truncated the sha at its first digit. His tool
   was always correct; the asymmetry became his `1136e0a7` and his write-up
   `src/rnd/v0.2.1/2026.09.02-a-narrow-character-class-truncates-where-it-should-refuse.md`.

**Checkpoint**: 7 commits — `985076a7` `19259d43` `db7af80e` `2393fdce` `f71ed629` `0768a8b2`
`ba657856`, each staged by explicit path and `git show --stat`-checked. Unit tier at
`ec0bf947`: 2 failed / 21,666 passed, both the held `test_secret_scan` reds. No rows created
(ticket moratorium); two peer-owned follow-ups named in my memento, not filed.
**Files**: `src/tests/unit/test_guard_remedies_are_checked.py` · `CLAUDE.md`

### 2026.09.01 (late) - Session b45d54db (Rio ⚡ re-spun into the same seat, worker for Mr. Radio 🦉; spec Tiffany 💍) | A confirmed cached row is served — proven by the arm that shows an unconfirmed one is not

1. **Row `734bd1bf` (AC-G3) closed after two days blocked, and the observable was reachable the whole time.** The AC wants `path="replay"` / `route_reason="exact_hit"` off `/api/v2/ask`; both fields have always been in the §8 response body. Rick's 08-30 probe was run through the web Q&A UI, which does not surface them — so *"the guard refused it"* and *"it was served and lost in delivery"* stayed indistinguishable, and the row accumulated two server-side blockers that were never the problem. New file `test_v2_ask_serves_a_confirmed_row.py`, green on `:8000` (`ts-fee0022f`, 2/0/0). Tiffany transitioned the row.
2. **The proof is the PAIR, not the passing test.** One arm seeds `answer_is_correct=True` and gets `replay`/`exact_hit`/`cache_hit`, plus `replayed_snapshot_id` equal to the seeded row — so the hit is provably OURS, not replay-in-general. The other seeds `None` on an otherwise identical row and gets `path="agent"`. A green in the first alone is equally consistent with a flow that serves every exact hit and never consults `_may_serve`; only the second arm makes the confirmation the measured cause. It also answers the row's own question from outside, needing no `replay_refused_unconfirmed` trace field.
3. **It escapes the monopolize wall by not needing the queue at all.** Tier 1 is plain equality on the verbatim question — one indexed lookup, no embedding — so a replay hit never needs the cold ask to have run. One seeded row, one ask, no drain, no human at a mic. **The seed is TWO writes** (the question text lives on the canonical-synonym row, the confirmation mark on the snapshot row; a seed writing one looks right and never matches) — found independently by Tiffany and Mr. Radio. Seeded through `V2Cache.write_back()`, the production writer, which does both and computes `question_normalized`/`question_gist` with the request path's own normalizers, so neither mistake is available rather than merely documented.
4. **A strict xfail next door named the necessary half as if it were the whole condition** (`6ab9374d`). `test_v2_ask_roundtrip.py`'s replay test promised it would XPASS *"the day the box gains a consumer which is not the test itself."* It would not: it never confirms its answer, so with a free consumer the guard refuses and it fails on `path="agent"`. A **new** constant rather than an edit to the shared `DRAIN_UNOBSERVABLE` — `test_v2_resume_live.py` uses the same string, is `expect_path="agent"` throughout, and is genuinely drain-only; rewriting it would have made that reason wrong, the same defect pointed the other way.
5. **`dry_run` skips the tests, not the queue** (`ce6c85c9` → `ec0bf947`, four commits, three of them corrections of my own). The flag is read at EXECUTION time, so submit pushes the job with `monopolize=True` regardless — found by accident, having to wait for my own two arg-probes to drain. Then, in order: **measured** the hold at **7.0s** (`ts-e929149f`, `/api/busy` at 2 Hz) because an unquantified warning gets read at whatever severity the reader already feared; **relabelled** the busy-box claim, which I had asserted flatly having only inferred it — I caught that in a DM and left the artifact still stating it, and *a retraction has to reach the artifact*; then **reordered** so the bound leads, since Mr. Radio had ranked it first on Rick's list on a "locks the box" framing and the number is what let him rank it down. Field now carries three labelled tiers: from-the-code · measured · follows-from-those-not-observed.

**Checkpoint**: `96af865e` · `6ab9374d` · `ce6c85c9` · `07695ea2` · `8f595228` · `ec0bf947`. Two `:8000` runs (`ts-fee0022f` 2/0; `ts-cdba4e5e` 4 passed / 0 failed / 2 skipped = the strict xfails HOLDING — junit renders xfail as skipped, a strict XPASS counts as failed, and the credential skipif cannot have fired since 4 passed under it), venue verified idle by exit code before each, no bounce (the `/src` bind-mount was 103 ahead of origin — a bounce would have loaded other seats' in-flight work). Unit tier green at `ec0bf947` (Tiberius: 21,666 passed, the 2 reds the known `test_secret_scan` rotation holds). Open and NOT mine to close: `a501a714` and `084adbaf`, both Rick's rulings. Two fleet notes, no rows per the moratorium: `dry_run: true` still queues a real job, and `/api/test-suite/submit` allowlists pytest args (`-rxX` → 400 naming the arg). `src/docs/fastapi/` deliberately NOT regenerated — it is built off a live server that has not picked the change up, so regenerating would capture the old text.
**Files**: `src/tests/integration/test_v2_ask_serves_a_confirmed_row.py` (new) · `src/tests/integration/v2_queued.py` · `src/tests/integration/test_v2_ask_roundtrip.py` · `src/cosa/rest/routers/test_suite.py`

### 2026.09.01 (evening) - Session 37316fd2 (Rio ⚡, worker 3 of 3 for Mr. Radio 🦉) | The audio socket never reused the queue session id, and a worktree test run was importing two trees at once

**INDEX 3 — doc-vs-code drift, resolved in favour of the CODE.** Mr. Radio measured two session ids under one user ('foolish goat' on the queue socket, 'slow zebra' on the audio one) and asked which was right. Three clients, two behaviours: lupin-mobile (`enhanced_websocket_service.dart:740-741`) and the web multiplexer (`boot.ts:623-624`) build both sockets from one id; the web app (`notifications.js:2441-2442`) keeps two, deliberately — its TTS requests carry `audioSessionId` explicitly. `git log -S AUDIO_SESSION_KEY` returns only the squash import, so nothing changed; the docs were over-general from the start. Fixed `websocket-architecture.md` (×2), `websocket-troubleshooting.md` — which told a debugger to unify ids the live client keeps apart — and the `connect()` docstring, which had dropped the scope its own inline comment kept. The `.py` change is AST-identical to HEAD with docstrings stripped.

**Second finding, next door**: `websocket-architecture.md` claimed *"All methods are documented below"* and four public methods had zero mentions — including `emit_to_user_and_admins_sync`, the one its own docstring calls canonical and whose absence is the `248e740e` stale-job-card defect. `connect`'s signature was missing `roles` and `client_type`, the F-S6-1 marker the same document describes two tables higher. Both now held by a mechanical AST-vs-doc guard.

**A worktree pytest run splits the import graph** (`e2eb1306`). Every seat's shell carries `PYTHONPATH=/…/lupin/src`; `conftest` inserts `$LUPIN_ROOT/src` at position 0, and `sys.modules` is sticky — so `lupin_app` loads from your worktree while `cosa` loads from the main repo. The assembled app is a mixture that exists in no checkout. Cost: a faithful reproduction of the shipped `/api/tasks/flow-ratio` 422 defect reported **6 passed**, and two correct guards were minutes from being filed as blind. Pin `PYTHONPATH` too and the same arm gives 2 failed, each naming its own test. Remedy and the three-pin command are in CLAUDE.md beside the other worktree traps.

**Manifest parser fix** (`a37d1b71`): a trailing note — `(merge 27f993b9 — maya)` — was folded INTO the path, so the section claimed a string no file equals and the file read as claimed by nobody. The parse never failed; it succeeded and produced a wrong answer. 9 files recovered across the live manifest. A follow-on control was **measured and rejected** at 32 false positives per real case and recorded at the parser (`2ca2e1dd`) so nobody re-derives it.

**Also**: the git pathspec trap (`'src/docs/**/*.md'` reaches 17 of 35 files — the subdirectories only, because `**/` requires an intervening directory) added as the fifth receipt in the empty-result table; a missing gitignored credential now SKIPs naming its path instead of failing with an asyncio traceback; and the no-symlink ruling on `src/conf/keys/**` — a venv is a build artifact, a key is a secret.

**Files**: `websocket_manager.py` · `websocket-architecture.md` · `websocket-troubleshooting.md` · `commit_scope_guard.py` · `test_notify_idempotency_midconnect_smoke.py` · two new websocket guards · `test_commit_scope_guard_annotated_paths.py` · `CLAUDE.md` · `TODO.md`

### 2026.08.31 (evening) - Session 1a265076 (Rio ⚡, author #1 for Mr. Radio 🦉; review Tiberius 👑 · Rachel 🕊️) | The checked-hash control went live, and every defect in it was an empty value read as permission

1. **Row `f313fc2d` needed WIRING, not building.** The mechanical control was built on 08-30 by `3688a47b` (row `9b5b97de`) and **nothing called it** — its own R&D banner says the module is inert pending Rick's placement ruling. Rick ruled placement 3+4 (patch prevents, verifier detects), and `src/sitecustomize.py` IS that placement. Merged `184ea9cb`.
2. **🔴 An empty root set was read as UNIVERSAL scope, in a permission check — found by Tiberius 👑.** `sitecustomize` installs in EVERY interpreter and `_default_roots()` returns `()` without `LUPIN_ROOT`, so on any such interpreter the patch owned the whole filesystem. Verified before fixing rather than read off the source: `_in_scope( "/usr/lib/python3.13/json/decoder.py", () )` answered **True**, and a scratch package outside every repo came back **checked-hash**. Now fails closed (`9226f26a`).
3. **🔴 And the test that should have caught it was BLIND — he proved that too, which is the sharper half.** Both scopes excluded `tmp_path` (the explicit `/nonexistent/elsewhere` AND the inherited `$LUPIN_ROOT/src`), so the negative arm alone could not tell an honoured scope from an inherited one: with only that arm, reverting the prologue to its pre-fix form **left the test passing**. I had handed him that gap BY NAME as the thing I could not close, and naming it is what produced the discriminator.
4. **The same shape, three more times, all mine.** `install()` returned a bare `False` for both *"already in place"* (the preventer works) and *"cannot be patched"* (there is no preventer); making them merely falsey was still wrong, because to a caller asking *"is it ACTIVE?"* already-installed is a **success** — so `__bool__` now RAISES and names `is_installed()` (Mr Radio's ruling). Three end-to-end tests took their scope from the **ambient environment**: one child reads `converted_count` **0** with `LUPIN_ROOT` set, **2** with it unset — one test failed outright, two passed **vacuously**. And Rachel 🕊️ measured that the shim caught `Exception`, not `BaseException`: a staged `KeyboardInterrupt` gave **`Fatal Python error: init_import_site`** and the interpreter never started.
5. **🔴 I withdrew my own explanation for the one red I could not place.** Called a `test_bounce_dev_server_busy_guard` timeout *system load*, then tested it: under `systemd-run` at `CPUQuota` 100/25/10/5% it passes every time at 1.15–1.18s. **Not CPU-bound, so the hypothesis failed.** Recorded in-file as **UNEXPLAINED** — not flake, not load — with *"do not wave a red here through"*; ceiling left at 30 because raising it to stop a red switches off the sensor. Also withdrew two of my own numbers: *"9ms and 9ms"* was **integer truncation** hiding a difference, and a re-take with `env -u` misplaced measured **`env` failing** at 0.68 ms/start.
6. **Every fix carries a two-arm proof on one mutated sha, restores verified by sha.** Fail-open mutant `702b3db7` survives the old fixture, killed by the new. Reverting `__bool__` reddens exactly the two new cases. Scope mutant `1828a077` reddens two table rows AND the real-child guard (3 failed / 56 passed), restored 59 passed at `85082d3c`.

**Checkpoint**: merged `184ea9cb` (6 files, 501 insertions) after Tiberius verified `9226f26a` in his own detached worktree — **disclosed in the merge message that it carries `61b2eb93`, one docstring-only commit past the sha he verified**, rather than letting an approval stretch silently. Tier on the merge: **2 failed / 21316 passed**, both the deliberate `secret_scan` rotation hold. ⚠️ **The passed count moved 21216 → 21316 and the extra hundred is NOT mine** — my branch is 4 commits off `904a9b01` while the working branch had advanced 6 to `b67fd52b`; **guessing** at the source, the `kill_guard` tests plus Rachel's merge. Two earlier tiers **void and withdrawn**: both measured trees I edited underneath them. Coverage on the module 100% (149 stmts / 0 miss, 42 branch / 0 partial); `sitecustomize` a declared omit with its reason. Left unstaged, not mine: `src/conf/epic-stories.json` and two peers' `src/rnd/` docs.
**Files**: `src/sitecustomize.py` (new) · `src/cosa/utils/checked_hash_pyc.py` · `src/tests/unit/test_checked_hash_pyc.py` · `src/tests/unit/test_sitecustomize_fails_open.py` (new) · `src/tests/unit/test_bounce_dev_server_busy_guard.py` · `pyproject.toml`

### 2026.08.30 (evening) - Session 93a8751c (Mr. Radio 🦉; crew Rachel 🕊️ · Tiberius 👑 · Pocholo 📣 · Clayton 😎 · Rio ⚡ · Krishna 🦚 · maya 🌻 · chloe 🗼) | Five tools, one shape: a clean exit that means nothing happened

1. **🔴 Five seats found the same defect in five different tools and none of us recognised it until they sat together.** `--cov` at a bad target: exit 0, no table, **never measured**. `--verify` on a fresh tree: exit 0, **nothing there to judge**. `purge-pycache.sh --verify`: exit 0, flag ignored, **purged anyway**. A purge on an already-emptied tree: exit 0, *"nothing to purge"*, **never ran**. `rc == 1` read as a mutation kill on a suite that was **already red at baseline**. In every one the failure and the success print the same thing. Landed `49dba474`, then **rewritten around Rachel's correction** at `e5e68c09`: mine told the reader to *check*, which is a rule requiring vigilance, and this page's own doctrine is that a rule depending on memory is not a control. **The fix is a tool that REFUSES and names what it did not do** — both live examples are ours: Rio's purge declining before the `rm`, and Rachel's harness printing `ANCHOR MATCHED 2x — NOT APPLIED` rather than guessing at placement.
2. **🔴 I published a rule, filed it P1, and retracted both within the hour.** Claimed an ordinary pytest run undoes the checked-hash conversion. **maya disproved it** with a probe module and a two-directional negative control — a source pyc stays checked-hash and the mutation IS seen. Pytest's rewriting writes a **disjoint filename tag** (`…-pytest-8.4.2.pyc`), which is why two honest counts looked contradictory. Measured across three trees: main checkout **2452 checked-hash / 0 timestamp**, a converted worktree 2434/1, an **unconverted worktree 0/724**. Corrected finding at `0a3a2471`, sharpened by Pocholo into **three states** — fresh-unused verifies `0` *vacuously*, fresh-then-one-run verifies `1`, converted-and-populated verifies `0` genuinely. **The first and third print the same code and mean opposite things.**
3. **🔴 The remedy this file prescribed reached into a shared tree.** `purge-pycache.sh` took its root from `$LUPIN_ROOT`, inherited from the shell — so run from a worktree it purged and reconverted the **MAIN REPO**, printed its success banner, and left your tree as poisoned as it found it. **Found and written up by Pocholo at ~17:52 in `TODO.md:671`**; re-derived independently by Rachel five hours later, who **declined the credit and pointed at his entry**; Krishna reached it a third time with the sharpest receipt (worktree kept all 91 dirs, script reported *"Purging 188"*). ⚠️ **It was written down, correctly and in detail, and the whole crew walked into it anyway for five hours** — including a reviewer whose every per-arm isolation went to the wrong tree. *A defect recorded in TODO.md is not a control; only the code is.* Fixed by Rio (`6cb757b3`): root from `BASH_SOURCE` unconditionally, **both scripts** (purge chains to migrate, so fixing one leaves them resolving different trees).
4. **My own stopgap left the exact half-done tree it warned about.** `LUPIN_ROOT="$PWD" purge-pycache.sh` **purges then fails to reconvert** in a venv-less worktree, because `PYTHON` derives from `LUPIN_ROOT`. Two variables, two questions — *which tree do I clean* versus *what runs compileall* — and I had treated them as one knob. Fixed `5e72942c`, verified by Rachel independently in a real worktree. ⚠️ **My original verification tested a hardcoded `PYTHON` while I published a `git-common-dir` expression** — I verified one command and shipped another.
5. **Three seats corrected the record against their own interest, and that is why the rest is trustworthy.** Tiberius **retracted his own approval** after proving the same arm at the same sha read KILLED then SURVIVED, and reported a coverage figure as *unverified* rather than take it under contention. Clayton audited his **own** fixture, found `acked=3` against `recipients=3` (two equal numbers cannot reveal a swap between them), and later **dropped his own duplicate branch** on discovering Pocholo's covered the class. Pocholo **declined to self-merge** though both reviewers told him to.
6. **maya's rule is the strongest thing on the branch and it did not come from me.** *Coverage measures whether a line RAN, never whether the test could have NOTICED it running wrong.* Two files at 100% lines and branches with the defect live in them — the number TRUE and useless, because a fake taking any arguments returns its scripted page whatever is asked. **The mechanical tell**: replace the code under test with a constant; if the fixture still yields the same data, the suite is measuring the fixture.

**Checkpoint**: 7 branches merged (`c91bd1bb` replay user_id · `168e677c` bounce_dev_warn 100% · `3ccc821a` coverage_contention · Chloé, three maya branches, Rio's eleven); ~25 CLAUDE.md commits including **three retractions of my own claims**; 3 seats re-spun on sensor-confirmed readings. **Ends blocked, correctly**: `wt-maya-4f0ced13` held unmerged until Rick rotates the postgres credential — the red is the signal working and greening it changes the signal, not the risk. Left unstaged: `src/conf/epic-stories.json`, a peer's work, unclaimed in `.claude-session.md`.
**Files**: `CLAUDE.md` (five new rules) · `TODO.md` · merges above
