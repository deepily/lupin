# Lupin Project History

> **📋 TODO FOR NEXT SESSION (Session 77)**:
> 1. **🎙️ Podcast Generator - Manual Testing**: Session 76 fixes complete (save timing + version suffix). Awaiting manual test of full workflow. See plan: `/home/rruiz/.claude/plans/humble-dancing-beacon.md`
> 2. **🔬 Deep Research Queue Integration - API Testing**: Test `POST /api/deep-research/submit` endpoint with proper JWT authentication (login first via `/auth/login`). Phases 1-4 backend implementation complete. See plan: `/home/rruiz/.claude/plans/tidy-doodling-pizza.md`
> 3. **🎙️ Podcast Generator Phase 2 (FUTURE)**: Implement TTS audio generation and stitching (ElevenLabs multi-voice, pydub concatenation).
> 4. **⏰ Job Queue Progressive Disclosure UI - MANUAL TESTING**: Full 6-phase implementation complete (Session 57 continuation), awaiting browser testing. See plan: `/home/rruiz/.claude/plans/cheeky-leaping-scone.md`
> 5. **Conversation Identity Phases 4-5 (FUTURE)**: Phase 1 (Session Identity) complete. Remaining: Phase 4 = AgentBase History Integration, Phase 5 = Conversation Lifecycle.
> 6. Voice discovery/configuration for notification TTS UI still pending
> 7. **Ongoing notification UI tweaks and bug fixes (ONGOING)**: Incremental improvements to the cosa-voice notification system.
>
> **✅ SESSION 76 COMPLETE**: Podcast Generator Save Timing & Version Suffix Fixes. See details below.
>
> **✅ SESSION 75 COMPLETE**: Podcast Generator Bugs 7 & 8 Fixed (iterative review + filename preservation). See details below.

> **🎯 CURRENT**: 2026.01.19 (Session 76) - PODCAST GENERATOR SAVE TIMING & VERSION SUFFIX FIXES! Fixed two issues from manual testing of `--edit-script` mode. **ISSUE 1 - PREMATURE SAVE**: Script was being saved at the TOP of the review loop BEFORE user made any decision - debug output showed "Script saved to: ..." before presenting review choices. **ROOT CAUSE**: Both `do_all_async()` and `do_review_only_async()` called `_save_script_async()` immediately when entering the review `while` loop. **FIX**: Moved save to AFTER user approves (final save) or AFTER revision is generated (versioned save). Now shows "Will save to:" preview path instead of actually saving. **ISSUE 2 - NO VERSION SUFFIX**: Revised scripts overwrote the original file instead of creating versioned copies. User expected: original preserved, revisions as `-v2.md`, `-v3.md`. **ROOT CAUSE**: `_save_script_async()` always reused `self._original_script_path` without version suffix. **FIX**: Added `is_revision: bool = False` parameter to `_save_script_async()`. When `is_revision=True`, generates versioned path: `stem-v{revision_num}.md`. Example: `2026.01.19-153550-voice-script.md` → `2026.01.19-153550-voice-script-v2.md`. **FILES MODIFIED**: `src/cosa/agents/podcast_generator/orchestrator.py` (~60 lines: save timing in both review methods + version suffix in _save_script_async). **SMOKE TESTS**: 8/8 PASSED. **PLAN FILE**: `/home/rruiz/.claude/plans/humble-dancing-beacon.md`. **STATUS**: Implementation complete, awaiting manual verification. 🎙️🔧✅
>
> **🎯 PREVIOUS**: 2026.01.19 (Session 75) - PODCAST GENERATOR BUGS 7 & 8 FIXED! User reported two critical bugs from manual testing of the `--edit-script` feature. **BUG 7 - NON-DESCRIPTIVE FILENAME**: Revised scripts were saved as `2026.01.19-160333-untitled-podcast-script.md` instead of preserving the original filename. Root cause: `_save_script_async()` generated a NEW filename each time based on current `script.title`. **FIX**: Added `_original_script_path` attribute to track original filename across revisions. Updated `from_saved_script()` to store original path, modified `_save_script_async()` to check and reuse original path for all subsequent saves. **BUG 8 - REVIEW NOT ITERATIVE (CRITICAL)**: After providing revision feedback and LLM regenerating the script, the process immediately EXITED instead of looping back to show the updated script for further review. User expected: review → feedback → regenerate → review again → repeat until explicit approval. Root cause: No `while` loop around review phase - code fell through to `script_approved = True` after revision. **FIX**: Wrapped entire review phase in `while not script_approved:` loop in BOTH `do_all_async()` and `do_review_only_async()` methods. Loop structure: (1) Save current draft (overwrites previous), (2) Present script preview with file path, (3) Handle choice - "Approve" exits loop, "Revise" or "Other" triggers regeneration and loops back, "Cancel" cleans up and returns None. **KEY CODE CHANGES**: (1) Added `self._original_script_path: Optional[str] = None` in `__init__`, (2) Store path in `from_saved_script()` with `agent._original_script_path = script_path`, (3) Modified `_save_script_async()` to reuse original path or generate new one only on first save, (4) Rewrote Phase 4 review section with iterative loop pattern. **FILES MODIFIED**: `src/cosa/agents/podcast_generator/orchestrator.py` (~80 lines: filename tracking + iterative review loop in both methods). **SMOKE TESTS**: 8/8 PASSED (config, state, prompts.script_generation, prompts.personality, cosa_interface, voice_io, api_client, orchestrator). **PLAN FILE**: `/home/rruiz/.claude/plans/mellow-chasing-tiger.md`. **STATUS**: Bug fixes complete, awaiting manual verification. 🎙️🐛✅
>
> **✅ SESSION 74 COMPLETE**: Podcast Generator Bug Fixes + Script Resume Feature. See details below.
>
> **✅ SESSION 74b COMPLETE**: Progressive Narrowing Test Harness - Isolated testing module for Deep Research theme clustering. See details below.
>
> **✅ SESSION 73 COMPLETE**: Notification UI bug fixes and updates. See details below.
>
> **✅ SESSION 72 COMPLETE**: Unified `_format_questions_for_tts()` implementations into shared utility. See details below.
>
> **✅ SESSION 71 COMPLETE**: Fix duplicate multiple-choice option rendering in agent interfaces. See details below.
>
> **✅ SESSION 70 COMPLETE**: Podcast Generator Agent Phase 1 - Script generation infrastructure. See details below.
>
> **✅ SESSION 69b COMPLETE**: Deep Research Background Job Integration - Phases 1-4 Backend Implementation. See details below.
>
> **✅ SESSION 69 COMPLETE**: Robust right-justification for sender stats in notification UI. See details below.
>
> **✅ SESSION 68 COMPLETE**: Reduce TTS verbosity for ask_multiple_choice - options now UI-only, not spoken. See details below.
>
> **✅ SESSION 67 COMPLETE**: Fix Sender ID and Session Name Separation for Deep Research CLI. See details below.
>
> **✅ SESSION 66 COMPLETE**: Fixed literal `\n` in abstract field breaking markdown rendering. See details below.
>
> **✅ SESSION 65 COMPLETE**: Markdown rendering for abstract field with XSS protection. See details below.
>
> **✅ SESSION 64 COMPLETE**: Deep Research Agent - Semantic Session IDs + Rate Limit Error Handling. See details below.
>
> **✅ SESSION 63 COMPLETE**: MCP Documentation Fix - `abstract` parameter. See details below.
>
> **✅ SESSION 62 COMPLETE**: Grace Period Message Bug Fix. See details below.
>
> **✅ SESSION 61 COMPLETE**: History Filter Race Condition Fix + Dropdown UX + UI Reorder. See details below.
>
> **✅ SESSION 60 COMPLETE**: Notifications UI Minor Tweaks - Action-required auto-expand, TTS queue empty state, Config reload button. See details below.
>
> **✅ SESSION 59 COMPLETE**: Deep Research Agent Voice-First CLI Testing + Notification Identity Implementation. See details below.
>
> **✅ SESSION 58 COMPLETE**: Gist Button UX, Recording Toggle Bug, Session Title Generation + Stop Token Sentinel Pattern. See details below.
>
> **✅ SESSION 57 COMPLETE**: Job Queue Progressive Disclosure UI - Full 6-phase implementation. See details below.
>
> **✅ COMPLETED**: Conversation Identity Architecture Phases 1-3 (Session 56) - parallel Claude Code sessions now distinguishable in notification UI. New sender_id format: `claude.code@{project}.deepily.ai#{session_id}`.
>
> **🎯 PREVIOUS**: 2026.01.19 (Session 73) - NOTIFICATION UI BUG FIXES AND UPDATES! Running session for multiple incremental improvements to the cosa-voice notification system. **FIX 1 - PAUSE BUTTON STATE RECOVERY**: Action-required notification pause button sometimes didn't work - user had to reload page. **ROOT CAUSE**: `togglePause()` silently failed when `activeActionRequiredId` was null (state out of sync with visible notification). **SOLUTION**: Enhanced `togglePause()` with: (1) Debug logging showing isPaused and activeId state, (2) State recovery logic that detects visible notification card and restores activeActionRequiredId from DOM, (3) Visual feedback (shake animation) when pause still can't execute after recovery attempt. **CSS ADDED**: `@keyframes pauseButtonShake` animation and `.action-required-pause-btn.shake` class for visual feedback. **FIX 2 - CANCEL BUTTON FOR ACTION-REQUIRED NOTIFICATIONS**: Users had no way to explicitly cancel/dismiss an action-required notification - had to wait for timeout. **SOLUTION**: Added cancel button (gray ✕) in upper-left corner of notification card header. Applies to all notification types (yes_no, open_ended, multiple_choice). Cancel submits `response_default` value (same as timeout behavior). Escape key also triggers cancel. **IMPLEMENTATION**: (1) Added `cancelActionRequired()` method that submits default value, (2) Cancel button in card header with click handler, (3) Escape key handler in `attachKeyboardListener()`, (4) CSS styling with dark gray border, white background, positioned absolute in header. **FILES MODIFIED**: `notifications.js` (~100 lines: cancel button HTML, cancelActionRequired method, click handlers, Escape key handler), `notifications.css` (~30 lines: .action-required-cancel-btn styles, header padding adjustment). **STATUS**: Both fixes complete and verified working. 🔧✅
>
> **🎯 PREVIOUS**: 2026.01.19 (Session 72) - UNIFIED `_format_questions_for_tts()` IMPLEMENTATIONS! Consolidated three identical function implementations into a single shared utility, eliminating ~150 lines of duplicate code and fixing broken smoke tests. **THE PROBLEM**: Three files had identical `_format_questions_for_tts()` functions: `src/cosa/agents/deep_research/cosa_interface.py`, `src/cosa/agents/podcast_generator/cosa_interface.py`, and `src/lupin_mcp/cosa_voice_mcp.py`. Additionally, the first two files also had broken smoke tests that expected options in TTS output (the function had been refactored in Session 68 to exclude options from TTS, but the smoke tests weren't updated). **SOLUTION**: Created new shared utility module `src/cosa/utils/notification_utils.py` with two public functions: `format_questions_for_tts()` (builds TTS-friendly message with question text only, adds multi-select hint when applicable) and `convert_questions_for_api()` (converts Claude Code's camelCase `multiSelect` to API's snake_case `multi_select`). **CHANGES MADE**: **1. NEW FILE**: `src/cosa/utils/notification_utils.py` (~135 lines) with both functions plus comprehensive smoke test. **2. deep_research/cosa_interface.py**: Added import from notification_utils, removed local `_format_questions_for_tts()` (~43 lines) and `_convert_questions_for_api()` (~31 lines), updated function calls to use imported names (no underscore prefix), fixed smoke test to correctly assert options are NOT in TTS output. **3. podcast_generator/cosa_interface.py**: Added import, removed local `_format_questions_for_tts()` (~28 lines), updated function call, fixed smoke test. **4. lupin_mcp/cosa_voice_mcp.py**: Added import, removed local `_format_questions_for_tts()` (~43 lines) and `_convert_questions_format()` (~32 lines), updated function calls to use new names (`convert_questions_for_api` instead of `_convert_questions_format`). **SMOKE TEST RESULTS**: All 4 modules passed: notification_utils (4 tests), deep_research/cosa_interface (7 tests), podcast_generator/cosa_interface (6 tests), lupin_mcp import verification. **KEY IMPROVEMENTS**: DRY principle (~150 lines removed), consistent function naming (public exports without underscore), single source of truth for TTS formatting logic, fixed pre-existing broken smoke tests. **FILES CREATED**: `src/cosa/utils/notification_utils.py` (~135 lines). **FILES MODIFIED**: `src/cosa/agents/deep_research/cosa_interface.py` (~-74 lines, +5 lines), `src/cosa/agents/podcast_generator/cosa_interface.py` (~-28 lines, +4 lines), `src/lupin_mcp/cosa_voice_mcp.py` (~-75 lines, +3 lines). **STATUS**: Refactoring complete, all smoke tests passing. 🔧✅
>
> **🎯 PREVIOUS**: 2026.01.18 (Session 71) - FIX DUPLICATE MULTIPLE-CHOICE OPTION RENDERING IN AGENT INTERFACES! Quick bug fix session addressing duplicate display of options in notification UI. **THE PROBLEM**: When multiple-choice questions were sent via `ask_multiple_choice()`, options appeared twice: (1) As narrative text in the TTS message ("Option 1: Voice recognition... Option 2: Natural language..."), and (2) As actual UI checkboxes. This created confusing duplicate presentation. **ROOT CAUSE**: Two agent interface files had a buggy `_format_questions_for_tts()` function that incorrectly included option labels in the TTS message - the same bug fixed in Session 68 for the MCP server, but not propagated to agent interfaces. **BUGGY CODE**: `option_texts = [f"Option {j}: {opt.get('label', f'Option {j}')}" for j, opt in enumerate(options, 1)]; part += " " + ". ".join(option_texts) + "."` **SOLUTION**: Updated both agent interface files to match the correct MCP server implementation - returns ONLY question text, options display in UI only, adds multi-select hint when applicable. **FILES MODIFIED**: `src/cosa/agents/deep_research/cosa_interface.py` (lines 389-431, ~40 lines replaced), `src/cosa/agents/podcast_generator/cosa_interface.py` (lines 330-357, ~25 lines replaced). **VERIFICATION**: Compared against reference implementation in `src/lupin_mcp/cosa_voice_mcp.py` lines 502-544 - all three files now have consistent logic. **TODO ADDED**: Future consolidation opportunity - all three `_format_questions_for_tts()` implementations could be unified into a shared utility. **STATUS**: Bug fix complete and verified. 🔧✅
>
> **🎯 PREVIOUS**: 2026.01.18 (Session 69b) - DEEP RESEARCH BACKGROUND JOB INTEGRATION - PHASES 1-4 BACKEND IMPLEMENTATION! Implemented complete backend infrastructure for submitting Deep Research jobs via REST API to run asynchronously in the existing queue system. **ARCHITECTURE**: Created AgenticJobBase abstract class for long-running Claude Code agentic jobs (distinguishes from quick AgentBase tasks), DeepResearchJob concrete implementation wrapping existing CLI logic, FastAPI endpoint `POST /api/deep-research/submit`, and RunningFifoQueue integration for non-caching agentic jobs. **PHASE 1 - AGENTICJOBBASE FOUNDATION**: Created `src/cosa/agents/agentic_job_base.py` (~175 lines) with: (1) `JOB_TYPE`/`JOB_PREFIX` class constants for ID generation (e.g., "dr-a1b2c3d4"), (2) `is_cacheable = False` property (agentic jobs don't snapshot), (3) Abstract methods `last_question_asked`, `do_all()`, `_execute()`, (4) Execution state tracking (`status`, `started_at`, `completed_at`, `error`), (5) `artifacts` dict for results (report_path, abstract, etc.), (6) `get_execution_duration_seconds()` helper. Smoke test: PASSED. **PHASE 2 - DEEPRESEARCHJOB**: Created `src/cosa/agents/deep_research/job.py` (~320 lines) inheriting from AgenticJobBase with: (1) `JOB_TYPE="deep_research"`, `JOB_PREFIX="dr"`, (2) `do_all()` bridges to async via `asyncio.run(self._execute())`, (3) `_execute()` wraps existing CLI functions (run_research, generate_abstract_for_cli, save_report_with_frontmatter), (4) Voice notifications via cosa-voice MCP at start/completion, (5) Session name generation via Gister, (6) Cost tracking via SessionSummary dataclass. Smoke test: PASSED. **PHASE 3 - FASTAPI ENDPOINT**: Modified `src/cosa/rest/routers/deep_research.py` (+90 lines) with: (1) Pydantic models `DeepResearchSubmitRequest` (query, user_email, budget, websocket_id, lead_model), (2) `DeepResearchSubmitResponse` (status, job_id, queue_position, message), (3) `get_todo_queue()` dependency, (4) `POST /api/deep-research/submit` endpoint with JWT auth via `get_current_user`. **PHASE 4 - RUNNINGFIFOQUEUE INTEGRATION**: Modified `src/cosa/rest/running_fifo_queue.py` (+95 lines) with: (1) Import `AgenticJobBase`, (2) Modified `_process_job()` to check AgenticJobBase BEFORE AgentBase, (3) Added `_handle_agentic_job()` method that calls `do_all()` without snapshot caching and moves to done/dead queue based on status. **DOCUMENTATION CREATED**: `src/rnd/deep-research-queue/` with 5 files following p-is-p-02 Pattern A: `00-index.md`, `01-implementation-current.md`, `02-architecture.md`, `03-decisions.md`, `04-testing-validation.md`. **TESTING STATUS**: Smoke tests all PASSED, API testing pending (requires JWT authentication - login via `/auth/login` first). **KEY DESIGN DECISIONS**: (1) AgenticJobBase separate from AgentBase (different lifecycle/caching needs), (2) `is_cacheable=False` for all agentic jobs, (3) Dedicated endpoint vs overloading `/api/push`, (4) Async execution via `asyncio.run()` within sync `do_all()`. **FILES CREATED**: `src/cosa/agents/agentic_job_base.py` (~175 lines), `src/cosa/agents/deep_research/job.py` (~320 lines), 5 documentation files in `src/rnd/deep-research-queue/`. **FILES MODIFIED**: `src/cosa/rest/routers/deep_research.py` (+90 lines), `src/cosa/rest/running_fifo_queue.py` (+95 lines). **PLAN FILE**: `/home/rruiz/.claude/plans/tidy-doodling-pizza.md`. **STATUS**: Phases 1-4 complete, API testing pending. 🔬✅
>
> **🎯 PREVIOUS**: 2026.01.18 (Session 70) - PODCAST GENERATOR AGENT PHASE 1 - SCRIPT GENERATION INFRASTRUCTURE! Implemented complete Phase 1 of the Podcast Generator Agent following the Deep Research Agent architecture pattern. Transforms Deep Research documents into conversational "Dynamic Duo" podcast scripts with two AI hosts (curious questioner + knowledgeable explainer). **ARCHITECTURE FOLLOWING DEEP RESEARCH PATTERN**: 11 files created in `src/cosa/agents/podcast_generator/`: (1) `__init__.py` - package exports with version 0.1.0, (2) `config.py` - PodcastConfig dataclass with HostPersonality/VoiceProfile classes for customizable hosts, default Alex (curious) + Jordan (expert) duo, ElevenLabs voice mappings (Sarah/Arnold), (3) `state.py` - OrchestratorState enum (10 states), ProsodyAnnotation enum (13 markers like *[pause]*, *[laughs]*), Pydantic models (ScriptSegment, PodcastScript, ContentAnalysis, PodcastMetadata), TypedDict PodcastState, (4) `api_client.py` - PodcastAPIClient with firewalled API key pattern, CostEstimate tracking, async calls for analysis/script/revision, (5) `cosa_interface.py` - async voice I/O wrappers with sender_id `podcast.gen@lupin.deepily.ai#cli`, (6) `orchestrator.py` - PodcastOrchestratorAgent with state machine phases: LOADING_RESEARCH → ANALYZING_CONTENT → GENERATING_SCRIPT → WAITING_SCRIPT_REVIEW → COMPLETED, (7) `__main__.py` - CLI entry point with --research, --dry-run, --smoke-test flags, (8-10) `prompts/__init__.py`, `prompts/script_generation.py`, `prompts/personality.py` - system prompts for content analysis and Dynamic Duo script generation with prosody annotation guidelines, (11) `tests/test_podcast_generator.py` - 28 unit tests. **SCRIPT FORMAT**: Minimalist markdown with prosody annotations: `**[Host Name - Role]**: Dialogue text with *[prosody]* markers`. **KEY FEATURES**: Customizable host personalities for A/B comparison, JSON-based script generation with automatic markdown conversion, voice I/O integration via cosa-voice MCP, script review checkpoint with revision support. **VERIFICATION**: 7 module smoke tests PASSED, 28 unit tests PASSED, live API call PASSED ($0.0010). **CLI USAGE**: `python -m cosa.agents.podcast_generator --research path/to/doc.md` or `--smoke-test`. **FILES CREATED**: 11 files (~2700 lines total) at `src/cosa/agents/podcast_generator/`. **REMAINING PHASES**: Phase 2 (TTS audio generation + stitching), Phase 3 (CLI with voice input), Phase 4 (COSA Router integration). **R&D UPDATED**: `src/rnd/README.md` entry added. **STATUS**: Phase 1 complete with full test coverage. 🎙️✅
>
> **🎯 PREVIOUS**: 2026.01.18 (Session 69) - ROBUST RIGHT-JUSTIFICATION FOR SENDER STATS! Fixed CSS alignment issue where stats elements (new count badge, message count, last activity) weren't properly right-justified when the new count badge was hidden. **THE PROBLEM**: Previous fix used `margin-left: auto` on `.sender-message-count`, but this failed when `.sender-new-count` was hidden (`display: none`) - the auto margin only pushes content if there's something before it in the flex container. DOM order: `[...] [sender-new-count] [sender-message-count] [sender-last-activity] [...]`. **SOLUTION**: Wrapped all three stats elements in a new container `<span class="sender-stats-group">` with `display: flex`, `align-items: center`, `margin-left: auto`, and `gap: 6px`. The container is always visible even when the new-count badge inside is hidden, so right-justification always works. **CSS CHANGES**: Added `.sender-stats-group` style (6 lines), removed `margin-left: auto` from `.sender-message-count`, removed `margin-left: 10px` from `.sender-last-activity` (gap handles spacing now), kept `margin-left: 6px` on `.sender-new-count` for buffer. **JS CHANGES**: Wrapped the three stats spans in `<span class="sender-stats-group">` container in `createSenderCard()` (~5 lines changed). **FILES MODIFIED**: `src/fastapi_app/static/js/notifications.js` (+5 lines), `src/fastapi_app/static/css/notifications.css` (+6 lines, -2 lines). **RESULT**: Stats now always right-justified whether new-count badge is visible or hidden. **PLAN FILE**: `/home/rruiz/.claude/plans/sorted-popping-bunny.md`. **STATUS**: Implementation complete and verified. 📐✅
>
> **🎯 PREVIOUS**: 2026.01.18 (Session 68) - COSA-VOICE MCP TTS VERBOSITY FIX! Reduced noise in voice notifications by removing redundant option reading from `ask_multiple_choice()` TTS output. **THE PROBLEM**: When `ask_multiple_choice()` was called, the TTS would read the question AND all option labels with descriptions aloud (e.g., "Option 1: Commit only - Keep changes local. Option 2: Commit and push - Sync to remote..."). This was redundant since options are clearly displayed in the UI, and made voice notifications annoyingly verbose. **SOLUTION**: Modified `_format_questions_for_tts()` in `cosa_voice_mcp.py` to return ONLY the question text. Options are now UI-only - displayed visually but not spoken. Multi-select hint still included when applicable. **CODE CHANGES**: Removed the option enumeration loop (~15 lines), updated docstring to reflect new behavior, added comment clarifying "Options are displayed in UI, not spoken in TTS". **FILES MODIFIED**: `src/lupin_mcp/cosa_voice_mcp.py` (~31 lines changed in `_format_questions_for_tts()`). **STATUS**: Cleaner voice UX - questions are concise, options visible in UI. 🔇✅
>
> **🎯 PREVIOUS**: 2026.01.15 (Session 59) - DEEP RESEARCH AGENT VOICE-FIRST CLI TESTING + NOTIFICATION IDENTITY! Comprehensive testing session for Deep Research Agent CLI followed by notification identity implementation. **PART 1 - VOICE-FIRST CLI TESTING**: Ran 5 test scenarios for `src/cosa/agents/deep_research/` CLI module. Test 1 (Dry Run) - PASSED: CLI parses arguments, shows workflow preview. Test 2 (CLI Mode with Budget) - PARTIAL: Hit rate limit at ~83k tokens from Anthropic web search tool (30k/min limit). Test 3 (Voice Mode) - PASSED: Voice service detected, notifications sent correctly. Test 4 (Smoke Tests) - ALL PASSED: voice_io, cosa_interface, config, api_client modules. **PART 2 - NOTIFICATION IDENTITY ARCHITECTURE**: Investigated pros/cons of direct library calls vs MCP tool calls for voice notifications. Key issue: Direct library calls show messages as "unknown origin" in UI because `sender_id` not set. **SOLUTION IMPLEMENTED**: Added sender_id support to Deep Research Agent with format `deep.research@{project}.deepily.ai`. **PATH DETECTION FIX**: User caught logic error - `/cosa` check needed special handling since cosa is a subdirectory of lupin. Fixed to check `/cosa` first but ONLY if `/lupin` is NOT in the path. **FILES MODIFIED**: `src/cosa/cli/notification_models.py` (updated regex from `^claude\.code@...` to `^[a-z]+\.[a-z]+@...` for multiple agent types, updated `extract_sender_from_message()` to accept `agent_type` parameter), `src/cosa/agents/deep_research/cosa_interface.py` (added `_get_sender_id()` function, `SENDER_ID` module constant, updated all 4 notification functions to pass `sender_id`). **VERIFICATION TESTS**: Regex pattern validation PASSED, SENDER_ID shows `deep.research@lupin.deepily.ai` PASSED, cosa_interface smoke test PASSED, live notification test PASSED. **PLAN FILE**: `/home/rruiz/.claude/plans/distributed-zooming-kazoo.md`. **STATUS**: Voice-First CLI testing complete, Notification Identity for Deep Research Agent implemented, rate limiting issue documented for future. 🎤✅🏷️✅
>
> **🎯 PREVIOUS**: 2026.01.15 (Session 58) - GIST BUTTON UX + SESSION TITLE GENERATION + STOP TOKEN SENTINEL PATTERN! Four initial tasks plus critical LLM infrastructure fix. **TASK 1 - GIST BUTTON UX**: Added working indicator (⏳) and disabled state to ✨ button while generating gist. CSS `.sender-gist-btn.working` styles for visual feedback. **TASK 2 - GIST DEBUG OUTPUT**: Enabled `Gister(debug=True)` in notifications.py for debugging. **TASK 3 - RECORDING BUTTON STYLING**: Fixed mic button in session name modal - changed class from `session-name-mic-btn` to `stt-button`, deleted ~28 lines duplicate CSS. **TASK 4 - RECORDING TOGGLE BUG**: Fixed click-to-record toggle logic - was starting new recording instead of stopping. Added `isRecording()` check, plus auto-focus for spacebar control. **GIST QUALITY ISSUE**: "Review and test the" was a terrible session title. Created new `session-title.txt` prompt with 3-5 word constraint, commit-style format, action verb prefix requirements. Added `prompt_key` parameter to Gister for using different prompts with cache bypass for non-default prompts. **XML PARSER BUG**: Pydantic parsing failed due to LLM generating explanation text after `</response>`. Initial fix used `rfind()` but found MENTIONS of tag in explanation text. Fixed by changing to `find()` for FIRST occurrence. **STOP TOKEN PARAMETER MISMATCH**: Critical bug discovered - config used `stop_sequence` but vLLM code looked for `stop`. Stop tokens were NEVER being sent! Fixed parameter name in all 3 model configs. **STOP TOKEN TRUNCATION**: vLLM stops BEFORE outputting stop token, causing truncated XML missing `</response>`. User proposed elegant sentinel pattern: use `</stop>` AFTER `</response>`. LLM generates complete XML, then vLLM stops at sentinel. **PHASE 2 GENERALIZATION**: Extended sentinel pattern to ALL 12 prompt templates by modifying `PromptTemplateProcessor.process_template()` to automatically append `</stop>` to XML examples. **FILES MODIFIED**: `notifications.js` (~70 lines: gist button UX, recording toggle, button class), `notifications.css` (~30 lines: gist working state, removed duplicate CSS), `notifications.py` (debug logging, session title prompt), `gister.py` (+prompt_key parameter, cache bypass), `lupin-app.ini` (stop tokens changed to `</stop>` in all 3 models), `lupin-app-splainer.ini` (documentation), `util_xml_pydantic.py` (suffix stripping fix rfind→find), `prompt_template_processor.py` (sentinel pattern generalization). **FILES CREATED**: `src/conf/prompts/agents/session-title.txt`. **PLAN FILE**: `/home/rruiz/.claude/plans/graceful-sprouting-dewdrop.md`. **STATUS**: All tasks complete, stop token sentinel pattern working across all prompt templates. 🎯✨🐛✅
>
> **🎯 PREVIOUS**: 2026.01.14 (Session 57 continuation) - JOB QUEUE PROGRESSIVE DISCLOSURE UI - FULL IMPLEMENTATION! Implemented all 6 phases of progressive disclosure UI for Job Queues section. **THE PROBLEM**: Jobs and notifications were disconnected - users couldn't see "what did Claude say to me about this job?" Jobs had `user_id` + `websocket_id` but no link to notification conversations. **SOLUTION**: Progressive disclosure UI with collapsible queue categories, expandable job cards, and lazy-loaded notification conversation history. **PHASE 1 - DATA MODEL**: Added `session_id` parameter to `SolutionSnapshot.__init__()` and `AgentBase.__init__()` for job-notification correlation. Updated 6 agent constructors in `todo_fifo_queue.py` to pass `session_id=websocket_id`. Extended `UserJobTracker` with `session_to_jobs`/`job_to_session` dicts and correlation methods. **PHASE 2 - API LAYER**: Added new endpoint `GET /api/get-job-interactions/{job_id}` (~130 lines) that returns job metadata + notifications within ±5 minute window of job execution. Enhanced `done_jobs_metadata` response with `session_id`, `agent_type`, `has_interactions` fields. **PHASE 3 - HTML**: Replaced flat `<ol>` queue lists with progressive disclosure structure - 4 `.queue-category` containers (TODO/Running/Done/Dead) with status indicators (🟡/🔵/✅/❌), count badges, expand toggles. **PHASE 4 - JAVASCRIPT**: Added `queueCategoryState` object, `expandedJobCards` Set, `jobInteractionsCache` Map to constructor. Added ~350 lines of new methods: `toggleQueueCategory()`, `loadQueueJobCards()`, `renderJobCard()`, `toggleJobCard()`, `toggleJobInteractions()`, `loadJobInteractions()`, `renderInteractionItem()`, `truncateText()`, `formatJobTimestamp()`, `escapeHtml()`. Updated `updateQueueLists()` to update count badges. **PHASE 5 - CSS**: Added ~300 lines for queue categories, job cards, interactions, status-based borders, agent badges, pulse animation for running status. **PHASE 6 - VERIFICATION**: All syntax checks passed (Python + JS), SolutionSnapshot.session_id test passed, UserJobTracker session mapping test passed. **FILES MODIFIED**: `solution_snapshot.py` (+2 lines), `agent_base.py` (+3 lines), `todo_fifo_queue.py` (+6 agent updates), `queue_extensions.py` (+40 lines), `queues.py` (+130 lines), `notifications.html` (~35 lines replaced), `notifications.js` (+350 lines), `notifications.css` (+300 lines). **PLAN FILE**: `/home/rruiz/.claude/plans/cheeky-leaping-scone.md`. **STATUS**: Implementation complete, awaiting manual browser testing. 📋🎨✅
>
> **🎯 PREVIOUS**: 2026.01.14 (Session 57 continued) - DEEP RESEARCH AGENT VOICE-FIRST UX IMPLEMENTATION! Critical UX update ensuring voice I/O is the PRIMARY interaction channel for the Deep Research Agent CLI. **THE PROBLEM**: CLI was using raw `input()` and `print()` calls, completely ignoring the voice I/O infrastructure built in Phase 1 (`cosa_interface.py`). User reported: "I didn't receive any notifications, the first clarification question popped up on the command line." **SOLUTION IMPLEMENTED**: Created `voice_io.py` abstraction layer (~280 lines) that tries voice first, automatically falls back to CLI text when voice unavailable. **NEW FILE `voice_io.py`**: (1) `set_cli_mode()` / `reset_voice_check()` - module state configuration, (2) `is_voice_available()` - checks voice service with cached result for session, (3) `get_mode_description()` - human-readable mode status, (4) `notify()` - progress notifications (voice-first, CLI fallback), (5) `ask_yes_no()` - binary questions with voice/CLI fallback, (6) `get_input()` - open-ended input with voice/CLI fallback, (7) `choose()` - multiple-choice with voice/CLI fallback. **CLI.PY UPDATES**: Added `--cli-mode` flag to force text-only interaction, replaced ALL `input()` calls with `voice_io.get_input()` and `voice_io.ask_yes_no()`, replaced ALL progress `print()` calls with `voice_io.notify()`. **__INIT__.PY UPDATES**: Added voice_io exports (`set_cli_mode`, `reset_voice_check`, `is_voice_available`, `get_mode_description`, `voice_notify`, `voice_ask_yes_no`, `voice_get_input`, `voice_choose`), version bumped from 0.2.0 to 0.2.1. **VERIFICATION TESTS PASSED**: voice_io smoke test, CLI mode import test, --cli-mode flag in dry-run. **FILES CREATED**: `src/cosa/agents/deep_research/voice_io.py` (~280 lines). **FILES MODIFIED**: `src/cosa/agents/deep_research/cli.py` (~50 lines), `src/cosa/agents/deep_research/__init__.py` (~20 lines). **PLAN FILE**: `/home/rruiz/.claude/plans/refactored-juggling-mitten.md`. **NOTE**: Will return to this in the morning for live testing with voice service. 🎤✅🔧
>
> **🎯 PREVIOUS**: 2026.01.14 (Session 57) - SEMANTIC SESSION NAMES FEATURE + BUG FIXES! Implemented comprehensive session naming system for notification UI with smarter auto-naming, localStorage persistence, inline editing, and LLM-powered gist generation. **INITIAL IMPLEMENTATION**: (1) localStorage persistence for session names (`SESSION_NAMES_KEY`, `sessionNames` Map), (2) `saveSessionName()` and `getSessionName()` methods, (3) Smarter auto-naming algorithm extracting action verbs + key nouns (e.g., "Fix Authentication Bug" instead of "Can you please help"), (4) Inline edit via `editSessionName()` with browser prompt, (5) Generate Gist button (✨) calling backend `/api/notifications/generate-gist` endpoint using `Gister` class. **CRITICAL BUG DISCOVERED**: Auto-naming returned "Session" fallback and gist button showed "No notifications" despite 17 notifications. **ROOT CAUSE**: Code accessed `group.notifications` which doesn't exist - notifications are stored in `group.dateGroups` (Map of date strings → notification arrays). **BUG FIXES APPLIED**: (1) Fixed `createSenderCard()` to flatten `group.dateGroups.values()` and sort by timestamp to get first message, (2) Fixed `generateSessionGist()` with same data access pattern. **UX ENHANCEMENTS**: (3) Made gist button look clickable with background/border/hover effects instead of transparent, (4) Replaced browser `prompt()` with voice-first edit modal - shows mic button + text input, auto-starts recording on open (audio-first UX). **FILES MODIFIED**: `notifications.js` (~200 lines: localStorage, smarter algorithm, data access fixes, voice-first modal with `showSessionNameEditModal()`, `closeSessionNameEditModal()`, `saveSessionNameFromModal()`), `notifications.css` (~150 lines: gist button styling, full modal CSS with mic button, input, footer buttons), `notifications.py` (+70 lines: `/api/notifications/generate-gist` endpoint using Gister). **R&D DOC CREATED**: `src/rnd/2026.01.14-semantic-session-names-design.md`. **PLAN FILE**: `/home/rruiz/.claude/plans/polymorphic-drifting-stallman.md`. **REMAINING FOR SESSION 58**: Gist button disable/working indicator, debug output for Gister, fix mic button to use canonical recording button styling. 🏷️✅🐛✅🎤
>
> **✅ COMPLETED**: MCP tools accessibility in Docker - dispatched Claude Code sessions can now use cosa-voice MCP tools (Session 54). Fixed 4 root causes: fastmcp not installed, PYTHONPATH missing from Docker MCP config, lupin_mcp/ directory not copied, conf/mcp/ directory not copied.
>
> **✅ COMPLETED**: Frontend timezone configuration - hardcoded `America/New_York` in notifications.js is acceptable for current single-timezone deployment.
>
> **✅ COMPLETED**: Choppy ElevenLabs audio - resolved with PCM 24000 streaming implementation (Session 46).
>
> **✅ COMPLETED**: Debug and test pulsing gold border during TTS playback - working correctly now.
>
> **✅ COMPLETED**: TTS pulsing gold border layout collapse bug - fixed with pseudo-element approach (Session 49). Animation now runs on ::before pseudo-element, completely isolated from layout. See `src/rnd/2026.01.09-debugging-css-layout-collapse.md` for root cause analysis.
>
> **✅ COMPLETED**: TTS Focus Mode race condition and action-required deferral bugs (Session 50). Fire-and-forget no longer plays during focus mode, action-required waits for current TTS to finish before displaying.
>
> **✅ COMPLETED**: Centralize voice-to-text recording button behavior and styling across notifications UI. (Session 47 continuation)
>
> **📋 TODO FOR NEXT SESSION (FUTURE)**: Revisit MessageHistory class for long-running SDK sessions. Current implementation uses simple truncation (`max_chars_per_message`) which works for short sessions but will lose important context in longer conversations. Future enhancements: LLM-based summarization, selective retention of key facts/decisions, token budget management, importance scoring. See plan file: `/home/rruiz/.claude/plans/declarative-riding-creek.md` (Future Work section).

> **🎯 CURRENT**: 2026.01.19 (Session 74b) - PROGRESSIVE NARROWING TEST HARNESS! Extracted and isolated the "progressive narrowing" phase from Deep Research CLI into standalone testable module. **THE PROBLEM**: The progressive narrowing phase (theme clustering → theme selection → topic refinement → final filtering) was embedded in `cli.py` (~125 lines, 309-434), making it difficult to test independently. **SOLUTION**: Created isolated test harness with mock support for testing without API calls. **FILES CREATED**: **1. `src/cosa/agents/deep_research/narrowing_mocks.py`** (~300 lines) - Mock API responses for testing: `MOCK_THEMES_3/4/6/1/EMPTY` canned responses, `SAMPLE_SUBQUERIES_5` (React vs Vue), `SAMPLE_SUBQUERIES_8` (Python vs Rust), `get_mock_theme_response(num_subqueries, variant)` function, `MockResearchAPIClient` class simulating API calls. **2. `src/cosa/agents/deep_research/narrowing_harness.py`** (~660 lines) - Core harness module: `NarrowingResult` dataclass (selected_themes, selected_topics, final_subqueries, phase_reached), `NarrowingHarness` class with methods for each phase (cluster_into_themes, select_themes, select_topics_within_themes, run_full_narrowing), CLI interface with flags (`--input`, `--output`, `--mock`, `--cli-mode`, `--auto-approve`, `--verbose`, `--debug`, `--phase`, `--sample`). **KEY DESIGN DECISIONS**: (1) **Dual-modality via voice_io.py**: Does NOT create separate mode management - delegates to existing `voice_io.py` for voice-first with CLI fallback. Only calls `voice_io.set_cli_mode(True)` when `--cli-mode` explicitly passed. (2) **Entry point smart detection**: `_has_cli_args()` helper checks ALL meaningful flags (input, output, mock, cli_mode, auto_approve, verbose, debug, phase, sample) before deciding smoke test vs CLI mode. (3) **Sample flag**: `--sample 5|8` selects between React/Vue (5 topics) or Python/Rust (8 topics) sample subquery sets for testing. **VERIFICATION**: Smoke tests pass for both narrowing_mocks.py and narrowing_harness.py. **FILES MODIFIED**: `src/cosa/agents/deep_research/__init__.py` (+exports for NarrowingHarness, NarrowingResult, get_mock_theme_response, MockResearchAPIClient; version 0.2.2). **CLI USAGE**: `PYTHONPATH="src:$PYTHONPATH" python -m cosa.agents.deep_research.narrowing_harness --verbose --mock --sample 8`. **STATUS**: Test harness complete and verified. 🔬✅
>
> **🎯 PREVIOUS**: 2026.01.19 (Session 74) - PODCAST GENERATOR BUG FIXES + SCRIPT RESUME FEATURE! Two-part session: first fixed 6 bugs discovered during initial testing, then implemented `--edit-script` feature for resuming script editing without regeneration. **PART 1 - BUG FIXES (6 issues)**: **Bug 1 (CRITICAL)**: `present_choices()` in `cosa_interface.py` received `abstract` parameter but never passed it to `NotificationRequest` - user couldn't see script preview before approving. **Fix**: Added `abstract=abstract, title=title` to NotificationRequest. **Bug 2**: "Other" response in review not handled - custom text fell through to script_approved=True. **Fix**: Added explicit handling for "Approve script", "Revise script", "Cancel", and else clause treating custom text as revision feedback. **Bug 3**: Unnecessary email sanitization replacing `@` with `_at_` in output paths. **Fix**: Removed `.replace("@", "_at_")` - user prefers clean email paths. **Bug 4**: Insufficient topic slug sanitization - only replaced spaces, left `:`, `?`, `/`. **Fix**: Added regex `re.sub(r'[^a-zA-Z0-9\s-]', '', topic)` plus hyphen collapsing. **Bug 5**: No file path in review abstract for full script access. **Fix**: Save draft BEFORE review, include path in abstract, delete on cancel. **Bug 6**: No CLI fallback for progress notifications. **Fix**: Created `voice_io.py` module (~300 lines) with dual-modality pattern (voice-first, CLI fallback), added `--cli-mode` flag. **PART 2 - SCRIPT RESUME FEATURE**: User wanted to debug/fine-tune revision workflow without regenerating scripts. **Implementation**: (1) Added `PodcastScript.from_markdown()` classmethod to `state.py` (~100 lines) - parses saved markdown back into PodcastScript with title, hosts, duration, segments, prosody extraction. (2) Added `--edit-script` CLI argument to `__main__.py`. (3) Added `from_saved_script()` classmethod + `do_review_only_async()` method to `orchestrator.py` (~150 lines) - loads script, skips phases 1-3, enters directly at review phase. **Design Decisions**: Standalone editing (no `--research` required), revision count continues from loaded value, new timestamp on save. **FILES CREATED**: `src/cosa/agents/podcast_generator/voice_io.py` (~300 lines). **FILES MODIFIED**: `cosa_interface.py` (+2 lines), `orchestrator.py` (+200 lines: bug fixes + new methods), `config.py` (+15 lines: slug sanitization + smoke test), `state.py` (+110 lines: from_markdown + smoke test), `__main__.py` (+80 lines: --edit-script + routing). **SMOKE TESTS**: 8/8 PASSED (config, state, prompts.script_generation, prompts.personality, cosa_interface, voice_io, api_client, orchestrator). **CLI USAGE**: `python -m cosa.agents.podcast_generator --edit-script path/to/script.md --user-id user@email.com`. **PLAN FILE**: `/home/rruiz/.claude/plans/mellow-chasing-tiger.md`. **STATUS**: Bug fixes complete, --edit-script feature implemented, awaiting manual end-to-end testing. 🎙️🐛✅🔧
>
> **🎯 PREVIOUS**: 2026.01.17 (Session 67) - FIX SENDER ID AND SESSION NAME SEPARATION FOR DEEP RESEARCH CLI! Comprehensive refactoring to separate sender ID (static for routing), session name (semantic for display), and session ID (technical identifier). **THE PROBLEM**: Three concepts were conflated: (1) sender_id included semantic topic (`deep.research@lupin.deepily.ai#cats-vs-dogs`), (2) `voice_io.notify()` didn't support `abstract` parameter, (3) `session_name` wasn't passed through to UI. **DESIGN DECISIONS**: (1) Sender ID is now static `#cli` suffix, (2) Session name generated with spaces ("cats vs dogs") for display, (3) Semantic topic derived from session name (hyphens for file naming), (4) `session_name` field added to notification models for UI to use. **CHANGES MADE**: **1. notification_models.py**: Updated sender_id regex pattern from `[a-z]+-[a-z]+-[a-z]+` to `[a-z]+(-[a-z]+)*` to allow simple identifiers like `#cli`. Added `session_name` field to both `NotificationRequest` and `AsyncNotificationRequest` with `to_api_params()` support. **2. voice_io.py**: Added `abstract` and `session_name` parameters to `notify()` function, passes through to cosa_interface. **3. cosa_interface.py**: Added `SESSION_NAME` module-level variable (auto-included in notifications). Updated `notify_progress()` to accept and use `abstract` and `session_name`. **4. cli.py**: Changed sender_id from `#semantic-topic` to static `#cli`. Now generates `session_name` with spaces using new prompt template. Derives `semantic_topic` by converting spaces to hyphens. Simplified `session_id` to `cli-{uuid8}`. Sets `cosa_interface.SESSION_NAME` for automatic inclusion. **5. research-session-name.txt (NEW)**: Prompt template that generates 3-5 word session names with spaces (not hyphens). **6. lupin-app.ini/splainer.ini**: Added config entry `prompt template for deep research session name`. **7. notifications.js**: Updated `addNotificationToSenderGroup()` to save server-provided `session_name` when present (takes priority over auto-generated). **DRY-RUN VERIFICATION**: Session name: "cats vs dogs", Semantic topic: "cats-vs-dogs", Sender: `deep.research@lupin.deepily.ai#cli`. **FILES CREATED**: `src/conf/prompts/agents/research-session-name.txt`. **FILES MODIFIED**: `src/cosa/cli/notification_models.py` (+15 lines), `src/cosa/agents/deep_research/voice_io.py` (+10 lines), `src/cosa/agents/deep_research/cosa_interface.py` (+10 lines), `src/cosa/agents/deep_research/cli.py` (~30 lines modified), `src/conf/lupin-app.ini` (+1 line), `src/conf/lupin-app-splainer.ini` (+1 line), `src/fastapi_app/static/js/notifications.js` (+8 lines). **STATUS**: Implementation complete, dry-run verified. 🏷️✅
>
> **🎯 PREVIOUS**: 2026.01.17 (Session 66) - FIX LITERAL `\n` IN ABSTRACT FIELD BREAKING MARKDOWN RENDERING! Quick bug fix session addressing issue where abstract field displayed literal backslash-n characters instead of line breaks. **THE PROBLEM**: When Claude generated `abstract="line1\nline2"` in MCP tool calls, the `\n` was transmitted as two literal characters (backslash + letter n), not as a newline character. This broke markdown-to-HTML transformation - bullet lists appeared as `**Header:**\n- Item 1\n- Item 2` with visible `\n`. **ROOT CAUSE**: MCP tool parameters are JSON strings. When Claude writes `\n` in a Python-style example, JSON doesn't interpret it as an escape sequence - it's already inside a parsed string value. **SOLUTION**: Belt-and-suspenders approach with two normalization layers. **FIX 1 - JAVASCRIPT DEFENSIVE LAYER**: Added `text = text.replace( /\\n/g, '\n' )` to `renderMarkdown()` function BEFORE markdown parsing. Handles all sources regardless of origin. **FIX 2 - MCP SOURCE NORMALIZATION**: Added `_normalize_abstract()` helper function after imports in `cosa_voice_mcp.py`. Converts literal `\\n` to actual newlines. Applied to all 4 MCP tools that accept `abstract` parameter: `converse()`, `notify()`, `ask_yes_no()`, `ask_multiple_choice()`. **VERIFICATION**: Sent test notification with multiline markdown abstract (headers, bold, lists, tables, blockquotes) - rendered correctly with proper line breaks. **FILES MODIFIED**: `src/fastapi_app/static/js/notifications.js` (+4 lines: normalization in renderMarkdown), `src/lupin_mcp/cosa_voice_mcp.py` (+17 lines: _normalize_abstract helper + 4 tool updates). **STATUS**: Fix complete and verified working. 📝✅
>
> **🎯 PREVIOUS**: 2026.01.17 (Session 65) - MARKDOWN RENDERING FOR ABSTRACT FIELD WITH XSS PROTECTION! Implemented marked.js and DOMPurify for safe markdown-to-HTML conversion in notification abstract fields. **THE PROBLEM**: Abstract field in action-required notifications was directly injected via innerHTML without escaping - XSS vulnerability. Also displayed raw markdown text (e.g., `**bold**` showed as literal asterisks) instead of rendered HTML. **SOLUTION**: Integrated marked.js (40KB) for markdown parsing and DOMPurify (23KB) for XSS sanitization. **NEW FILES**: `src/fastapi_app/static/js/vendor/marked.min.js`, `src/fastapi_app/static/js/vendor/purify.min.js`. **UTILITY FUNCTION**: Added `renderMarkdown(text)` method to NotificationsUI class (~75 lines) with: GFM support, `\n` → `<br>` conversion, explicit allow-list of safe tags (p, br, strong, em, ul, ol, li, a, code, pre, h1-h6, blockquote, hr, table), forbidden tags (script, style, iframe, form, input, img), event handler stripping, automatic `target="_blank" rel="noopener noreferrer"` on all links. Added `escapeHtml(text)` fallback if libraries unavailable. **ABSTRACT RENDERING UPDATES**: Action-required cards (line 8406) now use `this.renderMarkdown()`, history tooltips (line 4393) changed from `textContent` to `innerHTML` with `renderMarkdown()`. **CSS STYLING**: Added ~170 lines of markdown element styles for both `.action-required-abstract` and `.abstract-tooltip-content` - paragraphs, bold, italic, inline code, code blocks, lists, links, headings, blockquotes, horizontal rules, tables. **VERIFICATION**: Sent test notification with markdown content (bold, italic, lists, code blocks, tables, blockquotes) - all rendered correctly after browser refresh. **FILES CREATED**: `src/fastapi_app/static/js/vendor/marked.min.js` (40KB), `src/fastapi_app/static/js/vendor/purify.min.js` (23KB). **FILES MODIFIED**: `src/fastapi_app/static/html/notifications.html` (+3 lines: vendor script tags), `src/fastapi_app/static/js/notifications.js` (+109 lines: renderMarkdown, escapeHtml, updated abstract rendering), `src/fastapi_app/static/css/notifications.css` (+174 lines: markdown element styles). **STATUS**: Markdown rendering with XSS protection complete and tested. 📝✅🔒
>
> **🎯 PREVIOUS**: 2026.01.16 (Session 64) - DEEP RESEARCH AGENT: SEMANTIC SESSION IDs + RATE LIMIT ERROR HANDLING! Comprehensive enhancement session for Deep Research Agent CLI addressing two key issues from Session 59 testing. **ISSUE 1 - ANONYMOUS SESSION IDs**: Deep Research notifications showed `deep.research@lupin.deepily.ai` without topic context. User wanted semantic identifiers to identify research sessions. **SOLUTION**: Created 3-word hyphenated topic extraction using existing Gister class with custom prompt. New sender_id format: `deep.research@lupin.deepily.ai#python-javascript-comparison`. **IMPLEMENTATION**: Created `research-topic.txt` prompt template for LLM-based topic extraction, added config entry `prompt template for deep research session id generation`, integrated Gister call in `cli.py` before any notifications. **ISSUE 2 - RATE LIMIT CRASHES**: When rate limiter waited but still got 429, raw Anthropic error crashed research with no partial results. **SOLUTION**: Longer retry delays (30s initial for web search vs 1s default), partial results recovery with try/except around research loop, user-friendly error messages explaining 30k tokens/min limit vs ~80k tokens per web search. **BUG FIX - SENDER_ID REGEX**: Pydantic validation pattern only allowed 8 hex characters after `#` - rejected semantic topics like `#dogs-cats-comparison`. Updated regex from `(#[a-f0-9]{8})?` to `(#([a-f0-9]{8}|[a-z]+-[a-z]+-[a-z]+))?` to support both formats. **FILES CREATED**: `src/conf/prompts/agents/research-topic.txt` (3-word topic extraction prompt). **FILES MODIFIED**: `src/cosa/agents/deep_research/cli.py` (~100 lines: Gister integration, partial results recovery, rate limit error handling), `src/cosa/agents/deep_research/api_client.py` (~20 lines: `use_web_search` parameter for longer delays), `src/cosa/cli/notification_models.py` (2 lines: regex pattern at lines 214 and 575), `src/conf/lupin-app.ini` (1 line: new prompt template path), `src/conf/lupin-app-splainer.ini` (1 line: explainer for new config). **VERIFICATION**: User ran manual research session, semantic topic extraction working, notifications tagged correctly. **DEFERRED**: Where to store Deep Research results (local file vs GCP bucket). **PLAN FILE**: `/home/rruiz/.claude/plans/lexical-watching-kay.md`. **STATUS**: Semantic Session IDs and Rate Limit Error Handling implemented. 🏷️✅🛡️✅
>
> **🎯 PREVIOUS**: 2026.01.16 (Session 63) - MCP DOCUMENTATION FIX! Quick documentation fix session for cosa-voice MCP server. **THE ISSUE**: `ask_multiple_choice()` tool in `cosa_voice_mcp.py` had an `abstract` parameter but it wasn't documented in the docstring's Args section - other tools (`notify()`, `converse()`, `ask_yes_no()`) all documented it properly. **FIX 1 - DOCSTRING**: Added `abstract: Optional supplementary context (plan details, URLs, markdown)` to `ask_multiple_choice()` docstring at line 416. All four tools now have consistent `abstract` documentation. **FIX 2 - README.MD**: Updated `src/lupin_mcp/README.md` which was significantly outdated: (1) Added `ask_multiple_choice()` to Tools table (was completely missing), (2) Added new "Common Parameters" section documenting `priority` and `abstract` parameters, (3) Added `abstract` usage examples for `notify()`, `converse()`, `ask_yes_no()`, (4) Added complete `ask_multiple_choice()` example section, (5) Fixed code style to use spaces inside brackets per project conventions. **FILES MODIFIED**: `src/lupin_mcp/cosa_voice_mcp.py` (1 line: docstring fix), `src/lupin_mcp/README.md` (~50 lines: tool table, common params, examples). **STATUS**: Documentation fix complete, all cosa-voice tools now consistently documented. 📝✅
>
> **🎯 PREVIOUS**: 2026.01.16 (Session 62) - GRACE PERIOD MESSAGE BUG FIX! Quick bug fix session for action-required notification pause feature. **THE BUG**: Grace period message ("⏸️ Paused – 5-minute grace period added") was not appearing when pause button clicked. User reported clicking pause froze the timer but no message appeared. **ROOT CAUSE**: In `showGracePeriodMessage()` at line 9607, the card ID lookup was incorrect - used `action-required-card-${notificationId}` but all other methods use `action-required-${notificationId}` (no `-card` suffix). The DOM element lookup failed silently, message never created. **THE FIX**: Changed `action-required-card-${notificationId}` → `action-required-${notificationId}`. Single line fix. **VERIFICATION**: Sent test notification via cosa-voice MCP `ask_yes_no()`, clicked pause button, confirmed grace period message now appears correctly with amber styling. **FILES MODIFIED**: `notifications.js` (1 line: card ID fix at line 9607). **STATUS**: Bug fixed and verified working. ⏸️✅
>
> **🎯 PREVIOUS**: 2026.01.16 (Session 61) - HISTORY FILTER RACE CONDITION FIX + DROPDOWN UX + UI REORDER! Debugging session that uncovered and fixed a critical race condition in notification loading, plus several UX improvements. **THE BUG**: History filter showed inconsistent notification counts - API returned 52 notifications, but UI showed 8 on refresh (sometimes 36, sometimes 9). User observed counts changed between refreshes. **DIAGNOSIS**: Added `[HISTORY-DEBUG]` console.log statements to trace notification loading. Discovered: (1) `loadConversationHistory()` correctly loaded 56 notifications during `init()`, (2) WebSocket `auth_success` handler later called `loadInitialData()` → `loadInitialNotifications()`, (3) `loadInitialNotifications()` called `clearSenderGroups()` erasing all 56 notifications, (4) Then loaded only 9 from different endpoint (`/api/notifications/${userId}` vs conversation history endpoint). **ROOT CAUSE**: Race condition - `loadInitialNotifications()` was LEGACY code from before `loadConversationHistory()` existed. Both were running, with legacy code winning. **FIX**: Removed `loadInitialNotifications()` call from `loadInitialData()` with comment explaining why. **DROPDOWN UX BUG**: Clicking dropdown arrow to see filter options collapsed the entire notifications section! Root cause: Dropdown is inside section header with `onclick="toggleSection()"`, click bubbled up. **FIX**: Added `event.stopPropagation()` to both dropdown button and menu items. **SAME-VALUE GUARD**: Added early-return when selecting already-selected filter value - prevents unnecessary clear/reload. **REFRESH BUTTON**: Commented out the ↻ refresh button next to filter dropdown - was doing full page refresh, but filter dropdown now works correctly. **DEBUG CLEANUP**: Removed all 17 `[HISTORY-DEBUG]` console.log statements after bug was fixed. **UI REORDER**: Moved Queue Filter Settings section from after System Status to between Notifications and Job Queues. Reordered toolbar buttons to match (⚙️ now between 💬 and 📋). **FILES MODIFIED**: `notifications.js` (~50 lines: removed legacy load, added stopPropagation, added same-value guard, removed debug logs), `notifications.html` (~30 lines: commented refresh button, moved filter section, reordered toolbar). **STATUS**: All bugs fixed, UI reordered, clean code. 🐛✅🎨
>
> **🎯 PREVIOUS**: 2026.01.15 (Session 60) - NOTIFICATIONS UI MINOR TWEAKS! Three small UX improvements to the notifications system. **TWEAK 1 - ACTION-REQUIRED AUTO-EXPAND**: When a new action-required notification arrives, the section automatically becomes visible even if user had hidden it. Added `ensureActionRequiredExpanded()` method that handles both visibility mechanisms: (1) toolbar button visibility (`section-hidden` class on parent section), (2) header collapse (`collapsed` class on content div). Method removes appropriate classes, updates toolbar button active state, and persists to localStorage. Called from `addActionRequiredNotification()`. **TWEAK 2 - TTS QUEUE EMPTY STATE**: Added "🔇 Nothing in the queue" message when TTS queue section is visible but has no items playing. Added HTML element `#tts-queue-empty`, CSS styling `.tts-queue-empty-state`, updated `updateTTSQueueSection()` to show empty state when `totalCount === 0` AND toolbar wants section visible. Added hooks in `toggleSectionVisibility()` and `applySectionVisibility()` to update empty state when toolbar is toggled or page loads. **TWEAK 3 - CONFIG RELOAD BUTTON**: Added "↻ Reload" button to System Status section that calls `/api/init` endpoint to refresh configuration and reload solution snapshots without server restart. Button shows green ✓ on success, red ✗ with error message on failure. Added `reinitializeConfig()` async method with button disable during request, status span for feedback. **FILES MODIFIED**: `notifications.html` (+12 lines: TTS empty state div, Config reload button), `notifications.css` (+8 lines: tts-queue-empty-state styling), `notifications.js` (+100 lines: ensureActionRequiredExpanded, updateTTSQueueSection updates, reinitializeConfig method, toolbar hooks). **STATUS**: All three UI tweaks complete and tested. 🎨✅
>
> **🎯 PREVIOUS**: 2026.01.13 (Session 56 continued) - JOB QUEUE PROGRESSIVE DISCLOSURE UI DESIGN! Design exploration session for enhancing the Job Queues section in notification UI. **THE PROBLEM**: Jobs and notifications are disconnected - users can't see "what did Claude say to me about this job?" Jobs have `user_id` but no link to notification conversations; notifications have `sender_id` but no link to jobs. **SOLUTION DESIGNED**: Progressive disclosure UI with expandable queue categories (TODO/Running/Done/Dead), full job cards (question/status/timestamps/agent/runtime stats), and user interaction history showing notification conversations exchanged during job processing. **ARCHITECTURE**: 6-phase implementation plan covering data model (session_id linkage), API layer (new `/api/get-job-interactions/` endpoint), UI components (HTML progressive disclosure structure), JavaScript logic (~200 lines new methods), CSS styling (~150 lines following existing patterns), WebSocket integration (real-time updates). **PHASING OPTIONS**: MVP (~1 week, UI only with existing data) vs Full (~2-3 weeks, with interaction history backend). **FILES CREATED**: `src/rnd/2026.01.13-job-queue-progressive-disclosure-design.md` (comprehensive design doc). **PLAN FILE**: `/home/rruiz/.claude/plans/ethereal-drifting-thimble.md`. **STATUS**: Design complete, saved for future implementation per user request. 📋🎨✅
>
> **🎯 PREVIOUS**: 2026.01.13 (Session 56 continued) - CONVERSATION IDENTITY ARCHITECTURE PHASES 1-3 IMPLEMENTED! Comprehensive implementation enabling parallel Claude Code sessions to be distinguished in notification UI. **THE PROBLEM SOLVED**: Parallel sessions shared same sender_id (`claude.code@lupin.deepily.ai`), causing interleaved messages, ambiguous voice routing, merged conversation history. **NEW FORMAT**: `claude.code@{project}.deepily.ai#{session_id}` (e.g., `claude.code@lupin.deepily.ai#a1b2c3d4`). **PHASE 1 - SESSION IDENTITY (FOUNDATION)**: Added `SESSION_ID = uuid.uuid4().hex[:8]` generation at MCP startup, updated `_get_sender_id()` to append `#{session_id}`, updated regex pattern to accept both old and new formats (backward compatible), added `parse_sender_id()` helper for component extraction. **PHASE 2 - ACTIVE CONVERSATION ROUTING**: Added `get_active_conversation()` repository method (returns most recent sender_id for a recipient), added `get_sessions_for_project()` for listing sessions per project, added API endpoints `/notifications/active-conversation/{user_email}` and `/notifications/project-sessions/{project}/{user_email}`, added WebSocket broadcast of `active_conversation_changed` event after each notification. **PHASE 3 - NOTIFICATION UI UPDATES**: Added `parseSenderId()` JavaScript method (equivalent of backend helper), added `generateSessionName()` for auto-naming sessions from first message content, updated `createSenderCard()` to display session info (e.g., "LUPIN #a1b2c3d4 - Session Name") with active indicator (● green for active, ○ gray for inactive), added WebSocket handler for `active_conversation_changed` event with `handleActiveConversationChanged()` and `updateSenderActiveIndicator()` methods, added CSS styles for session identity display. **GLOBAL FIX**: Updated 18 occurrences of `senderId.replace(/[@.]/g, '-')` → `senderId.replace(/[@.#]/g, '-')` to handle `#` in new format. **FILES MODIFIED**: `cosa_voice_mcp.py` (+uuid import, SESSION_ID generation, updated _get_sender_id), `notification_models.py` (+regex update, +parse_sender_id helper), `notification_repository.py` (+get_active_conversation, +get_sessions_for_project), `notifications.py` (+2 API endpoints, +WebSocket broadcast), `notifications.js` (+parseSenderId, +generateSessionName, +handleActiveConversationChanged, +updateSenderActiveIndicator, updated createSenderCard, 18x replace pattern fixes), `notifications.css` (+session identity styles). **R&D DOC CREATED**: `src/rnd/2026.01.13-conversation-identity-phases-1-3.md` (full specification). **PLAN FILE**: `/home/rruiz/.claude/plans/tidy-yawning-brooks.md`. **TESTS PASSED**: parse_sender_id() with old/new formats, session_id generation, regex validation, all Python/JS syntax checks. **STATUS**: Phases 1-3 complete, ready for live UI testing. 🏗️✅
>
> **🎯 PREVIOUS**: 2026.01.13 (Session 56) - COSA DEEP RESEARCH AGENT QUEUE INTEGRATION DESIGN! Major refinement of the COSA Deep Research Agent implementation plan (`src/rnd/cosa-deep-research-agents.md`) to properly integrate with COSA's queue system. **PART 1 - NON-BLOCKING RUNNING QUEUE ARCHITECTURE**: Designed async/await concurrency model enabling concurrent job execution where jobs waiting for human input don't block other jobs. Added `JobSubState` enum (executing, waiting_for_human, waiting_for_subtasks, waiting_for_llm, paused) for jobs within RUNNING queue. Documented migration path for queue_consumer.py, running_fifo_queue.py, and agent_base.py to async. **PART 2 - TOP-LEVEL ORCHESTRATOR AGENT PATTERN**: Designed `ResearchOrchestratorAgent` as single queue entry that handles all orchestration internally. Added comprehensive `OrchestratorState` enum with 12 states (active: clarifying/planning/researching/gathering/synthesizing/citing, waiting: waiting_clarification/waiting_plan_approval/waiting_draft_review, terminal: completed/failed, control: paused/stopped). Implemented `async def do_all_async()` method showing full workflow with yield points at human feedback. **PART 3 - REST API DESIGN**: Documented 5 endpoints for orchestrator control: GET/POST state, pause, resume, stop, findings. **PART 4 - COSA-VOICE MCP INTEGRATION**: Updated to cosa-voice v0.2.0 with all 4 tools - `notify()` for progress, `ask_yes_no()` for binary decisions, `converse()` for open-ended questions, `ask_multiple_choice()` for menu selections. Added tool selection guide table. **PART 5 - IMPLEMENTATION LOCATION**: Defined complete directory structure at `src/cosa/agents/deep_research/` with 15 files organized into config, state, orchestrator, cosa_interface, prompts/, nodes/, tools/, and optional graph.py. **KEY DESIGN DECISIONS**: (1) Async/await chosen over thread pool or event-driven callbacks for FastAPI compatibility and scalability, (2) Single job in queue with multi-phase internal state machine, (3) All MCP calls are `await`ed yield points allowing other jobs to continue. **FILES MODIFIED**: `src/rnd/cosa-deep-research-agents.md` (+~500 lines: new Section 5 "COSA Queue Integration" with 5 subsections, updated Table of Contents). **PLAN FILE**: `/home/rruiz/.claude/plans/resilient-sleeping-wadler.md`. **STATUS**: Design documentation complete, ready for implementation. 🏗️📐✅
>
> **🎯 PREVIOUS**: 2026.01.12 (Session 55) - MULTIPLE-CHOICE NOTIFICATION UI ENHANCEMENTS + BUG FIX! Four improvements to action-required multiple-choice notifications. **ENHANCEMENT 1 - PROJECT BADGE**: Added source indicator badge `[LUPIN]`/`[COSA]`/`[PLAN]` to MC card header - user can now identify which project sent the question. Extracted from `sender_id` via existing `getProjectFromSenderId()` method. **ENHANCEMENT 2 - AUTO-FOCUS ACTION BUTTON**: When user clicks an option (radio/checkbox), the Next or Submit button automatically receives focus so Enter key advances/submits. Logic: `nextBtn || submitBtn` prioritizes Next on non-final questions. **ENHANCEMENT 3 - VISIBLE FOCUS RING**: Added blue glow focus indicator (`box-shadow: 0 0 0 3px rgba(0,123,255,0.4)`) to both `.mc-submit:focus` and `.mc-next:focus` - matches existing button focus patterns. **BUG FIX - DATETIME TIMEZONE MISMATCH**: Fixed 500 error on `/api/notify/response` with error "can't subtract offset-naive and offset-aware datetimes". Root cause: `datetime.utcnow()` (naive) compared to `expires_at` from database (aware). Fix: Changed to `datetime.now(timezone.utc)` at notifications.py:711. **FILES MODIFIED**: `notifications.js` (+15 lines: project badge extraction, action button focus logic), `notifications.css` (+12 lines: `.mc-project-badge` styling, focus ring for Next/Submit), `notifications.py` (1 line: datetime fix - in CoSA submodule, uncommitted). **COMMIT**: `d089147` - Session 55: Multiple-choice notification UI enhancements. **STATUS**: All UI enhancements complete and verified via MCP testing. 🎨✅🐛✅
>
> **🎯 PREVIOUS**: 2026.01.12 (Session 54+) - COSA UNIFIED AGENT ARCHITECTURE DESIGN! Comprehensive architecture exploration session covering agent patterns, conversation identity, and notification UI design. **PART 1 - AGENT ANALYSIS**: Created initial R&D document comparing COSA AgentBase vs Claude Code Dispatchers (Option A/B). Documented 5 commonalities (task/work unit, state tracking, result capture, serialization, callbacks) and 5 key differences (execution model, LLM integration, code execution, tool system, abstraction level). Recommendation: "Parallel with Shared Contracts" - keep systems separate but document shared behavioral patterns. **PART 2 - INTEGRATION PATTERNS**: Added code generation failure escalation pattern (catch `CodeGenerationFailedException` → dispatch to Claude Code as "rescue tier"), queue integration options for ClaudeCodeDispatcher tasks (recommended: extend RunningFifoQueue with `ClaudeCodeQueueTask` wrapper). **PART 3 - CONVERSATION IDENTITY ARCHITECTURE**: Major design work addressing fundamental UX problem - parallel Claude Code sessions in same project are indistinguishable in notification UI. **THE PROBLEM**: All sessions share same sender_id (`claude.code@lupin.deepily.ai`), causing interleaved messages, incorrect voice routing, and merged conversation history. **DESIGN DECISIONS** (confirmed via voice): (1) Unit of conversation = Process (each session/task gets unique ID), (2) Voice routing = Most recent sender, (3) Agent history = Full conversation history support. **NEW SENDER_ID FORMAT**: `{agent}@{project}.deepily.ai#{session_id}` (e.g., `claude.code@lupin.deepily.ai#a1b2c3`). **NOTIFICATION UI REDESIGN**: Nested accordion structure (project → sessions → dates) with active conversation indicator (⬤/○). **AGENTBASE ENHANCEMENT**: `ConversationContext` class for multi-turn history, optional `conversation_context` parameter, history injection into prompts. **5 IMPLEMENTATION PHASES DEFINED**: (1) Session Identity foundation, (2) Active Conversation Routing, (3) Notification UI Updates, (4) AgentBase History Integration, (5) Conversation Lifecycle management. **FILES CREATED**: `src/rnd/2026.01.12-cosa-unified-agent-architecture.md` (1,248 lines - renamed from dispatcher analysis doc). **FILES MODIFIED**: `src/rnd/README.md` (updated entry for renamed doc). **PLAN FILE**: `/home/rruiz/.claude/plans/cosmic-plotting-cherny.md`. **STATUS**: Architecture design complete, ready for phased implementation. 🏗️📐✅
>
> **🎯 PREVIOUS**: 2026.01.12 (Session 54) - MCP TOOLS IN DOCKER FIXED! Resolved issue where dispatched Claude Code sessions reported "I don't have a tool available to send notifications". **ROOT CAUSES FOUND (4 issues)**: (1) `fastmcp` module not installed in Docker - MCP server crashed on import, (2) `PYTHONPATH` missing from Docker MCP config - cosa.cli imports failed, (3) `src/lupin_mcp/` directory not copied to Docker image - MCP server script didn't exist, (4) `src/conf/mcp/` directory not copied - dispatcher's config path didn't exist. **FIXES APPLIED**: (1) Added `pip install fastmcp==2.14.2` to Dockerfile, (2) Added `PYTHONPATH=/var/lupin/src` to `docker/lupin/claude-config/mcp.json`, (3) Added `COPY src/lupin_mcp` to Dockerfile, (4) Added `COPY src/conf/mcp` to Dockerfile. **ADDITIONAL CHANGES**: Created `src/conf/mcp/cosa_mcp_docker.json` with hardcoded container paths, added Docker environment detection to dispatcher (`if lupin_root == "/var/lupin"`). **VERIFICATION**: Successfully dispatched task from UI Card - Claude called `notify("Hello from Docker")` and notification appeared in UI. Cost: $0.095, Duration: 9.8s. **FILES MODIFIED**: `docker/lupin/Dockerfile` (+3 lines), `docker/lupin/claude-config/mcp.json` (+1 env var), `src/cosa/orchestration/claude_code/dispatcher.py` (+4 lines Docker detection). **FILES CREATED**: `src/conf/mcp/cosa_mcp_docker.json`. **PLAN FILE**: `/home/rruiz/.claude/plans/logical-munching-unicorn.md`. **STATUS**: MCP tools now fully functional in Docker! 🐳🔧✅
>
> **🎯 PREVIOUS**: 2026.01.12 (Session 53) - CLAUDE CODE DISPATCHER DEBUG OUTPUT + OPTION A TESTING COMPLETE! Continued from Session 52 context compaction. **CLARIFICATION**: Docker CLI issue from Session 51 was ALREADY RESOLVED - Claude Code CLI is installed in Docker container. **OPTION A TESTING COMPLETE**: Tested bounded dispatch from UI Card - tasks stream responses, show completion status with cost. **DEBUG OUTPUT IMPROVEMENTS**: (1) Added granular debug to `claude_code.py` tracing message structure (assistant keys, message field types, content blocks), (2) Added TASK FINISHED banner and RESULT debug line to `dispatcher.py`, (3) Fixed stdout buffering with `PYTHONUNBUFFERED=1` env var in `start-docker-lupin.sh` - debug output now appears in real-time. **DOCKER PATTERN DOCUMENTED**: "Baked for Prod, Mounted for Dev" - Dockerfile COPY commands provide self-contained image for production, but development uses volume mounts (`-v`) that shadow baked-in code for live reload. **OPTION B STATUS**: DEFERRED - requires `claude-agent-sdk` which is not yet publicly available. Option B selection in UI will return expected error. **NEW ISSUE DISCOVERED**: Dispatched Claude Code sessions can't access MCP tools - Claude said "I don't have a tool available to send notifications" even though `mcp__cosa-voice__notify` is in `--allowedTools`. MCP server may not be connecting properly in Docker. **UI CHANGE**: Set Claude Code Dispatcher accordion to open by default (removed `collapsed` class). **FILES MODIFIED**: `dispatcher.py` (+8 lines: TASK FINISHED banner, RESULT debug), `claude_code.py` (+15 lines: granular message tracing), `start-docker-lupin.sh` (+1 line: PYTHONUNBUFFERED=1), `notifications.html` (accordion open by default). **PLAN FILE**: `/home/rruiz/.claude/plans/iterative-chasing-dove.md`. **STATUS**: Option A complete, Option B deferred, MCP issue identified for next session. 🐛✅⏸️
>
> **🎯 PREVIOUS**: 2026.01.12 (Session 52 + continuation) - USER-LEVEL MODE SYSTEM COMPLETE! Implemented stateful mode system allowing users to switch into agent-specific modes (math, calendar, weather, receptionist, todo, datetime) where all input bypasses LLM router and routes directly to selected agent. **IMPLEMENTATION COMPLETE**: (1) Backend mode state management in `todo_fifo_queue.py` - added `user_mode_state` dict, `MODE_TO_AGENT` mapping, `MODE_METADATA` for UI, methods `get_user_mode()`, `set_user_mode()`, `clear_user_mode()`, `get_available_modes()`. (2) Routing logic integration - modified `push_job()` to check mode BEFORE LLM router, bypasses routing when mode set. (3) Created new API router `src/cosa/rest/routers/mode.py` with 4 endpoints. (4) Registered router in `main.py`. (5) Added frontend mode selector UI in `notifications.html`. (6) Added JavaScript mode management in `notifications.js`. **BUG FOUND & FIXED**: Mode router incorrectly used `{user_email}` path parameter, but `push_job()` looks up by `current_user["uid"]`. Deep codebase analysis confirmed **system ID (uid) is canonical identifier** (15+ references). **FIX APPLIED**: Changed mode router endpoints from `/{user_email}` to `/current`, now extracts `current_user["uid"]` from auth token. Updated frontend to use `/api/mode/current` endpoints. User confirmed mode switching now working nominally. **FILES CREATED**: `src/cosa/rest/routers/mode.py` (268 lines), `src/rnd/2026.01.12-user-level-mode-system-design.md`. **FILES MODIFIED**: `todo_fifo_queue.py` (+150 lines), `main.py` (+2 lines), `notifications.html` (+15 lines), `notifications.js` (+100 lines, updated endpoints). **PLAN FILE**: `/home/rruiz/.claude/plans/cuddly-scribbling-waffle.md`. **STATUS**: Feature complete and verified working! 🎯✅

> **🎯 PREVIOUS**: 2026.01.09 (Session 51) - CLAUDE CODE DISPATCHER STREAMING FIXES! Tested Option A (Bounded) dispatch from UI Card - found 4 issues blocking streaming output. **ISSUE 1 - OUTPUT FORMAT**: Changed `--output-format json` (single final result) to `--output-format stream-json` (realtime streaming). **ISSUE 2 - SYNC SUBPROCESS**: Changed `subprocess.run()` (blocking) to `asyncio.create_subprocess_exec()` with async line-by-line streaming. **ISSUE 3 - BACKGROUND TASK**: Changed `background_tasks.add_task()` (doesn't await async) to `asyncio.create_task()` (proper async scheduling). **ISSUE 4 - CLI NOT IN DOCKER**: Claude Code CLI not installed in Docker container (`[Errno 2] No such file or directory: 'claude'`). Server runs at `/var/lupin` in Docker. **DOCUMENTATION**: Created comprehensive R&D document with trade-off matrix for 4 deployment options (Install CLI in Docker, Run FastAPI locally, Use Anthropic SDK, Hybrid API proxy). Recommendation: Option 1 (Docker CLI) for production, Option 2 (local FastAPI) for immediate testing. PostgreSQL accessible from local FastAPI via exposed port 5432. **FILES MODIFIED**: `dispatcher.py` (~80 lines: async streaming), `claude_code.py` (~50 lines: message handling + task scheduling + debug logging). **FILES CREATED**: `src/rnd/2026.01.09-claude-code-dispatcher-docker-deployment.md`. **PLAN FILE**: `/home/rruiz/.claude/plans/zesty-conjuring-manatee.md`. **STATUS**: Code complete, testing blocked by Docker environment. Next: test locally or install CLI in Docker. 🐳⏳
>
> **🎯 PREVIOUS**: 2026.01.09 (Session 50) - TTS FOCUS MODE BUG FIXES! Two critical bugs in notification TTS queue system. **BUG 1 - RACE CONDITION**: Fire-and-forget notifications were playing TTS while focus mode should be active (after action-required TTS completed). Root cause: `onTTSPlaybackComplete()` cleared `activeTTSItem` BEFORE entering focus mode, creating brief window where both guards were false. Fix: Enter focus mode FIRST, then clear `activeTTSItem`. **BUG 2 - ACTION-REQUIRED DEFERRAL**: Action-required notifications displayed immediately even while fire-and-forget TTS was playing - user wanted visual + audio synchronized. Fix: `addActionRequiredNotification()` now checks `activeTTSItem` and defers activation if TTS is playing, showing minimized indicator instead. When TTS completes, `onTTSPlaybackComplete()` activates deferred action-required notifications. **FILES MODIFIED**: `notifications.js` (~30 lines: race condition fix in `onTTSPlaybackComplete()`, deferred activation in `addActionRequiredNotification()`, deferred check in `onTTSPlaybackComplete()`). **PLAN FILE**: `/home/rruiz/.claude/plans/clever-gliding-fiddle.md`. **STATUS**: Both TTS queue bugs FIXED and tested. 🐛✅
>
> **🎯 PREVIOUS**: 2026.01.08 (Session 48) - ACTION-REQUIRED QUEUE SYSTEM + TIMESTAMP BUG FIXES! Implemented comprehensive queue system for action-required notifications: only ONE notification active at a time, others queued as minimized one-liners with position badges, timers deferred until activation, TTS only plays for active notification. **QUEUE SYSTEM**: Added `actionRequiredQueue` array and `activeActionRequiredId` tracking to constructor, new methods `activateNextNotification()`, `renderMinimizedNotificationDOM()`, `formatTimeoutDisplay()`, `showMinimizedTooltip()`, `recalculateQueuePositions()`, `playActivatedNotificationTTS()`. Modified `addActionRequiredNotification()` for deferred timers, updated `showConfirmation()` and `handleLocalTimeout()` for queue promotion. Updated localStorage persistence for queue state. **BUG FIX 1 - RESPONSE TIMESTAMP FORMAT**: Response messages showed "16:26" instead of "16:26 EST". Added `get_formatted_time_display()` helper to backend, added `time_display` to `/api/notify/response` endpoint and WebSocket broadcast. Updated frontend `addConversationPair()` to accept and use server `time_display`. **BUG FIX 2 - DATE GROUPING UTC**: Notifications appeared under wrong date near midnight (11:11 PM EST showed as Friday instead of Thursday). Root cause: `getTodayDateString()` used `toISOString()` which returns UTC. Added `get_formatted_date_display()` helper to backend, added `date_display` to response endpoint. Updated frontend with `getLocalDateDisplay()` fallback helper. **TODO ADDED**: Frontend timezone configuration - create `/api/config/client` endpoint for timezone config instead of hardcoded `America/New_York` in frontend fallback helpers. **FILES MODIFIED**: `notifications.py` (+60 lines: get_formatted_time_display, get_formatted_date_display, response endpoint updates), `notifications.js` (~400 lines: queue system, timestamp fixes), `notifications.css` (~130 lines: minimized cards, animations), `notifications.html` (~10 lines: active slot + pending queue structure). **PLAN FILE**: `/home/rruiz/.claude/plans/hazy-wishing-bachman.md`. **STATUS**: Queue system and both timestamp bugs FIXED. 🎯⏰🐛✅
>
> **🎯 PREVIOUS**: 2026.01.08 (Session 47 continued) - RECORDING BUTTON CONSOLIDATION + NOTIFICATION BUG FIXES! **RECORDINGMANAGER CREATED**: Unified 3 separate voice-to-text recording implementations into single `RecordingManager` class (~120 lines) with auto-cancel (starting new recording cancels previous), ESC key handling, timer display starting at "0/30s" (prevents button width change), and processing state management. Migrated Q&A recording, notification response recording, and multiple-choice "Other" recording. Removed ~100 lines of duplicate code. **CC DISPATCHER MIC BUTTON**: Added voice-first recording button to Claude Code Dispatcher card (button on LEFT of textarea). **CTRL+R REMOVED**: Keyboard shortcut removed - ambiguous with multiple recording contexts in same window. **UNIFIED CSS**: Created `.stt-button` class consolidating `.qa-stt-button`, `.response-mic-button`, `.mc-other-mic` styles. **BUG FIX 1 - WEBSOCKET TIMEZONE**: Added `_get_time_display()` method to `NotificationItem.to_dict()` generating "HH:MM TZ" format (e.g., "14:30 EST") for dynamically-inserted WebSocket notifications. **BUG FIX 2 - ACTION REQUIRED PERSISTENCE**: Added localStorage persistence for action-required notifications surviving page refresh. New methods: `saveActionRequiredState()`, `restoreActionRequiredState()`. Recalculates remaining timeout on restore. **FILES MODIFIED**: `notifications.js` (~300 lines: RecordingManager, migrations, persistence), `notifications.css` (~60 lines: unified .stt-button), `notifications.html` (CC Dispatcher mic button), `notification_fifo_queue.py` (+20 lines: _get_time_display). **PLAN FILE**: `/home/rruiz/.claude/plans/purring-painting-candy.md`. **STATUS**: Recording consolidation and both notification bugs FIXED. 🎤🐛✅
>
> **🎯 PREVIOUS**: 2026.01.07 (Session 46) - PCM 24000 STREAMING + TTFA FIXES + PULSING BORDER! Implemented smooth ElevenLabs audio playback using PCM 24000 format with Web Audio API. **PCM STREAMING**: Added `output_format=pcm_24000` to ElevenLabs WebSocket URL in `speech.py:861`. Frontend `notifications.js` now handles raw 16-bit PCM at 24kHz - converts PCM16 to Float32 (`float32[i] = pcm16[i] / 32768.0`), schedules audio chunks sequentially with precise timing (`nextStartTime = startTime + buffer.duration`). **TTFA BUG #1 - WRONG TIMING POINT**: Fixed Time To First Audio calculation - was measuring from AFTER fetch completed instead of before. Moved `this.startTime` and `this.metricsTTSStartTime` to BEFORE the fetch call (lines 2107-2109). **TTFA BUG #2 - RACE CONDITION**: Fixed duplicate TTFA logs (264ms AND 1108ms appearing). Root cause: `handleAudioComplete()` at line 2258 reset `firstChunkPlayed = false` while async `playPCMChunk()` was awaiting `arrayBuffer()`. Fix: Capture `isFirstChunk` state and `ttfaStartTime` BEFORE any awaits, set `firstChunkPlayed = true` immediately (lines 2311-2317). **DEFAULT TTS MODE**: Changed from "reliable" to "instant" in both JS constant and HTML select element. **PULSING BORDER FEATURE**: Added visual indicator during TTS playback - gold pulsing border around notification card. Created `ttsPulseGlow` CSS animation in `notifications.css:1175-1190`, added `.tts-playing` class. Added `startTTSPlayingIndicator()` and `stopTTSPlayingIndicator()` helper methods (lines 2736-2759). Fixed two bugs: (1) message divs had no ID attribute (line 3832), (2) `currentNotificationId` was null for incoming notifications (lines 2868-2873). **STATUS**: PCM streaming and TTFA fixes working perfectly. Pulsing border implemented but needs additional debugging - user reported notification text not visible until after playback finishes. 🎵🔧⏱️✨
>
> **🎯 PREVIOUS**: 2026.01.07 (Session 45 continued) - OPTION B SDK CLIENT: SESSION HISTORY MANAGEMENT COMPLETE! Implemented `MessageHistory` class to preserve conversation context across SDK `interrupt()` boundaries. **DISCOVERY**: SDK's `interrupt()` completely terminates sessions (Model C: Session Restart) - SYSTEM message appears twice proving Session 2 has zero memory of Session 1. **SOLUTION**: Track conversation history during Session 1, inject as context into Session 2's prompt. **DIRECTORY REORGANIZATION**: Created `src/cosa/orchestration/claude_code/` subpackage - moved `claude_code_dispatcher.py` → `dispatcher.py`, created `message_history.py`. **NEW CLASS**: `MessageHistory` with `set_original_prompt()`, `add_assistant_text()`, `add_user_message()`, `get_context_prompt()`. **DISPATCHER UPDATES**: `inject()` now has `preserve_context=True` parameter, history tracked via on_message callback, SessionInfo includes history reference. **TEST IMPROVEMENTS**: Weak verification (text matching) replaced with strong verification (notify() tool call inspection). Added `contains_notify_with()` to MessageTracker. Tests now require Claude to call notify() with MULTIPLE context pieces (e.g., Test 1: quantity AND missing info, Test 2: animal AND request type, Test 3: wrong city AND country). **VISIBILITY**: MessageTracker now displays notify() message content in output for visible proof. **FILES CREATED**: `src/cosa/orchestration/claude_code/__init__.py`, `message_history.py`. **FILES MODIFIED**: `dispatcher.py` (moved+updated), `orchestration/__init__.py` (re-exports), `test_option_b_inject_info.py` (robust verification). **PLAN FILE**: `/home/rruiz/.claude/plans/declarative-riding-creek.md`. **STATUS**: Implementation complete, ready for manual testing to verify context preservation works. 🧠✅

> **🎯 PREVIOUS**: 2026.01.07 (Session 45) - NOTIFICATION TIMESTAMP BUG FIXED! Resolved bug where fire-and-forget notifications were inserted into wrong date card (2026-01-06 instead of 2026-01-07). **ROOT CAUSE**: Multiple inconsistent timestamp sources - `get_current_datetime_raw()` in util.py used naive `dt.now()` which assumed system local timezone, then converted to target timezone incorrectly. **THE FIX (3 files)**: (1) `util.py:84` - Changed `dt.now()` → `dt.now(pytz.UTC)` to start with UTC-aware datetime before timezone conversion, (2) `notification_fifo_queue.py:87-88` - Fixed fallback to use `datetime.now(timezone.utc)`, (3) `notifications.js:5860` - Removed client-side timestamp override so responses inherit original notification's timestamp (keeps conversation together in same date card). **PLANNING SESSION**: Created comprehensive plan for 5 notification system improvements (timestamp fix, voice discovery, choppy audio, voice-to-text button unification). **TESTING**: Verified fix with medium and low priority fire-and-forget notifications - both correctly appear in today's date card. **PLAN FILE**: `/home/rruiz/.claude/plans/proud-enchanting-wadler.md`. **LESSON LEARNED**: During session-end, Claude committed and pushed without asking user approval via MCP tools - should have used `ask_yes_no()` or `ask_multiple_choice()` before git operations. **STATUS**: Timestamp bug FIXED and tested. 🐛✅

> **🎯 PREVIOUS**: 2026.01.06 (Session 44) - OPTION B SDK CLIENT: CONTINUOUS SESSION LOOP ARCHITECTURE! Resumed Phase B4 (Message Injection) testing from Session 37. **PROBLEM DISCOVERED**: After calling `interrupt()`, SDK closes the session - cannot call `query()` on disconnected client (CLIConnectionError). Previous approach of "interrupt then inject" fails because context manager exits when response loop ends. **SOLUTION IMPLEMENTED**: Continuous Session Loop architecture in `claude_code_dispatcher.py`. **NEW ARCHITECTURE**: (1) `_run_interactive()` now uses `while running` loop to stay connected, (2) `SessionInfo` TypedDict tracks client + pending_messages queue + running flag, (3) After response loop exits, checks `asyncio.Queue` for pending messages, (4) If pending messages exist, sends next message and continues loop. **REFACTORED METHODS**: `_run_interactive()` (major refactor - continuous loop), `inject()` (now queues messages instead of direct send), `interrupt()` (works with new session structure), `end_session()` (NEW - gracefully ends session by setting running=False). **SMOKE TEST UPDATED**: Added assertions for inject, interrupt, end_session methods. **FILES MODIFIED**: `src/cosa/orchestration/claude_code_dispatcher.py` (~100 lines changed). **PLAN FILE**: `/home/rruiz/.claude/plans/abstract-cuddling-hammock.md`. **STATUS**: Refactoring COMPLETE, needs manual testing to verify inject works with continuous session loop. 🔄🔧
>
> **🎯 PREVIOUS**: 2026.01.06 (Session 43) - ELEVENLABS TTS VOICE TESTING! Brief testing session to evaluate ElevenLabs TTS audio quality. Changed default voice from Rachel to Sam in `lupin-app.ini`. Audio quality was "choppy" during initial tests. **CONFIG CHANGED**: `elevenlabs tts default voice id` = `G7ILShrCNLfmS0A37SXS` (Sam). **STATUS**: Testing ongoing, investigating audio quality issues. 🎤🔧
>
> **🎯 PREVIOUS**: 2026.01.06 (Session 42) - SESSION SUMMARY LOST DUE TO CONTEXT COMPACTION. Session 42 work included: Action-Required TTS Fix (reads question/message aloud for high/urgent notifications), Priority Sound Updates (v2 cache-busting suffix, medium gets ding, low is silent). See commit `c565cb8` for details.
>
> **🎯 PREVIOUS**: 2026.01.06 (Session 41) - DIRECTORY RENAME POST-COMPLETION CLEANUP! Discovered Session 40 missed many `genie-in-the-box` references. **FIXED 25+ FILES**: Root settings (`.claude/settings.local.json` - 12 bash allowlist entries), legacy util (`io/lib/utils/util.py` - `GENIE_IN_THE_BOX_ROOT` → `LUPIN_ROOT`, `GENIE_IN_THE_BOX_TGI_SERVER` → `LUPIN_TGI_SERVER`), notebook helper (`src/ephemera/notebooks/cosa/gsm8k.py`). **COSA SUBMODULE (20+ files)**: Test scripts (`run-cosa-unit-tests.sh`, `run-cosa-smoke-tests.sh` - PYTHONPATH_BASE), test files (`test_quantizer.py`, `test_hf_downloader.py`, `test_xml_parsing_baseline.py` - hardcoded paths), 9 `.claude/commands/*.md` files, `orchestration/README.md`, `training/README.md`, `repo/README-branch-analyzer.md`, `CLAUDE.md`, `CLAUDE.local.md`, 3 `rnd/prompts/*.md` files. **OTHER SUB-REPOS**: `src/.claude/settings.local.json` (mobile), `src/lupin-plugin-firefox/README.md`, `src/lupin-mobile/ios/Flutter/flutter_export_environment.sh`. **ENV VARS RENAMED**: `GENIE_IN_THE_BOX_ROOT` → `LUPIN_ROOT`, `GENIE_IN_THE_BOX_TGI_SERVER` → `LUPIN_TGI_SERVER`, `GIB_CONFIG_MGR_CLI_ARGS` → `LUPIN_CONFIG_MGR_CLI_ARGS`. **PLAN DOC UPDATED**: Marked all phases complete, added Session 41 cleanup items to checklist. **INTENTIONALLY SKIPPED**: `io/log/*.json` (legacy artifacts), historical planning docs in `src/cosa/rnd/2025.*.md`, Flutter build artifacts (auto-regenerated). **STATUS**: Directory rename NOW TRULY COMPLETE - all active code/config updated. 📁✅✅
>
> **🎯 PREVIOUS**: 2026.01.06 (Session 40) - DIRECTORY RENAME PART D COMPLETE! Final phase of the `genie-in-the-box` → `lupin` rename. **EXTERNAL CONFIG UPDATES**: Updated `~/.claude.json` (10+ path entries via sed), updated `~/.notifications/config` (2 path entries). **DEPRECATED SCRIPTS DELETED**: Removed `~/.local/bin/notify-claude-async` and `notify-claude-sync` per user request (MCP server now handles notifications). **VENV RECREATED WITH DEPENDENCY RESOLUTION**: Complex numpy 1.x vs 2.x ecosystem conflict resolved. **DEPENDENCY CHANGES**: Removed `auto_gptq==0.7.1` (archived April 2025, unused - only format string reference in quantizer.py), upgraded `auto_round==0.4.6` → `0.9.4`, downgraded `numpy==2.2.5` → `1.26.4` (auto-round requires <2.0), downgraded `spacy==3.8.7` → `3.7.6`, `thinc==8.3.6` → `8.2.5`, `blis==1.3.0` → `0.7.11` (thinc 8.2.5 requires numpy<2.0), added `bcrypt==5.0.0`, `psycopg2-binary==2.9.11`, `fastmcp==2.14.2`. **DOCKER VERIFIED**: docker-compose.yml uses relative paths (`./src/conf/...`), no hardcoded paths requiring update. **MCP SERVER**: Re-registered cosa-voice with correct PYTHONPATH/LUPIN_ROOT env vars - now shows "Connected". **INTEGRATION TESTS**: 63 passed, 9 failed, 37 errors (pre-existing test infrastructure issues, not from rename). **KEY DISCOVERY**: auto-gptq is ARCHIVED (April 2025), successor is gptqmodel which requires numpy>=2.2 - mutually exclusive with Intel's auto-round ecosystem. **FILES MODIFIED**: `src/cosa/requirements.txt` (~15 changes), `~/.claude.json`, `~/.notifications/config`, `.mcp.json`. **STATUS**: Directory rename Parts A-D complete (cleanup in Session 41). 📁✅
>
> **🎯 PREVIOUS**: 2026.01.06 (Session 39) - DIRECTORY RENAME PART A COMPLETE! Executed Phase 2 of the directory rename plan (`genie-in-the-box` → `lupin`). **SCOPE**: Updated all internal file references BEFORE the actual directory rename. **SHELL SCRIPTS (8 files)**: `backup.sh`, `sync-to-backup.sh`, `run-postgresql-dev.sh`, `run-gsm8k.sh`, `run-lupin-gui.sh`, `download-from-huggingface.sh`, `install-mcp-server.sh`, `run-integration-tests.sh`. **PYTHON FILES (12 files)**: `cosa_voice_mcp.py` (detection logic), `main.py`, `conftest.py`, `test_api_key_generation.py`, 3 lupin_smoke tests, 5 integration/manual tests, 2 migration scripts. **DOCUMENTATION (~35 files)**: R&D docs, prompts, CLAUDE.md, .claude/commands/, history files. **DOCKER (2 files)**: openllm/Dockerfile, whisper/Dockerfile. **JSON BASELINES (2 files)**: test_baseline.json service name. **SKIPPED**: 22 Jupyter notebooks in `src/ephemera/notebooks/` (legacy artifacts per plan). **COMMIT**: `17a3a4b` - 58 files changed, 154 insertions/deletions. **STATUS**: Part A complete. 📁🔄
>
> **🎯 PREVIOUS**: 2026.01.05 (Session 38) - MULTIPLE-CHOICE QUESTION NOTIFICATIONS - ALL PHASES COMPLETE! Implemented full support for Claude Code's `AskUserQuestion` tool with multi-question flows. **PHASE 4 - MULTI-QUESTION NAVIGATION**: Added state tracking (`currentQuestionIndex`, `collectedAnswers`), navigation buttons ("← Back", "Next Question →", "Submit All ✓"), answer preservation when navigating between questions, event handler reattachment after DOM re-renders. New methods: `getCurrentQuestionAnswer()`, `saveCurrentQuestionAnswer()`, `navigateMultipleChoice()`, `rerenderMultipleChoiceUI()`, `attachMultipleChoiceEventHandlers()`, `submitAllMultipleChoiceAnswers()`. **PHASE 5 - MCP VOICE TOOL**: Added `ask_multiple_choice()` tool to cosa_voice_mcp.py (v0.2.0). Tool accepts questions in Claude Code's camelCase format, formats TTS-friendly message with "Question N of X" prefix via `_format_questions_for_tts()`, converts to API snake_case via `_convert_questions_format()`, parses JSON response via `_parse_multiple_choice_response()`. **BUG FIXES (Phase 3 polish)**: (1) Mic button styling - removed custom CSS overrides so button inherits from `.response-mic-button` and grows with timer text, (2) Timer display - shows "🔴 0/30s" immediately on recording start instead of just "🔴", (3) Radio button centering - changed from `margin-top: 2px` to `align-self: center`. **FILES MODIFIED**: `src/fastapi_app/static/js/notifications.js` (~200 lines: Phase 4 navigation + timer fix), `src/fastapi_app/static/css/notifications.css` (~20 lines: styling fixes + .mc-back button), `src/lupin_mcp/cosa_voice_mcp.py` (+180 lines: ask_multiple_choice tool + helpers). **PLAN FILE**: `/home/rruiz/.claude/plans/polished-exploring-beacon.md` - all phases marked complete. **STATUS**: Multiple-choice notification system fully implemented end-to-end. 🎯✅
>
> **🎯 PREVIOUS**: 2026.01.05 (Micro-Session) - NOTIFICATIONS DATE CARD BUG FIX! Fixed bug where live WebSocket notifications were inserted into wrong date cards (e.g., previous year's card instead of today). **ROOT CAUSE**: `getDateString()` in `notifications.js` used JavaScript's local timezone methods (`.getFullYear()`, `.getMonth()`, `.getDate()`) which convert timestamps to the browser's local timezone instead of preserving the server's America/New_York timezone. **THE FIX**: Added `extractDateFromTimestamp()` helper method that extracts the date directly from ISO timestamp strings (e.g., `"2026-01-06T02:30:00-05:00"` → `"2026-01-06"`) without any timezone conversion. Updated `addNotificationToSenderGroup()` to use new helper. **FILES MODIFIED**: `src/fastapi_app/static/js/notifications.js` (+13 lines: new helper method at line 3426, updated date extraction at line 3360). **STATUS**: Bug fixed, live notifications now insert into correct date card. 🐛✅
>
> **🎯 PREVIOUS**: 2026.01.05 (Session 37) - OPTION B PHASE 1 MANUAL TESTING COMPLETE! Successfully validated all interaction modalities for SDK Client interactive sessions. **TEST SCRIPT CREATED**: `src/tests/manual/test_option_b_interactive.py` - comprehensive manual test for notify(), converse(), and combined sequences with streaming message visualization. **BUGS FIXED**: (1) `response_options` JSONB column missing from PostgreSQL notifications table - added via direct ALTER TABLE, (2) Redundant `import json` inside `if` block in `notifications.py:280` causing closure scoping error ("free variable 'json' referenced before assignment") in SSE event generator - removed duplicate import, (3) MessageTracker not counting nested blocks inside AssistantMessage/UserMessage SDK containers - added counter increments for nested blocks. **MESSAGE TRACKER ENHANCED**: Updated to display detailed content for AssistantMessage (TEXT + TOOL blocks), UserMessage (RESULT blocks with content preview), SystemMessage, and proper nested block counting. **TEST RESULTS**: All modalities verified working - notify() sends TTS announcements, converse() blocks and waits for voice response, streaming messages display in real-time with proper timestamps. **STATUS**: Phase 1 complete, Phase 2 (inject/interrupt testing) ready to begin. **FILES CREATED**: `src/tests/manual/test_option_b_interactive.py` (290 lines). **FILES MODIFIED**: `src/cosa/rest/routers/notifications.py` (removed duplicate import), `src/rnd/2026.01.02-02-option-b-sdk-client-planning.md` (updated status). 🎤✅🐛✅
>
> **🎯 PREVIOUS**: 2026.01.05 (Session 36) - DIRECTORY RENAME PLANNING COMPLETE! Comprehensive analysis and planning for renaming project directory from `lupin` to `lupin`. **EXPLORATION**: Launched 3 parallel Explore agents to analyze: (1) all internal references to "lupin" (76+ files found), (2) external/system configurations (~/.claude.json, ~/.local/bin/notify-*, ~/.notifications/config), (3) path-related risks (nested git repos, Docker configs, virtual environments). **KEY FINDINGS**: 35 files with full hardcoded paths, 41+ with partial references, 6 external config files, 2 virtual environments needing recreation, 26 Jupyter notebooks (leave as legacy). **DECISIONS MADE**: (1) Rename both DATA01 and DATA02 directories, (2) Update run-gsm8k.sh (GENIE_IN_THE_BOX_ROOT → LUPIN_ROOT), (3) Leave Jupyter notebooks as legacy artifacts, (4) 100% removal of all "genie" naming. **PLAN CREATED**: `src/rnd/2026.01.05-directory-rename-genie-to-lupin-plan.md` (344 lines) with 6 phases, 24-step checklist, rollback plan, ~90 min estimated execution. **RnD README UPDATED**: Added entry for directory rename plan. **STATUS**: Planning complete, execution scheduled for next session. 📋🔄

> **🎯 PREVIOUS**: 2026.01.05 (Session 35) - HISTORY MANAGEMENT + MAJOR ARCHIVE! Ran `/plan-session-start` and `/plan-history-management` workflows. **ARCHIVE EXECUTED**: History.md was at 20,086 tokens (80% of 25k limit) - reduced to 3,669 tokens (15%). **ARCHIVE CREATED**: `history/2025-10-16-to-11-22-history.md` containing Sessions 1-6 (Oct 16 - Nov 22, 2025) covering Admin Dashboard, LanceDB multi-backend storage, PostgreSQL migration, and Cloud Run deployment work. **SESSIONS RETAINED**: Sessions 7-34 (Nov 23, 2025 - Jan 3, 2026) remain in main history.md with sequential numbering preserved. **TOKEN REDUCTION**: 82% reduction (16,417 tokens archived). **WORKFLOW USED**: Canonical history-management.md from planning-is-prompting repo applied to LUPIN project with project-specific configuration. **FILES CREATED**: `history/2025-10-16-to-11-22-history.md` (92 lines, comprehensive archive with navigation links). **FILES MODIFIED**: `history.md` (reduced from 720 lines to 78 lines, updated navigation section with new archive link). **History Health**: ✅ ~3.7k tokens (15%) - very healthy after archive. 📚✅

> **🎯 PREVIOUS**: 2026.01.03 (Session 34) - NOTIFICATIONS UI POLISH! Five improvements to action-required notifications and response handling. **FIX 1 - SENDER CARD ORDERING**: Added `&& !this.isInitialLoad` check at line 3402 to prevent `moveSenderCardToTop()` during initial load - preserves API sort order (newest first). **FIX 2 - GENIE ANIMATION REPLACED**: Removed flying bezier animation, replaced with simple shrink-fade in place. Card shrinks to 50% and fades to 0% opacity, then disappears. Simpler, cleaner UX. **FIX 3 - RESPONSE HIGHLIGHT**: Added gold pulsing glow (`responsePulseGlow` keyframe) on user's response message after action-required submission - 3 pulses over 1.8s to highlight where response now "lives" in conversation. Initial blue glow was invisible against blue backgrounds, changed to gold. **FIX 4 - CARD COLOR**: Changed action-required section from "heinous orange" (#fd7e14) to blue (#0d6efd) matching existing accent color. Updated border, background (#e7f1ff), header, and box-shadow. **FIX 5 - EMPTY STATE PADDING**: Reduced "No pending actions" vertical padding from 20px to 10px (50% reduction). **FILES MODIFIED**: `notifications.js` (sender ordering line 3402, shrink-fade animation lines 5163-5206, response highlight lines 5773-5781), `notifications.css` (shrinkFadeOut keyframe, responsePulseGlow keyframe, .shrink-fade class, blue color scheme 5 locations, empty state padding). **STATUS**: All UI polish complete and tested via MCP voice notifications. 🎨✅

> **🎯 PREVIOUS**: 2026.01.03 (Session 33) - PYTHON ENVIRONMENT BLOCKER RESOLVED + CLAUDECODEDISPATCHER RENAME + MCP VOICE TOOLS E2E TESTED! Resolved Session 31 Python environment blocker - venv with `claude-agent-sdk` works correctly with `PYTHONPATH="$PWD/src:$PYTHONPATH"`. **RENAME**: `CosaDispatcher` -> `ClaudeCodeDispatcher` across 3 production files (`claude_code_dispatcher.py`, `__init__.py`, `test_dispatcher_e2e.py`) - 18 total occurrences replaced. All 9 smoke tests passing. **MCP VOICE TOOLS E2E TESTED FROM BOUNDED TASKS**: (1) `notify()` - fire-and-forget notification ($0.20, 12.4s), (2) `converse()` - open-ended question with blocking wait User answered "English" ($0.06, 23.0s), (3) `ask_yes_no()` - boolean question User answered "Yes" ($0.13, 16.7s). **WORKING ENVIRONMENT PATTERN**: `PYTHONPATH="$PWD/src:$PYTHONPATH" LUPIN_ROOT="$PWD" MCP_PROJECT="lupin" src/cosa/.venv/bin/python`. **STATUS OVERVIEW CREATED**: Comprehensive project status in `/home/rruiz/.claude/plans/clever-napping-clover.md` - MCP Server 100%, Option A 100%, Option B 0% (deferred), Cold Call 0% (now unblocked). **FILES MODIFIED**: `src/cosa/orchestration/claude_code_dispatcher.py` (8 renames), `src/cosa/orchestration/__init__.py` (4 renames), `src/tests/integration/test_dispatcher_e2e.py` (6 renames). **NEXT SESSION**: Cold Call Flow implementation now unblocked (Option A works!), or documentation updates. 🎤✅🔧

> **🎯 PREVIOUS**: 2026.01.02 (Session 32) - VOICE-FIRST UX + ACTION REQUIRED CARD BUG FIXES! Comprehensive UI improvements for notifications system with focus on voice-first experience and scroll behavior fixes. **VOICE-FIRST UX CHANGES**: (1) Reordered open-ended card HTML layout - mic button now FIRST (leftmost) before text input, (2) Auto-focus mic button with `preventScroll: true` on render, (3) Added Enter/Space keyboard handler for voice activation, (4) Added focus styling (blue glow) for mic button. **BUG 1 - NOTIFICATION RENDERING AFTER REFRESH**: Fixed issue where after page refresh, original question was right-aligned (as if user's response) and actual response was missing. Root cause: single DB record stores both question and response, frontend checked `state === 'responded'` to determine alignment. Fix: Always render original notification as incoming (left), then if `response_value.value` exists, render as separate outgoing message (right). **BUG 2 - GENIE ANIMATION OPACITY**: Changed end opacity from 0 (invisible) to 0.5 (50% visible) so user can see where card animates to. Updated JS keyframes (line 5660) and CSS keyframes (genieShrink line 1130, simpleFadeOut line 1141). **BUG 3 - SCROLL BEHAVIOR**: (A) Removed initial scroll before animation to prevent double-scroll, (B) Added post-animation scroll to sender card after DOM changes (`moveSenderCardToTop` was shifting layout), (C) Added scroll to action-required-section when notification card appears (with 50ms delay for DOM settling). **FILES MODIFIED**: `notifications.js` (~8 locations: HTML reorder, focus changes, keyboard handler, scroll fixes), `notifications.css` (3 locations: mic button focus styling, genieShrink/simpleFadeOut opacity). **STATUS**: All UI improvements complete and tested via MCP voice notifications. 🎤✅🐛✅

> **🎯 PREVIOUS**: 2026.01.02 (Session 31) - OPTION A DISPATCHER IMPLEMENTATION COMPLETE + PYTHON ENVIRONMENT BLOCKER! Implemented Claude Code bounded task dispatcher with voice I/O via MCP tools. **COMPLETED**: Phases A1-A4 of Option A implementation. Moved dispatcher from R&D prototype (`src/rnd/2025.12.31-claude-code-via-mcp-and-cosa-vox/cosa_dispatcher.py`) to production (`src/cosa/orchestration/`). **FILES CREATED**: `src/cosa/orchestration/__init__.py` (34 lines), `src/cosa/orchestration/claude_code_dispatcher.py` (651 lines with inline smoke tests), `src/cosa/orchestration/README.md` (128 lines), `src/tests/integration/test_dispatcher_e2e.py` (370 lines). **KEY CHANGES**: (1) Tool names updated `mcp__cosa__*` -> `mcp__cosa-voice__*`, (2) Environment variable `COSA_PROJECT` -> `MCP_PROJECT`, (3) Working directory logic fixed to use `LUPIN_ROOT` directly instead of `parent/{project}`, (4) `claude-agent-sdk==0.1.18` added to requirements.txt. **TESTING**: 9/9 smoke tests passing, 11/11 pytest unit/mocked tests passing. **SUCCESSFUL E2E TEST**: Bounded task ran successfully via miniconda Python - listed files in orchestration directory (Cost: $0.19, Duration: 8.6s). **BLOCKER RESOLVED**: Python environment confusion resolved in Session 33. 🔧✅

> **🎯 PREVIOUS**: 2026.01.02 (Session 30) - NOTIFICATION SENDER CARD ORDERING BUG FIXED! Resolved bug where project/sender cards displayed in wrong order after page refresh (CoSA with 14-hour-old activity showed before Lupin with recent activity). **TWO-LAYER FIX**: (1) Backend SQLAlchemy `ORDER BY` used string literal `'last_activity'` instead of actual expression - fixed `notification_repository.py` lines 153 and 674 to use `desc(func.max(Notification.created_at))`. (2) Frontend `createSenderCard()` used `insertBefore(card, container.firstChild)` which prepended each card, reversing API order during initial load. **SOLUTION**: Added `insertAtTop` parameter to `createSenderCard()` method with class-level `isInitialLoad` flag. During initial load (`loadConversationHistory()`), cards are appended to preserve API sort order; at runtime, cards are prepended so new activity appears at top. **FILES MODIFIED**: `notifications.js` (+3 locations: constructor line 69 adds `isInitialLoad` flag, `loadConversationHistory()` lines 3851-3856 sets flag around load loop, `addNotificationToSenderGroup()` line 3375 passes `!this.isInitialLoad` to `createSenderCard()`). Also includes earlier session work on SQLite->PostgreSQL migration completion and unit test updates. **STATUS**: Bug fixed, sender cards now display in correct order (most recent activity first). 🐛✅

> **🎯 PREVIOUS**: 2026.01.02 (Session 29) - MCP VOICE INTEGRATION DOCUMENTATION COMPLETE! Created comprehensive documentation plan chronicling implementation trajectory and roadmaps for remaining work. **KEY DISCOVERY**: `cosa_dispatcher.py` (419 lines) in `src/rnd/2025.12.31-claude-code-via-mcp-and-cosa-vox/` contains COMPLETE implementations for both Option A (Print Mode) and Option B (SDK Client), but was never moved to production, integrated, or tested! **5 DOCUMENTS CREATED**: (1) `2026.01.02-00-mcp-voice-implementation-trajectory.md` - Design-to-implementation journey, gap analysis matrix, code inventory (MCP Server 372 lines complete, dispatcher 419 lines unintegrated), technical debt inventory, recommendations. (2) `2026.01.02-01-option-a-print-mode-planning.md` - Roadmap for bounded task execution (`claude -p "task"` subprocess pattern), 5 phases (A1-A5). (3) `2026.01.02-02-option-b-sdk-client-planning.md` - Roadmap for interactive SDK Client sessions, deferred pending `claude-agent-sdk` availability. (4) `2026.01.02-03-cold-call-flow-planning.md` - Roadmap for voice-initiated sessions (wake word, intent parsing, voice daemon), 6 phases (C1-C6). (5) Updated `src/rnd/README.md` with MCP project entries linking all documents. **GAP ANALYSIS**: MCP Server complete, Option A code exists but unintegrated, Option B code exists but SDK unavailable, Cold Call not started. **STATUS**: Documentation complete, Option A ready to implement when prioritized. 📚✅

> **🎯 PREVIOUS**: 2026.01.02 (Session 28) - MCP VOICE INTEGRATION PHASE 4-5 COMPLETE! E2E testing successful for Claude Code voice notifications via MCP. **DYNAMIC PROJECT DETECTION**: Added `_detect_project_from_cwd()` function that auto-detects project from working directory (lupin/lupin -> "lupin", planning-is-prompting -> "plan", cosa standalone -> "cosa"), falls back to `MCP_PROJECT` env var, then "unknown" with prominent console warning. Eliminates need for per-project MCP registrations - one server works for all projects. **MCP REGISTRATION FIX**: Discovered Claude Code stores MCP servers in `~/.claude.json` (NOT `~/.claude/settings.json`). Correct command: `claude mcp add cosa-voice --transport stdio ...`. Updated installation script with `--transport stdio` flag. **E2E TESTING PASSED**: All 4 MCP tools working: `notify()` (fire-and-forget), `converse()` (blocking), `ask_yes_no()` (boolean), `get_session_info()` (session details). Manual testing confirmed notifications appear in Lupin UI with correct sender_id based on auto-detected project. **FILES MODIFIED**: `src/lupin_mcp/cosa_voice_mcp.py` (+50 lines), `src/scripts/install-mcp-server.sh` (+1 line), `src/rnd/2026.01.02-mcp-e2e-test-instructions.md` (NEW). **STATUS**: MCP Voice Integration ALL 5 PHASES COMPLETE. Production-ready for multi-project Claude Code voice notifications. 🎤✅

> **🎯 PREVIOUS**: 2026.01.01 (Session 27) - FRESH QUEUE -> NOTIFICATIONS UI REFACTORING COMPLETE! Major UI overhaul renaming "Fresh Queue" to "Notifications" with date-based grouping. **PHASE 1 - DATABASE**: Added `is_hidden` column to notifications table for soft delete, created migration SQL with partial indexes for efficient visible-only queries. **PHASE 2 - BACKEND API**: Added 4 new endpoints: `GET /conversation-by-date` (date-grouped notifications), `DELETE /date/{sender}/{user}/{date}` (soft delete), `GET /sender-dates` (date summaries), `GET /senders-visible` (visible senders with new counts). Fixed SQLAlchemy `case()` syntax for compatibility. **PHASE 3 - FRONTEND**: Renamed all files (queue-fresh.{html,js,css} -> notifications.{html,js,css}), renamed class (FreshQueueUI -> NotificationsUI), refactored data structure for dateGroups Map, added date accordion UI components, added CSS for date accordions and new count badges. **PHASE 4 - ROUTING**: Updated admin pages and profile page to reference new notification paths, updated cloud-run-validate.sh script. **PHASE 5 - TESTING**: Verified database migration (is_hidden column + indexes), tested all API endpoints, verified soft delete excludes hidden notifications. **BUG FIX - DATE SORTING**: Three-layer bug where dates appeared in wrong order. Fixed with: backend sorting, frontend sorting, and changed DOM insertion from `insertBefore` to `appendChild`. **FILES CREATED**: `src/scripts/sql/migrations/2025.01.01-add-is-hidden-to-notifications.sql`. **STATUS**: UI refactoring complete, date accordions working, soft delete functional. 🎨✅

> **🎯 PREVIOUS**: 2026.01.01 (Session 26) - MCP VOICE INTEGRATION PHASES 1-3 COMPLETE! Implemented production MCP server for Claude Code voice notifications. **PHASE 1 - Validation & Setup**: Fixed FastMCP 2.14.2 API breaking change (`description` -> `instructions`), renamed confusing env var (`COSA_PROJECT` -> `MCP_PROJECT`, config-driven), renamed directory (`src/mcp/` -> `src/lupin_mcp/` to avoid conflict with `mcp` package), validated cosa.cli imports, created production files. **PHASE 2 - Installation & Configuration**: Created MCP config template (`src/conf/mcp/cosa_mcp.json`), installation script (`src/scripts/install-mcp-server.sh`), discovery test script (`src/scripts/test-mcp-discovery.sh`). **PHASE 3 - Smoke Tests**: Created comprehensive smoke test suite (10 tests) validating imports, sender_id generation, notification models, FastMCP instantiation, tool decorator, and server URL configuration. **KEY DECISIONS**: (1) Config-driven `MCP_PROJECT` - set by MCP JSON config's env section, not manually by user, (2) Renamed `COSA_APP_SERVER_URL` -> `LUPIN_APP_SERVER_URL` for consistency, (3) Directory renamed to avoid Python import conflict with `mcp` package. **FILES CREATED**: `src/lupin_mcp/__init__.py`, `src/lupin_mcp/cosa_voice_mcp.py` (300 lines), `src/lupin_mcp/README.md`, `src/conf/mcp/cosa_mcp.json`, `src/scripts/install-mcp-server.sh`, `src/scripts/test-mcp-discovery.sh`, `src/tests/smoke/test_mcp_smoke.py`. **STATUS**: Phases 1-3 complete, ready for Phase 4-5 (E2E integration testing with Claude Code CLI). 🔧✅

> **🎯 PREVIOUS**: 2025.12.31 (Session 25) - MCP VOICE INTEGRATION PLANNING COMPLETE! Comprehensive planning session for Claude Code Voice MCP integration with Lupin notification system. **DESIGN SCOPE**: Option A (Print Mode) MVP - bounded tasks with voice clarifications via MCP tools. **KEY DECISIONS**: (1) Location: `src/mcp/` in Lupin repo (not CoSA submodule) - imports from cosa.cli as dependency, (2) Pattern 1 (Multi-Phase Implementation) with 5 phases, (3) Focus on Print Mode first, defer SDK Client mode (Option B). **EXPLORATION FINDINGS**: cosa.cli notification system is production-ready, sender_id is native field, API endpoints exist (/api/notify, /api/notify/response), SSE streaming works. **CRITICAL ISSUES IDENTIFIED**: MCP tool naming convention needs verification (mcp__cosa__* vs bare names), hardcoded server URL should use COSA_APP_SERVER_URL, needs logging infrastructure, error messages need context. **DOCUMENTATION CREATED**: src/rnd/2025.12.31-mcp-implementation-plan.md (comprehensive 5-phase plan, ~3.5 hours estimated). **PROTOTYPE LOCATION**: src/rnd/2025.12.31-claude-code-via-mcp-and-cosa-vox/ (cosa_voice_mcp.py, cosa_dispatcher.py, cosa_mcp.json). **STATUS**: Planning complete, ready for implementation. 📋✅

> **🎯 PREVIOUS**: 2025.12.31 (Session 24) - SORT ORDER DISPLAY BUG FIXED! Resolved the Session 23 blocker where notification messages displayed oldest-first instead of newest-first. **ROOT CAUSE**: Complex chain of transformations (DB DESC -> JS `.reverse()` -> CSS `column-reverse` -> `appendChild`) cancelled each other out incorrectly for real-time vs initial load scenarios. **THE FIX (4 changes)**: (1) CSS `queue-fresh.css:793` - Changed `flex-direction: column-reverse` -> `column`, (2) JS `queue-fresh.js:4169` - Removed `.reverse()` call on initial load, (3) JS `queue-fresh.js:3449` - Changed `appendChild` -> `insertBefore(messageDiv, container.firstChild)` so new messages prepend to top, (4) Python `notification_repository.py:220` - Changed `.desc()` -> `.asc()` so DB returns oldest-first (insertBefore then prepends newest to top). **HOW IT WORKS NOW**: Database returns oldest->newest (ASC), JS iterates with `insertBefore` prepending each message, result is newest at top for both initial page load AND real-time WebSocket notifications. **FILES MODIFIED**: queue-fresh.css (1 line), queue-fresh.js (4 lines), notification_repository.py (4 lines). **STATUS**: Phase 8 sort order bug RESOLVED, sender-aware notification system fully functional. 🐛✅

> **🎯 PREVIOUS**: 2025.12.30 (Session 23) - SENDER-AWARE NOTIFICATION PHASE 8 TESTING + BUG FIXES! Four bugs fixed, one feature added, sort order issue resolved in Session 24. **BUG FIX 1 - Clear All Button**: Root cause: `updateClearButtonState()` never called after loading notifications. Fix: Added call in `updateTotalNotificationsCount()` (queue-fresh.js:3504). **BUG FIX 2 - History Dropdown**: Root cause: `this.userEmail` referenced non-existent variable. Fix: Changed to `this.currentUser` in 3 locations. **BUG FIX 3 - 500 Error on Hours Filter**: Root cause: `datetime.utcnow()` creates naive datetime, PostgreSQL returns timezone-aware -> comparison fails. Fix: Changed to `datetime.now(timezone.utc)` + added `timezone` import. **FEATURE - Delete Sender Conversations**: Added `delete_by_sender()` repository method, DELETE endpoint `/notifications/conversation/{sender_id}/{user_email}`, frontend delete button + `deleteSenderConversation()` method, CSS styling. **SORT ORDER - FIXED IN SESSION 24**: See Session 24 for resolution. **FILES MODIFIED**: queue-fresh.js (+80 lines), queue-fresh.css (+20 lines), notifications.py (+70 lines), notification_repository.py (+32 lines). **STATUS**: Phase 8 complete with Session 24 fix. 🐛✅

> **🎯 PREVIOUS**: 2025.12.29 (Session 19) - SENDER-AWARE NOTIFICATION SYSTEM DESIGN COMPLETE! Created comprehensive implementation plan for multi-project notification grouping. **DESIGN GOALS**: Add sender identification to notification system enabling: grouping notifications by Claude Code project (LUPIN, COSA, PLAN), chat-style UI with collapsible sender cards, user responses displayed as outgoing messages, conversation history persistence across page reloads, multi-terminal workflow support. **SENDER ID FORMAT**: `claude.code@{project}.deepily.ai` (e.g., `claude.code@lupin.deepily.ai`). **8-PHASE PLAN**: Phase 1 Queue Layer, Phase 2 CLI Layer, Phase 3 API Layer, Phase 4 Database, Phase 5 Frontend JS, Phase 6 API History Endpoints, Phase 7 Frontend CSS, Phase 8 Testing. **DOCUMENTATION CREATED**: `src/rnd/2025.12.29-sender-aware-notification-system-design.md` (513 lines, complete implementation plan). **STATUS**: Design complete, ready for implementation. 📋✅

> **🎯 PREVIOUS**: 2025.12.03 (Session 18) - SIMILARITY MODAL ENHANCED: THREE-COLUMN LAYOUT + FIELD RENAME! Extended the similarity visualization feature with a third column for "Similar by Gist" and renamed `code_gist` -> `solution_summary_gist` for consistency with solution-focused naming. **FILES MODIFIED**: snapshots.html (+7 lines), admin-snapshots.js (+40 lines), admin-snapshots.css (+21 lines). **STATUS**: UI enhancement complete. Similarity modal now shows three comparison dimensions side-by-side. 🎨✅

> **🎯 PREVIOUS**: 2025.12.02 (Session 17) - CODE SIMILARITY VISUALIZATION COMPLETE! Implemented full feature for visualizing code-level similarity between snapshots. **BUG FIX - code_gist NOT GENERATED**: Root cause found - `running_fifo_queue.py` called `self.normalizer.process_text()` but `self.normalizer` was NEVER INITIALIZED! Fixed by importing and initializing `GistNormalizer`. **STATUS**: Feature complete. 🔍💻✅

> **🎯 PREVIOUS**: 2025.12.02 (Session 16) - DUPLICATE SNAPSHOT BUG FIXED - TWO ROOT CAUSES! **ROOT CAUSE 1 - TOCTOU RACE CONDITION**: Added thread locking to `save_snapshot()`. **ROOT CAUSE 2 - id_hash NOT PRESERVED ON UPDATE**: Added `snapshot.id_hash = existing_id_hash` to preserve original id_hash when updating. **STATUS**: Fix complete and tested. 🐛🔒✅

> **🎯 PREVIOUS**: 2025.12.01 (Session 15) - SYNONYM SIGNAL LOSS ROOT CAUSE FOUND + FIXED! Finally traced the corruption of "What's 4 + 4?" -> "whats 4 4" to its source. **ROOT CAUSE**: `agent_base.py:129` was calling the DEPRECATED `SolutionSnapshot.remove_non_alphanumerics()` method. **FIX APPLIED**: Changed to store verbatim. **DEPRECATION NUKE**: Made `remove_non_alphanumerics()` SCREAM its deprecation with fire emojis. **STATUS**: Fix complete. 🔥🐛✅

> **🎯 PREVIOUS**: 2025.12.01 (Session 14) - Admin Snapshots Modal ESC Key Handler! Quick UI enhancement: added ESC key to close the snapshot detail modal. ⌨️✅

> **🎯 PREVIOUS**: 2025.12.01 (Session 13) - Snapshot UI Polish + Synonym Signal Loss Investigation! Multiple UI enhancements and deep debugging of synonym normalization issues. **FIX A APPLIED**: Added 24 STT-friendly contractions to Normalizer. 🎨🐛⚠️

> **🎯 PREVIOUS**: 2025.11.30 (Session 12) - LanceDB Part 6 Complete: Import Fix + Config-Driven Improvements! Fixed ConfigurationManager import path. **LanceDB UPGRADE STATUS**: All 6 parts complete. 🔧✅

> **🎯 PREVIOUS**: 2025.11.26 (Session 11) - Multiple Parts: Fresh Queue STT Planning, WebSocket Notification Retry Logic, Snapshot ID Hash Collision Bug Fixed! Classic Python mutable default argument bug fixed. 🐛✅🧹🔄✅📋✅🎤

> **🎯 PREVIOUS**: 2025.11.25 (Session 10) - Multiple Parts: Collapsible Synonyms UI, TTFA Metrics, Stale Stats Fix, LanceDB Query Pattern Bug Fix, Runtime Stats Persistence BREAKTHROUGH! Finally identified ROOT CAUSE: `_format_cached_result()` was updating stats in memory but NEVER calling `save_snapshot()` to persist them! 🎨✅📊🐛✅🎯🐛✅

> **🎯 PREVIOUS**: 2025.11.24 (Session 9) - Admin Snapshots UI Enhancements + DELETE Bug Fixes! **Session 8**: Runtime Stats Persistence Bug Investigation BLOCKED - root cause identified in Session 10. 🎨✅⚠️🚨🐛⚠️

> **🎯 PREVIOUS**: 2025.11.23 (Session 7) - Triple Bug Fix: LanceDB nprobes Warning, Synonymous Questions Tracking, Deprecated Normalization! Fixed THREE critical bugs in one session. ⚡🐛🔧

---

## Navigation

### Archive Links
- **[Oct 16 - Nov 22, 2025](history/2025-10-16-to-11-22-history.md)** - Sessions 1-6: Admin Dashboard, LanceDB, PostgreSQL Migration
- **[Oct 16-30, 2025](history/2025-10-16-to-30-history.md)** - SSE Notification System Phase 2
- **[Oct 1-15, 2025](history/2025-10-01-to-15-history.md)** - JWT/OAuth, User Filtering
- **[Sep 3-23, 2025](history/2025-09-03-to-23-history.md)** - History Management, WebSocket Architecture
- **[Earlier Archives](history/)** - June-August 2025, May and earlier

### Implementation Documents
- **Current Focus**: Cold Call Flow Path 1 - Claude Code UI Card Testing
- **Path 1 Plan**: `src/rnd/2026.01.08-cold-call-path-1-ui-card-plan.md`
- **Cold Call Flow Planning (updated)**: `src/rnd/2025.12.31-claude-code-via-mcp-and-cosa-vox/2026.01.02-03-cold-call-flow-planning.md`
- **Session 47 Plan File**: `/home/rruiz/.claude/plans/expressive-plotting-charm.md`
- **Project Status Overview**: `/home/rruiz/.claude/plans/clever-napping-clover.md`

### Quick Navigation
- **Run FastAPI server**: `src/scripts/run-fastapi-lupin.sh` (port 7999)
- **Run GUI client**: `src/scripts/run-lupin-gui.sh`
- **Integration tests**: `./src/tests/run-integration-tests.sh -v`
- **Smoke tests**: `src/scripts/run-websocket-smoke-tests.sh`

### Current Development Areas
- Directory Rename (COMPLETE - genie-in-the-box → lupin, Sessions 36-40)
- MCP Voice Integration (COMPLETE - Phases 1-5)
- Option A Dispatcher (COMPLETE - ClaudeCodeDispatcher working)
- Cold Call Flow Path 1 (IMPLEMENTED - UI Card needs testing, Session 47)
- Cold Call Flow Path 2 (DEFERRED - Intent parsing after Path 1 proven)
- Notifications UI (ONGOING - polish and improvements)
