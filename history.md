# Lupin Project History

> **Archives**: See [history/README.md](history/README.md) for the full chronological index. Most recent: [2026-05-28 to 06-16](history/2026-05-28-to-06-16-history.md). History health: ✅ **HEALTHY at ~6.9k tokens (28% of 25k)** — archived 2026-07-06 by Mr. Radio 🦉 (session 2352acab), ~16k tokens moved to archive.

### 2026.07.16 - Session 1a52ceb2 (Mr. Radio 🦉) | Heartbeat-hold janitor: Q1 root source SHIPPED (sweep reach 4→44 holds) · F-C #3 + a false kill-claim caught at the gate · deepseek truncation probe re-keys §4.1(b) · board catalog authored then SELF-REFUTED by measurement · backup + push (Rick, session-end)

**Accomplishments** (re-spun mid-arc by Rick; crew Clayton 😎 570f4d92 spawned/gated/reaped clean under Rick's ordered DM silence; MANAGE-not-build — every green re-executed by my own hand before gating, no claim accepted on report):
- **Q1 root source SHIPPED (`7f17de91`, Clayton, manager-gated)**: config + **host-path translation** + parent scan, unioned, deduped on `realpath`. Anchor-pair derivation off `docker-compose.yml:158`, accepted only if the translated dir EXISTS (self-calibrating, no brittle swap). **Sweep reach 4 → 44 holds** — `skills-distillation`'s 24 holds were the root the prior report flagged UNREACHABLE; that WAS the untranslated container path, and the report already carried the evidence as a number. Verified firsthand: targeted 68 · coverage **100%** (223 stmts/56 br, zero pragma) · full suite **9714/0** · **negative control 3 RED** · deletes-nothing + positive control · live report 45 requested → **42 swept, DELETED=0**, exact match to Clayton's ledger.
- **⛔ The safety is STRUCTURAL, not a flag**: `prune_stale_hold_files` remains ORPHANED (zero production callers); the arbiter calls `report_hold_files`. **46 holds on disk, nothing deleted.** Do not "clean up" the orphaned function — the deletion milestone inherits it. Enabling deletion is Rick's call (`11461241`, blocked); Rio's F-A is binding: **cargo_bearing is REPORTED, not a keep guard — deletion opens only when the cargo is out.**
- **F-C #3 (`8d7c5d5c`) — and a FALSE CLAIM caught at the gate**: Clayton's fix was green (73 passed, 100%, full suite 9719/0) but his ledger claimed a mutant kill that is **impossible** — `canonical_persona_key` returns `''` for `None`/`''`/blank, so the blank-name guard is **redundant with the key guard one line down** ⇒ equivalent mutant, unkillable. Proven on a **canary-verified** harness (canary → 3 RED ⇒ connected; real mutant → 7 passed ⇒ survives). Code correct, **claim false — a false claim does not enter the record.** Fixed before commit.
- **Deepseek truncation probe (`294a66ac`, `88f78150` done)**: truncated deepseek returns **PARTIAL CONTENT** (93 chars, `finish_reason='length'`), **NOT `content: null`** ⇒ **M1 §4.1(b) never fires for deepseek**; a truncated judge parses clean and **votes** on a unanimous gate. Tiberius's read of my own results table (credit him): `finish_reason=='length'` is the **only** field uniform across both arms ⇒ **`content is None` is gpt-oss's SYMPTOM; `finish_reason=='length'` is the DISEASE.** Re-keying also dissolves the (b)/(c) precedence race. Reusable: **deepseek-v3.2-maas serves `global` ONLY** (us-central1 → 400; 5 others → 404), same as `claude-opus-4-8`.
- **Board catalog authored (`78854959`) then SELF-REFUTED (`5e8373c1`)**: I claimed "the board grows by construction / only grows" — **mechanism real, effect ABSENT.** Measured off the store's Postgres: **93.1% all-time closure (861/925)** · **oldest OPEN item = 4 days, zero older than a week** · 40 of 64 open rows <1 day old · **52% of the open board belongs to the crew reaped at 22:11** ⇒ **the board didn't rot, it was DECAPITATED MID-SPRINT** · I am the single largest minter of the burst I catalogued. **Why it sailed: a finding that CONFIRMS the boss's suspicion passes a checkpoint a contradicting one never would.** María sent it back; I never ran the number my own report called decisive. **Agreement is not a checkpoint — it is the absence of one.** Surviving findings (filed as tomorrow's P0): zombie items, chase-expired rows, **priority carries no information** (41 P1 / 65%), **the Stop-hook owed-work oracle disagrees with the store**, and **the filter defect** — a scoped query cannot show you half the board is someone else's; I declared "board clean" 3×, each true of my filter.
- **The finding of the night (Clayton's tally, verified)**: **twelve+ defects, EVERY ONE IN A *FIX*, none in the original code, not one a knowledge problem.** Every instrument reached for lied at least once — coverage green over two vacuous tests · a canaried harness passing a mis-labelled mutant · `git ls-files` about to hide a live fleet-killer · `write_memento=True` returning success having saved nothing · **and my own catalog.** Clayton's transferable half: *"the finding came from the RE-RUN, not from me — that's an argument for the RITUAL, not for my judgment."* And his sharpest: *"the failure mode isn't sloppiness, it's a rule that's CLEANER THAN REALITY. Those are the ones that ship."*
- **My error ledger (6, all self-reported)**: asserted my own memento didn't exist after searching **one of two repos** (it was in PIP; caught by my own prior hold file, not my judgment — **`ls {lupin,planning-is-prompting}/io/mementos/` BOTH, always**) · read a timestamp **UTC as EDT** and reported a live broadcast dead · first negative control was a **disconnected tool** (patched the module attr; test used a direct name import) · raw `Write`+`cp` **bypassed the memento guard** and misfiled two amendments onto a legacy record · called `read_hold` with a path not a session_id · **shipped a catalog whose central claim I never measured, because it agreed with Rick.**

**Files changed**: 7 commits pushed (`46603eee` janitor · `7f17de91` Q1 · `8d7c5d5c` F-C #3 · `294a66ac` probe R&D · `78854959` catalog · `5e8373c1` the refutation · `6680cee1` TODO P0) + `8814281` in planning-is-prompting (tmux-guard mandate). Backup synced (4.22 GB / 149 files). **Fleet**: Clayton 570f4d92 reaped with memento verified on disk **before** the reap (35,195 b, seat awake ⇒ captured clean); he discharged every item with receipts, refuted my `git --git-common-dir` dedupe instruction and **was right** (it returns a relative path ⇒ collides every repo; and a worktree shares its main repo's common-dir while holding its own hold — the dedupe would have eaten the file). **Carry-over**: P0 = finish the board analysis (TODO.md) · enable-deletion gate `11461241` (Rick's) · M1 §4.1(b) re-key · `e2f11f6f` freed-name decision · 3 queued + 10 blocked, chase 13:00Z.

### 2026.07.15 - Session da517b03 (Mr. Radio 🦉) | GCP deployment arc + 4-merge bug-fix sprint · pilot-readiness review + VM getting-started runbook authored · orphan-ghost / vertex-guard / config-floor fixes CLOSED · Cloud-VM config-block naming decided (impl→AM) · 3-worker crew cycled clean · push + backup (Rick voice, session-end)

**Accomplishments** (re-spun off bf549da1; evening arc ~20:15–22:45 EDT; crew Cheech 🌿 + Clayton 😎 spawned/reaped clean, Krishna 🦚 vertex on-call held; MANAGE-not-build, every merge behind firsthand diff reads + independent test re-runs — including one false-RED caught and corrected before it could bounce correct work):
- **GCP deployment readiness + runbook (Rick's questions, verified live on the VM)**: probed `lupin-host-test` — Lupin FastAPI ALREADY serving `:7999` (`lupin-rest-cloud-gpu`, healthy) backed by Cloud SQL/pgvector; Cloud Run `lupin-model-server` confirmed 1-GPU scale-to-zero (`minScale:0`/`maxScale:1` → $0 GPU idle). Authored `src/rnd/v0.1.9/2026.07.15-gcp-pilot-readiness-review.md` + `2026.07.15-gcp-vm-getting-started-runbook.md` (SSH-via-IAP, port-forward, source bind-mount model, `:8001` arbiter provisioning, two-service Cloud-Run/VM split + billing). Key findings: **no `:8000` on the VM** (both cloud stacks publish 7999, collide); Rick's work CC routes an **InTraffic adapter → Model-Garden Opus** (Vertex path → the bare-slug clamps are on the critical path). Config-block naming DECIDED (`[Lupin: Cloud VM Development]`/:7999 + `[Lupin: Cloud VM Testing]`/:8000); implementation deferred to AM per Rick (plan in TODO.md).
- **4 bug-fix merges, all reviewed + independently verified**: `d3ccfbcd` **ef10c5b6** focus-bar invisibility durable fix (Cheech; launcher env-forward + `~/.lupin/config` INI fallback, 29 tests 100%) · `15bb77c4`+`2bc52c80` **ee59d5ed** orphan-bridge reap-survival (Cheech; Change 1 = durable history-safe roster eviction via `NOT EXISTS` on the persisted `session_reaped` marker — `is_hidden` REJECTED after the mandated blast-radius check proved it over-hides every history query; Change 2 = lineage-independent arbiter orphan-sweep, **DEFAULT-OFF**; 141 unit + `:8000` gate true-green ts-1956de25) · `68825c95` **260dba16** vertex GCP-id guard greened, 3 offenders, zero guard-weakening (Clayton) · `3442637f` **9fe8b80f** bare-unit/smoke config-collection floor in the parent conftest (Clayton; verified 9607/0 + 362/0 with env unset).
- **Fleet hygiene + lesson**: 2 authors spawned / verified-persona-first / reaped clean with mementos (`cheech-a6d0dd31.md`, `clayton-5e15e5de.md`); Krishna's `/clear`-invisibility proposal addendum reviewed + folded (Krishna authored / manager-reviewed). One **instrument-error false-RED** (guard test run without `LUPIN_ROOT` → scanned the main tree not the worktree) caught + corrected before bouncing Clayton's correct work — banked: worktree guard tests need `LUPIN_ROOT` set (`cu.get_project_root()` drives the scan).

**Files changed**: 4 feature merges (~23 commits ahead of origin: `d3ccfbcd` + the `ee59d5ed` 8-commit series + `68825c95` + `9fe8b80f` + merge commits) + 2 R&D docs + Krishna's proposal addendum + `history.md`/`TODO.md` this wrap. **Carry-over**: config-block impl (AM — TODO.md has the plan + the `+`→space encoding note); arbiter orphan-sweep flag flip (Rick — activates ee59d5ed Change 2 fleet-wide); GCP deploy gaps `f3b5ecf3` (:8000 stack) / `53bac23a` (:8001 arbiter); Rick's parked GCP gates (part-2 clamp paste, canary, VM re-suspend, coexist-vs-single-stack, clamp sizing vs Model-Garden throughput).

### 2026.07.14/15 - Session bf549da1 (Mr. Radio 🦉) | tmux fleet-killer P0: cascade close-out (Steps 8-9) → SWE-team build → FIX COMMITTED gate-clean · scratch project registered · full orphan sweep (Rick's word) · push + backup (Rick's recorded night directive)

**Accomplishments** (re-spin → cascade close-out → implementation, ~23:18–00:45 EDT; SWE crew Clayton 😎 impl / Tiffany 💍 review / Cheech 🌿 test + Rio ⚡ scratch lane; María 🌸 steward; MANAGE-not-build, both commit gates green AND adversarially reviewed):
- **Cascade `cascade-tmux-fleet-killer` CLOSED end-to-end**: Step 8 pipeline summary (34 findings A11·B12·C11, 0 foundational, 0 votes, 0 escalations, ~55 min) + Step 9 revision-handoff doc (`src/rnd/v0.1.9/2026.07.15-cascade-tmux-fleet-killer-revision-handoff.md`, cold-context test 6/6, Rachel light review thumbs-up after 1 revision turn) → `implementation_handoff_ready`. OSQ-1 Rick-confirmed via /plan-decide (execve tracer, LAST). Old crew reaped 4/4 w/ mementos post-harvest; 3 follow-up mints (g-s-d guard, p-i-p note, vertex-lane continuity `bd0b728b`).
- **THE FIX COMMITTED (`52ceabe0`, 16 files)**: `tmux_isolation.py` single-source strip helpers (100% L/B/F, both anti-reuse fences as executable tests) + session-scoped autouse conftest guard + quarantined test §5.1-fixed and RESTORED (fleet pid 387516 unchanged ×2 runs ×2 verifiers, zero leaked sessions, AC4 mutant control reddens) + AC2/AC5 tests + killtrace probe (live-instrument receipted) + execve tracer AUTHORED (.bt+.service; install = Rick-on-request; bpftrace -d root-gated) + runbook §10 correction/§10a (`e1ba0239` closed). **Cold-context dogfood: ZERO handoff-doc gaps.** Review earned its seat: Tiffany's F1 (join-in-printf invalid bpftrace — static-read catch that would have burned Rick's sudo dry-run) found→fixed→verified in one bounded re-loop; Cheech's §7 matrix 26/1-designed-skip with instrument-verified receipts.
- **Scratch project REGISTERED (4f42c218 done, Rio ×2 seats)**: repo pre-built by prior seat (verified by execution, not memento-trust); registration INI/splainer/KNOWN_PROJECTS/account; bounce proved MOOT (projects/ already mounted :ro in both containers); prior seat's `.docview.yml` `allow:`-key wildcard hole FOUND+FIXED (fail-closed proven live: manifest deleted → 400-deny, restored → authority back); Tiffany re-loop caught `test_sender_id` len-pin (UNION² consumer outside both verified subsets) + empty-prefixes fail-OPEN (→ `bin/` floor); all 6 missing splainer twins added (drift task `97c65861` retired).
- **Orphan sweep (Rick's direct word ~00:28)**: every orphaned file attributed→verified→committed. `7eef87ce` vertex-toggle lane WIP (20 files; guard's 3 reds proven self-healing artifacts of the half-committed lane; 2 REAL offenders fixed at source — stub tests fed the real sandbox project id, now inert fake). `3f567255` notif NOT-NULL migration trio (roundtrip smoke deliberately NOT side-door-run — :8000 venue). Staging hygiene: dead vertex session's pre-loaded git index detected + unstaged before the P0 commit.
- **Pattern-watches filed**: arbiter MANAGER-STALE advisory cc'd to a peer manager (the ff91cff4/f48f089d route class, N=1); persona-pool allocation loud-fail on spawn (N=2 same-night); slot-inheritance bug fixed at root (`ab721143` off the tiffany slot, re-blocked on the item edge).

**Files changed**: 4 commits (`52ceabe0` fix · `7eef87ce`+`3f567255` orphan adoptions · wrap commit this-entry) — tree EMPTY after wrap. **Fleet**: 8 seats cycled clean tonight (4 cascade + 4 SWE/scratch), all with mementos + post-game deposits; María's retro + cross-exam ran mid-flight. **Carry-over**: vertex lane re-staffs 13:00Z (5 blocked items + `ab721143`); tracer install offer open to Rick (one sudo, parse dry-run included); g-s-d/p-i-p guard mints trigger-based.

**Accomplishments** (evening arc off Rick's bake-vs-bind-mount question; crew Krishna 🦚 ×3 lanes + Clayton 😎 ×1; MANAGE-not-build, every code commit behind firsthand diff reads + independent test re-runs):
- **GCP code-delivery doctrine (Rick voice rulings, executed same evening)**: testing containers ship with **NO app code baked in** — deps+runtime image only, code = bind-mounted static snapshot of a committed ref (§4b); manual pull ratified, VM-side GitHub remote declined (§5); prod model POSTPONED (§6); local `:8000` snapshot-isolation gap analyzed w/ optional snapshot-dir hardening flagged (§7). Plan doc `src/rnd/2026.07.12-gcp-bind-mount-revert-plan.md`.
- **Codeless image lane (Clayton, `41d576ae`)**: `BAKE_CODE` FROM-selector (default off) — runtime-base / code-off / code-on stages; 8/8 structural probes (zero repo code ANYWHERE, deps byte-parity vs pinned, boots-with-mount, exit-127 without = the intended fail-loud contract, mux-builder stage pruned, local compose unaffected); cloud-run-build.sh behavior change named in-commit.
- **GCP DEPLOY EXECUTED end-to-end (Rick's "just do it", ~15 min)**: AR push `lupin:1.3.0-codeless` @ `dbb4b307…` (delta push) → VM resumed → checkout replaced w/ git archive of HEAD `df611aa7` + **mux dist materialized** (the git-archive trap, avoided) + keys preserved + `.deployed-ref` stamped → image pin swapped → recreate → **four-proof verify green** (/health 200 · backend=postgres @ migration head · HEAD sentinel · bare image `/var/lupin/src` = 0 files) → VM re-SUSPENDED. Blocker cleared live: expired gcloud auth (Rick re-logged in-session).
- **Arbiter sibling-gate trilogy (Krishna, all firsthand-reviewed, live via 3 :8001 bounces)**: `96e8e0f7` (1ff7be20) blocked-edge roster now store-corroborated — journal ground truth: sticky free-form `awaiting` label minted a phantom peer edge; ping leg had the gate, roster leg didn't; suppressed its FIRST real FP within minutes of landing. `de193b5a` (3287ee1e) stuck advisory honors the bridge-fresh veto — one poll had contradicted itself (veto fired + "1 stuck/dead" tap, same persona); e5e33795's manager-only scope pin ruled blast-radius scoping, inverted with archaeology. `d71eae1d` (52b8ed6b) `idle` recovery now consumes prior caps (was `honored`-only ⇒ permanent LIVE-STUCK); monotone True→False safety property; 10-consumer sweep + session-emitted-beacon membership rule ratified pre-build. Durable lesson written into `src/docs/fleet-liveness-and-task-store-architecture.md` §4: *a correctness gate wired into one consumer of a signal but not its siblings; found by reading the journal until the arbiter contradicts itself on a single poll*.
- **Fleet hygiene**: both workers verified persona-first, driven through ack→design→build→receipts contracts, reaped clean w/ mementos; 6 store tasks closed w/ receipts; arbiter blocker-nudge crossed-in-flight handled (no reply channel — arbiter is inbox-less by design).

**Files changed**: 7 commits (`96e8e0f7`→`df611aa7`: 3 arbiter fixes + codeless Dockerfile + 3 docs commits) + history.md/TODO.md this commit. **Carry-over**: `src/conf/lupin-app.ini` left untouched (third session's skills-distillation edit); María+Tiffany cascaded-review broadcast is their lane.

### 2026.07.11/12 - Session 372f9dc9 re-spin (Mr. Radio 🦉) | Evening arc: TODO horizon sweep (2,292→178 lines) · 3-layer arbiter-accuracy stack (dedup + designed-hold suppression LIVE, stale-edge queued) · focus-bar ghost-card fix · flake root-cause · 2 workers cycled clean · Enter-immune wedge + wake-path lessons banked

**Accomplishments** (post-board-clear re-spin, ~17:00–23:45 EDT; crew Clayton 😎 + Krishna 🦚, MANAGE-not-build, every merge behind firsthand diff reads + independent test/coverage re-runs):
- **TODO.md horizon sweep EXECUTED** (`eceddab7`): 2,292→178 lines (~85k→~6.9k tokens); 98 pre-2026.06.25 sections archived verbatim to `todo-history/2026-04-15-to-2026-06-16-todo.md`; 359 stale-open bullets flagged by-age-not-disposition (review open).
- **Arbiter accuracy, 3 layers from one evening's live evidence**: (L1) `ce13b134` blocker-cc re-announce dedup (`ad0f6199`, Clayton) — manager-cc rode every correctly-throttled blocker ping; (blocker, blocked_item, recipient) cooldown reusing advisory-cooldown machinery; the 8-fire evidence log drove item-in-key + window sizing. (L2) `cec10ef9` designed review-gate-hold suppression (`73378b09`, Krishna) — verify-first proved the FP live (3 pure repro tests); store-backed designed-holder suppressed on BOTH announce legs iff a blocking operator is alive; fail-safe-to-ROSTER; 20 RED-first tests, arbiter_job.py 100% L/B. **Both LIVE via two :8001 bounces**; post-bounce soak spotless (0 errors/taps/reannounces). (L3) `1ff7be20` stale holding_on-edge FPs (María + Sam evidence banked) — deliberately PARKED overnight: the quiet advisory stream IS its verify-first evidence; morning staffing planned.
- **`0b2783ef` focus-bar ghost cards** (`474c73ab`, Clayton): Rick's live repro (reaped workers' M-badged cards) → real defect = missed-frame reap + additive-only hydrate; reconnect-path `reconcile()` prunes snapshot-reported persona-less sessions, cold-boot hydrate untouched; null-persona-hydrate wart REFUTED w/ receipt (SessionStripStore.ts:261). 47 tests, 100% c8 both files.
- **`6487d5c0` unit-flake root-caused at source** (`cb3829c6`, Clayton): NOT an mtime race — backgrounded pytest orphaned to init → getppid=1 → bridge-reader skip; deterministic repro both directions; autouse fixture pins it. Belt 9,028/0.
- **Ops + doctrine**: arbiter bounce miscreated as a Rick-gate then CORRECTED on Rick's word (classifier wall ≠ authority ruling — memory banked, gate closed w/ reason on record); Enter-immune staged-buffer wedge broken live (Escape→C-u→retype recovery banked); worker→manager wake-path failure proven (Krishna's review DM + 3 arbiter nudges buffered ~55 min) — memory corrected, sweep-inbox-on-wake adopted; Sam's "stuck" advisories disproven by pane probe (evidence → L3).

**Files changed**: 5 commits (`eceddab7`→`73378b09`) incl. the TODO archive move; 4 R&D docs authored w/ README links; fleet-liveness doc touchpoint updated. **Fleet**: 2 authors spawned/verified/reaped w/ honors (mementos: clayton.md, krishna.md). **Carry-over**: `1ff7be20` morning staffing w/ overnight journal; harvey-labs + VM-socket chases at 13:00Z; stale-pending disposition review; Sam's parked /plan-decide queue (his lane).

### 2026.07.11 - Session 372f9dc9 (Mr. Radio 🦉) | Autonomous lunch-window bug-fix arc: 10 board items terminal · P2 monopolize deadlock KILLED (Shape-A + Shape-B, two 314/0 sweeps) · persona-key root fix + 13-row board heal · 24 legacy unit reds cleared + dev-DB pollution vector killed · crew of 8 cycled clean · push+backup (Rick voice, session-end)

**Accomplishments** (Rick's finish-the-board lunch directive, broadcast 5c0a592f; crew Rio ⚡ / Rachel 🕊️ ×2 / Clayton 😎 / Tiberius 👑 / Cheech 🌿 / Krishna 🦚 ×2 / Arnold 🪨; MANAGE-not-build; every code merge behind firsthand diff reads, two-key review on both dispatch-core changes):
- **3a14292b P2 :8000 monopolize deadlock KILLED** (`d28b496e`, Clayton): Shape-A lineage pass-through — `spawned_by_id_hash` + pytest-env bridge + selective Gate B (`pop_next_eligible` predicate) + classifier exemption; sweep `ts-dfc230a9` GREEN 314/0 exactly inverting ts-ad4670ec's 0/7 (7/7 children dispatched mid-window; 1916→0 blanket-deferrals). Doubles as 67473d91's deferred E2E confirm.
- **fe375cf6 Shape-B hardening** (`acd33478`, Clayton): monopolizer runs on a dedicated single-worker executor OUTSIDE its pool — pool_max==1 now deadlock-safe (width-1 in-process proof, RED-first), budget math exact, pool-status fields additive; sweep `ts-ce9e8599` GREEN 314/0 = behavior-preservation pair. Then `19a7a58d` retired the now-redundant pool_max==1 belt (guard-RED receipt: the in-process test catches the deadlock on reverted source).
- **951a22be persona-key normalizer root fix** (`2d78ca47`, Rachel; Krishna two-review): founding framing REFUTED twice — true defect = `canonical_persona_key` deleting separators ("mr_radio"→"mrradio"); one-regex root fix, 104/104 tri-file; manager-run backfill healed 13/13 board stragglers (rescan 0). Follow-on 65d5305b derive_dm_topic contract ratified on zero-orphan inventory (`d67f5764`).
- **cfcbb703 24 legacy unit reds ALL terminal** (`9696e4fa` + `cd32720a`, Rio): 24/24 test-side drift, 0 prod regressions; Family-B ephemeral-per-test-schema fixture killed the LIVE dev-Postgres pollution vector (row-count 78→78 across 9k tests); Option-3 txn-rollback disqualified pre-build by Rio (committed rows visible ≠ empty table).
- **c03d1870 persona policy shipped** (`fb2ef69a`, Cheech): unknown-persona soft-flag (roster via existing pool loader — "no accessor" premise overturned) + class-scoped owner default; advisory live in store responses same hour. **236c70ba** LANCEDB→POSTGRES default tripwire (`527c594a`, Clayton). **10347e69** arbiter -r2 re-delivery closed ALREADY-FIXED (Tiberius premise-inversion, 742fe8dc Lever C, receipts spot-checked).
- **Fleet hygiene**: 8 worker/reviewer sessions spun + reaped clean w/ mementos; persona-pool walk lesson banked (3× today: allocation chain walks — verify from check-in, never preference); non-destructive merge-conflict handling preserved a parallel session's pending README edit through two merges.

**Files changed**: 16 commits on wip (7cee2dcf→19a7a58d incl. 2 morning AGY commits) + history.md/TODO.md + `start-cc-with-tmux.sh` (Rick's persona-chain edit, committed on his "all commits" word). **Carry-over**: TODO.md horizon archive still pending (~2,250 lines, deferred again — next session); VM socket apply at next lupin-host-test unsuspend; Sam's parked /plan-decide queue (041b2de6, his lane). Memento: io/mementos/mr-radio.md.

### 2026.07.08 - Session f7af1ee6 (Mr. Radio 🦉) | Evening intercession ritual: push + full backup leg (Rick broadcast 0db68c9b)

**Accomplishments** (designated infra leg of the coordinated fleet wrap-up — Tiberius commit-only/no-infra, Mr. Radio do-both; no crew, no code authored this session):
- **Pushed the branch to origin** — 3 held commits carried up: `fcd0e257` (Tiberius session-end GCP CPU-VM app-restore doc slice) + `3b208f65` (arbiter case-16 MANAGER-AWAITING-USER → TIER_RICK_ONLY, D1 ratified) + `f325b31d` (arbiter manager-subject hardening: `_tap_managers` bridge-fresh veto + case-17 MANAGER_DONE → TIER_RICK_ONLY; Krishna review PASS, 3-leg verify, 1604 unit/0 fail).
- **Full DATA01→DATA02 backup executed** (dry-run reviewed → `--write`).
- **Orphaned-file sweep (Rick day-end directive: "document and commit all orphaned files")** — swept the working tree clean in themed commits, each documented: (1) **doc-viewer scope** — registered `google-project-wonderwall` as an external repo (`lupin-app.ini` external-repos list + path/prefixes; `document-viewer.html` KNOWN_PROJECTS set); (2) **LanceDB on-disk store retirement** — `rsync-exclude.txt` freezes the 30G DATA02 LanceDB mirror as a durable rollback snapshot (plain `--delete` protects excluded dest paths) + `TODO.md` LONG-TERM Phase-2 source-teardown backlog entry (deferred per Rick); (3) **tmux ergonomics** — `start-cc-with-tmux.sh` adds `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` (classic inline renderer → restores scrollback/search/copy-mode); (4) **adopted orphaned artifacts** — arbiter manager-subject hardening R&D doc (`2026.07.08-…-hardening.md`, the design record for the shipped `3b208f65`/`f325b31d`) + its `src/rnd/README.md` link (17→18) + the orphaned mux R5 E2E test `test_multiplexer_session_topic_name.py` (py_compile clean).

**Files changed** (this session): history.md (this entry); orphan sweep — `lupin-app.ini`, `document-viewer.html`, `rsync-exclude.txt`, `TODO.md`, `start-cc-with-tmux.sh`, `src/rnd/README.md`, + 2 adopted untracked (`2026.07.08-arbiter-manager-subject-routing-hardening.md`, `test_multiplexer_session_topic_name.py`). **Carry-over**: none — working tree clean after the sweep.

### 2026.07.08 - Session cda5f427 (Tiberius 👑) | GCP CPU-VM app-restore arc COMPLETE + verified (task b5854c3e) · MTU 1500→1460 root-cause was the last blocker · d9cca90c operator gate closed · Rachel spun/re-spun/reaped clean · restore runbook authored · NO push/backup (Rick: Mr. Radio's lane)

**Accomplishments** (crew Rachel 🕊️ ×2 spawns; MANAGE-not-build; every step verified firsthand):
- **b5854c3e GCP CPU-VM app-bringup DONE + verified** (Rachel drove): full arc — VM CPU-downgrade (`8636f84b`, earlier) → Phase-1 app up on the reused 125G local containerd images (/health 200, backend=PostgreSQL) → Phase-2 GCP networking (Cloud DNS private zone `run.app` + `*`/apex A→199.36.153.4-.7 + 199.36.153.4/30 route on dev-vpc; Rick cleared the classifier by adding `Bash(gcloud dns:*)`+`Bash(gcloud compute routes:*)`) → **last blocker root-caused: Docker-bridge MTU 1500 vs GCP-VPC 1460**. Durable proof: live app→model-server round-trip **200** via VIP 199.36.153.5 (58ms), survived a full compose recreate; MTU persisted in `docker-compose.cloud-gpu.yml` (VM).
- **d9cca90c operator gate (Rick-owned) → DONE** (event 2207): terraform-apply-perm gate from the VM-recreate maze, satisfied-but-stale-open (Rick added `Bash(terraform:*)`; recreate ran + verified); closed on standing authority with receipts. Rick's court then swept to 0.
- **Restore runbook authored** (`src/rnd/2026.07.08-cpu-vm-app-restore-runbook.md`): the 200G data disk (`lupin-data`) survived the boot-disk wipe and holds the full app checkout + 125G images + volumes → re-deploy is a RESTORE not a fresh clone; ordered restore path + Blocker-1 network recipe + a durable startup.sh fix (execution HELD pending Rick's scope ruling).
- **Over-gate lesson banked (7th instance)**: I wrongly asked Rick to authorize the idle-worker reap of Rachel — reaping idle + nothing-owed is manager STANDING authority; Rick called it out ("do your job").

**Files changed** (this session's doc commit): history.md, src/rnd/README.md (+CPU-VM runbook link), src/rnd/2026.07.08-cpu-vm-app-restore-runbook.md (NEW). Infra actuation (VM-side) landed off-tree per the runbook. **Carry-over**: runbook execution HELD pending Rick's scope ruling; one P3 backlog `10347e69` (arbiter advisory -r2 re-delivery, no worker needed). Push+backup = Mr. Radio's lane this close (Rick broadcast 0db68c9b). Memento: io/mementos/tiberius.md.

### 2026.07.07 - Session 17e81460 (Mr. Radio 🦉) | Full-day fleet arc: AM pgvector CUTOVER LIVE (0901984d) · PM riot-act sweep 8 items terminal · GCP pgvector bring-up COMPLETE (84d81d39) · LanceDB teardown PREP merged (d1a681af) · SWE-harness false-red fix (d4aa722c) + monopoly-deadlock live-fire evidence · crew cycled (Cheech/Clayton re-spun, Tiffany reaped) · backup+push (Rick broadcast 46be63bf)

**Accomplishments** (full day; crew Cheech 🌿 / Tiffany 💍 / Clayton 😎, two Cheech+Clayton generations; MANAGE-not-build; every APPROVE backed by firsthand re-runs + line-by-line diff reads — b4c7776b, e3a7c76a, c2b02065, d60ce097, 84d81d39, merge d1a681af, d4aa722c):
- **AM: v0.2.0 pgvector migration CUTOVER LIVE** (`0901984d`, Rick's GO + carte blanche): backfill ~202k vectors ×2 passes, dual-engine equivalence PASS, exact-scan ruling (keystone 97.2% dup vectors → HNSW dropped), INI flip both servers, integration gate `ts-c94c514d`. Full record: TODO.md 07-07-AM block + swap-chain execution doc.
- **Riot-act 22f367f0 executed**: 8 board items driven terminal with receipts (2d8b6d1c→`b4c7776b` · a5bf3298→`e3a7c76a` · 399a08d5 · dfe7c26c · b8fa9b7d dropped user_direct · gates 5d7cd5f8/b388803e · d57f38bd→`c2b02065`); board re-shaped honest after Rick's "board at zero" challenge (zero-UNGATED + named-gated language).
- **c845346a GCP pgvector bring-up COMPLETE** (`84d81d39` + doc-08 receipts, Cheech): image saga closed on -r3/`4748ab2a` live-correct after 3 structural pre-deploy catches (stale re-tag, baked-env, VM compose mount-shadow → mount removed, image IS runtime); schema via boot auto-migrate to e1f2a3b4c5d6; green-bar receipted (health/backend=postgres/keystone exact-scan/NO-HNSW); VM lupin-host-test TERMINATED. Self-consistent-image doctrine banked: baked-image probe mandatory pre-push.
- **df51e19a LanceDB teardown PREP Phase-1 merged** (`d1a681af` --no-ff, Tiffany): inert rollback-preserving slice (SolutionSnapshotManager rename + banner), 432 unit + ts-b305851a green; isolation-confirm infra-blocked by 30398595 → (c) evidence-complete close. Successor row 4955d0b9 filed (fire 07-08 09:00 EDT).
- **67473d91 SWE-harness false-red fix DONE** (`d4aa722c`, Cheech): pool-aware wait budget (90 + ceil(7/workers)×60; FAIL-LOUD pool-status query + LUPIN_TEST_SWE_BUDGET_S escape) + facet-3 global auto_fix_on_failure=False landing-run convention + facet-2 no-code (watchdog gates verified). Unit tier 12/12 proves the contract. **:8000 E2E confirm ts-ad4670ec RED-by-companion**: live-fire proof of the 30398595 monopoly-hold deadlock (`[CONSUMER] Monopoly hold active` defers the run's own 7 spawned swe- jobs → all pending, started_at=None, per-test timeouts — contradicts caf58f71's "no-op placeholder" finding — hold log ×1916, started_at flipped only after monopoly release = smoking gun); E2E confirm deferred pending the monopoly fix; 30398595 proved terminal(done) so evidence landed as durable bug `3a14292b` (P2, Clayton firsthand re-derive: RED 0/7).
- **Fleet ops**: Tiffany reaped no-respin (Rick ask 22:30; unsubmitted-pane-text doctrine — surfaced before acting) · Cheech + Clayton generation-cycled with memento continuity · TFE tfe-130826c7 treadmill squatter completed naturally (Rick's kill moot) · wake-gap d1bb1456 bit 3× (pane-nudge protocol standing) · 58660c64 filed+amended → Tiberius lane (fixed same evening `15f5d2f2`) · ~16 arbiter advisory spam ticks absorbed pre-fix.
- **Rulings banked** (TODO.md Decisions Log): no GPU-less instance ever · 15-min stockout-retry reflex · classifier reference case (5 walls, 0 bypasses) · Phase-2 teardown 09:00 = fire-time · exact-scan over HNSW.

**Files changed** (session doc commit): history.md, TODO.md, src/rnd/README.md (+2 v0.2.0 lane links), .claude/skills/push-to-completion/SKILL.md (riot-act trigger phrases), mux-discrepancy 00-index rows 05/06 (adopted July-1 doc orphans), + GCP-leg artifacts per author staging word. Code landed via crew commits above. **Carry-over**: 4955d0b9 fire 09:00 EDT (fresh author; spec = scoping doc §4) · bug `3a14292b` monopolize-agentic-pool-deadlock (P2, evidence-complete, blocks the 67473d91 E2E confirm + any monopolize-mode suite that spawns agentic jobs) · mux e2e orphan `test_multiplexer_session_topic_name.py` left untracked (Tiberius/María lane word needed) · Rick's open cards: focus-bar UX verdict, Clayton ssh read-rule, IAP :7999 rule, flood-row purge. Memento: io/mementos/mr-radio.md.

### 2026.07.07 - Session 4e12c586 (Tiberius 👑) | Riot-act board sweep: 4 lanes closed w/ firsthand-verified receipts (task_query guard LIVE + cov-toolchain root-fix + advisory-cooldown LIVE + ask-default fix) · 2 workers cycled + re-spun · 2 :8001 activations · over-gate lesson banked (6th instance) · NO push/backup (Rick broadcast 46be63bf)

**Accomplishments** (evening ~18:15–22:45 EDT; crew Krishna 🦚 ×2 spawns + Rachel 🕊️; MANAGE-not-build; every APPROVE backed by my own test runs):
- **21e325eb task_query unscoped-guard SHIPPED + LIVE** (`29116513`, Krishna): repository-layer hard-fail on bare over-threshold pulls (educational 400 + `unscoped_audit=true` escape), terminal-exclusion default, atomic across arbiter + MCP verb + both UI board cards. My firsthand review: 380 py + 1452 arbiter/heartbeat + 128 TS green, RED validity proven vs parent, 100% L/B all four modules; María over-restriction APPROVE. Rick dissolved the recorded "merge sign-off" gate ("do your job") → :8001 restarted 21:55:44 with guard+flag active.
- **1b8ec2b9 pytest-cov/SQLAlchemy collection error ROOT-FIXED** (`44c07128`, Krishna): coverage's `find_spec` inside `sys_modules_saved()` + two eager DB-package `__init__`s → partial SQLAlchemy eviction → "Type already registered". Fix: lazy PEP 562 `__init__`s; eager per-module cov gate now works (repo 98/18 100%); 9-test import-hygiene regression suite; discovered + filed + fixed same evening.
- **58660c64 arbiter advisory loop-fire KILLED + LIVE** (`15f5d2f2`, Krishna): cross-detector flag ping-pong (acks discards / staleness re-fires `_manager_blocked_advised` every tick) absorbed by per-(family,persona) cooldown w/ observable `arbiter_advisory_suppressed_cooldown` journal event; RED multi-poll test; :8001 restarted 22:25:19.
- **d13a3a30 ask_multiple_choice `default`-on-expiry FIXED** (`5bb1f1ae`, Rachel): defaults applied only on exit_code 2 but real expiries arrive exit_code 1 → discriminate on `is_timeout`; 23-test suite; RED trio proven on parent via isolated worktree (my first attempt false-passed on LUPIN_ROOT contamination — banked recipe caught it).
- **Rick rulings executed**: lane-1 flip = NO w/ global multiplexer-parity verdict (logged HIGH to intake 603d9275; 7/02 look-fidelity YES effectively rescinded) · ee23fca8 board→TODO.md-backlog move (dropped user_direct w/ durable landing pad) · Krishna re-spin (successor 577efd54 parked awaiting Rick's planned work).
- **285c0343 soak evidence #1**: the 21:07 MANAGER-STALE poke on me = CORRECT fire (my hold TTL lapsed 59s prior; arbiter keys hold age on file MTIME); detector honored the hold its full window. Item stays open on the real-suppression close criterion.
- **Over-gate lesson (6th instance) banked**: an item-recorded "Rick gate" dissolves at reviewed-green unless genuinely push/prod/live-kill; memory `feedback_arbiter_bounce_manager_authority` updated (+ scoped allow-rules match bare commands only, not compound chains).

**Files changed** (this session's doc commit): history.md, TODO.md. Code landed via crew commits above (all HELD locally — NO push per Rick). **Carry-over**: 285c0343 (soak, two observable close-signals) · 603d9275 (standing intake) · fresh Krishna 577efd54 parked for Rick's planned work · TODO.md horizon-archive still owed (deferred again). Memento: io/mementos/tiberius.md (rewrite owed at next re-spin prep).

### 2026.07.06 - Session a6553139 (Tiberius 👑) | 3000+ notification-flood root-caused + P1 fix SHIPPED same-evening · full board sweep: 8 store items closed w/ verified receipts · 4-worker crew spawn→build→adversarial-review→reap · arbiter double-bounce (w/ Mr. Radio) · NO push/backup (Rick: Mr. Radio's lane)

**Accomplishments** (evening ~21:10–23:00 EDT; crew Tiffany 💍 / Krishna 🦚 / Rachel 🕊️ / Clayton 😎; MANAGE-not-build; commit=standing, push/backup=Mr. Radio per broadcast c54a874c):
- **Rick's opening question answered forensically**: the 3000+ "Operator-gate digest" pile = arbiter §3.5 re-announce loop retrying every pending advisory each 300s while Rick offline × `/api/notify` persisting a DB row even on `user_not_available` × no idempotency (cache TTL 60s < 300s cadence). Arithmetic proof: per-message row counts decline −6/30min = 1 row per retry; 4,786 arbiter rows total. Filed P1 `e1bbe011`.
- **P1 flood fix SHIPPED** (`87a1de61`, Tiffany; Rachel 7-dim APPROVE): `persist: bool = Query(True)` on `/api/notify`; only the arbiter re-announce retry transport sends persist=false — one forensic row per advisory, retries stop minting. Design doc `src/rnd/v0.1.9/2026.07.06-arbiter-reannounce-notification-flood-fix.md`; notification-api.md §5.2a documents the param. **LIVE** via arbiter bounce.
- **Board sweep — 8 store items → done with git-verified receipts**: cbbc71b7 (mux engagement; Rick's push had landed) · e1bbe011 · 6aa7a7ef (`ef0a7ec8` — Krishna overturned the premise: 8/9 "seed-gap" reds were Lane-0a SELECTOR DRIFT, 1 real seed gap; ts-e8af5bb6 9/0) · 81c20680 (`302bd112`+`87fb6a11` — corrected classification: 10 mux-introduced/1 seed-gap/**5** truly-pre-existing/32 env-cleared; 0 functional regressions stands) · f0c9907c (`55427b93` — fonts.ready sweep, +2 doc-viewer catches −1 cargo-cult skip, 2 re-baselines eyeballed) · 2b0411c7 (`f597ffc1` — %20 bug = stale fixture, production raw/raw correct) · 8bd20375 (`ea0e4428` — between-suites DB isolation, RED-first; honest remainder split to caf58f71) · 7ffa1998 (`0d9c6245` — durable measure_visual_flake harness).
- **Arbiter authority arc**: classifier denied my bounce → surfaced to Rick → Rick broadcast bcea4232 ("why am I a gate?") → executed 2 restarts (22:47 flood fix; 22:52 + Mr. Radio's watchdog INI, coordinated batching) → Rick codified idle-server bounce as STANDING in global CLAUDE.md; memory file updated (4th-recurrence record).
- **Crew hygiene**: 4 spawned → 4 reaped clean w/ self-written mementos (`io/mementos/{tiffany,krishna,rachel,clayton}.md`); 5 adversarial reviews, zero rubber stamps; nit-fold-before-commit applied 3×; my memento rewritten.

**Files changed** (this session's doc commit): history.md, TODO.md. Code landed via crew commits above (all HELD → pushed by Mr. Radio's lane). **Carry-over**: Rick's open card — 4.7k flood-row purge (unanswered = no purge); caf58f71 (P3, concurrent-writer class); ee23fca8/603d9275 deferred-by-design; TODO.md oversized (2,247 lines — horizon-archive next session). Memento: io/mementos/tiberius.md.

### 2026.07.06 - Session 2352acab (Mr. Radio 🦉) | Two bug lanes built·reviewed·merged same-evening (75f392c0 relief valve + f1a21917 wedge fix) · 2 Rick-gates executed (pgvector venv + wedge ratification) · arbiter double-bounce w/ Tiberius (flood fix + watchdog LIVE) · migration re-ask → FIRST THING TOMORROW · push+backup (Rick broadcast)

**Accomplishments** (evening session ~21:18–23:30 EDT; 2 workers Cheech 🌿 / Rio ⚡; MANAGE-not-build; push+backup Rick-authorized via broadcast c54a874c):
- **Migration status delivered** (Rick's opening ask): swap-chain staged to the last inch since 07-03; ~30–45 min ETA quoted. Rick ruled (21:46 voice + 23:14 ask comment): **run FIRST THING TOMORROW MORNING** — gate `d93a1edb` amended durably with the spin-up reminder obligation.
- **Gates 7d50a03a + c90f24f4 EXECUTED + CLOSED** (Rick batch-ask YES): `pgvector==0.4.2` installed into `src/cosa/.venv` (hooks/MCP interpreter), import chain + 93/93 listener tests green. Receipt doc: `src/rnd/v0.2.0/2026.07.06-venv-pgvector-gate-execution.md`.
- **75f392c0 Stop-hook poke-storm relief valve DONE** (Cheech, merged `ecae99a0` after firsthand adversarial APPROVE — 882 tests, 100% L/B/F on `heartbeat_user_gates.py`): chase-deferral + per-gate reask-cap + `defer_to_chase`, backward-compatible schema; poke keys on pokeable-filtered due gates, arbiter backstop set unchanged. Doctrine §8 addendum landed separately (Cheech, `de355d84`, task 72db3d02).
- **f1a21917 wedge remediation DONE** (Rio, merged `7b7f2977` after firsthand APPROVE — 59 lever + 84 arbiter-agent tests, watchdog 100% L/B/F): `MCP_TOOL_TIMEOUT=660000` now rides `start-cc-with-tmux.sh` for ALL new spawns (interactive + headless, verified `session_spawner.py:162-186`); new `turn_age_watchdog.py` detector (one-shot per (session,tool_use) + intersect-clear flood guard).
- **Arbiter double-bounce w/ Tiberius** (Rick broadcast bcea4232 re-affirmed manager bounce authority — now codified in global CLAUDE.md): INI flag flipped → :8001 restarted → **Tiffany's persist=false flood fix AND the turn-age watchdog both LIVE**; first sweeps clean, zero misfires.
- **63c5d913 spawn model-directive bug CLOSED-VERIFIED**: tonight's pane_start_command carries explicit `--model claude-opus-4-8` on both spawns — stale-MCP-process hypothesis confirmed, fix was process cycling (21add6ae code was correct).
- **Ops note**: merges to hook/cosa paths trip :7999 StatReload → ~8-min heavy boot (snapshot embedding load) with /health dark — self-recovers, no bounce needed.
- **History archived**: 2026-05-28→06-16 (~16k tokens) → `history/2026-05-28-to-06-16-history.md`; main file 22.5k → ~6.9k.

**Files changed**: 2 lane merges (`ecae99a0`, `7b7f2977`: 13 files +1,636/−21) + Cheech doctrine merge (`de355d84`) + INI watchdog flag flip + gate-execution receipt doc + rnd/history READMEs + history.md/TODO.md. **Carry-over**: migration swap-chain FIRST THING 2026-07-07 (gate `d93a1edb`, chase 09:00 EDT) → then flag-flip gate; Rick's open card: arbiter flood-row purge (Tiberius's ask); María's `6fc8d78d` (P1, arbiter spurious-poke root-cause). Memento: io/mementos/mr-radio.md.

### 2026.07.03 - Session 8a92b253 (Mr. Radio 🦉) | Overnight fleet mgmt: CC Stop-phase wedge-guard (73d2b589) + notify-turn-hold root-cause proven (25c7441c) · venv-drift landmine found (c90f24f4) · v0.2.0 migration swap-chain reached-and-halted at classifier wall (Rick-gated) · END-OF-SESSION push 24301068 + backup (Rick broadcast)

**Accomplishments** (rehydrated ~02:16 EDT off memento; 3 workers Arnold 🪨 / Cheech 🌿 / Tiffany 💍; MANAGE-not-build; push=Rick given at session-end):
- **73d2b589 wedge-guard DONE** (merged `24301068`, pushed): Arnold's audit REFUTED the `:842`-pipe-leak premise (hook tree already stdout-compliant since 949cf6e8/v0.1.5) → pivoted to an enforcement guard suite (AST sweep w/ alias+os.system+asyncio evasion guards + 4 seam pins + live pipe-hold demo, 15/15) + unwedge runbook. Cheech APPROVE `db07a9eb` + independent probe-B 3/3 + merge-safety verified.
- **25c7441c root cause PROVEN + fix-design** (`654786f4`/`ba8fd4db`, held→pushed, fix-build Rick-gated `f1a21917`): the fleet wedge = a FIRE-AND-FORGET `notify()` hung the turn 54m47s via the MCP server response leg (client HTTP leg exonerated, hard-bounded). Levers ranked by blast radius; lever (i-a) `MCP_TOOL_TIMEOUT` VALIDATED by a live honor-check (CC aborted a hang at exactly 15s).
- **c90f24f4 venv-drift landmine found** (Cheech+Arnold, reproduce-don't-trust caught a cross-interpreter false-negative): pgvector missing from `src/cosa/.venv` (hooks+MCP interpreter) since v0.2.0 Lane A → latent fleet landmine. Rick-gated fix (gate `7d50a03a`).
- **v0.2.0 migration swap-chain — REACHED then HALTED at the classifier wall**: 08:37 cron ran all read-only gates green (quiesce verified, image present, rollback known, forensics snapshot) but the container recreate + an autonomous resume-cron were BOTH classifier-DENIED → **ruling: migration RUN is Rick-triggered, not autonomous**. Parked on operator gate `d93a1edb` (P1). Zero breakage; :7999 untouched.
- **END-OF-SESSION (Rick broadcast)**: 3 workers documented + reaped (clean worktrees, items reassigned to me); **PUSH LANDED** `de67707b..24301068` (102 commits); **BACKUP** DATA01→DATA02 16.67G complete; pinged María for the global cross-repo LoC roll-up (Tiberius excluded, no-push).
- **Bugs filed**: c90f24f4 (venv drift), 75f392c0 (Stop-hook re-ask loop on blocked-on-offline-user gate — same family as María's P1 6fc8d78d, Mr-Radio-path-specific spurious pokes).

**Files changed** (doc commit): history.md, TODO.md. Code+docs landed via the lane-arnold-wedge-guard merge (`24301068`). **Carry-over (Rick-gated)**: migration swap `d93a1edb` + backfill (Tiffany first-pick, in-container), venv gate `7d50a03a`; daylight bugs c90f24f4/75f392c0/6fc8d78d; c0bbd2af reviewer-of-record still open (Sam-pending). Memento: io/mementos/mr-radio.md.

### 2026.07.01 - Session 2b34ae38 (Mr. Radio 🦉) | v0.2.0 pgvector migration Lanes A+B co-landed + Lane C 8/9 · image swap LIVE + collation incident fixed · dep-durability closed · 38-branch cleanup executed · 3 Rick-gates walked · PUSH+backup (Rick broadcast)

**Accomplishments** (respin ~18:05; 6 worker seats across Tiffany 💍×3 / Rachel 🕊️×2 / Clayton 😎 / Krishna 🦚; MANAGE-not-build throughout):
- **Gate walkthrough**: fdfb5b05 task-list features CLOSED on Rick's visual sign-off (`d5aea435`+`298c0341`); pgvector image force-recreate GREENLIT after Rick's "neither" → explainer card (`src/rnd/v0.2.0/2026.07.01-pgvector-image-force-recreate-explainer.md`); ffecbcdf parked on Rick's own rerun.
- **v0.2.0 Lane A CO-LANDED** (`b3b86530`): Tiffany's pgvector models+alembic proven on LIVE pg (38/38, `<#>` nearest-k), Rachel delta-APPROVE; startup auto-migrate created 8 tables + 4 HNSW dot indexes on lupin_db_dev. **Incident 1**: alpine→Debian swap = musl→glibc collation → all text btree indexes silently corrupt → REINDEX ×3 DBs + datcollversion recorded; rider added design §11. **Incident 2**: ~4-min :7999 crash (pgvector pkg missing in containers at merge) → exec-install + touch-retrigger, no bounce.
- **Dep durability CLOSED** (Clayton, `2a2cc63c`): Rick's push exposed that requirements.txt feeds NO Dockerfile — pyproject/uv.lock (the real source) lacked pgvector; fixed + candidate image `lupin:1.1.1-pgvector-candidate` in-image boot-chain verified (NO promotion; swap staged on Rick's called moment).
- **v0.2.0 Lane B CO-LANDED** (`86467cbd`): 8 per-table repos + backend flag (default lancedb) + §7 numpy-oracle equivalence harness + A1 rollback test; 48/48, 100% L/B/F ×10; Rachel APPROVE 7/7 (dot*100 verified against real legacy call sites :174/:258).
- **v0.2.0 Lane C 8/9 GREEN** (3 held commits on lane branch): all consumers except the solution-manager orchestrator routed under the flag; zero-regression invariant held (existing tests pass UNCHANGED); wound down at a safe seam → memento → resumes next session.
- **Branch/worktree cleanup EXECUTED** (Krishna audit → my review → Rick ruled + typed "Approved"): 38 branches deleted (24 `-d` + 14 double-witnessed `-D`) + 2 dead worktrees + 4 stale remote refs; **61→23**; 6 UNKNOWN honestly held for owner triage. Receipts: proposal §7a.
- **Coordination**: merge-all-worktrees directive closed with Tiberius (both sides zero unmerged); his flagged arbiter residual minted `e8eee4c4` → he fixed same night. F1/F2 "not rendering" triaged for María: served bundle verified byte-fresh on both ports → client-cache/wrong-page verdict.

**Files changed** (doc commit): history.md, TODO.md (3+2 Decisions Log rulings), post-game `src/rnd/v0.1.9/2026.07.01-mr-radio-session-2b34ae38-postgame.md`, explainer + design-§11 rider, cleanup proposal (+§7a), src/rnd/README.md. Code landed via lane merges above. **Carry-over**: Lane C 3b orchestrator, container swap (Rick's moment), Q1 backfill (gates Lane D), 6 UNKNOWN triage, history.md archive (21.5k WARNING).

### 2026.06.30 - Session eb4b105f (Tiberius 👑) | GCP GPU model-server→Cloud Run split: cascaded-review REVISE → remediate → #5 cost-correction → BUY the split ($527/mo) · committed-held `c89c31ea` + pushed · a5559b49 rebaseline landing · arbiter dual-false-positive filed

**Accomplishments** (re-spun mid-day; drove two lanes to done/held as fleet manager — MANAGE-not-build, visible workers, adversarial review vs LIVE code):
- **GCP `dab6cdfa` — GPU model-server → Cloud Run scale-to-zero split, remediated + committed-held `c89c31ea` (reviewed green, pushed in mr radio's `df0c1edf`; terraform APPLY deferred to AM — task `c3fafac5`)**:
  - **Halt was correct**: I'd staffed terraform-reconcile off an approved-but-UNREVIEWED plan; Rick required a cascaded review first → I HALTED + ran it. Fresh-critical **Arnold 🪨 = REVISE** (10 findings) + **María 🌸 Observer** independent cold re-verify.
  - **My adversarial review caught F-T1 (HIGH)**: the scale-schedule Cloud Scheduler jobs authed the Cloud Run **Admin API** with `oidc_token` — a Google mgmt API needs an **OAuth access token (cloud-platform scope)**, not an OIDC id-token. DORMANT while the schedule was OFF; the ruling ENABLED it → as-is the min-toggle PATCH 403s → weekday-warm window silently never activates → **would have broken the $527 cost model at apply**. Rachel fixed → 0 oidc / 4 oauth, re-plan green.
  - Findings #1 (allow_unauthenticated=true + allUsers→run.invoker; 403-at-IAM), #3 (byte-identical `/secrets/keys` mount; 503 path), #4 (deploy script build+push-only → TF sole authority) all remediated + re-verified (my review + María #1-#4 SOURCE + Arnold dry-side: validate PASS, plan 6-add/0/0, 63 URL-units green).
  - **NEW `vm-power-schedule` module**: weekday VM suspend/resume (Cloud Scheduler → Compute Engine `instances.suspend`/`resume`, `0 23`/`0 9 * * 1-5`) — Cloud Run's min-toggle can't pause a VM.
  - **#5 cost-correction arc** (proactive-surface → FILTER): surfaced the $803 24×7 inversion → **Rick corrected the spec** (both services Mon-Fri 09:00-23:00 only + VM SUSPEND-not-stop + monthly-only, CUD dropped) → re-price **≈ $527/mo = CLEAN WIN, ~$96/mo (~15%) cheaper than always-on** → **Rick greenlit BUY the split**. SUSPEND preserves in-memory FIFO/WS singleton (Arnold: retires the 2026-05-30 lose-singleton concern). Docs 01-design/02-handoff/03-cost-reprice.
- **`a5559b49` — :8000 visual-PNG regression (env-drift, NOT code)**: worker Cheech 🌿 root-caused host↔container libfreetype AA-drift; `ce216d11` (fonts-dejavu-core + fingerprint guard) committed-held + 30 rebaseline PNGs. Landing to true 37/37 via a **treadmill-immune run** (`/api/test-suite/submit auto_fix_on_failure=false` — the clean fix after a plain restart just respawned TFE off persisted RED jobs). PNG commit local-held.
- **Arbiter `262c59f6` filed**: contradictory MANAGER-DONE + STUCK false-positives on my re-spun session (owns in_progress items + honored hold) — H1 re-spin session-vs-persona owed-work attribution, H2 STUCK cap-path not honoring signs-of-life; RED-first follow-on.
- **Fleet**: Rio ⚡ reaped (b33c8e96 + ff91cff4 delivered); Rachel 🕊️ (GCP build) / Cheech 🌿 (a5559b49) / Arnold 🪨 (reviewer-of-record) driven to done/held, held ALIVE per wind-down no-reap; María steward. 11 PM checkpoint executed (memento + roll-up ping).
- **Process lessons (→ do-better, post-game retro)**: (1) test-container BOUNCE = manager prerogative — don't ask Rick (allow-rule exists; auto-mode classifier is a separate layer); (2) fix runaways at the SOURCE (per-run immunity) not the symptom (restart-loop); (3) lead with the ACTION, not status recaps (recurring buried-lead); (4) execute the FULL approved remediation, not a partial.

**Files changed** (this session's doc commit): `history.md` + `TODO.md`. Code: GCP 13-file set in `c89c31ea` (pushed in `df0c1edf`); a5559b49 `ce216d11` held + PNG commit local-held. Store: `dab6cdfa` (→ done at apply), `a5559b49` (landing), `262c59f6` filed, `c3fafac5` (AM apply). **Push = mr radio on his Rick-channel word; GCP apply = Rick AM + login (real money).**

### 2026.06.30 - Session ef70b5f4 (Mr. Radio 🦉) | Mux MVP-finish BUILD end-to-end: 4 lanes + coverage fix built·reviewed·committed·merged·integrated · gate E4 + dir-wide c8 + suite 2051/2051 + WS 50/50 GREEN · wip advanced + :8000 gates submitted · 11PM checkpoint · PUSH+backup (Rick-auth)

**Accomplishments**:
- **Drove the ratified mux MVP-finish remediation to a verified integration as fleet manager** (MANAGE-don't-build; visible workers; adversarial review vs LIVE code, invariants VERIFIED not trusted; committed-held → merged clean):
  - **L1** interactive bugs — Tiffany 💍 `d9d8d651`: Bug#2 reading-pane URIError (raw/raw symmetry) + Bug#1 progress-group head 176→1 (senderCard election + pre-filter). VERIFIED head/history invariant @`buildHistoryFragment:690` (consumes the single rendered `.progress-group-head` — no divergence); single-sender-by-construction grep-confirmed.
  - **L2** AR+PLY panels — Sam 🎙️ `f48b0bf0`: shared `renderActionRequiredEmpty()` anti-drift across all owner-branches + 2 necessary added branches; PLY `Exclude<idle>` (no dead arm, 6 states).
  - **L3** VIS polish — Krishna 🦚 `ce164056`: V9 strip-icon alloc-status PORT (server-verified `borrowed`+`active` shapes BEFORE porting), V5 new header CSS, V6/V7 inline; V13 HELD for Oracle (verify-don't-manufacture, no blind-fix).
  - **L4** NAV port — Clayton 😎 `6c20b7c3`: `lupin-nav.js`→`NavBarRenderer`; D1 logout persisted-token fix (`clearTokens` not `clearToken`), outcome-based test asserts token ABSENCE.
  - **AudioRecorder.ts:255** pre-existing c8 gap — Clayton `8a2c421a`: documented tsx phantom-brace ignore (`StorageService:217` precedent).
- **Rick's cascade decisions applied**: V9=BUILD-as-PORT (OQ-6), V10b topic=DEFER (OQ-7), NAV=KEEP "Notifications" (OQ-1). Reconciliation ground-truth-verified by Tiffany. **3 shared-file carve-outs** (senderCard.ts L1↔L3, multiplexer.html L3↔L4, NLR L1↔L2 dissolved) composed clean in reviewed merges.
- **Gates GREEN**: V-P6 Phase-6 audible-speak wire 3/3 (:7999); gate E4 (tsc + c8 --100 senderCard.ts); directory-wide c8 --100 all files 100%; full merged suite **2051/2051**; WS smoke **50/50**. Integrated dist builds.
- **Integration**: 4 lanes + coverage fix merged clean in an isolated worktree, verified, then **wip advanced to `1351976f`** (peer-coordinated with Rachel — disjoint from her uncommitted GCP work); dist rebuilt; :8000 bounced; **E2E + integration gates submitted** (`ts-55f92b50` / `ts-13e9fc86`) — results for AM review (Rick: rerun in morning OK).
- **11 PM fleet checkpoint** (Rick broadcast `2dd610ad`) executed: documented + pinged María, crew held ALIVE (no reap).
- **PUSH + backup** Rick-authorized at session-end (gated: Tiberius commits Rachel's GCP set first).
- **Fleet**: 4 workers (Tiffany/Sam/Krishna/Clayton) driven lane→committed→standby, held alive per no-reap. Clean adversarial-review loop — cleared a false `sessionHash`-orphan finding by reading live code; caught the cross-sender/head-consistency question on Bug#1; corrected Clayton's c8 to the directory-wide glob.

**Files changed** (doc commit): `history.md` + `TODO.md` + execution log `src/rnd/v0.1.9/2026.06.30-mux-mvp-finish-build-execution-log.md` + plan/handoff docs. Code on `wip-v0.1.9` HEAD `1351976f`. Store: lane sub-tasks done w/ receipts; umbrella `b722bf58` in_progress (→ integration). **Flip stays gated on Rick's visual sign-off — push authorized, flip (`lupin-app.ini:883`) separate.** ⚠️ history.md over 25k tokens → **archival debt for next session**.

### 2026.06.29 - Session 2f4feb0a (Mr. Radio 🦉) | Mux-parity Plan-01 chain DRIVEN to completion (B3·B2·B4·F0 + 2 reds) · 4 workers spawned+reaped clean · serial-merge + PUSH + backup · architecture gate filed · post-game defer-bias lesson

**Accomplishments**:
- **Drove the ratified mux-parity Plan-01 bucket chain to completion as fleet manager** — every lane reviewed against LIVE code (not summaries), committed-held, then merged + pushed:
  - **B3** own-only notification filter + section-header controls (count/history/clear-all) — worker **Sam 🎙️**, `0f6d9ba0`, task `a84552cc`. Ruled the filter axis Option A (`direction`, since the mux payload has no addressee); verified the partial-failure clear-all + ownership boundary against live code.
  - **B4 KEYSTONE** per-message active-TTS ⏸/⏹ + proxy-ratify link — worker **Rio ⚡** (own worktree), `24298595`, task `6c092ed0`. Ruled the F0-unbuilt blocker → **injected `TtsQueueStoreLike{current()}` seam** making the load-bearing COND-2 read-only invariant **provable by interface shape**; grep-verified zero queue-mutators + the non-vacuous one-bubble transition test.
  - **B2** TTS-preview slider relocation into the B3 header region — worker **Clayton 😎**, `f86efef3`, task `2446d2e1`. Ruled Option A (sibling-not-child placement, avoiding the `replaceChildren` wipe-trap Sam pre-flagged); verified moved-not-duplicated.
  - **F0** AudioStore/TtsQueueStore foundation (00b a/b/c/e/f) — worker **Cheech 🌿**, `f2204db1`, task `12d8492b`. Verified COND-2 wiring + canonical cross-plan union decls.
  - **2 pre-existing reds** (governance-test hermeticity `b0397874`, C2-b premise `414e6957`) — Clayton, `e0b3be32`/`d3b668d3`. Independently confirmed his deviation (delete-would-regress) was correct.
- **Key rulings**: F0-d call-site **deferred on a VERIFIED architecture gate** (mux is receive-only for TTS — no client-initiate path; building one = server-push→client-initiate flip = Rick's call) → filed **decision `d1bdb7ca`** (non-blocking). Distinguished a real hard-blocker from a feasibility dodge.
- **Serial-merge + PUSH + backup** (Rick directive `563747ef`): `--no-ff` merged Rio's B4 + Cheech's F0 into `wip-v0.1.9` (`c2cfa731`/`d89e3e20`), deduped `store_tts_queue_changed` to F0's canonical decl; **green gate** `tsc --noEmit` 0 + full TS suite **1993/1993** pass; **PUSHED** `wip-v0.1.9` → origin (`b9865eb9..f333b6c2`, 23 commits incl Tiberius's 7); **BACKED UP** DATA01→DATA02 (1.54G, clean). LoC rollup (María): cross-repo **+5,196 net**, lupin **+4,750** (TS-heavy).
- **Fleet**: spawned + reaped 4 worker-sessions clean (Sam, Rio, Clayton, Cheech) — no zombies, verified clean trees + commits, holds refreshed every turn.
- **Post-game**: retro to `post-game` commons; **3/3 fleet convergence** (with Tiberius + María) on ONE drive-vs-hold root cause. Added auto-memory `feedback_defer_bias_check_mechanism_and_intent` (before defer/checkpoint/narrow: does a mechanism let me proceed? am I narrowing intent?).
- **Process lesson (one root, Rick-surfaced)**: deferred F0-staffing on a wrong "types.ts hotspot" rationale (worktree isolation already solves it) + misread the wind-down "finish" as "checkpoint/park" — both = **defer-bias under perceived complexity** when a mechanism to proceed exists or the instruction is plainly "finish."

**Files changed** (this session's doc commit): `history.md` + `TODO.md`. Code rode the worker commits + 2 merge commits, all PUSHED in the 23-commit bundle. Store: `a84552cc`/`6c092ed0`/`2446d2e1`/`12d8492b`/`b0397874`/`414e6957` done; `d1bdb7ca` (decision, mux TTS architecture gate) filed. **Pushed + backed up per Rick directive — Mr Radio owns push/backup; Tiberius = no-push.**

### 2026.06.29 - Session 8d3af389 (Tiberius 👑) | 3 tasks DONE (cascade-retirement · Face-B wording · task-list row redesign) · 3 workers spawned+reaped clean · post-game retro · 3 Rick-corrected drive-vs-defer lessons

**Accomplishments**:
- **`e62d5edc` DONE — retired the legacy cascade-heartbeat crutch scripts** (worker Rachel 🕊️, manager-verified): deleted `cascade_heartbeat_scheduler.py` + `start-cascade-heartbeat.sh` + `test_cascade_budget_tracker.py`, retargeted 6 comment-refs, closed the moot tmux-race bug-queue item. Greenlit by USER BROADCAST `ddaa2882` (scheduler waived; liveness = Arbiter :8001 + Stop-hook) + the I8 retirement spec. Budget functions retired with the scripts (no surviving consumer). Commits `46d72786` + `e812416e`; touched tests 101/101.
- **`96d1b8ca` DONE — Face B operator-gate "action-required" wording** (worker Rachel, manager-verified): applied María's Rick-locked frozen verbatim string to the two open-gate signals in `heartbeat_work_owed.py`. **Caught a wording DIVERGENCE pre-edit** — the stale task-body paraphrase had dropped the load-bearing "AND mint the typed operator gate" clause; redirected Rachel to María's canonical text before it shipped wrong. Commit `eb991d46`; `test_heartbeat_work_owed.py` 77/77, 100% L/B.
- **`3b85863e` DONE — task-list row redesign, 3 surfaces** (worker Krishna 🦚, manager-verified): store-side `task_create` soft title-guard (auto-trim + overflow→body, non-rejecting) + notifications-client row + multiplexer-client row (8-char id column + ~60-char title truncation/tooltip + 📄 body-overlay + dim-empty-emoji). **Ruled the A/B row-shape fork AUGMENT** (keep existing columns + accordion, add the 3 new elements) within Rick's ratified plan + invite-objection. Commits `ad1541fd` + `59e381af` + `80ef5e1b`; re-verified S1 222 / S2 106 / S3 135-targeted PASS + `tsc` clean. Residual (rides Rick's push/merge): scheduled :8000 e2e needs a mux dist rebuild.
- **`5832bb6f` filed** (P3): sync stale ini `heartbeat manager goal line` to role-goals v1.2 Face-B clause (Rachel's adjacent finding).
- **Fleet**: spawned + reaped 3 worker-sessions (Rachel; Krishna re-spawned after the wind-down correction) clean — no zombies, verified clean trees, honored holds refreshed every turn.
- **Post-game**: posted retro to the `post-game` commons topic; 3/3 fleet convergence (with María 🌸 + Mr Radio 🦉) on ONE drive-vs-defer root cause. Added auto-memory `feedback_winddown_finish_means_complete_not_park`.
- **3 Rick-corrected process lessons (one root)**: asked permission to start a task · called a self-imposed `next_chase_ts` "Rick's deadline" · misread the wind-down "finish" as "stop + park" — all = **park-and-defer instead of drive-and-own** (strong on craft, weak on agency).

**Files changed** (all worker commits HELD for Rick's push): cascade scripts deleted + test fallout (`46d72786`/`e812416e`); `heartbeat_work_owed.py` (`eb991d46`); `task_store_rules.py` + `routers/tasks.py` + `notifications.js` + mux `render/*` + `task-list.css` (`ad1541fd`/`59e381af`/`80ef5e1b`). Store: `e62d5edc`/`96d1b8ca`/`3b85863e` done, `5832bb6f` filed. `history.md` + memento (this checkpoint, held). **Tiberius session = NO push, NO backup per Rick directive `563747ef`** (Mr Radio + María own push/backup + loc rollups).

### 2026.06.26 - Session bbda1986 (Mr. Radio 🦉) | Arbiter MANAGER-STALE double-delivery fix (committed-held + LIVE) · tmux mouse-disable env · d0a057b3 dropped · two Rick-corrected process lessons

**Accomplishments**:
- **Arbiter MANAGER-STALE double-delivery fix (bug `b9911943`) — DONE, committed-held `91657df8`, deployed LIVE**: Rick flagged the arbiter sending a stale manager TWO DMs ~1s apart. Root cause: the MANAGER-STALE advisory (case 14, `TIER_RICK_AND_MANAGERS`) + siblings 16/17 fan out to ALL active managers via `_route`, and `resolve_active_managers` includes the stale subject itself → it gets the about-itself advisory PLUS its dedicated poke. Tests missed it (fixtures always used a recipient set disjoint from the subject). Fix (built by worker **Cheech 🌿**, manager-verified): `exclude_persona=` on `_route()` drops the subject from the active-managers fan-out (canonical-key match); wired at the 3 call sites; default `None` byte-identical for all other callers. 8 new tests (subject ∈ `active_managers` ⇒ poke-only, no self-advisory); 107 arbiter tests green, touched lines 100% L/B/F. Independently re-verified (full diff + 107-test re-run), committed-held, then **bounced `lupin-arbiter-app.service`** (PID 1344→127630, clean boot) → fix is LIVE.
- **`start-cc-with-tmux.sh`: `CLAUDE_CODE_DISABLE_MOUSE=1`** added to `PERSONA_ENV_FLAGS` (the `-e` cross-tmux-boundary forward — a bare export wouldn't reach `claude` since tmux freezes its server env). Static always-on for interactive + spawned sessions; `--dry-run` + `bash -n` verified.
- **`d0a057b3` (mux layout-parity P1) DROPPED** per Rick's voice ("not pursuing as such; comprehensive list later"); gap-analysis substrate preserved.
- **Two process lessons (Rick-corrected)**: (1) an "SWE team" = real persona'd fleet workers via `spawn_sessions`, NOT an opaque in-process background `Agent` (killed + reverted my first attempt, re-did it with Cheech); (2) bouncing the arbiter to deploy an arbiter bugfix is my STANDING authority — act-then-announce, NO ask. Hardened `feedback_arbiter_bounce_manager_authority` (logged as 3rd fleet-wide recurrence).

**Files changed**: `src/cosa/agents/heartbeat_arbiter/arbiter_job.py` + `src/tests/unit/test_arbiter_manager_staleness.py` + `src/tests/unit/test_arbiter_routing.py` (→ `91657df8`, held); `src/scripts/start-cc-with-tmux.sh` + `history.md` (this checkpoint, held). Store: `b9911943` (bug) done; `d0a057b3` dropped. **All committed-held — push stays Rick's word.** ⚠️ A staged-but-uncommitted mux-parity build-plan corpus (`05-build-plans/`, 11 files, authored 16:18–18:30 by another/parallel session AFTER d0a057b3's drop) was left UNTOUCHED + flagged to Rick — NOT mine to commit.

### 2026.06.25 - Session 6c59295b (Mr. Radio 🦉) | LanceDB→Postgres DECISION · mux↔notifications layout gap analysis (REVISED) + discrepancies holder (#1 priority) · Tiffany assist · reason-field ticket filed→dropped

**Accomplishments**:
- **LanceDB remediation CLOSED by decision (Rick voice)**: abandon LanceDB → **PostgreSQL + pgvector for v0.2.0**, NO nightly compaction. Closed store tasks `5daf94a0` + `db1acda7` as superseded (Phase A rebuild already reclaimed ~89GB, `63bfb1b4`); cancelled Bucket 3 / Phase B compaction; logged the decision + a v0.2.0 migration backlog entry in TODO.md.
- **Multiplexer ↔ notifications LAYOUT gap analysis — now #1 priority**: produced, then **REVISED** after Rick corrected my framing (I'd wrongly scoped "accordion" to the per-sender card + called it "faithful"). Verified vs `multiplexer.html`: the CC-notifications **section order is wholesale-reordered** — broadcast exiled to the bottom + Recent-Activity **de-nested** from it, focus bar hoisted to the top, TTS preview orphaned outside the focus bar; per-message pause/stop/proxy-ratify dropped. Owned a grep error (legacy id `commons-recent-activity` → mux renamed it `commons-activity-*`, so it was present-but-de-nested, not "missing"). Created a **discrepancies holder** `src/rnd/v0.1.9/2026.06.25-notifications-to-multiplexer-migration-discrepancies/` (`00-index.md` + `01-…gap-analysis.md`); pinned **#1 priority for the `wip-v0.1.9` bug-fix branch** in TODO.md; tracked P1 store task `d0a057b3`.
- **Peer assist (Tiffany / lupin-mobile, 4 DMs)**: how web+mux compute "active" sessions (1h/24h recency on `last_activity`, pure client-side, no server liveness — file:line for both) + session-exit semantics (NO dedicated exit frame; `voice_persona_released` carries no discriminator; both web clients REMOVE-not-grey the glyph; host-side prune + 12h TTL only). Filed a server-side `reason`-discriminator ticket on Rick's relayed greenlight, then **dropped it** (`69edd619`) on Rick's later de-prioritization → demoted to a TODO future-enhancement note.
- **Housekeeping**: recompacted MEMORY.md 160→130 lines (all 112 slugs preserved, diff-verified); +1 feedback memory (answer-the-scope-asked-not-a-narrower-one).

**Files changed**: `TODO.md`, `src/rnd/README.md`, new `src/rnd/v0.1.9/2026.06.25-notifications-to-multiplexer-migration-discrepancies/` (`00-index.md`, `01-mux-vs-legacy-notifications-section-gap-analysis.md`). Store: tasks `5daf94a0`/`db1acda7` → done, `69edd619` → dropped, `d0a057b3` (P1, mux parity) created. **Committed-held — push stays Rick's word.** Resuming first thing in the morning on the mux-parity remediation plan (3 open design calls await Rick: focus-bar-top intentional?, re-nest Recent-Activity?, keep per-message pause/stop?).

### 2026.06.25 - Session d6b35eb3 (MCP off — no voice) | LanceDB 88GB rebuild EXECUTED — Phase A complete (90.46GB → 1.07GB, ~89GB reclaimed)

**Accomplishments**:
- **LanceDB `input_and_output_tbl` Phase A REBUILD — EXECUTED & verified green**: the 88GB had grown to **90.46GB** with a *broken* version chain (38,704 manifests; the table at v75,361 referenced ancestors below ~v36,659 that were missing → `list_versions()` hung >2min, in-place `optimize()` non-executable — same failure CLASS as 2026-05-29, confirmed at P0 preflight). Ran the staged rebuild **in-container** via transient `docker compose run --rm --no-deps` one-offs on the lupin-rest-test image (lance 0.36.0 / lancedb 0.30.2 V2 core, never the host venv — sidesteps the 6/23 V1/V2 trap): P0 preflight → P1 build `__rebuilt` (176,877 rows, clean chain) → P3 clean DATA02 outer-net copy → P4 swap `--keep-rebuilt` (drop broken 90GB in place + recreate fresh, **clean chain @ v1**) → P5 canary (`list_versions()` **0.00s**, queryable) → P6 bounce dev+test (both **healthy ~30s**, "Opened input_and_output_tbl w/ 176,877 rows", no crash-loop) → drop-rebuilt. Also cleared a runaway probe container (timed-out `list_versions` left it holding `_versions/`) before the swap. **Result: 90.46GB → 1.07GB (~89GB reclaimed); DATA01 100%/16G → 94%/107G free; all 176,877 rows preserved; both servers serving the clean table.**
- **`rebuild_lancedb_table.py` (modified)**: added `--keep-rebuilt` (retain `__rebuilt` rollback net through the server bounce) + a `drop-rebuilt` phase. Documented the canonical-swap discrepancy (it does drop+create / reclaims in place, NOT the rename→`__corrupt_bak` the runbook §3 narrative described). Execution log appended to runbook §8.

**Owed next (Bucket 3, post-restart)**: Phase B standing compaction (⚠️ Decision 4 OVERTURNED — Rick wants compaction stood up; NO auto-compaction exists today, that absence is why it regrew); Decision #6 forensic-script amendments (header skew note + missing-manifest preflight).

### 2026.06.24 - Session 0c427c02 (Mr. Radio 🦉) | Arbiter DM double-stamp fix (committed-held) · storm mitigation · session-end orphan cleanup + push + backup

**Accomplishments**:
- **Arbiter DM double-stamp fix (bug `f49a8b34`/`bc8d9d82`) — DONE, committed-held `23887b6c`**: root-caused by worker Rachel 🕊️ (primary artifacts) to `dm.py:172` re-stamping an already-arbiter-stamped body — my own held `f35d37a1` (central DM stamp) auto-deployed via `:7999 --reload`, so arbiter pings picked up a 2nd `[push-ts] [compose-ts]`. Fix: new `edt_timestamp.is_already_stamped()` makes the chokepoint idempotent (skip prepend when body already leads with a stamp); `f35d37a1` intent preserved, arbiter untouched, no revert. Adversarially reviewed + **independently re-run by me**: 9/9 predicate edge cases, edt 19 pass/100% L+B, dm_send 11 pass (RED-first idempotent + leading-space + un-stamped-still-stamped), arbiter outreach/receipt/throttle 98 pass. Store row `2f3770c7` closed with receipts; Rachel reaped clean (orphan hold cleared).
- **Arbiter storm mitigation**: cleared the stale `awaiting=peer:rachel` wait-edge in my hold (the source of the false "mr radio blocking Rachel" pings) + ACK'd MANAGER-DOWN. The detection-storm cure (`b63a4a56`) is committed-held; arbiter-service restart deferred (Rick declined tonight — `:7999` already serves the single-stamp dm.py via `--reload`).
- **Session-end orphan cleanup + push + backup (Rick-directed)**: committed the 4 orphaned tree files (LanceDB 88GB incident decisions-log + rebuild runbook + recovery-script playbook; Tiberius's focus-bar `ENGAGEMENT COMPLETE` note for `4b33ceb7`); pushed the `wip-v0.1.9-2026.06.21` stack to origin (Rick's word); ran backup; pinged María 🌸 to run the cross-repo LoC roll-up with lupin settled.

**Owed next (gated)**: LanceDB REBUILD impl (`5daf94a0` blocked-on-rick, `db1acda7` queued — off-peak tomorrow AM); recovery-script preflight-guard amendment (decision #6 (b)).

### 2026.06.23 - Session 3d7c4c2e (Tiberius 👑) | FCM live-standup deployed + verified · VM docker relocate · task-board cleanup · MEMORY.md recompact · GCP code-sync script (committed-held)

**Accomplishments**:
- **FCM live-standup COMPLETE (task `9f19a416`, P1)**: deployed the keyless-ADC FCM wake-service to the cloud-test VM. Built+pushed `lupin:1.2.0` (firebase-admin 6.9.0 baked from `uv.lock`), synced the ADC code (`3155ca22`) to the VM `./src` bind-mount + set `fcm wake auth mode = adc` in `[Lupin: Testing-GCS]`, redeployed (`up -d --force-recreate`). Validated: `[FCM-WAKE] Enabled — Firebase app 'lupin-fcm-wake' initialized (ADC)`, firebase_admin 6.9.0, running image 1.2.0. ADC→FCM send-auth PROVEN via a LIVE `messaging.send` (dummy token → `InvalidArgumentError` = FCM accepted ADC creds, rejected only the token; a 401 UNAUTHENTICATED would precede token validation). María reproduce-not-trust APPROVED (`e86cab1f`); on-device runbook-92 probe handed to Tiffany (her task `63790ce3`, gated on Rick's APK rebuild). Closed, receipt `3155ca22`.
- **VM disk blocker → Option B relocate (Rick-decided)**: `docker pull 1.2.0` failed "no space left on device" — boot disk 97GB/86% full (a 2nd ~50GB image won't fit) while a 196GB data disk sat 99% idle (docker mis-provisioned onto the boot disk). Rick chose relocate; moved `/var/lib/containerd`+`/var/lib/docker` → `/mnt/lupin-data` + symlinked back (preserves overlay abs-paths) → boot disk **86%→6%** (~78G freed), 3 containers healthy, rollback copies deleted post-health. Filed `77cc8add` (delete old `1.1.0` after soak; non-urgent).
- **Task-board cleanup (Rick broadcast)**: triaged 11→7; verified Rachel `f6dc0043` + Clayton `084f78be` merged (María closed), `436a366b` deployed cycles=0 (Mr Radio closed), Krishna `1b3c2c43` (Mr Radio). No blind closes — each checked vs git/deploy state.
- **MEMORY.md recompact (task `06cdb6fa`)**: 26.2KB→16.1KB (<17.1KB read-limit), all 119 pointers preserved, 0 rules dropped; María reproduce-not-trust verified + closed. Resolved a 3-way collision (Mr Radio's concurrent draft bounced on "file modified", no clobber). 2 new memory refs: `reference_user_gate_row_schema_in_hold`, `reference_gcp_vm_docker_boot_disk_capacity`.
- **GCP code-sync deploy script (task `d8c699aa`, P1, in_progress) — Phase-1 impl driven to commit-held**: re-spun this session to finish the list, in-progress first. Refactored `deploy-cloud-test.sh`'s pure logic (arg-parse / ref-resolve / sanitize / axis-detect / clean-check / stamp) into a sourceable `src/scripts/lib/deploy-cloud-test-lib.sh`; added **20 unit tests** (`src/tests/unit/deploy/test_deploy_cloud_test_lib.py`) — behavioral branch coverage of every pure func (bash isn't line-instrumentable by the pytest `--cov` gate; documented in the suite docstring), 20/20 green. A **pre-commit reviewer caught a real dead-code defect** (the script reimplemented 3 of the 6 extracted funcs inline → tested-but-unused, false-assurance gap) → fixed so all 6 lib funcs are consumed on the live path → reviewer **APPROVE** (independently re-verified). Runbook §3 updated to point at the script as the PREFERRED code-currency path. Committed-held **`18c52f71`** (not pushed). **Live VM axis-A/rollback E2E parked** — Rick opted (voice "no") to hold for an off-peak window; task stays in_progress until that closes it. Handed `fcb5dbc0` (proactive-manager) → Mr Radio (co-drive w/ María's `bc1bc373`; operator rename folds into his single build; he loops me on the shared `:8001` bounce).

**Docs**: `src/rnd/2026.06.23-firebase-admin-image-rebuild-and-redeploy-runbook.md` (EXECUTED+VALIDATED), `src/rnd/2026.06.23-firebase-android-provisioning-for-live-fcm.md` (scoping), `src/rnd/2026.06.23-gcp-code-sync-to-runtime-design.md` (design → impl). **Unpushed/held**: `18c52f71` (GCP code-sync lib+tests, this session) + `ef36d243`/`dacc8684` (design+FCM docs) + `3155ca22` (ADC code the VM runs) — all local-only; push is Rick's gate. **Owed next (all gated)**: `d8c699aa` live VM E2E (Rick's off-peak word), `77cc8add` delete old `lupin:1.1.0` (1.2.0 soak), `fcb5dbc0` (handed to Mr Radio), `9cce9f27` task-reassign (blocked on Rick).

### 2026.06.23 - Session 9998b3ef (Mr. Radio 🦉) | Receipts-of-progress + governance-forward deployed · task-reassign Phase 2 merged · overnight fleet wind-down

**Accomplishments**:
- **Receipts-of-progress shipped (task 6929f4ac)**: managed Sam 🎙️'s deploy-honesty fix after Rachel 🕊️'s review flagged the outward-twin arbiter backstop INERT in prod (factory never wired `hold_reader_fn`). My WIRE-NOW call → Sam's root-cause correction (the real :8001 deploy builds via `lupin_arbiter_app/fleet_arbiter_loop.py`, NOT `arbiter_bootstrap.py`; the original wire-list would've left :8001 inert) → wired ALL real construction sites + regression guards. Rachel DELTA PASS (in-memory `:8001` factory build → `job._hold_reader_fn IS read_hold`). Cherry-picked `354e1575` (owed-calc: needs_verification + outstanding_user_gate) + `acfac855` (factory wiring) onto wip-v0.1.9, held. Rick restarted the arbiter himself (PID 792508) → backstop LIVE by construction.
- **Subagent-governance launcher forward (activation 0dcf3a10)**: `start-cc-with-tmux.sh` now forwards `LUPIN_SUBAGENT_GOVERNANCE` across the tmux `-e` boundary (conditional forward mirroring the `COSA_VOICE_*` idiom — default-OFF preserved, Rick toggles via shell export). Rachel-APPROVED (3-way dry-run + presence-vs-truthy semantic match), selective-staged (peer LUPIN_MOBILE hunk untouched), committed `6df6825e`. Both 6929f4ac + 0dcf3a10 closed; governance flag persisted in Rick's `~/.bashrc`.
- **Task-reassign Phase 2 merged (task 9cce9f27)**: Clayton 😎 built the multiplexer per-worker task-editing UI (TaskListStore patch/drop, per-row priority/exclude-Sam reassign/inline-drop; Rick's Q1 derived-actor / Q4 inline-drop / Q5 active-personas-exclude-Sam honored) — 1439 unit tests + c8 100% L/B/F on all 4 touched files, reasoned c8-ignores. Rachel adversarial PASS (real array/null shapes, zero inline-onclick, audio-debt git-blamed orthogonal). Cherry-picked `42896cb5` → `45e7a024`, held. **Live :8000 E2E HELD for morning**: multiplexer dist is baked at Docker image-build time (gitignored, not bind-mounted), so the editing E2E needs a Phase-2-inclusive image rebuild + Rick's visual-snapshot rebaseline — deliberately not done at 3am (shared-infra image-tag + subjective visual call).
- **Diagnosed recurring false MANAGER-DOWN** (DM-only manager): commons beacons ineffective because the arbiter tap-ACK isn't crediting my session despite continuous tool activity; refined root cause to a runtime bridge-discovery miss (hyp b), NOT a provenance gap (`_manager_bridge_activity` IS in the deployed `arbiter_job.py`; receipts backstop live by construction). Handed to María for the v2 inward-arbiter item (no new board item per Rick's directive).
- **Dogfooded the merged receipts mechanism**: the Stop-hook worker-verification obligation override fired against my own session, forcing genuine `last_looked_in_on_workers_ts` stamps each tick — the feature working as designed.

**Held this session (UNPUSHED — push is Rick's)**: `354e1575` → `acfac855` → `6df6825e` → `45e7a024` on wip-v0.1.9 (alongside Tiberius's mux R&D commits `b9491f91`/`2a3d3213`/`4920d63a`/`6640a187`). **Fleet wind-down (Rick-directed, coordinate-with-owners)**: Tiberius 👑 merging his 2 mux feature lanes tonight (f8-pred-vote `b4594ada` Krishna-approved + voice-input `01d2960f` in final review) → rebuild → reap his crew; he committed 2 of 3 dirty files (`TODO.md` `2f7cd712`, `start-cc-with-tmux.sh` `8592031c`); the 3rd (`test_heartbeat_arbiter_job.py` worktree-janitor seam tests) routed to María (the cffebc62 bundle owner). My crew (Sam/Rachel/Clayton) alive, parked for harvest at clean shutdown. **Rick gates open**: push the wip-v0.1.9 stack · morning Phase-2 E2E (image rebuild + visual rebaseline).

