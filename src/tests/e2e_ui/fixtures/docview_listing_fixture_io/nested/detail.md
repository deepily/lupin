# Nested Detail (fixture)

A file inside a subdirectory, so the parent listing renders one directory
entry (`nested/`) alongside the file entries — exercising the dir-vs-file row
chrome. Frozen fixture; do not modify.
