{
  "src/cosa/agents/podcast_generator/tests/test_podcast_generator.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_accuracy_comparators.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_config.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_notification_category_classifier.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_prediction_engine.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_prediction_result.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/prediction_engine/tests/test_xml_models.py": "UNGATED 2026-08-13 (c9d3ddcb): per-agent CoSA test suite under src/cosa/agents/*/tests/ — outside the src/cosa/tests/ tree wired by this row, reached by no runner. Left ungated pending a decision to wire or rule out the per-agent trees.",
  "src/cosa/agents/swe_team/test_runner.py": "NOT A TEST 2026-07-21: production module whose filename matches pytest discovery. Documented at pyproject.toml:213-217 (line reference re-checked 2026-08-23, aa450d32 — the old ref, 186-190, had drifted onto the [tool.coverage.run] header when the coverage frame widened on 2026-08-13) — deliberately NOT omitted from coverage, because it is production code.",
  "src/cosa/rest/routers/test_suite.py": "NOT A TEST 2026-07-21: production module whose filename matches pytest discovery. Documented at pyproject.toml:213-217 (line reference re-checked 2026-08-23, aa450d32 — the old ref, 186-190, had drifted onto the [tool.coverage.run] header when the coverage frame widened on 2026-08-13) — deliberately NOT omitted from coverage, because it is production code.",
  "src/cosa/rest/test_suite_completion_watchdog.py": "NOT A TEST 2026-07-21: production module whose filename matches pytest discovery. Documented at pyproject.toml:213-217 (line reference re-checked 2026-08-23, aa450d32 — the old ref, 186-190, had drifted onto the [tool.coverage.run] header when the coverage frame widened on 2026-08-13) — deliberately NOT omitted from coverage, because it is production code.",
  "src/cosa/tests/smoke/infrastructure/test_utilities.py": "ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_calendar_xml_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class CalendarXmlMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_confirmation_pydantic_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class ConfirmationDialoguePydanticMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_dateandtime_xml_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class DateAndTimeXmlMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_gister_pydantic_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class GisterPydanticMigrationTester with 4 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 4 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_math_xml_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class MathXmlMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_receptionist_xml_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class ReceptionistXmlMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/test_todolist_xml_migration.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class TodoListXmlMigrationTester with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/io_models/test_xml_parsing_baseline.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class XMLParsingBaselineUnitTests with 5 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 5 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_api_error_handling.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class APIErrorHandlingUnitTests with 8 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 8 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_data_types_and_exceptions.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class DataTypesAndExceptionsUnitTests with 7 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 7 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_date_and_time_agent.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class DateAndTimeAgentUnitTests with 7 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 7 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_math_agent.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class MathAgentUnitTests with 7 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 7 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_prompt_formatting_and_xml_parsing.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class PromptFormattingAndXMLParsingUnitTests with 9 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 9 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_token_counter.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class TokenCounterUnitTests with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_two_word_id_generator.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class TwoWordIdGeneratorUnitTests with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/agents/test_weather_agent.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class WeatherAgentUnitTests with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/core/test_config_mgr.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class ConfigurationManagerUnitTests with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/cosa/tests/unit/core/test_core_utils.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/cosa/tests/** is gate-wired via src/tests/run-cosa-tests.sh (which runs `pytest src/cosa/tests/`), so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class CoreUtilitiesUnitTests with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-08-13 (c9d3ddcb): src/cosa/tests/** is now gate-wired via src/tests/run-cosa-tests.sh, so this file is reachable — but the census AST detector finds no collectible test in it (tests inherit through a project-local base the AST cannot follow, or the file is a fixture/helper). Excused as zero-collect, not as unreachable.",
  "src/lupin_cli/notifications/test_notifications.py": "UNGATED 2026-07-21 (d97b024e): CLI-local test module outside every gated root.",
  "src/scripts/test_websocket_notification.py": "UNGATED 2026-07-21 (d97b024e): standalone operator script named test_*; not part of any suite.",
  "src/tests/e2e/test_ask_answer_handback.py": "UNGATED 2026-08-02 (rows 6f22531f / df698896): late-answer-handback e2e; boots its own uvicorn subprocess on a throwaway Postgres DB and kill+restarts it, so it needs Postgres + ~57s and cannot run in the fast unit gate. Distinct from the gated src/tests/e2e_ui/; no runner names it. Run directly: pytest src/tests/e2e/test_ask_answer_handback.py.",
  "src/tests/e2e/test_heartbeat_poker_e2e.py": "UNGATED 2026-07-21 (d97b024e): e2e tree distinct from the gated src/tests/e2e_ui/; no runner names it. ADDED 2026-08-23 (reason CORRECTED 2026-08-23 (aa450d32), aa450d32): the reason above is true but omits that the whole module is skipped at import pending task I7 (CJ Flow ingestion wiring plus the I4a-d swap gates), so running it directly today executes nothing. It is :8000 work when it is un-skipped.",
  "src/tests/e2e/test_podcast_fail_open_gate_live.py": "UNGATED 2026-08-04 (row 40fccd91), reason CORRECTED 2026-08-23 (aa450d32): live fail-open podcast gate e2e — an online-silent timeout resolves the default and the job reaches GENERATING_AUDIO. The prior reason said it \"needs a real server and real LLM spend\"; both are false, checked against the file. It boots its OWN uvicorn subprocess on a throwaway Postgres database — it imports handback_ctx from src/tests/e2e/test_ask_answer_handback.py — so the prerequisite is Postgres, not a shared server. Research, analysis, script generation and save are all monkeypatched to fakes, and _generate_audio_async, the first method that would bill ElevenLabs, is replaced by a sentinel that raises before any call: the test only passes if that sentinel fired, so a pass is its own no-billing receipt. Measured 2026-08-23: 1 passed in 6.97s. Still ungated because it needs Postgres and boots a server, which the fast unit gate cannot carry. e2e tree distinct from the gated src/tests/e2e_ui/; no runner names it. Run directly: pytest src/tests/e2e/test_podcast_fail_open_gate_live.py.",
  "src/tests/lupin_smoke/test_audio_tts.py": "UNGATED 2026-07-21 (d97b024e): legacy smoke tree; src/tests/run-lupin-smoke-tests.sh is not in job.py SUITE_SCRIPTS, so no test-type can invoke it.",
  "src/tests/lupin_smoke/test_notifications.py": "UNGATED 2026-07-21 (d97b024e): legacy smoke tree; src/tests/run-lupin-smoke-tests.sh is not in job.py SUITE_SCRIPTS, so no test-type can invoke it.",
  "src/tests/lupin_smoke/test_queue_filtering_smoke.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (ead5137f): the shared reason its three neighbours carry was FALSE for this file when it was written. run-lupin-smoke-tests.sh named the other three and never named this one, so that runner's absence from job.py SUITE_SCRIPTS had nothing to do with why this file was ungated - it was ungated because no runner drove it at all, and pytest could only reach it by being pointed at the directory by hand. The runner was taught to name it in 575f81a4, so as of that commit the shared reason is true here too: run-lupin-smoke-tests.sh is not in SUITE_SCRIPTS, so no test-type can invoke it.",
  "src/tests/lupin_smoke/test_queue_workflow.py": "UNGATED 2026-07-21 (d97b024e): legacy smoke tree; src/tests/run-lupin-smoke-tests.sh is not in job.py SUITE_SCRIPTS, so no test-type can invoke it.",
  "src/tests/manual/test_option_b_bidirectional.py": "UNGATED 2026-07-21 (d97b024e): requires human interaction; deselected by pytest.ini `addopts = -m \"not manual\"`. Ungated by design — run explicitly with `pytest -m manual`.",
  "src/tests/manual/test_option_b_inject_info.py": "UNGATED 2026-07-21 (d97b024e): requires human interaction; deselected by pytest.ini `addopts = -m \"not manual\"`. Ungated by design — run explicitly with `pytest -m manual`.",
  "src/tests/manual/test_option_b_inject_interrupt.py": "UNGATED 2026-07-21 (d97b024e): requires human interaction; deselected by pytest.ini `addopts = -m \"not manual\"`. Ungated by design — run explicitly with `pytest -m manual`.",
  "src/tests/manual/test_option_b_interactive.py": "UNGATED 2026-07-21 (d97b024e): requires human interaction; deselected by pytest.ini `addopts = -m \"not manual\"`. Ungated by design — run explicitly with `pytest -m manual`.",
  "src/tests/parity_oracle/test_golden_capture.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle/test_golden_capture.py.",
  "src/tests/parity_oracle/test_golden_conformance.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle/test_golden_conformance.py.",
  "src/tests/parity_oracle/test_tier0.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle/test_tier0.py. This file declares its own venue as :7999 / unit-eligible (pure Python, no browser, no server), so its exemption is a wiring decision nobody has made, not a technical bar.",
  "src/tests/parity_oracle/test_tier1.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle/test_tier1.py.",
  "src/tests/parity_oracle/test_tier2_tier3.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle/test_tier2_tier3.py.",
  "src/tests/parity_oracle_fullpage/test_golden_capture.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_golden_capture.py.",
  "src/tests/parity_oracle_fullpage/test_golden_capture_horizontal.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_golden_capture_horizontal.py.",
  "src/tests/parity_oracle_fullpage/test_tier0.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_tier0.py. This file declares its own venue as :7999 / unit-eligible (pure Python, no browser, no server), so its exemption is a wiring decision nobody has made, not a technical bar.",
  "src/tests/parity_oracle_fullpage/test_tier1.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_tier1.py.",
  "src/tests/parity_oracle_fullpage/test_tier2_tier3.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_tier2_tier3.py.",
  "src/tests/parity_oracle_fullpage/test_tier_horizontal.py": "UNGATED 2026-07-21 (d97b024e), reason CORRECTED 2026-08-23 (aa450d32): multiplexer full-page layout-parity oracle. The prior reason said it is \"driven by src/scripts/build-parity-harness.sh\" — that script is an esbuild BUNDLER that writes src/lupin_app/static/dist/multiplexer/parity-harness.js and names, selects and runs no test. It is a PREREQUISITE for the browser tiers (it refreshes the bundle the harness page loads), never a driver. Nothing in the repo selects these files: no runner script, no test-type, no testpaths entry — a repo-wide search for \"parity_oracle\" outside the test directories themselves finds only R&D documents. Run directly: pytest src/tests/parity_oracle_fullpage/test_tier_horizontal.py.",
  "src/tests/smoke/test_job_persistence_e2e.py": "ZERO-COLLECT 2026-07-21 (d97b024e): reachable by a gate root and named test_*, but defines no test — pytest collects nothing from it. Inside the gate and emitting nothing. Convert to real tests or rename; tracked as follow-up work.",
  "src/tests/smoke/test_prediction_hint_visual_qa.py": "ZERO-COLLECT 2026-07-21 (d97b024e): reachable by a gate root and named test_*, but defines no test — pytest collects nothing from it. Inside the gate and emitting nothing. Convert to real tests or rename; tracked as follow-up work.",
  "src/tests/smoke/test_proxy_notifications.py": "ZERO-COLLECT (see date/row below), reason CORRECTED 2026-08-23 (aa450d32): src/tests/smoke/ is a gate root, so this file IS reachable and pytest still collects nothing from it. The prior reason blamed inheritance through a project-local base the AST cannot follow, or a fixture/helper file — neither is what happens here. The file holds class ProxyNotificationSmokeTest with 6 test_* methods and NO base class at all; pytest skips it because pytest.ini:86 sets python_classes = Test* and the class name does not start with \"Test\". It runs as a standalone script from its __main__ block. Renaming the class would put those 6 tests inside the gate — a wiring decision nobody has made, recorded here rather than done quietly. Original entry: ZERO-COLLECT 2026-07-21 (d97b024e): reachable by a gate root and named test_*, but defines no test — pytest collects nothing from it. Inside the gate and emitting nothing. Convert to real tests or rename; tracked as follow-up work.",
  "src/tests/smoke/test_v2_ask_voice_path_live.py": "ZERO-COLLECT 2026-08-21 (maya, brain-integration): reachable by a gate root and named test_*, but defines no pytest test — it is a LivePipelineTestBase subclass driven from __main__. DELIBERATELY NOT given a pytest entry point like its sibling test_calculator_live_pipeline.py: that pattern RUNS the live pipeline at collection, and this file is :8000 work — every /api/v2/ask writes a snapshot row (flow.py:287-300; snapshotable defaults True at registry.py:82), so a collectable entry point would fire mutating work from any plain pytest run of src/tests/smoke/. Invoked explicitly on the gate rig, post-merge.",
  "src/tests/store_owed_cutover_e2e/test_store_owed_cutover_e2e.py": "UNGATED 2026-07-21 (d97b024e): THE WITNESS for this row — RED since commit 2c5d6822 (~2026-06) and dark for roughly a month because nothing asked it. Its stale-name fix is tracked on b6bd15ad. Allowlisted so the gate ships; test_detector_reports_the_witness_when_not_allowlisted keeps proving the detector still sees it.",
  "src/tests/test_auth_endpoints.py": "UNGATED 2026-07-21 (d97b024e): legacy top-level file in src/tests/; the gate roots are the subdirectories, so nothing collects it.",
  "src/tests/test_refactoring.py": "UNGATED 2026-07-21 (d97b024e): legacy top-level file in src/tests/; the gate roots are the subdirectories, so nothing collects it.",
  "src/tests/test_websocket_async_bridge.py": "UNGATED 2026-07-21 (d97b024e): legacy top-level file in src/tests/; the gate roots are the subdirectories, so nothing collects it.",
  "src/tests/test_websockets.py": "UNGATED 2026-07-21 (d97b024e): legacy top-level file in src/tests/; the gate roots are the subdirectories, so nothing collects it.",
  "src/tests/unit/test_websocket_connection_debug.py": "ZERO-COLLECT 2026-07-21 (d97b024e): reachable by a gate root and named test_*, but defines no test — pytest collects nothing from it. Inside the gate and emitting nothing. Convert to real tests or rename; tracked as follow-up work.",
  "src/tests/websocket_smoke/core/test_authentication_flow.py": "GATED VIA NON-PYTEST RUNNER 2026-07-21: imported directly by src/tests/websocket_smoke/infrastructure/smoke_test_runner.py, which the `websocket` test-type invokes. Reachable, just not through pytest.",
  "src/tests/websocket_smoke/core/test_close_codes.py": "UNGATED 2026-07-21 (d97b024e): sits under websocket_smoke but is NOT imported by smoke_test_runner.py, so the `websocket` test-type never runs it. Invisible despite the gated-looking directory.",
  "src/tests/websocket_smoke/core/test_connection_basic.py": "GATED VIA NON-PYTEST RUNNER 2026-07-21: imported directly by src/tests/websocket_smoke/infrastructure/smoke_test_runner.py, which the `websocket` test-type invokes. Reachable, just not through pytest.",
  "src/tests/websocket_smoke/core/test_event_system.py": "GATED VIA NON-PYTEST RUNNER 2026-07-21: imported directly by src/tests/websocket_smoke/infrastructure/smoke_test_runner.py, which the `websocket` test-type invokes. Reachable, just not through pytest.",
  "src/tests/websocket_smoke/core/test_session_management.py": "GATED VIA NON-PYTEST RUNNER 2026-07-21: imported directly by src/tests/websocket_smoke/infrastructure/smoke_test_runner.py, which the `websocket` test-type invokes. Reachable, just not through pytest.",
  "src/tests/websocket_smoke/infrastructure/test_utilities.py": "GATED VIA NON-PYTEST RUNNER 2026-07-21: imported directly by src/tests/websocket_smoke/infrastructure/smoke_test_runner.py, which the `websocket` test-type invokes. Reachable, just not through pytest.",
  "src/tests/websocket_smoke/test_multiplexer_transport.py": "UNGATED 2026-07-21 (d97b024e): sits under websocket_smoke but is NOT imported by smoke_test_runner.py, so the `websocket` test-type never runs it. Invisible despite the gated-looking directory.",
  "src/tests/ws_channel_browser/test_ws_circuit_banner.py": "UNGATED 2026-07-21 (d97b024e): browser-driven WebSocket channel suite with no runner script.",
  "src/tests/ws_channel_browser/test_ws_close_codes.py": "UNGATED 2026-07-21 (d97b024e): browser-driven WebSocket channel suite with no runner script.",
  "src/tests/ws_channel_browser/test_ws_lifecycle.py": "UNGATED 2026-07-21 (d97b024e): browser-driven WebSocket channel suite with no runner script.",
  "src/tests/ws_channel_unit/test_ws_channel_unit.py": "UNGATED 2026-07-21 (d97b024e): WebSocket channel unit suite sitting outside src/tests/unit/, so the `unit` gate cannot reach it."
}
