# Rsync Exclusion Patterns - Default Template v1.0
#
# This file defines patterns for files/directories to EXCLUDE from backup.
# Copy to your project's src/scripts/conf/rsync-exclude.txt and customize.
#
# Pattern syntax: See `man rsync` for details
# - Trailing slash (/) matches directories only
# - Wildcards: * (any chars), ** (recursive), ? (single char)
# - Lines starting with # are comments

# Version control
.git/
.gitignore
.gitattributes

# Python virtual environments
.venv/
venv/
env/

# Python cache and compiled files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS metadata
.DS_Store
.AppleDouble
.LSOverride

# Build artifacts
build/
dist/
*.egg-info/
.eggs/

# Test coverage
.coverage
htmlcov/
.pytest_cache/
.tox/

# Logs
*.log
logs/
nohup.out

# Large model files (uncomment if you don't want to back these up)
# models/
# *.pt
# *.pth
# *.ckpt
# *.safetensors

# Temporary files
tmp/
temp/
*.tmp

# Node.js (if applicable)
# node_modules/
# npm-debug.log
# yarn-error.log

# Database files (be careful - you may want to back these up!)
# *.sqlite
# *.db

# PostgreSQL data directory was relocated outside the project tree on 2026-04-30
# (now at /mnt/DATA01/include/www.deepily.ai/projects/lupin-data/postgresql-dev-data),
# so the rsync sweep no longer touches it from this project root.
# Use backup-postgres.sh to create postgresql-backup.sql for portable backups.

# LanceDB FTS indices (regeneratable, often created by root/docker)
*/_indices/fts/

# LanceDB legacy vector store — RETIRED after the v0.2.0 Postgres+pgvector
# cutover (backend = postgres, commit 0901984d, soaked clean). The DATA01
# working-tree copy was deleted 2026-07-08 (task 4955d0b9, Rick's word). This
# exclude FREEZES the 30G DATA02 mirror as a durable rollback snapshot: with
# plain --delete (not --delete-excluded), an excluded path is protected on the
# destination, so the mirror survives even though the source is gone. Do NOT
# add --delete-excluded to backup.sh without revisiting this.
/src/conf/long-term-memory/lupin.lancedb/

# Project-specific exclusions (add your patterns below)
