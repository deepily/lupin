# Lupin Baseline Smoke Test Remediation Plan

**Date**: 2025.09.23
**Baseline Report**: [2025.09.23-baseline-smoke-test-report.md](2025.09.23-baseline-smoke-test-report.md)
**Current Status**: 62.2% pass rate (46/74 tests)
**Target**: 100% correct behavior across all categories
**Strategy**: Component fixes + unit tests, smoke tests unchanged

---

## Executive Summary

Based on the baseline smoke test results, the Lupin system has **4 critical security issues** and **4 high/medium priority integration problems**. The smoke tests correctly identified end-to-end failures - now we need to fix the underlying validation components and verify with unit tests.

### Current State
- **Audio/TTS System**: ✅ 100% (9/9) - No changes needed
- **WebSocket Tests**: ⚠️ 92% (46/50) - Edge case validation issues
- **Basic API**: ⚠️ 80% (4/5) - Auth endpoint misconfiguration
- **Queue Workflow**: ⚠️ 75% (6/8) - State monitoring failures
- **Notifications**: ❌ 62.5% (5/8) - Queue/delivery problems

---

## Remediation Issues Tracking

| Priority | Issue | Component | Status | Unit Test | Notes |
|----------|-------|-----------|--------|-----------|-------|
| 🚨 CRITICAL | Session ID accepts tab/newline | WebSocket validator | ❌ TODO | ❌ TODO | Lines 146,149 in log |
| 🚨 CRITICAL | 7 invalid tokens accepted | Auth validator | ❌ TODO | ❌ TODO | Line 331 in log |
| 🚨 CRITICAL | 6 malformed messages accepted | Message validator | ❌ TODO | ❌ TODO | Line 381 in log |
| 🔴 HIGH | Auth endpoint returns 403 not 401 | `/api/auth-test` | ❌ TODO | ❌ TODO | Line 850 in log |
| 🔴 HIGH | Queue state monitoring 500 error | `/api/queue/status` | ❌ TODO | ❌ TODO | Line 922 in log |
| 🟡 MEDIUM | Notification queue empty | Queue operations | ❌ TODO | ❌ TODO | Line 868 in log |
| 🟡 MEDIUM | WebSocket delivery 403 errors | Connection auth | ❌ TODO | ❌ TODO | Line 871 in log |

---

## Testing Strategy

### Component-Level Unit Tests (NEW)
- **Purpose**: Test validation functions in isolation
- **Location**: `src/tests/unit/test_websocket_validators.py`
- **Approach**: Create focused tests for each validator component
- **Example**: `test_session_id_rejects_tab_character()`

### End-to-End Smoke Tests (EXISTING - NO CHANGES)
- **Purpose**: Test complete user flows client→server→response
- **Status**: Working correctly - identified all problems
- **Action**: Re-run after fixes to verify end-to-end behavior

---

## Detailed Remediation Tasks

### 🚨 CRITICAL Issue 1: Session ID Validation
**Problem**: Tab (\t) and newline (\n) characters accepted in session IDs
**Evidence**: Log lines 146, 149 - "wise\tpenguin" and "wise\npenguin" connected
**Expected**: Should reject all whitespace except single space between words

**Component to Fix**: WebSocket session ID validation
**Unit Tests to Create**:
- `test_session_id_rejects_tab_character()`
- `test_session_id_rejects_newline_character()`
- `test_session_id_accepts_valid_format()`

### 🚨 CRITICAL Issue 2: Token Validation
**Problem**: 7 invalid tokens incorrectly accepted
**Evidence**: Log line 331 - "7 invalid tokens were incorrectly accepted"
**Expected**: Reject null, empty, non-Bearer, malformed tokens

**Component to Fix**: WebSocket auth token validation
**Unit Tests to Create**:
- `test_token_rejects_null()`
- `test_token_rejects_empty_string()`
- `test_token_rejects_non_bearer_format()`
- `test_token_rejects_missing_user_id()`

### 🚨 CRITICAL Issue 3: Malformed Message Handling
**Problem**: 6 malformed auth messages incorrectly accepted
**Evidence**: Log line 381 - "6 malformed messages were incorrectly accepted"
**Expected**: Strict JSON schema validation for auth messages

**Component to Fix**: WebSocket message validation
**Unit Tests to Create**:
- `test_auth_message_requires_json()`
- `test_auth_message_requires_type_field()`
- `test_auth_message_requires_token_field()`
- `test_auth_message_rejects_null_values()`

### 🔴 HIGH Issue 4: Auth Endpoint Status Code
**Problem**: `/api/auth-test` returns 403 instead of 401
**Evidence**: Log line 850 - "expected 401, got 403"
**Expected**: Return 401 Unauthorized for auth failures

**Component to Fix**: FastAPI auth endpoint
**Unit Tests to Create**:
- `test_auth_endpoint_returns_401_for_invalid_auth()`

### 🔴 HIGH Issue 5: Queue State Monitoring
**Problem**: Queue status endpoint returns 500 Internal Server Error
**Evidence**: Log line 922 - "Expected status 200, got 500"
**Expected**: Return 200 with valid queue state data

**Component to Fix**: Queue status endpoint
**Unit Tests to Create**:
- `test_queue_status_endpoint_returns_200()`
- `test_queue_status_endpoint_returns_valid_json()`

### 🟡 MEDIUM Issue 6: Notification Queue Operations
**Problem**: Notification queue operations failing - empty queue
**Evidence**: Log line 868 - "Should have notifications in queue"
**Expected**: Queue should persist notifications between operations

**Component to Fix**: Notification queue persistence
**Unit Tests to Create**:
- `test_notification_queue_persistence()`
- `test_notification_queue_add_and_retrieve()`

### 🟡 MEDIUM Issue 7: WebSocket Delivery
**Problem**: WebSocket connections rejected with HTTP 403
**Evidence**: Log line 871 - "server rejected WebSocket connection: HTTP 403"
**Expected**: Valid notifications should deliver via WebSocket

**Component to Fix**: WebSocket auth for notifications
**Unit Tests to Create**:
- `test_notification_websocket_auth_accepts_valid_session()`

---

## Implementation Plan

### Phase 1: Critical Security Fixes (Priority 🚨)
1. **Session ID Validation** - Fix whitespace acceptance
2. **Token Validation** - Reject invalid token formats
3. **Message Validation** - Add JSON schema validation

### Phase 2: High Priority API Issues (Priority 🔴)
1. **Auth Endpoint** - Fix 403→401 status code
2. **Queue Monitoring** - Fix 500 error

### Phase 3: Medium Priority Integration (Priority 🟡)
1. **Notification Operations** - Fix queue persistence
2. **WebSocket Delivery** - Fix 403 rejections

### Phase 4: Verification
1. **Unit Test Validation** - All new tests passing
2. **Smoke Test Re-run** - Target 100% pass rate
3. **Regression Check** - No new failures introduced

---

## Success Criteria

### Per-Issue Targets
- **Session ID Edge Cases**: 0/2 accepted (currently 2/2 pass through)
- **Invalid Tokens**: 0/7 accepted (currently 7/7 pass through)
- **Malformed Messages**: 0/6 accepted (currently 6/6 pass through)
- **Auth Endpoint**: Returns 401 (currently returns 403)
- **Queue Status**: Returns 200 (currently returns 500)

### Overall System Health
- **Target Pass Rate**: 100% across all categories
- **Current**: 62.2% (46/74 tests)
- **Audio/TTS**: Maintain 100% (already perfect)
- **WebSocket**: 100% (from 92%)
- **API**: 100% (from 80%)
- **Notifications**: 100% (from 62.5%)
- **Queue**: 100% (from 75%)

---

## Session Progress Log

### Session 1 (2025.09.23 - Current)
- ✅ Created remediation tracking document
- ✅ COMPLETED: Session ID validation fix (tab/newline rejection)
  - Modified `websocket.py` regex from `\s` to literal space
  - Added explicit whitespace character rejection
  - Created unit test `test_session_id_validation_simple.py`
- ✅ COMPLETED: Token validation fix (7 invalid types)
  - Enhanced `auth.py` with type/length/null checks
  - Token validation unit test passes 100%
- ✅ COMPLETED: Malformed auth message handling
  - Enhanced WebSocket auth validation in `websocket.py`
  - All 8 malformed message types now properly rejected
- ✅ COMPLETED: WebSocket valid authentication fix
  - Fixed session ID format issue (underscore → space with URL encoding)
- 🔍 INVESTIGATING: Queue state monitoring 500 error
  - Added error handling to `/api/push` endpoint
  - **ROOT CAUSE FOUND**: "lxml not found" error in Gister XML parsing
  - First job works, 2nd/3rd jobs fail with XML parsing error
  - Error occurs in: push_job() → GistNormalizer → Gister.get_gist() → LLM response parsing
  - **HYPOTHESIS**: LLM returning error message or malformed XML on subsequent calls

**Current Status**: Paused remediation to investigate Gister XML parsing bug.
**Next Action**: Manual investigation of Gister object behavior across multiple job submissions.