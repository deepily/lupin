# Holding-Area Triage — Eight Keypresses for Rick

**Date**: 2026-09-09
**Authors**: Mr. Radio 🦉 (81381447) · María 🌸 (31297e6d)
**Ordered by**: Rick, broadcast `d9849959`, and row `76032470` (P0)
**Status**: **recommendation only.** Nothing has been promoted, dropped or closed. Every move below is a keypress Rick makes.

> Rick, 2026-09-09: *"I did not tell you to actually implement — I told you to create a list. I'll be doing all of the key presses myself."*

---

## 0. Read this first — the count, and why we cannot execute

| source | count |
|---|---|
| Rick, ~03:10 UTC | 21 |
| María, ~02:33 UTC | 22 |
| 🔴 **measured `task_query( status="not_approved" )`, ~14:2x UTC — both of us independently** | **23** |

Both spoken numbers were low. 22 rows are `project=lupin`; one (`1d40b477`, Krishna) is `project=plan`.

**Neither of us can promote, demote, won't-fix or un-park.** Measured, not assumed — live probe:

```
task_transition( 47f33bba → queued, authority="user_direct" )
403  admitting a row out of 'not_approved' requires a LOGIN ACCOUNT that maps to
     an approver. You were authenticated as no login account (API-key caller).
     ⚠️ The name you sent as `actor` ('mr radio 81381447') is recorded but confers
     nothing — Rick closed that door on 2026-09-07 (row b8205986).
```

| list | live value | usable by a seat? |
|---|---|---|
| `task approval approver personas` | `mr radio, cheech, maria` (+ `rick`) | ❌ **inert** — names three personas, none of whom can use it |
| `task approval approver accounts` | `ricardo.felipe.ruiz@gmail.com = rick` | ✅ Rick, and only Rick |

⇒ **All four approver-only moves land on one keyboard.** That is the design working as ruled — recorded here so the eight recommendations below are read as what they are.

### The two "easy kills" were already dead
`49e52a90` and `64b6a88f` are both already `dropped` (María verified via `task_query( status="dropped" )`; Mr. Radio independently observed their absence from the holding area). **We owe four fresh kills, not two.**

---

## 1. Method, settled before any row was opened

**Tie-break: unblock-most-others first, freshest evidence secondary.** Proposed by Mr. Radio, adopted unchanged by María — it replaces "top 4 by value" with something checkable against the store.

**A row dies only when its premise is dead**: already ruled elsewhere · measured and no longer true · superseded by shipped work · a question the code already answers. **"Nobody got to it" is not a reason to kill a row** — that is what the holding area is for.

---

## 2. PROMOTE — four, ranked

### #1 · `75044ab5` — Un-park is merged and invisible on the multiplexer
**FOR** — carries Rick's standing ruling on what *fixed* means, quoted on the row: *"it's fixed should mean that it's available to me to see and use… applied to both the client and the multiplexer."* Nothing enforces either clause today. The deliverable is a **gate that reddens when `dist/` is older than its sources**, protecting every future "fixed" claim on the compiled client. Highest unblock-count; evidence measured last night.

**AGAINST** — the rebuild is probably one command. Ship only that and this is a ten-minute row wearing a P1's clothes; **the gate is the real work and is the part most likely to be deferred once the verb appears in the UI.**

**Assign: María.** *(Both authors agree. No dissent.)*

---

### #2 · `c46ba7c0` — Four smoke tests authenticate as nobody, green in the merge gate the whole time
🔴 **RAISED FROM RUNNER-UP BY MR. RADIO ON A MEASUREMENT TAKEN THIS MORNING.** María ranked it out on a verification gap she named as hers — *"I did not check whether the uncommitted split still exists. That is a reason to go look, not a reason to rank it low."* **I went and looked. The answer changes the ranking.**

**FOR** — four tests in `src/tests/smoke/test_notifications_sse_smoke.py` send the API key as a **query param** to a door that reads a **header**. Measured by Chloé across four arms: sending the credential in the query is **byte-identical to sending none at all**. So these are not authenticating incorrectly — from the server's side they are **not authenticating**, and they have been riding the `smoke (:7999)` leg of the PR merge pyramid green while making no successful request in their lives. `test_api_key_validation` **has never once validated an API key**.

🔴 **AND THE SEQUENCING HAZARD IS STILL LIVE ON THE MAINLINE — this is the new fact.** Chloé split the two row-writing tests off to `:8000` and the row records that split as *"uncommitted… it dies with the seat."* Measured today, worktree `lupin-wt-cc-author-mr-radio-2`:

| claim on the row | measured 2026-09-09 | verdict |
|---|---|---|
| the split is uncommitted, dies with the seat | committed at **`eabd6778`**, `git status` clean | **stale — better than feared** |
| the hazard is DISCHARGED | `git branch --contains eabd6778` → **only `chloe-sse-smoke-response-door`** | **stale — worse than claimed** |
| smoke file holds 7 tests, none writing | **working branch holds 5**, both writers still present at `:47` and `:152` | **discharge is branch-local** |

`run-smoke-tests.sh:47` still sweeps `src/tests/smoke/` wholesale. ⇒ **On the branch we actually work, the loaded gun is still loaded.** Anyone who repairs the credential defect on the mainline starts minting a response-required orphan on **every merge-gate run** — the exact species row `bf4f65c3` tracks.

⇒ **That is why this outranks a gate-wiring row.** It is a false green in a required gate whose obvious repair is actively harmful until an unmerged branch lands. The remedy is a **three-step staircase** — header transport **and** a `ck_live_`-shaped key **and** that key seeded in the DB; any two of three still returns 401, and each intermediate fix looks like a failure.

**AGAINST** — the exit from fault 3 is **predicted, never driven**: nobody has seeded a real key and watched it return 200. So the honest claim is *"at least three steps"*, not *"exactly three"*, and a fourth fault behind the seeded-key arm would be invisible from where this was measured. The closing arm needs a throwaway DB **and** its own server subprocess, and its total cost is **unquantified** — the 45s in the code is a ceiling the code allows, not a duration anyone timed.

**Assign: Mr. Radio.**

---

### #3 · `508449b7` — Memento delivery announces a full record as "carries no state"
**FOR** — **the fix exists, is behaviourally verified at Krishna's tip, and is not landed.** Two arms, and it *discriminates*: a real record routes to *"ALL OF ITS STATE IS IN THE BODY"* with 6,721 bytes delivered, while a record with an amendment tail still routes to the amendment branch. 210 of 341 records affected. Until it lands, **every re-spinning seat is told its memento is blank.** Landing already-verified work is the cheapest unblock on the board.

**AGAINST** — the thing this row is named for is **not** what actually bit Tiberius. His memento block never arrived *at all*, and Krishna's fix changes what a block *says*; it cannot explain receiving **none**. That half is unmeasurable retrospectively and needs a receiving-end receipt nobody has built. **Landing this closes the announcement defect and leaves the delivery question exactly where it is.**

**Assign: María.** *(Both authors agree. No dissent.)*

---

### #4 · `507183ff` — The multiplexer cannot offer "Fixed"
**FOR** — Rick's own asymmetry complaint, on the client he actually uses: *"I see something's fixed, I'm going to mark it as fixed."* There is a mechanical chain behind it: a board whose only human-driven terminal verb is negative inflates the open count → the **ratio gate reads that count** → the ratio gate has been refusing `task_create` fleet-wide. The deliverable is a **parity guard** between the multiplexer's verb list and the shared module — the same *shape* as #1's stale-bundle gate, which is why one sitting is cheaper than two.

**AGAINST** — `task_verbs.test.ts:49` currently **pins the six-verb list**, so a guard and that test will fight; `holdingAreaBatch.ts` may carry a **third** copy of the vocabulary nobody has grepped for. And the row's own *not established* section says **we do not know whether `fixed` was omitted for a reason nobody wrote down.** Adding a verb on that basis is the move Rick has stopped twice this week.

**Assign: Mr. Radio.**

---

### 🔴 What Mr. Radio pushed OUT of the four, and why

**`7bc67019` — wire the three typechecks as a blocking merge gate.** María ranked it **#2**; I moved it out. Her own doc argues my case: *"it was scoped for Cheech, and we cannot spawn Cheech before 17:00… This is not a small row for a two-person day"* and *"ranked on what we can finish today it might not make the list at all."*

**The disagreement is real and Rick should see it rather than a smoothed-over four.** Her reason to rank it high is sound: Rick already ruled YES on the fourth ask (`answered: true`, `default_used: false`), the precondition cleared when `b3d17502` landed, and the type-error surface is live and accumulating. My reason to rank it out is the tie-break we both adopted — it wires a **required gate**, and row `76032470` already establishes that this lane can emit an unearned green. **Wiring a required gate onto a lane that can print a false pass makes the false green mandatory.** Sequence it after the lane is trustworthy, not before. It also carries María's un-waived condition of **three** plant-and-restore positive controls, one per config — which is the largest single item on either list.

⇒ **If Rick disagrees, this is the swap to make: `7bc67019` in, `507183ff` out.** They are the two closest calls.

---

## 3. KILL — four, each with a dead premise

### K1 · `3fdf4fb4` → **`dropped`** — "Main is RED: the prototype-chain sweep fails"
**Premise measured and no longer true.** `npx tsx --test …/no_lookup_walks_the_prototype_chain.test.ts` → **pass 4, fail 0, 226ms**, all four subtests green.

**And the row's wording is wrong a second way**: it says *"main is RED"*, but `main` is at `8bf71a64`, **1,431 commits behind**, and neither file exists there. *"Main"* meant *the mainline we work on*. A future reader chasing that sentence checks the wrong branch and finds no such test — **worth keeping in the kill reason**, because it is the reusable half.

⚠️ Which commit closed it is **not** proven (`05d7f5df` is a candidate, not a verdict). It does not need to be: the row reports a red that does not exist.

---

### K2 · `2a2ee2f2` → **`done`, not `dropped`** — "The multiplexer typecheck is RED at HEAD"
**Premise superseded by shipped work.** `b3d17502` merged; all three typechecks `exit 0` at the working tip; duplicate `ownLookup` import 2 → 1. The row's own last line reads *"Everything else on `2a2ee2f2` has shipped."* Its one remaining item was deliberately re-filed as `7bc67019`.

🔴 **The honest verb is `done` with `receipt_refs={"commit": "b3d17502"}`.** Rick asked for four to *"delete or drop or mark won't fix"* — **this one earned a close, and we are recording the distinction rather than taking the easier verb to fill a slot.**

---

### K3 · `b4bc91f2` → **`dropped`** — "Adversarially review the un-park verb"
**Premise already answered elsewhere.** The row exists because *"row `03d3bf78` sits at status `review` with nobody named as reviewer."* **`03d3bf78` is closed.** The residual — that the verb was never built for the multiplexer — is now tracked on `75044ab5` (promote #1) under a **stronger** acceptance test than this review asked for: Rick using it, on each client, rather than five proofs against source.

⚠️ Honest caveat, María's, against herself: **that close was premature** under Rick's later *fixed-means-he-can-use-it* ruling. So this is not *"the review happened"* — it is *"the review's subject moved to a row that supersedes it."* **Folding, not dismissing.**

---

### K4 · `97ff4426` → **`wont_fix`, not `dropped`** — "PostToolUse fires COMPLETE at ~120s, then FAILED at 660s"
**Premise: a question the code already answers, and the answer is not in this repo.**

| layer | evidence | verdict |
|---|---|---|
| server | 42/42 asks declaring >120s expired at their declared value ±0.1s | innocent |
| client | live probe answered at 151.4s, returned at 151.4s with the real answer | innocent |
| PostToolUse | fires at `min( answer, ~120s )`, 24/24, whatever the declared timeout | **the defect** |

The emitter holds no timer and no deadline — **the decision to invoke at ~120s is the harness's, above this repo.** Both owed items landed: the upstream report is filed and the recovery path is documented in two homes.

🔴 **This is Mr. Radio's row and he ruled on 2026-09-06 that it stays open. He is conceding it here.** That ruling's stated reason — *"what closed here is the reporting and the findability of the workaround"* — **has since been satisfied**, and María is right that nothing in this repo is left to do.

⚠️ **But the verb matters.** `dropped` reads as *this was never real*. The defect **is** real and still bites every seat on every long ask. `wont_fix` says *real, measured, not fixable here* — and it keeps the three-layer measurement findable for the next person who hits a 120s COMPLETE and starts blaming innocent client and server code, which this row's own title already did once.

---

## 4. What we are least sure of

1. **K2 and K3 are bookkeeping kills**, not premise-dead-in-the-world kills. They are correct, and they are also the two cheapest to defend — precisely the temptation row `76032470` warned about. Neither of us found a fourth genuinely-dead row to displace them.
2. **Promote #4 vs `7bc67019` is a live disagreement between the two of us**, recorded above rather than resolved. It is the one place Rick's ruling would change the set.
3. **`c46ba7c0`'s staircase has an unmeasured exit.** "At least three steps", never "exactly three".

---

## 5. Two findings that are NOT rows, and therefore not in the eight

Recorded so the non-inclusion is deliberate. **Both are false greens, which is why they are named together.**

- **The lane refusal.** A *killed* test run prints the **same** coverage number as a complete one — `99.83%` at 268, 738 and 3363 tests, identical. Brief: `planning-is-prompting/src/rnd/2026.09.09-lane-refusal-brief.md`. This is the reason `7bc67019` was pushed out of the four.
- **A stale reassurance in the approval gate's own docstring.** `src/cosa/rest/task_approval_settings.py:532` still promises *"returns None when the actor is an approver."* That has been false since 2026-09-07. **It misled a manager this morning** — I read it, concluded María's admission-wall claim was an over-claim, and had the correction half-written before probing instead of publishing. A wrong *instruction* is caught the first time someone follows it; a wrong *reassurance* disarms the reader who would have checked.

Neither is in the holding area, so neither is one of the eight. **Both want rows, and minting a row is also a keypress we do not have.**

---

## 6. Provenance

María read all 23 rows in full — bodies and amendments, not the terse projection — paged at offsets 0/6/12/18 plus `task_get` on the four too large to serialize. Mr. Radio independently measured the count, read `47f33bba` / `3fdf4fb4` / `c46ba7c0` in full, read the approval gate at source, probed it live, and verified the `c46ba7c0` worktree state this morning.

**One promotion was attempted and refused** (`47f33bba`, the 403 quoted in §0). That refusal is the receipt for §0 and was the only write either of us aimed at the store. **Nothing was promoted, dropped, or closed.**
