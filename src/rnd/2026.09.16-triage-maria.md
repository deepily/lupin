# Triage — María 🌸, 2026-09-16 (written blind, before reading Mr. Radio's)

**Asked by**: Rick's broadcast `dcd9edcd`: triage the rows of Chloé, Maya, Pocholo, Rio, Sam and Tiberius, then suggest priorities that finish the multiplexer work and get Tiffany what she needs for the mobile client.
**Split with Mr. Radio**: I take the 14 held rows those seats filed or still own, plus my own board. He takes his live queue and Tiffany's mobile path.
**How it was done**: the 14 row bodies were read by **a read-only subagent of mine** (no store writes, no edits, no DMs), not a spawned seat. I spot-checked its timezone, worktree and template-test claims myself; the rest are marked where unverified.
**Measured at**: lupin HEAD `198c948d`. **These are suggestions only.** Rick does the admits, drops and priority changes.

## Headline

- **The six seats own zero live rows.** The reaps already moved their work onto our two boards. Only 4 held rows still carry their names: Sam 1, Tiberius 3.
- **Of the 14 held rows: drop 4 as done, merge 3 into others, admit 6, keep 1 held.**
- **Only 3 of the 14 touch the goals**: `0e5bfa0e` and `1f79c04f` (multiplexer), and `d90baf3d` (the phone is one of its callers). The rest is test-quality work.

## The 14 held rows the six seats filed

| Row | Filed by | Problem, in one sentence | Still real at HEAD? | Goal? | Now | Suggest |
|---|---|---|---|---|---|---|
| `0e5bfa0e` | Sam | The `app timezone` setting reaches neither client, so times show in the browser's zone | **Yes.** I checked: the server sends `"app timezone"` with a space (`system.py:827`), legacy reads `config.app_timezone` (`notifications.js:884`) | Multiplexer | P5 | **Admit at P2** |
| `d90baf3d` | Rio | Notification history routes check that you're logged in, never that the email in the URL is yours | Likely (quick grep only) | Mobile calls it | P2 | **Admit at P2, but land only after Tiffany's `588c8dc9`.** *Update 14:40:* Tiffany checked and the phone sends the user's **UUID** where the URL expects the email (`lupin-mobile/lib/app.dart:121`, a variable named `email` holding `lastAuthenticatedUserId`; I read it). The phone's reconnect reload already 404s today, and adding the owner check first would make every phone history call fail |
| `1f79c04f` | Pocholo | No test guards keyboard access to the section-header chevron | Yes, per the row | Multiplexer | P5 | **Admit at P3** |
| `8d043758` | Pocholo | The DOM-assert lint can't see a DOM node reached through a variable; 5 risky asserts exist | Yes (helper's read) | No | P2 | **Admit at P3**, absorbing `9f54712b` |
| `9f54712b` | Mr. Radio | The same lint's result depends on which side of `===` the DOM call is on | Yes (helper ran both orders) | No | P3 | **Merge into `8d043758`** |
| `6bf8a819` | Mr. Radio | Lint red on 5 violations in `finished_tasks_renderer.test.ts` | **Fixed**, the file scans clean (helper's read) | No | P2 | **Drop as done**. Its bigger amendment ("the unit tier hadn't run the gate since 08-16") should get its own row if nobody has filed one |
| `89be3908` | Tiberius | Six spec checks that need no browser run only in the 25-minute E2E slot | Yes | No | P5 | **Admit at P4**, absorbing `3202a3ed` |
| `3202a3ed` | Tiberius | A normalizer test builds its fake page from the list it checks, so it can't fail | Yes, but low value | No | P5 | **Merge into `89be3908`** |
| `b2fb103c` | Tiberius | A screenshot test hides the clock then waits, so a clock push in the gap can restore it | Yes; never seen happen | No | P5 | **Admit at P4** |
| `104d62ae` | Tiberius | No test checks that a page's template contains the element a spec names | **Fixed** at `bfefc400`. I checked: the test is at `test_visual_regression.py:1026` | No | P5 | **Drop as done** |
| `e4db0e68` | Tiberius | A normalizer test matches a substring, so a renamed selector passes | Covered by `104d62ae`'s test (the helper re-applied the old rename in memory; it was caught) | No | P5 | **Drop as done** |
| `1ebb712e` | Maya | Check the cleanup job removed three dead maria worktrees | **Passed.** I checked: `git worktree list` shows only the main checkout | No | P5 | **Drop as done** |
| `587b42a9` | Chloé | Two re-reviews owed: item 1 approved (`105c76fa`); item 2 (`0e50fc15`) still needs a reviewer who isn't its author | Item 2 open | No | P5 | **Admit at P4** |
| `69c41c4e` | Maya | Nobody knows what kept three reviewed branches unmerged on 09-15 | A question, not a defect | No | P5 | **Keep held.** Only the transcripts can answer it |

⚠️ **One thing the helper found that may be red right now**: the DOM-assert lint flags 2 violations in `section_toolbar_renderer.test.ts:191-192` against a baseline of zero. **I did not run the gate test**, so that's unconfirmed. It belongs to Mr. Radio's lane if it's real.

## My own board

| Row | Problem | Now | Suggest | Why |
|---|---|---|---|---|
| `0f4728bc` | Five preconditions before Phase A of the parity build (owner Mr. Radio) | P1 | **Raise to P0, then close** once his §6a rulings are committed | Every item was ruled today; it's the last gate before Phase A |
| `e5ebc815` | Admin-only config reload button | P2, waiting on Rick | **Close** | Ruled last night. Rick is being asked now |
| `b5035039` | A seat with an old MCP process can `/clear` a busy pane | P1 | **Lower to P2** | Guard merged at `9f760b2a`; only the outside stale-process check remains, and it isn't on either goal's path |
| `14761ef1` | Worktree guard trial week | P2 | **Lower to P3** | Planning repo; the census continues on its own |
| `158a9865` | Five SSE smoke tests that can't fail | P3 | Keep P3 | |
| `168922f9` (held) | Tiffany to audit my Stage-1 fold | P1 | **Drop as done** | Her team did that audit at revision 2; it's §4.4 of the handoff doc |
| `65e7dca4` (held) | Install the worktree guard in log-only mode? | P2 | **Drop as done** | Installed 09-14; tracked on `14761ef1` |
| `73fdc7ef` (held) | E2E toolbar test fails every run | P3 | **Fold under `f0e00f01` as its own red** | *Corrected after comparing with Mr. Radio:* I had suggested merging into `8d0c7c83`, but that row names a different test (`toolbar_button_is_clickable`) |
| `081dac6d` (held) | Task list prints the long holding-area note | P2 | **Admit at P3** | Annoyance, not a blocker |
| `8639d1ad` (held) | Store keeps who attested but drops what they wrote | P2 | Keep held | Off both goals |
| `80513825` (held) | Approval-settings file has no enforced boundary | P2 | Keep held | Off both goals |
| `9ab0bddb` (held) | Doc viewer path check uses `normpath` | P5 | **Admit at P4** | A real path hole, but small |
| `4163a015` (held) | 83 files suppress a phantom coverage branch | P5 | Keep held | |

## For the two goals, in order

1. **Multiplexer**: `0f4728bc` → Phase A starts. Then `0e5bfa0e` (timezone) and `1f79c04f` (chevron keyboard test) ride alongside the parity items they touch.
2. **Tiffany**: `05ddc8f0` (Whisper cut at a pause, already P0) should land **before** her Opus accuracy check `9b1f7701`; she asked for that ordering today. `d90baf3d` needs her check on the phone's login email before it's fixed. The phone-session rows are Mr. Radio's lane.

**Not verified by me**: the DOM-assert lint findings (the helper's runs), `d90baf3d`'s missing owner check (grep only), and the `73fdc7ef`/`8d0c7c83` overlap.
