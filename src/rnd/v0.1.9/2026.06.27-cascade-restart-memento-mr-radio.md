# Cascade-Restart Memento — for the fresh cold Manager (from Mr. Radio 🦉)

**Date**: 2026-06-27
**Author**: Mr. Radio 🦉 (session cd637762 — being spun down per Rick's clean-restart call)
**Purpose**: hand a COLD fresh manager everything needed to run the multiplexer-parity MVP cascade review **correctly**, per the canonical workflow, after this session ran it wrong.

---

## 0. READ THIS FIRST — the canon + the visibility invariant

- **Follow the canonical workflow**: `planning-is-prompting/workflow/plan-review-cascaded.md` (+ siblings: `-parallelism`, `-personas`, `-stage-specs`, `-on-demand-spawn`, `-defaults`, `-common`, `-input-spec`).
- **Correct cast (per plan)**: 1 manager + 1 author + **3 DISTINCT reviewers** — Stage 1 usability/reuse · Stage 2 viability/gap · Stage 3 ownership. Sections pipeline through the 3 stages. **Run the cast once per plan.**
- **Manager = pure orchestrator** (no content opinions, no votes; procedural authority only). **Steward = observer** (NO APPROVE/BLOCK, NO citation-verification — process facilitation only).
- **VISIBILITY INVARIANT (hard)**: use **visible `spawn_sessions` persona sessions** Rick can see/DM/steer. **NEVER the Workflow tool or Task subagents** — invisible/unsupervisable = PROHIBITED. An invisible-agent review is VOID. (Memory: `feedback_visible_spawn_sessions_never_subagents`.)
- **Cold cast**: participants `/clear` before taking seats (canon §Prereqs). This session was the opposite (contaminated) — the reason for the restart.

## 1. Why this restart (my failures — don't repeat)

Postmortem: `planning-is-prompting/src/rnd/2026.06.27-cascade-review-orthodoxy-failures-postmortem.md` (María's, authoritative). Root: I never ran a true parallel cast — 1 reviewer doing all 3 stages serially (= `/plan-review` in cascade clothing). On top of that: (a) I ran the 01 review via the **prohibited Workflow tool** (24 invisible subagents — Rick furious); (b) I let the **steward act as a content-gate** (role drift); (c) I acted as a **substantive manager**. All four are corrected in the canon above.

## 2. The work corpus

`lupin/src/rnd/v0.1.9/2026.06.25-notifications-to-multiplexer-migration-discrepancies/05-build-plans/`
- `00-plans-index.md` — the index + shared template + cross-cutting mandates + the open-design-question table (Q e′/h/i/j/k/l/m for Rick).
- Switchover-critical foundations: `00b-f0-audiostore-foundation.md`, `00c-phase6-tts-playback.md`.
- Keystone: `01-cc-session-B1-B5.md`. Corpus: `02`…`11` (the accordions).

**Rick's MVP scope (Option A, switchover-critical)**: 00b → 00c → 01 → verify Fleet#6/TaskList#7. Corpus 02–11 = fast-follow. Rick later upgraded to **true parallel cascade across plans** (run the canonical cast once per plan, in parallel — capped for legibility).

## 3. State of each plan (what to PRESERVE vs RE-REVIEW)

- **00b F0 — APPROVED** (serial two-layer gate; outcome SOUND, but NOT canon-reviewed). Revised plan is on disk. Key ratified design: active notification id is **CLIENT-SIDE**, owned by `TtsQueueStore.current()` (AudioStore stays **id-blind**); exact field `Notification.id_hash` (`shared/types.ts:334` ← `routers/notifications.py:859`); F0-d is **net-new build** (port legacy `playNotificationAudio` `notifications.js:15007` speak-initiation seam); `queueLength()`→**`burstLength()`** (OQ-F0.4, **Rick-confirmed**). Cross-plan ownership LOCK: **00b TtsQueueStore is sole owner** of selection+id-capture+TTS-invoke; downstream plans CONSUME `current()` id-blind-but-correlated.
- **00c Phase-6 — revised + steward-approved serially; Cheech's delta was in-flight (abandoned).** Revised plan on disk (completion via `store_audio_ended` event-emit → TtsQueueStore self-advances; cites `audio_streaming_complete` speech.py:818-822/:1115-1119 ← AudioTransport.ts:24). **Recommend: re-review under the canon** (it was only serial-gated).
- **01 Notifications — RESET to queued. The prohibited-Workflow run returned BLOCK but it is NOT authoritative — discard it.** HOWEVER its core finding is likely real and worth the fresh reviewers checking: *B4/OQ-1 sources the active-id from AudioStore, contradicting the 00b/F0 lock — B4 must CONSUME `TtsQueueStore.current()` id-blind, never re-source/mutate it; cite `Notification.id_hash`.* Also flagged (verify): B1's "4b33ceb7 is push-held" premise is stale (it's on origin); per-card-collapse + Rachel's accordion-toggle are both already in HEAD. (Workflow output file, for reference only, not authority: `tasks/w60f63bct.output`.)
- **02–11 — not yet reviewed** (fast-follow).

## 4. Open obligation + decisions

- **Seam obligation `2605dca5`** (task store, P2, currently owned mr radio → reassign to F0 implementer at build): the `store_audio_ended` subscription (00c-introduced) must be back-folded into 00b/F0's `TtsQueueStore` work breakdown (postdated 00b's approval).
- OQ-F0.4 = `burstLength()` (Rick, decided). Corpus open questions (e′/h/i/j/k/l/m) live in `00-plans-index.md` — surface to Rick as the reviews reach them.

## 5. Task board (query the store: `task_query owner_persona=…`)

MVP items (project lupin): 00b review `100ee443` (done), 00c review `060cec7d` (reset to queued for canon re-review), 01 review `2788dff0` (queued), verify `91788c40` (queued). Worker sub-tasks from the contaminated run were dropped/closed. The fresh manager re-mints worker items per the canon cast.

## 6. Fleet + infra

- Crew being reaped by me: **Cheech** (was reviewer), **Krishna** (was reviser). **María 🌸** = Workflow Steward (separate session; readying her own memento; realigned to observer-only).
- **Arbiter false-poke bug FIXED** by Tiberius 👑 (`67c8642b` on wip-v0.1.9; persona-twin ghost suppression). If staleness false-pokes recur, it's a different issue.
- Servers: dev `:7999` (auto-reload), test `:8000` (scheduled via `/api/test-suite/submit`). Arbiter `:8001` (`systemctl --user restart lupin-arbiter-app.service`).
- cosa-voice MCP startup: fetch schemas via ToolSearch, `get_session_info()`, name persona, `set_session_topic()`.

## 7. First moves for the fresh manager

1. MCP startup + read the canon (§0).
2. Decide with Rick: accept 00b/00c as-is, or re-review under canon. (00b outcome is sound; 00c only serial-gated.)
3. Spawn the canonical cast (1 author + 3 distinct reviewers, COLD/`/clear`) and run it **once per plan**, starting at 01 (keystone) — fold in the §3 01-finding as a hypothesis for the reviewers to verify, not as fact.
4. Keep the manager + steward non-substantive. Visible sessions only.
