# GCP VM dual-stack bring-up — a `:8000` test server beside `:7999`

**Row**: `f3b5ecf3` · **Author**: Sam 🎙️ (session `c69ffc15`, Lane C) · **Date**: 2026-07-27
**Status**: DESIGN ONLY — nothing implemented, no compose port changed, no deploy attempted.

> ⚠️ **This document does not assert the VM's current state.** The row it comes from opens
> with *"the VM currently runs ONLY the cloud-gpu rest stack on 7999"* — a snapshot from
> 2026-07-16 that was already suspect by 07-26 (`:7999` went down at 14:21 that day on a
> deploy missing `--no-deps`, bug `70794d58`). **Re-derive the VM's state before acting on
> any of this.** The VM was read-only to me for the whole of this session.

---

## 1. The ruling this design implements

Rick, 2026-07-26, verbatim (via `ask_multiple_choice`, custom answer — `answered: true`,
`default_used: false`):

> *"the point of having a separate test server does not obviate my need to be able to run 2
> services bound to ports 7999 and port 8000. That is simply feature parity for what already
> exists on the dev server."*

⇒ **COEXISTENCE.** Both stacks bound simultaneously on the VM, mirroring the dev box. Not
one-stack-at-a-time.

María 🌸 recorded that her own recommendation (one-stack-at-a-time) was wrong, and *why*: she
reasoned about the VM in isolation instead of asking what the dev server already does. The
neighbour is the point. That reasoning defect is preserved here because the same trap is
available to whoever implements this.

## 2. Scope — corrected against the row's own body

| Item | Status |
|---|---|
| Remap `cloud-test` rest `7999:7999` → `8000:7999` | ✅ in scope |
| "Avoid the `7998` model-server clash" | ❌ **STRUCK** — no clash exists |
| Confirm DB targeting does not fight `cloud-gpu` | ✅ in scope, **unresolved — see §5** |
| Teach preflight about BOTH stacks | ✅ **precondition, not follow-up** |

**On the struck item** — confirmed at source by Mr Radio 🦉:

```
docker-compose.cloud-gpu.yml :94    - "7999:7999"
docker-compose.cloud-test.yml:84    - "7999:7999"   <- same host port, REAL collision
docker-compose.cloud-test.yml:148   - "7998:7998"   <- cloud-gpu publishes NO 7998
```

`cloud-gpu` runs the model server on Cloud Run, so it publishes nothing on `7998`. Under the
coexistence ruling **the remap is `:7999` only.** The row's body was stale here — the second
stale claim in it found by the simple act of opening the file.

## 3. The precondition — and why it is not a follow-up

Mr Radio's flag, promoted here to the top of the implementation order:

> **Preflight will not notice the second stack, and its silence will read as approval.**

All 31 of `preflight-vm.sh`'s assertions are written against ONE app on `:7999` — `svc
status`, the app-URL probes, the container-surface layer (A1/C). Stand a second stack up and
**preflight goes GREEN while structurally blind to half the machine.**

That is the same shape this lane keeps finding: *an assertion whose scope is narrower than the
thing it appears to bless.* It is `70794d58`'s false-healthy proxy (probes `:9090`, never
touches the socket) and `b39c350d`'s confident path resolution, one layer up.

⇒ **Coexistence must not land before preflight can see both stacks.** A second server with no
instrument is worse than no second server, because the instrument reports success.

## 4. Implementation order

1. **Re-derive VM state.** `lupin-vm.sh svc status`; what is actually bound on `:7999`/`:8000`;
   which compose files exist on the VM and whether they match this repo's.
2. **Teach preflight both stacks** — parameterize the app-URL and container-surface assertions
   by (stack, port, container) instead of an implicit single `:7999` app.
   ⚠️ `preflight-vm.sh` / `preflight-vm-lib.sh` are **Rio ⚡'s files**, under active edit on
   2026-07-27 (`b5ca8fd5`). Coordinate; do not fork them.
3. **Remap** `docker-compose.cloud-test.yml` rest ports `7999:7999` → `8000:7999`.
   ⚠️ `src/tests/unit/deploy/test_compose_service_parity.py` reads these compose files and will
   have an opinion. Satisfy it rather than exempt it — it caught a real divergence during the
   `b39c350d` work the same day.
4. **Resolve §5** — the proxy topology. Blocking.
5. **Bring up**, re-run preflight, confirm it now asserts about both stacks.

## 5. ⚠️ The unresolved question this design cannot close

**Do the two stacks share one Cloud SQL proxy, or run one each?**

Both compose files declare their own `cloud-sql-proxy` *and* their own `cloudsql-socket-init`,
with **identical `container_name`s** (`lupin-cloudsql-proxy`, `lupin-cloudsql-socket-init`).
Running both stacks as-written on one host means two compose projects each trying to own a
container of the same name.

And it collides head-on with `70794d58`. `cloudsql-socket-init` runs:

```
rm -f /cloudsql/*/.s.PGSQL.5432
```

⇒ **If the second stack's socket-init runs while the first stack's proxy is live, it deletes
the running proxy's socket** — the exact P1 closed today, re-armed by a topology change rather
than by a missing flag. **`--no-deps` does not save you here**, because bringing up a genuinely
new stack legitimately runs its own init. The mechanism was measured today (isolated compose
project): the socket is deleted, the proxy never re-creates it, and the proxy keeps reporting
`running health=healthy` the whole time.

Options, none yet chosen:

- **(a) One shared proxy** — owned by whichever stack starts first; the second declares no proxy
  and mounts the same socket volume. Cleanest, but makes the stacks non-independent: a
  `cloud-gpu` bounce silently takes `:8000`'s database with it.
- **(b) Two proxies, two socket volumes, distinct container names.** Fully independent; costs a
  second proxy process and requires both compose files to stop hardcoding `container_name`.
- **(c) Two proxies sharing one socket volume.** ❌ **Never** — that is the `70794d58` deletion
  as a design.

**Recommendation: (b).** The stacks are meant to be independently bounceable — that is the
entire point of a test server beside a dev server — and (a) reintroduces exactly the
cross-stack coupling the coexistence ruling was asked for in order to avoid. But this is a
ruling for the VM seat, not mine to take.

## 6. What was NOT done, stated plainly

No compose port changed. No preflight file touched. No deploy, no bounce, no `docker compose
up`. The VM was Rick's throughout this session and read-only to me. **This document is the
deliverable; implementation is gated on §5 being ruled and on Rio's preflight work landing.**
