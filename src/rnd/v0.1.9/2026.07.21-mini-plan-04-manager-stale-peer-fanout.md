# Mini-Plan 04 — MANAGER-STALE advisory fans out to peer managers

**Filed** 2026-07-21 by Mr. Radio 🦉 (`56a74d7b`) · **Reviewer**: María 🌸 · **Status**: ✅ REVIEWED (María 🌸, re-cut applied) · case 9 fork RULED by Rick — READY TO BUILD
**Trigger**: Rick, 2026-07-21 — *"the arbiter was supposed to only be enabled to send folks to me… this is obviously a bug."*

---

## 1. Verdict

**The config is correct. The routing table is wrong.** Case 14 (`MANAGER-STALE`) passes an operator-audience gate and is then fanned out to every active peer manager by its tier.

⚠️ **This plan REVERSES a deliberate carve-out, it does not complete a sweep.** My first draft claimed the latter; María 🌸 refuted it from the table (§2c). Case 14 was named and explicitly distinguished by the very commit that converted cases 16/17. The reversal is still right — but it must be argued on the merits, and §2c does that: **case 14 fires on SILENCE, not ABSENCE, so its subject's crew is not orphaned.**

---

## 2. Evidence, read at source

### 2a. Rick's config does what he intended

`src/conf/lupin-app.ini:629-630`

```ini
arbiter auto poke managers enabled     = false
arbiter auto poke operator enabled     = true
```

The manager-**directed** poke honors it — `src/cosa/agents/heartbeat_arbiter/arbiter_job.py:4580-4581` checks `AUDIENCE_MANAGER` → `poke_managers_enabled` → `false` → silent. **That half is not defective.**

### 2b. The advisory half routes past the gate it just passed

`src/cosa/agents/heartbeat_arbiter/arbiter_job.py:4564`

```python
if sid not in self._mgr_advised and self._poke_audience_enabled( AUDIENCE_OPERATOR ):
    ...
    self._route( CASE_MANAGER_STALE_ADVISORY, f"MANAGER-STALE: {persona} silent …",
                 active_managers=active_managers, exclude_persona=persona )
```

`src/cosa/agents/heartbeat_arbiter/arbiter_routing.py:141`

```python
CASE_MANAGER_STALE_ADVISORY : TIER_RICK_AND_MANAGERS,
```

`src/cosa/agents/heartbeat_arbiter/arbiter_job.py:1913` — `TIER_RICK_AND_MANAGERS → _emit_to_rick + _emit_dm each active manager`.

⇒ **An `AUDIENCE_OPERATOR` check authorizes the send; the tier then fans it to every active peer manager.** The audience gate governs *whether the case fires*, never *who receives it*. Those are different questions and the code reads as though they are the same one.

### 2c. 🔴 THIS IS A REVERSAL OF A CONSIDERED CARVE-OUT — NOT A MISSED SWEEP ROW

**My first draft said case 14 was missed by the `ff91cff4` / `f48f089d` sweeps and never re-examined. That is REFUTED.** María 🌸 found the disproof by reading the table instead of my prose. `src/cosa/agents/heartbeat_arbiter/arbiter_routing.py:85-86`, the case-16 comment, verbatim:

> *"a peer manager has zero legitimate action (it can't adopt an owned, alive crew, **unlike the case-14 orphaned-crew path**)."*

That is `f48f089d`'s own author, 2026-07-08, **naming case 14 and deliberately distinguishing it** while converting 16 and 17. Case 14 was examined and left on purpose. The distinction drawn was **orphaned crew vs owned-alive crew**.

⇒ **This plan REVERSES a considered decision.** Rick called it a bug and he outranks the comment — but the doc must say that plainly, and must answer the orphaned-crew argument on its merits, or the next reader finds that comment and reverts us with it.

#### The merits answer: a STALE manager's crew is NOT orphaned

The carve-out assumes *dark manager ⇒ leaderless crew ⇒ a peer could step in*. **The code itself refutes the first implication.** Case 14's own eligibility path carries four independent false-positive suppressors, every one of them added because "silent past threshold" kept meaning "alive and working":

| Guard | Bug | What it exists to prevent |
|---|---|---|
| persona-twin suppression | `7c931b3a` | a re-spun manager's ghost row poking its LIVE twin |
| bridge-mtime veto | `26dd3afb` | union age reads stale while the bridge was touched seconds ago |
| last-signal velocity streak | `285c0343` | *"mr radio 13:08→13:59→14:59→15:59, **four false episodes, all advancing**"* |
| store-health gate | `33949e83` | a degraded owed-read making silence look like darkness |

**Case 14 fires on SILENCE, not on ABSENCE.** A manager silent 45 minutes is overwhelmingly a manager on a slow wake-tick, parked, or blocked — alive, and still owning its crew. That is precisely the *"owned, alive crew"* the case-16 comment says a peer has zero legitimate action on. The orphaned-crew premise belongs to a manager that is **demonstrably gone**, which is case 9's subject, not case 14's.

⇒ The carve-out is not wrong about orphaned crews. **It is wrong that case 14 detects one.**

#### Corroborating rulings (context, not the argument)

| Sweep | Date | Ruling |
|---|---|---|
| `ff91cff4` | 2026-06-30 | Case 20: a stuck/dead **MANAGER subject** → **Rick-only**. *"Managers answer to Rick, not to each other: a manager can't own itself."* Incident named in the comment: **"Mr. Radio tapped about Tiberius."** |
| `f48f089d` | 2026-07-08 | Cases **16** / **17** → **Rick-only**, *"completing the `ff91cff4` sweep."* |

### 2d. The design already conceded half the point

Bug `b9911943` added `exclude_persona`, dropping the **subject** manager from the fan-out. So it was already established that these advisories should not reach the subject. **Nobody asked whether they should reach peers at all.**

### 2e. Observed cost

2026-07-21, this session: **4** MANAGER-STALE DMs about Sam's Tiberius, **1** about Sam, **2** staff-up advisories naming Sam as stuck — **7 interrupts, none of them my seats, while my fleet was at zero.** Each is a turn spent deciding it isn't mine.

---

## 3. The fix

**One table entry + its pins.**

```python
# arbiter_routing.py:141
CASE_MANAGER_STALE_ADVISORY : TIER_RICK_ONLY,   # was TIER_RICK_AND_MANAGERS
```

Update the comment block at `:67-70` to record the reversal and cite `ff91cff4` / `f48f089d` as the governing rulings, so the next reader does not re-derive the 2026-06-11 rationale and revert it.

### Consequential edits

| File | Change |
|---|---|
| `src/cosa/agents/heartbeat_arbiter/arbiter_routing.py:181` | smoke assertion `tier_for( CASE_MANAGER_STALE_ADVISORY ) == TIER_RICK_ONLY` |
| `src/tests/unit/test_arbiter_routing.py:171` | same flip — **this test currently PINS the defect** |
| `src/tests/unit/test_arbiter_manager_staleness.py:305` | `assert ( "OtherMgr", advisories[0] ) in gw.sent` → assert **no entry** in `gw.sent` has recipient `"OtherMgr"` |
| `…staleness.py:761` `test_case14_stale_subject_gets_poke_not_self_advisory` | **DELETE** — see §3a |
| `…staleness.py:776` `test_case14_subject_exclusion_is_canonical_key_tolerant` | **DELETE** — see §3a |
| `…staleness.py:824` `test_case14_no_regression_when_subject_absent_everyone_served` | **DELETE** — see §3a |

### 3a. 🔴 SIX PINS, NOT THREE — and `b9911943` loses its integration coverage

**Both corrections are Extra 2 🪨's, from receipts, caught against the live RED state. My draft was wrong twice in the same paragraph.**

**Undercount.** I listed three pins. **Six fail** (`6 failed, 276 passed`). The three I missed are `b9911943`'s **entire end-to-end coverage** — the only tests exercising `exclude_persona` through `_check_manager_staleness` rather than by calling `_route` directly. They assert a **peer received** the advisory, so this flip kills all three by construction.

**And the falsehood.** My draft said `exclude_persona` *"stays live for cases 5/8/9/11/13 — do not remove it."* **Verified false at source** (independent grep, one call site):

```
arbiter_job.py:1903  def _route( … exclude_persona=None )
arbiter_job.py:4576  exclude_persona=persona    ← THE ONLY CALLER, and it is case 14
```

Cases 5 (`:2371`), 8 (`:2723`), 9 (`:3436`), 11 (`:3715`), 13 (`:4198`) each pass `active_managers=` and **nothing else.** ⇒ After this change `exclude_persona` is **production-DEAD, not "inert for 14."** Written the way I wrote it, the next reader greps, finds it false, and distrusts the rest of the doc.

#### DISPOSITION — RULED

1. **KEEP the parameter.** It is the correct seam for any future manager-subject case, and it retains unit coverage via the direct `_route` calls at `test_arbiter_routing.py:212-250`.
2. ~~DELETE `:761` / `:776` / `:824`.~~ **AMENDED — DELETE `:776` ONLY. `:761` and `:824` STAND.**
3. **Say it in the commit message**: *"`b9911943`'s integration coverage NARROWS to `:761`/`:824`; `:776` is deleted as vacuously-passable."*

#### 🔴 3a-ii. THE AMENDMENT, AND WHY — the ruling lost to its own reviewer's receipts

I ruled "delete all three" **on a prediction**: that three inverted tests would assert one fact while wearing `b9911943`'s name. Extra 2 🪨 then **mutation-tested them and two-thirds of my prediction was false.**

**Mutation A** — revert the table line alone: `6 failed`. Every new/edited pin caught it. *The fence was tried and it held.*

**Mutation B** — María's F3 defeat, made concrete: revert the table **and** rename the marker `MANAGER-STALE:` → `MANAGER-QUIET:`, so **the peer still receives the DM while the text moves.**

| Test | Assert form | Under mutation B |
|---|---|---|
| `:761` | `[ s for s in gw.sent if s[0] == "OtherMgr" ] == []` | **recipient-keyed → CATCHES it** ✅ |
| `:824` | `[ s for s in gw.sent if s[0] in ("PeerA","PeerB") ] == []` | **recipient-keyed → CATCHES it** ✅ |
| `:776` | `[ s for s in gw.sent if "MANAGER-STALE:" in s[1] ] == []` | **MESSAGE-keyed → PASSES VACUOUSLY while both peers are DM'd** ❌ |

`:761` and `:824` are recipient-keyed, survive the defeat, and cover **different scenarios** (subject-present vs subject-absent). They are good tests. **Deleting them to satisfy a concern the receipts refuted would be preferring the ruling to the evidence.**

`:776` still goes — for a **better reason than mine**. It survives mutation B only on a sibling Rick assertion that happens to share the same string literal; its peer half goes silent. That is F3's weakness *in recipient-filter clothing*. Its canonical-key receipt is already re-homed to `test_arbiter_routing.py:223`.

#### ⇒ STANDING RULE FROM THIS
**A peer-silence assertion keys on `s[0]` (the RECIPIENT), never `s[1]` (the MESSAGE).** A message-keyed absence check goes quiet the moment the message is renamed — while the thing it forbids keeps happening. Leave this as a comment where `:776` was.

The record must read **"production-inert; retained as a `_route` capability, exercised only by direct-call unit tests"** — never "still load-bearing for 5/8/9/11/13."

### Explicitly NOT in scope

- The manager-**directed** poke at `:4581`. It obeys its config and is a different mechanism.
- **Removing** `exclude_persona`. It goes production-dead (§3a) — keep it anyway, as the seam plus its unit coverage.
- Cases 5, 8, 9, 11, 13 — all stay on `TIER_RICK_AND_MANAGERS`. **Case 9 is NOT exempt for the hand-wave reason my draft gave** ("genuine fleet crisis"); it is its own decision, argued in §7. Read §7 before touching any of these.

---

## 4. Acceptance criteria

1. `tier_for( CASE_MANAGER_STALE_ADVISORY ) == TIER_RICK_ONLY`.
2. A stale-manager episode with ≥1 active peer manager emits **to Rick and to nobody else** — asserted on the gateway's actual sent list, not on a return count.
   🔴 **Assert NO ENTRY in `gw.sent` has recipient `"OtherMgr"` — never that one specific `(recipient, message)` tuple is absent** (María, F3). The existing line 304 is a tuple-membership check; inverted naively it passes the moment the message text changes *while the peer still receives a DM*. **A control satisfiable by an unrelated edit is not a control.**
3. **Rick still receives it.** A regression that silences the advisory entirely passes AC-2 and fails the user; pin the Rick hop explicitly.
4. Cases 5/8/9/11/13 **still** fan out to peer managers — one assertion each, so this change cannot quietly widen into the fleet-crisis tier.
5. `arbiter_routing.py` smoke passes; the 20-case parametrized table test mirrors the change.
6. ~~100% lines/branches/functions on every file touched.~~ **AC-6 AS WRITTEN IS UNSATISFIABLE-BY-INFORMATION. Record it in the honest form below.**

> ### 🔴 AC-6 FINAL — 100%, AND IT CANNOT SEE THIS CHANGE
> **Verified by line range (Extra 2 🪨, `be3d2fe9..HEAD`), which is exactly why the AC demanded a line range instead of a percentage.**
>
> `arbiter_routing.py`: **18 stmts · 0 miss · 0 branch · 0 partial · 100%.** The 37 lines this change touched in that file:
> ```
> executed_lines : [30,31,32,33,34,35,37,63,94,95,118,119,129,137,148,150,174,186]
> changed lines  : 68-91, 107-109  → non-executable (comment block)
>                  164             → non-executable  ← THE TABLE ENTRY ITSELF
>                  202-211         → EXCLUDED by config ← the smoke assertions
> ```
> **Not one changed line appears in `executed_lines`.** Two mechanisms, individually benign, jointly fatal to the AC:
> 1. **`:164` is not a statement.** It is a continuation line inside the `CASE_TIERS = { … }` dict literal, which coverage counts as ONE statement at `:150`. That statement executes on import **whatever value the line holds** — coverage reports 100% for `TIER_RICK_ONLY`, for `TIER_RICK_AND_MANAGERS`, or for garbage.
> 2. **`:202-211`, the smoke assertions — consequential edit #1 in this very plan — are config-excluded** by `pyproject.toml:195-200` (`exclude_also = [ "def quick_smoke_test", … ]`, 47 lines).
>
> ⇒ **The 18/18 is honest and carries ZERO information about this diff.** It is the file's *other* statements.
>
> **THE RECORD READS**: *100% (18/18 stmts, 0 branches) on `arbiter_routing.py`; the change itself is INVISIBLE to coverage — the table entry is a dict-literal continuation and the smoke block is config-excluded. **The change is pinned by TESTS, verified by MUTATION in both directions — never by the coverage number.***
>
> **The general form, worth more than this fix**: a coverage gate cannot see a change to a **data literal**. Any table-driven routing/config/dispatch change is invisible to it. Mutation is the only instrument that answers "would a wrong value be caught."

### The control this needs

**A test that FAILS before the fix and passes after, whose failure text names the peer manager.** The current suite is green *against the defect* — `src/tests/unit/test_arbiter_routing.py:171` and `src/tests/unit/test_arbiter_manager_staleness.py:304` both assert the fan-out is present. A change that flipped the table without flipping those would go red for the right reason; a change that flipped all three together and never watched the RED state proves nothing. **Predict the failure text before running it.**

---

## 5. Risks

| Risk | Reading |
|---|---|
| A genuinely leaderless crew now goes unnoticed by peers | **Accepted, and it is Rick's ruling to make.** Case 20 already decided a manager-subject escalation is Rick's; he actuates reap/replace. A peer manager cannot adopt an owned crew. |
| Silencing Rick too | The real hazard. AC-3 exists for it. |
| Blast radius beyond the arbiter | None found: `CASE_MANAGER_STALE_ADVISORY` is imported only by `arbiter_job.py` and two test files. **Grep-derived, not measured against a running arbiter.** ⚠️ And the grep answers *who imports the constant*, which is NOT the question *who receives the DM at runtime* (María, F4). **The load-bearing evidence is §2e — 7 interrupts observed at a live seat this session. The grep corroborates; it does not carry the claim.** |
| Arbiter must be bounced to take effect | `systemctl --user restart lupin-arbiter-app.service`. Manager authority when idle; announce + log. **Not part of the code change.** |

---

## 6. Crew shape

**Implementer** — the table flip, the comment record, the three test edits, RED-first with a predicted failure text.
**Tester** — the four ACs plus the non-regression assertions on cases 5/8/9/11/13; runs the arbiter unit suites (`test_arbiter_routing`, `test_arbiter_manager_staleness`, `test_arbiter_poke_audience`, `test_arbiter_scenarios`).
**Reviewer** — adversarial: does the fix silence Rick? Does any *other* manager-subject case still fan out that the two sweeps also missed? Is `exclude_persona` still load-bearing elsewhere?

**Venue**: `:7999` — unit only, no persistent state, well under 2 minutes.
**Commits**: held. Push is Rick's alone.

---

## 7. The other cases — ANSWERED (María 🌸, 2026-07-21)

My draft asked whether case 14 was the last survivor. **The answer is no — case 9 is its near-twin, and my first draft exempted it in the same breath as it refuted 14's rationale.**

| Case | Tier | Verdict |
|---|---|---|
| **9** manager-down + HOLD | `RICK_AND_MANAGERS` | **STAYS. Distinguished below — this is the load-bearing call of the re-cut.** |
| **13** auto-poke reap-rec | `RICK_AND_MANAGERS` | **STAYS.** Confirmed examined: case 20's comment names it (*"the case-7 manager-tap OR the case-13 auto-poke reap-rec"*) and already carved the manager-SUBJECT path out. The remaining fan-out is for non-manager subjects and is correct. |
| **8** unresolved-manager **worker** | `RICK_AND_MANAGERS` | **STAYS.** Subject is a worker, not a manager — outside the family. Noted because **this is where the orphan-adoption argument legitimately lives**: it is the argument case 14 was carved out on, and case 8 is the case that actually earns it. |
| **5** deadlock · **11** fleet-stall | `RICK_AND_MANAGERS` | **STAY.** Fleet-level by subject. |

### 🔴 Why case 9 stays and case 14 flips — the distinction, stated so it can be attacked

María's challenge is exact: case 9 reads *"leaderless crew; re-staff"* and case 14 reads *"the dark manager's crew is leaderless-in-waiting; any manager could step in."* **Near-verbatim justifications, one table line apart.** Flipping one and preserving the other needs a reason or it is arbitrary.

**The reason is the detector, not the rationale.**

- **Case 9 fires on a manager that is DOWN and holding** — an absence verdict. Its crew genuinely has no owner. A peer stepping in is a real action on a real orphan.
- **Case 14 fires on 45 minutes of SILENCE from a manager that is still in the roster** — and, per §2c, carries four suppressors precisely because silence kept meaning *alive*. Its crew is owned by a living manager. A peer "stepping in" would be adopting a crew that already has an owner.

⇒ **Case 9's premise is measured; case 14's is inferred, and inferred wrong often enough that four guards exist to catch it.** Same words, different evidentiary standing.

**If that distinction does not survive scrutiny, the honest consequence is that case 9 flips too — not that case 14 stays.** I am not proposing that in this plan; I am naming it as the fork so nobody has to re-derive it.

> ### ✅ RULED BY RICK, 2026-07-21 — CASE 9 STAYS
> Put to him as a direct yes/no with the falsifier stated and my recommendation attached. Answer: **NO — do not flip case 9.** Only case 14 moves to `TIER_RICK_ONLY`.
>
> **Do not re-litigate this in review.** The detector distinction (measured absence vs inferred silence) is the ratified basis. A reviewer who wants case 9 flipped needs new evidence that case 9's subject is also inferred rather than measured — not a re-reading of the two rationales, which is the argument that was already heard and decided.
