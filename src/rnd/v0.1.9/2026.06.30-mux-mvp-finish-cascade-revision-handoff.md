# Mux MVP-Finish Cascade — Revision-Handoff Synthesis (Step 9)

> **Manager:** Mr. Radio 🦉 (`ef70b5f4`). **Cascade:** mux-mvp-finish. **Completed** 2026-06-30 (~21:26 EDT).
> **Input plan** (revised IN-PLACE by the author across the cascade — it IS the implementation-ready artifact):
> `2026.06.30-mux-mvp-finish-remediation-plan.md`. This doc = cascade telemetry + per-section revision
> summary + open decisions + hand-off statement.

## §1 Purpose
The `/plan-review-cascaded` review of the 6-item mux MVP-finish remediation plan is complete. All 5 sections
cleared all 3 review stages (Usability/Reuse → Viability/Fitness → Ownership-Language) plus author revisions.
This doc hands the revised plan to the build phase, once Rick resolves the open decisions (§6) and lifts the
client freeze.

## §2 Cascade telemetry
- **Cast** (visible spawned sessions): Manager Mr. Radio 🦉 · Observer/Steward María 🌸 · Author Tiffany 💍 ·
  Stage-1 (Reuse) Sam 🎙️ · Stage-2 (Fitness) Krishna 🦚 · Stage-3 (Ownership) Clayton 😎.
- **Config:** all workflow defaults; no external tick (arbiter + Stop-hook drove it to completion).
- **Findings:** ~30 closed across 15 stage-reviews. **ZERO foundational.** Mostly cross-section/inconsistency
  scope-corrections + fix-mechanism pins + ownership-language (EXECUTOR/coverage) refinements; a few cosmetic.
- **User escalations:** 0 synchronous. **3 escalations-considered-and-DECLINED** (B1, V9/C1, D1) — all
  factual-correction/simplification, no frozen decision hit; María-concurred (genuine, grep-verified receipts).
- **Author rounds:** A×3, B×3, C×3, D×3, E×3 — all diff-able, zero scope-creep, disciplined forward-sweeps.
- **Close-out grep-gate (OE-1):** clean — every verification step is EXECUTOR-tagged + coverage-shaped.

## §3 Per-section revision summary
### Section A — Interactive Bugs — CLOSED
- Bug#2 rationale corrected: raw-read is right for **mux-internal write/read symmetry** (the `setAttribute`
  writer is raw), NOT "matches legacy" (legacy encodes both sides — a match-legacy impl would ADD
  `encodeURIComponent`, re-introducing the bug).
- Bug#1: head pinned to full-list source `NotificationsListRenderer.ts:691` (not per-date `dateAccordion` →
  avoids cross-date multi-head); head = first-arrived (verified `list()` push-only); mechanism = **PRE-FILTER**
  entries before `keyedListMerge` (its `create` can't return null); `isGroupHead` threaded 4-hop; cross-date
  fixture; 176→1 Oracle assertion promoted first-class.

### Section B — Colored Panels (AR + PLY) — CLOSED
- Not ground-up (renderers/stores/templates exist → CSS + empty-state parity).
- **§2 merge-order CORRECTED:** AR→`action-required.css`, PLY→`tts-chrome.css` (dedicated), only VIS→shared
  sheet → **L2/L3 CSS parallelizable** (forced serialization dropped).
- AR dual-ownership resolved (`phase6bOwner` single-owner); one shared `renderActionRequiredEmpty()` helper;
  PLY empty-state is `audio.state()===idle` (not `count===0`); vestigial `TtsQueueStore` dropped.
- B2 (100%-gate): PLY test covers all 4 `audio.state()` branch-arms.

### Section C — Visual Polish (V5–V10/V13) — CLOSED
- 4/6 rows genuine polish (V5 is actually *new* header CSS; V6/V7/V13 retune). Per-row CSS files named +
  grep-verified.
- **V9** (🔉 sub-icon): **OQ-6 RESOLVED → BUILD, as a PORT** — grep-verified legacy DOES render the
  strip-icon sub-badge/indicator (`notifications.js:5087-5106` / `:10930-10973`); the earlier "legacy lacks
  it / NET-NEW" was a client-only-grep error. Driven by `StripSession.active` + `voice_persona.borrowed`.
- **V10:** badge + N-new pill already exist (retune, not "add"); (b) "topic" **OQ-7 RESOLVED → DEFERRED
  post-flip** (product choice). Factual correction: a server-side session-topic source DOES exist
  (`set_session_topic` + `session_topic` notif) — the "unbuilt server-side" framing was wrong.

### Section D — NAV — CLOSED
- NAV is a **PORT** of legacy `lupin-nav.js` (206L complete renderer + `lupin-nav.css` already linked), not a
  new build.
- **AUTH-CORRECTNESS FIX (F-K-D1):** logout must call `StorageService.clearTokens()` (persisted), NOT
  `AuthManager.clearToken()` (in-memory only → would leave the user logged in). Reuse legacy logout handler;
  only-new = wiring an exported `logout()`.
- **Ownership (F-Clayton-D1):** the logout test asserts the persisted-token-cleared **OUTCOME** (a "wired"
  assertion would pass on the bug) + branch completeness.
- **OQ-1 RESOLVED → KEEP "Notifications"** (Rick): the nav item stays labeled "Notifications" (not relabeled/dropped).

### Section E — Reconciliation + Verification — CLOSED
- §0 verify-not-rebuild **VERIFIED sound** (Phase-6 `2fcc68d0` + B5 `393251c5` real + do what claimed).
- V-P6 = **RUN** existing `test_phase6_tts_playback_protocol_e2e.py` (paid-skip gated), not hand-rolled;
  V-B5 = Oracle ±1px.
- **§2 TS-overlap CARVE-OUT:** CSS disjoint, but L1(`isGroupHead`)∩L3(V10) share `senderCard.ts` → within-file
  non-overlap gate (`tsc` + `c8 --100` on `senderCard.ts` after both merge).
- Added WS-smoke test layer; framed V-P6 as a bounded API-spend exception; directory-wide `c8` glob (new files
  covered by construction).

## §4 Cross-section ratification
- **Parallelization:** L1/L4 parallel; L2/L3 CSS parallel; the ONLY TS overlap is L1↔L3 on `senderCard.ts`
  (carve-out gated). Forced L2→L3 serialization RETIRED.
- **Plan-wide EXECUTOR-tag + coverage-shape sweep** applied across all sections (Clayton's cross-section
  pattern) — close-out grep-gate clean.

## §5 Post-cascade fold
- The plan doc is revised in-place and implementation-ready pending §6 decisions + freeze-lift.
- Build lanes (once greenlit): **L1** bugs (serial) · **L2** AR+PLY · **L3** VIS · **L4** NAV — L1/L4 + L2/L3
  parallel, with the L1↔L3 `senderCard.ts` carve-out. Plus **V-P6 / V-B5** verify tasks.

## §6 Open decisions for Rick (cascade output)
| # | Decision | Rick's ruling (2026-06-30) |
|---|---|---|
| **OQ-1** | NAV "Notifications" item label | **KEEP "Notifications"** (not relabeled/dropped; ported `NAV_ITEMS` keeps the label verbatim) |
| **OQ-6** | V9 🔉 session-strip sub-icon | **BUILD — as a PORT.** Grep-verified: legacy DOES render it (`notifications.js:5087-5106` / `:10930-10973`); the "legacy lacks it" claim was a client-only-grep error |
| **OQ-7** | V10(b) CC-card "topic" | **DEFER post-flip** (product choice). A server-side session-topic source DOES exist (`set_session_topic` + `session_topic` notif `routers/notifications.py:610`) — "no server source" was wrong; deferral ≠ blocker |
| **freeze** | client-freeze lift (build edits client code) | Lift when ready to build |
| **B5-drift** | run V-B5 Oracle to confirm B5 parity | Run at build (verify task) |
| **A/B-harness** | build the automated Playwright A/B report | Build as part of L3 / verification |

## §7 Hand-off statement
The revised plan is implementation-ready **and CLEARED to build**: Rick (a) resolved OQ-1/6/7 (KEEP
"Notifications" · V9 BUILD-as-PORT · V10(b) DEFER post-flip), (b) lifted the client freeze, and (c) authorized
the build (2026-06-30). The Manager (Mr. Radio 🦉) spawns the build worker-lanes per the §2 merge-order,
reviews adversarially at commit, and commit-held. **Push + the switchover flip remain Rick's word alone.**
