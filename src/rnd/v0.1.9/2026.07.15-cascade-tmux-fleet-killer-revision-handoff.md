# Cascade Revision-Handoff — cascade-tmux-fleet-killer

**Artifact class**: Step-9 revision-handoff doc (`NN-cascade-revision-handoff.md` per `plan-review-cascaded.md` §9.1; dated name per Lupin file-naming convention)
**Date**: 2026-07-15
**Author**: Mr. Radio 🦉 (cascade Manager, session bf549da1)
**Cascade**: `cascade-tmux-fleet-killer` (review-cascade flavor, 3 sections × 3 stages)
**State**: `implementation_handoff_ready` pending light-review thumbs-up (Rachel 🕊️)

---

## §1 Purpose + input plan

This doc bridges cascade-complete → implementer-ready. It consolidates the cascade's revision package so an implementer (or the SWE team) can proceed **without back-referencing any cascade topic file**.

- **Input plan (FINAL-CURRENT, all cascade revisions folded in on disk — this is the implementation source of truth)**:
  `src/rnd/v0.1.9/2026.07.14-tmux-fleet-killer-vertex-taint-test-isolation-leak-fix-plan.md` (last author edit 2026-07-15T03:07:18Z, Clayton round-2-C)
- **What the plan fixes**: P0 — the vertex-taint smoke test's fixture teardown ran `tmux kill-server` against the SHARED fleet socket (inherited `$TMUX` beats `TMUX_TMPDIR` on tmux 3.2a), repeatedly killing every Claude Code session across three projects. Three deaths positively attributed (14:55 / 20:04 / 21:13 EDT 2026-07-14) via transcript forensics.
- **Nothing remains to revise in the plan.** The cascade closed with the plan already at final text — the implementer implements the plan **as cascade-revised**, verbatim.
- **Two plan-text staleness notes (closures post-date the final author edit — SUPERSEDED by closures recorded here, do NOT stall on them)**: plan §8's "Open sub-question 5 — TBD" closed **NO-COLLISION** (§3.C below; María Stage-3, 03:11:51Z), and §5.3/§8's "user-confirm rides the walkthrough" flag on OSQ-1 is **satisfied** (Rick confirmed 03:26Z — §2 above). The plan's revision cap was consumed before these landed; this doc is the closure record.

## §2 Cascade telemetry

| Metric | Value |
|---|---|
| Sections | 3/3 accepted (A diagnosis/evidence · B env-strip fix · C instrumentation/continuity) |
| Stages per section | 3 (Stage 1 REUSE — Rachel 🕊️ · Stage 2 viability — Rio ⚡ · Stage 3 ownership — María 🌸; author Clayton 😎) |
| Findings | **34** (A 11 = Rachel 6/Rio 3/María 2 · B 12 = Rachel 6/Rio 4/María 2 · C 11 = Rachel 6/Rio 4/María 1) |
| Foundational | **0** |
| Votes called | **0** |
| User escalations | **0** (one manager-unilateral-ratify-by-concurrence: OSQ-4; reported, not gated) |
| Author revision turns | 6 — every section cap consumed exactly 2/2 |
| Reviewer turn-cap-3 | never touched across 9 stage-reviews |
| Wall-clock | ~55 min (first spawn → Stage-3 complete) |
| Verbatim-accept rate | all 34 closures ratified as classified; zero re-litigations |

**User decision (post-cascade, closed)**: OSQ-1 confirmed by Rick via /plan-decide, 2026-07-15T03:26Z — **execve tracer, ordered LAST**. No open user decisions remain.

## §3 Per-section revision summary

> Severity legend: F = foundational, I = inconsistency, C = cosmetic. Per-finding audit trail (verbatim classifications, `rounds_used`, provenance) lives on the section topics `cascade-tmux-fleet-killer-section-{a,b,c}`; nothing there is needed to implement.

### §3.A Section A — Diagnosis & evidence (§1–§3, §8-OSQ-1/6, Appendix A)

**Findings closed**: 11 (0 F · 3 I · 8 C). Headline inconsistencies: Rachel-F1 (runbook §10 contradiction — killtrace DOES capture kill-server; runbook rule overturned by the 21:13 receipt), F-Rio-A1 (positive attribution existed in CC transcripts — "no positive log" premise false, in the plan's favor), F-Rio-A2 (5-death narrative overclaimed: 14:12 predates the file; 19:21 was a deliberate acceptance kill).

**Revisions accepted** (author rounds 1+2, 02:37:43Z + 02:51:29Z):
- NEW **§2.7 transcript forensics table** — per-death receipts: 14:55 (c8a18353) / 20:04 (0e95f464) / 21:13 (bd9c55be), pytest invocation 2–4 s before each death; $TMUX-in-pane named as an INFERENCE.
- §1 death list annotated per-death: **3 positively attributed / 1 deliberate (19:21, supervise install) / 1 unattributed (14:12 → OSQ-6 carve-out)**; root-cause claim upgraded to "positively attributed ×3" (receipted-3 beats inferred-5).
- §2 header: killtrace = instrument-of-record + named runbook contradiction (amendment = store task `e1ba0239`, post-cascade).
- §2.4 signature-limits bullet (self-SIGTERM signature answers WHAT, not WHO; §2.2 journal ordering did the exclusion; §5.3 is the WHO discriminator).
- AC5 gained the implementation-time killtrace-observation probe (private `-S` throwaway server; FUTURE work, specced not run).

**Documented-not-revised**: Appendix-A Workflow-provenance residual (disclosed, classified documented). **Escalations**: none. **AC impact**: AC5 extended (probe + conditionality); AC6 cross-refs it as its benign probe; other ACs untouched by A.

### §3.B Section B — The fix (§5.1/§5.2, §6-AC1..AC5, §8-OSQ-3/4)

**Findings closed**: 12 (0 F · 5 I · 7 C). Headline inconsistencies: F-Rio-B1 (AC1's fleet-pid probe changes meaning UNDER the guard), F-Rio-B2 (AC2 as drafted was false-green-shaped — the exact class this plan fights), F-Rio-B3 (conftest import foot-gun + nondeterministic coverage), F-María-B1 (AC1 first-pane-run gate not operationalized + vacuous arm when fleet server down).

**Revisions accepted** (rounds 1+2, 02:42:10Z + 02:56:44Z; María's B-findings folded cross-section via C's round 2, 03:07:18Z):
- **§5.2 adopted form**: NEW pure helper module `src/tests/smoke/tmux_isolation.py` (`TMUX_ISOLATION_STRIP_KEYS` + `strip_tmux_context`/`restore_tmux_context`) + THIN session-scoped autouse wrapper fixture extending the EXISTING `src/tests/smoke/conftest.py`, snapshot/pop/restore per `hermetic_config_module_boundary` (`src/tests/conftest.py:46–89`). Docstring rider verbatim: "fixtures arm at first-test SETUP; import/collection-time tmux in smoke modules stays prohibited."
- **§5.1**: helpers strip via the imported single-source constant; ⚠️ anti-reuse blockquote — NEVER fold `TMUX`/`TMUX_PANE` into `MAX_PANE_UNSET_KEYS` (it feeds the production launcher scrub).
- **AC1**: fleet-pid mechanism named — read-only absolute-socket query `tmux -S /tmp/tmux-$(id -u)/default display-message -p '#{pid}'` or pre-capture; pre-capture MUST succeed (fleet live) else fail LOUD.
- **AC2**: regression test lives INSIDE the smoke suite, requests the guard fixture BY NAME, asserts `os.environ["TMUX_TMPDIR"] == str(pinned)` + `$TMUX`/`$TMUX_PANE` absent; guard removal = collection-time loud fail.
- **AC3**: helper module explicitly on the 100% list (both restore branches via monkeypatch); conftest thin wrapper = MEASURED under smoke-suite coverage (truthful; not pragma'd).
- **AC5**: precedence unit test at `src/tests/unit/` beside `test_vertex_env.py`, monkeypatch idiom + probe conditionality (tracer absent → skip-with-named-reason + request install, never silent).

**Documented-not-revised**: F-María-B1's AC1-side echo — enforced via §10-5's normative gate text rather than an AC1 edit (cap-preserving; recorded here per the classification). **Escalations**: none; **OSQ-4 = manager_unilateral_ratify_by_concurrence** (env-strip sufficient, `-S`/`-L` NOT adopted; AC5 is the standing precedence CANARY — reddens and reopens the question if tmux precedence ever changes). **AC impact**: AC1/AC2/AC3/AC5 amended as above; AC4 unchanged.

### §3.C Section C — Instrumentation, sequencing & continuity (§5.3/§5.4, AC6, §7, §9, §10, OSQ-1/2/5)

**Findings closed**: 11 (0 F · 1 I · 10 C). Headline: F-Rio-C1 (OSQ-1 disposition — execve tracer, ordered LAST, eyes-open on the resident-client coverage gap and the -vv fleet-bounce asymmetry).

**Revisions accepted** (rounds 1+2, 02:45:20Z + 03:07:18Z):
- **§5.3**: reframed as a SECOND probe extending the runbook §10 killtrace stack (two-file `.bt` + systemd unit, same append-only log); pre-adoption validation = bpftrace parse-only dry-run (EXECUTOR: AI iff unprivileged parse works — the privilege check is itself a named verify step); argv-match filter marked sketch-with-pass-criterion (per-index comparisons expected).
- **§5.3 Disposition block**: ship execve form, LAST in §10, never instrument-first — **now user-CONFIRMED (Rick, 03:26Z)**. Coverage caveat stated: execve names freshly-exec'd callers only.
- **AC6 three-leg split**: author (.bt + unit + probe) = AI · install/attach = HUMAN (install-only-on-request, runbook ~L141–142/L519) · post-install canary = AI iff `/var/log/tmux-killtrace.log` readable by uid 1001 (644 today, receipted), else HUMAN relays. Canary doubles as §2.1 signature-rule confirmation.
- **§10 step 4** rationale rider: guard lands FIRST — an active guard makes all subsequent test-file iteration fleet-safe.
- **§10 step 5 NORMATIVE GATE** (F-María-B1 fold): AC1's first from-a-pane run ONLY after AC5 + AC2 + AC3-on-`tmux_isolation.py` green AND fleet-pid pre-capture succeeds; failed pre-capture blocks LOUD.
- **§5.4/§10-6**: `src/tmp/README-QUARANTINE-vertex-taint-test.md` = the SINGLE restore authority.
- **OSQ-2 closed** follow-up-per-repo with both per-tree task specs VERBATIM in the plan (§8 OSQ-2 items 1–2); **§10 step 8** = Manager mints the store tasks at cascade close (executed — §5 below).

**Documented-not-revised**: F-María-C1 (README necessary-not-sufficient; audience dead-but-RESUMABLE) — closed by MANAGER COMMITMENT: third minted store item, vertex-lane continuity (§5 below). **Escalations**: none. **AC impact**: AC6 amended (three legs); AC1's gate made normative via §10-5.

**OSQ-5 (ownership) closed NO-COLLISION**: creator session c8a18353 died 9 s after launching the pytest that killed its own host (transcript frozen 14:55:13, never resumed); zero open handles, zero manifest claims, all 11 file-touching transcripts frozen except the cascade cast. **Cleared for §5.1 edit + §5.4 restore.**

## §4 Cross-section ratification summary

**Survived cascade (ratified inputs)**: root-cause mechanism (§2.5 precedence proof) — strengthened; quarantine stopgap (§9) — held as-is; env-strip fix shape (§5.1) — held, upgraded to single-source constant; suite-wide guard (§5.2) — held, upgraded to helper-module + thin wrapper; instrumentation (§5.3) — held, upgraded to runbook-stack extension + shipped-LAST disposition; restore path (§5.4) — held, README promoted to single authority.

**Retired inputs**: blanket "5 deaths, now proven" claim (→ per-death attribution, 3 receipted); "TMUX_TMPDIR beats $TMUX, isolated" file-history note (overturned — the false-green); runbook §10's "kill-server sends no signal" rule (overturned by the 21:13 receipt; amendment task `e1ba0239`); `-S`/`-L` defense-in-depth (NOT adopted — OSQ-4); instrument-first sequencing (rejected — tracer ships LAST); "no positive log exists" premise (false — transcripts held it).

**Cross-section dependencies (implementer-explicit)**:
1. §5.2 guard lands **before** any §5.1 test-file iteration (§10-4 rationale).
2. AC1's first pane-run only after §10-5's normative gate is fully green (AC5 + AC2 + AC3-on-helper + fleet-pid pre-capture).
3. §5.3 tracer work is LAST and CANNOT block §5.1/§5.2 (plan complete if install declined; AC6 conditional).
4. Restore (§5.4) only after §5.1 verified — per the README authority, which also carries the do-not-run warning.
5. The anti-reuse fence: test-isolation strip keys stay in `tmux_isolation.py`; the production `MAX_PANE_UNSET_KEYS`/`SERVER_SCRUB` surface is UNTOUCHED.

**Shared-mutable-state matrix (`os.environ` is the shared surface — touch order + who writes/reads what)**:

| Component | Writes | Reads | When |
|---|---|---|---|
| `tmux_fleet_socket_isolation` (session-scoped, §5.2) | pops `TMUX`/`TMUX_PANE`, sets `TMUX_TMPDIR`→pinned | snapshot at arm | arms at FIRST-test setup; restores byte-for-byte at END of the whole pytest session |
| `hermetic_config_module_boundary` (module-scoped, `src/tests/conftest.py:46–89`) | restores ITS tracked config vars per module | its own snapshot | per-module, nested INSIDE the session guard's lifetime |
| §5.1 helpers (`_tmux`/`_launch`) | pop strip keys on a per-call env COPY | `os.environ` copy | per-invocation; never mutate the process env |
| AC2 regression test | none | `TMUX_TMPDIR`, strip keys | under the guard, by-name fixture request |

Watch-pair + **latent invariant (Rachel light-review G2)**: the TMUX keys must **never join `_HERMETIC_TRACKED_ENV_VARS`** (the module-scoped restore would fight the session-scoped pin — restoring a pre-strip `TMUX` mid-session would re-arm the leak). Key sets are disjoint today (verified at light review); keep them disjoint — this is the same fence-class as the `MAX_PANE_UNSET_KEYS` warning.

## §5 Post-cascade fold bundle

Executed or owed at/after cascade close, outside the plan's implementation steps:

| Item | Owner | State |
|---|---|---|
| Store mint: **g-s-d conftest guard** (extend EXISTING `src/tests/conftest.py`, reuse lupin helper shape; trigger = first tmux-invoking test) — spec verbatim in plan §8-OSQ-2 | Mr. Radio → g-s-d lane | minted at close (correlation_key `cascade-tmux-fleet-killer`) |
| Store mint: **p-i-p testing-doc note** + guard-on-first-tmux-test trigger — spec verbatim in plan §8-OSQ-2 | Mr. Radio → p-i-p lane | minted at close (same correlation_key) |
| Store mint: **vertex-lane continuity** — any session resuming c8a18353 or picking up the orphaned vertex WIP (monitoring_oracle / pilot_ac_instruments / spend_ceiling) reads `src/tmp/README-QUARANTINE-vertex-taint-test.md` + plan §9 FIRST (F-María-C1 rider) | Mr. Radio | minted at close (same correlation_key) |
| Runbook §10 amendment (`e1ba0239`, P1 queued): kill-server DOES appear in the killtrace + consolidation with the shipping tracer (ONE runbook change now OSQ-1 is confirmed) | rides the SWE team | queued |
| OSQ-6 residual population (14:12, "18:32", day-1 deaths) — unattributed by record; the shipped tracer is the forward answer | nobody (carve-out) | documented |
| Post-game retro (`567df730`) + cross-exam round 1 (all three seats answered 03:22–03:23Z) | María 🌸 | DONE (addendum = live edit) |

## §6 Workflow-guidance candidates index

Candidates for `plan-review-cascaded*.md` folding — PIP-side deep redline owns disposition; indexed here so they survive the cascade (empirical anchor in parentheses):

**Manager-seat (Mr. Radio, retro deposit 02:29Z + Step-9 sweep)**
1. **Forward cross-section folds** — a late finding anchored in a cap-consumed section rides the NEXT section's still-open final author turn under the ratified ownership map (A-folds→B, B-folds→C): zero cap burn, zero votes, full audit trail. Pair with Clayton's counterweight #13. (F-María-A1/A2 rode B round-2; F-María-B1/B2 rode C round-2.)
2. **Per-death/per-instance annotation discipline** — never headline an undifferentiated incident count; annotate each instance's attribution class. (F-Rio-A2: 5× → 3+1+1.)
3. **Ownership-map-at-ratification** — decomposition must map EVERY plan element (OSQs, appendices) to a section, not just body sections. (María's catch at ratification.)
4. **Probe-before-declare at the stall threshold; stall clock starts at DELIVERY** — DM-batching latency mimics a wedge; a sender cannot observe delivery. (Clayton "stall" 02:29Z.)
5. **commons_who measures posting recency, NOT liveness** — the spawn-ack persona field is the truthful instrument; worker loud-fail reporting is load-bearing. (María-seat collision → Krishna.)

**Stage-1 seat (Rachel, post-game 03:14Z)**
6. **Persona-3 REUSE rubric: evidence-section variant** — for sections proposing no code: claims-vs-owning-docs, grep the owning doc for the SPECIFIC claim. (Produced the cascade's biggest Stage-1 catch: the runbook §10 contradiction.)
7. **Focus-bar/set_session_topic mandate belongs in the seat SPAWN TEMPLATE** (it reached her three ways, none at Phase A).
8. **Typed `handoff_to_next_stage` field on stage_close** — converged with Rio via cross-exam Q1: ONE handoff schema, two load-bearing legs (producer stage_close state + manager-authored routing incl. carried-items + `reviewed_revision_ts` currency key).

**Stage-2 seat (Rio, post-game 03:14Z + cross-exam)**
9. **"Review as-revised" triggers must carry the revision's landing timestamp** (or fire only after it lands) — mechanical disk-currency check; the save this run was human vigilance. (Stale-read race, Section A.)
10. **Reviewer probe authority stated as a CLASS** ("any read-only instrument"), destructive-verb prohibition the only fence — enumerating safe examples invites under-probing. (His decisive forensics came from un-enumerated instruments.)
11. **Manager's per-section carried-items list = REQUIRED handoff field** (every medium+ Stage-2 finding traces to a carried item or a probe it prompted).
12. **Post-AC self-check**: re-read each verification step ASSUMING THE FIX IS ACTIVE — does the probe still measure what it measured, can it pass with the fix deleted? (F-Rio-B1/B2.)

**Author seat (Clayton, post-game 03:14Z)**
13. **Author append-window**: author's LAST action before posting a revision = cheap DM poll for in-flight findings; manager marks bundles "open for appends until author posts". (In-flight fold worked by timing luck.)
14. **`folds:[...]` metadata key on author_revision posts** — cross-section folds land on one topic while the diff touches another section's text; name every fold.
15. **Cap-survivable-iff-manager-batches** — cap 2 held because only ratified findings were bundled; an unbatched cascade blows the cap on volume.
16. **Post-bundle whole-document consistency grep as a NAMED author step** — sections are a review construct; the document is the edit unit. (Caught stale cross-refs 3×.)

**Stage-3/steward seat (María)**
17. **Parallel-session-safety §3.5 has zero coverage exactly where it matters** — the orphaned vertex lane ran manifest-unregistered; OSQ-5 took ~6 forensic probes that one manifest grep should have answered. (Post 03:11:54Z.)
18. **The steward is not exempt** — manifest-I/O-via-heredoc self-correction during the post-game itself (03:20Z).

**Spawn machinery (Krishna, 02:14Z)**
19. **Seat briefs phrase persona as requested-not-guaranteed** + verify-actual-persona contingency (hard-coded persona collided with live pool state; the brief's own contingency absorbed it).

## §7 Hand-off statement

**Mode**: implementer implements the input plan **as cascade-revised** (no plan revision owed). Per Rick's directive (relayed 03:27Z, verbatim: "let's make sure that Mr Radio is actually running the SWE team workflow for the implementation"), implementation runs the **full SWE-team workflow** (`/spin-up-swe-team`): Implementer + Reviewer + Tester with role charters, implement → adversarial-review → integration-test → green+reviewed gate, post-game at teardown. **The Implementer seat's SOLE brief is this doc + the input plan** (cold-context dogfood — if the seat needs a cascade topic file, this doc failed).

**Gates already cleared**: cascaded review (34/34 findings closed) · OSQ-1 user-confirm (Rick, 03:26Z — execve tracer, LAST) · OSQ-5 no-collision (vertex lane cleared for edit+restore).

**Gates the SWE team carries (from the plan — restated, not new)**:
1. ⚠️ **TMUX SAFETY ABSOLUTE** (§7 hard rule): NO `tmux kill-server` (and no quarantined-test run) from any `$TMUX`-set shell until the fix is in place; quarantined file stays put until §5.4's restore step; destructive tmux verbs on the default socket are PROHIBITED throughout.
2. **§10-5 normative gate**: AC1's first from-a-pane run only after AC5 + AC2 + AC3-on-`tmux_isolation.py` green AND fleet-pid pre-capture succeeds (fail-loud).
3. **AC3 = Lupin-wide 100% coverage gate** (lines + branches + functions); pragma only with same-line reason; per-artifact dispositions as written in AC3/§7.
4. **Venue**: all layers are `:7999`-eligible (§7 table); no `:8000` scheduling implicated.
5. **Merge-held on the working branch after green + reviewed; push stays Rick's** (§10-7).
6. Standing guidance that binds the crew: POST-EDIT `py_compile` floor · TEST OWNERSHIP MANDATE (the human is never the tester) · `.claude-session.md` manifest registration for EVERY touched file (§6 candidate 17 is the cautionary tale) · visible `spawn_sessions` only, never invisible subagents · no destructive git on the shared tree.
7. **Instrumentation legs**: AC6 leg 2 (tracer install) is HUMAN-on-request — the team authors and validates (parse dry-run) but NEVER installs; offer, don't block.

**Completion definition**: plan §10 steps 4–7 done (implement → verify all §7 layers at 100% → restore quarantined test per README authority → merge-held) + runbook amendment `e1ba0239` executed (rides the team, post-OSQ-1-confirm) + AC6 legs 1/3 done with leg-2 offer recorded. Then SWE post-game per the workflow.
