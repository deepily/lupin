# Mux ⇆ Legacy — Adversarial Parity Gap Matrix

**Created**: 2026-07-01 (Sam 🎙️, session `02dbe7b6`) — commissioned by Mr. Radio 🦉 after Rick's charge that the multiplexer is *underdone* and its parity verification is *half-assed* (green proves the mux matches **its own** baselines, not the legacy notifications client).
**Branch / commit under audit**: `wip-v0.1.9-2026.06.21-bug-fix-implementation` @ `2a8defa0` (integrated tip; L1–L4 folded).
**Reference (legacy)**: `static/js/notifications.js` (20,523 ln) + `static/css/notifications.css` (5,845 ln) + `notifications.html` + `lupin-nav.js`.
**Target (mux)**: `static/js/multiplexer/**/*.ts` (~85 files, 22.5k ln) + `static/css/multiplexer/*.css` (3.5k ln) + `multiplexer.html`.
**Method**: code-grounded reconciliation of the three prior discrepancy corpora against **current** wip reality (not lane-diffs-in-isolation), plus a :7999 liveness confirm (health 200). Precise-pixel A/B (Oracle Tier-2/3 vs a fresh legacy golden) is the one tier NOT executed here — see §Verification Integrity, the crux of Rick's charge.

## Headline verdict

Rick's charge is **partially validated, with an important nuance**:

1. **The MVP-scoped work is largely BUILT and functional in current code** — not vaporware. Phase-6 audio, the B1 section reorder + Recent-Activity re-nest, B4 per-message controls **with** a wired active-TTS gate, the Action-Required responder, NAV, PLY, the N-new pill, persona badge, and strip status sub-icons all exist in `2a8defa0`. The earlier "❌ SILENT / not built" verdicts in the 06-30 roadmap are **stale** — those items landed after it.
2. **But Rick is right that "green ≠ parity"**, for three concrete reasons:
   - **(A) Verification integrity** — the visual-regression baselines were *rebaselined against the mux's own container* (`2a8defa0`), so that suite proves **internal consistency only**. The single legacy-referenced check (Layout-Parity Oracle Tier-2/3 geometry vs a legacy golden) was "gated last / not run" and remains Tier-0/1-only. **No current automated artifact proves mux-vs-legacy geometry parity.** This *is* the "half-assed verification."
   - **(B) Residual visual gaps inside claimed-done surfaces** — V2 (no env label / date / live clock), V10-topic (session name/topic never displayed), and V13 (toolbar overlap, unconfirmed) are real and still open.
   - **(C) Scope truncation** — the mux exposes **6 of legacy's 13** accordions. The 7 absent + 3 partial-section regressions are *ratified Phase-5 deferrals*, but they remain genuine parity gaps that an MVP flip would leave open. Rick should re-confirm he accepts flipping at 6/13.

## THE PARITY GAP MATRIX

Legend: ✅ at parity · ⚠️ present-but-degraded · ❌ missing/broken · ⏸ deferred (ratified Phase-5) · ❓ unknown (needs live pixel). Severity: 🔴 flip-blocker · 🟠 visible-degradation · 🟡 minor · ⚪ deferred-by-ruling.

### High-traffic regions (Mr. Radio's priority: sender cards · toolbar · AR/PLY)

| # | Region / feature | Legacy | Mux current (`2a8defa0`) | Status | Evidence | Sev |
|---|---|---|---|---|---|---|
| R1 | **Section order** (top→bottom) | broadcast(+nested Recent-Activity) → focus/strip → sessions | broadcast-mount FIRST → cc-session-strip → notifications-pane | ✅ | `multiplexer.html:92,99,140`; re-nest note `:165` | — |
| R2 | **Recent-Activity nesting** | nested inside broadcast card | re-nested inside `broadcastCard.ts` | ✅ | `multiplexer.html:165-168` | — |
| R3 | **CC-card N-new pill** | green `N new` pill | `.sender-new-count` rendered when `unread_count>0` | ✅ | `senderCard.ts:171-172` | — |
| R4 | **CC-card persona badge** | orange persona-name badge | `.sender-persona-badge` button — **icon only**, name behind popover (no inline name) | ⚠️ | `senderCard.ts:103-105` | 🟡 |
| R5 | **CC-card session topic** | topic shown in card header (updated by `session_topic` msgs) | `.sender-session-name` **always empty** — "SenderRecord carries no session-name field" | ❌ | `senderCard.ts:136-137,158`; legacy `notifications.js:5854-5856` | 🟠 |
| R6 | **CC-card strip status sub-icons** (V9) | avatars carry allocation 🔉 sub-icons | ported | ✅ | `sessionStripIcon.ts` (L3) | — |
| R7 | **Progress-group head** (V11 / Bug#1) | 1 head per group | elected 1 head per group_id | ✅ | `senderCard.ts:205-222` (election) | — |
| R8 | **Reading pane open** (V12 / Bug#2) | 📋 opens pane | URIError fixed (raw read, no decode) | ✅ | `ReadingPaneRenderer.ts` (L1) | — |
| R9 | **Per-message ⏸/⏹ + proxy-ratify** (B4) | rendered on every msg, CSS-gated to `is-playing-current` | corner controls rendered; active gate wired | ✅ (see R10) | `notificationItem.ts:105,161,185`; `NotificationsListRenderer.ts:750-766` | — |
| R10 | **B4 active-id precision** | active bubble = the one actually speaking | gate wired to playback classes; **exact active-id source deferred to F0-d** | ⚠️ | roadmap "mock-verified until F0-d"; class-driver `NotificationsListRenderer.ts:764` | 🟡 |
| R11 | **⚠️ Action Required panel** (V3/AR) | blue full-width accordion + `✓ No pending actions` empty state | colored panel + empty state + full responder (pending→interactive+countdown, responded→read-back, failed→error-stripe) | ✅ (shell+responder) | `ActionRequiredRenderer.ts` (380 ln); `action-required.css` (L2) | — |
| R12 | **AR funnel extras** | active/pending split, count, **kbd-nav (arrows)**, own toolbar toggle | no kbd-nav / no dedicated AR toolbar toggle found | ⏸ | no `keydown/arrow` in `ActionRequiredRenderer.ts`; ruling (d) Phase-5 | ⚪ |
| R13 | **🔊 Playing / TTS panel** (V4/PLY) | green accordion, ⏸/▶, `🔇 Nothing in the queue` | colored panel + empty state | ✅ | `ttsChrome.ts` + `tts-chrome.css` (L2) | — |
| R14 | **TTS-Queue full** (per-item queue, clear-all, focus-resume) | full queue mgmt | transport/chrome only; no per-item queue list | ⏸ | 00-index #4 REMAPPED; ruling (e) Phase-5 | ⚪ |
| R15 | **Section-toolbar (accordion selector)** | 13 section toggles | **6** toggles (Notifications, Jobs, Recent-Activity, TTS, Fleet, Task) | ⚠️/⏸ | `sectionToolbar.ts:36-41` vs `notifications.html:42-60` (13) | 🟠→⚪ |
| R16 | **Toolbar placement** (V13) | integrated floating toolbar | reported overlapping top-left corner | ❓ | needs live pixel — not code-confirmable | 🟡 |

### Header / chrome / page frame

| # | Region | Legacy | Mux current | Status | Evidence | Sev |
|---|---|---|---|---|---|---|
| H1 | **Top nav bar** (V1/NAV) | dark `Lupin·Home·Notifications·Profile·email·Logout` | NavBarRenderer mounts `#lupin-nav-mount` | ✅ | `navBar.ts`,`NavBarRenderer.ts`,`multiplexer.html:55` (L4) | — |
| H2 | **Env title + live clock** (V2) | `[DEVELOPMENT]: Notifications 2026-06-30 @ 16:31 EDT` | hardcoded `"🔔 Notifications"` — no env, no date, no clock | ❌ | `NotificationsHeaderRenderer.ts:95` | 🟠 |
| H3 | **Notifications header spacing** (V5) | spaced count·badge·TTS pill·`Today▾`·`Clear All` | restyled via `notifications-header.css` (141 ln, L3) | ✅ | `notifications-header.css` (L3) | — |
| H4 | **TTS-fraction pill placement** (V6) | inline pill in header row | ported into header | ✅ | L3 VIS | — |
| H5 | **Broadcast card ▼ inline** (V7) | ▼ inline-right of heading | CSS fix (L3) | ✅ | `broadcast.css` (L3) | — |

### Whole absent accordions (ratified Phase-5 — real gaps the MVP flip leaves open)

| # | Legacy section | Mux | Status | Sev |
|---|---|---|---|---|
| A1 | Q&A Interface (`section-qa`) | none | ⏸ absent | ⚪ |
| A2 | Submit Agentic Jobs (`section-job-submit`) | none (jobs-pane can't submit) | ⏸ absent (highest build) | ⚪ |
| A3 | Job Queues mutations (delete/retry/time-window/pagination) | display-only `jobs-pane` | ⏸ partial | ⚪ |
| A4 | Time Saved (`section-time-saved`) | none | ⏸ absent | ⚪ |
| A5 | Filter Settings (`filter-settings-section`) | none | ⏸ absent | ⚪ |
| A6 | System Status (`section-status`) | none | ⏸ absent (high) | ⚪ |
| A7 | Direct TTS Test (`section-direct-tts`) | none | ⏸ absent (likely intentional) | ⚪ |
| A8 | Debug Info (`section-debug`) | none | ⏸ absent (likely intentional) | ⚪ |

Fleet Status (#6) + Task List (#7) = ✅ faithful/superset ports (prior `93-` closure).

## §Verification Integrity — the crux of Rick's charge

- **Visual-regression snapshots are tautological w.r.t. legacy.** Commit `2a8defa0` = "Rebaseline visual baselines to DejaVu-fixed :8000 container." The goldens are captured **from the mux itself**. A green visual suite therefore proves *the mux still looks like the mux* — it can NOT catch mux-vs-legacy drift. This is precisely the "internal consistency, not parity" Rick named.
- **The one legacy-referenced oracle was not run to green for this state.** The WS3 Layout-Parity Oracle (`src/tests/e2e_ui/parity_oracle.py`) has Tier-0/1 green on :7999 (`024ca2db`) but Tier-2/3 (computed-style + ±1px geometry vs a **fresh legacy golden**) was "gated last / not run" (roadmap 06-30) and remains open (memory: WS3 Tiers 0/1 + golden green, Tier 2/3 remaining). **So there is currently NO automated proof of mux-vs-legacy geometry parity.**
- **The green gate I ran earlier (c8 100/100/100, 2051 tests, tsc, dist) is unit/type/build integrity — it says nothing about legacy visual parity.** Correctly scoped, it was never a parity check.

## Recommendations (for Mr. Radio's adversarial review)

1. **Run the Oracle Tier-2/3 vs a FRESH legacy golden** (`?classic=1`) on a monopolized :8000 (Mr. Radio's to schedule) — this is the missing legacy-referenced check and the direct rebuttal-or-confirmation of Rick's charge. Do NOT trust the mux-self-rebaselined snapshots as the parity oracle.
2. **Close the confirmed residual gaps** before flip sign-off: R5 (session topic display), H2 (env label + live clock), R4 (inline persona name), and live-confirm R16 (toolbar overlap).
3. **Re-ratify the 6/13 flip boundary with Rick explicitly** — the 7 absent + 3 partial sections (A1–A8, R12, R14) are ratified Phase-5, but "MVP flip leaves 7 legacy accordions unreachable" is a decision Rick should re-affirm with eyes open, not inherit silently.
4. **Retire the self-referential visual baseline as a *parity* signal** — keep it as a regression guard, but the flip gate must be the legacy-referenced Oracle + Rick's subjective Chrome A/B, not the tautological snapshots.

## ADDENDUM — Real-DOM Oracle verdict (2026-07-01, :7999, legacy-golden diff)

Ran the component-isolation Layout-Parity Oracle against the **existing legacy golden** (`fixtures/golden/notifications-legacy.golden.json`) on :7999 — the non-tautological, legacy-referenced check (distinct from the mux-self-rebaselined visual snapshots). Command: `LUPIN_TEST_BASE_URL=http://localhost:7999 pytest src/tests/parity_oracle/{test_tier0,test_tier1,test_tier2_tier3,test_golden_conformance}.py`.

**Result: 10/10 PASSED** — Tier 0 (CSS source identity), Tier 1 (DOM skeleton ×3), Tier 2 (computed-style isomorphism), Tier 3 (±1px geometry), golden-conformance ×4 incl. `test_golden_shared_sheet_hash_is_current`. Golden is **FRESH** (shared-sheet `notifications-surface.css` hashes `f1f04a9ee2f7` = golden). So for the **sender-card region**, the mux is at **PROVEN real-DOM parity** (structure + declarative style + absolute intra-card geometry) with legacy. That region is NOT half-assed.

### But the golden has two load-bearing coverage holes (findings, not passes)

- **The oracle's contract-node set excludes the persona badge.** Golden `style_geom` keys are card / header / acc-header / date-text / messages / msg[n] / time / text — there is **NO persona-badge node**. So Tier-2/3 never compared the badge. **R4 is a CONFIRMED real gap that the oracle greenlit**: legacy `notifications.js:10818-10823` renders `.persona-badge-icon` **+ `.persona-badge-name`** (icon AND name); mux `senderCard.ts:105` renders `${persona.icon}` **icon-only**. A real legacy divergence passes the parity oracle — direct evidence the component-isolation golden is under-scoped.
- **The fixture is narrow and text-blind.** `notifications-parity-scenario.json` has **no `session_topic`/session-name** (→ R5 golden-blind) and **no `progress_group_id`** (→ V11 / the 176× progress-group head is NEVER exercised — Tier-1 pass cannot prove it closed; only the L1 unit fixture does). Text content is Category-4 noise (excluded), so content divergences are invisible.

**Net:** the sender-card GEOMETRY/STYLE is genuinely at parity for the canonical fixture, but "parity as verified" ≠ content parity ≠ real-data parity ≠ page-chrome parity. Rick's distrust is well-placed at the SEAMS the oracle doesn't cover.

### Recast verdict matrix — CLOSED / PARTIAL / OPEN

| Row | Region | Verdict | Basis |
|---|---|---|---|
| §sender-card structure+style+geometry | R1/R3/R6/R7 substrate | ✅ CLOSED (PROVEN) | Oracle Tier 0-3 + golden-conformance 10/10 :7999 |
| V8 | Recent-Activity re-nest | ✅ CLOSED | code `multiplexer.html:165` |
| B5 | CSS single-source | ✅ CLOSED | Tier 0 CSS-source-identity PASS; shared sheet fresh |
| Phase-6 | audio scheduler | ✅ CLOSED (audibility = separate protocol-E2E) | `AudioStore` scheduler + P6 unit tests |
| V1/NAV | top nav bar | ✅ CLOSED (code+static-DOM) — needs full-page Oracle | `NavBarRenderer`, mount `:55` |
| V5/V6/V7/V9 | header spacing · TTS pill · broadcast ▼ · strip sub-icons | ✅ CLOSED (code) — needs full-page pixel | L3 CSS/template diffs |
| V3/AR · V4/PLY | Action-Required · Playing panels | 🟡 CLOSED-pending-independent-review (Sam authored L2) | `ActionRequiredRenderer` (380 ln) + `ttsChrome` + L2 CSS |
| V10 | CC-card header chrome | ⚠️ PARTIAL | box geom parity (oracle) BUT content gaps R4+R5 below |
| R4 | persona **name** inline | ⏸ INTENTIONAL DIVERGENCE (not a bug) — Rick V10 ruling PENDING | legacy `notifications.js:10823` icon+name; mux glyph-only BY DESIGN per F-Arnold-3 (`senderCard.ts:88-95`, 2026-05-19) — name relocated to the persona popover. Design-divergence bucket (with dark-theme / AR-PLY palette / 🔔): "match legacy exactly vs keep mux's intentional choice." Rec (b) restore-additively IF Rick wants strict parity; do NOT override the ratified decision without his word. |
| R5 | session **topic** display | ✅ CLOSED (client-side, commit-held `16d0952c`) | session_topic intercept → SenderStore setter + pre-card buffer + localStorage mirror → `.sender-session-name` render; 100% unit tests. Oracle N/A by design — R5 is TEXT CONTENT (Category-4 noise the oracle excludes); proof is unit tests, not the golden (Mr. Radio ruling (a), 2026-07-01). R5b click-to-rename deferred. |
| H2/V2 | env label + live clock | ❌ OPEN | mux hardcoded `"🔔 Notifications"` `NotificationsHeaderRenderer.ts:95`; legacy `notifications.js:811-812,2793` env_label + `#clock` |
| V11 | progress-group head (176×) | ✅ CLOSED in code/unit — ⚠️ NOT golden-exercised (fixture has no progress group) | L1 election + `templates_sender_card.test.ts` |
| V13 | section-toolbar overlap | ❓ OPEN/UNKNOWN — needs full-page pixel | not code-confirmable |
| R15 | 6 of 13 accordions | ⏸ DEFERRED (ratified Phase-5) | `sectionToolbar.ts:36-41` |

## Raw-honesty summary (Rick reads this)

The crew did real work — most MVP surfaces are genuinely built and function. But the *claim of parity* is not yet earned, because the check that would prove it (mux-vs-legacy geometry) was never run for this state, and the visual snapshots were quietly rebaselined against the mux itself. Add to that four still-open degradations inside "done" surfaces (topic, env/clock, persona-name, toolbar overlap) and a 6-of-13 accordion surface, and the honest status is: **MVP functionally close, parity UNPROVEN, four visible polish gaps open, 7 accordions consciously deferred.** Not done. Not half-built either — under-verified and under-finished at the edges.
