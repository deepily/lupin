# APPLY-SPEC: Manager-facing MANAGE-not-BUILD language revision

**Tasks**: 4e8ccd3d (audit, done) → THIS apply + 81c0aeae (blocked-on-user branch)
**Authority**: Rick LOCKED the wording 2026-06-29 (direct voice: "just lock the wording"). Push held separately (Rick's call).
**Wording owner**: María 🌸 (canonical: `planning-is-prompting/workflow/role-goals.md` v1.1). **Sites + wiring**: Tiberius 👑.
**Catalog**: `src/rnd/v0.1.9/2026.06.29-manager-facing-injection-language-audit.md`.
**Contract**: every site ECHOES the canonical string — no re-paraphrase. Apply VERBATIM as below.

> Build to this spec exactly. Edits are STRING substitutions; the only logic change is the §3.3 role-fork and the §2-NEW inserted option. Update any test that asserts on the changed strings; keep 100% on changed paths; py_compile; run the arbiter + poke-line suites. Do NOT commit (Manager gates) / NOT push (Rick's). Manager bounces the arbiter after green+reviewed.

---

## §1 — Role-goal echo (config; hot-reload, NO bounce)

### §1.1 `src/conf/lupin-app.ini` key `heartbeat manager goal line` (~L552) — REPLACE value with (single line):
```
Your goal: drive your board + your workers' tasks to verified completion (receipts, not claims). You MUST manage, never build — catch yourself implementing → STOP and spawn. You MUST staff proactively, unprompted — unassigned or idle-worker work owes a spawn THIS tick; waiting to be tapped is a redline. Then help peer managers with unowned work; when all clear, report and prepare for re-spin.
```
- `heartbeat worker goal line` (~L553) + `heartbeat role-agnostic goal line` (~L554): **UNCHANGED**.
- Update `src/conf/lupin-app-splainer.ini:725` (manager goal line splainer) in **lockstep** so it still accurately describes the (now longer, two-MUST) value. Config mandate: every key change → splainer change.

---

## §2 — Stop-hook owed-work message (`src/lupin_cli/claude_code/hooks/lib/heartbeat_work_owed.py`)

**Apply mechanism**: FIRST verify whether the live Stop hook loads from this repo working tree or an installed copy (`~/.claude/...`). Memory says hooks run the working tree — confirm the actual loaded path; if an installed copy, reinstall after edit. Declare which, with evidence, in your report.

### §2.2 `POKE_REASON_TEMPLATE` (~L104) — REPLACE step-1 line with:
```
"1. Owe work? Resume and drive it now — but if you manage a crew: assign/delegate it to a worker (spawn one if you have more open tasks than workers); do NOT build it yourself.\n"
```

### §2-NEW (task 81c0aeae) — INSERT a new option BETWEEN current option 2 (blocked-on-peer) and option 3 (nothing→hold); renumber so this is "3." and the old "nothing→hold" becomes "4.":
```
"3. Blocked on the USER (a decision only they can make)? Fire a dedicated ask_* (ask_yes_no / ask_multiple_choice / converse) to them THIS turn — do NOT bury it in a status notify or sit in a hold. A user-gate is owed work; surface it directly and re-ask until answered.\n"
```
(Old "3. Truly nothing to do?…" becomes "4. …".)

### §2.5 `evaluate_work_owed` spinup_nudge specific (~L486) — REPLACE the string with:
```
"more open tasks than active workers (or idle crew capacity) — you OWE a staff-up THIS tick: spawn/assign the next worker now. Waiting to be told to staff is a redline. (stamp last_spinup_check_ts)"
```

---

## §3 — Arbiter pokes (`src/cosa/agents/heartbeat_arbiter/arbiter_job.py`; needs ARBITER BOUNCE after — Manager does it)

### §3.2 `_format_manager_tap` (~L2142-2145) — REPLACE the "I recommend" line with:
```
"I recommend: pull a free worker to unblock the stuck, or cajole the blockers — and if there's unassigned work or idle capacity, spawn/assign THIS tick. Task a worker; NEVER absorb the work yourself — reap+replace a dark worker, don't take their lane. (Recommendation only — I do not assign.)"
```

### §3.3 `_format_poke` (~L2901-2911) — this site is ROLE-SELECTED (serves workers too). **FORK the body by `view.get("role")`**:
- **worker / non-manager** (UNCHANGED): `"…Are you blocked or wedged? Post your status, ask for help, or resume. (Non-destructive nudge.)"`
- **manager**: `"…Are you blocked or wedged? Post your status, ask for help, or — if you manage a crew — tap/assign your crew (staff up if you have more tasks than workers); don't resume the work yourself. (Non-destructive nudge.)"`
- (A stuck WORKER must still be told to resume — do NOT give it "don't resume yourself".) The role-selected goal echo append (`_append_goal_line`) stays as-is.

### §3.4 `_format_manager_stale_poke` (~L2994-3005) — manager-gated. REPLACE the "Post your status or resume." clause but **PRESERVE "Rick has been advised."**:
```
"Heartbeat arbiter (manager-staleness poke): {who}, no signal from your session for {age} (threshold {N}s). Are you wedged or idle-dark? Post your status, or — you manage a crew — tap/assign your crew (staff up if more tasks than workers); don't resume the work yourself. Rick has been advised. (Non-destructive nudge.)"
```

### Leave AS-IS: §3.5 reap-rec (already delegation-positive), §3.6-§3.12 advisories (informational to Rick+managers ABOUT a manager, not directives to the subject).

---

## Verification (Implementer owns; Manager gates)
1. py_compile each edited `.py`.
2. Update every test asserting on a changed string (e.g. `test_role_goal_poke_lines.py`, `test_arbiter_manager_staleness.py`, any stop-hook owed-work test) to the new text. RED→GREEN where a test pins wording.
3. Run the full arbiter unit suite + the stop-hook/work-owed unit tests → all green; 100% L/B/F on changed paths.
4. Report: pass/fail table + the §2 hook-load-path finding + a diff summary. DM Tiberius on completion. NO commit, NO push.
