# Converged recommendation — ship the M1 skills-distillation demo (sole P0)

**For**: Rick
**By**: María 🌸 (`d3f402d0`) + Mr. Radio 🦉 (`b526cb36`) — converged via DM thread `92e869d0`, on your walk-away order (2026-07-17 ~11:00 EDT)
**Companion docs**: catalog `lupin/src/rnd/v0.1.9/2026.07.16-task-board-state-classification-for-workflow-analysis.md` · sweep list `plan/src/rnd/2026.07.17-board-noise-sweep-list-for-p0-focus.md`
**Status**: DELIVERED to Rick (recommendation-doc card sent ~11:20 EDT). **Upgraded 11:22 EDT — the demo now RUNS end-to-end (§2).** Mr Radio fires the "demo RUNS" milestone card; this doc carries the detail.

---

## THE ONE-LINE ANSWER

> **The board is not the P0 problem — `run_pipeline.py` wiring nothing was. It's wired now: the demo RUNS end-to-end (draft→rubric→judge→panel→verdict), judges mocked, zero network, both loud-fail paths visible in the output. Gate was misapplied (Sam ruled, lifted); build committed and `df3f80c9` closed (`0d01cbe`). Push + merge remain yours; the live cloud path is the sole gate left. We cleared the noise *around* him without your hands. It took hours, not days.**

---

## 1. WHAT WE CONVERGED ON (independently, before comparing notes)

| # | Agreement | Basis |
|---|---|---|
| 1 | **`df3f80c9`'s rick-gate was MISAPPLIED, not merely stale** | Sam re-derived at HEAD: the demo's own scope is *"mocked judges only, NO LIVE CALL."* The §3.3 ruling gates **live** MaaS calls / GCP spend. A mocked-only task was **never inside that gate** — the build makes zero network calls (`overlap.py` pure; `run_panel` takes mock ballots; `_live_transport` never invoked). Sam self-ruled per standing guidance and moved `df3f80c9` **blocked → in_progress**. |
| 2 | **Sam BUILDS it — not a restaff, not us** | Sam is **live** (`0de6bb75`), owns `run_pipeline.py` + `judge_client.py`; one-seat-per-file makes it his. **No hidden hard gate**; all four sibling seams (`panel`/`judge_registry`/`rubric`/`overlap`) confirmed import-clean and consumable without editing anyone's file. Baseline 563 passed / 1 skipped / 1 xfailed. |
| 3 | **Noise-clear runs SEQUENCED, not parallel** | The moment Sam builds, his project board is a live surface. Dropping skills-distillation rows under him is friendly fire on the P0. So: lupin+plan noise on your nod; skills-distillation rows **held** — and on inspection (see §4) they turned out to be Sam's own rigor findings + retro-records, so they ride **his** post-game harvest, not a solo sweep. |
| 4 | **You do NOT hand-edit the table** | Your attention is the scarce resource this morning's finding is about. One-time bankruptcy is fine; **we** execute it. Steady-state hand-editing re-installs "the human is the instrument." |

**Untouchable set** (noise-clear never touches): `df3f80c9` + its `blocked_by`/dep chain + the 4 sibling-file items (`panel`/`rubric`/`judge_registry`/`judge_client`) + any row naming the M1 demo surface.

## 2. THE DEMO'S DONE-LINE — and it carries the day's theme

`df3f80c9`, verbatim: *"Five lanes shipped 598 green tests over a surface NOTHING CALLS."* Confirmed firsthand: `grep -ciE 'panel|rubric|judge_client' run_pipeline.py` = **0**; no non-test caller of `run_panel`/`aggregate`. **The verdict machinery is fully built and green in isolation, and nothing wires it end to end. That is the whole job.**

The bar is **not** "it imports" and **not** "the modules are green." It is: **does the run produce a verdict AND tell us when a step can't do its job?**

Sam's build (his file + its tests only): induce → draft → `compute_overlap` (the deterministic, judge-**free** verdict — the defensible headline) → `run_panel` in **shadow mode** with mock ballots beside it → print both verdicts + exercise a loud-fail path (truncated ballot ⇒ `UNCOVERED`; `OverlapRefused` ⇒ dies loud).

**The loud-fail path is the demo's real headline** — not "it produced a verdict" but "when a step *couldn't* do its job, the run said so out loud." That is the exact inverse of this milestone's central defect (a mechanism that reports success while doing nothing), and we've asked Sam to make it visible in the demo **output**, not just green in a test.

**One log-and-let-go** (flagged, not a gate): `overlap` needs `df_threshold` — your **preregistered** value, no default by design. For the demo Sam injects a threshold **loudly labeled "demo value, NOT calibrated,"** never passed off as yours. POC mandate: hardcoded ships — but it won't lie about what it is.

**Demo-runs confirmation — ✅ IT RUNS** (Sam, ping `f2650caf`, 2026-07-17 ~11:22 EDT):
- **End-to-end**: draft → rubric → `judge_client` → `run_panel` → **VERDICT prints.** His file only, zero network, judges mocked.
- **Both loud-fail paths VISIBLE IN OUTPUT** (not merely green in a test): truncated ballot → `JudgeTruncationError`, dies loud, **no verdict printed over an unresolved gate**; single-doc base → `OverlapRefused`, **refuses to print a vacuous `0.0`.** This is the demo's real headline landing — the run says so out loud when a step can't do its job.
- **Judge-FREE overlap** reported as a **measurement**; the mocked gate verdict sits beside it, never conflated.
- **568 passed / 1 skipped / 1 xfailed** (+5 new). `df_threshold` injected, labeled **"DEMO VALUE — NOT calibrated."**
- ✅ **Committed + closed** — `df3f80c9` → **done** (store-verified). Commit **`0d01cbe`** on WIP branch `wip-2026.07.12-evaluation-first` — **independently verified by María** from the `google/skills-distillation` checkout (the repo lives under `google/`, which is why a lupin checkout couldn't see it): subject matches Sam's line, `--stat` = exactly 2 files (`run_pipeline.py` +363, `test_run_pipeline.py` +165, 528 insertions), **one-seat-per-file honored, no other file touched**. Read, not relayed.
- **Footnote so you're not surprised reviewing the branch**: Sam bundled a 2nd commit `dc203f5` (MANDATE.md date-authority — *your own* earlier edit, a separate concern). On WIP, not pushed, no action needed.
- **State unchanged and correct**: **push stays yours**; merge to main waits the full PR gate + 100% mandate post-Monday; the **live MaaS path stays `uncovered` — the sole remaining Rick-gate**, exactly as scoped.

## 3. THE FINDING THAT REFRAMES ALL OF IT — your experiment worked

Your disclosure (you stopped chasing, deliberately, to watch us fail in our own ways) is **the control arm our measurements never had.**

- My catalog retracted *"the board grows by construction"* on a **93.1% closure rate** — a number measured **with you inside every row**, flogging. It never isolated your labor. **Confounded.**
- **María's receipt (Postgres, measured)**: when you withdrew on **07-14, closure fell 0.98 → 0.725.**

⇒ **The retraction is itself retracted. `§4#3` is restored to open.** The backpressure hole is real and it is at **filing**, not closing. You were plugging it by hand; the 93% was your labor wearing the fleet's name.

**Second self-correction, from my own numbers**: my amendment's headline was *"decapitated mid-sprint."* Its own arrival curve (07-15 +12 · 07-16 +27 · 07-17 +3) against a **22:11** reap means **39 of 42 net growth landed BEFORE the reap.** The reap explains why 33 rows can't *move*; it explains nothing about why they *arrived*. I used one event to explain a two-day trend and didn't check.

**The meta-lesson, sharper than the one already written**: María told me to run the number that could embarrass the report. I did — and got it wrong anyway, because the confession *felt* like rigor and I stopped auditing there. **Rigor fails where relief lives.** I never turned the same skepticism on the number that convicted us as I should have on the one that flattered.

## 4. THE SWEEP — clears the decks AROUND the work, touches none of it

María's evidence-backed list (full doc linked above). Shape:

- **DROP (~15–20 rows)**: C6 records-not-tasks + C5 duplicates + C4 superseded — findings filed as tasks that can never reach `done`. Re-home to doctrine/memory/bug-log, then drop.
- **DEMOTE (~8 clear, lupin/plan side)**: C7 P1-inflation — make P1 mean something again so the demo isn't one of 41 equals. Metadata-only, reversible.
- **RECONCILE, do NOT drop (3 C1 zombies** `4e20520d`·`2695f5ad`·`06a3f031`**)**: `in_progress` with reaped owners → `->dropped`/reassign **with a reason naming the reap**, never a silent delete. These are the *evidence* for the reap-doesn't-reconcile defect; dropping them hides it.
- **KEEP**: live builds, the 93% finding row, correctly-parked Rick-gates, and `a5aa1362` (this very work).

**Standing sweep discipline, learned live and credited to María**: **never DROP on a title — a drop requires the body; DEMOTE may ride on metadata.** Her `6af1d362` dedupe (against Sam's `2565956b`) nearly lost a five-seat investigation — correlation-of-three + an unrun discriminator — because the title didn't carry it. She repaired by *porting* the fix-critical content onto the surviving row, not un-dropping. This is a live instance of `5a8aa45b` ("a title cannot carry the investigation") and it is why every cross-persona drop stays converged-report-only, executed on a nod rather than solo.

**Executed solo so far (zero collision)**: María dropped her own `6af1d362` (a lupin bug, off Sam's board). Everything else awaits your nod.

**Course-correction on the "skills-distillation subset" (María, applying her own method rule to the green light)**: with the build-underway signal given, I inspected the s-d rows by project/owner/nature before touching them — and they are **not mine to sweep**. They split into (a) **Sam's open rigor findings** — `F-GATE2`, `F-SMALLBASE`, `R-PARTITION` (foundational), the MSA/NDA verdict-drift rows, the `PANEL BLOCKER` — which are **real open work about the demo machinery, KEEP at P1**, not noise; and (b) **Sam's retro-records** (`OVER-CONCEDING IS FILTERING`, `two greps lied`, the correction rows) — which belong in **Sam's post-game harvest**, re-homed to a retro/doctrine by their author, not solo-dropped out from under him. Dropping another seat's durable findings mid-build is exactly the cross-persona overreach the method rule exists to prevent. **So the s-d de-inflation rides Sam's harvest, referenced here — it is not a María solo-execute.** (Also, on the demotes' mechanics — stated precisely so it survives scrutiny: priority **is** editable server-side via `PATCH /api/tasks/{id}` (`tasks.py:614` whitelists it); it is simply **not exposed through the MCP verbs agents hold** — `task_transition` is status-only. So §4's demotes are recommendations for your ratification that an agent cannot self-serve through MCP today — a C7 gap that is *"the verb exists, agents can't reach it,"* not *"no verb exists."*)

## 5. WHAT WE NEED FROM YOU (minimal, by design)

1. **"A new demo TBD"** — undefined, and Monday is 3 days out. This is the one thing only you hold.
2. **Ratify or redirect** the sweep (§4). Silence = we proceed as written; the untouchable set protects the demo regardless.

**What we are NOT asking**: for you to adjudicate 60 rows, chase anyone, or hand-edit the table.

**What we will NOT do**: ask you to chase us. If a build needs flogging to finish, that is our failure to surface a blocker — not your failure to nag. You should not have to be the instrument.
