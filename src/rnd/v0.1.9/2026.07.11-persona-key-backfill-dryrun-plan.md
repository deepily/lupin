# Persona-Key Backfill — Dry-Run Plan (RECEIPT / reversibility record)

**Captured**: 2026-07-11T18:36:19Z · **By**: Rachel 🕊️ · **Bug**: 951a22be
**Command**: `python src/cosa/rest/persona_key_backfill.py` (DRY-RUN, no `--apply`, read-only)
**Normalizer**: the FIXED `canonical_persona_key` (worktree `lane-rachel-persona-key-normalizer`) — plans are only correct with the fix in place; from the unfixed main tree these would MIS-heal (`mr_radio`→`mrradio`).
**Scan**: 662 rows → **13 stragglers**, 0 applied.

This artifact preserves the BEFORE value of every planned row so a future
manager-run `--apply` is receipt-backed and reversible row-by-row (restore =
`UPDATE ... SET <column> = <before> WHERE id = <row>`).

## Full plan (all 13 rows)

| # | column | row id | BEFORE | AFTER | sub-class | pass-1 (Mr. Radio ruling 2026-07-11) |
|---|---|---|---|---|---|---|
| 1 | owner_persona | 09d0b7c2-4cb7-4b9c-a169-7afc462b523b | `swe-implementer` | `swe implementer` | role-slug | **EXCLUDED** (verify first) |
| 2 | owner_persona | a97b616a-5be2-4e6d-ace6-59f4e51e1f10 | `swe-implementer` | `swe implementer` | role-slug | **EXCLUDED** |
| 3 | owner_persona | 81a02abe-1070-4da2-a746-3381434c4ddb | `swe-tester` | `swe tester` | role-slug | **EXCLUDED** |
| 4 | owner_persona | 82e4eaf0-7968-47f8-8720-d67f0baeb9e2 | `mr_radio` | `mr radio` | persona (separator) | heal |
| 5 | accountable_manager | 82e4eaf0-7968-47f8-8720-d67f0baeb9e2 | `mr_radio` | `mr radio` | persona (separator) | heal |
| 6 | owner_persona | 0b63f906-df49-4785-b6ba-ed80864c48cd | `swe-reviewer` | `swe reviewer` | role-slug | **EXCLUDED** |
| 7 | owner_persona | c38e3134-2d66-469c-8360-b8fc694d5e2b | `mr_radio` | `mr radio` | persona (separator) | heal |
| 8 | accountable_manager | c38e3134-2d66-469c-8360-b8fc694d5e2b | `mr_radio` | `mr radio` | persona (separator) | heal |
| 9 | owner_persona | bf6a93a5-24f8-4ec1-8c4c-009509f56975 | `Rachel` | `rachel` | persona (case) | heal |
| 10 | accountable_manager | bf6a93a5-24f8-4ec1-8c4c-009509f56975 | `Mr Radio` | `mr radio` | persona (case) | heal |
| 11 | owner_persona | e759b0b9-f006-49de-a143-1a71188141d8 | `Krishna` | `krishna` | persona (case) | heal |
| 12 | accountable_manager | e759b0b9-f006-49de-a143-1a71188141d8 | `Tiberius` | `tiberius` | persona (case) | heal |
| 13 | accountable_manager | 487dc704-9500-4243-bd3e-b77925158b2d | `mr-radio` | `mr radio` | persona (separator) | heal |

**Pass-1 scope** (manager-run, post-merge): rows 4,5,7,8,9,10,11,12,13 (the 9
clearly-persona separator/case stragglers). Rows 1,2,3,6 (`swe-*`) EXCLUDED
pending the persona-vs-role-slug verification below.

## swe-* persona-vs-role-slug verification (Mr. Radio ruling #3)

**Verdict: NOT load-bearing outside the canonical seam — safe to heal, but
low-value; correctly deferred.**

- **No writer**: `grep` across `src/**/*.{py,js,ts,json,ini,yml,yaml}` (excl.
  `.venv`) finds ZERO code that writes `swe-implementer`/`swe-tester`/
  `swe-reviewer`. Current spawn roles are `reviewer`/`author`/`observer`/
  `default` (`session_spawner.py:1083`), NOT `swe-*`. The hyphenated forms are
  legacy role-slug DATA in `owner_persona`, not a current role name.
- **No literal reader**: the only `owner_persona ==` comparisons are the query
  filters at `task_repository.py:451,504` — and those compare a value the router
  ALREADY canonicalized via `_canon_persona` (`tasks.py:725-726`). i.e. every
  comparison sits BEHIND the canonical seam; nothing compares the raw hyphenated
  literal. Other `== "swe"` hits (`seed_proxy_decisions.py`, `swe_team/*`) are
  `ProxyDecision.domain` / job-type / session-id — unrelated to `owner_persona`.
- **Conclusion**: healing `swe-*` would break nothing, but they are role-slugs
  (not voice-pool personas) so healing is cosmetic. Keep as a follow-on item.

## Fixed-point caveat (unhealable by backfill)

A truly-fused value with no separator (e.g. María's original `mrradio`) satisfies
`canonical("mrradio") == "mrradio"`, so it is INVISIBLE to this scan and cannot be
auto-repaired — it needs a MANUAL targeted `UPDATE`. None appear in the 13 above
(consistent with being undetectable). If such a straggler is later found, repair
it by hand: `UPDATE task_items SET owner_persona = 'mr radio' WHERE owner_persona = 'mrradio' AND ...`.
