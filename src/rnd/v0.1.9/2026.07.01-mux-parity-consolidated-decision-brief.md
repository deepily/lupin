# Multiplexer ↔ Legacy Parity — Consolidated Decision Brief

**For:** Rick · **From:** Mr. Radio 🦉 (session 2b34ae38) · **Date:** 2026-07-01
**Status:** Fix queue COMPLETE; awaiting final gate + coordinated batch merge (held for your push).
**Branch base:** wip `90df7d68` · **Nothing pushed. No switchover flip. Nothing accepted on a builder's word.**

---

## TL;DR

You said the mux was underdone and the crew was half-assing parity verification. **You were right about the verification** — and it's now fixed. The mux itself turned out **more built** than the stale 2026-06-30 roadmap claimed, but its "green" only proved *the mux matches itself*, never the legacy client. We rebuilt honest, legacy-referenced verification, closed the real gaps, and everything that remains resolves to **two decisions that are yours** (not bugs).

---

## What "half-assed" actually was — and how it's fixed

- **The tautology:** the visual snapshots were rebaselined *against the mux itself* (commit `2a8defa0`), so they only proved "the mux looks like the mux." No current check compared mux → legacy.
- **Fix 1 — sender-card oracle:** ran `src/tests/parity_oracle/` Tier 0–3 (computed-style isomorphism + ±1px geometry) vs a **fresh legacy golden**, freshness-guard GREEN. Non-tautological. **10/10, two independent reads (Sam + Rachel).**
- **Fix 2 — full-page chrome oracle (was entirely missing):** Rio built `src/tests/parity_oracle_fullpage/` + a separate full-page legacy golden — 33 tests green, additive, zero production change. Chrome parity is now machine-checkable.

## Proven at real legacy parity (non-tautological)

| Surface | Proof |
|---|---|
| Sender-card interior | Oracle Tier 0–3 vs fresh legacy golden (Sam + Rachel independent) |
| Nav (V1) | Full-page oracle Cat A style+geom isomorphism |
| Broadcast (V7) | Oracle geometry + byte-faithful CSS port |
| Action-Required geometry (V3) | Full-width geometry proven (style = Decision 2) |
| Bug#1 progress-group head, Bug#2 reading-pane | Closed + covered |

## Closed + independently verified (real gaps fixed)

- **H2** — env-label + live clock (was hardcoded "🔔 Notifications"). Commit `a81b2114`. Rachel ACCEPT (faithful port, envelope fix, non-tautological tests). Full-page proof lands when the oracle's V2 sentinel flips at merge.
- **R5** — session-topic display (name never showed; `.sender-session-name` always empty). Commit `16d0952c`, client-side, zero server change, 100% unit-covered. At Rachel's final gate now. (Content feature → proven by unit tests + legacy-faithful seam, not the oracle, which excludes text content by design.)

---

## DECISION 1 — Parity scope

How wide does the parity effort go?

1. **Finish + prove the 6 built accordions** *(recommended)* — the built set (Notifications, Jobs, Recent-Activity, TTS, Fleet, Task List) at proven parity. Fastest to an honest milestone. Defers the ratified 3 partials + 7 absent.
2. **+ restore the 3 partials** — Action-Required funnel, TTS per-item queue, Job-Queues mutations. ~2–3× work, more workers.
3. **Full 13/13 legacy replacement** — also port the 7 absent sections (Q&A, Submit-Jobs, Time-Saved, Filter-Settings, Direct-TTS, Debug, System-Status). Largest, multi-day.

## DECISION 2 — Design divergences: keep the mux's choices, or match legacy?

These are **coherent deliberate choices, not bugs or drift** (Rachel's systemic finding). One decision covers all of them; concrete revert edits are documented and ready either way.

| Item | Mux (deliberate) | Legacy |
|---|---|---|
| Top-chrome theme (V5) | Dark band, light text, off the shared sheet by design | Uniform light |
| AR panel (V3) | 2px border, no solid-blue filled header bar | 3px, filled bar |
| Playing panel (V4) | Emerald palette (`#34d399`) | Bootstrap green (`#198754`) |
| Persona badge (R4) | Glyph-only button, name in popover (ratified F-Arnold-3 declutter, 2026-05-19) | Inline icon+name — **you flagged this missing in your V10 drill-down** |
| Title (H2) | Keeps 🔔 emoji | Plain "Notifications" |
| TTS slider (V5 width) | Relocated to sibling mount (733px header-mount; region still full-width) | Nested inside header (956px) |

**The call:** keep the mux's modern reskin, or revert to legacy's uniform-light look. R4 specifically *overrides a ratified decision*, so it needs your explicit word.

## Deferred (ratified — not counted as gaps)

R5b (click-to-rename affordance); V4/V9 display-state + strip sub-icon geometry (seeded-fixture tier); the 3 partials + 7 absent sections (pending Decision 1).

## Held for your push (no flip, nothing pushed)

- `mux-parity-fixes` — H2 + R5 (commit-held)
- `mux-fullpage-oracle` — Rio's chrome oracle (commit-held)
- **Coordinated batch merge** pending Rachel's R5 gate: merge both to wip, flip the V2 sentinel green (= H2's full-page proof), held for your push. The switchover flip remains yours alone and is **not** on the table.

## Evidence (worker docs)

- Sam's gap matrix + §ADDENDUM: `2026.07.01-mux-vs-legacy-adversarial-parity-gap-matrix.md`
- Rachel's authoritative verdict: `2026.07.01-authoritative-mux-legacy-parity-verdict-rachel.md`
- Rachel's chrome style-fidelity: `2026.07.01-chrome-row-style-fidelity-verdicts-rachel.md`
- Rachel × Rio unified chrome verdict: `2026.07.01-unified-chrome-parity-verdict-rachel-rio-reconcile.md`
- R5 build-spec: `2026.07.01-r5-session-topic-build-spec.md`
