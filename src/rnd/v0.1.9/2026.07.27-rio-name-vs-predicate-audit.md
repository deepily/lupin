# Name-vs-Predicate Audit — Rio's 2026-07-27 Instruments

**Auditor**: Rachel 🕊️ (`ef2f3363`) — assigned by Mr. Radio because the author correctly
declined to grade his own checks.
**Question, per the A5 precedent**: *is the NAME broader than the PREDICATE?* A check
named "all `cc-*.json` bridges are group-writable" that asserts file **mode** only let
all 11 corrupted files pass green. A check answering a narrower question than its name
is worse than no check, **because it occupies the slot**.
**Second axis, per Rio**: is the check in the right **VENUE**? A5 lived in preflight,
which targets the VM, while the defect occurs on the dev box — a correct check aimed
where the defect cannot happen.

---

## VERDICT

| Artifact | Name vs predicate | Venue | Ruling |
|---|---|---|---|
| `test_timeout_marker_is_armed.py` | matches | correct | ✅ **CLEAR** |
| `check_schema_at_head.py` | matches | correct | ✅ **CLEAR** |
| `src/tests/conftest.py` guard | matches | correct | ✅ **CLEAR** |
| `verify-running-code.sh` | **BROADER in the HAS direction** | **wrong for one venue** | 🔴 **FINDING** |

One finding of four. Three are clear on both axes, and I am saying so plainly rather
than distributing a quota.

---

## 🔴 FINDING — `verify-running-code.sh`: exit 0 claims more than the clock can prove

### The contract vs the predicate

The header states the exit contract:

```
#   0  the running process HAS the commit
#   1  the running process does NOT have it  (recreate required)
#   2  cannot determine
```

The predicate is a comparison of two clocks — host `git` commit time vs container
`.State.StartedAt` (plus `.State.Running`, which Rio correctly added after catching his
own first cut):

```python
print( 'HAS' if commit <= started else 'MISSING' )
```

**The two directions are not symmetric:**

| Direction | Claim | Sound? |
|---|---|---|
| commit **newer** than start → `MISSING` | the process cannot have code authored after it started | ✅ **unconditionally sound** |
| commit **older** than start → `HAS` | the process has this commit | ⚠️ **conditional on an unchecked precondition** |

`HAS` is sound only if **the container's code came from this repo tree**. The script
never verifies that. Its own on-screen text is honest — it prints *"the running process
started after this commit"* — but the documented exit-0 contract says *"HAS the commit"*,
and exit codes are what callers branch on.

### Why this is live, not theoretical — the VENUE half

Measured across the compose files:

| Venue | code source | clock verdict |
|---|---|---|
| `docker-compose.yml` (dev :7999, test :8000) | `./src:/var/lupin/src` (line 139, 273) | sound |
| `docker-compose.cloud-gpu.yml` | `./src:/var/lupin/src` (line 100) | sound |
| **`docker-compose.cloud-test.yml`** | **`image: ${LUPIN_IMAGE:-lupin:1.1.0}` — NO `./src` mount** | 🔴 **unsound** |

Verified: `grep -nE '\./src:/var/lupin/src' docker-compose.cloud-test.yml` returns **only
a comment** — line 127, *"if this service ever gains a `./src:/var/lupin/src` mount…"* —
confirming it does not have one. Code is **baked into the image**.

⇒ On `cloud-test`, a container started at time *T* reports **HAS** for every commit
authored before *T*, **regardless of which commit the image was built from**. The image
can be arbitrarily old. The verdict is a statement about *when the container started*,
not about *what code it contains*.

**And the container name is a free argument** (`verify-running-code.sh <container>`), so
nothing stops it being aimed at exactly the venue where it is unsound — which is the
deploy venue.

### Why this is the row's own thesis, one level up

The header already names this exact shape at lines 47–49:

> *"A start-clock alone answers 'did a process ever start after this commit' — a
> different question with the same shape, which is this row's entire thesis wearing the
> fix's clothes."*

Rio applied that reasoning to the `Running`/`StartedAt` gap and closed it. The same
sentence applies one level up, to the `HAS` verdict itself: **for a baked image, the
start-clock answers "did a process start after this commit" and the contract reads it as
"the process has this commit."**

Reinforced by two of today's sibling rows, which establish the precondition is violable
in this repo *right now*: `5f421be0` — *"a mount can be healthy in `docker inspect` and
serving June's bytes"* — and `28f125b1` — *"39 file binds, two failure modes, and dev has
been blind."*

### Suggested remedy (not applied — Rio's file, and the fix is a design call)

Either **narrow the name/contract** — exit 0 means *"no commit newer than the process
start"*, which is what it proves — or **add the missing precondition**: assert the
container's code path is bind-mounted from this tree, and return **exit 2
(CANNOT_DETERMINE)** where it is not. Exit 2 already exists and already carries "cannot
answer", which is the correct verdict class for a baked image; the third exit code Rio
built is exactly the right home for this, and using it costs no new vocabulary.

---

## ✅ CLEAR — the other three, with what I checked

### `test_timeout_marker_is_armed.py`

Name claims the marker is **armed**. The predicate proves it, in the strong direction:
plugin importable, **a cap actually kills an over-running test** (a 5s sleep under a 1s
cap must fail, and fail *with* `Timeout` in the output), and no global `--timeout` in
`addopts` so the blast radius stays the 18 opted-in tests.

**It carries the positive-control arm** row `ed4df64a` asks of every gate —
`test_the_probe_harness_can_also_report_a_PASS` exists precisely because `rc != 0` would
be satisfied by a harness that always fails. That is the arm most of today's gates lack,
present here before I asked for it.

Venue checked, hypothesis **rejected**: I suspected the caps might live in the
un-runnered `src/cosa/tests/**`. Measured — all 7 cap-bearing files are under
`src/tests/`, **zero** in `src/cosa/tests/`. No venue gap. Recorded because the
hypothesis was reasonable and the measurement killed it.

Confirms my own earlier finding is now **resolved**: I measured `pytest-timeout` MISSING
at ~14:00 and flagged the marker as registered-but-inert. It is now **installed (2.4.0)**
and pinned; this guard is what keeps it that way. My review's note on that point is stale
and superseded.

### `check_schema_at_head.py`

Name claims it checks the schema is at head. Predicate: reads the tree's head revision
and the DB's current revision and classifies. Name matches. It carries a genuine
**third exit code** (`CANNOT_DETERMINE`) rather than collapsing "no" into "cannot tell" —
the same discipline whose *absence* is the finding above, applied correctly here. The
docstring's `Ensures:` block explicitly enumerates the `( None, None, reason )` failure
path per source.

### `src/tests/conftest.py` guard

Two mechanisms, both scoped to what they claim. The config floor uses `setdefault`, not a
hard set, and the comment states why — an explicit export from CI, the container, or the
integration conftests still wins; it is a **floor**, and the name says floor. The venue
router delegates its logic to `tests/venue_routing.py` so it is unit-testable **in both
directions** without a container, and names deselected node-ids rather than counting them
— which I independently verified earlier in the day (`21745/21752 collected, 7 deselected,
zero errors`).

---

## ROUND 2 — the row listed FIVE instruments, and I had audited THREE

⚠️ **Correction to the verdict table above.** I audited from Mr. Radio's DM, which named
four artifacts. Row `4e033ce2` — Rio's own, filed against himself — lists **five**, and
adds an axis I did not apply:

> *"Whether the failure MESSAGES (not just the docstrings) carry the scope. A docstring
> nobody reads at 2am is not where a caveat does its work."*

Two instruments were genuinely unaudited, and both are findings.

| Artifact | Verdict |
|---|---|
| `verify-running-code.sh` | 🔴 finding — already filed as `897b01b3` |
| **`code_identity.py` + `GET /api/code-identity`** | 🔴 **NEW FINDING** |
| **`src/conftest.py` re-import guard** | 🔴 **NEW FINDING** |
| `check_schema_at_head.py` | ✅ defensible — name is accurate; one cheap improvement |
| `test_timeout_marker_is_armed.py` | ✅ clear |

---

### 🔴 NEW FINDING 1 — `/api/code-identity` ranks nothing, and the convenience field is first

The module docstring is explicit about authority:

> *"`imported_at` is the authority. `git_sha` is a convenience… it cannot prove that
> every OTHER module in the process was loaded from that same revision."*

**Read live from `:7999`** (`urllib`, GET, read-only):

```
LIVE PAYLOAD KEYS (in order): ['git_sha', 'git_branch', 'git_sha_source', 'imported_at', 'pid']
any field naming which is authoritative?  False
```

⇒ `git_sha` is **first** and reads as the answer. `imported_at` is fourth and
indistinguishable from metadata. The endpoint `description` lists them as peers — *"The
git sha, branch and load time captured at MODULE IMPORT"* — with no ranking.

A caller who has only the JSON will compare `git_sha` to their commit, because that is
what the field is called and where it sits. That is the convenience field, and the
docstring says plainly it cannot carry that weight. **This is Rio's own caveat, confirmed
exactly as he posed it**: *"Stated in the docstring — is it stated where a caller reading
the JSON would see it?"* — **No.**

**Remedy is one key**, not a redesign: emit the authority in the payload
(`"authoritative_field": "imported_at"`, or a `"compare_against"` hint). The frozen-at-
import mechanism is correct and pinned by tests; only the *self-description* under-informs.

### 🔴 NEW FINDING 2 — the re-import guard protects exactly ONE package, and says "protected module(s)"

```python
_PROTECTED_ROOTS = ( "sqlalchemy", )     # src/conftest.py:44
```

One element. The failure text reads *"This test RE-LOADED N protected module(s)"*; the
fixture is `_fail_on_protected_module_reimport`; the docstring says *"a package which
cannot survive re-import."* Nothing in the name, the message, or the green state says the
protected set is **a single package**.

⇒ **A green means "no `sqlalchemy` re-import." It reads as "no dangerous re-import."**
Any other re-import-hostile package is silent by construction — and the guard occupies
exactly the slot a reader consults for "did a test unload something that cannot survive
it?" That is A5's shape precisely: the check answers a narrower question than its name,
and the silence reads as coverage.

⚠️ **Same-day precedent, which is why this is not a nitpick.** Rick had
`_PG_ISOLATION_MODULES` **deleted** this morning (`d621b111`) for this exact property —
per Tiffany's amendment on `ed4df64a`: *"an allowlist protecting the two modules someone
thought of, silent by construction about the third,"* and her keeper: **"a known-bad list
is a hand-kept allowlist wearing a different hat."** `_PROTECTED_ROOTS` is a hand-kept
allowlist with one entry.

**Remedy is a naming/scope call, not necessarily a widening** — say what it watches. Either
name the root in the message and the fixture (`_fail_on_sqlalchemy_reimport`), or state the
watched set in the failure text so a reader knows what the green did *not* cover. Widening
the tuple is a separate decision with a real cost (every protected root adds import-hook
work), and should not be smuggled in as a rename.

### ✅ `check_schema_at_head.py` — defensible, with one cheap improvement

The name does **not** overclaim: it is `check_schema_at_head`, the verdict token is
`AT_HEAD`, and revision-equality is exactly what it proves. Ruling: **defensible**.

But Rio's own question — *"is the cross-reference reachable from the failure message?"* —
resolves **no**. On success `classify()` returns detail `""`, so a green prints
`VERDICT=AT_HEAD` and nothing else; the `check_schema_parity.py` cross-reference (*"a
hand-edited DB can match head and still have drifted columns"*) lives only in the module
docstring at lines 43–60. Suggested: one line in the `AT_HEAD` detail pointing at parity.
Not a finding — the name is honest, and the risk is slot-adjacency rather than overclaim.

---

## METHOD

Read-only throughout. `verify-running-code.sh` was executed against `lupin-rest-test` in
both directions (a HEAD commit → `MISSING`; `da1a5ed8`, predating container start → `HAS
IT`) — it inspects only, and nothing was bounced, recreated, or deployed. Compose files
were read, never edited. No infrastructure touched.

⚠️ **One measurement error of my own, recorded**: my first pass captured `exit=0` from a
pipeline whose exit status belonged to `head`, not to the script. Re-run without the pipe
to get the script's real code. A pipeline's exit status is the *last* command's — which is
the same class of defect as everything else in this document, in the harness reading it.
