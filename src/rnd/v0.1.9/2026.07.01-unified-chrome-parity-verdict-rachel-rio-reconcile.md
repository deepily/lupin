# Unified Chrome Parity Verdict — Rachel (style) ⊕ Rio (structure/geom) reconcile

**Author**: Rachel 🕊️ (`01b19b90`) — for Mr. Radio 🦉. **Date**: 2026-07-01.
**Reconciles**: my chrome STYLE-fidelity verdicts (`2026.07.01-chrome-row-style-fidelity-verdicts-rachel.md`)
against Rio's full-page chrome oracle (closure `11a38cf6`, Cat A/B/C). No overlap, no contradiction — Rio owns
structure+geometry, I own the look his iso deliberately skips. Closes reconcile task #2. Read-only.

## Rio's categories (from `11a38cf6`) and how my verdicts map

| Cat | Rows | Rio proves | My layer |
|---|---|---|---|
| **A** single-sourced (shared `lupin-nav.css`) | V1 nav | FULL style + geometry isomorphism (Tier 2 + Tier 3) | **DROP — covered.** Nav is PROVEN at parity by Rio. Nothing for me. |
| **B** re-implemented | V2, V3, V5, V7 (+V1-logout) | STRUCTURE + GEOMETRY only | **My STYLE verdicts stand** for palette/border/theme his iso can't see. |
| **C** mux-native / display-asymmetric | V4 PLY, V9 strip, V13 toolbar | framed as mux-native, not strict parity rows | **Align** — these are design/mux-native, not OPEN parity gaps. |

## Unified per-row verdict

### Cat A
- **V1 nav → CLOSED (Rio-proven at parity).** Style + geom isomorphism green off the shared sheet. Dropped from my scope.

### Cat B — Rio's geometry + my style, combined
- **V2 env-label + clock → CLOSED (via H2, code+unit) — golden recapture owed.** Rio's oracle pins
  `V2-env-label`/`V2-clock` in `KNOWN_OPEN_CHROME_ROWS` with a break-on-close assertion, because it was built
  against a tree WITHOUT H2 (H2 is commit-held on `mux-parity-fixes`, not yet merged). I ACCEPTED H2
  (`a81b2114`) — the mux now renders both spans, legacy-faithful. **Coordination flag ①**: when the
  H2+R4+R5 batch merges to wip, Rio's break-on-close sentinel fires BY DESIGN → he must shrink
  `KNOWN_OPEN_CHROME_ROWS` and recapture `notifications-legacy-fullpage.golden.json` with env/clock present.
  Style note: mux env-label `#e8e8ec` + clock `#9aa0aa` are dark-theme colors (Rick design call).
- **V3 Action Required → geometry PROVEN + style Rick-design-pending.** Rio's Tier-3
  `test_tier3_action_required_full_width` proves the AR panel is full-width == legacy. My style layer stands:
  **border 2px vs legacy 3px** (concrete), **no solid-blue filled header bar** (legacy fills `.section-header`
  `#0d6efd` white); empty-state byte-identical. → palette/bar = Rick design decision, not repainted.
- **V5 notifications header → cramping DEFECT stays FIXED; 733-vs-956 RESOLVED as a node-boundary artifact
  (NOT a residual gap).** *Reconcile flag resolution:* the `956` is WIDTH (height is a normal `h:63`), so this
  is a width delta, not a height/content gap. The measured nodes differ: legacy `#section-notifications >
  .section-header` **nests the TTS-fraction slider inside it** (`notifications.html:641`, w=956,
  slider-inclusive), while the mux mounts `#notifications-header-mount` (733) and
  `#tts-preview-slider-mount` as **siblings** in `.notifications-header-region` (`multiplexer.html:125-136`,
  slider-exclusive). The ~223px delta ≈ the slider that legacy keeps inside its header node. The mux header
  REGION (both mounts) still spans full width. This is the **V6 slider-relocation node boundary**, NOT the
  horizontal-cramping defect (that is fixed via flex-gap; `flex-wrap` prevents re-cramping at the narrower
  width) and NOT a real OPEN geometry gap. **Coordination flag ②**: recommend Rio measure
  `.notifications-header-region` (the wrapper of both mounts) for an apples-to-apples width, else the
  relocation reads as a false 223px gap. Style: the dark-theme reskin remains a Rick design call.
- **V7 broadcast → CLOSED (Rio geom + my style, both green).** Rio's Tier-3 proves the ▼-inline geometry;
  my style diff confirms `broadcast.css` is a byte-faithful port of legacy `broadcast-panel.css`. At parity.

### Cat C — mux-native / display-asymmetric
- **V4 PLY → mux-native display-state (Rio) + palette Rick-pending (me).** Rio frames the idle display-state
  divergence as mux-native, not a straight parity row. My style deltas (emerald `#34d399`/`#ecfdf5` vs legacy
  Bootstrap `#198754`/`#d1e7dd`, no filled green header bar, 1px vs 2px, amber `#fbbf24` vs `#ffc107`) are the
  same Rick DESIGN decision as AR — empty-state byte-identical. Not an OPEN gap; a design divergence.
- **V9 session strip → mux-native (dark full-bleed bar).** Out of both strict-parity lanes.
- **V13 toolbar → mux-native (Rio).** Supersedes my earlier "OPEN/unknown (pixel)" — it is NOT a legacy-parity
  row, so not an OPEN gap; a mux-native surface (design).

## Net — the decision surface collapses to ONE Rick call

Structure + geometry are PROVEN at parity where they matter (nav full-iso; AR full-width; broadcast ▼-inline;
sender cards Tier 0–3 from the earlier run). Every remaining non-closed item is **look/palette/theme** —
which folds into the single systemic decision from my prior doc, now corroborated by Rio's geometry:

> **Keep the mux's modern dark top-chrome + own panel palette, OR revert to legacy's uniform light theme.**

- Already CLOSED & proven: V1 nav (Rio), V7 broadcast (both), V2 (H2 code+unit; golden recapture owed).
- Geometry-proven, style = that one Rick design call: V3 (AR border/bar), V5 (dark header; 733/956 is a
  non-issue node boundary), V4 (PLY palette).
- Mux-native by Rio's framing (design, not gaps): V4 display-state, V9 strip, V13 toolbar.

No contradiction between my style verdicts and Rio's oracle; the two compose into full chrome coverage.

## Coordination flags for Mr. Radio
1. **V2 golden recapture** — on the H2+R4+R5 batch-merge, Rio's `KNOWN_OPEN_CHROME_ROWS` break-on-close fires
   by design; Rio shrinks the set + recaptures the fullpage golden with env/clock present.
2. **V5 width measure** — Rio should measure `.notifications-header-region` (both mounts) not
   `#notifications-header-mount` alone, so the V6 slider relocation isn't misread as a 223px width gap.
3. **R4/R5 golden interplay** — R4/R5 involve interrelated golden recaptures (your batch-merge note); when they
   land I'll re-check BOTH the sender-card golden freshness guard and the fullpage golden as part of verifying
   R4/R5.
