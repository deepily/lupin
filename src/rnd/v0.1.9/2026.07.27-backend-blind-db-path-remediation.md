# Backend-Blind `db_path` — Remediation Record

**Date**: 2026-07-27
**Session**: `951a4459` (Mr. Radio 🦉)
**Decision**: `2b20a6d6` (ruled by Rick, both arms) · **Bug**: `d621b111` · **Implementation**: `beadc2b0` · **Cleanup follow-on**: `77a9a323`
**Commit**: `a5a182e1` — 26 files, +749/−158

---

## 1. The defect in one line

Nine classes route on the ambient `vector store backend` flag. Four of them *also* accept a caller-supplied location, store it on the attribute, and then ignore it whenever the postgres path is active — so a caller who believed it had redirected the store was reading and writing the shared one, silently.

`vector store backend = postgres` has been live in `[Lupin: Baseline]` with **no per-block override since 2026-07-07**, so this was every venue, host included.

---

## 2. Rick's ruling (2026-07-27)

Both arms of `2b20a6d6`, taken via `ask_multiple_choice`, `answered=true`, `default_used=false`.

**Arm 1 — remedy**: option **(1b)** — reconcile the production call sites *first*, **then** make construction raise, **then** land the guard test. A raise is unaffordable while live code legitimately passes an ignored path.

**Arm 2 — the offenders**: verbatim —

> *"I absolutely do not want any test touching a live dev data store! If it's not isolated then it needs to be removed or fixed."*

This **overrode all four options offered**. The framing put to Rick split the six into four "lying about isolation" and two that pointed at the real store "on purpose", and recommended re-scoping the latter to *declare* it. He rejected the premise: intent does not launder the contact. There is no declared tier, no known-bad list, no allowlist.

That framing was also **factually wrong**, discovered after the ruling: both "deliberate" tests passed `table_name="prediction_decisions_test"`, i.e. they *were* attempting isolation. `_pg_add_decision` never forwards `table_name` to `PredictionDecisionRepository`, so that isolation was inert too. They were not honest exceptions; they were the same defect.

---

## 3. What the sweep had wrong

| Claim in `d621b111` | Measured reality |
|---|---|
| "Three live production call sites" | **Five.** Two were missed. |
| "Six test offenders" | **Five.** One was a docstring. |
| "Junk accumulated in `lupin_db_test`" | For the unit-test venue, the target is **`lupin_db_dev`**. |
| "Nine classes silently discard any `db_path`" | Nine *route on the flag*; only **four** accept a location at all. |

### 3a. The two missed call sites

- **`src/cosa/memory/solution_manager_factory.py:167-171`** — a **fourth authority**: a pre-construction gate demanding `db_path|gcs_uri` *before* `SolutionSnapshotManager` exists. This is the one that broke server startup mid-change: `main.py` correctly stopped building a path under postgres, and this gate then rejected the only valid config.
- **`src/cosa/memory/gister.py:65`** — a **fifth live site**, building a path unconditionally for `GistCacheTable`.

### 3b. The authorities, counted

One fact — *which store backs this data* — had **four** authorities and no comparator:

| Authority | Where | Problem |
|---|---|---|
| `vector store backend` | `vector_store_backend.py` | the real one |
| `solution snapshots manager type` | `main.py:508` | names the MANAGER, gates the STORAGE |
| hardcoded literal | `prediction_engine.py:163` | unreachable by config |
| `db_path\|gcs_uri` presence | `solution_manager_factory.py:168` | infers backend from config shape |

### 3c. The docstring

`integration/conftest.py:503` was counted as an offender. AST reports **zero** `SolutionSnapshotManager` call nodes in that file — line 503 is a `Usage:` example inside a docstring. It *taught* the defect rather than committing it. Now it teaches the pin.

---

## 4. The venue defect nobody had named

`src/cosa/rest/db/database.py:73` reads `LUPIN_ENV`, **defaulting to `development`** → `lupin_db_dev` (line 110).

| Venue | `LUPIN_ENV` | Database |
|---|---|---|
| `:7999` container | `development` (compose:184) | `lupin_db_dev` |
| `:8000` container | `testing` (compose:292) | `lupin_db_test` |
| `run-integration-tests.sh` | exports `testing` (line 202) | `lupin_db_test` ✅ |
| `run-e2e-ui-tests.sh` | exports `testing` (line 249) | `lupin_db_test` ✅ |
| **`pytest src/tests/unit/`** | **nothing sets it** | **`lupin_db_dev`** ⛔ |

Both sanctioned runners are correct. **Unit tests are the gap** — and they are exactly the venue that was constructing these classes with tmpdir paths.

### The dated artifact

`lupin_db_dev.solution_snapshots` contains `"What is 2 + 2?"`, created **2026-07-26 12:24:22 EDT**. That literal is `src/tests/unit/test_answer_is_correct.py:31` — the module commit `e4113d64` fixed **the same day at 14:25 EDT**. The row predates its own fix by two hours.

### What was NOT found — recorded so nobody inherits a false alarm

- `prediction_decisions` is **clean** across the whole window: every row 2026-07-07 → 2026-07-27 is `data_origin='organic'`, zero synthetic.
- The 6 `synthetic_*` rows in `lupin_db_dev` are dated **2026-02-25** with hardcoded fixture timestamps — five months *before* the cutover. The e2e tests write `now()`, so those are not theirs. An early read of them as test contamination was wrong.

---

## 5. The remedy

One seam, in the module that already owns the flag:

```python
def resolve_lancedb_path( db_path, caller, config_mgr=None ):
    if not is_postgres_backend( config_mgr ): return db_path   # lancedb: honored
    if db_path is not None: raise ValueError( ... )            # postgres + path: REJECTED
    return None                                                # postgres, nothing promised
```

All four path-taking classes resolve the backend **before** storing a location. The five that accept no location (`EmbeddingCacheTable`, `QuestionEmbeddingsTable`, `InputAndOutputTable`, `QueryLogTable`, plus `routers/system.py` which was already correct) cannot be lied to and are untouched.

Reference form for a call site — `src/cosa/rest/routers/system.py:272-289`, which builds its path inside the `else` arm of `if is_postgres_backend( config_mgr )`. It was the only site that asked first.

---

## 6. Scope: the ticket said 6 tests; the number is ~350

Measured against a clean HEAD worktree under an identical environment (`LUPIN_ROOT` + `PYTHONPATH` set both runs — the first attempt was not comparable and was discarded):

| Run | Failed | Passed |
|---|---|---|
| HEAD baseline | 38 | 19803 |
| With the raise, before test fixes | 360 | 19481 |
| After test fixes | **6** | **19836** |

**345 of the 348 new `ValueError`s carried the seam's own message.** 345 unit tests across nine files had been running against the shared store.

**30 of the 38 pre-existing baseline failures were this same bug** wearing a different exception: `AttributeError: 'GistCacheTable' object has no attribute '_gist_cache_tbl'` — an attribute that only exists inside the `if not self._use_postgres:` arm. They are green now.

The 6 remaining are pre-existing and unrelated: HF_HOME (×2), alembic revision chain, GCP identifiers, vertex regions, test_suite job.

> Two tests passed in the main tree but failed in the worktree baseline (`test_terraform_invariants`, `test_gate_reachability_census`). They scan the repo tree and neither references anything changed here — **worktree artifacts, not fixes**. Not claimed.

---

## 7. Two dispositions, applied by intent

Not one blanket rule — the right fix depends on which path the test is *for*.

**LanceDB-path tests** → pin the flag at its single definition site, **during construction**:

```python
with patch.object( vector_store_backend, "get_vector_store_backend",
                   return_value=vector_store_backend.LANCEDB ):
    mgr = SolutionSnapshotManager( config )
assert mgr._use_postgres is False, "backend pin failed — ..."
```

The **during** matters. `test_lancedb_solution_manager.py` already set `mgr._use_postgres = False` — but *after* `__init__`, which had already resolved the backend and (now) raised. A post-hoc pin is not a pin.

**Postgres-path tests** → stop passing a path at all. `test_gist_cache_table` passed the literal `"/unused"`; that string named the defect out loud.

Every pin carries a **control assert** that fires if the pin ever stops pinning.

---

## 8. `_PG_ISOLATION_MODULES` is deleted

The `cfcbb703` Family B remedy worked, but its gate was a hand-kept set of two module names, so any module not on it inherited the hazard **by default, silently** — which is how `test_answer_is_correct` spent three weeks writing to the live store *with the remedy already in the tree*.

103 lines (allowlist + per-test-schema fixture) replaced by a tombstone comment quoting the ruling. Both covered modules now own a `_pin_lancedb_backend` fixture giving them **real** tmpdir isolation. Evidence the LanceDB branch is actually taken: those runs now emit LanceDB deprecation warnings that were absent before.

---

## 9. Guards, with controls in both directions

`src/tests/unit/test_vector_store_path_guard.py` — 14 tests across four concerns: the seam itself, every path-taking class rejecting an unhonored location, the postgres path staying constructible, and the LanceDB path staying first-class.

| Control | Result |
|---|---|
| Neuter `resolve_lancedb_path`'s raise | **7 of 14 RED**; restore → 14 green. The 7 that stay green are the ones that don't test the raise. |
| Neuter the factory's postgres branch | `test_missing_location_allowed_under_postgres` goes red alone; restore → green. |

`test_missing_location_allowed_under_postgres` guards the exact gate that broke startup this morning.

---

## 10. Still open

- **`77a9a323`** — measure and clean whatever landed in `lupin_db_dev`. Dry-run first; deletion from a live dev store is Rick's word, not standing authority.
- **Whether each offender WROTE or merely CONSTRUCTED** is still unmeasured per-test. It no longer affects disposition (all are isolated either way) — only cleanup sizing.
- **`src/tests/smoke/` and the rest of `src/cosa/tests/`** (row `5bf28e07`, 410 gate-invisible files) were not swept for this pattern beyond what the unit run surfaced.
- **The `LUPIN_ENV` unit-test gap itself** — nothing forces `testing` for unit runs. The seam now makes the *symptom* loud, but the venue default is still `development`. Worth its own decision.
