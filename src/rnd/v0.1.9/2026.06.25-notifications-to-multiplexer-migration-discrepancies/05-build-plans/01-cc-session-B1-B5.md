# CC-Session Accordion (Notifications #5) — Restore Plan (B1–B5)

**Date**: 2026-06-26
**Status**: 🟡 **IN STRICT-CANON CASCADE REVIEW** (build-forward; Tiffany 💍 author for Mr. Radio 🦉, same cast as the closed 00b/00c cascade). **01-A Stage-1 (REUSE, Krishna 🦚) folded by Tiffany 💍, 2026-06-27**: F-Krishna-BA1 (§5 B1 re-nest was understated — commons chrome is STATIC in multiplexer.html + broadcast card is renderer-INJECTED ⇒ relocate the chrome INTO `broadcastCard.ts` + flip boot.ts mount-order to broadcast-FIRST; machinery reused as-is; verified against source). BA2 (collapse double-ownership) = adequately surfaced (§4.1/OQ-2), tracked by mgr as a cross-crew BUILD gate (not a plan defect), no plan change. **01-A Stage-2 (Fitness, Sam 🎙️) folded by Tiffany 💍, 2026-06-27**: F-Sam-BA2 (boot commons lookup must be DYNAMIC post-mount `querySelector` on the live rendered subtree, NOT a page-load `getElementById` → null-throw; §5 B1 boot.ts + ACs) + F-Sam-BA1 (nest commons OUTSIDE `#broadcast-recipients-row` so it survives the `row.replaceChildren()` recipient refresh) + F-Sam-BA3 (pinned exact remove-range `multiplexer.html:138-183` + broadcast-card-mount L133 — corrected a line-drift I introduced in the BA1 rewrite). **01-B Stage-1 (REUSE, Krishna 🦚) folded by Tiffany 💍, 2026-06-27**: F-Krishna-BB1 (B2 is NOT placement-only — stated B3-header collapse behavior + ADD `ttsPreviewSlider.ts` `stopPropagation`, verified absent today) + F-Krishna-BB2 (B2 DEPENDS on B3 → re-sequenced B2 after B3 in §9 + fixed the reversed dependency note). **01-C Stage-1 (REUSE, Krishna 🦚) folded by Tiffany 💍, 2026-06-27** (confirm-absent-not-renamed PASSED): F-Krishna-BC1 (reuse the TaskListRenderer/FleetStatusRenderer header idiom, don't greenfield; OQ-4 RESOLVED → PORT legacy history-dropdown UI + REUSE NotificationStore.history()) + F-Krishna-BC3 (clear-all needs a NEW NotificationStore.clearAll() + emission + 100% L/B/F — not UI-only) + F-Krishna-BC2 (filter-badge DEFERRED → OSQ-B3.1, Rick's scope call; other 3 controls ship; swept §1/§2 mentions). **01-D Stage-1 (Krishna 🦚) folded by Tiffany 💍, 2026-06-27 — KEYSTONE**: F-Krishna-BD1 (active-id seam rewritten to the F0 model — SUBSCRIBE `store_tts_queue_changed` + gate on `TtsQueueStore.current()` === `Notification.id_hash`; OQ-1 RESOLVED; AudioStore id-blind, SequentialAudioManager DELETE; swept §3/§4/§5/§8/§9) + F-Krishna-BD3/OQ-5 (mux apiClient lacks proxy-ratify → thin wrapper + OSQ-B4.1) + F-Krishna-BD4 (corner-btn clicks extend the existing delegated `closest()` handler `:428-453`, NO `stopPropagation` — mux delegated-collapse architecture). **01-D Stage-2 (Sam 🎙️) folded by Tiffany 💍, 2026-06-27**: F-Sam-BD1 (class-driver CLEAR-PRIOR-THEN-SET → exactly one bubble lights; +`current()`===null branch; negative test across a `current()` TRANSITION) + F-Sam-BD2 (driver ALSO subscribes `store_audio_state_change` for the glyph — identity-vs-glyph split, stays id-blind) + F-Sam-BD3 (OSQ-B4.1 CLOSED: legacy = `POST /api/proxy/acknowledge` + `window.open`; wrapper acknowledge-only, page-open renderer-side). Pre-swept the line-163 clear-all straggler → active-filter scope (OSQ-B3.6). **01-D Stage-3 (Ownership, Cheech 🌿) folded by Tiffany 💍, 2026-06-27 — KEYSTONE last gap**: F-Cheech-BD1 (corrected the AC — stop = halt + `current()`-clear de-light via the F0 seam, NOT a B4 advance; `stop()`→idle emits `store_audio_state_change{idle}` only, `store_audio_ended` is natural-completion-only per 00c; added the COND-2 consumer-side AC = B4 makes ZERO `TtsQueueStore` mutator calls; cross-section stop→`current()`-clear is an F0/`2605dca5` obligation, NOT a 00c change). 01-D KEYSTONE closes pending Cheech downstream-confirm. **01-B Stage-2 RE-FOLD (Sam 🎙️): F-Sam-BB1 reverts Krishna's BB1 — B2 is genuinely PLACEMENT-ONLY (dropped the `ttsPreviewSlider` stopProp; mux collapse is delegated on `#section-toolbar-mount .toolbar-btn`, no slider→collapse path); BB2 §9 lane table verified.** **01-C Stage-2 (Sam 🎙️): F-Sam-BC1 (clear-all REUSES existing `DELETE /api/notifications/bulk` + `confirm()` + filter-coupling cross-link to OSQ-B3.1) + BC2 (`clearAll` changeKind → full `renderAll`, no stale cards) + BC3 (count = `list().length`, not `unreadCount`; OSQ-B3.2).** **01-C/B3 FILTER RE-AUTHOR (Rick scope-expansion ruling 2026-06-27, ratified — supersedes the OSQ-B3.1 defer): authored the NET-NEW notification-filter feature into B3 — `NotificationStore` `filterMode {own/others/all}` (mirrors the CommonsStore filter pattern; ports legacy `notifications.js:6235-6315`) + `visibleEntries()`/`matchesNotificationFilter` + filter-badge + admin-gating; clear-all now FILTER-SCOPED; OSQ-B3.3..B3.6 raised for the delta-re-review (Krishna→Sam→Cheech). 100% L/B/F + EXECUTOR:AI on the new surface.** **01-C filter-delta Stage-1 (Krishna 🦚) folded by Tiffany 💍, 2026-06-27**: BC2-d4 (scope-divergence stated — legacy `:6235-6315` is the unified notif+queues filter; B3 narrows to NotificationStore-only) + BC2-d5 (OSQ-B3.6 sharpened — server route INCONCLUSIVE; PREFERRED safe path = client-side delete-by-id over `visibleEntries()`, no server change; mode-param = CROSS-STACK) + BC2-d3 (renamed `visibleList`→`visibleEntries`, `isFiltered`→`isFilterActive` to match CommonsStore idiom); d1 was the line-163 pre-sweep. **01-C delta cosmetics folded by Tiffany 💍, 2026-06-27**: BCd2 (OSQ-B3.6 — cite verified per-id `DELETE /api/notifications/{id}` `:1761` + N-non-atomic partial-failure handling) + BCd3 (renderer reads `visibleEntries()` + filter-aware empty-state). ⚠️ **01-C filter AXIS FROZEN pending RICK RULING — F-Sam-BCd1 FOUNDATIONAL (own/others NOT client-implementable: no owner/recipient discriminator in the mux `Notification`, vacuous single-user; ESCALATED, mgr rec = reframe to mux-native sender-persona/direction). Did NOT touch `filterMode`/`matchesNotificationFilter`; the axis-independent mechanism stands.** **01-E Stage-2 (Sam 🎙️) folded by Tiffany 💍, 2026-06-27**: F-Sam-BE1 (MOVE delete-range corrected to the COMPLETE corner-gate `~:381-501` — stop block `:464-501` was outside `:381-438`; replaced the load-order AC with a grep-NO-RESIDUAL sole-definition AC) + F-Sam-BE2 (pinned the `cc-tts-fraction` CLASS block `:2410-2439` in the delete-list; shared sheet carries BOTH families — mux reuses `cc-tts-fraction-*` as IDs so legacy CLASS rules don't reach it).** **01-E Stage-3 (Ownership, Cheech 🌿) folded by Tiffany 💍, 2026-06-27**: F-Cheech-BE1 (the 3rd moved block — section-header cluster — was still unpinned; ENUMERATED it: legacy controls inherit the GENERIC shared `.section-header*` `:126-160` [SHARED across AR/TTS-queue → NOT a B3-only move], grep found NO dedicated `#notifications-count`/etc. rule block, B3's filter selectors are net-new shared-sheet-native; GATED the B5 pin on B3's FINALIZED selector set + named the B5↔B3 coupling). 01-E closes pending Cheech downstream-confirm. **01-A CLOSED (Stage-3 Cheech 🌿): F-Cheech-BA1 (EXECUTOR:AI tags) + BA2 (broadcastCard.ts coverage AC). PLAN-WIDE EXECUTOR/coverage sweep DONE (B1–B5 ACs all EXECUTOR-tagged; coverage AC on B1+B4; manager-authorized).** **01-E Stage-1 (Krishna 🦚): F-Krishna-BE1 (CSS port = MOVE not ADD — DELETE from `notifications.css` monolith `:381-438` + load-order AC) + BE2 (cc-tts-fraction class divergence → shared sheet carries BOTH legacy + mux class families).** Cascaded review is the gate before any implementation.
**Author**: this session (for Rick)
**Accordion**: Notifications / CC-sessions (`section-notifications`, mux `notifications-pane`) — **#1 priority** for the current bug-fix dev branch.
**Source audit refs**: doc `01-mux-vs-legacy-notifications-section-gap-analysis.md` (§0.2 verdict, §2 side-by-side, §4 per-message regressions, §6 buckets B1–B5, §7 evidence appendix); doc `02-reconciliation-with-in-flight-parity-work.md` (coverage map + the B1↔`4b33ceb7` conflict).
**Decision-of-record refs**: `00-index.md` §"Resolved design calls (a)/(b)/(c)" + §"B4 active-bubble TTS mechanism"; TODO Decisions Log 2026-06-26. Cross-cutting mandates: `00-plans-index.md` §"Cross-cutting mandates" (1–7) — **inherited, not restated here**.

---

## 1. Goal & parity target

Restore the CC-session section to legacy top→bottom order and per-message function so the multiplexer matches `notifications.html`'s `#section-notifications`: **Broadcast card (with Recent-Activity re-nested inside it) at the TOP → Focus bar beneath → Sessions list**, the TTS-preview slider living in the section header above the focus bar, the section-header control cluster (count · filter-badge · history-dropdown · clear-all) **+ the notification-filter feature (BUILT per Rick scope-expansion ruling 2026-06-27)** present, and the per-message ⏸/⏹ + proxy-ratify-link rendered on every bubble but CSS-gated to the single actively-spoken message. "Done" = the §2 Oracle tiers green against a fresh legacy golden, with the five ratified buckets B1–B5 closed and the `4b33ceb7` sibling mount restructured into the legacy nesting.

## 2. Scope

**IN** (the five ratified buckets, decisions (a)/(b)/(c) of `00-index.md`):
- **B1** — section reorder + Recent-Activity re-nest, **restructuring the `4b33ceb7` sibling mount** (decisions (a)+(b)).
- **B2** — relocate the already-built F6 TTS-preview slider into/above the focus-bar section header.
- **B3** — build the section-header controls absent in mux (count · filter-badge · history-dropdown · clear-all) **+ the notification-filter feature (Rick scope-expansion ruling 2026-06-27 — NET-NEW)**, confirmed-absent first.
- **B4** — per-message active-TTS-gated ⏸/⏹ + proxy-ratify-link, replicating legacy render-all + `is-playing-current` CSS gate (decision (c)).
- **B5** — CSS / Oracle Tier-2/3 pass + golden rebaseline, gated last.

**OUT** (owned elsewhere / not this plan):
- Broadcast-to-all compose port, per-CC-card collapse toggle, worker message-count-badge silencing — **already built + committed `4b33ceb7`** (doc 02 §1); we reuse, B1 only restructures the mount.
- The sender-CARD interior (header / voice-row / date-accordions / messages) — internally consistent already (doc 01 §3); F1–F12 lane-owned, largely parity-proven (doc 02 §3). Out of scope except where B4 adds the per-message corner controls.
- Action-Required funnel, TTS-Queue restore, Job-Queues mutation gaps, and the 7 absent accordions — their own plans (`02-`…`11-`).
- The mux-native ADDs (`prediction-vote` 👍/👎, `progress-group-toggle`) — keep as documented supersets; not touched here.

## 3. Source anchors

All paths relative to repo root `/Volumes/data/include/www.deepily.ai/projects/lupin`.

### Legacy (reference behavior — `notifications.html` / `notifications.js` / `notifications.css`)
| Element | Anchor |
|---|---|
| Section shell | `static/html/notifications.html:631` `#section-notifications` |
| Section-header (B3 cluster + B2 slot) | `notifications.html:632-682` — `#notifications-count` (L633), `#notifications-filter-badge` (L634), `.cc-tts-fraction-control`+`#cc-tts-fraction-slider` (L641-665), `#history-dropdown-container` (L666), `#clear-all-notifications` (L675) |
| Broadcast card (TOP) | `notifications.html:692` `#broadcast-submit-card` |
| Recent-Activity (NESTED in broadcast) | `notifications.html:740` `#commons-recent-activity-section` |
| Focus bar (beneath broadcast) | `notifications.html:810` `#cc-session-strip` |
| Sessions list (bottom) | `notifications.html:828` `#notifications-list` |
| Per-msg ⏸ render | `notifications.js:14696` (render-all `notification-corner-pause-btn`) + builder `:13869` |
| Per-msg ⏹ render | `notifications.js:13919` `notification-corner-stop-btn` |
| Pause/resume click → class flip | `notifications.js:14718-14744` (toggles `is-paused-current`) |
| Active-TTS class driver | `notifications.js:14903-14971` (adds/removes `is-playing-current` / `is-paused-current` as audio engages/ends) |
| proxy-ratify-link | `notifications.js:7161` `createProxyRatifyLink()` (gate: `groupId.startsWith('pr-')`) |
| CSS gate (port source) | `notifications.css:381-438` — `.notification-corner-pause-btn{display:none}` + `li.is-playing-current .notification-corner-pause-btn, .sender-message.tts-playing .notification-corner-pause-btn{display:inline-flex}` + `.is-paused-current` palette. **Note the already-present `.sender-message.tts-playing` selector — the mux-friendly (non-`<li>`) hook.** |

### Multiplexer (targets — add/edit)
| Element | Anchor | Action |
|---|---|---|
| Static mount order | `static/html/multiplexer.html:72-200` | **B1** restructure |
| Focus bar | `multiplexer.html:81` `#cc-session-strip` (currently top) | **B1** move below broadcast |
| TTS-preview mount | `multiplexer.html:101` `#tts-preview-slider-mount` (orphan sibling) | **B2** relocate into focus-bar header |
| Sessions pane | `multiplexer.html:104-115` `#notifications-pane`→`#action-required-section`(L110)→`#sender-cards-container`(L113) | **B1** keep contents, reposition pane |
| Broadcast mount | `multiplexer.html:133` `#broadcast-card-mount` (currently bottom, sibling) | **B1** move to TOP, nest commons inside |
| Recent-Activity | `multiplexer.html:138-184` `#commons-activity-pane` (separate sibling) | **B1** re-nest INSIDE broadcast card |
| Boot mount wiring | `multiplexer/boot.ts:456-484` (`createCommonsActivityRenderer`/`createBroadcastCardRenderer`/`createTtsPreviewSliderRenderer` + `getElementById` lookups) | **B1/B2** re-point mount targets |
| Broadcast renderer | `multiplexer/render/BroadcastCardRenderer.ts` + template `render/templates/broadcastCard.ts` | **B1** host the nested commons slot |
| Commons renderer | `multiplexer/render/CommonsActivityRenderer.ts` + `render/templates/commonsActivityEntry.ts` | **B1** mount into broadcast subtree |
| TTS-preview renderer | `multiplexer/render/TtsPreviewSliderRenderer.ts` + `render/templates/ttsPreviewSlider.ts` | **B2** placement contract only (component done, F6) |
| Section-header (B3) | **NEW** — section-header chrome around `#notifications-pane`; no current mux equivalent (`#section-toolbar-mount` L72 is a *different* mechanism — per-section visibility) | **B3** build |
| Message bubble | `multiplexer/render/templates/notificationItem.ts:60-110` (`.sender-message`, flat + progress-group branches) | **B4** add corner ⏸/⏹ + ratify-link |
| Sessions renderer | `multiplexer/render/NotificationsListRenderer.ts` | **B4** wire click delegation + class driver |
| Active-TTS **id** source (F0 seam — F-Krishna-BD1) | `multiplexer/stores/TtsQueueStore.ts` `current()` (the active `id_hash`) published on the **`store_tts_queue_changed`** event (F0/00b) | **B4** subscribes to `store_tts_queue_changed` and gates the bubble whose `Notification.id_hash` (`shared/types.ts:333`) === `TtsQueueStore.current()`. **CONSUME id-blind** — do NOT extend `AudioStore` (id-blind per 00b OQ-F0.3) or build on `SequentialAudioManager` (DELETE per 00b/00c). `AudioStore.state()` (playing/paused) still drives the play/pause GLYPH, but message *identity* comes from the F0 seam. |
| TTS chrome (existing class helper) | `multiplexer/render/TtsChromeRenderer.ts` + `render/templates/ttsChrome.ts:67-69` (already ports `is-playing-current`/`is-paused-current`) | **B4** reuse the class vocabulary |
| Shared CSS surface | `static/css/shared/notifications-surface.css` (736 lines) — does **not** yet contain corner-btn / section-header / `cc-tts-fraction` selectors | **B5** port from `notifications.css` |

## 4. Dependencies & prerequisites

**Inherits all 7 cross-cutting mandates** from `00-plans-index.md` (100% L/B/F · Oracle Tiers 0–4 · single-source CSS · venue routing · manage-don't-build/lane isolation · coordinate-with-in-flight-crews · doc touchpoints). Below are the plan-specific prereqs.

### 4.1 Pre-req checks BEFORE any B1 code (gating)
1. **Confirm per-card collapse actually shipped in `4b33ceb7`** (NOT stranded on Rachel's `mux-section-toolbar-accordion-toggle` branch). Doc 02 §1 records it as double-owned: DFB Lane B claims "already wired, 11/11" folded into `4b33ceb7`, *and* D06/Rachel's branch is commit-held with no ref. **B4/B1 must verify** the collapse toggle is live in the `4b33ceb7` tree before restructuring — otherwise B1 restructures a mount whose collapse owner is unmerged, risking a double-ownership collision (doc 02 §"per-card-collapse double-ownership question"). Verification = grep the `4b33ceb7` working tree for the collapse handler + run the 11/11 lane test; if absent → coordinate the merge order with Rachel first.
2. **Coordinate with Tiberius's crew before restructuring `4b33ceb7`** (mandate 6). The `4b33ceb7` commit is **push-held for Rick** (store item `34466d69`) on `wip-v0.1.9-2026.06.21-bug-fix-implementation`; the full-parity build is mgr **Tiberius 👑** (`704c71b2`), verification **Krishna 🦚** (`0d69e015`), Foundation merged `3a5d87eb`. Because `4b33ceb7` is push-held, B1 can restructure it *before it lands upstream* (no revert of pushed history) — but only after confirming no unpushed follow-up from Tiberius's crew targets the same broadcast/commons mounts (doc 02 §6 prereq ii).

### 4.2 The B1 ↔ `4b33ceb7` conflict (doc 02 §2 — must resolve in execution)
`4b33ceb7` mounted `#broadcast-card-mount` (L133) **above** `#commons-activity-pane` (L138) as **two siblings** ("mirrors legacy order" per the L130-132 comment — but the mounts don't). Rick's B1 ruling (decisions (a)+(b)) wants the **opposite topology**: commons **nested inside** the broadcast card, whole broadcast block at the **TOP**, focus bar beneath. So B1 is not a greenfield add — it **edits freshly-committed, push-held code**. Sequence B1 to fold into / land on top of `4b33ceb7`.

### 4.3 Component / data prereqs
- **B2** rides the **done** F6 `TtsPreviewSliderRenderer` (100% L/B/F per doc 02 §1) — only a *placement* contract is net-new; no component rebuild.
- **B4 is BUILD-GATED on F0/00b (F-Krishna-BD1 — resolves the former OQ-1):** B4's active-id seam CONSUMES F0's `TtsQueueStore.current()` + the `store_tts_queue_changed` event (plan `00b-f0-audiostore-foundation.md`), which are **currently UNBUILT** — B4 cannot land until F0's `TtsQueueStore` + event ship. The active id is **NOT** sourced from `AudioStore` (id-blind per 00b OQ-F0.3) or `SequentialAudioManager` (DELETE per 00b/00c). Legacy tied `is-playing-current` to a list item via the TTS-queue activation path (`notifications.js:14903-14971`); the mux equivalent is the F0 seam. This is a **hard cross-plan dependency**, not a soft one.
- **B3** `#history-dropdown-container` (legacy `notifications.html:666`) is populated by a history-dropdown component; confirm the mux has (or needs) an equivalent before building the slot.
- **INI/endpoints**: commons hydrate `/api/commons/broadcast-history` (CommonsActivityRenderer), `store_session_strip_changed` event (recipient/persona-filter refresh) — both already wired (`boot.ts:452-465`); B1 must preserve them across the re-nest. No NEW endpoints — but **B3's clear-all REUSES the EXISTING `DELETE /api/notifications/bulk/{email}?hours=…` bulk endpoint** (F-Sam-BC1; the original "no new endpoints" was true-but-misleading — it's a reuse, not a no-op).

## 5. Work breakdown

Each bucket: **what · files · ACs (functional + structural) · Oracle tier(s)**. Buckets are ordered; B5 is gated last.

### B1 — Restore section order + re-nest Recent-Activity (restructure `4b33ceb7`)
**What**: Reorder the `multiplexer.html` mount points to legacy top→bottom — Broadcast card (TOP) → Focus bar → Sessions — and re-nest the commons "Recent Activity" chrome *inside* the broadcast card subtree. Restructures the `4b33ceb7` sibling mount (§4.2).

⚠️ **The re-nest is NOT "add a child slot + re-point the mount" — F-Krishna-BA1 (verified against source):** the commons chrome is **STATIC** in `multiplexer.html` (the whole `#commons-activity-pane` section, L146-185: header + 4 filter `<select>`s [window / direction / kind / persona] + refresh button + body/entries/empty), and `CommonsActivityRenderer.mount()` querySelectors *that* static chrome — whereas the broadcast card is **renderer-INJECTED** (`broadcastCard.ts` renders into the empty `#broadcast-card-mount` div, L142). An empty injected mount div cannot host a static child slot, so nesting requires:
- **(a)** relocate the **whole static commons chrome into `broadcastCard.ts`'s rendered template** (so the rendered card *contains* the commons mount root);
- **(b)** flip the **boot.ts mount ORDER** — commons currently mounts (`boot.ts:461-463`) BEFORE broadcast (`:471-473`), so a nested slot would be `null`→throw; it must be **broadcast-FIRST, then commons-into-the-nested-chrome**;
- **(c)** keep the machinery **reuse-as-is** (`store_session_strip_changed` subscription + `/api/commons/broadcast-history` hydrate + the filter/refresh logic — unchanged).

**Files**:
- `static/html/multiplexer.html` — move `#broadcast-card-mount` (**L133**) above `#cc-session-strip` (L81); move `#cc-session-strip` below broadcast; **REMOVE the static `#commons-activity-pane` chrome — exact range `multiplexer.html:138-183` (F-Sam-BA3, pinned vs source)** from its standalone position (it relocates into `broadcastCard.ts`, step (a)). Net target order: `#section-toolbar-mount` → `<h1>` → **broadcast-card-mount (commons chrome now nested inside the rendered card)** → **cc-session-strip (focus bar)** → `#missed-badge-mount` → `#notifications-pane` (sessions) → `#jobs-pane` → fleet/task/tts panes.
- `multiplexer/render/templates/broadcastCard.ts` — **host the relocated commons chrome** as a nested subtree (the `#commons-activity-pane` header + 4 selects + refresh + body/entries/empty), mirroring legacy nesting (`notifications.html:740` inside `:692`); CommonsActivityRenderer then querySelectors the chrome from *within* the rendered card. **Placement constraint (F-Sam-BA1):** nest the commons subtree **OUTSIDE `#broadcast-recipients-row`** — that row is surgically cleared by `BroadcastCardRenderer.ts:327` `row.replaceChildren()` on every recipient refresh, so commons nested inside it would be wiped; place it as a sibling of the recipients-row within the card so it survives recipient-row updates.
- `multiplexer/boot.ts:456-484` — **FLIP the mount order to broadcast-FIRST, then commons** (`broadcastCardRenderer.mount` before `commonsActivityRenderer.mount`), and **shift the commons mount lookup from a page-load `document.getElementById` to a DYNAMIC post-mount lookup (F-Sam-BA2): after `broadcastCardRenderer.mount()` returns, `querySelector` the commons mount root from the LIVE rendered broadcast subtree** — the root is now renderer-PRODUCED (not static), so a page-load `getElementById` would resolve `null`→throw. Preserve the `store_session_strip_changed` subscription + `/api/commons/broadcast-history` hydrate (reuse-as-is).
- `multiplexer/render/BroadcastCardRenderer.ts` / `CommonsActivityRenderer.ts` — adjust mount wiring to the nested chrome; the renderer logic (filters / hydrate / refresh) is reused unchanged.

**ACs**:
- *Structural*: mount document-order in `multiplexer.html` is broadcast → focus-bar → sessions (DOM-spine assertion, Oracle T1). The commons chrome is a **descendant of the rendered broadcast card**, not a standalone sibling (T1 parent assertion). **boot.ts mounts broadcast BEFORE commons** — assert the order so the nested mount target exists (no null-mount throw); the commons root is acquired by a **post-mount `querySelector` on the live rendered subtree** (F-Sam-BA2), not a page-load `getElementById`. The nested commons is placed **outside `#broadcast-recipients-row`** and **survives a recipient-row refresh** (a test refreshes recipients → `row.replaceChildren()` runs → commons subtree still present — F-Sam-BA1).
- *Functional*: broadcast send still works; Recent-Activity still hydrates from `/api/commons/broadcast-history`; the 4 filter selects + refresh still drive CommonsActivityRenderer from the nested chrome; persona-filter still refreshes on `store_session_strip_changed`; focus toggle + hide-inactive toggle on the strip still function in their new position.
- *Conflict-resolution*: the change lands as an edit folded into / on top of `4b33ceb7` (push-held), with §4.1 pre-req checks passed.
- *Coverage (F-Cheech-BA2)*: `broadcastCard.ts` now hosts the relocated ~40-line commons chrome (net-new coverable) → `c8 --100` lines/branches/functions on `broadcastCard.ts` + the boot mount-wiring changes (the sibling renderers already carry coverage).
- *Ownership (F-Cheech-BA1)*: every AC above is **EXECUTOR: AI** — the `parityFixture.ts` DOM-spine assertions, the broadcast-before-commons null-throw test, the post-mount `querySelector` check, and the recipient-row survival test are all Claude-run (no human).
- **Oracle**: **T1 (DOM-contract — primary gate)** for order + nesting + boot mount-order; T0 (CSS-hash unchanged where untouched); T3 (geometry) deferred to B5.

### B2 — Relocate TTS-preview slider into the B3 section header (DEPENDS ON B3)
**What**: Move the F6 `#tts-preview-slider-mount` (orphan sibling at `multiplexer.html:101`) into the **B3 section-header chrome** (above the focus bar), matching legacy's placement (`notifications.html:641-665`, right-aligned via `margin-left:auto`). **Sequencing (F-Krishna-BB2): B2 DEPENDS ON B3** — the destination header must exist before the slider relocates into it; B2 is sequenced **AFTER** B3, not bundled ahead of it (see §9 correction).

**Destination collapse behavior (F-Sam-BB1, round-2 RE-FOLD — supersedes Krishna's Stage-1 BB1):** B2 is **genuinely PLACEMENT-ONLY — NO `ttsPreviewSlider.ts` edit, NO `stopPropagation`.** Source-verified (Sam Stage-2, re-confirmed): (1) the slider has **only an `input` listener** (no click/pointer) → an input-`stopPropagation` is a no-op for collapse anyway; (2) mux section-collapse is a **SEPARATE delegated `.toolbar-btn` click on `#section-toolbar-mount`** (`SectionToolbarRenderer.ts:81-119` — fires only on `.toolbar-btn` / collapse-all / expand-all), NOT a click-anywhere-on-header toggle; (3) there is **NO mux handler that collapses on a header-child click** (legacy's guard `notifications.js:1726/1741` is legacy-only). So a slider interaction has **no bubble path to any collapse handler** — a guard would protect a non-existent path and its test would be **vacuous**. Krishna's Stage-1 BB1 was legacy-true but mux-false; dropped. (Converges with 01-D BD4 — the mux delegated-collapse architecture moots the `stopPropagation` concern plan-wide.)

**Files**:
- `static/html/multiplexer.html` — relocate the `#tts-preview-slider-mount` div from L101 into the B3 section-header chrome (above `#cc-session-strip`); keep the id stable.
- `multiplexer/boot.ts:478-484` — update the mount lookup only if the element id/position changes (id kept stable → no renderer change needed).
- **NO `ttsPreviewSlider.ts` / `TtsPreviewSliderRenderer.ts` change — genuinely placement-only (F-Sam-BB1).** The F6 component is done; the relocation is a pure HTML mount move.

**ACs**:
- *Structural*: `#tts-preview-slider-mount` is a child of the B3 section-header region above `#cc-session-strip`, not a standalone sibling (T1).
- *Functional*: slider still reads/writes the StorageService-backed fraction with the INI default seed (`boot.ts:478-483` seed path preserved). (No collapse-guard AC — F-Sam-BB1: no slider→collapse bubble path exists in the mux, so no guard and no vacuous test.)
- *Coverage*: **n/a — placement-only** (id kept stable → no renderer change → no net-new coverable TS); F6 component coverage unchanged.
- *Ownership*: every AC above is **EXECUTOR: AI** (placement T1 + storage/functional assertions run via the parity harness + unit tests; plan-wide EXECUTOR sweep).
- **Oracle**: T1 (placement) + T2 (computed-style: right-alignment / `margin-left:auto`) at B5.

### B3 — Build section-header controls + notification-filter feature (count · filter-badge · history-dropdown · clear-all)

> **🟢 SCOPE EXPANSION — RICK RULING (2026-06-27, ratified): BUILD filtering now (chose build-now over defer; user prerogative).** This SUPERSEDES the former OSQ-B3.1 defer — the filter-badge is now IN scope and B3 authors the full notification-filter feature (design below). NET-NEW design (not a fold); the Manager runs a 3-lens DELTA-RE-REVIEW (Krishna reuse → Sam fitness → Cheech ownership) over the expanded B3 before it closes.
**What**: Build the section-header control cluster the mux lacks. Doc 01 §2 marks these "0 refs" in mux; `#section-toolbar-mount` (L72) is a *different* mechanism (per-section visibility). ✅ **confirm-absent-not-renamed PASSED** (Krishna Stage-1: grep confirmed no filter-mode in `render/`/`stores/` + no bulk-clear in NotificationStore). **Reuse, don't greenfield (F-Krishna-BC1a):** mirror the EXISTING header-chrome idiom already built at `TaskListRenderer` (`:5-6`) + `FleetStatusRenderer` (`:13`) — *title · count · ⟳ refresh · updated-stamp · container* — rather than inventing a new chrome shape.

**Files**:
- `static/html/multiplexer.html` — add a section header above `#notifications-pane` carrying: count span, **filter-badge (`#notifications-filter-badge`) — now IN scope per the Rick ruling**, history-dropdown container, clear-all button (legacy ids `#notifications-count`, `#notifications-filter-badge`, `#history-dropdown-container`, `#clear-all-notifications` — port verbatim where the mux delegated-listener convention allows, or document the rename).
- **NEW** renderer under `multiplexer/render/` mirroring the `TaskListRenderer`/`FleetStatusRenderer` chrome idiom (BC1a) to drive count state from the NotificationStore + wire clear-all + history-dropdown. **Count semantics (F-Sam-BC3):** `#notifications-count` pins to the **active-list TOTAL** (`NotificationStore.list().length` `:242`), NOT `unreadCount()` (`:250`) — matching legacy (`notifications.js:15229/15294`). **OSQ-B3.2 (cosmetic):** confirm at build whether action-required items count toward the total (vs legacy).
- `multiplexer/stores/NotificationStore.ts` — **NEW `clearAll()` method + event emission (F-Krishna-BC3):** clear-all is NOT UI-only — the store today has only `markRead()` (`:119/:254`) + `history()` (`:115/:246`), **no bulk-clear**. Add `clearAll()` (empties the active list, emits `store_notifications_changed`) with **100% L/B/F coverage on the new branch**. **Emission changeKind + full re-render (F-Sam-BC2):** `clearAll()` emits a changeKind that `NotificationsListRenderer` handles with a **full `renderAll()`** arm (`renderAll()` `:245` = `renderActionRequiredSection` + `renderSenderSection`) — an unhandled/keyed changeKind empties the store but leaves STALE CARDS on screen; add a test (clear → zero cards rendered).
- **clear-all is a SERVER bulk-DELETE, not client-only (F-Sam-BC1):** legacy `clearAllNotifications` (`notifications.js:15206`) is async — a `confirm()` "cannot be undone" guard → `DELETE /api/notifications/bulk/{email}?hours=…` → THEN clears local. A client-only clear → notifications **REAPPEAR on reload**. B3's clear-all calls the **EXISTING bulk endpoint** + a `confirm()` guard, THEN `NotificationStore.clearAll()`. ⚠️ **FILTER COUPLING — RESOLVED by the Rick ruling (build filtering):** clear-all now scopes to the ACTIVE filter (NOT a safe-default) — see the NOTIFICATION-FILTER FEATURE block + **OSQ-B3.6** (sender-scope `own/others/all` vs the endpoint's `?hours=` axis reconciliation, for the delta-re-review).
- **history-dropdown (F-Krishna-BC1b / OQ-4 RESOLVED):** PORT the legacy history-dropdown UI (`notifications.js:14428-14552`) and **REUSE the existing data path** — `NotificationStore.history()` (`:246`) + the cold-load hydration already present; do NOT build a from-scratch history store.
- `multiplexer/boot.ts` — mount + wire.
- `static/css/shared/notifications-surface.css` — section-header styles + filter-badge/filter-control styles (port from `notifications.css`, B5).

**🆕 NOTIFICATION-FILTER FEATURE (Rick scope-expansion ruling 2026-06-27 — NET-NEW, supersedes OSQ-B3.1):**

> ⚠️ **AXIS FROZEN PENDING RICK RULING (F-Sam-BCd1 FOUNDATIONAL, escalated 2026-06-27):** Sam's delta Stage-2 found that **`own/others` filtering is NOT client-implementable** — the mux `Notification` payload carries no owner/recipient discriminator, and `own/others` is a multi-user-admin concept that is **vacuous in the single-user mux**. ESCALATED to Rick (manager rec: reframe to a **mux-native axis — sender-persona / direction**). The `filterMode` modes + `matchesNotificationFilter` **AXIS below is FROZEN** until Rick rules. The surrounding MECHANISM is axis-INDEPENDENT and stands: `visibleEntries()` render, the filter-badge, `isFilterActive()`, the filter-aware empty-state, clear-all delete-by-id, and StorageService persistence all work for whatever axis Rick chooses.
- **Reuse anchor (structural template):** mirror the proven `CommonsStore` filter pattern (`stores/CommonsStore.ts`) — raw entries + a filtered view getter + a `filter` state (defensive-copy getter) + a `matchesFilter()` predicate + StorageService-persisted `hydrateFilter()` (schemaVersion envelope, `STORAGE_KEY`) + an `isFilterActive()` flag for empty-state copy. (Different subsystem; same shape.)
- **Port source (modes + badge):** legacy notification filter `notifications.js:6235-6315` — modes **`own | others | all`** (validModes `:6304`); badge text `${icon} ${label}` + `data-mode` attr (`:6240-6246`); persisted to a localStorage pref key (legacy `QUEUE_FILTER_PREF_KEY`); **admin-gated** — admins get the filter panel + badges, regular users locked to `own` default (`:6300-6312`). **Scope divergence (BC2-d4):** legacy `:6235-6315` is the UNIFIED notifications+QUEUES filter (two badge locations, one shared mode); **B3 NARROWS it to NotificationStore-only** (correct — the mux queues are a separate surface). We port the modes + badge semantics, NOT the queue coupling.
- **`NotificationStore` filter-mode (NET-NEW):** add `filterMode ∈ {own,others,all}` + `setFilterMode(mode)` (emits `store_notifications_changed` with a full-`renderAll` changeKind) + a filtered `visibleEntries()` getter (`active.filter(n => matchesNotificationFilter(n, mode))`) leaving `list()` as the raw total + a `matchesNotificationFilter(n, mode)` predicate (own = recipient is me, others = not-me, all = pass) + StorageService persistence (mirror `hydrateFilter()`) + an `isFilterActive()` flag. **100% L/B/F on every mode branch + the predicate.**
- **`NotificationsListRenderer` applies the filter (BCd3, axis-independent):** render from **`visibleEntries()` (NOT raw `list()`)**; re-render on the `setFilterMode` changeKind (the same full-`renderAll` arm as clear-all, F-Sam-BC2). **Filter-aware empty-state (BCd3):** when `isFilterActive()` AND `visibleEntries()` is empty, render a filter-specific empty message ("no notifications match this filter") distinct from the unfiltered empty state — keys on the `isFilterActive()` flag, so it works for whatever axis Rick chooses.
- **Filter-badge (un-deferred):** `#notifications-filter-badge` reflects the active mode (`${icon} ${label}`, `data-mode`); clicking opens the filter control (port legacy `showAndScrollToFilterPanel` intent, or an inline header control — OSQ-B3.3).
- **Filter control + admin-gating (ported):** the own/others/all selector; filter UI + badge show for admins, regular users default to `own` (legacy `:6300-6312`).
- **clear-all is now FILTER-SCOPED (supersedes the F-Sam-BC1 safe-default):** the server bulk-DELETE scopes to the ACTIVE filter rather than a blanket window (see OSQ-B3.6 for the axis reconciliation).
- **Coverage + ownership:** 100% L/B/F + **EXECUTOR: AI** on the entire new filter surface (filterMode branches, `matchesNotificationFilter`, badge render, renderer filter-application, clear-all scoping) — Convention-3/6.

**Open sub-questions for the DELTA-RE-REVIEW (net-new surface):**
- **OSQ-B3.3 (UI shape):** legacy filter PANEL (`filter-settings-section` + `showAndScrollToFilterPanel`) vs an inline mux header control — confirm.
- **OSQ-B3.4 (admin-gating):** preserve legacy's admin-only filter (regular users locked to `own`), or expose to all mux users? (Rick ruled "build it"; the gating scope is a sub-decision.)
- **OSQ-B3.5 (count under filter):** `#notifications-count` = filtered `visibleEntries().length` when a filter is active, or always the raw total? (Refines F-Sam-BC3.)
- **OSQ-B3.6 (clear-all axis reconciliation — LOAD-BEARING; sharpened per Krishna BC2-d5):** filter modes are SENDER-SCOPE (`own/others/all`) but the legacy bulk endpoint is `DELETE /api/notifications/bulk/{email}?hours=` (a TIME window) — DIFFERENT axes. Krishna grepped the server route — **INCONCLUSIVE**, so do NOT assume clean reuse. **Safe mux-only path (PREFERRED): client-side delete-by-id over `visibleEntries()`** — each id deleted server-durably (no reappear-on-reload), no server change, scopes exactly to the active filter. **Per-id endpoint VERIFIED (BCd2): `DELETE /api/notifications/{id_hash}` (`notifications.py:1761` → `delete_by_id_hash`, server-durable; the bulk `?hours=` endpoint is `:1813`)** — so the delete-by-id path needs NO server change. **Partial-failure handling (BCd2):** the loop is N INDEPENDENT (non-atomic) DELETEs — track per-id success; on ANY failure do NOT claim a full clear, keep the failed ids in the store (re-sync from server state), surface the partial result; 100% L/B/F on the partial-failure branch. **Alternative (CROSS-STACK if later chosen): add a sender-scope/mode param to the bulk endpoint** — flag as a server/cross-stack change, NOT a mux-only fold. (This is where "?hours= maps to the active filter window" gets made concrete.)

**ACs**:
- *Pre-req*: ✅ documented grep PASSED — each control **absent (not renamed)** (methodology §5 step 5); NotificationStore confirmed to lack a bulk-clear (drives BC3); render/stores confirmed to lack any filter-mode (drives the BC2 OSQ defer).
- *Functional*: count reflects the live active-list TOTAL (`list().length`, F-Sam-BC3); clear-all → `confirm()` guard → `DELETE /api/notifications/bulk` (existing endpoint, **active-filter scope per OSQ-B3.6**) → `NotificationStore.clearAll()` (emits the full-`renderAll` changeKind) → **zero cards rendered AND no reappear-on-reload** (F-Sam-BC1/BC2); clear-all disabled when empty (per legacy `disabled` default at `notifications.html:678`); history-dropdown opens/loads from `NotificationStore.history()` (ported legacy UI).
- *Functional (filter — Rick ruling)*: `setFilterMode(own/others/all)` re-renders the list from `visibleEntries()` (own = mine, others = not-mine, all = all); `#notifications-filter-badge` reflects the active mode (`icon+label`, `data-mode`); the mode persists across reload (StorageService); admin-gating per OSQ-B3.4; clear-all deletes the active-filter scope (OSQ-B3.6). **100% L/B/F + EXECUTOR: AI** on the filter surface.
- *Structural*: header sits above the focus bar / sessions per legacy order (T1), with the B2 slider slotted in (right-aligned); the header chrome matches the `TaskListRenderer`/`FleetStatusRenderer` idiom (BC1a).
- *Coverage*: `NotificationStore.clearAll()` + its emission at `c8 --100` (BC3).
- *Ownership*: every AC above is **EXECUTOR: AI** (count / clear-all / history-dropdown assertions + the `clearAll()` unit tests are Claude-run; plan-wide EXECUTOR sweep).
- **Oracle**: T1 (presence + order) + T2 (computed-style) at B5.

**OSQ-B3.1 — ✅ SUPERSEDED by the Rick scope-expansion ruling (2026-06-27): BUILD filtering now.** The former defer is RETIRED; the filter feature is authored above (NET-NEW). Remaining filter sub-questions are tracked as OSQ-B3.3..B3.6 for the delta-re-review. (Original F-Krishna-BC2 was a correct no-pre-decide flag; Rick decided BUILD — user prerogative.)

### B4 — Per-message active-TTS-gated ⏸/⏹ + proxy-ratify-link
**What**: Add `notification-corner-pause-btn` (⏸/▶) + `notification-corner-stop-btn` (⏹) + proxy-ratify-link to **every** message bubble, but **CSS-gate visibility to the single actively-spoken message** — replicating legacy render-all + `is-playing-current` gate. **A static per-bubble render is the wrong implementation** (`00-index.md` §B4, decision (c)).

**Files**:
- `multiplexer/render/templates/notificationItem.ts:60-110` — render the two corner buttons (`display:none` by default) into both the flat and progress-group branches; render the proxy-ratify-link when `progress_group_id` starts with `pr-` (legacy gate `notifications.js:7168`).
- `multiplexer/render/NotificationsListRenderer.ts` — (1) **extend the EXISTING delegated `closest()` click handler (`:428-453`)** with corner-⏸/⏹ + ratify-link branches (early-return, mirroring the progress-group-toggle branch `:433-444`): pause-btn toggles `is-paused-current` on its `.sender-message` + calls `AudioStore.pause()/resume()`; stop-btn calls `AudioStore.stop()`; ratify-link calls the proxy-ratify wrapper (BD3). **No `stopPropagation` guard needed (F-Krishna-BD4):** collapse is a single delegated `closest()` handler and the corner buttons render into `.sender-message` (accordion BODY) — `closest('.date-accordion-header')`/`closest('.progress-group-toggle')` resolve `null` for a corner-btn click, so it cannot toggle a collapse. (2) an **active-TTS class driver** with TWO subscriptions (F-Sam-BD2): **`store_tts_queue_changed` = IDENTITY** (which bubble) and **`store_audio_state_change` (`shared/types.ts:76`, id-blind) = authoritative playing/paused glyph** for the current bubble — so a pause/resume that does NOT change `current()` (e.g. the global TTS-chrome-bar pause) still refreshes the glyph instead of going stale. On either event the driver runs **CLEAR-PRIOR-THEN-SET (F-Sam-BD1 — so exactly one bubble ever lights):** `querySelectorAll` the active classes (`is-playing-current`/`is-paused-current`/`tts-playing`) → **remove from ALL**; then if `TtsQueueStore.current()` is non-null, add them to the bubble whose `Notification.id_hash` (`shared/types.ts:333`) === `current()` (playing-vs-paused from `store_audio_state_change`); if `current()` === null, leave all cleared. *Identity* comes from the F0 seam; the audio-state side stays id-blind (legacy analog `notifications.js:14903-14971`).
- **Active-id source = F0 seam, NOT AudioStore (F-Krishna-BD1 — build-forward alignment to ratified 00b/00c):** B4 CONSUMES the active id from `TtsQueueStore.current()` via `store_tts_queue_changed` — it does **NOT** extend `AudioStore` (id-blind per 00b OQ-F0.3) and does **NOT** build on `SequentialAudioManager` (DELETE per 00b/00c). Reuse the `is-playing-current`/`is-paused-current` class vocabulary already in `templates/ttsChrome.ts:67-69`.
- **Cross-section: stop→`current()`-clear is an F0 obligation, NOT a B4 call (F-Cheech-BD1):** on `stop()`, `AudioStore` emits `store_audio_state_change{idle}` (id-blind, verified `AudioStore.ts:239-250` → STOP_REQUESTED→idle); **F0's `TtsQueueStore` (00b) subscribes to that and clears `current()`→null** (id-blind, same shape as its `store_audio_ended` self-advance), which de-lights the bubble through the F0 seam. NOT a 00c change (00c stays CLOSED), NOT a B4 call — track alongside the F0 back-fold task `2605dca5`.
- `multiplexer/.../apiClient` — **proxy-ratify endpoint (F-Krishna-BD3 / OQ-5 / ✅ OSQ-B4.1 RESOLVED — Sam traced it):** legacy `createProxyRatifyLink` (`notifications.js:7161`) = **`POST /api/proxy/acknowledge`** + `window.open('/app/admin/proxy-ratify')`. B4 adds a **thin apiClient wrapper for the acknowledge call ONLY** (`POST /api/proxy/acknowledge`); the **page-open (`window.open('/app/admin/proxy-ratify')`) is renderer-side** (the ratify-link click handler), NOT in the apiClient. No build-time trace remaining.
- `static/css/shared/notifications-surface.css` — the gate selectors (B5): `.sender-message.tts-playing .notification-corner-pause-btn{display:inline-flex}` etc. (port from `notifications.css:381-438`; note `.sender-message.tts-playing` already exists there as the non-`<li>` hook).

**ACs**:
- *Structural*: every `.sender-message` carries the two corner buttons in the DOM (T1), but only the bubble matching the active TTS message has them visible (computed `display` ≠ none — T2). Proxy bubbles (`pr-` group) carry the ratify-link; non-proxy bubbles do not.
- *Functional*: when TTS reaches a message, exactly that bubble's ⏸/⏹ become visible; pause flips ⏸↔▶ via `is-paused-current` and pauses/resumes audio; **stop halts playback (`AudioStore.stop()` → `idle`, emitting `store_audio_state_change{idle}`) and the bubble DE-LIGHTS when `current()` CLEARS via the F0 seam — NOT a B4-driven advance (F-Cheech-BD1)**; ratify-link calls `POST /api/proxy/acknowledge` (apiClient wrapper) + opens the ratify page (renderer-side `window.open`). **Distinguish STOP = halt + `current()`-clear (de-light) from NATURAL-ENDED = advance-to-next** (`store_audio_ended` → `TtsQueueStore` self-advances; `stop()` does NOT emit `store_audio_ended` — 00c P6-c fires it on natural completion ONLY — so the advance is correctly UNWIRED to stop). **Exactly one bubble lights at a time via CLEAR-PRIOR-THEN-SET (F-Sam-BD1)**; a `store_audio_state_change` pause/resume that does NOT move `current()` still refreshes the current bubble's glyph (F-Sam-BD2) (Rick's one-bubble constraint, `00-index.md` §B4).
- *COND-2 ownership boundary (F-Cheech-BD1 — mirrors 00c-B's "P6 makes zero calls into TtsQueueStore")*: B4 calls only `AudioStore` mutators (`pause`/`resume`/`stop`) + reads `TtsQueueStore.current()` **READ-ONLY** — **ZERO `TtsQueueStore` mutator / `advance()` / `clear()` calls** (assert by a test counting `TtsQueueStore` mutator invocations === 0).
- *Negative*: a static-render regression (controls visible on non-active bubbles) **fails** the suite — and the test runs across a `current()` **TRANSITION** (bubble A active → A cleared + B set), not just initial render (F-Sam-BD1), so a missing clear-prior is caught.
- *Coverage*: `c8 --100` on the `notificationItem.ts` corner-button + ratify-link render branches, the `NotificationsListRenderer` delegated-handler additions (pause/stop/ratify branches), the active-TTS class driver **including the clear-prior loop, the `current()`-non-null SET branch, the `current()`===null all-cleared branch, AND the `store_audio_state_change` glyph-refresh arm (F-Sam-BD1/BD2)**, and the apiClient `POST /api/proxy/acknowledge` wrapper.
- *Ownership*: every AC above is **EXECUTOR: AI** — corner-control presence/visibility (T1/T2 via the parity harness), the `store_tts_queue_changed` (identity) + `store_audio_state_change` (glyph) driven class-gate with clear-prior (injected stores + events), the click-delegation behavior, and the negative cross-transition static-render test are all Claude-run; plan-wide EXECUTOR sweep.
- **Oracle**: T1 (presence on all bubbles) + **T2 (the active-gate: visible only on `.tts-playing`)** — the load-bearing tier for B4 + T3 (corner geometry) at B5.

### B5 — CSS / Oracle Tier-2/3 pass + golden rebaseline (gated last)
**What**: Single-source the CSS for everything B1–B4 touched into `css/shared/notifications-surface.css` (never fork a copy — mandate 3) — **a MOVE, not an ADD (F-Krishna-BE1, range corrected per F-Sam-BE1): DELETE the COMPLETE corner-control gate from the `notifications.css` monolith (`~:381-501` — pause `:381-438` + is-tts-pending `:439-463` + STOP `:464-501`; the stop block sits OUTSIDE the originally-cited `:381-438`, so deleting only that would leave `.notification-corner-stop-btn` DOUBLE-DEFINED) as the rules move**, else double-definitions + a cascade-order hazard (the shared sheet loads at `notifications.html:13`; if the monolith loads AFTER and redefines, it WINS and the shared copy is silently dead for legacy). Run the Tier-2/3 pass; rebaseline goldens. Gated on B1–B4 structural ACs green (methodology §5 steps 7–8 — CSS diff only after DOM is clean).

**Files**:
- `static/css/shared/notifications-surface.css` — port (MOVE) the corner-btn gate (`notifications.css:381-438`), the section-header cluster styles, and the `cc-tts-fraction` slider placement styles into the shared sheet; legacy + mux both consume it (legacy `notifications.html:13` + mux `multiplexer.html:15` both link it — verified).
- `static/css/notifications.css` — **BE1: DELETE the EXACT moved ranges** (F-Sam-BE1, pinned vs live CSS) — the complete corner-control gate `~:381-501` (pause `:381-438` + is-tts-pending `:439-463` + stop `:464-501`), the `cc-tts-fraction` CLASS block `:2410-2439` (BE2 below) — so each rule has exactly ONE definition. NOT "any other moved rules" (too vague for a MOVE — pin every range).
- **Section-header cluster — ENUMERATED, not vague (F-Cheech-BE1):** grep shows the legacy notification controls inherit the GENERIC shared `.section-header` / `.section-header h3` / `.section-header-actions` block (`notifications.css:126-160`), which is **SHARED across sections** (`#action-required-section .section-header` `:644`, `#tts-queue-section …` `:3125+`) — so it is **NOT a B3-only move**: single-sourcing the generic block is a broader decision (if moved, EVERY `.section-header*` consumer is verified; otherwise LEAVE the generic block in place and single-source only B3-specific rules). grep found **NO dedicated `#notifications-count` / `#notifications-filter-badge` / `#history-dropdown-container` / `#clear-all-notifications` rule block** in `notifications.css` → confirm at build whether any notification-specific section-header rule actually exists to move. **B3's NET-NEW filter-badge/filter-control selectors are authored DIRECTLY in the shared sheet (not moved-from-monolith), so they are trivially residual-free.**
- **B5↔B3 coupling (gate — F-Cheech-BE1):** B5 is gated-LAST on B1–B4 structural-green, so it pins the section-header delete/port list against B3's **FINALIZED** selector set (count + filter-badge + filter-control + history-dropdown + clear-all, post-filter-expansion) — NOT a pre-filter guess.
- **BE2 — `cc-tts-fraction` class divergence (F-Sam-BE2):** legacy markup uses CLASS rules `.cc-tts-fraction-control`/`.cc-tts-fraction-slider`/`.cc-tts-fraction-value` (CLASS block `notifications.css:2410-2439`); the mux F6 slider renders `.tts-preview-slider-value`/`.tts-preview-slider-input` AND reuses `cc-tts-fraction-slider`/`-value` only as **IDs** (`TtsPreviewSliderRenderer.ts:136-137`) — so the legacy CLASS rules do NOT reach the mux slider. **Resolution: the shared sheet carries BOTH class families** (legacy `.cc-tts-fraction-*` CLASS rules + mux `.tts-preview-slider-*` rules sharing the same declarations) — a CSS-only reconciliation touching neither the done F6 renderer markup nor the legacy markup, so the mux slider actually benefits from single-source. (The corner-btn `.sender-message.tts-playing` hook is already mux-friendly → ports clean; the slider is the only divergent case.)
- Golden captures under the Oracle fixtures (legacy `:8000` capture cost per mandate 2).

**ACs**:
- *Structural*: zero forked CSS — all new selectors live in the shared surface; legacy + mux both consume it. **The moved rules are DELETED from the `notifications.css` monolith (BE1) — no double-definition.**
- *Sole-definition (F-Krishna-BE1 + F-Sam-BE1 + F-Cheech-BE1 — replaces the load-order AC)*: a **grep-NO-RESIDUAL** assertion — ZERO copies of each moved rule remain in the `notifications.css` monolith: the corner-control gate `~:381-501`, the `cc-tts-fraction` CLASS block `:2410-2439`, **and the section-header rules ACTUALLY moved (per the enumerated/gated list above — the generic `.section-header*` block `:126-160` ONLY if the move-it-shared decision is taken; B3's net-new filter selectors are shared-sheet-native, so trivially residual-free)** — exactly ONE copy in the shared sheet, consumed by both legacy + mux. (Grep is stronger than a `<link>`-order check: it proves no double-definition exists at all, not merely that order would mask one.)
- *Slider single-source (F-Krishna-BE2)*: the shared sheet styles BOTH the legacy (`.cc-tts-fraction-control`/`#cc-tts-fraction-slider`) AND mux (`.tts-preview-slider-value`/`.tts-preview-slider-input`) class families — assert the mux slider's computed style now resolves from the shared sheet (T2).
- *Visual*: T2 computed-style parity on header controls, slider alignment, corner-btn states; T3 geometry parity on the reordered section (broadcast/focus/sessions) and corner-btn placement; T4 pixel backstop only where T0–T3 leave residual.
- *Rebaseline*: new goldens captured for the reordered section + per-message corner controls; the two documented CC-card carves (doc 02 §4 — AR read-only↔interactive node; CC-card ~51px height re-scoped into F5-v) remain carved, not regressed.
- *Ownership*: every AC above is **EXECUTOR: AI** — the load-order/single-source assertions + the T0–T4 Oracle sweep + the golden rebaseline (`--update-snapshots`, :8000-scheduled) are all Claude-run. (If a subjective pixel residual ever needs human eyes, that one sign-off is EXECUTOR: HUMAN — named, not silently deferred; the methodology Oracle is otherwise mechanical.)
- **Oracle**: T0→T4 full sweep; this bucket is where T2/T3/T4 gate.

## 6. Test strategy & venue routing

Inherits mandate 1 (**100% lines/branches/functions** — Python `pytest --cov-fail-under=100`; TS `c8 --100`; `# pragma: no cover` / `c8 ignore` only for genuinely-unreachable defensive branches with a same-line reason) and mandate 4 (venue routing).

- **:7999 (AI-discretionary)** — TS unit tests for every new/edited renderer + template (B1 mount-nesting, B2 placement, B3 header renderer, B4 corner-control template + class driver + click delegation); `c8 --100` on lane-owned files; the storeless parity harness (`testkit/parityFixture.ts`) for DOM-spine assertions (Oracle T1); inline `quick_smoke_test`/import-chain + `py_compile` for any Python touched. WebSocket smoke (`src/scripts/run-websocket-smoke-tests.sh`) if B4's audio-state wiring touches the audio WS path.
- **:8000 (scheduled via `POST /api/test-suite/submit`, self-authorized on a verified-idle server — `list-pending` first; never side-door)** — E2E UI + visual regression (`src/scripts/run-e2e-ui-tests.sh --bg`, includes `-k visual`) for the reordered section + active-TTS gate; the integration FINAL gate (`src/tests/run-integration-tests.sh --bg`) before any merge. Golden rebaseline (`--update-snapshots`) after B5 structural green.
- **New fixtures**: a CC-session fixture mounting broadcast(+nested commons) → focus → ≥2 sessions with ≥1 progress group + ≥1 proxy (`pr-`) message + an active-TTS state, to exercise B1 nesting, B3 header, and the B4 single-active gate (methodology §5 step 7 ground-truth render).
- **Negative test (B4)**: assert corner controls are NOT visible on non-active bubbles (guards against the static-render anti-pattern).

## 7. Oracle & visual parity

Tiers per mandate 2 (methodology `2026.06.19-…/01-layout-parity-methodology.md`):
- **T0 CSS-hash** — confirm untouched nodes' CSS unchanged; flag the shared-surface additions (B5).
- **T1 DOM-contract** — **the primary gate for B1/B2/B3** (order + nesting + presence). DOM-spine must include `multiplexer.html` mount order (methodology §2: position is decided by mount order, not the renderer).
- **T2 computed-style** — **the load-bearing gate for B4** (active-TTS visibility gate) + B3 header alignment + B2 slider right-alignment.
- **T3 geometry** — reordered-section geometry + corner-btn placement (B5).
- **T4 pixel backstop** — residual only.

**Golden captures needed** (legacy `:8000` capture cost): (1) reordered CC-section (broadcast-top → focus → sessions); (2) Recent-Activity nested inside broadcast; (3) section-header control cluster; (4) per-message corner controls in the active-TTS state (one bubble visible). Rebaseline gated on B1–B4 structural ACs green (methodology §5 steps 7–8). Preserve the two inherited carves (doc 02 §4).

## 8. Risks & open questions (for reviewers)

- **OQ-1 (load-bearing — B4): ✅ RESOLVED (F-Krishna-BD1 — build-forward alignment to closed 00b/00c).** The active-TTS → notification-id signal is NOT an AudioStore/SequentialAudioManager extension. The active `id_hash` is owned by F0's **`TtsQueueStore.current()`** and published on **`store_tts_queue_changed`** (00b OQ-F0.3 proved the id is inherently client-side; `AudioStore` stays id-blind; `SequentialAudioManager` is DELETE). B4's class driver SUBSCRIBES to `store_tts_queue_changed` and gates the bubble whose `Notification.id_hash` (`shared/types.ts:333`) === `current()`; `AudioStore.state()` drives only the play/pause glyph. The earlier "extend AudioStore" framing (former §3 row + this OQ) is **RETIRED**. B4 is **build-gated on F0 landing** (§4). **F0 obligation extends to STOP (F-Cheech-BD1):** F0's `TtsQueueStore` also subscribes `store_audio_state_change{idle}` → clears `current()`→null (de-lights the bubble on `stop()`), mirroring its `store_audio_ended` self-advance — tracked alongside the F0 back-fold task `2605dca5`. B4 makes ZERO `TtsQueueStore` mutator calls (COND-2 consumer-side mirror of 00c-B).
- **OQ-2 (B1 sequencing): per-card collapse double-ownership.** Doc 02 §1 lists the collapse toggle as owned by *both* DFB Lane B (folded into `4b33ceb7`, "11/11") and D06/Rachel (`mux-section-toolbar-accordion-toggle`, commit-held). §4.1 pre-req #1 must resolve which is canonical **before** B1 restructures the mount, or B1's edit collides with an unmerged collapse owner. Reviewers: confirm the merge order with Rachel + Tiberius.
- **OQ-3 (B1 ↔ `4b33ceb7`): restructuring push-held code.** B1 edits the freshly-committed, push-held `4b33ceb7` to invert its sibling topology (broadcast+commons siblings → commons nested in broadcast, broadcast at top). Safe because unpushed (no history revert), but requires confirming no unpushed follow-up from Tiberius's crew touches the same mounts (§4.2 / doc 02 §6 prereq ii). Reviewers: is folding into `4b33ceb7` preferred over a stacked commit on top of it?
- **OQ-4 (B3): ✅ RESOLVED (F-Krishna-BC1b).** Mux has no history-dropdown UI, but legacy has the full component (`notifications.js:14428-14552`) AND the data path exists (`NotificationStore.history()` `:246` + cold-load hydration). B3 **PORTS the legacy UI + REUSES `NotificationStore.history()`** — not a from-scratch history store. (See §5 B3.)
- **OQ-5 (B4): ✅ RESOLVED (F-Krishna-BD3 + F-Sam-BD3 trace).** Legacy `createProxyRatifyLink` (`notifications.js:7161`) = **`POST /api/proxy/acknowledge`** + `window.open('/app/admin/proxy-ratify')`. B4 adds a thin apiClient wrapper for the **acknowledge call only**; the page-open is renderer-side. **OSQ-B4.1 CLOSED** — endpoint known, no build-time trace remaining. (See §5 B4.)
- **Risk**: B1's mount reorder is a convergence-file edit (`multiplexer.html`, `boot.ts`) — **manager-serial-merge** (mandate 5), not a parallel-lane free-for-all, to avoid colliding with Tiberius's `multiplexer.html` edits.

## 9. Lane decomposition & estimate

Convergence files (manager-serial-merged — mandate 5): `multiplexer.html`, `boot.ts`, `css/shared/notifications-surface.css`. (No `types.ts` union edit from this plan — B4 SUBSCRIBES to F0's existing `store_tts_queue_changed`; it adds no new EventBus event — F-Krishna-BD1.)

| Lane | Buckets | Files (lane-owned vs convergence) | Rough size | Notes |
|---|---|---|---|---|
| **L-order** | B1 | `multiplexer.html`*, `boot.ts`*, `broadcastCard.ts`, `BroadcastCardRenderer.ts`, `CommonsActivityRenderer.ts` | M (mostly mount/nesting rewiring; no new component) | Must land first / fold into `4b33ceb7`; gates the rest. *=convergence |
| **L-header** | B3 (+ filter feature) → then B2 | new section-header renderer + template, NEW `NotificationStore.clearAll()` + `filterMode`/`visibleEntries()`/`matchesNotificationFilter` (Rick scope-expansion), filter-badge + control, `multiplexer.html`*, `boot.ts`* | **L** (was M–L; +notification-filter feature NET-NEW + delta-re-review) | **B2 DEPENDS ON B3 (F-Krishna-BB2)**: the section-header must exist before the slider relocates into it — B3 builds the header, THEN B2 moves the slider in. **B2 is placement-only — no `ttsPreviewSlider.ts` edit (F-Sam-BB1).** (Corrects the prior reversed "L-header depends on L-order" note.) |
| **L-tts** | B4 | `notificationItem.ts`, `NotificationsListRenderer.ts` (extend delegated `:428-453` handler), apiClient (proxy-ratify wrapper), CSS (B5) | L (**gated on F0/00b landing**, not an AudioStore extension) | Heaviest; **build-gated on F0's `TtsQueueStore`/`store_tts_queue_changed`** (F-Krishna-BD1) — NO `AudioStore`/`SequentialAudioManager` edit |
| **L-css** | B5 | `css/shared/notifications-surface.css`* | S–M (port + rebaseline) | Gated LAST on L-order/L-header/L-tts structural-green |

**Sequencing**: L-order (B1) → L-header (**B3 → then B2**) ∥ L-tts → L-css. Pre-req checks §4.1 block L-order. **B2 is sequenced AFTER B3 within L-header (F-Krishna-BB2 — B2 depends on B3's header; corrects the prior reversed dependency note).** Total: a medium-sized multi-lane effort dominated by B4's F0-seam consumption (build-gated on F0/00b's `TtsQueueStore`/`store_tts_queue_changed`, F-Krishna-BD1) and B1's `4b33ceb7` restructure coordination.

**Doc touchpoints** (mandate 7): on completion update `00-index.md` (mark B1–B5 closed), this folder's discrepancy ledger, `src/docs/websocket-events.md` is UNAFFECTED (B4 adds no EventBus event — it subscribes to F0's `store_tts_queue_changed`; F-Krishna-BD1), and the layout-parity methodology golden manifest with the new captures. No `lupin-app.ini` keys anticipated.
