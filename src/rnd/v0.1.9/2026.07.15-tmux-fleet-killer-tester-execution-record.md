# Tester Execution Record — tmux fleet-killer fix, §7 matrix (third leg)

**Date**: 2026-07-15 (run window 00:12–00:16 EDT)
**Tester**: Cheech 🌿 (SWE-team Tester seat, session 7e383179)
**Manager**: Mr. Radio 🦉 (GO received 00:11:54 EDT, thread 9a858343; third leg ACCEPTED 00:17:53 EDT)
**Spec**: `2026.07.14-tmux-fleet-killer-vertex-taint-test-isolation-leak-fix-plan.md` §6/§7/§10-5 + `2026.07.15-cascade-tmux-fleet-killer-revision-handoff.md` §4/§7
**Venue**: all layers `:7999`-eligible / local pytest, per plan §7 — no `:8000` scheduling. Run genuinely **from inside a fleet pane** (`TMUX=/tmp/tmux-1001/default,387516,13`).
**Env**: `LUPIN_CONFIG_MGR_CLI_ARGS` exported (Testing block) for every pytest invocation.

## Verdict: §7 matrix GREEN — all layers pass; one named, by-design env-skip; fleet untouched.

## Results

| Layer | Command | Result |
|---|---|---|
| Compile | `py_compile` on all 9 changed `.py` + `bash -n src/scripts/start-cc-with-tmux.sh` | 10/10 OK |
| Unit — AC5 precedence canary | `pytest src/tests/unit/test_tmux_socket_precedence.py -v` | 2/2 PASS |
| Unit — helper module | `pytest src/tests/unit/test_tmux_isolation.py -v` | 6/6 PASS |
| Unit — probe library | `pytest src/tests/unit/test_killtrace_probe.py -v` | 12/12 PASS |
| Regression — AC2 (under armed guard) | `pytest src/tests/smoke/test_tmux_fleet_socket_isolation_guard.py -v` | 1/1 PASS |
| Smoke — killtrace-observation probe | `pytest src/tests/smoke/test_killtrace_observation_probe.py -v` | 1/1 PASS (instrument receipt below) |
| §10-5 normative gate | fresh pre-capture `tmux -S /tmp/tmux-$(id -u)/default display-message -p '#{pid}'` | SUCCEEDED → 387516 |
| Smoke — AC1/AC4 pane-run | `pytest src/tests/smoke/test_vertex_launcher_server_taint.py -v` from `$TMUX`-set pane | 4 PASS / 1 SKIP; **fleet pid 387516 pre == post** |
| Coverage — AC3 | unit+smoke combined with `--cov-branch`; conftest via `coverage run --branch --source=src/tests/smoke -m pytest` | 100% L/B/F on all changed code (below) |
| Combined re-run (pid-bracketed) | all 6 test files, one session | 26 passed / 1 skipped; pid 387516 stable |

## Coverage detail (AC3 — 100% lines + branches + functions on changed code)

| Artifact | Stmts | Branches | Missed | Cover |
|---|---|---|---|---|
| `src/tests/smoke/tmux_isolation.py` | 12 | 6 | 0 | 100% |
| `src/tests/smoke/utilities/killtrace_probe.py` | 50 | 18 | 0 | 100% |
| `src/tests/smoke/conftest.py` **changed hunk L36–88** (guard fixture incl. teardown restore) | — | — | 0 in hunk | 100% of changed code |

- conftest whole-file misses are all at L169–205 = **pre-existing** `_auto_proxy_for_module` machinery; zero intersection with the changed hunk (verified via `coverage json` missing-lines list vs `git diff` hunk range).
- `src/scripts/tmux-execve-killtrace.bt` — **excluded-with-reason** per plan §7 (bpftrace source, not pytest-measurable) AND per Manager instruction (mid-fix on Tiffany's blocking finding; its dry-run rides Rick's install session).
- Zero misses across statements + branches ⇒ every function in the changed modules executed (function-coverage leg satisfied).

## Instrument receipt (killtrace-observation probe — the tap provably fired)

`/var/log/tmux-killtrace.log`:

```
00:14:04 sig=15 target=619838 from=tmux: server pid=619838 uid=1001
```

Self-SIGTERM, sender == target == 619838 = the **throwaway** server born on a private `-S` socket — confirming the §2.1 signature rule end-to-end. Fleet server pid 387516 appears nowhere as a kill target; re-queried read-only after the probe: alive, unchanged.

## The one skip (named, not silent)

`test_the_vertex_path_still_reaches_the_pane_despite_the_server_scrub` SKIPPED with the test's own designed conditionality:

```
--vertex refused to compose in this environment: [--vertex] REFUSING TO LAUNCH:
LUPIN_GCP_PROJECT_ID is not set. There is no safe default: guessing it would bill
a project nobody chose, in a region that may not serve the model.
```

This is the launcher's deliberate fail-safe (no default GCP billing project), not a fix regression. AC4's substance — the MAX-path taint-freeze assertion plus its negative control (de-scrubbed mutant goes RED) — **passed**, so the probe is proven able to detect taint and the clean reading is admissible.

## Safety attestation

- No destructive tmux verb ever addressed the default socket: every `kill-server` in the exercised code paths rides an explicit private `-S <tmp_path>` socket from a `$TMUX`-stripped env (verified by inspection of `test_tmux_socket_precedence.py`, `killtrace_probe.py`, and the `rig` fixture pre-run, then by the pid brackets).
- Fleet-pid brackets around both smoke runs: 387516 → 387516, both times.
- AC1's from-a-pane run executed only after the §10-5 gate was green from **my own** runs (AC5 2/2, AC2 1/1, helper coverage 100%) and the fresh pre-capture succeeded.

## Harness lesson (deposited to post-game 04:17Z)

Measuring coverage of a `conftest.py` yields silent empty reports under `pytest --cov` (conftest imports before the plugin arms) and `coverage run --include` is silently ignored when the repo config sets `source`. Working instrument: `coverage run --branch --source=<dir> -m pytest …` + `coverage report --include` on the one file.
