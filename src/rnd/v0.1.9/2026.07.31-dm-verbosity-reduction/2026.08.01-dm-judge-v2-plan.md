# DM Quality Judge v2 — extraction, not judgement

**Row**: `ca7a2cbf` (P1) · **Synthesizes**: `2026.08.01-improving-the-dm-quality-judge-claude.md` + `-gpt.md`

---

## Context

The judge grades a peer DM on three dimensions: Length (Python), Directness and Tone (one LLM call).
Measured live on 2026-08-01, across four bodies crossing `{verdict leads, buried} × {plain, jargon}`,
three runs each, 15/15 identical: **hold the prose jargony and the model can no longer tell a leading
verdict from a buried one.** It scores that same body `bad` on Tone and names the invented vocabulary
correctly — so it is not blind to the jargon. The error is always toward `good`, never toward `bad`.

Rick's ruling on that finding: qualitative grading is **OFF** (`dm quality qualitative enabled = False`),
Length only. This plan builds the replacement without touching the code that ruling applies to.

**Both experts, independently, name the same root cause and the same fix**: directness is currently
specified as a vibe with prose anchors, and a vibe judgement degenerates to a register prior when the
surface signal is masked. **Stop asking the model to grade directness. Ask it to locate the verdict,
and compute the grade in Python.** A grade can never be validated; a quote can.

### Three things confirmed by reading the code, not assumed

1. **`CompletionClient` stores `prompt_format` and never uses it** (`completion_client.py:88`; `run()`
   at :203 sends the string raw). The judge's Alpaca-style `### Instruction:` prompt reaches
   `/v1/completions` with **no chat template applied at all** — while Phi-4 was instruction-tuned on
   `<|im_start|>` turns. Both experts flagged this; it may be confounding every result so far.
2. **`PromptFormatter` is unwired**, and its `special-tokens-phi-4.txt` is the **Phi-3** `<|system|>`
   shape (`prompt_formatter.py:371`) — wrong for this model even if it were called.
3. **Chat mode is one INI word away** — `completion_mode = prompt_format in ["instruction_completion",
   "special_token"]` (`llm_client_factory.py:149`), so `json_message` routes to `ChatClient` at `/v1`
   and lets vLLM apply the tokenizer's own template. But **all 8 vLLM keys in this repo are
   completion-mode**; v2 would be the first chat client. That gets a spike, not an assumption.

### Outcome

A `DmQualityJudgeV2` that sits **beside** v1, selected by an INI key. v1, its prompt, its tests and
its probe are untouched and remain the live path until Rick reads v2's numbers and says otherwise.

---

## Decisions taken (Rick, this session)

- **Exit bar**: build v2 → run the 2×2 probe ×3 → report the table. **The toggle stays OFF** until Rick
  reads it. A passing probe is four bodies, and today already produced a sample that passed while
  measuring nothing.
- **Two calls**, one per dimension. Jargon then has no path to the directness answer at all.
- **No third jargon dimension.** Claude's §1.1 falsifies the "recognition has nowhere to go" story with
  evidence already in hand; GPT would allow at most an unweighted Tone subflag. Neither builds one.
- **Tone keeps its current rubric** — it already grades all four probe bodies correctly. Its only change
  is field order.
- **No recalibration against v1.** Qualitative is off, so there is no live behaviour to preserve; adopt
  GPT's table as written (Claude's §2.5 caveat does not bind).

---

## The core change: directness becomes an extraction

### What the model is asked for

Python numbers the body's sentences before sending. The model returns **raw observations and no
grade** — evidence fields only, in generation order, because a grade token emitted before its
justification cannot be informed by it (Claude §3.1):

```xml
<response>
  <first-payload-quote>Phase 1 is done and green.</first-payload-quote>
  <first-payload-index>1</first-payload-index>
  <stray-after-indices>4,7</stray-after-indices>
</response>
```

**A list of indices, not a count** (review fix 1). A count is a bare number with nothing to check it
against; a list of indices can be range-checked, ordered, de-duplicated, and required to sit after the
payload. The count is then derived in Python from a list that survived those checks.

The instruction block carries GPT's payload definition verbatim — a clear payload states an outcome,
status, decision, defect, request, blocker, or risk; narration, courtesy padding and self-evaluation
are not — plus the line both experts insist on: **unfamiliar technical terms are opaque identifiers,
evidence about Tone only and never about Directness.**

### What Python computes

GPT's structural code, because it keeps the "carries only what matters" half of the original rubric
that a bare index drops:

| condition | code | weight |
|---|---|---|
| index 1, 0 strays after | `lead_clean` | +2 |
| index 1, 1 stray after | `lead_one_stray` | +1 |
| index 1, ≥2 strays · or index 2 | `mixed` | 0 |
| index ≥ 3 | `late` | −1 |
| index 0 (no payload anywhere) | `missing` | −2 |

### Why this is different in kind — and exactly how far it goes

The extraction is **falsifiable in Python**, which a grade never was. Before the weight is computed:

- the quote must appear in the body (whitespace-normalized);
- the index must be in `1..len(sentences)`, or exactly 0;
- the quote must **equal the whole numbered sentence** at that index — **equality, not containment**
  (review fix 2). Containment loses the case the check exists for: a model quotes the clean clause of a
  sentence that also buries a blocker, lands index 1 with no strays, and scores +2 on a body that
  should not get it;
- when the same sentence text appears twice, the index must be its **first** occurrence — otherwise a
  model can pick whichever of two identical sentences grades better and pass every check.

**Where the guarantee stops, stated plainly.** These checks ground the **position** of the verdict
completely. They do **not** ground the *stray* classification: deciding that sentence 4 carries nothing
the reader needs is the same payload judgement Phi-4 failed at, and the indices list makes it
structurally checkable, not semantically true. So:

- **Position weight** — `{1:+2, 2:+1, ≥3:−1, 0:−2}` — is fully validated.
- **Concision adjustment** — the stray-driven step down from `lead_clean` — is a **disclosed model
  judgement**, and the probe reports the position weight and the final weight as **separate columns**
  so we can see whether the remaining noise lives in the part we did not ground.

That is the honest version of the claim. The plan's earlier wording implied all three inputs were
validated; one of them is not.

A failed check is **not** a grade. It raises, retries, and on exhaustion returns a **distinct new detail
string** — which must be registered in the probe's non-answer set (`dm_judge_discrimination_probe.py:174-177`),
or it wears a real `meh`'s costume and can silently satisfy an ordering. That is the defect the current
probe was rebuilt to close; v2 must not reintroduce it one level out.

---

## Files

### New

| File | Contents |
|---|---|
| `src/cosa/agents/dm_quality_judge/xml_models_v2.py` | `DmDirectnessExtraction` (3 evidence fields, no grade), `DmToneJudgement` (note before grade); `STRUCTURE_TO_WEIGHT`; `structural_code()`; `validate_extraction()` |
| `src/cosa/agents/dm_quality_judge/judge_v2.py` | `DmQualityJudgeV2` — same `judge(body_text)` contract and same return shape as v1; `number_sentences()`; two private graders |
| `src/conf/prompts/dm-quality-judge-v2-directness.txt` | Extraction prompt, `{{PYDANTIC_XML_EXAMPLE}}` + `{numbered_dm_body}` |
| `src/conf/prompts/dm-quality-judge-v2-tone.txt` | Current tone rubric, evidence-first schema, `{dm_body}` |
| `src/tests/unit/test_dm_quality_judge_v2.py` | Unit tier, 100% lines/branches/functions |

### Modified

| File | Change |
|---|---|
| `prompt_template_processor.py` | Two `MODEL_MAPPING` entries — `dm quality judge v2 directness`, `dm quality judge v2 tone` |
| `lupin-app.ini` + `-splainer.ini` | `dm quality judge version = 1`; `dm_quality_judge_v2/phi_4` spec + `_params` (chat mode, `max_tokens` 1024) |
| `dm_quality_judge/__init__.py` | `get_dm_quality_judge()` factory reading the version key |
| `rest/routers/dm.py:483-485` | Lazy singleton calls the factory instead of `DmQualityJudge()` directly |
| `src/tests/smoke/dm_judge_discrimination_probe.py` | `--version {1,2}` flag; register v2's new non-answer detail strings. **The 2×2 bodies, the per-cell guard and the non-answer classifier are unchanged** — they are the measurement and they were hard to get right |

### Reused, not copied

`judge.py` already owns everything v2 needs on the deterministic side. v2 **imports** `length_bucket`,
`round_half_up`, `combine_overall`, `QUALITATIVE_WORD_LIMIT`, `_repair_llm_xml`, `_is_garbage_output`,
`_fallback_dimension`, `_withheld_dimension`, `_too_long_dimension`; and from `xml_models.py`,
`GRADE_TABLE`, `WEIGHT_TO_EMOJI`, `normalize_grade_label`. The repair layer in particular encodes six
separately-diagnosed live failure modes — v2 gets all of them free, and a fix to either version fixes
both.

---

## Order of work

**Phase 0 — serialize.** This plan → `src/rnd/v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-dm-judge-v2-plan.md`,
linked from `src/rnd/README.md`. No code before the doc exists.

**1. Chat-template spike (~15 min, gates everything).** Build a `ChatClient` against the vLLM server via
a `json_message` spec key and send one trivial prompt.
- ✅ reaches the model → v2 uses chat mode, and vLLM applies Phi-4's real template server-side.
- ❌ → **Route B**: stay on `CompletionClient` and hand-write the `<|im_start|>` markers into the two v2
  template files. Costs no new infrastructure; the risk moves to getting the token shape exactly right,
  so diff it against `tokenizer.apply_chat_template` before trusting it.
- ⚠️ `ChatClient` sets the **process-global** `OPENAI_BASE_URL` (`llm_client.py:110`) — but so does the
  `CompletionClient` v1 uses today (`completion_client.py:84`). **Pre-existing, not v2-new**; the earlier
  wording framed it as a discovery. Still check it in the spike, since chat mode may read the variable
  at call time rather than construction time, which completion mode does not.

**2. `xml_models_v2.py`** — schemas, structural table, validator. Pure Python, unit-testable with no
model. This is where the falsifiability lives, so it gets tested hardest.

**3. Sentence numbering** — a small, deliberately boring splitter. Tested against abbreviations,
decimals, ellipses and a trailing fragment. If it miscounts, every index downstream is wrong and the
validator will reject correct model output. **One call, one list**: the numbered text shown to the model
and the list the validator checks against are the *same* value passed through, never two calls that can
drift apart.

**4. Two prompt files.** No few-shot exemplars; if any are added later they must be freshly written —
**never the four probe bodies**, or the probe stops being a measurement (Claude §5).

**5. `judge_v2.py`** — same contract as v1: always returns `{length, directness, tone, overall}`, never
raises, degrades to a named non-answer. **It must carry v1's `qualitative_enabled=None` injection seam**
(`judge.py:425`): the probe constructs the judge with `qualitative_enabled=True` (`dm_judge_discrimination_probe.py:343`,
verified), and without that seam v2's probe run would measure withheld non-answers at the default-OFF
toggle — a run that passes while measuring nothing.

**6. Selector + wiring**, defaulting to version 1. Nothing changes for anyone until the key is flipped.

**7. Measure**, then hand Rick the table.

---

## Verification

| Tier | What | Venue |
|---|---|---|
| Compile | `py_compile` on every edited `.py` | — |
| Unit | `pytest src/tests/unit/test_dm_quality_judge_v2.py` — every structural-code branch, every validator rejection, retry/fallback, both schemas' round trip | :7999 |
| Regression | `pytest src/tests/unit/ -k dm_judge` — v1's suite must stay green untouched | :7999 |
| Coverage | `pytest --cov=cosa.agents.dm_quality_judge --cov-fail-under=100` (lines + branches) | :7999 |
| Live probe | `python src/tests/smoke/dm_judge_discrimination_probe.py --version 2 3` — the 2×2, three runs | :7999 |
| Control | see below | :7999 |

**Controls that must fail.** A harness nobody has seen fail is unaudited:

1. Hand the validator a fabricated quote absent from the body → **must reject**. If it passes, the
   substring check is not running and every "validated" extraction today is worthless.
2. Hand it a correct quote with a deliberately wrong index → **must reject**. This is the check that
   catches quote-right/count-wrong, the failure a substring test alone cannot see.
3. Hand it a **sub-clause** of the sentence at that index → **must reject**. This is the one that
   proves the check is equality and not containment; without it, review fix 2 is unenforced.
4. Hand it a stray index that is **before** the payload, out of range, or duplicated → **must reject**.
5. Give it a body with the **same sentence twice** and the second occurrence's index → **must reject**.
6. Run the probe with v2 pointed at a dead endpoint → **must exit non-zero**, never report separation.

**Expected, and stated before running** (Claude §6.4, so a disagreement is informative rather than
explained away): `BURIED_JARGON` directness `late` or `missing` (−1/−2); `DIRECT_JARGON` `lead_clean`
or `lead_one_stray` (+2/+1); tone unchanged from today on all four cells.

**Then stop.** The table goes to Rick. `dm quality qualitative enabled` stays `False` and
`dm quality judge version` stays `1` until he rules.

---

## Risks

- **The chat template may not be the confounder.** If v2 in chat mode reproduces today's failure cell for
  cell, the extraction still holds — it removes the model from the directness weight entirely — but the
  §3.2 hypothesis dies and should be recorded as dead, not quietly dropped.
- **The 2×2 is four bodies.** It cannot certify v2; it can only fail it. GPT's 30 minimal pairs remain the
  real evidence bar, deferred by Rick's decision to see a first table sooner. Say so when reporting —
  a green 2×2 is a reason to keep going, not a result.
- **The stray classification is not grounded, and the position is.** If the probe shows the position weight
  clean and the final weight noisy, the concision half should be dropped rather than defended — position
  alone is Claude's original table and is fully validated.
- **`prompt_format` being inert is a live bug in v1's config too.** v2 routes around it; v1's spec key still
  claims a format nothing applies. File separately, do not fix inside this plan.

---

## The 24B A/B — the checkpoint separated from the code path

Every measurement that condemned the Mistral-24B was taken through the **old broken path**
(Alpaca prompt, no chat template, `/v1/completions`) — the same path that made Phi-4 look bad until it
was fixed. That left an open question: was the checkpoint ever the problem, or only the path?

Rick swapped `:3001` to the 24B and both models were run through the **identical** chat path, prompts
and bodies.

| | Phi-4 | Mistral-24B |
|---|---|---|
| verdict | **✓ MONOTONIC** | **✗ INSUFFICIENT** |
| real grades | 24/24 | 15/24 |
| dead cells | 0 | **3** |
| BURIED bodies | −1 (correct) | **+2 — graded as if the verdict led** |

**How it failed, and it is not what we blamed before**: spaced tags (`< response >`), a quote appearing
nowhere in the message, **24** stray indices listed, and on retry it abandoned XML for prose. On Maria's
DM, a mismatched tag at column 1126. Not the parroting the original ceiling finding described. v1's
repair layer existed to massage exactly this sloppiness; **v2's validator refuses it instead** — the
design working as intended.

⚠️ **Do not over-read this.** These prompts were tuned on Phi-4. It measured *"does the Phi-4-tuned
setup work on the 24B"* (no), **not** *"which checkpoint is better"*. What it does settle: **the
chat-format fix was not the whole story for that build.**

**Cost**: one INI key added and then removed once it had answered (it would otherwise point at a model
that is not served); `--model <spec key>` added to both probes, so re-running is adding one key back,
not rebuilding an instrument.

**Rewriter ruling (Rick, same day)**: one box serves one judge model, so a local rewriter beside the
judge is not available — **the rewriter goes to an API**. A 24B-class model is still the right size;
only where it runs changed. That makes the "measure the net saving" caveat sharper, not softer: API
tokens are metered against a real bill, and this lands on the **firewalled-SDK** side of the cost model,
not the Max-plan-covered side. Recorded in store item `5c78f7f9`.

---

## Filed separately (found during review, not fixed here)

**The judge's model is misnamed everywhere.** INI key `dm_quality_judge/mistral_small_24b` (`lupin-app.ini:185-186`)
serves `kaitchup/Phi-4-AutoRound-GPTQ-4bit`, and `judge.py`'s docstrings say "Mistral judge" throughout.
The live v1 judge **is Phi-4** — which is what makes the chat-template finding bite, since the prompt was
written for one model family and is being sent to another. Cosmetic in behaviour, actively misleading in
every future diagnosis. Own row, own commit.

---

## 🔴 MIDCOURSE CORRECTION — Rick, 2026-08-01, after reading the first v2 table

**Read this before touching the concision half again. It is the reason the code looks the way it does.**

### What the first table showed

Position worked. Concision destroyed it.

| body | position (grounded) | final (with concision) | structure |
|---|---|---|---|
| DIRECT_PLAIN | **+2** | 0 | mixed |
| DIRECT_JARGON | **+2** | 0 | mixed |
| BURIED_JARGON | **−1** | −1 | late |

On the contrast that *defined* row `ca7a2cbf` — directness with the prose held jargony — v1 scored
`+1 vs +1`, a flat failure. The position half scored `+2 vs −1`. DIRECT_PLAIN and DIRECT_JARGON landed
**identically** at +2, which is the correct answer: both lead with the verdict and differ only in prose.

Then the stray count collapsed both +2 bodies to 0, because the model marked 2–3 sentences "stray" that
the prompt itself lists as **needed** — evidence, a decision, a risk. The reviewer predicted this before
a line was written.

### The ruling

> *"I want something that works approximately and at least better than what we had, and later we can
> return to it to optimize."*

**Score the grounded half only.** `CONCISION_ADJUSTMENT_ENABLED = False` in `xml_models_v2.py`; the
directness weight is `position_weight( index )`, every input to which survived a check against the
source text.

### This is a park, not a deletion

The strays are still extracted, still shape-validated, still carried in the result as `stray_count` and
named in the detail line (which says **"not scored"** so nobody has to know a flag's value to read the
number). Flip the constant to re-weight them. `code_weight`, `structural_code` and the whole 5-code
table are intact and unit-tested on both settings.

### What the follow-up must solve — do not re-derive this

- The model over-marks strays specifically on bodies whose later sentences are **evidence and risks** —
  the two categories the prompt explicitly protects. So the failure is in the model's reading of the
  rubric, not in the rubric's wording alone.
- Any replacement must be **checkable in Python**, or it inherits this exact problem.
- **Candidate not yet tried**: ask for a per-sentence needed/not-needed verdict for *every* sentence
  rather than a free-form list, so each answer is attributable to one sentence and can be scored
  individually.

---

## Result — v2 as shipped

`python src/tests/smoke/dm_judge_discrimination_probe.py 3 --version 2` → **✓ MONOTONIC on both
dimensions**, twice, on independent invocations. 24/24 real grades, zero non-answers.

| body | directness | tone |
|---|---|---|
| DIRECT_PLAIN | **+2** | +2 |
| DIRECT_JARGON | **+2** | −2 |
| BURIED_PLAIN | **−1** | +2 |
| BURIED_JARGON | **−1** | −1 |

Directness moves **only** with burial and is identical across both prose conditions — jargon no longer
reaches it at all. That is the defect closed, measured on the contrast that defined it.

### Five things found only by running it

1. **v1's repair layer silently deleted v2's evidence field.** `_repair_llm_xml` rebuilds the response
   from a hardcoded tag set and drops the rest; called with v1's tuple it kept `<tone>` and lost
   `<tone-evidence>`. Graded tone, blank justification, nothing raised — the XML was well-formed before
   and after. The field set is now a parameter; v1's default is unchanged and its 122 tests stay green.
2. **The factory's vLLM chat branch had never had a caller.** It passed a bare model id pydantic_ai
   cannot resolve, and no `api_key`. Both fixed. Every vLLM key in the INI was completion-mode, so the
   branch had never executed.
3. **The probe's `_cell_counts` dict was hand-copied**, so adding a non-answer kind raised `KeyError`
   mid-run — *after* every model call had been paid for. Now derived from `NONANSWER_KINDS`.
4. **`"no"` was in the abbreviation list** (for "No. 5") and ate a real sentence boundary:
   `He said "no." Then he left.` came back as one sentence, shifting every index after it. No entry may
   be a common English word.
5. **A retry that repeats the identical prompt at temperature 0 gets the identical answer.** BURIED_PLAIN
   was a dead cell — the model dropped the leading `"Anyway, "` from its quote, 3/3, correctly refused
   3/3. Three calls, one piece of information. A retry after a *failed check* now carries the checker's
   own complaint back to the model. The check is unchanged and still rejects a paraphrase; it just stops
   spending two attempts asking the same question the same way. That alone turned INSUFFICIENT into a
   clean table.

### Not done

- The 30 minimal pairs the GPT expert asked for. Four bodies **cannot certify** this — a green 2×2 is a
  reason to keep going, not a result.
- `dm quality qualitative enabled` = **False**, `dm quality judge version` = **1**. Nothing live changed.

---

## Review

Sanity-checked before implementation by `cc-reviewer-mr-radio-1` (read-only, Opus 4.8).
**Verdict: safe to implement with three named changes** — all three factual claims confirmed with
file:line, no premise wrong. The three fixes are folded in above (indices-not-count + honest scope of
the guarantee; equality-not-containment; the `qualitative_enabled` seam), as are the smaller
duplicate-sentence and one-splitter points and the correction to the `OPENAI_BASE_URL` framing.
It verified the claim sites, the call site, the factory, the INI, the probe's non-answer matcher and
`MODEL_MAPPING`; it ran nothing, because no v2 code exists yet.
