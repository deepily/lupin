# Memento — Krishna 🦚 mux visual-regression VERIFY lane (2026-07-01) — LANE A CLOSED

**Role:** visual-regression VERIFY worker (index 1), reporting to Tiberius 👑 (session b75d199b). Session 6949ad7f.
**Outcome:** Lane A FULLY DONE + accepted by Tiberius (he eyeballed both PNGs). Commit-HELD `f3878bc0` (push is Rick's — NOT pushed). Prepared for re-spin.

## Result
Both mux visual stragglers driven to DONE with a 5/5 sequential green :8000 receipt (`ts-bb0cd434`, `/tmp/e2e-20260701-191831.log`). All 4 store items transitioned DONE with receipts `{commit: f3878bc0, test_run: ts-bb0cd434}`:
- `a90cc63e` (phase6b tts_chrome) · `660d02b4` (task_editing) · sub-items `04e7d441` · `a1e464df`.

## a90cc63e — stale-baseline rebaseline (76→64)
Stale-selector fix (362318d8, waits `[data-testid=multiplexer-tts-chrome][data-state=idle]`) reached pixel compare and exposed a SIZE mismatch: render 960x64 vs committed baseline 960x76. The 960x76 baseline was STALE — the pre-`f48b0bf0` idle-WITH-controls panel (—/Stop/Skip). Current correct idle render is `renderTtsEmpty()` → "🎵 Nothing in the queue" @960x64, contract-locked by `templates_tts_chrome.test.ts`. Root-caused in-container rebaseline 76→64, VIEWED clean. Runbook's "no rebaseline expected" was empirically wrong.

## 660d02b4 — the THREE-CORRECTION ARC → resolution D
1. **CSS pins.** Control-height pin (`task-list.css` Actions-column controls: `line-height:14px; height:20px; box-sizing:border-box`) fixed the OUTER ±1px height flap (169↔170). Row-cell line-height pins (`.task-row td`/`.task-group-header td`/`thead th`) made it 163 and did NOT help → REVERTED. **CSS can't fix it.**
2. **Settle-timing hypothesis (self-refuted).** The GRADED diff variance (0/29/1830/4311px) looked like a paint-timing race. Replaced `time.sleep(0.3)` with `wait_for_load_state("networkidle") + document.fonts.ready + double-rAF`. Empirically REFUTED: 0/3 green, diffs 417/566/3821px — a settle race would go green once settled; it didn't.
3. **Font-AA high-variance → resolution D.** Offline forensic on a settle-fix failing pair: vertical-shift 0.0 is OPTIMAL (±shift worse) → NOT a position/clip shift; 2× downscale doesn't collapse → not sub-pixel positioning; NO full-width border-line rows → control-pin fixed the border axis. Residual = scattered per-pixel glyph diffs = **irreducible Chromium font-AA rasterization non-determinism** on the single most glyph-dense of the 37 mux snapshots. High-variance to ~3800px → NO safe pixel/count budget (would mask a real <2000px regression). Tiberius ratified **resolution D**: drop the exact-pixel assertion, keep a DETERMINISTIC structure-only gate.

**Implementation (all in commit f3878bc0):**
- `visual_height_tolerance.py` — new `compare_pngs_structure_only` (width-exact + height-tolerance, PIXEL-BLIND BY DESIGN, LOUD do-not-fix-back note).
- `conftest.py` — new `assert_snapshot_structure_only` fixture.
- `test_multiplexer_task_editing.py` — rewired to structure-only + KEPT the settle-fix; docstring documents resolution D.
- `test_visual_height_tolerance.py` — +7 unit tests, **15/15 total, 100% line+branch** on the core, incl. the never-false-green proofs: two "same-size/DIFFERENT-content → matched=True BY DESIGN" (loud), width-differ→FAIL, height-beyond-budget→FAIL.
- Kept the control-height pin + settle-fix (genuine determinism improvements).

## THE (C) FINDING (reshaped the follow-on)
Option C's font-determinism flags are ALREADY deployed suite-wide: conftest `browser_type_launch_args` sets `--font-render-hinting=none / --disable-lcd-text / --force-color-profile=srgb / --force-device-scale-factor=1` (from bug 99326963). They stabilise the other 36 tests but are INSUFFICIENT for this glyph-dense card. So (C) was never a fresh lever. **P3 follow-on `d90dcfc2`** (now OWNED BY TIBERIUS — he reassigned it off me) captures the real ask: a STRONGER lever (`--disable-font-subpixel-positioning` / `text-rendering:geometricPrecision` / 2× capture+downsample / a proven erosion-based AA-signature comparator with a mandatory 20×20-contiguous-block-still-FAILS proof) + all-37-baseline reshaping (Oracle domain) + Rick's blast-radius ok.

## :8000 ops learnings (reusable)
- Verified-idle :8000 → submit self-authorized via `POST /api/test-suite/submit`; params: `test_types="e2e"`, `pytest_args="-k \"<expr>\""` (append `--update-snapshots` to rebaseline), `auto_fix_on_failure=False`, `scheduled_at`=now+~15s ISO EDT.
- **NO bounce for test-file changes:** repo bind-mounted at `/var/lupin`; the e2e subprocess reads test files fresh. **CSS served RAW** from `/static/css/multiplexer/task-list.css` (`<link>` in multiplexer.html, NOT bundled) → CSS edits live immediately.
- **PID-guard collision:** staggering :8000 submits <~40s apart trips `run-e2e-ui-tests.sh`'s `/tmp/e2e-ui-tests.pid` guard → "already in progress" + a contended half-render (false huge diffs). Run stability sweeps STRICTLY SEQUENTIAL (wait each job → done + pid clear before next). Reusable script: `scratchpad/final_verify.py`.
- Receipt schema: `task_transition ->done` receipt keys are whitelisted; `log_line` must be `<scope>/<rel>:<lineno>` (NOT free text); `doc_path` needs a registered scope prefix (`lupin/src/rnd/...`). `commit` + `test_run` are free-form and were what I used.

## State at hand-off
- Working tree: commit `f3878bc0` HELD (7 files), nothing else of mine uncommitted. Other sessions' files (`.gitignore`, `00-index.md`, untracked mementos) untouched.
- Board: empty of my open items (d90dcfc2 reassigned to Tiberius). Ready for re-spin.
