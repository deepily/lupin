# Per-Card-Collapse Double-Ownership — Investigation Findings

**Author**: Sam 🎙️ (investigator) for Mr. Radio 🦉 (manager)
**Date**: 2026-06-29
**Task**: store `af8fb1ee-7520-4554-838c-e7e1dae0f0a1` (owner=sam, mgr=mr radio) — feeds coordination gate **eb84266b** (Plan 01-B1 build-sequencing)
**Mode**: READ-ONLY. No checkout/reset/stash/clean of the shared tree. `git show`/`git log`/grep only.
**Branch**: `wip-v0.1.9-2026.06.21-bug-fix-implementation` @ `a0a10692`
**Refs**: `05-build-plans/01-cc-session-B1-B5.md` §4.1/§4.2 · `02-reconciliation-with-in-flight-parity-work.md` §1/§2/§6 · 92-01 closure §4 item 2

---

## TL;DR — the double-ownership question is RESOLVED by the git state

The plan (doc 02, snapshot **2026-06-25**) framed per-card collapse as **double-owned** between two *unmerged/held* branches, with a gating pre-req to "confirm it shipped, not stranded." As of **2026-06-29** that framing is **stale**:

1. **Both "held" branches are already merged into HEAD AND pushed to origin.** There is **no live merge collision** to sequence.
2. They are **cleanly linearized**: Rachel's accordion system landed FIRST, Tiberius's focus-bar built on top — `a4878f23 → (merge acf6665a, María APPROVE) → 4b33ceb7 → HEAD`.
3. **Collapse is live and green in HEAD: 71/71** (11/11 accordion lane + 12 task-list + 9 section-toolbar + 39 broadcast-card).
4. The "double-ownership" is a **historical provenance ambiguity** (two crews both touched/claimed per-card collapse), **NOT** a current code collision. In HEAD, ownership is cleanly partitioned across three parallel systems.

**Recommended merge order**: **B1 proceeds directly on current HEAD as an ordinary forward edit — no pre-merge coordination of Rachel's or Tiberius's branches is required** (both already ancestors). One caveat for the manager to confirm with the crews: uncommitted work in *separate worktrees* is invisible from here (see §5).

---

## 1. Git state — both branches already merged + pushed

| Commit | Author / work | Ancestor of HEAD? | In origin? | Distance behind HEAD |
|---|---|---|---|---|
| `a4878f23` | Rachel 🕊️ — "Mux section-toolbar carbon-copy + per-accordion collapse toggle" (branch `mux-section-toolbar-accordion-toggle`, store task `f6dc0043`) | **YES** | **YES** | 56 commits |
| `4b33ceb7` | Tiberius 👑 crew — "Focus-bar parity: worker count-badge silencing + broadcast-to-all-CC compose port" (store item `34466d69`) | **YES** | **YES** | 23 commits |

- `git merge-base --is-ancestor a4878f23 HEAD` → **YES** (also pushed: `origin/wip-v0.1.9-2026.06.21-bug-fix-implementation` contains it).
- `git merge-base --is-ancestor 4b33ceb7 HEAD` → **YES** (also pushed).
- `git merge-base --is-ancestor a4878f23 4b33ceb7` → **YES** — Rachel's collapse is an ancestor of Tiberius's commit.
- Merge commit `acf6665a [LUPIN] Merge mux section-toolbar + accordion-toggle into wip-v0.1.9 (Rachel build, María APPROVE)` — Rachel's branch merged **with approval** ~4 days before this investigation.
- Working tree for all collapse files (`render/`, `stores/ViewStateStore.ts`) is **clean** — no uncommitted edits in *this* tree.

**⇒ doc 02's "push HELD for Rick" (4b33ceb7) and "commit-held" (Rachel/D06) statuses are both resolved.** The plan's §4.2 maneuver — "restructure before it ever lands upstream (no revert of pushed history needed)" — is **moot**: it already landed upstream. B1 edits ordinary committed code with a new forward commit; no history rewrite, no Rick push-gate entanglement on the *edit itself*.

---

## 2. Collapse ownership map (current HEAD, file:line)

Collapse in the multiplexer is **three parallel systems**, cleanly partitioned:

### A. Section / accordion collapse — Rachel's system (`a4878f23`)
- **STATE owner** — `multiplexer/stores/ViewStateStore.ts`
  - `isAccordionCollapsed` / `setAccordionCollapsed` (`:51`–`:54`, silent persist), `requestBulkAccordionCollapse` (`:64`, the only emitter), `isSectionVisible`/`setSectionVisible` (`:44`–`:47`), keys `:31`/`:33`. Header `:2` tags it **"2026-06-23, Rachel."**
- **TOOLBAR driver** — `multiplexer/render/SectionToolbarRenderer.ts`
  - collapse-all / expand-all → `requestBulkAccordionCollapse(true/false)` (`:106`–`:114`); per-section visibility `toggleSection` (`:124`–`:127`). Drives via store events; owns **no** accordion DOM.
- **ACCORDION-DOM owner** — `multiplexer/render/NotificationsListRenderer.ts` ("sole owner of the accordion DOM", `:232`)
  - progress-group toggle → `toggleProgressGroup` (`:432`–`:444`)
  - date-accordion header → `toggleDateAccordion` (`:448`–`:453`)
  - sender-card header → `toggleSenderCard` (`:457`–`:464`)
  - consumes bulk → `applyBulkAccordionCollapse` (`:236`); re-applies persisted state on render → `reapplyAccordionCollapse` (`:315`)
- **TEMPLATE** — `render/templates/senderCard.ts` emits the sender-card header DOM `toggleSenderCard` operates on.

### B. Per-CARD (broadcast) collapse — Tiberius's commit (`4b33ceb7`, NEW file)
- `multiplexer/render/BroadcastCardRenderer.ts` — `#broadcast-submit-header` click → `toggleCard()` (`:253`–`:255`, body `:271`–`:278`); toggle glyph `#broadcast-submit-toggle` (`:151`). **Self-contained card-level collapse, independent of the accordion system.**

### C. Task-list owner-group collapse — separate pre-existing system
- `multiplexer/render/taskListCollapse.ts` — `loadCollapsedOwners` (`:55`), `saveCollapsedOwners` (`:75`), `toggleCollapsedOwner` (`:91`); `ownerKeyForGroup` (`:30`), `taskGroupIdSlug` (`:42`); key `:18`.
- `multiplexer/render/TaskListRenderer.ts` — collapse-all/expand-all buttons (`:140`–`:156`); imports the helpers (`:34`).
- `render/templates/taskListTable.ts` — imports `ownerKeyForGroup`/`taskGroupIdSlug` (`:26`).

> **What "double-owned" really meant**: within system **A**, collapse is split state→driver→DOM across 3 files (by design — ViewStateStore §17 EMISSION POLICY), AND systems **B**/**C** are parallel collapse mechanisms. Two crews (DFB Lane B + Rachel/D06) independently *claimed* the per-card collapse line in the gap docs. The git history shows it is one lineage, not a fork.

---

## 3. Where `4b33ceb7` lands (and what it does NOT touch)

`git show --stat 4b33ceb7`: 26 files (broadcast.css, multiplexer.html +8, ApiClient +44, boot.ts +12, **BroadcastCardRenderer.ts +583 NEW**, broadcastCard.ts +92 NEW, BroadcastStore.ts +151 NEW, SenderStore.ts, **senderCard.ts +10/-2**, notifications.js +72, e2e + unit specs).

- **senderCard.ts diff is worker-badge silencing, NOT collapse**: adds `data-worker="true"` (`:61`-ish) + gates `.sender-new-count` on `is_worker !== true`. Zero overlap with `toggleSenderCard`'s collapse path.
- Commit message Lane B verbatim: **"per-card collapse toggle verified (already wired 2026-06-23)."** ⇒ `4b33ceb7` **did not author** collapse; it *verified* Rachel's. No ownership conflict introduced by Tiberius's commit.

---

## 4. Collapse-lane test result (:7999, AI-discretionary) — 71/71 GREEN

`npx tsx --test`:

| Spec | Tests | Result |
|---|---|---|
| `notifications_list_accordion_collapse.test.ts` — **the "11/11 lane test"** | 11 | ✅ pass |
| `task_list_collapse.test.ts` | 12 | ✅ pass |
| `section_toolbar_renderer.test.ts` | 9 | ✅ pass |
| **Accordion-lane subtotal** | **32** | **32/32 pass, 0 fail** |
| `broadcast_card_renderer.test.ts` (owns `toggleCard`) | 39 | ✅ 39/39 pass |
| **TOTAL collapse surface** | **71** | **71/71 pass** |

The §4.1 gating pre-req #1 ("grep the `4b33ceb7` tree for the collapse handler + run the 11/11 lane test; if absent → coordinate merge order with Rachel first") is **SATISFIED**: collapse is present and green. The "if absent → coordinate with Rachel" branch does **not** fire.

---

## 5. B1 collision analysis + recommended merge order

**B1's footprint** (doc 01 §3 table): `multiplexer.html` mount order (`:72`-`:200`), `boot.ts` mount wiring (`:456`-`:484`), `BroadcastCardRenderer.ts`/`broadcastCard.ts` (host nested commons), `CommonsActivityRenderer.ts` (mount into broadcast subtree), `NotificationsListRenderer.ts` (B4 extends the delegated click handler `:428`-`:453`).

- **Collision with Rachel's accordion collapse (system A): NONE.** B1 repositions panes/mounts; it does **not** modify `toggleDateAccordion`/`toggleSenderCard`/`toggleProgressGroup` or `ViewStateStore`. B4 *extends* the existing delegated handler with corner-⏸/⏹ branches (early-return, per F-Krishna-BD4 — corner buttons render into `.sender-message` body, so `closest('.date-accordion-header')` returns null and cannot trip a collapse). No rewrite of collapse logic.
- **Collision with Tiberius's `4b33ceb7` (system B): EXPECTED + BENIGN.** B1 restructures the broadcast mount (move `#broadcast-card-mount` to TOP, nest commons inside). Since `4b33ceb7` is **already merged + pushed**, this is an ordinary forward edit on HEAD — **not** the "restructure-before-upstream / no-revert-of-pushed-history" maneuver the plan anticipated. The `toggleCard()` collapse handler itself is untouched by the re-nest (it keys on `#broadcast-submit-header`, which moves with the card).

### Recommended merge order (manager rules)

1. **No pre-merge coordination of Rachel's or Tiberius's branches is required.** Both `a4878f23` and `4b33ceb7` are already ancestors of HEAD and in origin. The "merge order" the gate worried about is **already executed and approved** (Rachel first via `acf6665a`, then Tiberius).
2. **B1 builds directly on current HEAD** as a normal forward commit. No fork-merge, no revert, no "land on top of unpushed `4b33ceb7`" sequencing.
3. **Update the plan's stale framing**: doc 02 §1/§2 ("push HELD", "commit-held", "restructure before it lands upstream") and §4.1 pre-req #2 should be amended to "already merged + pushed; B1 is a forward edit." The gate eb84266b can clear on this basis.
4. **One caveat the investigation cannot close from here** (doc 02 §6 prereq ii): I see only committed state + this clean working tree. If Tiberius's or Rachel's crews hold **uncommitted** work in *separate worktrees* targeting the same broadcast/commons mounts, that is invisible to a read-only git inspection of this tree. Branch labels + origin show nothing unmerged, but **Mr. Radio should confirm "no unpushed follow-up targeting the broadcast/commons mounts" directly with the crews** before B1 starts — that is a coordination check only the manager can run.

---

## Appendix — commands run (all read-only)

```
git merge-base --is-ancestor a4878f23 HEAD            # YES
git merge-base --is-ancestor 4b33ceb7 HEAD            # YES
git merge-base --is-ancestor a4878f23 4b33ceb7        # YES
git branch -r --contains a4878f23 | grep origin       # in origin
git branch -r --contains 4b33ceb7 | grep origin       # in origin
git rev-list --count a4878f23..HEAD                   # 56
git rev-list --count 4b33ceb7..HEAD                   # 23
git show --stat 4b33ceb7
git show 4b33ceb7 -- .../templates/senderCard.ts      # worker-badge diff, not collapse
npx tsx --test <3 accordion-lane specs>               # 32/32
npx tsx --test broadcast_card_renderer.test.ts        # 39/39
```
