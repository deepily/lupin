# Focus-Bar Parity — Build Plan & Execution Log

**Date**: 2026-06-24
**Manager**: Tiberius 👑 (session bb260a06)
**Gap list / rulings**: `src/rnd/v0.1.9/2026.06.24-notifications-multiplexer-focus-bar-parity-gap-list.md` §6
**Status**: 🟡 Planning → crew spin-up

Three features, ruled by Rick 2026-06-24. Predicate everywhere: **`isWorker(sender) := managerPersona(sender) != null`** — managed workers get **no count badge** (faint pulse kept); unmanaged/manager/root keep the count.

---

## Lanes (parallel, worktree-isolated)

Convergence files (`boot.ts`, `multiplexer.html`, EventBus union, `StorageService`, shared CSS) are **manager-serial-merged** — workers propose diffs, Tiberius integrates to avoid collisions.

### Lane A — Worker badge silencing (legacy + multiplexer)
**Owner**: Worker-1
**Files**:
- Legacy: `js/notifications.js` — `_markStripIconActivity()` (~10971): early-return the `data-unread-count` write when `managerPersonaMap.get(senderId)` is non-null, BUT still set `data-unread="true"` (keep pulse). `updateSenderCardHeader()` (~14152): skip `.sender-new-count` text when worker.
- Legacy CSS: `css/notifications.css` — add `.cc-strip-icon[data-worker="true"][data-unread]::after { display:none }` (kill count circle, keep `cc-strip-icon-pulse`). Set `data-worker` in `_addStripIcon`/`_applyManagerBadge`.
- Mux: thread `manager_persona` (or derived `is_worker`) into `SenderRecord` (`shared/types.ts`, `stores/SenderStore.ts`) from the same notification field `SessionStripStore` already reads.
- Mux: `render/templates/senderCard.ts:164-166` — gate `.sender-new-count` on `!sender.is_worker`; add faint-pulse affordance parity.
**ACs**:
1. A managed worker's strip icon (legacy) pulses but shows **no** numeric badge.
2. A managed worker's section-notifications card shows **no** "N new" count (both clients); manager/root cards DO.
3. Predicate is `manager_persona != null` — no backend change, no new endpoint.
4. 100% lines/branches/functions on all touched TS + new unit tests for the predicate.
**Tests**: unit (predicate + render-gate), e2e_ui (`test_multiplexer_*` worker-vs-manager badge), legacy smoke.

### Lane B — Per-card collapse toggle (verify + rebuild)
**Owner**: Worker-1 (folds in after Lane A) or Tiberius
**Work**: Confirm `NotificationsListRenderer.ts:490 toggleSenderCard` renders + fires in a fresh `dist/multiplexer` build (`src/scripts/build-multiplexer.sh`). Session-only (no persistence). If a real gap surfaces (glyph not clickable / not in dist), fix minimally.
**ACs**: every CC session card in `section-notifications` collapses/expands via its `.sender-toggle`; chevron flips ▼/▶; verified by e2e_ui.

### Lane C — Broadcast-to-all-CC compose port (multiplexer)
**Owner**: Worker-2
**Build (new mux surfaces, mirror legacy `notifications.html:692-726` + `broadcast-panel.js`)**:
- Template `render/templates/broadcastCard.ts` — collapsible compose card: recipients row + ↻ refresh, 🎤 STT, textarea (`@Persona:` + markdown), Send.
- Renderer `render/BroadcastCardRenderer.ts` + store `stores/BroadcastStore.ts` (recipient list, card-open persisted).
- `api/ApiClient.ts` — `broadcastToCcSessions({message, require_ack, include_originator})` → `POST /api/commons/broadcast-to-cc-sessions`; surface `{broadcast_id, recipients, filtered_out}`.
- Confirm modal before send; status line; `commons_broadcast_ack` feedback; recipient auto-refresh on persona lifecycle events.
- CSS parity (reuse legacy broadcast styles into shared sheet where possible).
**ACs**:
1. Compose card renders in mux with STT + recipient chips + confirm modal at legacy parity.
2. Send posts to the real endpoint and reflects `recipients`/`filtered_out`.
3. 100% L/B/F on all new TS; functional unit tests + e2e_ui.

---

## Integration & gates (Tiberius)
- Serial-merge convergence files; keep `?classic=1` legacy path intact.
- Per-feature: 100% coverage + functional E2E green BEFORE commit-held.
- Test venue routing per CLAUDE.md: unit/smoke on :7999; e2e_ui/integration scheduled on :8000 (self-authorized on verified-idle).
- **Commit + local merge** when reviewed-green (standing). **Push is Rick's word alone.**

---

## Commit staging (SELECTIVE — never `git add -A`)

The working tree carries **foreign concurrent work** (arbiter / heartbeat-hooks / lancedb / config from other sessions) that MUST NOT be staged. `dist/multiplexer` is **gitignored** (bundle not committed; deploy rebuilds from source). Stage ONLY these engagement files:

**Modified (14):** `css/notifications.css` · `css/shared/notifications-surface.css` · `html/multiplexer.html` · `js/multiplexer/api/ApiClient.ts` · `js/multiplexer/boot.ts` · `js/multiplexer/render/index.ts` · `js/multiplexer/render/templates/senderCard.ts` · `js/multiplexer/shared/types.ts` · `js/multiplexer/stores/SenderStore.ts` · `js/multiplexer/stores/index.ts` · `js/notifications.js` · `tests/unit/multiplexer/render/templates_sender_card.test.ts` · `tests/unit/multiplexer/sender_store.test.ts` · `tests/unit/multiplexer/sender_store_hydrate.test.ts`

**New (15):** `css/multiplexer/broadcast.css` · `js/multiplexer/render/BroadcastCardRenderer.ts` · `js/multiplexer/render/templates/broadcastCard.ts` · `js/multiplexer/stores/BroadcastStore.ts` · `rnd/v0.1.9/2026.06.24-focus-bar-parity-build-plan.md` · `rnd/v0.1.9/2026.06.24-lane-c-broadcast-port-convergence-handoff.md` · `rnd/v0.1.9/2026.06.24-notifications-multiplexer-focus-bar-parity-gap-list.md` · `tests/e2e_ui/test_multiplexer_broadcast_card.py` · `tests/e2e_ui/test_multiplexer_worker_badge_silencing.py` · `tests/e2e_ui/test_worker_badge_silencing_legacy.py` · `tests/unit/multiplexer/broadcast_api_client.test.ts` · `tests/unit/multiplexer/broadcast_store.test.ts` · `tests/unit/multiplexer/render/broadcast_card_renderer.test.ts` · `tests/unit/multiplexer/render/templates_broadcast_card.test.ts` · `tests/unit/notifications_js/worker_badge_silencing.test.ts`

**EXCLUDE (foreign — do NOT stage):** `TODO.md`, `lupin-app.ini`, `lupin-app-splainer.ini`, `src/cosa/agents/heartbeat_arbiter/*`, `src/cosa/rest/arbiter_bootstrap.py`, `src/lupin_arbiter_app/*`, `src/lupin_cli/claude_code/hooks/*`, `src/scripts/cascade_heartbeat_scheduler.py`, `src/tests/unit/test_stop_hook_heartbeat.py`, `src/tests/unit/test_role_goal_poke_lines.py`, `src/rnd/...lancedb...md`, `src/scripts/lancedb_incident_recovery_2026_06_23.py`. Verify with `git diff --cached --stat` before commit.

Gate before commit: Tester combined :8000 e2e green. Coverage: per-file c8 100% L/B/F confirmed by crew + Rachel on every changed/new file; whole-tree unit **1848/1848** (background run). dist gitignored → no bundle commit; deploy rebuilds.

## Execution log
- 2026-06-24 — Gap list ruled (Rick); build plan authored (Tiberius).
- 2026-06-24 — Crew spin-up: 2 implementers spawned via `spawn_sessions`. **Clayton 😎** (`cc-author-tiberius-1`) → Lane A; **Krishna 🦚** (`cc-author-tiberius-2`) → Lane C. Both spawn-acked + persona-verified. Manager-tracking store item `34466d69`.
- 2026-06-24 — Ruling: build **in-tree** (no worktrees). Lanes are file-disjoint except convergence files; Tiberius owns `shared/types.ts`, `boot.ts`, `multiplexer.html` (serial-merge). No destructive git on shared tree. Reviewer + Tester spawn at the gate.
- 2026-06-24 — Tiberius merged convergence files for Lane A: `shared/types.ts` → `SenderRecord.is_worker?: boolean`; `css/shared/notifications-surface.css` → worker rule (`display:none` count + faint `worker-activity-pulse` dot on `.sender-stats-group::after`, keyed on `.sender-card[data-worker="true"]`). One sheet, both clients link it (notifications.html:13 + multiplexer.html:15) → drift-proof.
- 2026-06-24 — **Lane A GREEN + review-ready** (Clayton 😎): legacy (`notifications.js` `_isWorkerSender`/`_applyCardWorkerFlag`, data-worker on strip+card, count suppressed at **3** strip-count write sites — one beyond the plan's 2 — `notifications.css` ::after carve) + mux (`senderCard.ts` gate, `SenderStore.ts` is_worker from assigned+hydrate, fill-forward-never-regress). Receipts: tsc 0, eslint 0, 1744/1744 unit pass, c8 100% L/B/F on senderCard.ts + SenderStore.ts, fresh build `boot.90985659ccaf.js`. **Lane B VERIFIED** (collapse toggle already wired 2026-06-23, 11/11 dedicated tests, present in fresh dist — no gap, per Decision C).
- 2026-06-24 — Gate running: Reviewer **Rachel 🕊️** (verified) + Tester (`d163d7ae`, **persona-null** — named pool exhausted, `*` overflow didn't allocate; functional by role, declined self-grab). Clayton authored P1 e2e for BOTH surfaces (`test_multiplexer_worker_badge_silencing.py` mux 3 + `test_worker_badge_silencing_legacy.py` legacy 4 = 7 tests, py_compile clean, collect-only green) → handed to Tester; near-collision with a Tester duplicate stub resolved (Clayton's files canonical, stub deleted). Commit HELD pending green AND adversarial-review.
- 2026-06-24 — **Rachel verdict: ✅ PASS** (adversarial). Call-site enumeration: exactly 3 count-PRODUCING sites in legacy, all gated; other 2 `data-unread-count` are CLEARS, correctly ungated; NO 4th. Mux single render site `senderCard.ts:172` `is_worker!==true` correct; is_worker flows via SenderStore. Non-worker path provably untouched; parity of `managerPresent` reads confirmed (not a defensive-getattr violation); tests non-vacuous both directions. ONE non-blocking finding: mux regular-notification path does NOT fill-forward `is_worker` (only assigned+hydrate do) → narrow silent-WS-reconnect window where a worker could flash a count; legacy stamps from every notification. Minor edge (re-parent-to-root never-regress) accept-as-designed.
- 2026-06-24 — **Manager decision: LAND the hardening NOW** (not fast-follow) — parity + provably-zero-flash for a 1-liner. Re-looped Clayton: add fill-forward `if ( managerPresent( n.payload ) ) record.is_worker = true` (set-true-only) in SenderStore regular path + 1 test (worker-plain-notification-first → suppressed) + re-green (tsc/eslint/unit/c8 100%). Rachel to quick-verify the delta, then Lane A folds into commit-held.
- 2026-06-24 — **Lane C code-complete + green** (Krishna 🦚): 5 new/edited files (broadcastCard.ts, BroadcastStore.ts, BroadcastCardRenderer.ts [STT+chips+confirm-modal+send+status], ApiClient.broadcastToCcSessions, broadcast.css) + 4 test files. Receipts: tsc 0, eslint 0, stylelint 0, **47 unit pass, c8 100% L/B/F** on the 3 new TS; whole-repo TS suite **1844/1844**. Self-contained — NO EventBus/types/StorageService edits (recipient auto-refresh rides existing `store_session_strip_changed`). Handoff doc: `2026.06.24-lane-c-broadcast-port-convergence-handoff.md`.
- 2026-06-24 — **Tiberius serial-merged the 4 Lane-C convergence files**: `stores/index.ts` (broadcast store), `render/index.ts` (renderer barrel), `boot.ts` (import + construct/mount after commonsActivity, `getAuthToken: () => cachedAccessToken`), `multiplexer.html` (`#broadcast-card-mount` above commons-activity + `broadcast.css` link). Verified: **tsc --noEmit exit 0, eslint exit 0**. **Bundle NOT rebuilt** — esbuild rebuild GATED until the Tester's live Lane-A e2e on :8000 completes (a rebuild would flip the runtime manifest mid-run and the old bundle doesn't expect `#broadcast-card-mount`).
- 2026-06-24 — Tester (`d163d7ae`) scheduled Lane-A e2e on verified-IDLE :8000 (job ts-ea09b5d3, queue 0): 20 tests (Clayton's 7 worker-badge + 13 regressions manager/missed/reap). Sharp evidence-based **no-bounce** call (manifest already serves boot.90985659ccaf.js; Lane A pure-frontend zero-Python) — endorsed. **Lane C still needs adversarial review (Rachel) + its own e2e before its gate.**
- 2026-06-24 — Tester found+fixed **two test-only harness bugs** in `test_multiplexer_worker_badge_silencing.py` (selector `#section-notifications`→`#sender-cards-container`; missing `id_hash` → `NotificationStore.normalize()` dropped the synthetic notification). Product logic untouched. Clayton rebuilt dist to **a41b17952cc6** when landing his fill-forward hardening — which ALSO integrated the Lane C convergence merge, so the bundle now carries BOTH lanes (process note: rebuild mid-`:8000`-run flipped the manifest; no harm, Tester adapted; Clayton acked the coordination rule).
- 2026-06-24 — **Lane A e2e GREEN: 20/20 on :8000** (job ts-605fd2bc, build a41b17952cc6) — 7 worker-badge (3 mux + 4 legacy) + 13 regressions (manager/missed/reap). Combined bundle boots clean (no throw on `#broadcast-card-mount`) → Lane C wiring runtime-clean.
- 2026-06-24 — **Rachel BLESSED Clayton's Lane A delta — Lane A FULLY GATE-PASSED** (green ✓ + adversarial review ✓ + e2e 20/20 ✓). Verified: fill-forward set-true-only on BOTH regular-path branches (new L272 + existing L280; assigned L320 authoritative preserved), worker-plain-first test both branches, old-gap test re-conditioned, 55/55 + c8 100%. One non-blocking nit (never-regress semantic self-documenting assertion) → landing now via Clayton (test-only, no rebuild). Then folds to commit-held.
- 2026-06-24 — Lane C now in adversarial review (Rachel); Krishna authoring the Lane C broadcast-card e2e P1 (with the two harness lessons baked in) → Tester owns the combined `:8000` run.
- 2026-06-24 — **Rachel: LANE C VERDICT ✅ PASS** (adversarial). Send-path TRIPLE-guarded (disabled btn + click gate + showConfirmModal 0-recipient early-return; recipients/filtered_out surfaced, no silent fanout miss); confirm-modal removeModal()-first + body-mounted isolation (no overlay leak / re-trigger); @Persona literal-insert parity with legacy (server _parse_body owns scope); XSS-clean (no innerHTML, textContent + auto-escape html helper); broadcast POST rides authenticated this.post (Bearer/JWT); 10/10 renderer↔template IDs present; store hydrate robust (non-array guard + corrupt-storage guards). 46/46 + c8 100% on all 4 Lane C files (incl ApiClient). Two non-blocking nits: (1) recipients() docstring promises defensive copy but returns reference → **HONOR NOW** (Krishna, `return [ ...this.recipientCache ]`); (2) refreshRecipients no debounce → **follow-on** (matches legacy; task filed). Parity Q: multi-word @Persona server-parse → Krishna to confirm (non-gating). **Both lanes now adversarially reviewed.**
- 2026-06-24 — **Lane C e2e P1 authored** (Krishna): `src/tests/e2e_ui/test_multiplexer_broadcast_card.py` (3 tests, collect-green; harness lessons baked in — real mux selectors, REST-hydrate avoids id_hash trap, exact server route-stub shapes). Routed to Tester → **combined :8000 run** (3 broadcast + 20 Lane-A sanity = 23) on a41b17952cc6. Nit-1 lands in parallel (inert, read-only consumer); ONE final rebuild + whole-tree c8 --100 before commit-held.
- 2026-06-24 — **Nit-1 landed + Rachel-blessed**: `BroadcastStore.ts:108 return [ ...this.recipientCache ]` (true list-level defensive copy, docstring now matches, c8 100%, 40/40). **Lane C review fully CLOSED — both lanes through adversarial review, zero open findings.** Remaining gate: Lane C e2e (Tester combined run). Then ONE final esbuild (folds nit-1; nit-1 inert so the a41b17952cc6 e2e stays valid) → whole-tree c8 --100 → commit + local merge (push = Rick). Clayton reaped (memento clayton-…1735.md); crew now Krishna/Rachel/Tester; Rachel → prepare-for-re-spin.
- 2026-06-24 — **LANE A FULLY CLOSED** ✅ — Clayton landed the never-regress self-doc test (worker stays is_worker=true after a later no-manager plain notification). Receipts: SenderStore set **63/63**, c8 SenderStore.ts **100% L/B/F**, dist a41b17952cc6 unchanged (test-only). Lane A = green + Rachel-blessed + 20/20 e2e + invariant documented. **Folded to commit-held** (commits together with Lane C; push = Rick). Clayton → prepare-for-re-spin (memento) → clean reap.
