# Mr. Radio 🦉 — Re-Spin Memento (mux-parity Plan-01 build drive)

**Date**: 2026-06-29 (~21:30 EDT) · **Outgoing session**: 2f4feb0a · **Persona**: Mr. Radio 🦉 (manager)
**Re-spin trigger**: Rick "prepare to be respun." Safe checkpoint: all lanes dispatched, NO half-edited files by me.

> **READ-FIRST on respin**: you are the MANAGER of an active 3-lane build fleet. Do NOT reap the workers — they are in-flight. Pick up supervision immediately.

## 0. Standing directives (Rick, this session — LOAD-BEARING)
- **DRIVE the ratified Plan-01 bucket chain (B1→B5) to completion. Keep going until Rick says STOP.** Do NOT pause at "natural stopping points," do NOT ask continue-vs-pause — that's a penalized passive move. When one bucket lands, immediately staff the next. (Memory updated: [[feedback_not_a_gate_drive_to_completion]].)
- **MANAGE, don't build.** Delegate every build to a worker; you REVIEW (adversarial) at commit, never self-implement. (Rick corrected me hard mid-session for hand-editing a plan.)
- **I review EVERY commit before it lands held.** Commit/merge on standing authority once reviewed-green. **PUSH is Rick's word ALONE** — never push.
- **Parallelize** — Rick pushed for more workers. Independent lanes get their own worker; file-colliding lanes get worktree isolation + manager-serial-merge.

## 1. Active worker lanes — SUPERVISE THESE (do not orphan)
| Worker | Session | Task | What | Status / your job on return |
|---|---|---|---|---|
| **Sam 🎙️** (cc-author-mr-radio-2) | d560353f | a84552cc | **B3** — section-header controls (count=list().length, history-dropdown, clear-all) + own-only NotificationStore filter | BUILDING full mechanism. **RULED Option A**: matchesNotificationFilter keys on `direction` (own=inbound/!=="outgoing", others==="outgoing", all=true); default+only-wired="own"; admin toggle/badge/gating DEFERRED. Clear-all = delete-by-id over visibleEntries via DELETE /api/notifications/{id_hash} (N-non-atomic). → expect done-ready DM; **YOU do the ownership review (01-C Stage-3 was token-waived)** — chase store-ownership boundaries (who owns filterMode, who triggers renderAll, no cross-store leak). |
| **Rio ⚡** (cc-author-mr-radio-1) | b8357f19 | 6c092ed0 | **B4** — KEYSTONE per-message ⏸/⏹ + proxy-ratify | In **OWN WORKTREE** (collides w/ B3: both touch NotificationsListRenderer.ts + apiClient). COND-2 (load-bearing): B4 makes ZERO TtsQueueStore mutator calls, current() READ-ONLY, stop()=de-light not advance. Subscribe store_tts_queue_changed (gate on current()===id_hash) + store_audio_state_change; clear-prior-then-set (one bubble). → expect done-ready DM; **serial-merge his worktree lane** after review. |
| **Clayton 😎** (cc-author-mr-radio-3) | db55dd23 | b0397874 + 414e6957 | Two pre-existing RED fixes | b0397874 (P3): governance test hermeticity (force enable-env OFF in-test). 414e6957 (P2): remove residual mux `.sender-card[data-collapsed]` rule (complete WS2 C2-b single-sourcing). Disjoint files, main tree. → expect done-ready DM(s); review at commit. |

**Comms**: workers report to topic **dm-mr_radio** (you ARE mr radio on respin). Check `dm_list`/inbound DMs first thing.

## 2. Tiberius 👑 (session 8d3af389) — SEPARATE peer manager (NOT yours to manage)
- Arbiter staleness fix **25ba173e** committed-held (commit **07cd43d4**), running his own crew. Self-driving.
- Holds the **manager-language MANAGE-not-BUILD apply** (§1 goal-echo live-retune / §2 stop-hook reinstall / §3 arbiter bounce) — WAITING on Rick+you to lock the finalized strings. **María is coordinating the wording** (tap-site→role catalog). Audit doc: `src/rnd/v0.1.9/2026.06.29-manager-facing-injection-language-audit.md`. When you + Rick lock strings, DM Tiberius to apply.

## 3. The held bundle — 6 commits on `wip-v0.1.9-2026.06.21-bug-fix-implementation`, PENDING RICK PUSH
1. `a0a10692` — 91788c40 Fleet#6/Task#7 verify closure (MVP set).
2. `af821e92` — 2605dca5 F0-f back-fold (00b plan).
3. `8dd5a718` — d647b531 reap-reconcile (session_spawner auto-reconcile on dismiss).
4. `5906508f` — 25eced88 B1 (mux Notifications section-reorder + commons re-nest).
5. `07cd43d4` — Tiberius's arbiter 25ba173e (his commit).
6. (+ Sam's gate-resolution `a6a1c361` from the B1 collapse investigation.)
**Push is Rick's alone.** Pre-existing reds b0397874 + 414e6957 block MERGE-to-main (not the WIP push) — Clayton fixing now.

## 4. Bucket chain — order + next steps
**B3 → B2 → B4 → B5** (B1 done). Feed the next as deps land:
- **B2** (01-B): relocate TTS-preview slider INTO B3's section header (PLACEMENT-ONLY). Needs B3's header → assign a worker once B3 lands.
- **B4** (01-D): in flight (Rio).
- **B5** (01-E): CSS single-source into shared sheet + Oracle T2/T3 + golden rebaseline. GATED LAST — pins against B3's finalized selectors. Assign after B3/B2/B4 CSS settles.

## 5. Pending Rick decisions (surface, don't gate work on them)
1. **Push the 6-commit bundle** (his word).
2. **Lock the manager-language MANAGE-not-BUILD wording** (with María/Tiberius) → then Tiberius applies.

## 6. Key artifacts
- Plan: `src/rnd/v0.1.9/2026.06.25-notifications-to-multiplexer-migration-discrepancies/05-build-plans/01-cc-session-B1-B5.md` (§01-A..E = B1..B5).
- Closures: `91-00c-…`, `92-01-…`, `93-fleet-task-…` (same dir).
- Gate resolution: `…/2026.06.29-per-card-collapse-double-ownership-findings.md` (B1 builds forward on HEAD, no branch coordination).
- F0 foundation plans: `00b-f0-audiostore-foundation.md` (+ F0-f), `00c-phase6-tts-playback.md`.

## 7. ON RESPIN — DO FIRST (checklist)
1. MCP Phase A (get_session_info, confirm persona = Mr. Radio 🦉).
2. `task_query(owner_persona="mr radio")` + check inbound DMs (dm-mr_radio) for done-ready from Sam/Rio/Clayton.
3. Any done-ready → adversarial review → commit-held (selective stage) → feed next bucket. (Sam→then B2; Rio worktree→serial-merge; Clayton reds→merge-to-main unblock.)
4. Refresh heartbeat hold (work_owed=true, awaiting peer:crew). Hold file: `.heartbeat-hold-2f4feb0a-f958-4343-9fcf-4d4d1ce0a1f4.json`.
5. Do NOT reap the 3 in-flight workers. Keep driving B3→B2→B4→B5 until Rick says STOP.
