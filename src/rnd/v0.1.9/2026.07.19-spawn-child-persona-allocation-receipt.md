# Receipt — spawn_sessions child persona allocation (task 179fe3ab)

**Date**: 2026-07-19
**Seat**: Arnold 🪨 (session `1c295071`)
**Task**: `179fe3ab-9a42-4351-9ca0-213654e69a5d` — "spawn_sessions child skipped persona allocation (voice_persona=null)"
**Instruction**: Rick, relayed by Rio ⚡ — *mark it done, with receipts, not assertion*
**Verdict**: ✅ **GREEN — both directions**, with a stated scope limit (see §5)

---

## 1. Why assertion could not close this row

The row's own finding is that the defect **degrades silently in one direction**:

- **Outbound works perfectly** whether or not the child has a persona.
- **Inbound and attribution break** — the child's actor reads literally `<unknown>`, so a parent `dm_send` returns `recipient_not_found`.

⇒ **The seat with the defect cannot detect it, and the seat that can has already lost the address.**

That asymmetry is why three sessions of writes piled up before anyone noticed, and why the row prescribes a **live re-derivation** rather than a code diff.

---

## 2. Method

Spawned one real child via `spawn_sessions`:

| Parameter | Value | Why |
|---|---|---|
| `count` | 1 | minimum sufficient |
| `role` | `observer` | no work ownership |
| `project` | `lupin` | matches the row |
| `persona_preference` | **`null`** | **deliberately NOT the `["Arnold","*"]` chain** of the prior failed repro — that chain tests a named-fallback path, not the default allocation the row is about |

**The probe brief explicitly FORBADE `request_persona`**, in these words: *"A null is the finding. Repairing it destroys the measurement… A green that you manufactured is worse than a red."*

Without that instruction a null child would likely have self-healed and reported success — the measurement would have been destroyed by the child's own helpfulness. The child confirmed it never called `request_persona`.

---

## 3. Results

### 3a. Child allocation — NOT null

```
session_id:    fa9152a1-ee24-421e-b948-bb130d52fc40
voice_persona: { name: "Rachel", display_name: "Rachel",
                 voice_id: "21m00Tcm4TlvDq8ikWAM", icon: "🕊️",
                 borrowed: false,
                 assigned_at: "2026-07-20T00:43:35+00:00" }
```

Allocated **before the child's first tool call**. `borrowed: false` — a real pool member, not a hash-borrow.

### 3b. Parent → child BY NAME — dispatched

```
dm_send( recipient="Rachel" )
  -> status: "sent", dispatched: true
     recipient_persona: "Rachel"
     recipient_session: fa9152a1-ee24-421e-b948-bb130d52fc40
```

This is **the exact call that previously returned `recipient_not_found`** when the actor read `<unknown>`. Name resolved to a live session and the child replied on-thread — closing the inbound direction, which is the half that was actually broken.

---

## 4. Lineage — closing the child's own caveat

Rachel raised a correct objection before the receipt was banked:

> *"I cannot verify from my own side that I am a spawn_sessions child. My evidence is 'a session that booted named and is reachable by name' — attaching 'spawn-child' to it is your inference."*

She was right, and it is **the parent's instrument to read, not hers**. Closed with three independent bindings:

| Evidence | Value |
|---|---|
| Child transcript contains the verbatim `task_prompt` | `fa9152a1-….jsonl` — `"RECEIPT PROBE for task 179fe3ab"` **×3** |
| tmux session created | `cc-observer-arnold-1` @ `20:43:34` EDT |
| Child `session-env` created | `fa9152a1-…` @ `20:43:35.6` EDT — **the only one** in the `20:43:25–20:43:45` window |
| `list_spawned_sessions` | binds `cc-observer-arnold-1` → manager `1c295071` |

The verbatim-prompt match is the strong link; the session-env uniqueness rules out a coincidental concurrent boot.

---

## 5. ⚠️ Scope limit — stated deliberately

**This closes the `spawn_sessions` CHILD allocation path ONLY.**

It is **not** a claim that persona allocation works generally. Counter-evidence from the same day, same fleet:

> My own **top-level** seat (`1c295071`) booted `voice_persona: null` at SessionStart and ran unattributed for hours.

That is a **different path** and remains **OPEN** under task `86aa79ac` — sharpened there to: *why does the SessionStart allocation path not reach overflow when an explicit no-arg `request_persona()` does, against the same endpoint and same pool state?*

A reader who takes this receipt as "persona allocation is fixed" would be making exactly the adjacency error this fleet logged five times on 2026-07-19: **an instrument authoritative for something adjacent to the question asked.** This document is authoritative for spawn-children. Nothing more.

---

## 6. Related

- `86aa79ac` — top-level SessionStart null path (**OPEN**, owner Mr. Radio 🦉)
- `70f17193` — `request_persona` could not reach allocate Mode 1 (**closed**, commit `32e59475`)
- `f8422ffa` — ORM-model-ahead-of-migration detector (**open**, owner Arnold 🪨)
