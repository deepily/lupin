# Arbiter Persona-Collapse Phantom Fix — SWE-Team Handoff / Problem Statement

**Date**: 2026-06-24
**Manager**: Mr. Radio 🦉 (session `0c427c02`)
**Store bug**: `8a450183` (P2, follow-on to `7f9a8ee2` / commit `ef159f48`)
**Status**: Rick-authorized SWE-team fix (2026-06-24 — "pursue the bounded arbiter code fix with another SWE team and fix the phantom")

> Build to this spec. 100% COVERAGE MANDATE (line + branch + function) applies. Do NOT break the `:1018` invariant (below) and do NOT regress `ef159f48`.

---

## TL;DR

The arbiter loop-fires a **phantom** advisory `"maria is blocking worker mr radio"` (and the periodic `"N blocked: mr radio"`) every poll. The first fix (`ef159f48`, `hold_contradicts_peer_edge`) correctly handles the **live-session contradicting-hold** case but does **NOT** stop this, because the real source is a **DEAD session's stale edge persona-collapsed onto the live persona**. Fix it at **edge-inference level**: a holder **session** whose own last activity is beyond the alive-threshold must contribute **ZERO** peer edges — keyed by **session**, not persona.

---

## CRITICAL LESSON (from a FAILED stopgap — do not repeat)

The Manager tried a stopgap: moved the dead session's heartbeat-events jsonl (`~/.claude/heartbeat-events/7c9092d4-…jsonl`) aside + restarted the arbiter. It cleared for **one** poll (11:44:55, 0 outreach) then **regressed within ~3 minutes** (11:47:57, back to `edges=10, pings_fired=1`). **Conclusion: the arbiter's in-memory accumulator (`self._acc`) re-populates the stale edge from a source the event-file move did not address.** → **Do NOT fix this by manipulating event/bridge files.** Fix it in the **edge-inference logic** so that *any* beyond-alive-threshold session contributes no edge regardless of where its `last_activity` came from. (The file was restored; system is as-found.)

---

## Root cause (pinned)

- `fleet_data_model.py:412`: `"holding_on": ( last_activity.get( "awaiting" ) or "none" ) if last_activity else "none"` — the peer edge is minted from `last_activity.awaiting`.
- Dead session `7c9092d4-805f-499f-8630-c8ebbe0303e1` (persona **mr radio**, role manager, **no live process**, last heartbeat-event `2026-06-24T03:04Z` — ~12h stale, FAR beyond the 3600s alive-threshold) has a final `awaiting="peer:maria,peer:Tiberius,user:rick"` → mints `maria→mr radio` (first-peer rule).
- **The persona-collapse**: the arbiter treats the persona "mr radio" as alive (my live session `0c427c02` IS alive), so the dead session's stale edge survives the `alive_personas` filter (`arbiter_job.py:~1026`, which is **per-persona**) and `ef159f48`'s `_stale_hold_holders` reconciliation reads the wrong/absent hold for the dead sid. Net: a dead session's stale `peer:maria` is attributed to the live "mr radio".
- This is exactly **open-question #1** in `2026.06.24-arbiter-phantom-block-seam-fix.md` (is the fleet view keyed by persona, collapsing a dead session's activity onto the live one?) — **answer: YES.**

## Investigation the implementer MUST do FIRST (the key unknown)

1. **Pin the accumulator source.** Where does `self._acc.snapshot()` (fed into `build_fleet_view` at `arbiter_job.py:969`) get `last_activity` from, and why did a fresh restart re-ingest `7c9092d4`'s 12h-old `awaiting` within ~3 min? (Tail watcher? bridge re-read? persisted accumulator state?) Confirm before designing.
2. **Confirm the alive-threshold is NOT applied per-session at edge-inference.** `build_fleet_view`/`build_wait_edges` (`dependency_graph.py`) + the `alive`/`alive_personas` computation — establish that a beyond-threshold *session* can still mint an edge when its *persona* is alive elsewhere.

## Fix direction (design it; confirm the investigation first)

**Gate peer-edge inference on the HOLDER SESSION's own freshness.** A session whose `last_activity` timestamp is older than `alive_threshold_seconds` contributes **zero** `holding_on` peer edges — decided per **session id**, never collapsed to persona-liveness. Apply **upstream of all edge consumers** (the FILTERED advisory/ping graph), **additive + fail-SAFE** (missing/unparseable timestamp → fall back to current behavior; never suppress a *real* escalation). Compose with — do not replace — `ef159f48`'s `hold_contradicts_peer_edge`.

## HARD INVARIANT — do NOT break (same as `ef159f48`)

The UNFILTERED store-corroborated deadlock escalation at `arbiter_job.py:1018` (`find_deadlock_cycles( build_wait_edges( fleet_view ) )` → `_escalate_deadlocks`, gated by `cycle_is_store_backed`) must stay UNFILTERED. A real store-backed `X→Y→X` ring must still escalate even with a stale/contradicting/dead participant. Any `stale_holders`/freshness gating threaded into `:1018` = automatic BLOCKING review. The `alice↔bob` veto test from `ef159f48` must still pass.

## Tests (Test-Ownership; 100% L/B/F; venue :7999)

1. **Dead-holder** (last_activity beyond alive-threshold) + `holding_on=peer:X` ⇒ **no edge** (the regression case; assert no `maria→mr radio` advisory/cc).
2. **Persona-collapse scenario**: a DEAD session + a LIVE session, same persona "mr radio"; dead one has stale `awaiting=peer:maria`, live one has `awaiting=none/user:rick` ⇒ **no phantom edge** attributed to the persona.
3. **Alive holder, genuine fresh peer wait** ⇒ edge **survives** (no over-filter).
4. `ef159f48` cases still green (fresh contradicting hold drops edge; matching hold keeps it).
5. **`:1018` veto**: real store-backed `alice↔bob` ring STILL escalates with a dead/contradicting participant.
6. Full arbiter/heartbeat suite ≥ baseline (prior baseline: 125 cosa + 1276 unit incl. `ef159f48`'s tests).

## Repro (deterministic, no live arbiter)

Build a `fleet_view` with two "mr radio" entries: a DEAD session (last_activity ~12h old, `holding_on=peer:maria`) and a LIVE session (fresh, `holding_on=none`). Run edge inference + advisory path; assert **no** `maria→mr radio` edge. Write this RED first.

## SECOND WORK ITEM — timestamped arbiter outreach (Rick, 2026-06-24)

Rick wants **every** arbiter shoulder-tap / outreach message (the ones DM'd to a person — blocker pings, manager-blocking cc, the "N blocked" advisory, manager-down/deadlock escalations, stall pokes) to **carry a formatted timestamp** in the style:

```
YYYY.MM.DD at HH:MM:SS
```

- **Where**: the outreach/ping/advisory message TEMPLATES in `arbiter_job.py` — e.g. `PING_MESSAGE_TEMPLATE` (~:111), the manager-blocking cc template (~:1748-1749: `"Heartbeat arbiter (cc): {blocker} is blocking worker {blocked_worker}..."`), the `_format_manager_tap` / `_attention_workers` advisory text, and any other `arbiter_outreach` summary that reaches a human. Sweep them ALL — enumerate every outreach message string (Reviewer: verify completeness).
- **Format**: `YYYY.MM.DD at HH:MM:SS` (e.g. `2026.06.24 at 11:47:57`). The arbiter already stamps each poll (`ts`/`ts_local`) — derive from the same poll clock; do NOT introduce a new `datetime.now()` call site if an existing poll timestamp is threadable (keep it testable/injectable). Place the stamp consistently (recommend a leading prefix, e.g. `[2026.06.24 at 11:47:57] <message>`), but the Manager will confirm placement.
- **Timezone = EDT, from CONFIG (Rick, 2026-06-24)**: render the stamp in the configured local tz, NOT hardcoded. **REUSE the existing arbiter tz machinery** — `arbiter_journal.py` already resolves the INI key **`arbiter journal local timezone`** (default `America/New_York` → DST-aware EDT/EST) via `ZoneInfo`, degrade-safe (`_resolve_tz`: invalid name → one loud event + UTC fallback, never crashes). Do NOT build new tz infra; thread the same resolved `ZoneInfo` into the outreach formatter. (There is also a broader `app timezone` key, same default — but the arbiter-local key is the right one for arbiter outreach.)
- **Tests**: assert each outreach template renders the timestamp in the exact format, with an INJECTED clock (no wall-clock in tests). 100% L/B/F on the changed lines.
- **Decoupling**: this is independent of the persona-collapse fix; it can be its own commit (cleaner) or bundled — Manager's call. It does NOT touch edge-inference, so it's lower-risk; still gate it green+reviewed.

## THIRD WORK ITEM — outreach-DM throttle state (Rick, 2026-06-24)

Rick wants the arbiter to **track state about outreach DMs sent — a count + the last-sent timestamp — so the fleet can THROTTLE them.** This is the rate-limit safety net that complements ITEM A's root fix (A reduces *why* so many fire; C *caps* the rate regardless).

- **REUSE the existing throttle module**: `src/cosa/agents/heartbeat_arbiter/ping_throttle.py` already exists — examine it first; extend it rather than building parallel machinery.
- **Track**: number of outreach DMs sent + the **last-sent timestamp** (per the Manager's read of Rick: at minimum per-recipient; consider per-(recipient,kind)). Use the formatted EDT stamp from ITEM B for the human-facing last-sent value where surfaced.
- **Throttle policy (Rick, 2026-06-24): N messages per Y minutes** — a trailing-window rate limit. **Both N and Y are RUNTIME-CONFIGURABLE via `lupin-app.ini`** — add two keys (suggested: `arbiter outreach throttle max messages` = N, `arbiter outreach throttle window minutes` = Y) **WITH matching `lupin-app-splainer.ini` entries (mandatory — every new INI key needs a splainer)**. The tracked count + last-sent-timestamp feed the window check: **suppress an outreach when N have already been sent within the trailing Y minutes.** Confirm whether `ping_throttle.py` already implements a window this plugs into vs needs adding.
- **Purpose**: cap tap volume + give visibility into frequency.
- **Open design Qs for the implementer to resolve or flag to the Manager**: scope (per-recipient vs global vs per-(recipient,kind)) · persistence (in-memory across polls vs survive arbiter restart) · whether to expose the state on an observability surface (e.g. the poll_activity journal line). Keep bounded; flag scope creep.
- **Tests + 100% L/B/F.** Injectable clock (no wall-clock in tests).

## Gate & reporting

- Manager holds the gate: **green AND adversarially-reviewed** before commit. Manager commits + merges on green+reviewed; **push = Rick's word**.
- Crew DMs the Manager (`dm-mr_radio` / returned `collection_topic`) at spawn-ack, on-completion/verdict/result, on-blocker immediately.
- No-confabulation: cite primary artifacts (test output, file:line, counts). **And: do NOT claim the phantom "verified cleared" until you've watched MULTIPLE consecutive live polls fire zero outreach — a single clean poll is insufficient (that exact mistake cost the Manager an overclaim today).**
