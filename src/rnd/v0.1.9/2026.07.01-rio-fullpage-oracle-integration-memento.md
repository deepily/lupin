# Memento — Rio ⚡ (full-page parity-oracle + batch integration)

**Date:** 2026-07-01 · **Author:** Rio ⚡ (session `5a1b1c4c`) · **Manager:** Mr. Radio 🦉
**Trigger:** Rick re-spinning the MANAGER (not me — workers survive a manager re-spin; I stay warm).
**Purpose:** so the re-spawned Mr. Radio inherits a complete picture and can drive the wip fast-forward + Rachel's final verify.

---

## Current checkpoint (HELD — no push, no merge into wip-proper)

- **Integration branch:** `mux-fullpage-integration` @ **`16a2dcc5`**, off wip tip **`362318d8`**.
- **Worktree:** `../lupin-wt-fullpage-integration` (sibling to main repo; `node_modules` symlinked → `../lupin/node_modules` for the TS gates — NOT committed).
- **Merges (both CLEAN, disjoint):**
  - `191d98fc` ← `mux-parity-fixes` (Sam: **H2** `a81b2114` env-label+clock, **R5** `16d0952c` session-topic/name).
  - `7f49c3c0` ← `mux-fullpage-oracle` (`11a38cf6`, my page-chrome oracle).
- **My post-merge commit `16a2dcc5`:** V5 header-region boundary fix + V2 sentinel flip (details below).

## What the full-page chrome oracle is

New page-scoped parity harness at `src/tests/parity_oracle_fullpage/` (sibling to the sender-card `parity_oracle/`), + page-chrome helpers appended to `src/tests/e2e_ui/parity_oracle.py`, + new legacy golden `src/tests/e2e_ui/fixtures/golden/notifications-legacy-fullpage.golden.json` (SEPARATE from the sender-card golden). Closes the "no legacy-referenced page-chrome proof" hole from the 2026.07.01 adversarial matrix. Three honest parity categories (chrome is NOT uniformly single-sourced):
- **Cat A** (style+geom ISO valid): V1 nav (shared `lupin-nav.css`).
- **Cat B** (structural+geometric only; look-fidelity = Rachel): V1-logout, V2 env/clock, V3 AR, V5 header, V7 broadcast.
- **Cat C** (display-state / mux-native): V4 PLY + V9 strip idle display divergences; V13 toolbar.

## Batch-merge hardening (commit `16a2dcc5`)

- **V5 boundary fix (Rachel's verdict):** mux V5 node → `.notifications-header-region` (spans BOTH `#notifications-header-mount` + `#tts-preview-slider-mount`), not the sub-mount alone. Legacy nests the TTS slider inside its header (956px); the mux splits it to a sibling mount, so header-mount alone reads 733px — a FALSE gap. New `test_tier3_notifications_header_full_width` asserts EACH region is full-width (mux ~960 not cramped 733). The cross-client 2px-inset delta (legacy 956 vs mux 960) is **intentionally NOT asserted** — Cat-B look-fidelity deferred to Rachel/Mr. Radio (Mr. Radio endorsed: not a bug).
- **V2 sentinel flip:** H2 now renders env-label + live clock → `V2-env-label` + `V2-clock` promoted from `KNOWN_OPEN_CHROME_ROWS` (expect-ABSENT break-on-close sentinel) to Tier-1 **present-required**. `KNOWN_OPEN_CHROME_ROWS` emptied (kept as the future-gap hook).

## Gate results

| Gate | Result |
|---|---|
| tsc (merged tree) | ✅ clean |
| TS unit suite (`tsx --test`) | ✅ **2064 pass / 0 fail** |
| full-page oracle (live :7999) | ✅ **10/11** (see V2 note) |
| pure-logic unit (`test_fullpage_chrome_oracle.py`) | ✅ 23 pass; `_fullpage_helpers` 100% L/B/F |
| c8 --100 directory-wide | ⏳ **PENDING** (background; see below — update on completion) |

## THE ONE non-green — V2 live-browser (serve-freshness, NOT a code defect)

`test_tier1_present_required_rows` reads V2 env/clock ABSENT on live :7999 because `lupin-rest-dev` bind-mounts the **MAIN-tree** bundle (`docker-compose.yml:133`), which lacks H2 (H2 isn't in wip yet — it's in THIS held merge). H2 IS proven at unit level by `notifications_header_renderer.test.ts` (inside the 2064-pass suite).

**Mr. Radio's RULING (2026-07-01 14:13, msg `5551a7a1`):** ACCEPT the unit-level H2 proof; let the live-browser V2-present go GREEN **naturally** once wip fast-forwards + the dev bundle rebuilds. **Do NOT rebuild the served :7999 bundle pre-merge** (shared-infra, other sessions live; reversible/safe default wins). 10/11 with the one serve-freshness non-green is the correct hold state.

## Next steps (for re-spawned Mr. Radio)

1. Confirm the **c8 directory-wide GREEN** (I report it into thread `d4e253a8` + append here on completion).
2. Drive the **wip fast-forward** of `mux-fullpage-integration` (HELD for Rick's push — push is Rick's word alone).
3. **tmux-wake Rachel** for the final merged-tree verify.
4. **Merge ordering:** a PARALLEL index-1 worker is running a SEPARATE `mux-mvp-integrate` (l1–l4 lanes) — coordinate wip fast-forward ordering between the two integrations.
5. Post-merge, the live-browser V2-present flips GREEN automatically (dev rebuild).

## Task-store anchors
- Integration task: `3ec1089f` (owner rio, mgr mr_radio, P1).
- Oracle-build task: `aa2b0010` (done, receipt commit `11a38cf6`).
