# Mux MVP-Finish — Build Execution Log

> **Execution log** (90-series companion to the plan `2026.06.30-mux-mvp-finish-remediation-plan.md`).
> **Manager:** Mr. Radio 🦉 (session `ef70b5f4`). **Branch base:** `wip-v0.1.9-2026.06.21-bug-fix-implementation`.
> **Status as of 2026-06-30 ~22:33 EDT:** ✅ **BUILD COMPLETE — all 4 lanes committed-held.** Integration
> (merges + :8000 Oracle/E2E) + Rick's visual sign-off + push + flip remain. **Push + flip = Rick's word alone.**

## Discipline applied
MANAGE-don't-build (every lane a visible `spawn_sessions` worker; Mr. Radio reviewed adversarially at commit,
never wrote lane code). Per-lane worktree isolation → reviewed merges only. 100% L/B/F on all changed TS.
Server-side data-shape verification before any port (Rick's cascade lesson: 2 client-only-grep facts overturned).

## Rick's decisions applied (from the cascade review)
- **OQ-1 NAV label** → KEEP "Notifications" (L4 `NAV_ITEMS`).
- **OQ-6 V9 strip-icon** → BUILD as a PORT (legacy renders it; `notifications.js:5087-5106/:10930-10973`).
- **OQ-7 V10(b) topic** → DEFERRED post-flip (product choice; server source exists, not a blocker).
Freeze lifted + build authorized (Rick, 2026-06-30). Reconciliation ground-truth-verified by Tiffany.

## Lanes — all committed-held (reviewed-green, push held)

| Lane | Worker | Commit | Scope | Review invariant VERIFIED (not trusted) |
|---|---|---|---|---|
| **L1** Interactive Bugs | Tiffany 💍 | `d9d8d651` | Bug#2 URIError (raw/raw); Bug#1 progress-group head 176→1 (senderCard election + pre-filter) | `buildHistoryFragment` (`NotificationsListRenderer:690`) reads `headIdHash` from the **rendered** `.progress-group-head` (`:543`), so it consumes the single elected head **by construction** — no divergence. `progress_group_id` single-sender by construction (`notifications.py:539` + deep_research one-group-per-job + `resolve_sender_id:700` + mux `bySender:339-368`). |
| **L2** AR + PLY panels | Sam 🎙️ | `f48b0bf0` | AR amber→blue + shared empty-state helper; PLY green accordion + idle empty-state | ONE `renderActionRequiredEmpty()` called from all owner-branches (anti-drift); 2 added branches (clearEmpty-before-widget, last-remove repaint) necessary + directly tested incl. negative one-of-two; PLY `Exclude<"idle">` hides no arm (idle short-circuits; 5 active arms + idle empty = 6 states covered). |
| **L3** VIS polish | Krishna 🦚 | `ce164056` | V5 new header CSS; V6/V7 inline; V9 strip-icon PORT; V10a redundant #id drop | V9 all 3 arms + negative no-icon + removeAttribute branch; `voice_persona.borrowed` + `StripSession.active` server-verified before porting; V10a `sessionHash` const stays live (consumed by `renderVoiceInputRow:160`) → c8-ignore not masking dead code. VIS off the shared sheet (grep-no-residual). |
| **L4** NAV top-bar | Clayton 😎 | `6c20b7c3` | PORT `lupin-nav.js` → `NavBarRenderer`; logout persisted-token fix; auth-state re-render | D1 logout OUTCOME test asserts **persisted tokens ABSENT** (`getAccessToken/getRefreshToken===null`) via the REAL `authGuard.logout()` — would fail on the `clearToken`-only bug, not false-pass. `clearTokens` (not `clearToken`) confirmed. D2 email-null arm + D3 `auth_state_change` transition covered. |

## Carve-outs (shared files — all resolved)
1. **`senderCard.ts` L1↔L3** — L1 election `:186-205` vs L3 V10a single-line del `:156`. Non-overlapping (Krishna's precise hunk `@@ -156 +155,0 @@`). Reconciles at merge; **gate E4** = `tsc` + `c8 --100` on merged `senderCard.ts`.
2. **`multiplexer.html` L3↔L4** — L3 `<head>` `<link>` (notifications-header.css) vs L4 `<body>` `#lupin-nav-mount`. Different regions → auto-merge.
3. **`NotificationsListRenderer.ts` + test L1↔L2** — **DISSOLVED**: L1's election moved entirely into `senderCard.ts` (single-sender proof), so L1 never touches NLR → L2-exclusive.

## Verifications
- **V-P6** (Phase-6 audible-speak, server-push wire) → ✅ **GREEN** 3/3 on :7999 (`test_phase6_tts_playback_protocol_e2e.py`, paid opt-in). Phase-6 stays closed (does NOT re-open).
- **V-B5** (B5 CSS parity Oracle) → deferred to the :8000 integration Oracle pass.

## Remaining (integration phase — Mr. Radio owns; post-11PM-checkpoint)
1. Merge 4 lane branches → wip (carve-out reconciliation + **gate E4** on `senderCard.ts`; auto-merge `multiplexer.html`).
2. Rebuild `build-multiplexer.sh` dist.
3. **:8000 scheduled** — Oracle Tier-0→3 (grounds the Oracle-held rows: **V13** stale-symptom check, V6/V7 geometry, V10a spacing, L2 Playing-N-vs-Queued-N redundancy + AR widget tint) → E2E UI + visual regression → **integration final gate**.
4. **Pre-existing c8 gap** `audio/AudioRecorder.ts:255` (97.72% branch, outside all lanes, present on HEAD) — triage: documented c8-ignore if genuinely unreachable, else a coverage add. Blocks the directory-wide `c8 --100` gate; NOT a lane regression.
5. Rick's subjective visual sign-off (Chrome) — the one EXECUTOR:HUMAN tier.
6. **Push + the switchover flip** (`lupin-app.ini:883` `legacy notifications redirect enabled=True`) = **Rick's word alone.**

## Deferred (post-MVP / post-flip)
- 6 ADMIN nav items (L4 `TODO(post-MVP)` in `NavBarRenderer.ts`) — roles-claim shape unverified vs `jwt_service`; do not gate on an unverified client claim.
- V10(b) CC-card topic (OQ-7) — product-choice deferral.
- `d1bdb7ca` mux TTS server-push vs client-initiate — queued, post-flip.

## Fleet at checkpoint (11 PM EDT, Rick broadcast 2dd610ad)
All 4 workers (Tiffany 💍 · Sam 🎙️ · Krishna 🦚 · Clayton 😎) build-complete, store tasks closed with receipts,
**held ALIVE — NOT reaped** per Rick's directive. Mr. Radio pings María 🌸 (`f1cb1e70`) at the 11 PM pause for
her Lupin + P-is-P roll-ups; work resumes (integration) after.
