# Authoritative Mux ⇆ Legacy Parity Verdict (Oracle-run + adversarial recast)

**Author**: Rachel 🕊️ (PARITY-PROOF worker, session `01b19b90`) — for Mr. Radio 🦉.
**Date**: 2026-07-01. **Branch/commit**: `wip-v0.1.9-2026.06.21-bug-fix-implementation` @ `2a8defa0` tip.
**Method**: RAN the `src/tests/parity_oracle/` Tier 0–3 + golden-conformance harness on `:7999` (read-only,
DB-free, conftest auto-builds fresh bundles), then recast Sam's gap matrix into a CLOSED/PARTIAL/OPEN verdict
backed by Oracle results where covered, else code + static-DOM evidence (file:line). Independent adversarial
re-verify of the AR + PLY "closed" claims Sam built (L2).

---

## 1. Oracle run result — 10/10 PASS, freshness guard GREEN

```
src/tests/parity_oracle/test_tier0.py::test_tier0_css_source_identity                       PASSED
src/tests/parity_oracle/test_tier1.py::test_tier1_mux_emits_contract_skeleton               PASSED
src/tests/parity_oracle/test_tier1.py::test_tier1_responded_split_and_badges_present        PASSED
src/tests/parity_oracle/test_tier1.py::test_tier1_outgoing_direction_conformance            PASSED
src/tests/parity_oracle/test_golden_conformance.py::...card_and_message_structure           PASSED
src/tests/parity_oracle/test_golden_conformance.py::...message_widgets                      PASSED
src/tests/parity_oracle/test_golden_conformance.py::...message_direction                    PASSED
src/tests/parity_oracle/test_tier2_tier3.py::test_tier2_computed_style_isomorphism          PASSED
src/tests/parity_oracle/test_tier2_tier3.py::test_tier3_geometry_isomorphism                PASSED
src/tests/parity_oracle/test_golden_conformance.py::test_golden_shared_sheet_hash_is_current PASSED
============================== 10 passed in 5.40s ==============================
```

### FRESHNESS GUARD — the decisive result
- Golden's baked `shared_sheet_hash` = **`f1f04a9ee2f7`**.
- Live `css/shared/notifications-surface.css` content-hash = **`f1f04a9ee2f7`** → **MATCH**.
- Therefore `test_golden_shared_sheet_hash_is_current` **PASSED** and Tier 2/3 **RAN** (did NOT skip).
- **This is a real legacy-referenced geometry proof, not a tautological one.** The golden was captured from
  the *legacy* `notifications.js` render (`captured_from: "legacy notifications.js"`), and the mux
  component-isolation render was diffed against it node-by-node. Tier 2 asserts FULL computed-style
  isomorphism with **NO allowlist**; Tier 3 asserts absolute intra-card geometry within **±1px**.
- Bundle authenticity independently verified: `parity-harness.js` rebuilt from current source (26 015 bytes,
  contains live contract classes, pulls `renderSenderCard`) — the render reflects `2a8defa0` code, not a
  stale artifact.

**This directly answers Rick's "half-assed verification" charge — but ONLY for the sender-card scope** (see §2).
The self-rebaselined visual snapshots (`2a8defa0`) remain tautological; THIS Oracle is the non-tautological check.

## 2. What the Oracle actually PROVES (scope = sender cards only)

Golden = 2 sender cards / 28 style-geom nodes / 6 message rows:
- persona'd CC card `claude.code@lupin.deepily.ai#parity01` (badge=True, 5 msgs)
- persona-less arbiter card `lupin-arbiter-app-8001` (badge=False, 1 msg)

Proven nodes: each card box · card header box · date-accordion header/date-text/messages region · every
message row + its `.message-time` + `.message-text`. Proven at Tier 2 (declarative style) AND Tier 3 (±1px
geometry), plus Tier 1 DOM-contract + golden structural/widget/direction conformance, plus Tier 0 CSS source
identity (both pages `<link>` the same `notifications-surface.css`).

**The harness is SENDER-CARD-SCOPED. It cannot and does not cover page chrome**: top nav, env/clock, the
Action-Required panel, the Playing panel, the notifications-header spacing, the broadcast card, the session
strip, or the section-toolbar. Those need the full-page Oracle (sibling worker building it). Also: the fixture
is 6 messages, so the 176× progress-group-head defect (V11/Bug#1) is not *stressed* by the Oracle — its fix is
verdicted by code below.

## 3. Authoritative verdict — §5 V1–V15

Legend: **CLOSED** = at parity (proof cited) · **PARTIAL** = present but degraded/not-pixel-parity · **OPEN** = missing/broken/unproven.
Coverage: **[O]** Oracle-proven · **[C]** code+static-DOM · **[O-page]** needs full-page Oracle.

| # | Surface | Verdict | Evidence |
|---|---|---|---|
| V1 | Top nav bar (NAV) | **CLOSED [C]** / needs [O-page] | `NavBarRenderer.ts` + `templates/navBar.ts` exist; `multiplexer.html:55` `#lupin-nav-mount` (L4). Geometry unproven vs legacy. |
| V2 | Env title + live clock | **OPEN [C]** | `NotificationsHeaderRenderer.ts:95` hardcodes `"🔔 Notifications"` — no env, no date, no clock. Legacy `notifications.js:811` `envLabel` + live-clock title. |
| V3 | ⚠️ Action Required panel (AR) | **PARTIAL [C]** / needs [O-page] | Built (L2): blue `#action-required-section` + `✓ No pending actions` empty-state + full responder. NOT pixel-parity — see §5. |
| V4 | 🔊 Playing panel (PLY) | **PARTIAL [C]** / needs [O-page] | Built (L2): green playing / amber paused + transport + `🔇 Nothing in the queue`. NOT pixel-parity — see §5. |
| V5 | Notifications header spacing | **PARTIAL [C]** / needs [O-page] | Restyled via `notifications-header.css` (L3, H3). "Looks broken" (⚠️) is a pixel judgment the Oracle doesn't cover. |
| V6 | TTS-fraction control placement | **PARTIAL [C]** / needs [O-page] | B5 single-sourced (`393251c5`). Placement is page-chrome; unproven vs legacy geometry. |
| V7 | Broadcast card ▼ inline | **PARTIAL [C]** / needs [O-page] | CSS fix (L3, `broadcast.css`). Page-chrome; unproven. |
| V8 | Recent Activity | **CLOSED [C]** | Present, nested in broadcast card (toggled off by default). |
| V9 | Session strip status sub-icons | **CLOSED [C]** / needs [O-page] | `sessionStripIcon.ts` ported (L3). Page-chrome; geometry unproven. |
| V10a | CC-card N-new pill (R3) | **CLOSED [C]** | `senderCard.ts:171-172` renders `.sender-new-count` when `unread_count>0 && !is_worker` (worker-silencing `4b33ceb7`). Header box itself is **[O]**-proven. |
| V10b | CC-card persona-name badge (R4) | **PARTIAL [C]** | `senderCard.ts:105` — badge is an ICON-ONLY `<button>`; name behind a `popovertarget` popover. Legacy shows the inline orange persona NAME. |
| V10c | CC-card session topic (R5) | **OPEN [C]** | `senderCard.ts:158` `.sender-session-name` is ALWAYS EMPTY — `:136` "SenderRecord carries no session-name field." Legacy displays + updates the topic. |
| V11 | Progress-group head (Bug#1) | **CLOSED [C]** | Per-sender head election `senderCard.ts:188-215` (earliest ts, tie→id_hash); non-head members suppressed from flat stream. Oracle fixture (6 msgs) doesn't stress 176× but Tier-1 structure is green. |
| V12 | Reading pane / abstract (Bug#2) | **CLOSED [C]** | `ReadingPaneRenderer.ts:436` reads `getAttribute("data-abstract") ?? ""` RAW — decodeURIComponent removed; writer `notificationItem.ts:196` stores raw → symmetric. |
| V13 | Section-toolbar overlap | **OPEN [C]** / needs [O-page] | Not code-confirmable (pixel-position). Sam left ❓; remains unproven. |
| V14 | Fleet/Task collapsed headers | **CLOSED [C]** | Render correctly (prior `93-` closure). |
| V15 | Overall gestalt (VIS gate) | **PARTIAL / OPEN** | Sender-card interior is [O]-proven; but V2 + R4 + R5 open, V13 unknown, AR/PLY not pixel-parity → the VIS quality gate is NOT met. Needs Rick's subjective A/B. |

## 4. Authoritative verdict — roadmap MVP set

| Piece | Verdict | Evidence |
|---|---|---|
| **Phase-6** (TTS playback) | **CLOSED [C]**, audibility-unproven-here | `AudioStore.ts` is 579 ln with a gapless PCM scheduler (`createBufferSource → connect → start(max(nextStartTime,currentTime))`) + `suspend`/`resume`/`stop`. No longer the decode-only stub — roadmap's "mux SILENT" is stale. **Audibility is NOT provable by this layout harness** — needs the protocol E2E (`POST /api/get-speech-elevenlabs`), a separate proof. |
| **B5** (CSS single-source + Oracle T2/T3 + golden) | **CLOSED [O] — PROVEN** | Tier 0 proves both pages link `notifications-surface.css`; Tier 2/3 green vs a **FRESH** golden. This is the row the Oracle most directly closes. |
| **NAV** | **CLOSED [C]** / needs [O-page] | See V1. |
| **AR** | **PARTIAL [C]** / needs [O-page] | See §5. |
| **PLY** | **PARTIAL [C]** / needs [O-page] | See §5. |
| **VIS** | **PARTIAL / OPEN** | See V15 — gate not met. |

## 5. Independent adversarial re-verify — AR + PLY (Sam built these; I did NOT trust his self-assessment)

I diffed the mux templates/CSS against legacy `notifications.html` + `notifications.css` directly.

**AR (Action-Required):**
- ✅ Present + colored + functional: `#action-required-section` renders blue border + `#e7f1ff` fill;
  `ActionRequiredRenderer.ts:148,325` emit the `✓ No pending actions` empty-state on count===0 (the 06-30
  "invisible when idle" is stale — L2 fixed it). Empty-state CSS is **byte-identical** to legacy
  (`notifications.css:523-529`). Full responder (interactive/countdown/read-back/error-stripe) present.
- ⚠️ NOT pixel-parity: mux border is **2px** vs legacy **3px** (`notifications.css:509`); mux has **NO
  solid-blue filled section-header bar** — legacy's dominant blue is the header bar (`#0d6efd` bg, white text,
  `notifications.css:513-521`); the mux uses border+tint only.

**PLY (Playing/TTS):**
- ✅ Present + colored + functional: `.tts-chrome.is-playing-current` shades green, `.is-paused-current`
  shades amber; transport = Pause/Resume/Stop/Skip with a state matrix; `🔇 Nothing in the queue` empty-state
  is **byte-identical** to legacy (`notifications.css:531-537`).
- ⚠️ NOT pixel-parity: mux uses an **entirely different green palette** — Tailwind emerald
  `#34d399`/`#ecfdf5`/`#065f46` vs legacy Bootstrap `#198754`/`#d1e7dd` (`notifications.css:2951,2962`); **no
  solid-green filled header bar** (legacy fills `.tts-queue-header` `#198754` white); border **1px** vs legacy
  active-card **2px**; paused amber `#fbbf24` vs legacy `#ffc107`.

**My verdict (overriding Sam's "at parity"):** AR = **PARTIAL**, PLY = **PARTIAL**. Both satisfy Rick's
"any viable MVP client MUST include these accordions" mandate (present, colored, empty-states at byte-parity),
so they are MVP-adequate — but Sam's "closed/at-parity" is **overstated**: neither matches legacy pixel-for-
pixel (border widths, the solid-filled colored header bar, and PLY's whole green palette differ). **No Oracle
node covers these panels**, so no geometry proof exists — my verdict rests on the CSS diff above. The full-page
Oracle must capture these nodes to convert PARTIAL → a proven verdict.

## 6. Bottom line for the flip decision

- **Sender-card interior parity is now PROVEN** against a fresh legacy golden (Tier 0–3, 10/10, freshness
  GREEN). That is the authoritative, non-tautological check that was missing — for that scope.
- **Page chrome has NO legacy-referenced geometry proof yet.** V2 (env/clock) and R5 (session topic) are
  genuinely OPEN; R4 (persona name) and AR/PLY pixel-fidelity are PARTIAL; V13 (toolbar) is UNKNOWN. All
  await the full-page Oracle (sibling) + Rick's subjective Chrome A/B.
- Honest status: **B5 sender-card parity earned; MVP functionally present across NAV/Phase-6/AR/PLY; but the
  VIS quality gate is not met and 3–4 chrome gaps remain open/partial.** Not a clean "at parity" for the whole
  surface.
