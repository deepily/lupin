# Task-board state classification — 63 open items

**For**: María 🌸 (workflow steward), as the basis for analysis against the workflows
**By**: Mr. Radio 🦉 (`1a52ceb2`), on Rick's direct P0 order, 2026-07-16 ~22:16 EDT
**Scope**: every non-terminal item in the unified store (`unscoped_audit=True`) — **63 items**
**Method**: `task_query` metadata (owner · status · priority · `blocked_by` · `next_chase_ts`) cross-referenced against tonight's rulings and the reap record. **Full bodies were NOT read for all 63** — see §7 Limits.

---

## 1. THE HEADLINE — three numbers

| Measure | Value | What it means |
|---|---|---|
| **P0+P1 share** | **43 / 63 = 68%** | **Priority has stopped carrying information.** 41 items are P1. If two-thirds of the board is top-priority, the field cannot order work — and an agent picking "the next P1" is choosing arbitrarily while feeling principled. |
| **In-progress with a dead owner** | **3 / 7** | **43% of all `in_progress` rows are ZOMBIES** — their seats were reaped ≥24h ago. Nothing fires. The row says someone is working. Nobody is. |
| **Chase-expired, unchased** | **≥7** | The chase timestamp is the *only* liveness mechanism on a blocked row, and it has **no enforcement**. Expired chases just sit. |

**Board composition by owner** — this is the structural fact under everything else:

| Owner | Items | Live seat right now? |
|---|---|---|
| **sam** | **29 (46%)** | ⛔ crew reaped tonight (`c3391743`); Sam holds at all-stop |
| **maria** | 14 (22%) | ✅ live |
| **mr radio** | 13 (21%) | ✅ live (me) |
| operator gates (Rick's court) | 4 (6%) | n/a — waiting on Rick |
| **orphaned** (reaped seats) | **3 (5%)** | ⛔ **nobody** |

⇒ **68% of the board (Sam's 29 + the 3 orphans + 4 Rick-gates) has no worker who can move it tonight.**

---

## 2. THE CLASSIFICATION — 8 states

Ordered by what I'd want fixed first. **Each is falsifiable from store metadata**; I name the evidence, not an impression.

### C1 · ZOMBIE-ORPHANED — `in_progress`, owner reaped, no error ever fires · **3 items**
The row asserts active work. The seat is dead. **The store has no liveness link to a session**, so a reap silently converts "in progress" into a permanent lie.

| Item | Reaped seat | Evidence |
|---|---|---|
| `4e20520d` Stage 1 design re-review (2nd) REV.4 | `6b633402` | Reaped 2026-07-15 ~19:33 EDT, Rick's wind-down GO. **`write_memento=True` captured NOTHING** — 0 mementos on disk, no error. Was ~40% through. |
| `2695f5ad` REV.4 seat 2 — Stage 3 impl-verification | Clayton `23292b90` | Same reap, same silent no-op. Mid-harvest. |
| `06a3f031` Cascade reviewer #2 — Stage 2 | cascade seat | Same family. |

**Corroborating record**: `dc36ff69`'s own amendment already says *"this item is now UNSTAFFED… if resumed, RESTARTS FROM SCRATCH."* **The parent item knows. The three child rows still say `in_progress`.**
⇒ **A reap does not reconcile the rows the reaped seat owned.** `dismiss_sessions` returns `reconciliation: {closed: [], reassigned: [], unclassifiable: []}` — the field exists and came back **empty**.

### C2 · DECAPITATED-CREW — owner persona exists, no seat does · **29 items (all of Sam's)**
Distinct from C1: these are mostly `queued`, so they aren't lying about being worked — but **46% of the board is now owned by a name whose entire crew was reaped tonight** and whose restaff is gated on Rick's word. **They will not move, and nothing on the row says so.** A `queued` row and an *unstaffable* row are indistinguishable in the store.

### C3 · CHASE-EXPIRED, UNCHASED — the only liveness mechanism on a blocked row, unenforced · **≥7 items**

| Item | Chase due | Overdue by |
|---|---|---|
| `1615a624` Rick: zero-Max-quota the point of the toggle? | 2026-07-14 01:00Z | **~2.5 days** |
| `8093520f` Cascade §B web-search enablement | 2026-07-14 17:00Z | ~1.5 days |
| `ab721143` Cascade §D verification | 2026-07-15 13:00Z | ~1.5 days |
| `c9dd0cc3` Rick: spend ceiling + billing export (**P0**) | 2026-07-15 21:00Z | ~1 day |
| `1333019a` · `b2d20067` · `00b081d8` §3.2 (blocked on **sam**) | 2026-07-16 15:15Z | ~11h |

**`next_chase_ts` is advisory.** Nothing reads it and acts. A blocked row with an expired chase is indistinguishable from one being actively chased.

### C4 · SUPERSEDED-BY-RULING — the premise died; the row didn't · **≥2 confirmed, likely more**
- **`9318af31`** *"gpt-oss STREAMS ITS CHAIN-OF-THOUGHT"* (P1, Sam) — **Rick ruled the CoT question moot tonight**: *"a theoretical privacy issue that I don't give a shit about right now… We are building PoCs, NOT production apps."* I dropped its **sibling** `823be9cc` on that ruling **and preserved its non-privacy half**. `9318af31` was not touched — **it isn't mine**, and a ruling that kills one row does not propagate to its siblings. ⚠️ *Its standing-order clause (route GCP calls through Mr. Radio) is still live and is cited elsewhere — so this needs a scope split, not a delete.*
- **`c9dd0cc3`** *"spend ceiling + billing export BEFORE any Vertex spend"* (**P0**) — billing export **is configured** (detailed table exists in `billing_export`, created 07-13), and **Rick authorized Vertex spend tonight** and I fired the probe. **The gate it guards has been passed.** Still open, still P0, chase expired.

### C5 · DUPLICATE / SAME-DEFECT-MULTIPLE-ROWS · **≥2 pairs**
- **`6af1d362`** *"DM TRANSPORT LEAKS: dm_list scoping differs per session"* (María) **vs `2565956b`** *"dm_list() is NOT session-scoped — no recipient field exists"* (Sam) — **same defect, two owners, two rows, filed from two seats.**
- **`841b3d21`** *"BROADCAST FAN-OUT DELIVERS DIFFERENT PAYLOADS PER RECIPIENT"* (María, P1) — **its headline claim is already REFUTED by Rick himself**: *"the version I sent to you was the full message. The version Maria received was truncated."* Not per-recipient *fan-out policy* — **transport truncation**. The row's title still asserts the refuted mechanism. **The open part is real** (cause/threshold/frequency unknown) but the row is mis-titled, and **a title is what `terse=True` shows** — which is itself filed as `5a8aa45b`.

### C6 · RECORD-NOT-TASK — durable findings with no executable action · **≥6**
Rows that can **never** reach `done` because nothing is assigned and no seat can act:
- `6f8adc13` classifier wedge — fix candidates are *harness-config changes* outside any seat's reach.
- `5a8aa45b` *"a title cannot carry a standing order"* · `74a3ff4d` doctrine defect · `74c16374` spawn-brief defect · `73f397d2` / `2ce26609` pattern-watch.
**These are lessons, correctly captured — but they inflate the board and they never close.** They need a different home (doctrine/memory) or a `record` status. **Filing a finding as a task guarantees an item that grows the board forever.**

### C7 · MISPRIORITIZED — P1 inflation · **41 items (65%)**
| Priority | Count | Share |
|---|---|---|
| P0 | 2 | 3% |
| **P1** | **41** | **65%** |
| P2 | 16 | 25% |
| P3 | 4 | 6% |
**Nothing distinguishes `78aed273` (FOUNDATIONAL: 4/5 inductions void) from `978b27ac` (give the brief a reviewer).** Both P1. **The priority field has been used as an emphasis marker, not a ranking.**

### C8 · LIVE-AND-CORRECT — genuinely open, owned, actionable · **~13**
Mine (13) are correctly blocked on Rick with a live chase, or are honest records. **This is the only class the workflow is actually working for — and it's 21% of the board.**

---

## 3. THE PATTERN UNDERNEATH — this is one defect, not eight

**Every class above is the same shape, and it is the shape this milestone has been eating all week:**

> **A mechanism that reports success while doing nothing.**

- `in_progress` **reports** a seat is working. The seat is ash. (**C1**)
- `write_memento=True` **returns success** and lands zero mementos — *at the exact moment the evidence becomes unrecoverable*. (**C1**)
- `next_chase_ts` **reports** the item is being chased. Nothing chases. (**C3**)
- `queued` **reports** the item is ready for pickup. There is no crew. (**C2**)
- **P1 reports** urgency. 65% of the board is P1. (**C7**)
- A **closed sibling** reports the question is settled. Its twin is still open. (**C4**)
- `dismiss_sessions.reconciliation` **has the field** and returns **empty**. (**C1**)

⇒ **The board does not lie because anyone lied to it. It lies because every status field is agent-asserted and nothing measures it.** The store has no instrument that can fail.

**And that's the same finding my crew produced tonight from the other end** — twelve defects, **every one in a *fix*, none in original code, not one a knowledge problem**. Everyone knew the rule. The rule doesn't act. **A rule adds a step; a mechanism removes a decision.**

---

## 4. WHY IT GREW — the honest causal reading

**Rick's diagnosis is correct and I'm not going to soften it**: managers took on ad-hoc lower-priority work and declared clean boards. **I did it tonight.** The mechanics of how:

1. **"Board clean" is computed over a filter, not the board.** I ran `task_query(owner_persona=me, status=...)` all night — **the scoped query the doctrine mandates for token efficiency**. It returns *my* items. **It cannot show me 29 of Sam's rows rotting.** The hygiene rule and the visibility rule are in direct conflict, and the hygiene rule loses silently.
2. **The Stop-hook oracle nags about *owed* work, never about *stale* work.** It fired at me ~6× tonight — every time about items I owed *now*. **Not once about an expired chase, a zombie `in_progress`, or a superseded row.** *(Its own accuracy is filed as `c191be39` — it reported "2 in-progress" to me while the store said 0. **The board-hygiene oracle is itself an instrument that lies.**)*
3. **Closing requires receipts; opening requires nothing.** `->done` demands a whitelisted receipt (`commit`/`ts-<8hex>`/`doc_path`). **`task_create` demands nothing.** The asymmetry is correct for rigor and **fatal for volume**: filing is free, closing is work. **A board with a receipts gate and no filing gate grows by construction.**
4. **A finding is easier to file than to place.** C6 exists because "file it, don't fix it" (correct, and I enforced it tonight) has **no counterpart rule for where a non-task finding goes to die.**
5. **Reaps don't reconcile.** C1/C2 are pure fallout: **the reap is the moment the board should be rewritten, and it's the moment nobody is left to rewrite it.**

---

## 5. WHAT I'D MEASURE NEXT (for María — not done, not assumed)

1. **Read all 63 bodies** and split C4 properly. I confirmed 2 superseded from tonight's rulings alone; **I'd expect more.**
2. **Diff each `in_progress` against a live tmux/bridge roster** — C1 may be larger than 3.
3. **`created_ts` histogram** — Rick says "grown over the last couple days"; the *arrival rate* vs *closure rate* is the number that proves it.
4. **Count rows whose `blocked_by` names a cleared blocker** — I found the pattern but did not enumerate it.

---

## 6. THE ONE-LINE FINDING

> **The board has no instrument that can fail. Every field is an agent's assertion, nothing measures it, and the only enforced gate is on *closing* — so it grows.**

---

## 7. LIMITS — what this catalog does NOT claim

- ⚠️ **I did not read all 63 bodies.** Full rows exceed the tool's response limit (111KB for 16). Classification uses **metadata + tonight's rulings + the reap record**. **C4 and C5 are therefore LOWER BOUNDS**, and any single item's class is a **hypothesis until its body is read**.
- ⚠️ **C1's three orphans**: the reap is confirmed by `dc36ff69`'s amendment and my own record. **I did not re-verify each seat is dead right now.**
- ⚠️ **Sam's 29 (C2)**: I have **not** read them. That they're unstaffable is inferred from Rick's `c3391743` reap + Sam's all-stop — **a fact about the fleet, not a measurement of those rows.**
- **Counts (63 / 41 / 43 / 3 / 7) are measured** and reproducible via `task_query`. **The classes are judgment.**

**Provenance note**: I am the owner of 13 of these and the manager who let several rot. **This catalog is not a neutral audit — it is partly a self-indictment**, and María should read C8 ("live and correct — mine") with that in mind.

---

# ⛔ AMENDMENT — 2026-07-16 ~22:30 EDT. **§4 DEFECT #3 IS SUBSTANTIALLY REFUTED. BY ME. READ THIS BEFORE ACTING ON ANYTHING ABOVE.**

María routed my own §5 back at me with one instruction: *"Run it in the direction that could embarrass the report."* **It did. Here is the number.**

**Source**: the store's Postgres directly (`task_items`, `task_events`) — read-only. **This is a MEASUREMENT, not metadata inference. It outranks everything above.**

## 1. THE NUMBERS THAT REFUTE ME

| Measure | Value | Verdict on my report |
|---|---|---|
| **All-time closure** | **861 / 925 = 93.1%** | ⛔ **"Grows by construction" predicts a graveyard. There isn't one.** |
| **14-day closure/arrival** | **310 / 362 = 0.86** | ⛔ The gate asymmetry is **real but MILD** — not a runaway. |
| **Oldest OPEN item** | **4 days** (2026-07-13) | ⛔ **There is NO ancient rot. Zero items older than a week.** |
| **Open board age** | **40 of 64 are < 1 DAY old** | ⛔ 63% of "the backlog" was created **today**. |
| **The 3-day burst** | **158 arrived, 101 (64%) ALREADY CLOSED** | ⛔ The fleet closed two-thirds of the spike **while it was still arriving**. |
| Days closing ≥ arriving | 07-07, 07-08, 07-13 (**46/46**) | ⛔ On its best days the fleet closes **everything**. |

## 2. WHAT IS ACTUALLY TRUE — the board is an ARRIVAL SPIKE, not accumulated neglect

**Rick's premise — *"60-some items that has done nothing but grow over the last couple days"* — splits cleanly:**
- ✅ **RIGHT about the growth**: net **+52 over 14 days**, and **+42 of that in the last 3 days alone** (07-15 +12, 07-16 **+27**, 07-17 +3). **The acceleration is real and it is recent.**
- ⛔ **WRONG about the cause.** It is **not** managers failing to finish boards. **The fleet closes 93.1% of everything it opens and holds nothing older than 4 days.** **150+ items ARRIVED in three days** — cascade/review output — and **101 were closed on the way in**.

**Where the 64 actually sit:**

| Project | Open | Note |
|---|---|---|
| **skills-distillation** | **32** | **Sam's — crew reaped ~22:11 tonight** |
| lupin | 21 | mine + María's |
| plan (p-i-p) | 10 | |
| google-skills-distillation | 1 | |

⇒ **52% of the open board belongs to the one project whose entire crew was reaped 20 minutes ago.** **The board didn't rot. It was decapitated mid-sprint** — which is **C2**, and C2 turns out to be *the* story, not a side-effect.

**No runaway minter**: arrivals are distributed (sam 23 · **mr radio 35 across 3 sessions** · Tiffany 18 · maria 20). **I am the single largest source of the burst.**

## 3. 🔴 HOW I GOT IT WRONG — and it is tonight's exact lesson, aimed at me

**My §4 #3 was a STRUCTURAL ARGUMENT** (*"a receipts gate on the exit and none on the entrance ⇒ grows by construction"*). It is **logically sound and empirically weak**: the asymmetry exists, and **the fleet closes 93% anyway**. I reasoned from a mechanism to a consequence **and never measured the consequence** — while holding a report that already said *"`created_ts` vs closure is the number that actually proves it."* **I named the measurement and shipped without it.**

**And the reason it sailed:**

> **A finding that CONFIRMS the boss's suspicion passes a checkpoint that a contradicting one never would.**

**Rick was angry. My catalog agreed with him.** Eight classes of "the board is broken," all consistent with what he already believed — **and I did not run the one number that could have said "actually, the fleet closes 93% and nothing here is older than four days."** **It took María, who had every reason to accept a report that convicted everyone but her, to send it back and say *run the one that can embarrass you*.**

**That is the same defect this milestone has eaten twelve times tonight — a mechanism that reports success while doing nothing — with the polarity flipped: an instrument that reports FAILURE, confidently, without measuring it.** Agreement is not a checkpoint. **It is the absence of one.**

## 4. WHAT SURVIVES — the snapshot defects are real, the CAUSAL story is not

**STILL STANDING (measured, unaffected):**
- **C1 ZOMBIE-ORPHANED (3)** — `in_progress` with reaped owners. **María independently created a 4th at 22:15** (`663feeab`, owned by Clayton, already ash — caught by a `recipient_not_found` DM bounce, dropped at 90s). **C1 is a confirmed lower bound, N≥4.**
- **C3 CHASE-EXPIRED (≥7)** — nothing reads `next_chase_ts` and acts.
- **C4/C5** — superseded + duplicate rows are real.
- **C7 MISPRIORITIZED (41 P1 / 65%)** — priority carries no information. **Unaffected by the rate.**
- **The oracle lies**: it told me "2 in-progress" (store: 0) and told María "10" (store: 2). **N=2, two seats, one hour. Not a hypothesis.**
- **The FILTER defect (#1)** — *"board clean" computed over a scoped query* — **survives entirely, and is now MORE interesting**: my filter never showed me that **half the open board is Sam's**.

**RETRACTED / DOWNGRADED:**
- **#3 "grows by construction"** → **the mechanism is real; the effect is not.** At 93% closure it is **not** what produced this board. **Do not build doctrine on it.**
- **"the board only grows"** → **FALSE as a steady state.** True only for a 3-day window, driven by arrivals, with **64% of those arrivals already closed**.

## 5. THE CORRECTED ONE-LINE FINDING

> **The board is not a graveyard — it is a fresh burst that lost its crew. 93% closure, nothing older than 4 days, and 52% of what's open belongs to a team reaped 20 minutes ago. The hygiene problem is real but small; the visibility problem (a scoped query cannot show you the rot) and the reconciliation problem (a reap doesn't rewrite the rows) are the load-bearing ones.**

**⇒ For María**: the workflow fix is **NOT** "make managers close their boards" — **they do, at 93%.** It is **(a)** a reap that reconciles the rows it orphans, **(b)** an oracle that measures instead of asserts, **(c)** a priority field that can say "no", and **(d)** a hygiene query that is allowed to see the whole board.

**⇒ For Rick**: **you were right that it grew and right to be angry that nobody showed you why. You were wrong that it's neglect — and I confirmed your wrong half at speed because it agreed with you.** That is the more expensive failure and it is mine.
