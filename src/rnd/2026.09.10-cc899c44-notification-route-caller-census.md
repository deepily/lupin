# cc899c44 — notification route caller census

Measured by Rio ⚡ (session 8450aa99) on 2026-09-10 ~20:10 EDT at `327dcd04`
(branch `wt-rio-notif-route-auth`, off `wip-v0.2.1-2026.08.29-cjflow-v2-followup`).

## Handler count

Read off `cosa.rest.routers.notifications.router.routes`, walking each `APIRoute` dependant tree — not grep.
The instrument sees a guard: it reports the 8 credentialed handlers by name.

| | count |
|---|---|
| handlers | 23 |
| credentialed (`require_api_key_or_jwt`) | 8 |
| anonymous | 15 |

Router-level dependencies: none. App middleware (`src/lupin_app/main.py`): CORS and security headers only.

## Anonymous handlers × callers × credential sent

Paths are under `/api/notifications` unless noted.

| # | handler | legacy `notifications.js` | multiplexer `ApiClient` | lupin-mobile `NotificationRepository` |
|---|---|---|---|---|
| 1 | GET `/{user_id}` | ❌ fake `?api_key=claude_code_simple_key` query, server ignores it | — | Bearer |
| 2 | GET `/{user_id}/next` | — | — | Bearer |
| 3 | POST `/{notification_id}/played` | ❌ same fake query key | — | Bearer |
| 4 | DELETE `/{notification_id}` | ❌ same fake query key | Bearer (`NotificationsHeaderRenderer`) | Bearer |
| 5 | DELETE `/bulk/{user_email}` | ❌ nothing | — | Bearer |
| 6 | GET `/senders/{user_email}` | — | — | Bearer |
| 7 | GET `/conversation/{sender_id}/{user_email}` | — | — | Bearer |
| 8 | DELETE `/conversation/{sender_id}/{user_email}` | Bearer, non-refreshing | Bearer (`NotificationsListRenderer`) | Bearer |
| 9 | GET `/conversation-by-date/{sender_id}/{user_email}` | Bearer, non-refreshing | Bearer (`NotificationStore`) | Bearer |
| 10 | DELETE `/date/{sender_id}/{user_email}/{date_string}` | Bearer, non-refreshing | Bearer (`NotificationsListRenderer`) | Bearer |
| 11 | GET `/sender-dates/{sender_id}/{user_email}` | — | — | Bearer |
| 12 | GET `/senders-visible/{user_email}` | Bearer, non-refreshing | Bearer (`coldHistoryHydration`, `StripReconnectRehydrator`) | Bearer |
| 13 | GET `/active-conversation/{user_email}` | — | — | Bearer |
| 14 | GET `/project-sessions/{project}/{user_email}` | — | — | Bearer |
| 15 | POST `/generate-gist` (LLM spend) | Bearer, non-refreshing | Bearer (`NotificationsListRenderer`) | Bearer |

"Non-refreshing": `getAuthHeaders()` reads `this.authToken` without refresh; `authedFetch()` refreshes first.
An expired token succeeds on these today and would 401 after the guard.

Mobile's shared `Dio` carries `AuthInterceptor`, which injects `Authorization: Bearer` (`lib/core/di/service_locator.dart`).

## Not callers of any of the 15

Fixed-string search for `api/notifications` over tracked files: cosa-voice MCP / `notify_user_sync.py`, hooks
(`answer_catchup.py`), arbiter, proxies, `src/scripts/` — they reference only the 8 guarded routes.
`lupin-plugin-firefox`: zero hits. `git grep` cannot see untracked files.

## Tests

| test | effect of the guard |
|---|---|
| `src/tests/integration/test_notifications_integration.py` `/played` harness | ❌ overrides the queue dependency only → 401 |
| `src/tests/test_refactoring.py` | ❌ legacy script, no auth on #1 and #2 |
| `src/tests/unit/test_event_loop_starvation_regression.py` | ✅ already overrides `require_api_key_or_jwt` |
| `src/tests/lupin_smoke/` | ✅ `LupinTestClient` authenticates by default |
| `src/tests/smoke/test_proxy_notifications.py`, `test_ws_event_cleanup.py` | ✅ send Bearer |
| `src/tests/unit/test_multiplexer_route_liveness.py` | ✅ asserts not 404/405; 401 passes |
| `src/tests/e2e_ui/*`, `src/tests/parity_oracle/*` | ✅ `page.route` intercepts, never reach the server |

## Not measured

- Whether `:7999` is reachable from off-host.
- Whether any page still loads `notifications.js` in daily use.

## Decisions put to review

| # | question | recommendation | pro | con |
|---|---|---|---|---|
| D1 | authentication only, or also path email must match token email? | authentication only — `require_api_key_or_jwt`, the e20e249a pattern | small blast radius; every live caller already holds a token | a valid token for user A can still read or delete user B's history on 13 path-keyed routes |
| D2 | legacy `notifications.js` calls | route all 9 calls to the 15 through `authedFetch`; delete the fake `?api_key=` params | refreshing token; no stale-token 401s | churn in a legacy file |
| D3 | public exceptions | none | no caller is anonymous by design | — |
| D4 | test shape | enumerate `router.routes`, fail on any `APIRoute` lacking `require_api_key_or_jwt`; TestClient anonymous → 401 and credentialed → 2xx per family, one revert arm each | no hand list to drift | — |

## Ruling (Mr. Radio, 2026-09-10 20:03 EDT)

- Approved: `require_api_key_or_jwt` on all 15, every caller updated in the same change, both breaking tests repaired.
- D2 approved: every `notifications.js` call to the 15 goes through `authedFetch`, including the fake-query-key calls and the bare one.
- Out of scope: path email must match token email — authorization, and API-key seats carry no email. Filed as row `d90baf3d` (holding area, not_approved). Filed at P5, because a worker seat gets a 403 above P5; Mr. Radio to raise it to P2.
