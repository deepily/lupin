# Rebaseline decision packet: row f0e00f01

**Author**: Maya 🌻 (author #5 for Mr. Radio 🦉) · **Written** 2026-09-16 ~18:40 EDT, at `fa41c222`
**For**: Rick's rebaseline calls on the visual reds in row `f0e00f01`
**Authority**: this is a recommendation only. Nothing was rebaselined and `--update-snapshots` was never passed.

## ⭐ Update, 2026-09-16 22:05 EDT: all 19 reds of the fresh-bundle run, one row each

**Input**: `ts-0b573b67`, bundle at `27b125ea`, report `io/test-suite/2026.09.16-at-20:05-EDT-e2e_a-e2e_b-results.md`: 60 passed, 6 failed, 13 errors. Every diff figure below comes from that run's own `visual-failures/` artifacts, counted at the >16 threshold. A "probe" means the test's own injection script driven in headless Chromium against `:7999`: read-only, nothing captured as a golden.
**Fixes** are on branch `maya/f0e00f01-e2e-red-fixes`, cut from `27a52245`, **not merged**: `7d9d0f98` · `a6dd213f` · `dfb03d31` · `3e97f269`.

🔴 **All 13 ERRORs are pixel mismatches.** The snapshot plugin defers its failure to teardown (`plugin.py:370`), so a plain mismatch prints as ERROR. None of the 13 is a fixture or setup crash. Two of them had a test-side cause and are fixed (admin-ratify, dm-badge). The rest need a baseline.

⚠️ **The phase5 artifacts are gone.** The snapshot plugin deletes `visual-failures/` at the start of every pytest session (`plugin.py:158`, `shutil.rmtree`), so the e2e_b session wiped what e2e_a had written. The phase5 verdict is therefore partly a probe, and it is marked that way.

| # | test (snapshot) | kind | diff (this run) | cause | verdict | recommend |
|---|---|---|---|---|---|---|
| 1 | phase6b `tts_chrome_focus` | FAILED | 960x244 → 262 | **Test defect.** The injected prompt POSTs to `/api/get-speech-elevenlabs`. Since `0206270b` real audio returns about 250 ms **after** the fake `audio_ended`, so the pane turns `playing`: the buttons enable, a frame adds 18 px, and a re-render overwrites the 01:45 time pin (the capture shows 00:05, the run's UTC time). Probe: live 261.5 px, stubbed 243.5 px with the pin intact, and it matches the golden by eye. | not stale, not a regression | ✅ **No rebaseline.** Fixed in `7d9d0f98`: the test stubs the POST and checks `data-state=idle`. This also stops each run paying for an ElevenLabs call. |
| 2 | `test_visual_page[admin-ratify]` | ERROR | 1,060 px, only the Oldest Pending card | **Test defect.** `bfefc400` pins `#stat-oldest` to "12h ago". The fixture truncates `proxy_decisions`, so the real value is a deterministic "—". | not stale | ✅ **No rebaseline.** Fixed in `a6dd213f`: the pin is removed and a guard is extended. A mutation proves the guard. |
| 3 | `test_dm_badge_visual_baseline` | ERROR | 1,633 px at (0,0); **0** at (0,−1) | The whole row shifts 1 px, a sub-pixel position flap. | not stale | ✅ **No rebaseline.** Fixed in `3e97f269`: the test uses the content-shift comparator. Against this run's own artifacts it forgives the badge and still refuses fleet-status, the doc-viewer listing and admin-ratify. |
| 4 | phase6b `action_required` | FAILED | 960x307 → 350 (+43) | (a) The ratified card changes: ✕ `a79dc8de`, ⏸️ `b00968ee`, progress bar `59b662d5`. The row shading swapped too, but that is **hover**: the pointer sits in the same place and the layout moved 43 px under it. | (a) stale | ✅ **Rebaseline** at or after `dfb03d31`. Without it the countdown ticks between pin and capture (923 px apart, measured one second apart). Also move the mouse off the section before capture, so the next golden has no hover in it. |
| 5 | phase5 `notifications_pane` (2 snapshots) | FAILED + ERROR | AR section 960x201 → 244 (+43), from the ValueError's byte counts. Sender cards, from ts-99047bac's own artifacts at `23d90150`: same size (960x298), 3,340 px differ | AR section: same as #4; probe measured 244. **Sender cards: nothing visible changed.** Side by side at 2x the two cards are identical: same text, same active border, same pins. Every differing region reconciles to **0 px when moved 1 px down on its own** (b's name, header border and date), except sender-a's `Last:` cell, which drops to 104 px. The whole image does not: at a uniform offset (0,0) is best, 3,340 px. Some text bands landed a pixel lower, the same sub-pixel flap as fleet-status (#17). No commit is named; bd73ed24..23d90150 has no card CSS change. **Both comparators refuse it**: content-shift leaves 1,039 px; aa-scatter finds a 135 px contiguous cluster. | AR (a) stale · cards: sub-pixel flap, not a content change | ✅ AR: rebaseline at or after `dfb03d31`. 🟡 Cards: rebaseline, expecting it may flap again as #17 does. Whether it flaps between two runs of the same sha is **not measured**, since there is one artifact pair. A tolerance won't hold it; a band-wise shift comparator would. |
| 6 | phase6a `jobs_pane` | FAILED | 404 → 398 (−6) | (a) `97ab72a2`, the chevron turned into a button (already R5). The shaded Done row is hover, as in #4. | (a) stale | ✅ Rebaseline **after** the job-history count stub (R5), with the mouse moved off. |
| 7 | `task_editing_controls` | FAILED | 213 → 215, new controls in the actions row | (a) `2e28a992`: staged priority **Update** and the reason 🎤, "every pane gets the reason mic" | (a) stale | ✅ Rebaseline |
| 8 | section_a `popover_borrowed` | FAILED | 11 px, bottom-right corner only | The popover is unchanged. Dark page pixels show through its transparent rounded corner, because the card behind it moved (`a11575b1` layout). | background bleed, not the popover | ✅ Rebaseline. Optional: capture with an opaque background so the page behind cannot reach it. |
| 9 | section_a `chip` | ERROR | 953 px, same size | (a) `a11575b1`: the active dot takes the persona colour (green → Tiberius indigo) and the date caret moves right | (a) stale | ✅ Rebaseline. The target is now the narrowed card (`c5a022cb`), so this is the manager's call. |
| 10–11 | section_c `idle`, `filled` | ERROR | 2,315 px each, same size | (a) `a11575b1`: active border, `#abcd1234` restored, caret moved right. The **doubled** `#abcd1234` is the fixture's: it has no `display_name`, so the name falls back to the full sender id. | (a) stale | ✅ Rebaseline. First add `display_name: "phase6c-c-visual"` to the fixture, so the golden shows a real card's shape. |
| 12–14 | section_d `baseline`, `pinned`, `pin_moved` | ERROR | ~2,480 px each, same size | (a) `a11575b1` (already R4) | (a) stale | ✅ Rebaseline after the timezone pin `c8442f2a`, which is **still not merged** at `27a52245` |
| 15 | `test_visual_page[notifications]` | ERROR | 10,010 px | (a) Ratified changes that piled up: the jobs list lost Podcast and SWE and gained Run Test Suite and Resume Stalled TFE (`8bf71a64`); the toolbar lost collapse/expand (`5a3e57f0`) and gained 🗂️ and 🗃️; the clock pin puts "12:00" in the title (`bfefc400`). Nothing unintended seen. | (a) stale | ✅ Rebaseline, **deferred** as R1 says: first confirm on one run that the page matches itself. |
| 16 | `test_visual_page[dev-tools]` | ERROR | 26,711 px, text only | **Environment, in the golden.** No source commit touches the page or its CSS since its golden (`8bf71a64`, 08-29). The five 08-03 admin goldens on the same font stack still pass today, so the 08-29 golden is the one captured under a different font. | golden captured wrong | ✅ Rebaseline. Rio's option (a) is still the open question from R2. |
| 17 | `test_populated_panel_visual_baseline` (fleet) | ERROR | 3,875 px, same size | In alternate 12 px bands the text sits 1 px lower; the other bands match exactly. The panel is unchanged. Its y offset on the legacy page moved because content above it changed (`c4f8e39c`, `d0c0b9f7` and others), so the rounding differs. | sub-pixel flap, not a panel change | 🟡 Rebaseline, but expect it to flap again whenever content above it moves. The content-shift comparator **refuses** this one (921 px left), so a tolerance does not fix it. |
| 18 | `test_visual_docs_scope_listing` | ERROR | 75 px, one size cell | A live file's size in `src/rnd/v0.1.7` (V3, already described) | test reads live data | ❌ **Do not rebaseline.** Freeze the size column or use the sibling test's frozen fixture. This is a test change for the manager. |

**Suspected product defect: none.** Everything moved by a ratified commit, a test defect, the font environment, or sub-pixel rounding. One lead is **not measured**: audio that arrives after focus mode discarded its item still plays (row 1's mechanism). A real `audio_ended` cannot come before its own audio starts, so this is probably test-only. It is left out of the verdicts.

**Re-run after `maya/f0e00f01-e2e-red-fixes` merges.** The first three should go green with no baseline.
- Run 1, both halves: `-k "test_multiplexer_phase6b_tts_chrome_focus_visual or admin-ratify or test_dm_badge_visual_baseline"`. Expect 3 passed.
- Run 2, **e2e_a only**, so that no e2e_b half clears the artifacts: `-k test_multiplexer_phase5_notifications_pane_visual`. Expect it to stay red, leaving the sender-card diff in `visual-failures/`.
- **Done, 2026-09-16 22:25 EDT.** Run 1, ts-dec590a3: 6 of 6 passed. Run 2, ts-99047bac: red as expected, and the artifacts were read into row 5.
- ⚠️ ts-99047bac's report prints **"-1 passed"**. `_parse_junit_xml` in `src/cosa/agents/test_suite/job.py` computes passed = tests − failures − errors − skipped. JUnit counts the one testcase once (`tests=1`) while it carries both a `<failure>` and a teardown `<error>` (`failures=1 errors=1`). This hits any run where a visual test fails in its body and again at teardown.

## How to read this

Rick's line (relayed 2026-09-15 ~19:10 EDT): if the test's **target** changed, the manager regenerates the baseline without asking. If the **page itself looks different**, Rick makes the call. Six snapshots fall on Rick's side. Each has one entry below.

**Sources.** Row `f0e00f01`, read in full (all 40 amendments through 2026-09-16 19:28Z).
[Krishna's triage](v0.2.1/2026.09.15-visual-snapshot-triage-f0e00f01.md). *[link dead — target removed in the 2026-09-22 R&D cleanup, ticket `3a2f726b`; recover with `git log --diff-filter=D -- <path>` then `git show <sha>^:<path>`]*
Tiffany's `io/reviews/2026.09.15-visual-baseline-venue-investigation.md`.
The `:8000` results files in `io/test-suite/`.
The git log of the working branch.

🔴 **The newest visual evidence is older than the newest fixes.** The last `:8000` run that captured
snapshots was the 2026-09-15 22:05 EDT subset (63 selected) at `4cb0fa33`. Three fixes landed after
it, and none has run at the E2E tier since:

- `0deed068` pins the debug log on V1.
- `21bdd95d` adds the commons clear-and-inject on V1 and fixes the admin-table crash.
- `0968f7ae` adds the font fingerprint guard.

**All six Rick entries therefore share one precondition: one fresh visual run at the working branch
tip, before any baseline is written.** Every diff figure below comes from a run's own artifacts, at
the >16 threshold. None comes from a host render; those were withdrawn in the row on 2026-09-15 23:42Z.

### Fixes that gate a rebaseline

| fix | state at `fa41c222` | what it gates |
|---|---|---|
| **Timezone pin**: `c8442f2a`, sam, branch `sam/f0e00f01-visual-timezone-pin` | 🔴 **not merged** | Any snapshot showing a time or date: V5–V7, plus the regenerated V10–V12. sam measured that the pin changes no pixels here, because the containers already run in UTC. Land it first anyway, so every baseline is captured under the pin. |
| **appTimezone threading**: row `0e5bfa0e`, sam | open, not started | If this lands after a rebaseline, **nine** clock-bearing baselines churn again (Tiffany's count). Rebaseline once, after it, or in the same change as the amendment asks. |
| **Commons pin**: clear-and-inject on V1, `21bdd95d` | ✅ merged, **not run** | V1 |
| **Debug-log pin**: V1, `0deed068` | ✅ merged, **not run**. Before the fix, 10 of 10 load-pairs differed; after it, 6 of 10 still did (measured on :7999). The commons feed was the leftover, and `21bdd95d` targets it. | V1 |
| **Font thread**: per-venue fingerprint, `0968f7ae` | ✅ merged, **not run at E2E** | V2, and any page with long prose |
| **Job-history count stub**: V8 | ❌ **not built, no owner** | V8 |

---

## R1 · V1: `test_visual_page[notifications]` → `notifications.png`

| | |
|---|---|
| **Frame** | full page, 1280x720; the only snapshot with the legacy toolbar in frame |
| **Diff** (ts-93b7e480's own artifacts) | 11,578 px (1.26%), rows 470–719 |
| **Why it moved** | The toolbar's button **set** changed. The baseline had collapse-all and expand-all; HEAD has epic-board and holding-area. `c4f8e39c` added a max-height and a scrollbar. `notifications.js` grew by about 5,850 lines. The other ten pages in the same parametrised test pass, so the cause is specific to this page. |
| **Also** | The page was not self-consistent: the debug log stamps the wall clock, and the live commons feed changes between loads. Both are now pinned (`0deed068`, `21bdd95d`), but neither pin has run on `:8000`. |
| **Fix first?** | **Yes.** The pins must show V1 is self-consistent (two consecutive passes against itself) before a baseline is written. Otherwise the new baseline captures a coin flip. |

- **Pro, rebaseline:** the toolbar changes are ratified ("Pull both pairs" and the cap-and-scroll), so the golden is the stale side.
- **Con:** a capture taken before the pins are proven brings the flake back as a "new" red. The 2026-09-15 18:00 run's capture predates all three toolbar commits and is unusable.
- ✅ **Recommend: rebaseline, deferred.** First confirm the pins on one fresh run, then capture from that run.

## R2 · V2: `test_visual_page[dev-tools]` → `dev-tools.png`

| | |
|---|---|
| **Frame** | full page, 1280x720, no toolbar |
| **Diff** | 26,880 px (2.92%), rows 24–698 |
| **Why it moved** | **Not a source change:** no commit touched dev-tools html/js/css between its 08-29 baseline and the run tree. **Self-consistent:** 0 px across two loads. Krishna read the same paragraph in both images: the text is identical but advances at different widths, which points to font metrics. The five other admin pages carry less prose and pass. That link is a hypothesis, not a measurement. |
| **Conflict, not reconciled** | Tiffany classes this pair as "content/layout", and her rule 2 withdrew the fonts framing for host-versus-container comparisons. Krishna's advance-width reading comes from the run's own artifacts, not from a host render, so the withdrawal may not cover it. |
| **Fix first?** | **Yes, two things.** (1) The font fingerprint (`0968f7ae`) must be green on `:8000`, so the new baseline records a known font environment. (2) Rio's deferred **option (a), deleting `notification_sound_tester.html`**, removes a link from this page. Decide that first, or dev-tools gets rebaselined twice. |

- **Pro, rebaseline:** the page is deterministic, so a new golden will hold.
- **Con:** if the font environment moves again, this goes red with no code change. The fingerprint guard is what would name that cause.
- ✅ **Recommend: rebaseline** after the fingerprint is green on `:8000` and option (a) is ruled.

## R3 · V4: `test_populated_panel_visual_baseline` → `fleet-status-populated.png`

| | |
|---|---|
| **Frame** | `fleet-status-container`, 916x232, same size on both sides |
| **Diff** | 3,956 px (1.86%), rows 10–222 |
| **Why it moved** | **Not established.** Tiffany classes it as content/layout. The only time text (`stale 12m`) is a stubbed literal (`test_fleet_status_panel.py:55`), so the clock is ruled out. The toolbar is out of frame by 56 px (measured). No commit has been named. |
| **Fix first?** | No known fix. The timezone pin does not apply. |

- **Pro, rebaseline:** the seeded data and stable size fit the usual shape of a deliberate restyle.
- **Con:** nobody has named the change. A blind rebaseline could hide a regression in the fleet panel.
- 🟡 **Recommend: one look at the diff** (or a `git log` over the fleet-status renderer since 2026-08-03), **then rebaseline** if the change is intended. Don't rebaseline without looking.

## R4 · V5, V6, V7: `phase6c_section_d` baseline, pinned, pin_moved

| | |
|---|---|
| **Frame** | `#sender-cards-container`, 960x298, same size on both sides |
| **Diff** | about 4,350 px each, rows 1–286 |
| **Why it moved** | `a11575b1` (2026-09-10, deliberate parity with legacy): the active card gets `sender-card-active`, the `#<hash>` session id is restored, and the indicator becomes ●/○. The date caret also moved from beside the count to the right edge. On top of that, one band (y279–286) shifts down by exactly 1 px, and a −1 offset brings it to zero mismatch. |
| **Clock** | The card shows a message time (the seeded time renders as `15:00` in UTC). The timezone pin must be in place when this is recaptured. |
| **Fix first?** | **Yes: merge the timezone pin `c8442f2a`.** Separate question: should these tests switch to `assert_snapshot_content_shift_tolerant` (±1 px, forgiven only when the shift leaves zero mismatch)? That swap is a test change, so it is the manager's call. |

- **Pro, rebaseline:** every visible change traces to a ruled parity commit, and the diff was confirmed on the run's own artifacts.
- **Con:** without the shift-tolerant comparator, the 1 px row flap can come back after a rebaseline.
- ✅ **Recommend: rebaseline all three**, after the pin merges, with the comparator swap in the same change.

## R5 · V8: `phase6a_jobs_pane_visual` → `multiplexer_phase6a_jobs_pane.png`

| | |
|---|---|
| **Frame** | `[data-testid="multiplexer-jobs-pane"]`, height-tolerant comparator |
| **Diff** | 960x404 → 960x397/398. The structural change is **−6 px**; the extra pixel is sub-pixel rounding. |
| **Why it moved** | `97ab72a2` (2026-09-06, Rick's divergence-#5 ruling) turned the section-header chevron from a `<span>` into a `<button>`. The header went from 66 to 60 px. pocholo checked it three ways: live button, span swapped in, restore. The change is ratified, so the golden is stale. Doc: `src/rnd/v0.2.1/2026.09.15-phase6a-header-height-cause-f0e00f01.md` (commit `a4a2d4a4`, **not merged** into the working branch). | *[link dead — target removed in the 2026-09-22 R&D cleanup, ticket `3a2f726b`; recover with `git log --diff-filter=D -- <path>` then `git show <sha>^:<path>`]*
| **Fix first?** | **Yes.** The pane shows a **live `GET /api/job-history` total** (the band at x761–789). A capture freezes today's count, and the test goes red again the next day. Pin or stub that route first. Nobody owns this yet. |

- **Pro, rebaseline:** the height change is fully explained and ratified.
- **Con:** rebaselining before the stub captures a moving number.
- ✅ **Recommend: rebaseline after the job-history stub lands.** Also merge pocholo's `a4a2d4a4` doc so the explanation is on the branch.

## R6 · V9: `phase6b_tts_chrome_focus_visual` → `multiplexer_phase6b_tts_chrome_focus.png`

| | |
|---|---|
| **Frame** | `#tts-pane`, height-tolerant comparator; baseline `a3b04f1e`, 2026-09-10 |
| **Diff** | 960x244 → 960x262, **+18 px**. The same value came back in three runs (18:00, ts-93b7e480, 22:05), so this is not rounding. |
| **Why it moved** | **Not established.** No commit is named in the row. The pane has its own `.message-time` pin (`'01:45'`), so the clock is ruled out. The change is the same size every run and goes one way, which suggests a deliberate layout edit. |
| **Fix first?** | No known fix. |

- **Pro, rebaseline:** the number is stable across three runs, and the baseline is only six days old, so the change is recent and findable.
- **Con:** +18 px (almost a full text row) is not noise, and nobody has named its cause.
- 🟡 **Recommend: find the commit first** (`git log a3b04f1e..HEAD` over the TTS pane renderer and CSS), then rebaseline if the change is intended.

---

## Rick's six, at a glance

| # | snapshot | fix that must land first | recommendation |
|---|---|---|---|
| R1 | V1 notifications | pins proven on one run | ✅ rebaseline, deferred |
| R2 | V2 dev-tools | font fingerprint green, option (a) ruled | ✅ rebaseline after both |
| R3 | V4 fleet-status | none | 🟡 look at the diff, then rebaseline |
| R4 | V5–V7 section_d | timezone pin `c8442f2a` | ✅ rebaseline, with the comparator swap |
| R5 | V8 phase6a jobs pane | job-history stub (no owner yet) | ✅ rebaseline after the stub |
| R6 | V9 phase6b tts focus | none | 🟡 name the commit, then rebaseline |

**Order.** Whenever `0e5bfa0e` (appTimezone) is going to land, rebaseline after it. Every clock-bearing baseline moves once more when it lands.

---

## Reds that need no rebaseline from Rick

| red | why no rebaseline from Rick | state | owner |
|---|---|---|---|
| **V10, V11, V12** section_a chip, section_c idle and filled | **Target changed**: `c5a022cb` (merged) narrowed the capture from `#notifications-pane` to `#sender-cards-container`. Under Rick's line, regenerating the baseline is the manager's job. The new images will show the `a11575b1` changes (#hash, ●/○), which is expected. | Narrowing is merged; the regeneration isn't scheduled. Merge the timezone pin before it runs. | Mr. Radio (runs the regeneration) |
| **F2** `dm-badge-recent-activity.png` | A **1 px clip offset**, not a page change: expected and actual are identical once shifted down one row (Tiffany). Recapturing only hides a coin flip that comes back. The fix is a 1 px clip tolerance, or finding the fractional layout above the row. ⚠️ The baseline file was renamed by `82ea33df` but never recaptured. | Not started | **No owner.** Mr. Radio to assign. |
| **V3** `doc_viewer_directory_listing_docs.png` | The capture lists the **live** `src/rnd/v0.1.7` folder, and one file's size changed (15.9 → 17.6 KB, rows 1639–1647). Any doc edit reddens it again. Freeze the size column or use the frozen fixture its sibling test already has. That is a test change. | Not started | **No owner.** Mr. Radio to assign. |
| **admin-users, admin-ratify, admin-trust** `test_visual_page` | They never reached a comparison: a Playwright serializer crash (integer dictionary keys). Fixed in `21bdd95d`, with table-cell pins in `NORMALIZE_TABLE_SPECS`. Nothing is known about their pixels yet. | Fixed, **not run** | Krishna (fix) · Mr. Radio (run) |
| **F6** ask survives audio reconnect | A test that a product ruling outran (`5e1851a0` made the pane show one card at a time). | Merged `9cc21c25`. **Not run**: the 19:38 red came from tree `74c9822a`, which does not contain the merge. | Maya (the earlier Maya seat) · Mr. Radio (run) |
| **F7** focused card grows to 500 px | The icon-exit gesture became a deliberate no-op in `7f73000c`. | Merged `9cc21c25`, **not run** | same |
| **F1** drain restores section | Fixed | ✅ closed, receipt ts-ed41f311 | — |
| **F3, F4, F5** toolbar clicks | Fixed by `c4f8e39c` and `5a3e57f0`. ⚠️ F5 stays green only by a 4 px margin, on Rick's "Accept the four pixels" ruling. If it goes red with "intercepts pointer events", that is the known exposure: ask Rick again rather than re-triaging it. | ✅ closed, receipt ts-a9579ddf | — |
| **`8d0c7c83`** toolbar_button_is_clickable plus two others · **`73fdc7ef`** toolbar_show_scrolls_section_into_view | Functional reds, folded under this row 2026-09-16 | Needs a re-run to see whether F3/F4 cleared them | Mr. Radio |
| **`test_section_header_keyboard_activation`** Enter/Space on the notifications header | Functional. Red at 22:24, green at 22:35 on the same sha. | Row `1f79c04f` | Mr. Radio (holds the row) |
| **Clock race inside the capture window** | A pushed `sys_time_update` can land between normalize and capture, about 1% per notifications capture | Row `b2fb103c` | Tiberius |
| **Seven stabilizers written by hand in six files** | Duplication. ⚠️ Consolidating them changes pinned values, **and that will need a rebaseline from Rick** when it lands. | Recorded in the row, not built | No owner |

**Not run anywhere since the fixes landed:** V1, the three admin pages, F6, F7. One visual run plus one functional re-run at the branch tip settles every "not run" cell in this table.
