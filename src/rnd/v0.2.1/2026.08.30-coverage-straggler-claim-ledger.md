# Coverage straggler ledger — who is covering what, and which lists to distrust

**Kept by**: Pocholo 📣 · **Started**: 2026-08-30 · **Tip when started**: `d81a9faa`
**Claim section re-derived by**: Rio ⚡ at `c67d4b62`, 2026-08-30 17:33, on Mr. Radio's ruling

⚠️ **THE HEADER SHA IS THE OLDEST THING IN THIS FILE.** Sections carry their own — read those.

**Why a file and not a DM**: peer DMs are being condensed in transit and corrections are
being dropped from them. Two seats have already acted on a garbled version of this list.
A file can be opened.

---

## 🔴 DISTRUST ANY STRAGGLER LIST BUILT FROM `git grep`, INCLUDING BOTH OF MINE

I ran two grep censuses an hour apart and got **22** and **33** stragglers. Both wrong, and
they fail in **opposite directions**:

| pass | what it did wrong | error |
|---|---|---|
| loose | counted a script **mentioned in a test comment** as tested | too few stragglers (22) |
| strict | missed a **dashed-name** import | too many stragglers (33) |

**The receipt.** `watch-hook-events.py` has a dedicated suite that loads it as

```python
whe = importlib.import_module( "watch-hook-events" )    # the DASHED filename
```

My strict regex searched the *underscored* module name `watch_hook_events`, found nothing,
and reported a well-tested file as untested. Same trap swallowed `capture-bounce-resubmit.py`
and `migrate-sqlite-to-postgres.py`.

⇒ **A grep for a module name cannot answer "is this covered."** The instrument is a coverage
run. Publishing either list would have sent three seats to write second suites for files that
already have them — wasted work that looks like progress.

### The one grep result that IS sound, and the asymmetry is the whole point

```bash
git grep -l -- "<stem>" -- src/tests src/cosa/tests
```

- **EMPTY is conclusive** — nothing named cannot be loaded. Use the **dashed stem**, as on disk.
- **A HIT proves nothing** — it may be a comment, a docstring, or a rename-consistency check.

Checked for the thing that would break even that: a test loading scripts generically by
`glob`/`listdir` with no name in its source. Only **enumerators** exist, and enumerating a
filename does not execute it, so no coverage comes from them.

---

## RECONCILED — the count is SIXTEEN, and 22/33 were never rival answers

Clayton 😎 applied the empty-grep rule across `src/scripts` and got **16**. I re-derived it
independently. **Compared by FILE LIST rather than by total:**

```
mine: 16   clayton: 16
files only in mine     : (none)
files only in clayton's: (none)
in both                : 16
```

⇒ **Zero difference in either direction.** Two seats, independent derivations, identical set.

**And my 22 and 33 were not competing answers to this question — they answered different
ones**, which is why comparing totals would have read as a three-way disagreement:

| number | the question it actually answered |
|---|---|
| **16** | which scripts does **no test file name at all** — the sound one |
| 22 | 16, plus 6 whose only mentions I could not classify |
| 33 | which scripts my **broken load-regex** failed to match — an artefact, not a question |

⇒ **Three counts of three different questions is not a disagreement until you know which
files each one includes.** The reconciliation took one `comm` and settled it; arguing the
totals could not have.

⚠️ Still to come: Clayton's coverage census cross-checks all sixteen against measured 0%.
The empty-grep 16 is *conclusive about naming*, and coverage is the instrument for
*measured*. If they disagree on any file, coverage wins and the file goes back on the list.

## Ledger

**Re-derived at `c67d4b62`** by Rio ⚡, 2026-08-30 17:33, from the tree — not from anyone's
report. Every line carries the sha its claim was made at, per the rule below.

### 🔴 A CLAIM WITH NO SHA IS NOT A CLAIM

Ratified by Mr. Radio 🦉, 2026-08-30, after five seats were re-spun and this section's claim
state went stale by construction — a claim is a statement about a tree, and a re-spun seat
cannot be asked which tree it meant.

**Write `file | holder | claimed-at-sha | state`. A row missing the sha is a rumour with a
name attached, and the next reader will act on it.** Two costs already paid: Chloé took
Clayton's exact three off a census five commits old, and I claimed two files that a merge
and a deletion had already settled. Nobody was careless — everyone read a document that was
true when written.

**And a claim expires.** Read the sha as an expiry date, not a warranty: `rollback_migration.py`
is the clean case, a file the list still named after the tree had deleted it.

### Live claims

| file | holder | claimed at | state at `c67d4b62` |
|---|---|---|---|
| `bridge_pin_sweep.py` | Rio ⚡ | `d81a9faa` | ✅ **LANDED** `b5eb2056` — 62 stmt / 26 br, 0 miss |
| `embedding_regen_probe.py` | Rio ⚡ | `d81a9faa` | ✅ **LANDED** `b5eb2056` — 71 / 24, 0 miss |
| `purge-offline-heartbeat-events.py` | Rio ⚡ | `d81a9faa` | ✅ **LANDED** `b5eb2056` — 50 / 16, 0 miss |
| `create_api_keys_table.py` | Clayton 😎 | ⚠️ unrecorded | ✅ **LANDED** `1164ae87` |
| `create_notifications_table.py` | Clayton 😎 | ⚠️ unrecorded | ✅ **LANDED** `1164ae87` |
| `auth_migration/migrate_mock_users.py` | Clayton 😎 | ⚠️ unrecorded | ✅ **LANDED** `1164ae87` (mutation fix `f3a611b9`) |
| `auth_migration/validate_migration.py` | Chloé 🗼 | ⚠️ unrecorded | ✅ **LANDED** `56f08986` |
| `scan-prose-task-refs.py` | Pocholo 📣 | `a8b40fe1` | 🔴 **REPORTED DONE, NOT IN GIT** — see below |
| `init_test_database.py` | Pocholo 📣 | `a8b40fe1` | 🔴 **REPORTED DONE, NOT IN GIT** |
| `run-heartbeat-arbiter.py` | Pocholo 📣 | `a8b40fe1` | 🔴 **REPORTED DONE, NOT IN GIT** |
| `probe_commons_post_direct.py` | Chloé 🗼 | ⚠️ unrecorded | ⬜ open — no test on any branch |
| `thread_attribution_plugin.py` | Chloé 🗼 | ⚠️ unrecorded | ⬜ open — no test on any branch |
| `create_admin_test_account.py` | Chloé 🗼 | ⚠️ unrecorded | ⬜ open — no test on any branch |
| `clock_hold_plugin.py` | Chloé 🗼 | ⚠️ unrecorded | ⬜ open — no test on any branch |
| `probe_cc_bounded_billing.py` | — | — | 🔒 **HELD** — Rick's decision `2b2f426e` |

⚠️ **The `unrecorded` rows are the rule proving itself.** Those claims were made in DMs that
carried a holder and no sha, so the ledger cannot say which tree they described. They are
recorded as unknown rather than back-filled with a plausible sha — inventing one is how a
rumour becomes a citation.

### 🔴 "REPORTED DONE" IS NOT "LANDED", AND THE DIFFERENCE IS CHECKABLE

Pocholo's successor reported his three finished. **At `c67d4b62` all three are still named by
zero tests, and no matching test file exists on ANY branch:**

```bash
git log --all --oneline --diff-filter=A -- "*test_scan_prose_task_refs*"   # empty
git log --all --oneline --diff-filter=A -- "*test_init_test_database*"     # empty
git log --all --oneline --diff-filter=A -- "*test_run_heartbeat_arbiter*"  # empty
```

**What that proves and what it does not.** It proves the work is not committed anywhere git
can see. It does NOT prove the work was not done — uncommitted files in a re-spun seat's
worktree die with the seat, and that is the likeliest reading. **Do not re-assign these on
this evidence alone; ask the seat first.** But do not treat them as covered either: the
branch is not covered until the tests are on it.

⇒ **Report "done" and "landed" as separate columns, each with a sha.** A worker's file can be
finished and still be at zero on the branch, and collapsing the two is what turns honest work
into a phantom.

### Retired — not stragglers

| file | why |
|---|---|
| `watch-hook-events.py` | `test_watch_hook_events.py`, loads via `importlib.import_module( "watch-hook-events" )` |
| `capture-bounce-resubmit.py` | `test_capture_bounce_resubmit.py`, `spec_from_file_location`, 14 tests |
| `migrate-sqlite-to-postgres.py` | `test_migrate_sqlite_to_postgres.py`, `spec_from_file_location`, 42 tests |
| `auth_migration/rollback_migration.py` | **DELETED from the tree** at `c93785fb` — there is no file to cover |

### The three retirements, and the proof they needed

Clayton 😎 pushed back that *"a test file exists"* is not proof the tests **execute** the
module, and he was right to — I had asserted it for two of these from the filename alone.
Checked properly, by opening each test file and finding the load line:

| script | loaded by | how | tests |
|---|---|---|---|
| `watch-hook-events.py` | `test_watch_hook_events.py` | `importlib.import_module( "watch-hook-events" )` | — |
| `migrate-sqlite-to-postgres.py` | `test_migrate_sqlite_to_postgres.py` | `importlib.util.spec_from_file_location( "migrate_sqlite_to_postgres", … )` | 42 |
| `capture-bounce-resubmit.py` | `test_capture_bounce_resubmit.py` | `importlib.util.spec_from_file_location( "capture_bounce_resubmit", … )` | 14 |

⇒ **His objection was right in principle and wrong on these three.** All three genuinely load
and run the module. The retirements stand — but they stood on evidence only after he asked,
and for two of them I had been one filename away from saying so without checking.

⚠️ Note the load mechanism differs per file — `import_module` on a dashed name in one,
`spec_from_file_location` in the other two. That variety is exactly why a single grep pattern
cannot survey this, and why the coverage run is the instrument.

**Authoritative list pending**: Clayton 😎 is running the full unit tier at `d81a9faa` with an
**unscoped** `--cov`, so pyproject's `source` governs. A second tier of mine was killed to clear
contention. When his lands, the zero list goes here **with the sha and the instrument named** —
because the instrument is exactly why the first two lists disagreed.

⚠️ **Do not scope that run.** Passing `--cov=<path>` silently overrides pyproject's `source`,
and a file absent from a scoped report reads as zero when it was never measured at all.
