# Live seeded-spawn wake capture — the criterion, locked BEFORE the fire

**Rachel 🕊️ · 2026-09-03 20:17 EDT · written and committed before the spawn is fired**

Mr. Radio is firing a real `spawn_sessions` carrying a `seed_memento`, so a wake watch
actually arms, then reaping it. This file exists so the pass criterion cannot be fitted to
whatever comes back — the same discipline that made the cross-repo fire readable (criterion
locked as event 10871, judged at 10875).

---

## What the capture CAN and CANNOT see — stated first, because two of the three asks are out of reach

| ask | reachable from my seat? |
|---|---|
| the receipt the writer lands, and the tmux name actually created | ✅ **yes** — filesystem + `tmux list-sessions`, snapshotted before and polled after |
| the verdict, **read directly rather than inferred from whether a DM arrives** | ✅ **yes, but as an INDEPENDENT read** — I drive `check_respin_wake` in **my** process with the spawner's `session_name`. That is a direct read of what the checker returns for those inputs; it is **not** a read of the arm's own returned assessment |
| the kwargs the live arm is **handed**, at arm time | 🔴 **NO** |

🔴 **The arm runs inside the manager's own MCP process.** Nothing outside it can read what it
was handed without instrumenting that process — and that process is already serving a module
older than the commit that added `get_claude_session_id_with_source`, so an edit on disk would
not even take effect. **Claiming to have captured those kwargs would be exactly the stored
verdict this exercise exists to avoid.** They stay measured only in the synthetic capture
already on record (row `7ad5eba6`, event 10889).

---

## THE CRITERION — decided now, before any result exists

Let `fired_at` be the moment the spawn fires, `NAME` the spawner's `session_name`.

**REPRODUCED — tonight's failure is a live defect** *iff both*:
1. a boot receipt appears, dated **after** `fired_at`, under the seat's data root; **and**
2. a watch armed on `NAME` still yields **`DEAD_NO_WAKE`**.

That is the only combination that shows a seat which came back being reported as one that
did not.

**NOT REPRODUCED**, and each of these is a *different* answer, not a fudge:

| outcome | what it means |
|---|---|
| **no receipt appears at all** | the seat genuinely never woke. The alarm would have been **correct**, and tonight's was too. Not a watch defect |
| a receipt appears **and** the checker returns a **non-alarm** verdict | the watch works on this path. Tonight's alarm had a cause **outside** it, still unexplained |
| a receipt appears under a **different data root** than the watch reads | names the **wrong-root** cause specifically |
| a receipt appears whose `tmux_session` **≠ `NAME`** | names the **writer/reader mismatch** cause specifically — the third cause, so far unrealised |

⚠️ **A single fire is a sample of one.** Whatever comes back, it is one spawn under tonight's
conditions. It can demonstrate a defect; it cannot establish absence.

---

## Instrument, and its positive control

`capture_wake.py baseline` snapshots every receipt (201 across three roots) and every tmux
session (8), then **proves the finder can locate an existing receipt** —
`find_receipt_by_identity` looking for `cc-reviewer-maria-1` under `lupin` → **FOUND**.

⇒ **A later "no receipt found" is therefore evidence, not an untested instrument.** An empty
result and a broken reader look identical, and the control is what separates them.
