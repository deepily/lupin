# The Decisions Log judged against D10 — a pass/fail sweep

**Author**: Clayton 😎 (`80d429df`) · **Requested by**: Mr. Radio 🦉 (`93a8751c`), 2026-08-31 00:22 EDT
**Judged against**: D10 at `6907fc26` — *name the COMMAND, the COMPARISON, or the ARTIFACT a reader would produce*
**Subject**: `TODO.md` §📚 DECISIONS LOG 2026-08-30 night, lines 86–195, at `83269fcb`

**Nothing was altered.** This is a judgment, not a rewrite. I did not touch a single entry, and
where an entry fails I name the remedy it would need rather than supplying one — writing the
remedies is Tiberius 👑's job and judging them is mine, and Mr Radio separated the two deliberately.

**No store row was opened for this work**, per **D4** — findings go to TODO.md and the R&D corpus,
never new store rows, while Rick's moratorium stands. Applying the log's own rule to the sweep of it.

---

## Result: 8 pass · 2 fail · 1 outside the test's scope

| entry | verdict | what carries it, or what it lacks |
|---|---|---|
| **D1** cosa-tier coverage append | ✅ **PASS** | COMPARISON — *"the per-file delta against the unit-only run"*, plus the ARTIFACT (*"report the union"*). Two named runs, compared per-file. A reader can produce it and be checked on it. |
| **D2** per-arm isolation mandatory | ✅ **PASS**, by pointer | Names no command itself, but points at `CLAUDE.md § A MUTATION HARNESS CAN LIE`, which carries `purge-pycache.sh` per arm. A section TITLE is a content-name, not a coordinate, so the pointer survives the file moving. ⚠️ Its pass is **borrowed** — it holds only while the pointed-to section keeps the command. |
| **D3** only the reviewer's own word | ✅ **PASS** | ARTIFACT — the reviewer's own statement, with `33f39e7a` cited as the worked example. Failable by pointing at any merge and asking where that statement is. |
| **D4** findings to TODO.md, not the store | ✅ **PASS** | COMPARISON, and a cheap one: `task_query` against the TODO.md section. Binary and inspectable by anyone. |
| **D5** a retro prompt must be answerable "no" | 🔴 **FAIL** | *"Answerable no"* is a **property assessed by judgment**. No command, no comparison, no artifact. Nothing here can be failed by a reader. |
| **D6** one wide rule, three narrow remedies | 🔴 **FAIL** | **It carries my shipping condition verbatim and does not meet it.** D6 states *"THE THREE REMEDIES MUST BE PRINTED UNDER IT"* — and does not print them. It names the three CONTRIBUTORS and the three MECHANISMS; the three remedies are required, not supplied. |
| **D7** drop "count" from the valid-observation rule | ✅ **PASS** — strongest in the log | COMPARISON *and* ARTIFACT, explicitly: *"a NAMED test that was PASSING at baseline, now failing, ON AN ASSERTION YOU CAN POINT AT."* Plus two divergent actions (a distrusted KILL → isolated re-run; a distrusted SURVIVOR → read the edit and the data). |
| **D8** the condenser broke ratification | ✅ **PASS** | COMMAND-shaped and checkable against the sent message: *"send a to-be-ratified rule BARE — one artifact per message, no surrounding prose."* A reader can open the DM and fail it. |
| **D9** the run's signature was ten refusals | ⚪ **OUTSIDE SCOPE** — see below | Descriptive, not prescriptive. It tells nobody to do anything, so there is no remedy to print. |
| **D10** itself | ✅ **PASS** on the test; ⚠️ its **retroactive clause** did not | The test is self-satisfying — it names what to produce. But *"it governs every entry above, retroactively"* was a claim with **no artifact** until this document existed. **This sweep is that artifact.** |
| `leading-harvest` failure-mode block | ✅ **PASS** | A real guard: *"ask for PROVENANCE and the BOUNDARY OF KNOWLEDGE and nothing else — never name the finding you expect back."* Checkable against any drafted prompt. |

---

## The two failures, and the remedy each would need

I name these rather than write them in. A remedy authored by the judge is a judge marking his own paper.

**D5** — the missing artifact is the *"no"* itself. A prompt is failable the moment you are required to
write down the answer it could receive that would disappoint you; if you cannot write one, the prompt
could not have returned it. That produces a **written artifact per prompt**, and it can be failed by
inspection. Note D5 is already marked PROPOSED/HELD and half-refuted by Rio ⚡ — it fails D10 on top
of that, independently.

**D6** — the remedy is the three lines it demands and omits: compare **components**, not the aggregate ·
exchange shas to find a **disagreement**, never a confirmation · read every *because* / *so* / *which
means* **one at a time**. Each names an action a reader performs; none is a "be careful".

⇒ **D6 is the entry Mr Radio predicted**: written tonight, quoting my condition, failing it.

---

## 🔴 The finding this sweep produced: D10 needs a SCOPE QUALIFIER, and it is D10's own defect

**D9 cannot pass and should not be made to.** It is a description of what happened, not an instruction
to anybody. Forcing a remedy under it would manufacture exactly the cosmetic filler D10 exists to
stop — *"be alert for refusals"* would satisfy the letter and be unfailable.

⇒ **D10 governs PRESCRIPTIVE entries. Applied to descriptive ones it produces the defect it names.**

That qualifier is not a softening. Without it, the first person to sweep this log under D10 either
marks a truthful description as a failure, or invents a remedy to clear it — and the second is worse,
because it passes.

**What this does NOT establish**: I judged eleven entries in one log at one sha. Whether the
prescriptive/descriptive split is the only scope problem D10 has is unmeasured — one more sweep, of a
log I did not write against, would test it. And my own D6 and D10 are in the sample: **I am judging
two entries that carry my name**, which is a conflict I can declare but not remove. Both of my
verdicts on them are unfavourable, which is evidence of nothing either way.
