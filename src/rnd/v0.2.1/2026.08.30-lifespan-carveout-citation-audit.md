# The lifespan carve-out cites a document that never mentions the file

**Author**: Rio ⚡ (session `d5818089`)
**Filed**: 2026-08-30
**Requested by**: Mr. Radio (`93a8751c`)
**Measured at**: sha `e1fc27ae`, root `/mnt/DATA01/include/www.deepily.ai/projects/lupin-wt-rio-8593bf65`
**Status**: ✅ ESTABLISHED, ✅ RULED, ✅ APPLIED — options 1+2 landed in the docstring; option 3 declined
**Subject**: `src/tests/unit/test_main_lifespan_carveout.py` and the exemption it claims for `src/lupin_app/main.py`

---

## Verdict in one line

**The exemption is warranted; the citation is not; and the test file that carries both
measures nothing at all.** Three separate facts, and only the first one is good news.

---

## 1. The ruling is real, and it IS about this file

`test_main_lifespan_carveout.py` says *"Per Q9 hybrid scope ratification"*. That half **holds**.

`92-phase5-closure.md:18`, verbatim:

> Lifespan switch in `fastapi_app/main.py` documented as smoke-tier coverage (the live branch
> behavior is exercised by `src/tests/smoke/test_model_server_smoke.py`).

`fastapi_app/main.py` **is** `lupin_app/main.py` — renamed `R090` in commit `53fef419`,
2026-06-19, confirmed by `git log --follow`:

```
R090	src/fastapi_app/main.py	src/lupin_app/main.py
```

So a ruling exists, it names this file, and it says smoke-tier. Nothing wrong with the substance.

## 2. The document it cites for that ruling does not contain it

The docstring's last line:

> Branch-body coverage (the actual remote-vs-local model loading + probe loop) is smoke-tier
> per `91-phase5-smoke-audit.md`.

**`91-phase5-smoke-audit.md` mentions `lupin_app` or `fastapi_app` zero times.** Measured:

```
grep -n "fastapi_app\|lupin_app\|lifespan" 91-phase5-smoke-audit.md
67: | 5.0c read `lupin_model_server/main.py` | ... The model-loading lifespan ... pragma-skipped |
```

One hit, and it is `lupin_model_server`'s lifespan, a different file. What that document actually
does is audit two **smoke test files** for carve-out compatibility and refine a test list for
`lupin_model_server/main.py`. It certifies that `test_model_server_smoke.py` still works. It never
rules that anything in `lupin_app/main.py` is exempt.

⇒ **The ruling lives in `92-phase5-closure.md`, which the docstring does not cite. The docstring
cites `91`, which does not carry it.** A reader checking the citation finds a document about
another file and cannot tell whether the exemption was ever granted.

## 3. The bigger finding: the file named for the carve-out never touches the carve-out

```
COVERAGE_FILE=… LUPIN_ROOT="$PWD" .venv/bin/python -m pytest \
  src/tests/unit/test_main_lifespan_carveout.py -q --cov=lupin_app.main --cov-branch
```

```
CoverageWarning: Module lupin_app.main was never imported. (module-not-imported)
CoverageWarning: No data was collected. (no-data-collected)
4 passed in 0.22s
```

The four tests are: two `importlib` checks on `cosa.memory.*` (not `main.py`), and two that compare
string literals to string literals —

```python
assert ( "model-server" == canonical_remote_value ) is True
```

— which pass regardless of what `main.py` contains. The file is named
`test_main_lifespan_carveout.py`, its docstring reasons about the lifespan at length, and it
contributes **zero statements** of coverage to the file it is named for. Its own docstring says so
in a NOTE, honestly, and then the filename says otherwise to everyone who greps.

## 4. What the plan of record actually asked for

`02-phase5-unit-tests-and-coverage-design.md` **§8**, titled *"`fastapi_app/main.py` lifespan switch
branch — test plan"*, names three tests:

| planned | written |
|---|---|
| `test_lifespan_declares_local_owner_when_provider_local_and_models_loadable` | ✗ |
| `test_lifespan_declares_remote_only_when_provider_model_server` | ✗ |
| `test_lifespan_handles_load_failure_gracefully` | ✗ |

None exist, in this file or anywhere — `grep -rl "declare_remote_only"` over both test trees returns
this file plus the two provider test files, and no test in the repo imports the lifespan.

## 5. What the smoke tier actually covers, measured both ways

The claim is that the branch bodies are covered by `test_model_server_smoke.py`. Run at
`e1fc27ae`, with the API key present:

```
9 passed in 2.92s
```

Of those nine, **eight hit `:7998` directly and say nothing about `lupin_app/main.py`.** Exactly
one — `test_proxy_through_compute_mp3` — transcribes an MP3 through `:7999`, which can only succeed
if that process's lifespan took the remote branch (`declare_remote_only()`, `whisper_pipeline = None`,
HTTP proxy). So the **remote** branch's effect is genuinely observed end-to-end. The claim is true,
narrowly, by one test.

**The local branch is observed by nothing.** `src/conf/lupin-app.ini:569` reads
`speech to text provider = model-server`, so no running server in this configuration takes it; no
unit test drives it; no smoke test asserts it.

⚠️ **A worktree caveat worth recording**: on first run that suite reported `4 passed, 5 skipped`,
and the skipped one was the only test that touches this file. Cause was
`src/conf/keys/model-server-api` — gitignored at `.gitignore:71`, present in the main tree, absent
from every worktree. Same family as the 10-failure worktree gap in CLAUDE.md § TESTING VENUES: the
smoke-tier evidence for this exemption **silently skips in any worktree**, and a seat verifying the
claim from one would conclude nothing covers it.

## 6. The measurement

The switch spans lines 898–1015 of `src/lupin_app/main.py`. Measured against the union of every
unit test that imports the module (`src/tests/unit/lupin_app/`, `test_main_boot_order_v2_submit.py`,
`test_managed_bounce_broadcast.py`, and the carve-out file itself — 87 tests, all passing):

| region | statements | missing |
|---|---|---|
| carve-out switch (898–1015) | 60 | **60 — 0% covered** |
| whole lifespan (575–1258) | 275 | 274 |
| file total | 509 | 296 (40%) |

## 7. Recommendation — the citation, not the carve-out

**Do not test the 274.** That half was withdrawn on measurement and stays withdrawn.

For the citation, three options, cheapest first. All are Mr. Radio's call, not mine:

1. **Fix the pointer** — cite `92-phase5-closure.md` §Q9 (where the ruling is) instead of, or
   alongside, `91-phase5-smoke-audit.md` (where it is not). One line. Restores the audit trail.
2. **Name the coverage honestly** — add that the *remote* branch is observed by exactly one smoke
   test, that the *local* branch is observed by none, and that the evidence skips in a worktree
   without the key file.
3. **Rename the file** — `test_main_lifespan_carveout.py` does not test `main`, its lifespan, or a
   branch. Something like `test_carveout_provider_api_surface.py` would stop it reading as coverage
   that exists.

⚠️ Separately, and outside the question asked: the two predicate tests assert literals against
literals. Per CLAUDE.md's fourth reading of a surviving mutant — *a fixture that cannot
discriminate* — no edit to `main.py` can redden them. Flagging, not fixing.

---

## Why this shape matters

An exemption justified by a document about a different file is the same defect as a scoped `--cov`
read as a census, or a host shell querying the dev database: **an instrument that returns a
confident answer to a question nobody asked.** Here the ruling happened to be right, so the cost was
only an audit trail that dead-ends. The next one may not be right, and it will look identical.

## 8. Mr. Radio's ruling — 2026-08-30 16:00 EDT

**Options 1 and 2. Not 3. And do not write the three missing tests.**

| option | ruling | applied |
|---|---|---|
| 1 — fix the pointer to `92-phase5-closure.md:18`, noting the `R090` rename so the trail survives | ✅ do it | ✅ docstring |
| 2 — name the coverage honestly, including that the one covering smoke test silently skips in a worktree | ✅ do it | ✅ docstring |
| 3 — rename the file | ❌ declined | — |
| write design-doc §8's three lifespan tests | ❌ declined | — |

Verbatim on the last two: *"you established this morning that testing lifespan is marginal, so the
honest answer is a declared gap, not a manufactured one."* The filename stays, and the docstring now
opens by saying the file does not import the module it is named for — which is the point, not a
defect to be tidied away by renaming around it.

**Applied**: docstring rewritten in `src/tests/unit/test_main_lifespan_carveout.py`. Docstring only —
no test added, removed, or changed; `4 passed in 0.11s` at sha `afbba978`, `py_compile` clean.

## Idempotency marker

AUDIT-v1 — 2026-08-30 by Rio ⚡ at `e1fc27ae`. Re-derive the coverage figures rather than quoting
them; `main.py` is under active change.
