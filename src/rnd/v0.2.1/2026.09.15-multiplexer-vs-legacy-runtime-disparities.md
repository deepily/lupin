# Multiplexer vs legacy notifications client — runtime behaviour disparities (Phase 2)

**Row**: `645a7da5` (Rick, P0). **Author**: María 🌸. **Started**: 2026-09-15, ~16:45 EDT, at `6f6d0ab0`.
**Lead**: the legacy client (`src/lupin_app/static/js/notifications.js`). **Follower**: the multiplexer (`src/lupin_app/static/js/multiplexer/`).
**Phase 1** (layout, containment, rulings): [2026.09.15-multiplexer-vs-legacy-layout-disparities.md](2026.09.15-multiplexer-vs-legacy-layout-disparities.md)

> **Scope — Phase 2 only.** This document records what each accordion **does** at runtime, one accordion at a time, top to bottom in the lead's page order. It does **not** propose fixes. The build plan comes after this list is complete, per the ticket.

---

## 1. The parity rule (Rick, 2026-09-15, Phase 1 §8)

Parity means the multiplexer **contains** every behaviour the legacy client has. Anything the multiplexer does *beyond* legacy is allowed and stays. So every finding here gets one of three verdicts:

| Verdict | Meaning | Owed? |
|---|---|---|
| **MATCH** | Both clients behave the same | no |
| **OWED** | Legacy has the behaviour and the multiplexer lacks it, or does it differently | **yes** — goes to the build list |
| **EXTRA** | The multiplexer has a behaviour legacy lacks | no — keep it, do not back-port |

A behaviour that *conflicts* with a legacy behaviour (for example, the multiplexer auto-expands where legacy stays collapsed) is **OWED**, not EXTRA: the lead's behaviour wins.

## 2. Method

Each accordion is inventoried on both sides against the same fixed list of behaviours, so a missing row is visible rather than silently skipped.

| # | Behaviour | The question asked of both clients |
|---|---|---|
| B1 | **Default disclosure** | Expanded or collapsed on a first load with no saved state? |
| B2 | **Toggle trigger** | What opens and closes it: header click, chevron only, keyboard? |
| B3 | **Persistence** | Is the open/closed state saved? Under which storage key? Restored on reload? |
| B4 | **Toolbar show/hide** | Does the toolbar button hide the whole section? Is that saved? |
| B5 | **Collapse/expand-all** | Does a global or per-section `⊟`/`⊞` act on it or on its children? |
| B6 | **Auto-reveal** | Does an event (new item, a focus request) un-hide or expand it by itself? |
| B7 | **Live header** | Do the title, count or "updated …" text change while the page is open? |
| B8 | **Refresh control** | What does `⟳` (or equivalent) refetch, and what does it do to disclosure state? |
| B9 | **Nested accordions** | Each child accordion's default, trigger and persistence (B1–B3 again, one level down) |
| B10 | **Empty state** | What renders when there is nothing to show? |
| B11 | **Layout mode** | Does the horizontal (reading-pane) layout move or change it? |

**Evidence standard.** Every cell cites source as `file:line` on both sides. A behaviour read from source is marked **(source)**. Where it has also been watched in a browser it is marked **(observed)**. Nothing is inferred from a missing search hit: an absent behaviour is stated with the search that found nothing.

**Browser confirmation — constraint.** Clicking a toggle writes persisted UI state that the operator's own tabs share. Browser confirmation therefore waits for a method that does not disturb his state, such as a separate browser profile. Until then, cells are **(source)**.

## 3. Order — the lead's page, top to bottom

| # | Legacy accordion | Multiplexer counterpart | Status |
|---|---|---|---|
| A0 | Section toolbar (global behaviours) | `SectionToolbarRenderer.ts` | ✓ |
| A1 | Q A Interface | absent (S1) | ✓ |
| A2 | Submit Agentic Jobs | broadcast card only (S2) | ✓ |
| A3 | Action Required | `ActionRequiredRenderer.ts` | ✓ |
| A4 | TTS Queue / Playing | TTS pane | ✓ |
| A5 | Notifications (date → sender → card) | `NotificationsHeaderRenderer.ts`, `NotificationsListRenderer.ts` | ✓ |
| A6 | Fleet Status | `FleetStatusRenderer.ts` | ✓ |
| A7 | Finished Tasks | `FinishedTasksRenderer.ts` | ✓ |
| A8 | Task List | `TaskListRenderer.ts` | ✓ |
| A9 | Holding Area | `HoldingAreaRenderer.ts` | ✓ |
| A10 | Epic Board | `EpicBoardRenderer.ts` | ✓ |
| A11 | Queue Filter Settings | relocated into two headers (S11) | ✓ |
| A12 | Cosa Jobs Flow | `JobsPaneRenderer.ts` | ✓ |
| A13 | Time Saved | absent (S13, ruled: carry) | ✓ |
| A14 | System Status | absent (S14, ruled: carry) | ✓ |
| A15 | Direct TTS Test | absent (S15, ruled: carry) | ✓ |
| A16 | Debug Information | absent (S16, ruled: carry) | ✓ |

`○` not started · `~` in progress · `✓` inventoried on both sides.

---

## 4. Findings, one accordion at a time

*Complete: A0 through A16, inventoried 2026-09-15 by María with Tiberius, Krishna and Clayton. Every file was checked against source before it was merged.*

> 🔴 **Correction to Phase 1, found here.** Phase 1 recorded Submit Agentic Jobs, Filter Settings, Cosa Jobs Flow, Time Saved, System Status, Direct TTS and Debug as **hidden in legacy**, and §6.3 called them "hidden or collapsed at load". **They are not hidden by the code.** Every legacy toolbar button starts `active` (`notifications.html:43-75`) and no section carries `section-hidden` in the markup. Legacy adds `section-hidden` in exactly two places: a toolbar click (`notifications.js:15340`) and replaying saved state (`:15414`). The Phase 1 probe ran in the operator's own browser, so it read **his saved toolbar choices**, not the default. On a fresh browser every legacy section is visible. The Q3 ruling ("carry all five") stands; how they should **start** went back to Rick (row `645a7da5`, ~16:55 EDT).

### A0 — Section toolbar (global behaviours) `✓`

Legacy: `notifications.js:15280-15423`, `notifications.html:33-82`. Multiplexer: `render/SectionToolbarRenderer.ts`, `render/templates/sectionToolbar.ts`, `stores/ViewStateStore.ts`.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default visibility, no saved state | **every** section visible: every button `active`, no `section-hidden` in the markup (`html:43-75`) (source) | every section visible **except `jobs-pane`**, which is hidden by a code default (`templates/sectionToolbar.ts:80`) (source) | **OWED** — legacy's Jobs counterpart (Cosa Jobs Flow) starts visible |
| B2 | Toggle trigger | click a `.toolbar-btn[data-section]` → flips `section-hidden` on the section and `active` on the button (`:15299-15346`) (source) | one delegated click on the toolbar → the same flip, plus the `hidden` attribute (`SectionToolbarRenderer.ts:127-172`) (source) | MATCH |
| B3 | Persistence | `localStorage["lupin_section_visibility"]`, a map of every button's `active` state (`:15360-15373`). Restore **only hides** ids saved `false` (`:15409-15417`) (source) | `ViewStateStore` key `view_state_section_visibility`, schema 1 (`ViewStateStore.ts:31`). Restore applies both directions (`reconcileSectionVisibility`) (source) | MATCH in behaviour. **Note:** the two clients use **different keys**, so a choice made in one does not carry to the other |
| B4a | Show → scroll into view | showing a section scrolls it into view (`scrollIntoViewIfNeeded`, `:15349`) (source) | no scroll: no `scrollIntoView` call anywhere in non-test multiplexer source (search: `grep -rn scrollIntoView multiplexer --include=*.ts`) (source) | **OWED** |
| B4b | TTS empty state on toggle | toggling `tts-queue-section`, and restoring on load, re-runs `updateTTSQueueSection()` (`:15355`, `:15420`) (source) | not yet inventoried; A4 covers it | open → A4 |
| B5 | Toolbar `⊟`/`⊞` | collapse/expand every **Task List owner group**, persisted to `lupin.taskList.collapsedOwners` (`:14312-14337`) (source) | toolbar `⊟`/`⊞` collapse/expand every **Notifications** sender card and date accordion (`NotificationsListRenderer.ts:879-885`). Task-owner collapse lives in the Task List header instead, under the **same** key `lupin.taskList.collapsedOwners` (`render/taskListCollapse.ts:18`) (source) | MATCH for task owners (the placement is Phase 1 T4). The Notifications bulk collapse is **EXTRA** |
| B6 | Auto-reveal from elsewhere | a filter-mode badge in a section header un-hides Filter Settings, saves it and scrolls to it (`showAndScrollToFilterPanel`, `:6364-6384`) (source) | Filter Settings absent (S11) | **OWED** → with A11 |
| — | Bounce `🔄` | toolbar action → `POST /api/system/bounce` (`html:76-81`) | in the Notifications header (Phase 1 H3) | layout only (Phase 1 T1). Runtime not yet compared; A5 covers it |

**A0 build items**: (1) start `jobs-pane` visible; (2) scroll a newly shown section into view; (3) the filter-badge reveal arrives with A11.

### A1 — Q A Interface `✓`

Legacy: `notifications.html:103-160` (markup and `toggleSection`, `:1534-1566`), `notifications.js:1777-1812` (wiring), `:1908-1935` (agent select and mode badge), `:3099-3300` (`submitQA`, `handleQAResult`), `:4005-4009` (STT), `:6118-6150` (metrics), and `shared/agent-select.js` and `shared/arg-interview.js`. Multiplexer: **absent**. A search of non-test multiplexer source for `qa-input`, `agent-mode`, `submit-qa`, `/api/v2/submit` and `arg-interview` returns nothing.

Because the section is absent, every legacy behaviour below is **OWED**. The rows exist so the build has a complete behaviour spec, not just "add a Q A section".

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | open: `#qa-section` carries no `collapsed` class | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` calls `toggleSection('qa-section')`, flipping `collapsed` and the chevron `▼`/`▶` | OWED |
| B3 | Persistence | **none**: `qa-section` is not in `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1515-1532`), so it opens on every load | OWED (match: no persistence) |
| B4 | Toolbar | `❓` hides and shows the section (A0) | OWED |
| B5 | Collapse/expand-all | not affected | — |
| B6 | Auto-reveal | none found | — |
| B7 | Live header | static title "Q&A Interface" | OWED |
| B8 | Refresh control | none | — |
| B9 | Nested accordions | none | — |
| B10 | Empty state | `#response-text` reads "No response yet..." | OWED |
| B11 | Layout mode | not referenced by the horizontal-layout code | — |
| Q1 | Agent select | options fetched at runtime from `GET /api/v2/agents` into `#agent-mode`, with an Auto-Route sentinel. **One-shot**: picking an agent routes only the next submission (Rick's ruling, 2026-08-22), and the change updates `#mode-status` | OWED |
| Q2 | Mode badge | shows the sticky **voice** mode; a click clears it (`setAgentMode(null)`) and does not touch the select | OWED |
| Q3 | Submit | the button or Enter in `#qa-input` → `submitQA()`: empty text refused, **2 s debounce**, button disabled with a spinner, "Submitting Q&A..." shown. Auto-Route → `POST /api/v2/ask {question, websocket_id}`; a named agent → `POST /api/v2/submit {args…, websocket_id}` | OWED |
| Q4 | Result | `handleQAResult`: an answerable `needs_input` (it has a `pending_id`) renders an inline argument interview in `#qa-arg-interview`, and Enter or its button answers it. Any other result clears the interview and shows the answer. If the interview module is missing, it falls back to dumping the raw JSON | OWED |
| Q5 | Voice input | `🎤` → the shared recording manager fills `#qa-input` (30 s max, Esc cancels) | OWED |
| Q6 | TTS mode | `#tts-mode` select, Instant or Reliable. It is **read by other sections too** (`notifications.js:15756`, `:20591`, `:20940`), so it is page-wide state that happens to live in Q A | OWED — see note |
| Q7 | Timing metrics | `#qa-metrics` appears after a submit, showing TTT (submit → text), TTFA (TTS start → first audio) and RTT (submit → first audio) | OWED |

**Note on Q6.** Other parts of legacy read the TTS-mode select out of the Q A section. When the multiplexer builds Q A, the build must decide where that page-wide value lives. That is a build decision, recorded here so it is not lost.

**A1 build items**: the whole section, specified by B1–B4, B7, B10 and Q1–Q7.

### A2 — Submit Agentic Jobs `✓`

Legacy: `notifications.html:163-408` (markup), `notifications.js:1876-1908` (CC wiring), `:2105-2184` (research / test-suite / TFE wiring), `:3370-3467` (`submitResearchJob`), `:3538-3638` (`submitTestSuiteJob`), `:4116-4219` (`submitClaudeCode*`), `:6184-6197` (`_getSchedulingParams`), `:8161-8264` (`submitTFEResume` + candidates), `:917-924` (auto-fix INI sync). Multiplexer: **absent**. A search of non-test multiplexer source for `cc-submit`, `cc-prompt`, `research-topic`, `test-suite`, `tfe-resume`, `api/v2/submit`, `api/test-suite/submit`, `resume-from` and `Submit Agentic` returns **nothing** (`grep -rnF <each> --include=*.ts multiplexer/ | grep -v .test.ts`). The single `job-submit-card` hit in the multiplexer is `templates/broadcastCard.ts:47`, which is the **broadcast** card — legacy's broadcast card lives in the Notifications section (`html:540`), not here, so it is not a counterpart. (source)

Because the section is absent, every legacy behaviour below is **OWED**. The rows exist so the build has a complete behaviour spec.

> **Correction to the ticket's framing:** the section holds **four** submit cards, not three — Claude Code (`html:171`), Research (`:236`), Test Suite (`:292`) and TFE Resume (`:381`). The fourth is a card with **no** accordion header (`job-submit-header`, not `job-submit-card-header`), so it never collapses.

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | **open**: `#job-submit-section` carries no `collapsed` class (`html:168`). The section itself is visible — its toolbar button starts `active` and the markup has no `section-hidden` (`html:44`, `:163`) | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('job-submit-section')`, flipping `collapsed` and the chevron `▼`/`▶` on `#job-submit-toggle` (`html:164-167`, `:1534-1567`). No keyboard handler: no `tabindex`/`role`/`keydown` on the header (`grep -n 'job-submit' html/notifications.html`) | OWED |
| B3 | Persistence | **none** for the section: `job-submit-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) and no per-feature key names it (`grep -nF 'job-submit' notifications.js` → 0 hits). It opens on every load | OWED (match: no persistence) |
| B4 | Toolbar show/hide | `📝` (`data-section="section-job-submit"`, `html:44`) hides/shows the whole section and persists to `lupin_section_visibility` (A0 B3). Showing it scrolls it into view (A0 B4a) | OWED |
| B5 | Collapse/expand-all | not affected — the toolbar `⊟`/`⊞` act on Task List owner groups only (`js:14312-14337`) | — |
| B6 | Auto-reveal | none found: no code path adds/removes `section-hidden` or `collapsed` on this section outside the toolbar (`grep -nF 'section-job-submit' notifications.js` → 0 hits) | — |
| B7 | Live header | static title "📝 Submit Agentic Jobs" — no count, no timestamp (`html:165`) | OWED |
| B8 | Refresh control | none in the header. A **successful CC submit** calls `this.refreshAllQueues()` (`js:4209`), which refetches the A12 queues; the research / test-suite / TFE handlers do not | OWED |
| B9 | Nested accordions | **three** card accordions, each `job-submit-card-header` → `toggleSection(<card>-section)`: Claude Code (`html:172-176`), Research (`:237-241`), Test Suite (`:293-297`). All three start **collapsed** — `class="section-content collapsed"` in the markup. None is persisted (none is in `LUPIN_ACCORDION_PERSIST_KEYS`). The **fourth** card, TFE Resume (`:381-405`), has no toggle at all and is always open | OWED |
| B10 | Empty state | none — the cards are always present; each card's `*-submit-status` div starts empty (`html:215`, `:287`, `:376`, `:402`) | OWED |
| B11 | Layout mode | not referenced by the horizontal-layout code: `_layoutMode` is read at `js:15206`, `:17206`, `:17590`, `:17703` and none touches this section (`grep -nF 'job-submit' notifications.js` → 0 hits) | — |
| **Card 1 — Claude Code** | | | |
| J1 | Project select | `#cc-project`, three static options `lupin` \| `cosa` \| `plan`, `lupin` selected (`html:180-184`). Not fetched, not persisted | OWED |
| J2 | Prompt + voice | `#cc-prompt` textarea; `🎤 #cc-stt-button` → `handleCCSTTButtonClick()` → shared `handleSTTButtonClick('cc-prompt', button)` (`js:4116-4120`), 30 s max, Esc cancels (`html:189`) | OWED |
| J3 | Task type | `#cc-task-type`: `BOUNDED` selected; `INTERACTIVE` is **disabled** with a "coming back later" title (`html:198-201`). The type sets `max_turns`: 200 for INTERACTIVE, 50 otherwise (`js:4182`) | OWED |
| J4 | Dry run | `#cc-dry-run` checkbox, **checked by default** (`html:204`), travels as `args.dry_run` | OWED |
| J5 | Submit | `#cc-submit` click **or** `Ctrl+Enter` in `#cc-prompt` (`js:1880-1906`). Empty/whitespace prompt refused with "⚠️ Please enter a task prompt." in red (`js:4145-4149`). Button disabled + `#cc-loading` spinner shown, status "Submitting to CJ Flow queue..." (`js:4153-4156`). **No debounce** (unlike Q A's 2 s) | OWED |
| J6 | Submit wire | `POST /api/v2/submit` with `command: 'agent router go to claude code'`, `args: {prompt, project, task_type, max_turns, dry_run}`, plus top-level `question`, `websocket_id` and the scheduling params (`js:4170-4189`). Auth via `getAuthHeaders()` | OWED |
| J7 | Result | success → `✓ Claude Code job submitted! Job ID: <id>` in green, **no queue position** (deliberate, `js:4200-4206`), then `refreshAllQueues()`. Failure → `✗ Error: <msg>` in red. `finally` re-enables the button and hides the spinner. The prompt is **not** cleared on success | OWED |
| J8 | Scheduling | `#cc-schedule` checkbox reveals `#cc-scheduled-time` via an inline `onchange` (`html:221-224`); `#cc-monopolize` = "🔒 Exclusive mode" (`html:226`). `_getSchedulingParams('cc')` emits `scheduled_at` (ISO) only when **both** the box is checked and a time is set, and `monopolize: true` only when checked (`js:6184-6197`) | OWED |
| **Card 2 — Research** | | | |
| J9 | Topic + voice | `#research-topic` text input; `🎤 #research-stt-button` → `handleSTTButtonClick('research-topic', btn)` (`js:2128-2133`) | OWED |
| J10 | Budget | `#research-budget` number, default `3.00`, min `0.50`, max `20.00`, step `0.50` (`html:252`); parsed with a `|| 3.00` fallback (`js:3381`) | OWED |
| J11 | Podcast / presentation | two checkboxes, **mutually exclusive at runtime**: checking one unchecks the other (`js:2114-2124`). They choose the *command*: `…to presentation` \| `…to podcast` \| plain `…deep research` (`js:3407-3412`), and add `target_duration_minutes: 15` or `target_languages: ['en','es-MX']` to `args` (`js:3419-3423`) | OWED |
| J12 | Dry run | `#research-dry-run`, **checked by default** (`html:263`) | OWED |
| J13 | Submit | `#submit-research-job` click **or** `Enter` in `#research-topic` (`js:2137-2145`). Empty topic refused with "⚠️ Please enter a research topic." Button disabled + spinner + "Submitting research job..."; `ensureValidToken()` first (`js:3400`) | OWED |
| J14 | Submit wire | `POST /api/v2/submit`, headers `Authorization` + `X-Session-ID: queueSessionId`, body `{command, args, question, ...schedulingParams}` (`js:3428-3436`) | OWED |
| J15 | Result | success → `✓ <Research\|Research→Podcast\|Research→Presentation> job submitted! Job ID: <id>` in green **and the topic input is cleared** (`js:3452-3457`) — unlike the CC card. Failure → `✗ Error: …` red | OWED |
| J16 | Scheduling | same pattern, prefix `research` (`html:271-278`, `js:3417`) | OWED |
| **Card 3 — Test Suite** | | | |
| J17 | Suite select | `#test-suite-types`, 4 optgroups / 12 options; `integration,e2e` selected by default (`html:301-322`) | OWED |
| J18 | Conditional fields | an inline `onTestSuiteTypeChange()` shows `#test-suite-file-path-row` iff the value is in `FILE_DRIVEN_TEST_TYPES_HTML = {smoke_direct, pytest_direct}`, and `#test-suite-fail-fast-row` iff the value is exactly `all` (`html:336-349`). The same set is duplicated in JS as `FILE_DRIVEN_TEST_TYPES` (`js:12`) and again in `cosa/agents/test_suite/job.py` | OWED |
| J19 | Extra args | `#test-suite-pytest-args` free text (`html:334`) | OWED |
| J20 | Dry run / auto-fix | `#test-suite-dry-run` **checked by default** (`html:351`). `#test-suite-auto-fix` is **unchecked in markup but overwritten at config load** from the INI default `tfeAutoFixDefault` (`js:917-924`); the per-run toggle travels as `auto_fix_on_failure` and does not round-trip to the INI | OWED |
| J21 | Validation + arg building | a file-driven type with no path → `✗ Error: <type> requires a test file path`, return (`js:3557-3561`). Otherwise the path is **prepended** to `pytest_args`, and `--fail-fast` **appended** when type is `all` and the box is checked (`js:3567-3573`) | OWED |
| J22 | Submit wire | `POST /api/test-suite/submit` — **not** `/api/v2/submit` — body `{test_types, dry_run, pytest_args?, auto_fix_on_failure?, scheduled_at?}` (`js:3587-3616`). Monopolize is **always on** server-side and shown as static text, not a checkbox (`html:367`) | OWED |
| J23 | Result | success → `✓ Test suite job submitted! Job ID: <id>, Position: <n>` — this card **does** print a queue position, unlike CC and research (`js:3627`). No input clearing | OWED |
| J24 | Scheduling | `#test-suite-schedule` + `#test-suite-scheduled-time` read **inline**, not through `_getSchedulingParams` (`js:3602-3606`) | OWED |
| **Card 4 — TFE Resume** | | | |
| J25 | Input | `#tfe-resume-input` textarea, free-form: a job id (`tfe-*`), a plan-doc path, or a description (`html:391-395`). No voice button | OWED |
| J26 | Submit | `#submit-tfe-resume-job` click only — no Enter handler (`js:2172-2177`). Empty input → a **browser `alert()`**, not an inline status (`js:8170-8173`). Button disabled, status "Resolving...", candidate list hidden | OWED |
| J27 | Submit wire | `authedFetch('POST /api/test-fix-expediter/resume-from', {resume_from})` (`js:8184-8188`) | OWED |
| J28 | Ambiguous result | `status === 'ambiguous'` → `_renderTFEResumeCandidates()` paints a clickable list into `#tfe-resume-candidates` — confidence %, job id, `stalled_at`, summary and reason — and the status reads `Found <n> possible matches — pick one.` (`js:8197-8257`) | OWED |
| J29 | Pick a candidate | clicking a row calls `_pickTFEResumeCandidate(jobId)`, which writes the id into the input and **re-submits** (`js:8259-8264`) | OWED |
| J30 | Resumed result | `status === 'resumed'` → `✓ Resumed via <source_type>: <job_id> from <phase>` and the input is cleared (`js:8206-8214`). Failure → `✗ <msg>` inline; `finally` re-enables the button | OWED |

**Note on J5 vs A1 Q3.** The Q A submit has a 2 s debounce; none of these four cards does. That is a legacy inconsistency, recorded so the build does not "fix" it into a disparity.

**Note on J6 / J14 / J22.** Three cards, three different auth header shapes (`getAuthHeaders()` spread, explicit `Authorization` + `X-Session-ID`, and `authedFetch`) and two different endpoints. The build should reproduce the *wire contract* per card; the header plumbing is the multiplexer's own `ApiClient` concern.

**A2 build items**: the whole section, specified by B1–B4, B7–B10 and J1–J30 — the section accordion (open, unpersisted, toolbar `📝`), three collapsed-by-default card accordions plus the always-open TFE card, and the four submit pipelines (CC → `/api/v2/submit` with Ctrl+Enter and `refreshAllQueues()`; Research → `/api/v2/submit` with Enter, mutually-exclusive podcast/presentation and input clearing; Test Suite → `/api/test-suite/submit` with conditional fields, arg building and the INI-seeded auto-fix box; TFE Resume → `/api/test-fix-expediter/resume-from` with the ambiguous-candidate picker), plus the shared scheduling/monopolize controls.

### A7 — Finished Tasks `✓`

Legacy: `notifications.html:780-853` (markup), `notifications.js:12501-12829` (statuses, glyphs, fetch, render, table, relative clock, refresh, polling), `:611` (poll start), `notifications.html:1517-1532` (accordion persist map), `:1534-1567` (`toggleSection`), `:1571-1593` (`applyPersistedAccordions`). Multiplexer: `render/FinishedTasksRenderer.ts`, `render/finishedTasksModel.ts`, `render/templates/finishedTasksTable.ts`, `stores/FinishedTasksStore.ts`, `render/templates/sectionHeader.ts`, `render/templates/sectionToolbar.ts:58`, `boot.ts:659-671`.

The multiplexer pane is a deliberate independent port of the same design (`FinishedTasksRenderer.ts:9-12`, no-code-reuse ruling, row `87812328`), so most rows match. The disparities are concentrated in disclosure state.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure, no saved state | **open**: `#finished-tasks-section` carries no `collapsed` class (`html:799`) (source) | **open**: the section root gets `data-collapsed` only on a click; `setSectionCollapsed` is not called at mount (`FinishedTasksRenderer.ts:199`, `sectionHeader.ts:157-164`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` calls `toggleSection('finished-tasks-section')` → flips `collapsed` on the CONTENT and the chevron `▼`/`▶` (`html:781`, `:1534-1567`). The ⟳ button carries `event.stopPropagation()` so it does not collapse (`html:790`) (source) | one delegated click on the header; `headerClickShouldCollapse` skips `button, a, input, select` except the chevron itself, so ⟳ does not collapse. Flips `data-collapsed` on the SECTION ROOT (`sectionHeader.ts:145-192`) (source) | MATCH in behaviour. **Note:** legacy marks `.collapsed` on the content, the multiplexer marks `data-collapsed="true"` on the root — different attributes, same visible result, both hidden by the shared sheet |
| B3 | Persistence of open/closed | **persisted**: `finished-tasks-section` is one of the three keys in `LUPIN_ACCORDION_PERSIST_KEYS` → `localStorage["notifications_finished_tasks_open"]`, a boolean in isOPEN sense, restored before first paint by `applyPersistedAccordions` (`html:1517-1532`, `:1562-1566`, `:1571-1593`) (source) | **not persisted**: collapse is declared SESSION-ONLY in the header template's own contract (`sectionHeader.ts:12-15`) and nothing reads or writes a collapse key for this pane (search: `grep -rn "finished.*open\|collapsed" multiplexer --include=*.ts \| grep -v test` finds only the session-only `data-collapsed`) (source) | **OWED** — a closed Finished Tasks re-opens on every reload |
| B4 | Toolbar show/hide | **no toolbar button at all**: the toolbar has 14 `data-section` buttons and none names `section-finished-tasks` (`html:43-75`; search: `grep -n 'data-section="section-finished-tasks"' notifications.html` → no match). The section can never be hidden (source) | **has one**: `✅` → `finished-tasks-pane`, persisted through `ViewStateStore` (`sectionToolbar.ts:58`, `SectionToolbarRenderer.ts:133-170`) (source) | **EXTRA** — the multiplexer can hide a section legacy cannot. Keep it (Rick's superset ruling) |
| B5 | Collapse/expand-all | the toolbar `⊟`/`⊞` act on Task List owner groups only; this section is untouched (`js:14312-14337`) (source) | the toolbar `⊟`/`⊞` call `requestBulkAccordionCollapse`, whose only consumer is `NotificationsListRenderer` (`SectionToolbarRenderer.ts:115-122`; search: `grep -rn store_view_state_changed multiplexer/render/*.ts` → one non-test hit, `NotificationsListRenderer.ts:362`) — this pane is untouched (source) | MATCH (neither acts on it) |
| B6 | Auto-reveal | none found (search: the only writers of `collapsed` for this id are `toggleSection` and `applyPersistedAccordions`) (source) | none found (search: `setSectionCollapsed` is called only from `wireSectionCollapse`) (source) | MATCH |
| B7 | Live header | count chip shows **`—` until the first poll**, then the number of VISIBLE rows; `#finished-tasks-updated` shows a bare `toLocaleTimeString()` (`js:12643-12644`, `html:782-787`) (source) | same em-dash rule, gated on `measuredOnce`; the stamp reads **`updated HH:MM:SS`** (`FinishedTasksRenderer.ts:315-317`, `:371-375`) (source) | MATCH on the count. **Note:** the stamp text differs ("14:22:01" vs "updated 14:22:01") — cosmetic, and the multiplexer's is the clearer of the two; flagged for María rather than filed as OWED |
| B8 | Refresh control | `⟳` → `refreshFinishedTasks()`: in-flight guard, fetch three statuses, re-render. Disclosure untouched (`html:788-791`, `js:12754-12777`) (source) | `⟳` → `store.refresh()`: same in-flight guard, same three calls, emits `store_finished_tasks_changed` with `stampUpdated:true`. Disclosure untouched (`FinishedTasksRenderer.ts:158-164`, `FinishedTasksStore.ts:114-154`) (source) | MATCH |
| B9 | Nested accordions | none — the body is a flat controls bar plus one table (`html:801-851`) (source) | none — same shape (`FinishedTasksRenderer.ts:190-196`) (source) | MATCH |
| B10 | Empty state | **six sentinels**, distinct testids: `finished-tasks-error`, `-unmeasured` ("Loading…"), `-no-filter`, `-none-in-window`, `-partial`, and the table (`js:12648-12674`) (source) | the same six, same wording, from `FINISHED_SENTINELS` + `finishedPaneState` (`FinishedTasksRenderer.ts:54-60`, `finishedTasksModel.ts:289-303`) (source) | MATCH in behaviour. **Note:** the empty-window testid is `finished-tasks-none-in-window` in legacy and `multiplexer-finished-tasks-empty` in the mux, and the mux's text names no statuses where legacy lists the lit ones. Wording drift, not a behaviour gap |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n section-finished-tasks notifications.js` → one hit, the element resolver at `:12535`; the reading pane carries Action Required and abstracts only, `js:17206-17241`, `:21527`) (source) | the pane is a sibling in the main column and the reading pane is a separate shell (`multiplexer.html:239-240`) (source) | MATCH |
| F1 | Status pills | three, multi-select, **DONE pre-lit alone**, positive polarity (`aria-pressed="true"` = shown). A zero-count pill stays visible, dimmed and clickable. A pill click is a **client-side re-render**, never a fetch. Turning the last pill off **re-lights DONE** (`html:809-828`, `js:12548-12564`, `:12631-12637`, `:12796-12810`) (source) | identical on every clause, same ids (`finished-pill-wont-fix` spelled with a dash deliberately) (`finishedTasksTable.ts:41-56`, `:134-168`, `FinishedTasksRenderer.ts:267-296`) (source) | MATCH |
| F2 | Pill-selection persistence | **none**: the lit set lives only in the DOM's `aria-pressed`; no key is read or written (search: `grep -n "shownStatuses\|finished.*pill.*localStorage" notifications.js` → no match) (source) | **persisted** to `localStorage["lupin.finishedTasks.shownStatuses"]`, positive polarity, corrupt/unknown values fall back to `["done"]` (`finishedTasksModel.ts:86`, `:160-171`, `FinishedTasksRenderer.ts:100-117`) (source) | **EXTRA** — the multiplexer remembers a choice legacy forgets. Keep it |
| F3 | Window slider | 1–14 **days** on screen, hours on the wire (×24 in the handler). `input` = live preview, **no fetch**; `change` = commit + fetch (`html:830-846`, `js:12812-12825`) (source) | same range, same split; `input` also writes the store's `days` so a drag that is never released still moves the preview, and the ×24 lives in `windowSinceIso` only (`finishedTasksTable.ts:170-193`, `FinishedTasksRenderer.ts:246-264`, `finishedTasksModel.ts:137-139`) (source) | MATCH |
| F4 | Fetch shape | `GET /api/tasks/events?to_status=<s>&since=<iso>&limit=500`, **one call per status, all three every poll** regardless of which pills are lit; a failed status leaves its key **absent** (never `[]`) (`js:12566-12607`) (source) | identical, plus the result object is built fresh and swapped in at the end so a mid-loop repaint cannot mix two windows (`FinishedTasksStore.ts:114-154`) (source) | MATCH |
| F5 | Auto-poll | 60 s `setInterval`, started at init after Fleet Status and before Task List (`js:12828`, `:611`) (source) | 60 s, `startPolling()` after mount, same position in boot (`FinishedTasksStore.ts:156-160`, `boot.ts:671`) (source) | MATCH |
| F6 | Row shape | four cells: WHEN (relative age, absolute on hover, status glyph as a **prefix inside the cell**, never a fifth column), TITLE, WHO (persona only), WHY (clamped, full text on hover). Header reads "Title" (`js:12677-12734`) (source) | the same four cells and the same glyph-as-prefix rule; rows additionally carry the shared `task-status-*` tint class, and WHO routes through `shared/personaLabel` (`finishedTasksTable.ts:215-244`, `finishedTasksModel.ts:203-208`) (source) | MATCH (the tint is EXTRA) |
| F7 | Row interaction | none — the table is read-only, no click handlers on rows (search: `grep -n "finished-task-row" notifications.js` → only the template at `:12722`) (source) | none — same (search: `grep -rn "finished-task-row" multiplexer --include=*.ts \| grep -v test` → only the template) (source) | MATCH |
| F8 | Unmeasured vs zero | pill badge shows `—` for a status never measured and `0` for a measured zero; the count chip shows `—` before the first poll and never `0` (`js:12635`, `:12643`) (source) | same, and `measuredOnce` gates the chip explicitly (`FinishedTasksRenderer.ts:356-369`, `:315-317`) (source) | MATCH |

**A7 build items**: (1) persist the Finished Tasks open/closed state across reloads, the way legacy's `notifications_finished_tasks_open` does.

### A3 — Action Required `✓`

Legacy: `notifications.html:410-432` (markup), `notifications.js:318-341` (state fields), `:208` (storage key), `:21327-21376` (save), `:21378-21530` (restore), `:21552-21576` (`ensureActionRequiredExpanded`), `:21578-21663` (`addActionRequiredNotification`), `:21673-21742` (`activateNextNotification`), `:23070-23330` (`renderActionRequiredNotification`), `:24173+` (countdown), `:24999+` (`togglePause`), `:25056+` (`cancelActionRequired`), `:25189-25214` (`updateActionRequiredCount`), `:25751-25808` (keyboard), `:17671-17755` (reading-pane lift).
Multiplexer: `multiplexer.html:134-135` (mount), `render/ActionRequiredRenderer.ts`, `render/templates/actionRequiredInteractive.ts`, `actionRequiredQueueRow.ts`, `actionRequiredReadOnly.ts`, `render/templates/sectionHeader.ts`, `stores/ActionRequiredStore.ts`, `render/ReadingPaneRenderer.ts:300-347`.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **open and visible**: `#action-required-content` carries no `collapsed`, `#action-required-section` no `section-hidden`, and its toolbar button starts `active` (`html:411-416`, `:45`) (source) | **open and visible**: `#action-required-section` has no `hidden` attribute (`multiplexer.html:134`) and the renderer sets no `data-collapsed` on mount (`ActionRequiredRenderer.ts:128-176`) (source) | MATCH |
| B2 | Toggle trigger | click anywhere on `.section-header` → `toggleSection('action-required-content')` (`html:412`). Because the id does not end in `-section`, `toggleSection` falls back to the previous-sibling header's `.toggle-button` (`html:1545-1549`). The chevron is a real `<button>` (`html:414`) | click anywhere on the header **except** a real control, plus the chevron itself (the carve-out), → flips `data-collapsed` on the section root (`sectionHeader.ts:117-190`). Chevron is a focusable `<button type="button">` with `aria-label="Collapse section"` (`:100-106`) | MATCH. **Note:** legacy flips `.collapsed` on the *content*; the mux flips `data-collapsed` on the *section root*. Different mechanism, same user-visible result |
| B3 | Persistence (disclosure) | **none**: neither `action-required-content` nor `action-required-section` appears in `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **none, explicitly**: "Session-only collapse on the section root (07 §3.A U-A3)" (`ActionRequiredRenderer.ts:166-167`); `wireSectionCollapse` writes no storage (`sectionHeader.ts:176-190`) | MATCH (both: not persisted) |
| B3b | Persistence (**contents**) | the notifications themselves survive a reload: `saveActionRequiredState()` writes `localStorage['notifications_action_required']` (`js:208`, `:21327-21376`) with the queue order, the active id, the pause flag, per-item `expiresAt`/`activatedAt`/`timeoutSeconds`, the multiple-choice `currentQuestionIndex` and `collectedAnswers`. `restoreActionRequiredState()` replays it, drops expired items, restarts the countdown and re-attaches the keyboard listener (`:21378-21530`) | **none**: `grep -c localStorage stores/ActionRequiredStore.ts` → **0**. Items live only in memory; a reload loses the queue, the half-answered multiple-choice stepper (kept in `this.steps`, `ActionRequiredRenderer.ts:122`) and the active-card position | **OWED** |
| B4 | Toolbar show/hide | `⚠️` button, `data-section="action-required-section"` (`html:45`), persisted to `lupin_section_visibility` | **absent**: `SECTION_TOGGLES` has nine entries and `action-required-section` is not one of them (`templates/sectionToolbar.ts:49-67`). Search: `grep -n 'action-required' render/templates/sectionToolbar.ts` → 0 hits. There is no way to hide this section | **OWED** |
| B5 | Collapse/expand-all | not affected — the toolbar `⊟`/`⊞` act on Task List owner groups only (`js:14312-14337`) | not affected — `requestBulkAccordionCollapse` is consumed only by `NotificationsListRenderer.ts:358` (`grep -rn requestBulkAccordionCollapse` → toolbar, store, notifications list; **no** AR subscriber) | MATCH (neither is affected) |
| B6 | Auto-reveal on a new item | **yes, both axes**: `ensureActionRequiredExpanded()` removes `section-hidden`, re-lights the toolbar button and **saves the visibility change**, then removes `collapsed` and resets the chevron (`js:21552-21576`); `addActionRequiredNotification` also forces `section.style.display='block'` (`:21612-21618`). Rendering the active card additionally `scrollIntoView({behavior:'smooth', block:'start'})` after a 50 ms settle (`:23204-23210`) | **none**: the renderer only repaints on `store_action_required_changed` (`ActionRequiredRenderer.ts:170-176`); no code clears `data-collapsed`, and `grep -rn scrollIntoView --include=*.ts multiplexer/` (non-test) returns nothing. A collapsed section stays collapsed while a prompt waits inside it | **OWED** |
| B7 | Live header | `⚠️ Action Required: <span id="action-required-count">` (`html:413`), updated by `updateActionRequiredCount()`, which counts **only** items that are neither responded nor expired (`js:25189-25197`) | `renderSectionHeader({icon:"⚠️", title:"Action Required"})` with a `.section-header-count` chip, set to `items.length` — the store's **whole** list, which retains responded / expired / cancelled cards for their grace period (`ActionRequiredRenderer.ts:141-146`, `:227`, `:256-259`; grace periods at `ActionRequiredStore.ts:31-34`) | **OWED** — the count differs for the 600–1500 ms a finished card lingers |
| B8 | Refresh control | none in the header (`html:412-415`) | none — `renderSectionHeader` is called with no `actions` array (`ActionRequiredRenderer.ts:141-146`) | MATCH |
| B9 | Nested accordions | none. The body is an **active slot** (one full card) plus a **pending queue** of minimized one-liners (`html:418-425`); a minimized row is not an accordion | none, same two-part body: `.action-required-active-slot` + `.action-required-pending-queue` (`ActionRequiredRenderer.ts:150-156`), queued rows via `renderActionRequiredQueueRow(item, i+1)` with `#N` badges (`:250`) | MATCH |
| B10 | Empty state | `#action-required-empty` reads `✓ No pending actions`, shown when both halves are empty (`html:428-430`, `js:25199-25211`, `:21683-21687`) | `renderActionRequiredEmpty()` builds the same `#action-required-empty.action-required-empty-state` with the same `✓ No pending actions` text, painted at count 0 and on unmount-at-0 (`templates/actionRequiredReadOnly.ts:86-92`, `ActionRequiredRenderer.ts:229-233`, `:190-196`) | MATCH |
| B11 | Layout mode | in **horizontal** layout the live `#action-required-content` element is **lifted** into the reading pane at a forced 50/50 split, and moved home with the prior pane content and split ratio restored when the queue drains (`js:17671-17755`, `:21620-21624`, `:21688-21693`) | same behaviour, one level up: `#action-required-**section**` is lifted (`ReadingPaneRenderer.ts:325-347`), gated on a count of *non-terminal* items only so a grace-period card cannot hold the pane (`:308-323`), and re-entered on a layout toggle (`:356-360`) | MATCH. **Note:** legacy moves the content div, the mux moves the whole section (header included) |
| **Card behaviours** | | | |
| R1 | One card at a time | one active card; the rest are minimized rows with a 1-indexed queue position (`js:21646-21651`, `:21743`) | same, stated as parity with legacy (`ActionRequiredStore.ts:31-37`, `ActionRequiredRenderer.ts:29-33`) | MATCH |
| R2 | Timer start | **deferred**: `expiresAt` is null on arrival and set only when the card is activated (`js:21584`, `:21712`) | same, and it cites the legacy lines (`ActionRequiredStore.ts:33-35`) | MATCH |
| R3 | TTS deferral | if TTS is playing, activation is **deferred** entirely — the card arrives as a minimized row at position 1 and activates when the current TTS ends (`js:21636-21645`) | not found: the store activates the head as soon as the slot frees (`activateHead`, `ActionRequiredStore.ts:371-394`); `grep -n "activeTTS\|ttsQueue" stores/ActionRequiredStore.ts` → 0 hits | **OWED** |
| R4 | Countdown | per-card `setInterval` timer, `--:--` placeholder, a countdown div and a **progress bar** that drains (`html`-built at `js:23190`, `:23196-23198`; `startCountdownTimer` `:24173`) | 1 Hz `setInterval` in the store, renderer updates `.action-required-countdown` via `textContent` only (`ActionRequiredStore.ts:432-433`, `ActionRequiredRenderer.ts:15-17`, `:364`, `:421`). **No progress bar**: `grep -n "progress" render/templates/actionRequiredInteractive.ts` → 0 hits | **OWED** — the progress bar |
| R5 | Pause / resume | a `⏸️` button in every card header (`js:23187-23189`) plus the **P** key, freezing the timer and the audio and accumulating `totalPausedDuration`; the pause state is persisted (`js:24999+`, `:21351-21353`, `:25763-25768`) | **absent**: `grep -n "pause" render/templates/actionRequiredInteractive.ts render/templates/actionRequiredQueueRow.ts` → 0 hits. The store freezes a card only for an **offline/backoff** connection state, which is not a user control (`ActionRequiredStore.ts:10`, `:194`, `:432`) | **OWED** |
| R6 | Cancel | a `✕` button in every card header, "Cancel and use default (Esc)" (`js:23180-23182`, `:23221-23224`), and the **Escape** key (`js:25794-25804`) | **absent**: no cancel control in the interactive template, and no `keydown` listener for the AR section (`grep -rn "keydown\|keypress" multiplexer/ --include=*.ts` non-test → `sessionNameEditModal`, `EpicBoardRenderer`, `taskLookupBox`, `jobBucket`, `TaskListRenderer`, `recordingManager` — none is AR) | **OWED** |
| R7 | Keyboard shortcuts | **Y** / **N** answer a `yes_no`, **C** toggles the comment field, **P** pauses, **Esc** cancels; suppressed while focus is in an `INPUT`/`TEXTAREA`; attached once and re-attached on restore (`js:25751-25808`, `:21515-21517`) | **absent** (same search as R6) | **OWED** |
| R8 | yes_no controls | three buttons — **✓ Yes (Y)**, **✗ No (N)** and **⊘ Neither** ("the question itself needs re-framing") — with the server's `response_default` highlighted via `.default-value` (`js:23095-23112`) | two buttons, **Yes** and **No**, no Neither, no default highlight (`actionRequiredInteractive.ts:23-24`, `:108-121`) | **OWED** — the Neither option and the default highlight |
| R9 | yes_no comment | a hint line ("Press C to add comment", or "You may comment…" when `display_qualifier_widget` is set, which also pre-expands it), a 300-char text input and a **🎤 mic** (`js:23113-23123`, `:25837+`) | **absent**: no comment row in the template (`grep -n comment actionRequiredInteractive.ts` → only a legacy-citing code comment) | **OWED** |
| R10 | open_ended | a voice-first row: **🎤 mic first** ("Enter or Space to record, 30 s max, ESC cancels"), a text input prefilled from `response_default`, then Submit (`js:23132-23143`) | text input with the default as **`placeholder`**, not as a value, plus Submit and Enter-to-submit (`actionRequiredInteractive.ts:26`, `:247-256`). No mic | **OWED** — the mic, and prefill-as-value rather than placeholder |
| R11 | multiple_choice | one question at a time with Back / Next / Submit, saved answers, plus an **"Other" free-text** option and prediction prefill (`js:23145`, `renderMultipleChoiceUI` `:23519`, `navigateMultipleChoice` `:23729`) | the same one-at-a-time stepper with Back / "Next Question →" / Submit, radio or checkbox by `multiSelect`, answers kept across a rebuild (`actionRequiredInteractive.ts:14-17`, `:25-27`). Its own header states "**Legacy's 'Other' free text, 🎤 mic and prediction prefill are NOT ported**" (`:11-12`) | **OWED** — Other, mic, prediction prefill (self-declared) |
| R12 | open_ended_batch | stacked inputs prefilled from each question's default, Submit All, "No questions provided." for an empty payload (`js:23147`, `renderOpenEndedBatchUI`) | same, `value="${q.defaultValue}"`, Submit All, and the same empty-payload text (`actionRequiredInteractive.ts:28-31`, `:279`, `:313-320`) | MATCH |
| R13 | Validation | Submit is inert until every question is answered; unanswered questions are marked | same, and it cites legacy's functions (`actionRequiredInteractive.ts:32-33`, `:144`, `:207`) | MATCH |
| R14 | Card chrome | project badge from `sender_id`, persona badge from the notification's `voice_persona`, markdown-rendered message, an `.action-required-abstract` block capped at 200 px, an `📋` **abstract indicator** opening the full-abstract popup, and a prediction-hint section (`js:23151-23200`) | none of these six: `grep -n "abstract\|persona\|prediction\|badge" render/templates/actionRequiredInteractive.ts` → 0 hits | **OWED** |
| R15 | Finished-state visuals | the card is replaced by a read-back / expired message; the item is dropped from the map and storage | five explicit renderer states — `submitting` (controls disabled + "Submitting…"), `responded` (read-back), `failed` (re-enabled + `.action-required-error-stripe`), `expired` ("Expired — default applied"), `cancelled` ("✓ Responded in another session") — each shown for a grace period (`ActionRequiredRenderer.ts:20-27`, `:290-360`) | **EXTRA** — a richer, explicit state machine; keep it |
| R16 | Answer path | direct POST, optimistic | `respondAndAwait()` only — the optimistic path was deleted because it swallowed a rejected answer (`ActionRequiredRenderer.ts:38-43`) | **EXTRA** |
| R17 | Local expiry | local countdown hits zero → the card expires locally | same, local-only, deliberately does **not** POST a default (`ActionRequiredStore.ts:22-25`) | MATCH |

**Note on R3.** Legacy couples the Action Required queue to the TTS queue: a prompt will not take the slot while audio is playing. The multiplexer's two stores are independent. This is a cross-section behaviour, so it lands where A4 does too.

**A3 build items**: (1) reload persistence for the AR queue, active id, pause state and the half-answered stepper; (2) a toolbar button for the section; (3) auto-reveal on arrival — un-hide, un-collapse, save the visibility change and scroll into view; (4) count only non-terminal items in the header chip; (5) defer activation while TTS is playing; (6) the draining progress bar; (7) pause/resume — the `⏸️` button and the **P** key; (8) cancel — the `✕` button and **Escape**; (9) the **Y**/**N**/**C** keyboard shortcuts; (10) the **⊘ Neither** button and the `response_default` highlight; (11) the yes_no comment row with its hint, 300-char input, mic and `display_qualifier_widget` pre-expansion; (12) the open_ended mic and default-as-value prefill; (13) multiple_choice "Other" free text, mic and prediction prefill; (14) the card chrome — project badge, persona badge, abstract block, `📋` abstract indicator and prediction hint.

### A5 — Notifications (date → sender → card) `✓`

Legacy: `notifications.html:479-680` (markup), `:1517-1598` (accordion persistence), `notifications.js:389-410` (history window model), `:1747-1855` (header wiring), `:5141-5155` + `:25324-25381` (auto-reveal), `:16873-16935` + `:18083-18107` (Recent Activity), `:18803-18893` (ingest), `:19013-19331` (sender card + date accordions), `:19604-19822` (counts, deletes, Clear-All state), `:19833-20121` (history load + window picker), `:20775-20905` (Clear All), plus `broadcast-panel.js`.
Multiplexer: `multiplexer.html:158-209`, `render/NotificationsHeaderRenderer.ts`, `render/NotificationsListRenderer.ts`, `render/templates/senderCard.ts`, `render/templates/dateAccordion.ts`, `render/historyWindowDropdown.ts`, `stores/historyWindow.ts`, `render/BroadcastCardRenderer.ts` + `render/templates/broadcastCard.ts`, `stores/BroadcastStore.ts`, `render/CommonsActivityRenderer.ts` + `stores/CommonsStore.ts`, `render/TtsPreviewSliderRenderer.ts`, `render/SessionStripRenderer.ts`.

#### A5.1 — The section itself

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#notifications-section` carries no `collapsed` class (`html:531`) (source) | open: `#notifications-pane` has no `data-collapsed` attribute at load (`multiplexer.html:175-176`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('notifications-section')` (`html:480`), flipping `collapsed` + the chevron `▼`/`▶` (`html:1534-1567`). Header controls call `event.stopPropagation()` (`html:490`, `:518`, `:524`) (source) | a click on the header bar → collapse the SIBLING `#notifications-pane` via `data-collapsed`, using the shared `headerClickShouldCollapse` predicate so a click on a control or the chevron is not a collapse (`NotificationsHeaderRenderer.ts:300-312`) (source) | MATCH |
| B3 | Persistence | **none**: `notifications-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`), so it opens on every load (source) | **none**: the collapse writes only the DOM attribute; no `ViewStateStore` call in the header renderer (search: `grep -n "setAccordion\|viewState" render/NotificationsHeaderRenderer.ts` → no hits) (source) | MATCH |
| B4 | Toolbar show/hide | `💬` (`html:47`) toggles `section-hidden` on **`#section-notifications`** — header AND body (`notifications.js:15299-15346`) (source) | `💬` targets **`notifications-pane`** (`templates/sectionToolbar.ts:49`), and `applyVisibilityToDom` hides only that id (`SectionToolbarRenderer.ts:165-173`). The header bar lives in a separate `.notifications-header-region` (`multiplexer.html:158-162`), so hiding leaves the title, count, slider, picker and Clear All on screen | **OWED** — hiding the section must hide its header too |
| B5 | Collapse/expand-all | toolbar `⊟`/`⊞` acts on Task List owner groups only; Notifications cards are untouched (`notifications.js:14312-14337`) (source) | `⊟`/`⊞` also collapse/expand every sender card and date accordion, persisted per id (`NotificationsListRenderer.ts:357-363`, `:877-885`) | EXTRA (A0 B5 already records it) |
| B6 | Auto-reveal | TTS playback start auto-expands the sender card AND its date accordion, then scrolls the row into view (`notifications.js:5141-5155` → `:25337-25381`, `expandSenderCard` `:25324-25333`, `expandDateAccordion` `:19317-19331`) (source) | marks `tts-playing` on the bubble only (`NotificationsListRenderer.ts:967-985`); no expand and no scroll — `grep -rn "scrollIntoView\|expandAccordions" multiplexer --include=*.ts` (non-test) returns nothing (source) | **OWED** |
| B7 | Live header | title `Claude Code Notifications: N`, N = the sum of every group's `totalCount`, recomputed on each arrival/removal (`html:481`, `notifications.js:19811-19822`). Admin filter badge text follows the mode (`notifications.js:6339-6345`, `:6401-6417`) (source) | same title string; N = `store.list().length`, repainted on `store_notifications_changed` (`NotificationsHeaderRenderer.ts:266-275`, `:371-388`). Badge text from `FILTER_MODES` (`:101-105`, `:394-405`). Also paints an env-label prefix + live clock from `sys_time_update` (`:162-176`, `:359-365`) | MATCH (the clock/env prefix is EXTRA). **Note:** legacy counts outgoing response bubbles in `totalCount` (`notifications.js:19946-19955`); the mux counts store rows, where a response is a `direction:"outgoing"` row (`shared/types.ts:427-432`) — same intent, populations not proven equal |
| B8 | Refresh control | no `⟳`: the markup's refresh button is commented out (`html:515-522`). Refetch happens through the history-window picker (`notifications.js:19987-20015`) (source) | no `⟳` either; the picker drives the reload (`historyWindowDropdown.ts:45-50`). The header adds an `Expired ▾` panel listing `store.history()` (`NotificationsHeaderRenderer.ts:223-232`, `:424-451`) and a `🔄 Bounce server` button (`:246-253`) | MATCH on refresh; `Expired ▾` and the bounce button are EXTRA (the bounce's placement is Phase 1 H3/T1) |
| B9 | Nested accordions | four levels: broadcast card, Recent Activity, sender cards, date accordions — see A5.2–A5.5 | same four — see A5.2–A5.5 | see below |
| B10 | Empty state | nothing: `#notifications-list` is simply empty (`html:676-678`); no placeholder element or copy exists (search: `grep -n "notifications-list" notifications.js` — every hit writes cards or clears the list) (source) | paints `[data-testid="multiplexer-empty-state"]` in four states — `loading`, `failed`, `filtered`, `empty` (`NotificationsListRenderer.ts:445-453`, `:642-695`) | EXTRA |
| B11 | Layout mode | horizontal mode moves the section into `.left-column` and opens a reading pane; state in `notifications_layout_mode` + `notifications_pane_split_ratio` (`notifications.js:224-225`, `:17490-17510`, `:17927-17975`) (source) | same two-column model, `body[data-layout-mode]` + `.left-column` (`ReadingPaneRenderer.ts:160-168`, `:241`) (source) | MATCH (ReadingPane detail belongs to its own row set; not re-inventoried here) |
| N1 | Filter badge → Filter Settings | clicking `#notifications-filter-badge` un-hides Filter Settings, saves it and scrolls to it (`notifications.js:1831-1845` → `showAndScrollToFilterPanel`, `:6364-6384`) (source) | the badge is a passive `<span>`; no click listener (`NotificationsHeaderRenderer.ts:198-201`) and no Filter Settings section (S11) | **OWED** (same item as A0 B6 / A11) |
| N2 | Admin filter control | three buttons live in the Filter Settings section, not in this header; the header carries only the badge (`html:482-483`) (source) | the Mine / Not Mine / All Users switch is built INTO this header, admin-gated (`NotificationsHeaderRenderer.ts:202-220`, `:407-410`) | EXTRA |
| N3 | TTS preview slider | `0–100 step 12.5` range + datalist + `%` label inside the header, `event.stopPropagation()` on the container; value persists in `notifications_tts_preview_fraction_runtime`, INI default seeds it (`html:489-513`, `notifications.js:1747-1769`, key at `:216`) (source) | same range/step/datalist (`templates/ttsPreviewSlider.ts:74-79`), mounted in `#tts-preview-slider-mount` inside the header with a click-swallow (`NotificationsHeaderRenderer.ts:182-186`), and writes BOTH its own envelope key `tts_preview_fraction` and the legacy raw key (`TtsPreviewSliderRenderer.ts:38-42`, `:261`) (source) | MATCH (the second key is a superset write, shared with legacy) |
| N4 | History window picker | `#history-window-dropdown` with six options (Today / 24 h / 2 days / week / month / All time), value in `notifications_history_window`, default 48 h; a change clears every sender group and reloads (`notifications.js:389-410`, `:19987-20015`, `:20059-20121`) (source) | same ids, class names, options, order, key and 48 h default (`stores/historyWindow.ts:15-39`, `historyWindowDropdown.ts:53-60`); the store owns the value and the reload | MATCH |
| N5 | Clear All | enabled only when the count is non-zero (`notifications.js:20888-20905`); confirm names the count AND the window label — "Clear all N notifications (Last 2 days)? This cannot be undone." — then ONE bulk `DELETE /api/notifications/bulk/{email}?hours=…[&exclude_own_jobs]`, then wipes cards, strip icons and the counter (`:20775-20872`) (source) | enabled only when `visibleEntries()` is non-empty (`NotificationsHeaderRenderer.ts:385`); confirm is the fixed string "Clear all notifications? This cannot be undone." (`:97`), then an N-times `DELETE /api/notifications/{id}` loop that removes only the ids the server confirmed and reports `Cleared X, Y failed.` (`:457-482`) | **OWED** — the confirm must name the count and the active window. The per-id loop + partial-failure report is EXTRA and stays |
| N6 | CC session strip | `#cc-session-strip` above the list: one icon per CC session, `👁 Focus` and `👁 All` toggles, unread badges, focus persisted in `notifications_cc_focus_state`, hide-inactive in `notifications_cc_hide_inactive_strip` (`html:658-674`, `notifications.js:214-215`, `:16559-16690`, `:16744`) (source) | same markup and both toggles (`multiplexer.html:191-202`), focus and hide-inactive persisted under the **same legacy keys and JSON shape** (`SessionStripRenderer.ts:73-100`) (source) | MATCH |

#### A5.2 — Broadcast card (`#broadcast-submit-card`)

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#broadcast-submit-section` carries no `collapsed` class (`html:548`), per the Q9 comment at `:545-547`. Its chevron is nevertheless authored `▶` (`html:543`) — a legacy quirk the first click corrects (source) | open by default: `DEFAULT_CARD_OPEN = true` with the chevron `▼` when open (`stores/BroadcastStore.ts:33`, `templates/broadcastCard.ts:43-58`) (source) | MATCH on disclosure. **Note:** legacy's initial chevron glyph disagrees with its own state; the mux is self-consistent |
| B2 | Toggle trigger | a click on `.job-submit-card-header` → `toggleSection('broadcast-submit-section')` (`html:541`) (source) | a click on `#broadcast-submit-header` → `toggleCard()`, flipping `data-card-open` and the chevron (`BroadcastCardRenderer.ts:253-278`) (source) | MATCH |
| B3 | Persistence | `localStorage["notifications_broadcast_card_open"]`, boolean isOPEN, written by `toggleSection` and replayed pre-paint (`html:1518`, `:1563-1566`, `:1571-1593`) (source) | `StorageService` key `broadcast:card-open`, schema 1, `{open:boolean}` (`stores/BroadcastStore.ts:31-32`, `:115-118`, `:138-140`) (source) | MATCH in behaviour. **Note:** different keys, so a choice in one client does not carry to the other |
| Q1 | Recipients row | "Sending to:" + `↻ refresh` re-fetches the active-session list (`html:550-553`; `broadcast-panel.js`) (source) | same row and button; a user click refreshes immediately, and a persona-lifecycle burst refreshes on a trailing debounce (`BroadcastCardRenderer.ts:210-242`, `:285-300`) | MATCH (the debounced auto-refresh is EXTRA) |
| Q2 | Compose row | `🎤` STT (30 s, Esc cancels) + textarea + Send, Send disabled until the textarea has content (`html:560-574`) (source) | same three controls and the same enablement rule (`templates/broadcastCard.ts:71-84`, `BroadcastCardRenderer.ts:243-266`) | MATCH |
| Q3 | Send | posts the broadcast; feedback arrives as `commons_broadcast_ack` cards in the stream — the inline status/aggregate panels were removed 2026-05-13 (`html:575-579`) (source) | opens a confirm modal, then posts, and writes into `#broadcast-submit-status` (`BroadcastCardRenderer.ts:247-252`, `templates/broadcastCard.ts:86-87`) | EXTRA (a confirm step and a status line legacy no longer has) |

#### A5.3 — Recent Activity (commons)

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#commons-recent-activity-body` has no `collapsed` class, chevron `▼` (`html:638-641`) (source) | open: the body carries no `collapsed` class until clicked (`CommonsActivityRenderer.ts:309-313`) (source) | MATCH |
| B2 | Toggle trigger | a click on `.commons-recent-activity-header` → `toggleSection('commons-recent-activity-body')`; each control inside stops propagation (`html:589`, `:600`, `:609`, `:619`, `:625`, `:637`) (source) | a delegated header click, ignoring clicks that originate on a control (`CommonsActivityRenderer.ts:242-252`) (source) | MATCH |
| B3 | Persistence | `localStorage["notifications_recent_activity_open"]` via the persist map (`html:1519`, `:1563-1566`) (source) | **none** — `onHeaderToggle` only flips a CSS class; no storage write (`CommonsActivityRenderer.ts:309-313`; `grep -n "storage\|setJSON" render/CommonsActivityRenderer.ts` → no hits) (source) | **OWED** |
| Q1 | Time window | six options (Today / 24 h / 2 days / week / month / All time), seeded from the INI default every load and **not persisted**; a change refetches (`html:622-632`, `notifications.js:16884-16890`, `:17149-17152`) (source) | same options in the template; the value lives in `CommonsStore` (default from the same INI-fed default) and a change re-hydrates (`CommonsStore.ts:140`, `:231-264`, `CommonsActivityRenderer.ts:254-257`) (source) | MATCH |
| Q2 | Three-axis filter | Direction / Kind / Persona selects, applied client-side over the cached entries, persisted as one JSON object in `notifications_commons_activity_filter` (`html:596-621`, `notifications.js:219`, `:18083-18107`) (source) | same three selects; the filter lives in `CommonsStore` and persists under `commons:activity-filter` schema 1 (`CommonsStore.ts:46-47`, `:273-285`, `:321-333`) (source) | MATCH in behaviour. **Note:** different keys |
| Q3 | Refresh `↻` | reloads the activity stream AND repopulates the persona dropdown, keeping a still-valid selection (`notifications.js:16918-16931`, `_repopulatePersonaDropdown` `:18115+`) (source) | same pair — `hydrate()` plus `populatePersonaDropdown()`, sticky-when-valid (`CommonsActivityRenderer.ts:301-307`, `:403-449`) (source) | MATCH |
| B10 | Empty state | `<em>No activity in window.</em>` (`html:646-648`) (source) | same copy, plus a distinct line when a filter is active (`CommonsActivityRenderer.ts:355-365`) | MATCH (the filter-aware copy is EXTRA) |
| B4 | Toolbar show/hide | none — Recent Activity has no toolbar button; it is hidden only by the INI kill-switch (`notifications.js:16875-16882`) (source) | has its own toolbar button `📡` (`templates/sectionToolbar.ts:51`), and still honours the server kill-switch (`CommonsActivityRenderer.ts:325-327`) | EXTRA |

#### A5.4 — Sender cards

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | expanded: `group.collapsed` starts `false` and the dates container is not hidden (`notifications.js:18820-18829`, `:19695-19713`) (source) | expanded: the card renders without `data-collapsed`, then persisted state is re-applied (`NotificationsListRenderer.ts:822-830`, `:869-875`) (source) | MATCH |
| B2 | Toggle trigger | a click on `.sender-card-header` → `toggleSenderCard`; every control in the header calls `event.stopPropagation()` (`notifications.js:19142-19155`, `:19695-19713`) (source) | a delegated header click that first exempts copy / gist / rename / delete (`NotificationsListRenderer.ts:781-797`) (source) | MATCH |
| B3 | Persistence | **none** — `group.collapsed` is in-memory only; no storage write in `toggleSenderCard` (`notifications.js:19695-19713`) (source) | persisted per sender as `sender::<sender_id>` in `ViewStateStore`, re-applied after every keyed re-render and across reloads (`NotificationsListRenderer.ts:826-829`, `:869-875`) | EXTRA (survives more than legacy's does) |
| Q1 | Header content | active dot `●`/`○`, recency glyph, PROJECT name (uppercased), `#sessionId` + 📋 copy + ✨ gist + click-to-rename name, persona badge, `N new`, `(total)`, `Last: …`, `×` delete-all, chevron (`notifications.js:19068-19160`) (source) | the same elements and class names (`templates/senderCard.ts:188-207`), with `display_name` in `.sender-project-name` (`:192`) and the new-count printed as the bare number (`:196-198`) (source) | **OWED** — the new-count reads `N` where legacy reads `N new` (`notifications.js:19785`). The project-name content difference is a noted Category-4 choice, not re-litigated here |
| Q2 | Conversation-mode button | icon pair depends on `ttsInteractionMode`: chorus `🔊`/`🤭`, solo `📞`/`🔔`, with four distinct tooltips (`notifications.js:19050-19067`) (source) | fixed `🔊`/`🤭` and two tooltips; no solo variant (`templates/senderCard.ts:293-298`; `grep -rn "📞" multiplexer --include=*.ts` non-test → no hits) (source) | **OWED** |
| Q3 | Ordering | newest activity first; a live arrival moves the card to the top, and a conversation-mode-pinned card always holds index 0 (`notifications.js:18885-18889`, `:18981-19006`, `:19167-19184`) (source) | sorted by last activity, with a boot-supplied comparator that hoists pinned senders (`NotificationsListRenderer.ts:148-150`, `:182`, `:475-476`; pin attributes in `ConversationModePinRenderer.ts`) (source) | MATCH |
| Q4 | Active indicator | `group.isActive` — the most recently active sender gets `●` and `.sender-card-active` (`notifications.js:19081-19084`) (source) | the max `last_active_ts` among rendered cards, repainted in place on kept cards (`NotificationsListRenderer.ts:483-495`, `:1230-1232`, `templates/senderCard.ts:323-327`) (source) | MATCH |
| Q5 | `×` delete-all | confirm, then delete that sender's conversation server-side and drop the card (`notifications.js:19153`, `deleteSenderConversation`) (source) | confirm naming the count and project, `DELETE /api/notifications/conversation/{sender}/{email}`, and a warning line when the server delete fails (`NotificationsListRenderer.ts:1080-1103`) (source) | MATCH |
| Q6 | CC voice-input row | conv-mode button + 🎤 + text input + Send, between header and dates, for `claude.code` sessions with a session id (`notifications.js:19120-19139`) (source) | the same four controls with the same ids, emitted for any sender whose id contains `#` (`templates/senderCard.ts:173-185`, `:292-312`) (source) | MATCH |

#### A5.5 — Date accordions

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | expanded: created without `collapsed`, chevron `▼` (`notifications.js:19236-19246`) (source) | expanded: `data-collapsed="false"`, chevron `▼` (`templates/dateAccordion.ts:53-66`) (source) | MATCH |
| B2 | Toggle trigger | a click on `.date-accordion-header` → `toggleDateAccordion`; the `×` stops propagation (`notifications.js:19237-19240`, `:19295-19310`) (source) | a delegated header click, with the `×` checked first so it never collapses (`NotificationsListRenderer.ts:763-779`, `:812-820`) (source) | MATCH |
| B3 | Persistence | **none** — `toggleDateAccordion` writes no storage (`notifications.js:19295-19310`) (source) | persisted as `date::<sender_id>::<dateKey>` in `ViewStateStore` (`NotificationsListRenderer.ts:818-819`, `:841-847`) | EXTRA |
| Q1 | Ordering | dates descending, newest first; messages newest-first inside a day (`notifications.js:19248-19261`, `:19750-19751`) (source) | same both ways (`templates/senderCard.ts:366-376`) (source) | MATCH |
| Q2 | Per-day count | `(N)` = `dateGroup.length`, every row of that day, refreshed on arrival and after a delete (`notifications.js:19604-19619`) (source) | `(N)` from the full per-sender list, not the progress-collapsed render list (`templates/senderCard.ts:249-262`, `templates/dateAccordion.ts:62`) (source) | MATCH |
| Q3 | `×` hide-this-day | confirm "Hide all notifications from {date}?", `DELETE /api/notifications/date/{sender}/{email}/{date}`, then drop the accordion, update the header and remove the card if it was the last day (`notifications.js:19626-19670`) (source) | the same confirm string and endpoint, dropping that day's rows only on success (`NotificationsListRenderer.ts:1108-1130`) (source) | MATCH |
| Q4 | Progress groups | one in-place entry per `progress_group_id`, with a `▲/▼ #count` toggle revealing its history (`notifications.js:19263-19278`, `:19348-19356`) (source) | one elected head per group, lazy-filled history on first expand (`templates/senderCard.ts:230-243`, `NotificationsListRenderer.ts:747-761`, `:887-949`) (source) | MATCH |

**A5 build items**: (1) the `💬` toolbar button must hide the notifications HEADER as well as the pane (B4); (2) auto-expand the sender card and date accordion, and scroll the row into view, when TTS playback starts (B6); (3) the filter badge must reveal and scroll to Filter Settings (N1, arrives with A11); (4) the Clear All confirm must name the count and the active window label (N5); (5) Recent Activity's open/closed state must persist (A5.3 B3); (6) the sender-card new-count must read "N new" (A5.4 Q1); (7) the conversation-mode button needs the solo icon/tooltip pair (📞/🔔) as well as the chorus pair (A5.4 Q2).

### A4 — TTS Queue / Playing `✓`

Legacy: `notifications.html:434-476` (markup), `notifications.js:209` (storage key), `:308-313`, `:353-363` (state), `:15354-15357` + `:15419-15420` (toolbar hooks), `:22110-22135` (enqueue), `:22459-22478` (`clearTTSQueue`), `:22489-22498` (clear-all button state), `:22505-22582` (`updateTTSQueueSection`), `:22589+` (`onTTSPlaybackComplete`), `:22706-22762` (focus mode), `:22797-22880` (pause/resume), `:22904-22990` (save/restore).
Multiplexer: `multiplexer.html:145-146` (mount), `render/TtsChromeRenderer.ts`, `render/templates/ttsChrome.ts`, `ttsActiveCard.ts`, `ttsMinimizedCard.ts`, `templates/sectionHeader.ts`, `stores/TtsQueueStore.ts`, `stores/AudioStore.ts`, `templates/sectionToolbar.ts:53`.

> 🔴 **A second correction to the Phase-1 "every legacy section is visible" reading — and this one is not the operator's saved state.** `#tts-queue-section` carries **`style="display: none;"` in the markup** (`html:435`), and the only thing that clears it is `updateTTSQueueSection()`. Every one of that method's 15 call sites is event-driven — a toolbar toggle (`:15356`), a visibility **restore** that early-returns when nothing is saved (`:15404-15407`, `:15420`), a TTS **restore** that early-returns when nothing is saved (`:22936-22939`, `:22960`), or a queue mutation (`:22123`, `:22159`, `:22190`, `:22227`, `:22442`, `:22476`, `:22651`, `:22659`, `:22723`, `:22759`, `:22819`, `:22857`). Search: `grep -n 'updateTTSQueueSection()' notifications.js` — all 15 listed. **So on a genuinely fresh browser this section is invisible until the first TTS item arrives**, while its toolbar button already reads `active` (`html:46`). The A0 finding stands for every *other* section; this one is the exception, and the mismatch between an `active` button and a hidden section is legacy's own.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **section hidden** on a cold load (`style="display:none"`, `html:435`, and no init call path — see the box above); the **content** inside is open (`#tts-queue-content` has no `collapsed`, `html:464`). Once `updateTTSQueueSection()` has run, the section is shown iff *items exist OR the toolbar button is `active`* (`js:22516-22531`) | **section visible** on a cold load — `<section id="tts-pane">` carries no `hidden` attribute (`multiplexer.html:145-146`) and `tts-pane` is not in `DEFAULT_HIDDEN_SECTION_IDS` (`templates/sectionToolbar.ts:80`). It paints `🔇 Nothing in the queue` immediately. Visibility never tracks queue contents | **OWED** — María ruled 2026-09-15 ~18:37: the lead wins, so the multiplexer must **hide the pane while the queue is empty and reveal it when an item arrives** |
| B2 | Toggle trigger | click on `.section-header.tts-queue-header` → `toggleSection('tts-queue-content')` (`html:436`). The three nested control clusters each call `event.stopPropagation()` so they do not collapse the section (`html:441`, `:446`, `:458`). Chevron is a `<button>` (`html:462`) | click anywhere on the header except a real control, plus the chevron carve-out → flips `data-collapsed` on the section root (`TtsChromeRenderer.ts:159`, `sectionHeader.ts:176-190`) | MATCH — same outcome, two mechanisms (legacy hand-writes `stopPropagation` per control; the mux uses one `headerClickShouldCollapse` predicate) |
| B3 | Persistence (disclosure) | **none**: `tts-queue-content` is not in `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) | **none**: session-only `data-collapsed` (`TtsChromeRenderer.ts:159`) | MATCH |
| B3b | Persistence (**contents**) | the queue survives a reload: `saveTTSQueueState()` → `localStorage['notifications_tts_queue']` (`js:209`, `:22904+`) carrying `queue`, `focusModeActive`, `focusModeNotificationId`, `focusModeEnteredAt`, `focusModeTimeoutMs`. Restore re-renders every minimized card, **always resets `isTTSPaused` to false** because a refresh kills the audio context, and **auto-exits a stale focus mode** whose triggering notification is gone (`:22933-22975`) | **none**: `grep -c localStorage stores/TtsQueueStore.ts` → **0**, and the same for `AudioStore.ts`. A reload empties the queue | **OWED** |
| B4 | Toolbar show/hide | `🔊` button, `data-section="tts-queue-section"`, title "TTS Queue" (`html:46`). Toggling it **also re-runs `updateTTSQueueSection()`** so the empty state appears/disappears correctly (`js:15354-15357`) — the A0 B4b row, now closed | `🔊` button, `data-section="tts-pane"`, title "**TTS Audio**" (`templates/sectionToolbar.ts:53`); the toggle flips `.section-hidden` + `hidden` and persists, with no TTS-specific follow-up (`SectionToolbarRenderer.ts:127-172`) | MATCH on the toggle. The follow-up call is **moot**, because the mux's visibility does not depend on queue contents. **Note:** the button titles differ ("TTS Queue" vs "TTS Audio") |
| B5 | Collapse/expand-all | not affected (toolbar `⊟`/`⊞` act on Task List owner groups, `js:14312-14337`) | not affected (`requestBulkAccordionCollapse` has one consumer, `NotificationsListRenderer.ts:358`) | MATCH |
| B6 | Auto-reveal | **yes** — any queue mutation calls `updateTTSQueueSection()`, which sets `display:block` as soon as an item exists, regardless of the toolbar (`js:22516-22531`). No scroll | n/a — always visible, so nothing to reveal. No scroll either | covered by B1 |
| B7 | Live header | **one** `<h3>`, rewritten in place by three states: `🔊 Playing: <n>` (n = queue + active), `Paused: <n>` on manual pause (n = queue + active), `Paused: <n> waiting` in focus mode (n = queue only) (`js:22546-22574`). The section also gains a `.paused` or `.focus-mode` class | **two** headers. (a) the section-header bar is the static "🔊 Playing" plus a count chip set to **active + pending** (`TtsChromeRenderer.ts:148-151`, `:232-234`, `:298`); (b) inside the body, `.tts-playing-header` runs the same three-state machine — `🔊 Playing: N` / `Paused: N` / `Paused: N waiting` — but **N is `pendingCount` in all three**, not the total (`ttsChrome.ts:180-193`, `TtsChromeRenderer.ts:253`) | **OWED** — the body header undercounts by one whenever an item is playing, and the static bar title never switches to "Paused" |
| B8 | Refresh control | none | none (`renderSectionHeader` called with no `actions`) | MATCH |
| B9 | Nested accordions | none — an active slot (`#tts-active-slot`) plus a pending queue of minimized cards (`html:466-469`) | none — the same two-part body rendered as `renderTtsActiveCard` + 1-indexed `renderTtsMinimizedCard`s (`TtsChromeRenderer.ts:275-287`) | MATCH |
| B10 | Empty state | `#tts-queue-empty` reads `🔇 Nothing in the queue`, shown only while the section is visible and nothing is active or queued (`html:472-474`, `js:22542-22544`) | the same string in `renderTtsEmpty()`, `.tts-queue-empty-state` inside `.tts-chrome-empty` (`ttsChrome.ts:123-129`). Queue-driven, not audio-idle-driven, and **focus-aware**: a focused queue is never "empty" even at zero pending (`TtsChromeRenderer.ts:239-250`) | MATCH |
| B11 | Layout mode | not referenced by the horizontal-layout code (`grep -n 'tts-queue' notifications.js` shows no `_layoutMode` site) | not referenced (`grep -rn 'tts-pane' render/ReadingPaneRenderer.ts` → 0 hits) | MATCH |
| **Controls** | | | |
| T1 | Pause / Play | two buttons in the header, `⏸️ #tts-pause-btn` and `▶️ #tts-play-btn`, each starting `.disabled`, enabled/disabled by `updateTTSPausePlayButtons()` (`html:446-453`, `js:22797-22880`). A manual pause freezes the queue **and blocks advance** on playback-complete (`js:22590-22594`) | **one** toggle button, `.tts-btn-toggle`, whose label and action derive from the six-value `AudioPlaybackState` — enabled only in `playing`/`paused`, showing "—" and disabled in idle/decoding/ended/error (`ttsChrome.ts:96-105`, `:203`) | **OWED** — two dedicated buttons vs one derived toggle is a different control surface; also check the "manual pause blocks advance" rule, which has no counterpart in `TtsQueueStore.advance()` (`grep -n 'paused' stores/TtsQueueStore.ts` → only focus-mode guards) |
| T2 | Stop / Skip | **neither exists** in legacy — `grep -n 'tts-btn-stop\|tts-skip\|skipTTS' notifications.js` → 0 hits | `Stop` (`audio.stop()`) and `Skip` (`audio.skip()`) buttons (`ttsChrome.ts:204-205`, `TtsChromeRenderer.ts:262-264`) | **EXTRA** — keep |
| T3 | Clear all | `#tts-clear-all-btn` labelled `Clear`, **hidden and disabled when nothing exists**, shown and enabled otherwise — counting a "ghost" `.tts-active-card` left in the DOM as an item (`html:455-460`, `js:22459-22498`). Clearing empties both halves and **saves** | `.tts-btn-clear-all` labelled `Clear all`, "disabled + hidden when N === 0" (`ttsChrome.ts:20`), calling `ttsQueue.clear()` | MATCH in behaviour. **Notes:** the label differs (`Clear` vs `Clear all`), and legacy's ghost-card term has no counterpart — the mux clears from store state only |
| T4 | Focus-mode Resume | `#tts-resume-btn` ("▶️ Resume"), shown **only** in focus mode, calling `toggleTTSFocusMode()` (`html:440-443`, `js:22777`) | `.tts-btn-resume` ("Resume"), rendered only in focus mode, calling `ttsQueue.resumeFocus()` — explicitly distinct from the transport toggle's resume (`ttsChrome.ts:191-194`, `TtsChromeRenderer.ts:266-268`) | MATCH |
| T5 | Focus mode itself | entered when an action-required item's audio completes, holding the queue until the response lands; carries a triggering notification id, an entered-at stamp and a timeout; persisted, and auto-exited on restore if the triggering notification is gone (`js:22706-22762`, `:22943-22975`) | present and live: `focusModeActive` + `focusModeNotificationId`, exit driven by `store_action_required_changed` matching that id, with `advance()` a hard no-op while focused (`TtsQueueStore.ts:113-125`, `:168-172`). Cites the legacy line numbers | MATCH for the mechanism. **OWED** for the stale-focus auto-exit, which depends on the restore path the mux does not have (rides B3b) |
| T6 | Queue order | strict FIFO — every item pushes to the back regardless of type; the priority-displacement of action-required items was deliberately removed (`js:22110-22119`) | FIFO: `queue` is the pending tail, `active` the head (`TtsQueueStore.ts:11-16`) | MATCH |
| T7 | Enqueue → play | if nothing is active and not focused → play at once; if focused → render minimized, do not auto-play; else → render minimized (`js:22125-22135`) | the head plays; focus holds the roll (`TtsQueueStore.ts:168-172`) | MATCH |
| T8 | Per-card controls | minimized cards render from `renderMinimizedTTSCard` (`js:22324+`), and `reorderTTSPendingQueue` re-orders the DOM children to match the array (`js:22399-22410`) | active card takes `onStop` + `onDelete`; minimized cards take `onDelete` (`TtsChromeRenderer.ts:277-287`). Order comes from `pending()` on every full rebuild, so no re-order pass is needed | **note** — per-card delete needs a legacy counterpart check before it is called EXTRA; `grep -n 'tts-card-delete\|removeTTSItem' notifications.js` returns nothing, so as read it is **EXTRA** |
| T9 | Queue-length indicator | the count lives in the `<h3>` only | an additional `.tts-queue-length` line reading `Queued: N` (`ttsChrome.ts:208`) | **EXTRA** |
| T10 | Cross-section coupling | an action-required arrival **defers** while TTS is playing (A3 R3), and TTS enters focus mode for an action-required response (T5). The coupling runs both ways | only the TTS→AR half exists (T5); the AR→TTS half does not (A3 R3) | **OWED** — same row as A3 R3, recorded on both sides |

**Note on the count (B7).** Legacy's total and the mux's pending differ by exactly the active item. The mux already computes the total for its section chip (`TtsChromeRenderer.ts:232-234`), so the body header is reading the wrong one of two values it already has.

**Note on B1 and the exact predicate (read before building it).** María's ruling is "hide while empty, reveal on arrival". Legacy's runtime predicate is one term wider — `showSection = hasActiveOrQueued || toolbarWantsVisible` (`js:22521`) — so an *empty* queue whose toolbar button is `active` still shows the section with its `🔇 Nothing in the queue` panel. The cold-load invisibility comes from that predicate never being evaluated, not from the predicate itself. Build the predicate, and the ruling follows from it on a cold load. This is also the other half of **A0 B4b**: a toolbar toggle of this section re-runs `updateTTSQueueSection()` (`js:15354-15357`) precisely so the empty panel appears and disappears with the toggle — that row is now closed.

**A4 build items**: (1) match the visibility rule — visible iff an item exists or the toolbar button is active, evaluated on load, on every queue mutation and on a toolbar toggle (María's ruling; closes A0 B4b); (2) reload persistence for the queue and the focus-mode fields, with the paused-state reset and the stale-focus auto-exit; (3) the body header must count active + pending, and the section-bar title must follow the Paused / focus states; (4) separate Pause and Play buttons, and a manual pause that blocks queue advance; (5) the stale-focus auto-exit (rides 2); (6) defer an action-required activation while TTS is playing (shared with A3 R3). Cosmetic, not owed but worth a build decision: the toolbar title ("TTS Queue" vs "TTS Audio") and the clear-button label.

### A6 — Fleet Status `✓`

Legacy: `notifications.html:687-757` (markup, incl. the fleet-size-cap dial at `:736-752`), `notifications.js:422-424` (poll constants), `:606` (poll start), `:8698-8729` (fetch), `:8731-8888` (model + offline toggle), `:8899-9088` (table + rows), `:9090-9157` (render dispatch), `:9158-9196` (timestamp + stamp), `:9205-9395` (fleet-size-cap dial), `:9396-9455` (refresh + 60 s poll).
Multiplexer: `multiplexer.html:214-215` (mount), `render/FleetStatusRenderer.ts`, `render/fleetModel.ts`, `render/templates/fleetStatusTable.ts`, `stores/FleetStatusStore.ts`, `render/templates/sectionHeader.ts`, `boot.ts:650-657`.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#fleet-status-section` carries no `collapsed` class (`html:699`) (source) | open: the pane has no `data-collapsed` attribute until clicked (`multiplexer.html:214-215`, `FleetStatusRenderer.ts:123-131`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('fleet-status-section')` (`html:688`), flipping `collapsed` + the chevron; the `⟳` stops propagation (`html:695`) (source) | `wireSectionCollapse` on the header bar, flipping `data-collapsed` on the pane; clicks on `button/a/input/select` are exempt, with an explicit exception for `.toggle-button` (`sectionHeader.ts:181-192`, `:26-30`) (source) | MATCH |
| B3 | Persistence | **none** — `fleet-status-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **none** — `wireSectionCollapse` writes only the DOM attribute (`sectionHeader.ts:185-189`); no `ViewStateStore` call in this renderer (search: `grep -n "viewState\|setAccordion" render/FleetStatusRenderer.ts` → no hits) (source) | MATCH |
| B4 | Toolbar show/hide | `🛰️` (`html:48`) hides `#section-fleet-status` — header and body together (`notifications.js:15299-15346`) (source) | `🛰️` targets `fleet-status-pane` (`templates/sectionToolbar.ts:54`), which is the renderer's whole subtree — header included (`FleetStatusRenderer.ts:127-131`) (source) | MATCH |
| B5 | Collapse/expand-all | not affected — the toolbar `⊟`/`⊞` acts on Task List owner groups (`notifications.js:14312-14337`) (source) | not affected — bulk collapse is emitted to the notifications accordions only (`NotificationsListRenderer.ts:357-363`) (source) | MATCH |
| B6 | Auto-reveal | none found: `grep -n "fleet-status-section" notifications.js` returns **zero** hits — the id appears only in the markup (`html:688`, `:699`), so no JS path expands or un-hides it. Instrument check: the same grep for `fleet-status-container` in that file returns 2 (source) | none found: `grep -rn "fleet-status-pane" multiplexer --include=*.ts` (non-test) returns only the boot mount (`boot.ts:654-655`) and the toolbar spec (`templates/sectionToolbar.ts:54`) (source) | MATCH |
| B7 | Live header | `🛰️ Fleet Status: N` where N is the count of VISIBLE rows (live-only unless the offline toggle is on), plus an `updated HH:MM:SS TZ` stamp in the configured zone, re-stamped on every real fetch and NOT on a view toggle (`html:689-692`, `notifications.js:9136`, `:9155-9156`, `:9185-9196`) (source) | identical: the count chip is set from `visible.length`, the stamp from `formatFleetTimestamp` with `composite.app_timezone`, and the toggle path emits `stampUpdated=false` (`FleetStatusRenderer.ts:189`, `:223-227`, `FleetStatusStore.ts:96-98`) (source) | MATCH |
| B8 | Refresh control | `⟳` → `refreshFleetStatus()`: an in-flight guard debounces a click landing on the 60 s tick; it fetches `/api/arbiter/fleet-state`, re-renders, and **also** refreshes the fleet-size-cap dial. Disclosure is untouched (`html:693-696`, `notifications.js:9396-9421`) (source) | `⟳` → `store.refresh()` with the same in-flight guard and the same endpoint, disclosure untouched (`FleetStatusRenderer.ts:104-108`, `FleetStatusStore.ts:84-93`). No cap refresh — there is no dial (see Q3) (source) | MATCH for the table; the missing cap refresh rides Q3 |
| Q1 | Auto-poll | 60 s interval, one immediate refresh first, idempotent start/stop (`notifications.js:422`, `:9423-9455`), started at init (`:606`) (source) | 60 s (`FLEET_STATUS_POLL_INTERVAL_MS = 60000`), one immediate refresh then the interval, started after mount (`FleetStatusStore.ts:26`, `:100-104`, `boot.ts:657`) (source) | MATCH |
| Q2 | Render states | four: `auth_required` → "🔒 Sign-in required."; unreachable or null arbiter → "🛰️ Arbiter offline — last known: none."; zero sessions → "No active sessions."; all-offline-and-hidden → "No live sessions."; otherwise the grouped table. Count forced to 0 in the first two (`notifications.js:9110-9153`) (source) | the same four states with the same copy and the same count-zeroing (`FleetStatusRenderer.ts:170-205`) (source) | MATCH |
| Q3 | Fleet-size-cap dial | a hidden-until-painted cluster inside the section body: a `range` with NO `max` in markup (the ceiling comes from `GET /api/arbiter/fleet-size-cap` on every paint), a value readout, a status span. `input` moves the readout only; `change` writes `PUT /api/arbiter/fleet-size-cap`, disables the slider while saving, then repaints from the server's answer (snapping back on a refusal). Handlers bind exactly once (`html:736-752`, `notifications.js:9205-9395`) (source) | **absent** — `grep -rn "fleet-size-cap\|fleetSizeCap" multiplexer --include=*.ts` (excluding `*.test.ts`) returns nothing, and the renderer's container holds only the toggle plus the table or a message (`FleetStatusRenderer.ts:194-208`) (source) | **OWED** — the whole control, including hidden-until-real-payload, the server-supplied ceiling, write-on-`change`-not-`input`, and the repaint-from-the-server's-answer |
| Q4 | Offline toggle | `Show offline (N)` / `Hide offline (N)` above the table, rendered only when N > 0; flips a view flag and re-renders the CACHED composite with no re-fetch and no re-stamp (`notifications.js:8860-8897`) (source) | same label pair and same N>0 condition, flipping the store flag and re-emitting with `stampUpdated=false` (`templates/fleetStatusTable.ts:55-66`, `FleetStatusRenderer.ts:195-199`, `FleetStatusStore.ts:96-98`) (source) | MATCH |
| Q5 | Table shape | eight columns — Who · Role · State · Holding on · Stuck · Liveness · % Window · Window — grouped by manager, workers indented, the Unmanaged group last, with verdict and percent classes driving colour (`notifications.js:8899-9088`) (source) | the same eight column classes and labels in the same order, the same grouping model, built with `createElement`/`textContent` instead of `innerHTML` (`templates/fleetStatusTable.ts:165-179`, `:186-196`, `fleetModel.ts` `groupFleetByManager`) (source) | MATCH |
| Q6 | Read-only discipline | pure observability except the cap dial: no mutating row controls (`html:682-686` design note; no `POST`/`PUT` in `renderFleetStatusTable`/`_renderFleetRow`) (source) | same: the row template writes no controls (`templates/fleetStatusTable.ts:72-143`) (source) | MATCH |
| B9 | Nested accordions | none — the body is a flat table plus the toggle (`html:699-756`) (source) | none (`FleetStatusRenderer.ts:194-208`) (source) | MATCH |
| B10 | Empty state | "No active sessions." (zero rows) and "No live sessions." (all offline, hidden) — see Q2 (source) | identical copy (`FleetStatusRenderer.ts:201-205`) (source) | MATCH |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "fleet" notifications.js | grep -i "layout\|left-column\|pane-split"` → no hits) (source) | not referenced (search: `grep -rn "fleet" render/ReadingPaneRenderer.ts` → no hits) (source) | MATCH |

**A6 build items**: (1) the fleet-size-cap dial — hidden until a real payload, ceiling and value painted from `GET /api/arbiter/fleet-size-cap`, readout on `input`, `PUT` on `change` with the slider disabled while saving and repainted from the server's answer, refreshed on the same `⟳`/60 s tick as the table (Q3, and the cap half of B8).

### A8 — Task List `✓`

Legacy: `notifications.html:859-940` (markup), `notifications.js:43-64` (`ROW_SCHEMA`), `:425-462` (keys and guards), `:9463-9691` (fetch, lookup, describe, run), `:9770-10060` (open filter, ranks, grouping), `:10132-10175` (header row, `_rowWidth`), `:10291-10576` (row render, disclosure, actions cell), `:10578-10740` (priority cell, patch), `:10802-11046` (transition, request chips), `:11116-11208` (park predicate, count text), `:11894-12140` (count, group keys, table, dispatch, pinned row), `:12142-12243` (clear, New Ticket, POST, created), `:12245-12406` (truncation banner, unreachable), `:12408-12480` (refresh, after-write), `:12831-12905` (polling, collapsed-owner load/save), `:13216-13230` (press release), `:13231-13435` (operator-state capture/restore), `:13464-13745` (verb selects, submit), `:14185-14337` (body overlay, accordion wiring, collapse/expand all), `:14945-15030` (id click-to-copy). Multiplexer: `render/TaskListRenderer.ts`, `render/taskListModel.ts`, `render/rowSchema.ts`, `render/taskVerbs.ts`, `render/taskListCollapse.ts`, `render/taskLookupBox.ts`, `render/newTicketCard.ts`, `render/requestChips.ts`, `render/templates/taskListTable.ts`, `templates/taskRowControls.ts`, `templates/taskRowDisclosed.ts`, `stores/TaskListStore.ts`, `boot.ts:673-698`.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **open**: `#task-list-section` carries no `collapsed` class (`html:873`) (source) | **open**: `data-collapsed` is set only by a click (`TaskListRenderer.ts:329`, `sectionHeader.ts:157-164`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('task-list-section')`; ⟳ carries `event.stopPropagation()` (`html:860`, `:869`) (source) | delegated header click via `wireSectionCollapse`; every header control is a `<button>`, so the control guard exempts it (`sectionHeader.ts:145-192`) (source) | MATCH |
| B3 | Persistence of the SECTION's open/closed state | **not persisted**: `task-list-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (which holds only `broadcast-submit-section`, `commons-recent-activity-body`, `finished-tasks-section`) (`html:1517-1532`) (source) | **not persisted**: session-only by the header template's own contract (`sectionHeader.ts:12-15`) (source) | MATCH (both forget) |
| B4 | Toolbar show/hide | `🗒️` → `section-task-list`, persisted to `lupin_section_visibility` (`html:49`, `js:15299-15373`) (source) | `🗒️` → `task-list-pane`, persisted to `view_state_section_visibility` (`sectionToolbar.ts:59`, `ViewStateStore.ts:31`) (source) | MATCH. **Note:** different storage keys, so a choice made in one client does not carry to the other |
| B5 | Collapse/expand-all | the toolbar `⊟`/`⊞` (`html:68-69`) collapse/expand every **owner group** and persist the set (`js:14312-14337`) (source) | the same two buttons live in the card header and call the same model (`TaskListRenderer.ts:220-234`, `taskListCollapse.ts:18-21`); the mux TOOLBAR's `⊟`/`⊞` act on Notifications instead (`SectionToolbarRenderer.ts:115-122`) (source) | MATCH in behaviour and storage key. Placement is Phase 1 T4; the Notifications fan-out is A0's EXTRA |
| B6 | Auto-reveal | none found (search: the only writers of this section's `collapsed` are `toggleSection` and `applyPersistedAccordions`) (source) | none found (`setSectionCollapsed` is called only from `wireSectionCollapse`) (source) | MATCH |
| B7 | Live header | the count is **"Live: L"**, widening to **"Live: L · Parked: P · Total: L+P"** when any row is park-ACTIVE — and it can move with no row changing, because a park expiring shifts a row from parked to live on the next poll (`js:11177-11208`, `:11894-11918`, `:12083`). Plus the demote-request badge (`html:861-863`) and `#task-list-updated` as a bare clock time (`js:_stampTaskListUpdated`) (source) | the count is a **plain integer** — `setCount( openTasks.length )` (`TaskListRenderer.ts:394`, `:486-488`). The park predicate exists in the model (`taskListModel.ts:488-522`) but no caller renders the split. The badge is present (`requestChips.ts` via `wireRequestPane`); the stamp reads `updated HH:MM:SS` (`TaskListRenderer.ts:490-494`) (source) | **OWED** — the live/parked/total split. The badge and stamp MATCH (the stamp's "updated " prefix is the same cosmetic note as A7 B7) |
| B8 | Refresh control | `⟳` → `refreshTaskList()`: in-flight guard, one fetch, renders the task list **and** the epic board off the same composite (`html:867-870`, `js:12408-12456`). Disclosure untouched | `⟳` → `store.refresh()`: same guard, one fetch, emits `store_task_list_changed`, which `EpicBoardRenderer` also consumes — so it likewise repaints both (`TaskListRenderer.ts:214`, `TaskListStore.ts:243-247`, `boot.ts:719-741`). Disclosure untouched | MATCH |
| B9 | Nested accordions (owner groups) | one `<tbody class="task-group" data-owner>` per owner, Unassigned last; header bar = chevron `▾`/`▸` + "owner · N" (or "(Unassigned)"), `role="button" tabindex="0" aria-expanded`, `aria-controls` the slug; **expanded on first load**; collapsed set persisted to `localStorage["lupin.taskList.collapsedOwners"]` as an ARRAY of COLLAPSED keys with the `__unassigned__` sentinel (`js:11920-12017`, `:12876-12905`) (source) | identical on every clause — same key, same sentinel, same slug rule, same chevrons, same aria, same expanded-first-load default, declared as the parity contract (`taskListCollapse.ts:1-60`, `templates/taskListTable.ts:40-110`) (source) | MATCH |
| B10 | Empty / degraded states | **five**: `auth_required` → "🔒 Sign-in required." (count "Live: 0"); **`query_unavailable`** → "🧩 Task-list query did not load … This is a deploy problem, not a store outage."; `unreachable` → "⚠️ Store unreachable." **plus the last-known rows** (never blank); empty → "✅ No open tasks."; else the table (`js:12050-12100`, `:12367-12406`) (source) | **four**: auth_required, unreachable (with last-known rows, or "No tasks loaded yet." when there is no cache), empty, table. No `query_unavailable` — the shared query is a static `import` (`TaskListStore.ts:19`), and the store maps 401 → `auth_required`, everything else → `unreachable` (`:327-329`). Search: `grep -rn query_unavailable multiplexer --include=*.ts \| grep -v test` hits only `HoldingAreaRenderer.ts` and `EpicBoardRenderer.ts` (source) | **OWED** — the deploy-defect state is missing from this pane, and the two sibling panes already have it. **Note:** a static import fails at boot rather than at poll time, so the mux's remedy may differ; the state itself is still owed (María's call) |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n section-task-list notifications.js` → **no match**; the reading pane carries Action Required and abstracts only) (source) | the pane is a sibling in the main column (`multiplexer.html:244-245`) (source) | MATCH |
| T1 | Find ticket by id | `type="text"` (not `search`, so no browser ✕), Enter or `🔎` runs it, hits **`/api/tasks/<ref>`** (visibility-free, so held rows are findable), and a hit **filters the board to that one row** rendered through the same table renderer; the ✕ is hidden until a filter is applied; the count reads "Live: 1"; the filter survives polling; **seven** outcome states with distinct sentences and `data-state`: found, refused, missing, ambiguous, auth_required, **module_missing**, unreachable (`html:885-923`, `js:9518-9691`, `:12107-12159`) (source) | the same box, the same endpoint, the same pin-through-the-table-renderer, the same ✕-hidden-until-filtered, the same survive-polling re-assert; **six** states — no `module_missing`, because the shared modules are static imports rather than `window` globals (`taskLookupBox.ts:112-160`, `:214-259`, `TaskListRenderer.ts:250-259`, `:427-436`) (source) | MATCH on behaviour; the missing state is the same structural point as B10 — recorded once there rather than counted twice |
| T2 | ＋ New Ticket | opens the card built by `shared/task-create.js` (the SAME module both clients use, so the fields cannot diverge); assignees are the owners on the board as last fetched; POSTs JSON to `/api/tasks` through `authedFetch` so Rick's login token travels; on success the new row is looked up **through the Find box** and pinned; the board refreshes either way; a missing module reports a deploy defect in the result line (`html:911-918`, `js:12161-12243`) (source) | the same shared module, the same POST-through-the-client, the same look-up-and-pin, the same refresh (`newTicketCard.ts`, `TaskListRenderer.ts:262-271`, `:457-462`) (source) | MATCH |
| T3 | Row disclosure `⋯` | an ellipsis right-justified on the title line, `aria-expanded="false"` by default; a click toggles the controls row's `hidden`; **pane-scoped**, because a row rendered in two panes has two controls rows with the same `data-controls-for`; collapsing also clears that row's error stripe (`js:10378-10439`) (source) | the same control, the same pane-scoped toggle through `toggleDisclosure( this.container, … )` (`TaskListRenderer.ts:526-530`, `templates/rowDisclosure.ts`) (source) | MATCH |
| T4 | Row verb select | ONE select, ONE reason field, ONE Submit for every non-terminal row. Seven verbs in the order **park, un-park, drop, demote, won't fix, fixed, approve** — un-park deliberately beside park, "because that is where an operator looks for it". An illegal verb is a DISABLED option whose LABEL carries the reason. A terminal row disables every option plus the select and Submit. Approve only on `not_approved`; Demote on every other non-terminal row; Un-park keyed on the STORED `parked` status so an expired park still offers it (`js:10441-10576`) (source) | the same one-select shape, the same legality rules, the same reason-in-the-label, the same terminal lockout — but the order is **park, drop, demote, wont_fix, fixed, un-park, approve** (`taskVerbs.ts:66`, `:240-249`, `taskRowControls.ts:94-130`) (source) | **OWED** — the verb ORDER conflicts; the lead wins (María's ruling, 2026-09-15). ⚠️ The mux order comes from the **shared module's** order (park, drop, demote, wont_fix, fixed, unpark); the comment at `taskVerbs.ts:54-55` speaks to where `fixed` sits and says nothing about un-park, so nothing there overrides legacy's stated reason for putting Un-park beside Park |
| T5 | Verb-driven reshape | choosing a verb repoints the reason placeholder, disables and clears the reason box for verbs that take none, inserts a date input only for verbs that need one, and **disarms Submit** so an arming from a previous verb cannot swallow the next click (`js:13496-13550`) (source) | the same three moves and the same disarm, anchored on Submit's own parent so the date box lands at either nesting depth (`TaskListRenderer.ts:588-629`) (source) | MATCH |
| T6 | Two-click arming | a terminal verb arms Submit for a second click (`js:13565-13745`, `_disarmSubmit` at `:13551`) (source) | same, via `dataset.armed` + `task-submit-armed` (`TaskListRenderer.ts:703-705`, `:765-772`) (source) | MATCH |
| T7 | Dictate the reason 🎤 | a `.task-reason-stt` mic sits **immediately before** the reason field it fills and resolves its target by SCOPE, never by id (`js:10568-10570`, `_handleReasonSttClick` at `:13156`) (source) | **absent**: search `grep -rn "task-reason-stt\|reason.*stt" multiplexer --include=*.ts \| grep -v test` → no match; `taskRowControls.ts:94-130` builds select, reason input and Submit with no mic (source) | **OWED** |
| T8 | Drop-reason suggestions | one `<datalist id="task-drop-reason-suggestions">` with four canned reasons, shared by every row (`html:933-938`) (source) | **absent**: search `grep -rn "drop-reason-suggestions\|datalist" multiplexer --include=*.ts \| grep -v test` → no match (source) | **OWED** |
| T9 | Priority control | a `.task-priority-select` **paired with a `.task-priority-update` button** that stays disabled until the value differs from `data-original`, and PATCHes only on the click. Six values P0–P5; an unrecognised stored value gets its own selected option rather than being re-labelled (`js:10578-10740`) (source) | the same class name with **no Update button at all — it commits on `change`** (`TaskListRenderer.ts:547-563`, `taskRowControls.ts:147-198`). The divergence is already documented, from the legacy side, at `js:10608-10625` (source) | **OWED** — a conflict, and the lead wins: legacy's select is a staged edit, the mux's is immediate |
| T10 | Owner reassignment | **absent**: search `grep -n "task-owner-select" notifications.js` → no match; the legacy row offers no owner control (source) | a `.task-owner-select` whose roster is the active personas from the fleet store, Sam included (`taskRowControls.ts:198`, `TaskListRenderer.ts:443-445`) (source) | **EXTRA** — keep it |
| T11 | Detail 📄 | a live emoji opens the row's body in an overlay dismissed by Esc; a dimmed (empty-body) emoji is inert; Enter/Space activates a focused one (`js:14185-14254`, `:14284-14293`) (source) | the same live/dimmed split, the same overlay plus its Esc listener, the same keyboard activation (`TaskListRenderer.ts:188`, `:314-326`, `:506-515`) (source) | MATCH |
| T12 | ID click-to-copy | clicking the id cell copies the FULL id; `task-id-copied` / `task-id-copy-failed` flash and clear on a timer; an insecure origin without `navigator.clipboard` reports a failure rather than a dead click (`js:14945-15030`) (source) | the same copy plus flash, ~1.2 s dwell, and the cell is keyboard-operable only when it carries `role="button"` (a real id) (`TaskListRenderer.ts:126-128`, `:314-326`, `:531-538`) (source) | MATCH |
| T13 | Request chips | the demote-request badge beside the count, and Approve/Deny behind each row's pending chip, hydrated after every paint (`html:861-863`, `js:10883-11114`, `:12102`) (source) | the same badge and verdict door, through `wireRequestPane` with `BADGE_TASK_AREA`, hydrated after every paint (`TaskListRenderer.ts:292-299`, `:439-441`, `requestChips.ts`) (source) | MATCH |
| T14 | Truncation warning | a banner whenever the server held rows back: `has_more`, or `count < total`, or a page that came back **exactly** `limit` long with no total (reported as completeness UNKNOWN, not "truncated"); the `limit` is read off the shared query at call time; server `warnings[]` ride out VERBATIM on their own line (`js:12245-12406`) (source) | **absent**: search `grep -rn "has_more\|truncated\|Board truncated" multiplexer --include=*.ts \| grep -v test` → no match (`taskListModel.ts`'s "truncation" hits are the TITLE-length cap, a different thing) (source) | **OWED** — the board can silently under-report again |
| T15 | Press-hold guard | `mousedown` holds the repaint and `mouseup`/`mouseleave`/`blur` replays it, because a repaint landing between a press and its release replaces the pressed node and the click then reaches no handler at all (`js:14274-14283`, `:13216-13230`, `:12085-12090`) (source) | **absent**: search `grep -rn "pressInFlight\|mousedown" multiplexer --include=*.ts \| grep -v test` → no match; every paint is an unconditional `replaceChildren` (`TaskListRenderer.ts:396-402`) (source) | **OWED** |
| T16 | Operator-state survival across a repaint | `_captureOperatorState` / `_restoreOperatorState` preserve the operator's in-progress work — the open disclosures, the chosen verb, typed reason and date text, the focused control — across the 60 s repaint (`js:13231-13435`) (source) | **absent**: search `grep -rn "captureOperatorState\|restoreOperatorState\|preserve" multiplexer --include=*.ts \| grep -v test` → no match (source) | **OWED** — a poll lands mid-edit and the typed reason is gone |
| T17 | Refresh-after-write | a verdict landing while the 60 s tick is in flight **awaits** the in-flight read before asking for its own, so a write's confirmation is never served from a pre-write read (`js:12458-12480`) (source) | the mutation handle's `done` drives the repaint and the store's own in-flight guard drops a colliding refresh (`TaskListStore.ts:243-247`, `TaskListRenderer.ts:907-926`); no await-then-refetch sequencing was found (search: `grep -rn "refreshAfterWrite\|refreshSettled" multiplexer --include=*.ts` → no match) (source) | **OWED** — a write can be confirmed against a read that predates it |
| T18 | Column schema | eleven fields plus one control cell, all colspans derived from `ROW_SCHEMA` so none can go stale: line1 `id, title, class, status, priority`; line2 `blocked, chase, accountable, filer, project`; line3 `detail, actions` (`js:43-64`, `:10155-10171`) (source) | **byte-identical** schema, labels and `rowWidth()` (`render/rowSchema.ts:28-70`) (source) | MATCH |
| T19 | Auto-poll | 60 s `setInterval`, one immediate refresh first, idempotent restart (`js:12831-12848`) (source) | 60 s, same shape (`TaskListStore.ts:106`, `:243-247`, `boot.ts:698`) (source) | MATCH |
| T20 | Title length | **no character cap** — the full string is emitted and the cell wraps; the cap was removed deliberately and the comment forbids reintroducing it, because cutting in JS made every "widen the cell" change cosmetic by construction (`js:465-472`) (source) | `TASK_TITLE_TRUNCATE_LEN = 60` and `truncateTaskTitle()` are **defined and never called** — search `grep -rn truncateTaskTitle multiplexer --include=*.ts \| grep -v "\.test\.ts"` returns only the definition at `taskListModel.ts:223`, so no production path truncates a title (source) | MATCH. **Note:** dead code that reads like a live cap — worth deleting, but that is tidy-up, not parity |

**A8 build items**: (1) the live/parked/total count split (B7); (2) the `query_unavailable` deploy-defect state (B10); (3) the legacy verb order with un-park beside park (T4, pending María's check on the later ruling); (4) the reason mic 🎤 (T7); (5) the drop-reason datalist (T8); (6) the staged priority edit with its Update button (T9); (7) the truncation banner including the exactly-full-page case and verbatim server warnings (T14); (8) the press-hold repaint guard (T15); (9) operator-state capture and restore across a repaint (T16); (10) await-the-in-flight-read before a post-write refresh (T17).

### A9 — Holding Area `✓`

Legacy: `notifications.html:942-1077` (markup, including the flow-ratio cluster at `:993-1067`), `notifications.js:9693-9769` (fetch + filer grouping), `:11210-11442` (ratio formatting), `:11443-11562` (ratio fetch + paint), `:11563-11798` (settings fetch/save, manager-pull, reset), `:11799-11893` (control bind + retrying paint), `:12436-12452` (the shared tick), `:13804-13867` (render), `:13869-13921` (one group), `:13923-13966` (the pane's only click listener), `:13968-14042` (row ids, group status, batch-button lockout), `:14043-14166` (the batch loop and the two batch verbs), `:14167-14183` (refresh). Multiplexer: `render/HoldingAreaRenderer.ts`, `render/holdingAreaModel.ts`, `render/holdingAreaBatch.ts`, `render/templates/holdingAreaTable.ts`, `stores/HoldingAreaStore.ts`, `render/templates/sectionToolbar.ts:65`, `boot.ts:700-717`.

The batch machinery is a close, deliberate port — in two places the multiplexer fixed a legacy defect on the way (rows T5 and T6). The gap is the operator control cluster, which the multiplexer does not have at all.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **open**: `#holding-area-section` carries no `collapsed` class (`html:966`) (source) | **open**: `data-collapsed` is set only by a click (`HoldingAreaRenderer.ts:249`, `sectionHeader.ts:157-164`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('holding-area-section')` (`html:954`) (source) | delegated header click, controls exempted (`sectionHeader.ts:145-192`) (source) | MATCH |
| B3 | Persistence | **not persisted**: `holding-area-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **not persisted**: session-only (`sectionHeader.ts:12-15`) (source) | MATCH |
| B4 | Toolbar show/hide | `🗃️` → `section-holding-area`, added on Rick's 2026-09-02 voice ruling, persisted to `lupin_section_visibility` (`html:51-63`) (source) | `🗃️` → `holding-area-pane`, persisted to `view_state_section_visibility` (`sectionToolbar.ts:60-65`) (source) | MATCH (different keys, as elsewhere) |
| B5 | Collapse/expand-all | not affected — the toolbar pair acts on Task List owner groups (`js:14312-14337`) (source) | not affected — the toolbar pair acts on Notifications (`SectionToolbarRenderer.ts:115-122`) (source) | MATCH |
| B6 | Auto-reveal | none found (search: the only writers of this id's `collapsed` are `toggleSection` and `applyPersistedAccordions`) (source) | none found (source) | MATCH |
| B7 | Live header | `🗂️ Holding Area · <count>` — the count is the number of held rows, or **`—` under any sentinel**; plus the PROMOTE-request badge; plus **the flow-ratio readout `#task-list-flow-ratio`**, which states the created-vs-closed ratio across every project and priority (`html:955-960`, `js:13829-13844`, `:11476-11562`) (source) | `🛑 Holding Area · <count>` with the same count rule including the em dash under a sentinel, plus the promote badge. **No ratio readout** — search `grep -rln "flow-ratio\|flowRatio" multiplexer --include=*.ts \| grep -v test` returns only `templates/finishedTasksTable.ts`, and that is the `flow-ratio-field` CSS class borrowed for the window slider, not the readout (`HoldingAreaRenderer.ts:206-215`, `:546-548`) (source) | **OWED** — the flow-ratio readout. Count and badge MATCH; the header glyph differs (🗂️ vs 🛑) and that is Phase 1's business |
| B8 | Refresh control | `#holding-area-refresh` renders in the header with **no `onclick` and no listener anywhere** — search `grep -rn -F holding-area-refresh static/` hits only the markup at `html:961-963` and the multiplexer's own button; the positive control `task-list-refresh` carries its handler inline on the element at `html:869`. So the legacy ⟳ does nothing except collapse the section, since the header's toggle is what its click reaches (source) | ⟳ → `store.refresh()`, and being a `<button>` the header guard exempts it, so it refreshes without collapsing (`HoldingAreaRenderer.ts:195-200`) (source) | **EXTRA** — the multiplexer's button works. ⚠️ Do not "restore parity" here: legacy's is a dead control, and this is a legacy defect worth its own row |
| B9 | Nested accordions | **none, deliberately**: rows are grouped by FILER into `.holding-area-group` divs with a header bar, and the legacy source says in as many words that this pane has no accordion, so a non-control click is ignored rather than collapsing anything (`js:13869-13921`, `:13941-13943`) (source) | the same filer grouping and the same explicit no-accordion note (`templates/holdingAreaTable.ts:13-15`) (source) | MATCH |
| B10 | Empty / degraded states | **four**: `auth_required`, `query_unavailable` ("a deploy defect, not an outage"), `unreachable` ("last known state not shown" — and it deliberately does **not** replay stale rows the way the task list does), and the genuinely-empty "Nothing waiting on triage." (`js:13830-13861`) (source) | the same four, same strings, carbon-copied — plus a **pre-first-poll null composite takes the unreachable sentinel** rather than claiming the queue is empty, and a status-less composite with a non-array `tasks` is treated as malformed rather than empty (`HoldingAreaRenderer.ts:85-95`, `:288-320`) (source) | MATCH, with two multiplexer refinements that are EXTRA |
| B11 | Layout mode | not referenced by the layout code (search: `grep -n section-holding-area notifications.js` → no match) (source) | a sibling pane in the main column (`multiplexer.html:254-255`) (source) | MATCH |
| H1 | Poll cadence | **no timer of its own**: `refreshHoldingArea()` rides the task list's 60 s tick — "one clock, two fetches", because the held query is a second question the composite cannot answer (`js:12446-12449`) (source) | **its own** 60 s `setInterval`, started after mount, for the same stated reason — a second FETCH rather than a second view (`HoldingAreaStore.ts:130`, `:258-261`, `boot.ts:700-715`) (source) | MATCH in cadence. **Note:** one clock vs two. Two timers can drift apart, which is the failure legacy's shared tick exists to avoid — worth María's eye, but it is not a behaviour the operator can see today |
| H2 | Grouping | by FILER, not owner, because triage asks "what did this person file" and the two fields disagree on roughly a quarter of live rows; groups alphabetical (`html:942-952`, `js:9731-9769`) (source) | the same, in `groupHeldRowsByFiler` (`holdingAreaModel.ts`) (source) | MATCH |
| H3 | Group header | filer label · count · **Approve all** · **Won't fix all** · one batch-reason input · a status span, every control carrying `data-filer` (`js:13903-13920`) (source) | the same six elements with the same two tooltips, carbon-copied verbatim and pinned against the legacy strings (`templates/holdingAreaTable.ts:36-100`) (source) | MATCH |
| H4 | Rows | rendered through the **same row renderer** as the task list, so the ⋯ disclosure, the verbs, the detail 📄 and the id-copy all behave identically (`js:13901`, `:13954-13962`) (source) | the same shared row templates (`templates/holdingAreaTable.ts` → `taskRowControls` / `taskRowDisclosed`) (source) | MATCH — and A8's row-level OWED items (the mic, the datalist, the priority Update button, the verb order) apply here too, since both panes draw the same row |
| H5 | Which rows a batch acts on | read off the **rendered DOM at press time**, keyed on `.task-verb-select[data-task-id]` (a control every row has) and filtered to rows where the `approve` option is **enabled** — never a cached list, which would go stale the moment a peer approved something (`js:13968-14003`) (source) | the same rule, and the same two traps named (`HoldingAreaRenderer.ts:389-416`) (source) | MATCH |
| H6 | Batch approve | no reason, no confirm — the non-destructive direction, since an over-approved row is demoted straight back (`js:14136-14147`) (source) | same (`holdingAreaBatch.ts`, `HoldingAreaRenderer.ts:361-369`) (source) | MATCH |
| H7 | Batch won't-fix | terminal, and refuses **before any request leaves the browser** when the one shared reason is blank; every row in the group gets that same reason, and the tooltip says so (`js:14149-14166`) (source) | same, via `HOLDING_BATCH_BLANK_REASON` (`HoldingAreaRenderer.ts:497-501`) (source) | MATCH |
| H8 | Batch loop | **sequential, never `Promise.all`** — eight parallel 403s against one allowlist race in an unreproducible order. Counts successes and refusals separately, keeps the FIRST server message, repaints the status line **after every row** (the in-flight number counts ATTEMPTS), and both batch buttons are dead for the whole batch (`js:14043-14134`) (source) | the same loop, the same attempt-counting, the same both-buttons lockout — plus the lockout is backed by a `Set` keyed on filer, because `disabled` lives on an element the 60 s poll repaints (`HoldingAreaRenderer.ts:163-172`, `:490-537`) (source) | MATCH, with the set-based guard as EXTRA |
| H9 | Where the final report lands | painted, then `refreshHoldingArea()` is awaited on the **next line** — and that refresh rebuilds every group including the status span, so the partial-failure report the method exists to produce is **erased before it can be read** (`js:14126-14132`) (source) | the refresh is awaited **first** and the final line painted after it, onto the group that survived — the group holding the refused rows; and remembered per filer so a later poll cannot erase it either (`HoldingAreaRenderer.ts:326-341`, `:465-473`, `:533-536`) (source) | **EXTRA** — the multiplexer fixed a legacy defect. Keep it; do not port legacy's ordering back. Worth its own legacy bug row |
| H10 | Read-after-write | the batch calls `refreshHoldingArea()`, whose in-flight guard means it can **join** a fetch that began before the transitions landed (`js:14167-14183`) (source) | a separate `refreshAfterWrite()` that guarantees a read which STARTED later, and it is on the store interface on purpose so a fake cannot blur the two (`HoldingAreaRenderer.ts:100-109`, `:533-535`, `HoldingAreaStore.ts:253`) (source) | **EXTRA** — same class of fix as H9 |
| H11 | Flow-ratio operator cluster | inside the section body (never the header, which would collapse on every drag): **Gate opens below** threshold slider 0–200 % → `PATCH /api/tasks/flow-ratio/settings`; **Window** slider 1–14 days (days on screen, hours on the wire); **Freeze manager pull** checkbox, `checked` in the markup so an unreachable server cannot read as "pull is allowed", writing `PATCH /api/tasks/manager-pull`; **Reset** to configured defaults; a status line naming the SOURCE ("saved override" / "from config"). Writes on `change`, never `input`. The cluster stays HIDDEN until a settings fetch succeeds, and a failed paint is retried on the next tick while the bind happens exactly once (`html:993-1067`, `js:11563-11893`) (source) | **entirely absent** — search `grep -rln "flow-ratio\|flowRatio\|manager-pull\|managerPull" multiplexer --include=*.ts \| grep -v test` returns one file, `templates/finishedTasksTable.ts`, and only for the borrowed `flow-ratio-field` class. Legacy's own comment already records the absence: "No-op when the controls are absent (the multiplexer page has no cluster; this file serves both)" (`js:11828-11829`) (source) | **OWED** — five controls plus their hidden-until-readable rule and their retrying paint |
| H12 | Truncation warning | the same banner the task list uses, fed this pane's own query so its own `limit` is read rather than the board's — measured: a status-filtered call to the same endpoint returned exactly 500 of 1,912 rows, silently (`js:13846-13856`) (source) | **absent** (same search as A8 T14: no `has_more` / `truncated` anywhere in non-test multiplexer source) (source) | **OWED** — and it is the same build item as A8 T14, needing the per-pane query |
| H13 | Operator-state survival | `_captureOperatorState` / `_restoreOperatorState` wrap the repaint here too, so a typed batch reason or an open disclosure survives the 60 s tick (`js:13863-13865`) (source) | **absent** for the row/disclosure state; only the batch REPORT is remembered (`HoldingAreaRenderer.ts:326-339`) (source) | **OWED** — same build item as A8 T16, and the batch-reason box is the sharpest case: it is typed slowly and cleared by the next poll |
| H14 | Request chips | the promote-request badge beside the count and Approve/Deny behind each row's pending chip, hydrated after every paint (`html:955-957`, `js:11107`, `:13866`) (source) | the same through `wireRequestPane` with `BADGE_HOLDING_AREA` (`HoldingAreaRenderer.ts:221-230`, `:344-347`) (source) | MATCH |
| H15 | No search box | **deliberately none** — Rick removed it on 2026-09-11 after a day's use, because the Task List's Find box already reaches held rows through the visibility-free endpoint (`html:1068-1072`) (source) | none, and boot records the same ruling as the reason for passing no `lookupFetch` (`boot.ts:707-708`) (source) | MATCH |

**A9 build items**: (1) the flow-ratio readout in the header (B7); (2) the flow-ratio operator cluster — threshold slider, window slider, Freeze-manager-pull, Reset, source line, hidden-until-readable, retrying paint, writes on `change` only (H11); (3) the truncation banner fed by THIS pane's query (H12, shares A8 T14); (4) operator-state capture and restore across the repaint, the batch-reason box especially (H13, shares A8 T16). Plus A8's shared row items, which this pane inherits.

**Legacy defects found while inventorying, for a separate row — do NOT back-port**: the `#holding-area-refresh` button is unwired (B8); the batch's final report is erased by the refresh on the next line (H9); the post-batch read can join a fetch that predates the writes (H10).

### A11 — Queue Filter Settings `✓`

Legacy: `notifications.html:1112-1139` (markup), `notifications.js:207` (storage key), `:303` (state), `:1816-1845` (wiring), `:6224-6231` + `:6562-6563` (the `user_filter` query param), `:6288-6362` (`setFilterMode`), `:6364-6384` (`showAndScrollToFilterPanel`), `:6386-6430` (`initializeFilterUI`), `:5252-5257` (live-event filtering), `:7506` (owner badge), `:19849`, `:20826`.
Multiplexer: **the section is absent; the function is not.** Search for `filter-settings`, `filter-own-jobs`, `filter-others-jobs`, `filter-all-jobs`, `filter-mode-display`, `Queue Filter Settings` and `My Jobs Only` across non-test multiplexer source → **0 hits**. But the same three-mode switch is rendered **inline in two section headers**: `render/NotificationsHeaderRenderer.ts:101-105` (`FILTER_MODES`), `:193-220` (badge + switch), `:394-410` (sync + click), and `render/JobsPaneRenderer.ts:253-275`, `:434`. The mode itself lives in `stores/NotificationStore.ts:78` (`FILTER_MODE_KEY`), `:348`, `:427-442`, `:959-962`, `:965`, with reload wiring at `stores/coldHistoryHydration.ts:106` and `boot.ts:564`.

> **This is not a missing feature — it is a relocated one.** Phase 1 listed A11 as "absent (S11, ruled: carry)". That is true of the **section**; the build item is therefore about the section and the behaviours the relocation dropped, not about the filter mechanism, which is present and uses **the same storage key and the same three modes**.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **hidden by markup** (`style="display:none"`, `html:1113`) and then shown **only for an admin**: `initializeFilterUI()` sets `display:block` for an admin and `display:none` for everyone else (`js:6404-6428`). The **content** is open (`#filter-section` has no `collapsed`, `html:1118`) | no section at all. The inline switch and badge are `hidden = !isAdmin()` at mount (`NotificationsHeaderRenderer.ts:217-219`; the Jobs copy at `JobsPaneRenderer.ts:253-258`) | **OWED** — the section. The admin gate itself is MATCH |
| B2 | Toggle trigger | click on `.section-header` → `toggleSection('filter-section')` (`html:1114`); chevron is `#filter-toggle`, a `<button>` (`html:1116`) | n/a — no section | **OWED** |
| B3 | Persistence (disclosure) | **none**: `filter-section` is not in `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`). Its **visibility** is persisted, though, through the ordinary toolbar path plus the explicit `saveSectionVisibility()` inside `showAndScrollToFilterPanel` (`js:6380`) | n/a | **OWED** |
| B3b | Persistence (**the mode**) | `localStorage['notifications_filter_preference']`, written on every `setFilterMode` (`js:207`, `:6353`), read at init with a whitelist of `own` / `others` / `all` and a fallback to `own` (`js:6411-6413`) | **the same key**, `FILTER_MODE_KEY = "notifications_filter_preference"` (`NotificationStore.ts:78`), written in `persistFilterMode()` (`:432`, `:965`) and read with the same three-value whitelist and the same `own` fallback (`:959-962`) | **MATCH**, and unusually so: the two clients share this key, so a mode chosen in one **does** carry to the other |
| B4 | Toolbar show/hide | `⚙️` button, `data-section="filter-settings-section"`, title "Filter Settings (Admin)" (`html:70`) | **absent** — `SECTION_TOGGLES` has no entry for it (`templates/sectionToolbar.ts:49-67`) | **OWED** |
| B5 | Collapse/expand-all | not affected | n/a | — |
| B6 | Auto-reveal from a badge | **yes** — clicking either filter badge (`#notifications-filter-badge`, `#queues-filter-badge`) calls `showAndScrollToFilterPanel()`, which removes `section-hidden`, re-lights the toolbar button, **saves the visibility** and smooth-scrolls to the panel; `stopPropagation()` keeps the click from collapsing the host section (`js:1830-1845`, `:6364-6384`) | **no** — the badges are inert `<span>`s with no click listener (`NotificationsHeaderRenderer.ts:198-201`; `JobsPaneRenderer.ts:258-263` sets only a `title`). Nothing to reveal, since the switch sits beside the badge already | **OWED** — this is the A0 B6 row, now specified |
| B7 | Live header | static "⚙️ Queue Filter Settings" (`html:1115`). The **status line** inside is live: `Currently viewing: <strong id="filter-mode-display">` is rewritten to "Your jobs only" / "Other users' jobs" / "All users' jobs" (`html:1134-1136`, `js:6336`) | no header. **No status line anywhere**: `grep -rn "filter-mode-display\|Currently viewing\|Your jobs only" --include=*.ts` (non-test) → 0 hits. The badge shows the short label ("👤 Mine") but never the sentence | **OWED** — the section header and the `Currently viewing:` status line |
| B8 | Refresh control | none | n/a | — |
| B9 | Nested accordions | none | n/a | — |
| B10 | Empty state | none — the three buttons are always present | n/a | — |
| B11 | Layout mode | not referenced (`grep -n 'filter-settings' notifications.js` shows no `_layoutMode` site) | n/a | — |
| **Function** | | | |
| F1 | The three modes | `👤 My Jobs Only` / `🚫 Not My Jobs` / `👥 All Users' Jobs` as three `.filter-button`s, `own` active at start (`html:1124-1132`) | the same three modes, same order, same icons, but the **short** labels `Mine` / `Not Mine` / `All Users` (`FILTER_MODES`, `NotificationsHeaderRenderer.ts:101-105`) | MATCH on the modes. **Note:** legacy's long button labels are the badge's short labels in disguise — legacy already uses `Mine` / `Not Mine` / `All Users` for its badges (`js:6324-6326`), so the mux kept the badge wording and dropped the button wording |
| F2 | Active-state display | `classList.toggle('active', …)` on each button (`js:6332-6335`) | the same, **plus `aria-pressed`** on each button (`NotificationsHeaderRenderer.ts:402-405`) | MATCH, with an accessibility **EXTRA** |
| F3 | Admin gate | non-admins cannot change the mode: `setFilterMode` warns and returns (`js:6315-6318`), and the mode is forced to `own` (`:6426`) | the controls are hidden for non-admins, and `boot.ts:564` / `:378` / `:404` gate on `authManager.isCurrentUserAdmin()`. There is **no guard inside `setFilterMode`** (`NotificationStore.ts:431-442`) — the store will accept any mode it is handed | **OWED** (narrow) — hiding a control is not refusing the call; legacy refuses at the setter |
| F4 | Wire effect | the mode becomes a `user_filter` query param: `all` → `?user_filter=*`, `others` → `?user_filter=!self`, `own` → omitted (`js:6224-6231`, `:6562-6563`) | the same three-way mapping in `stores/jobViewFilter.ts:27` / `JobStore.ts:101`, `:205`, `:269`, and an `exclude_own_jobs` flag for the senders call (`boot.ts:564`) | MATCH |
| F5 | Change → refresh | `setFilterMode` calls `refreshAllQueues()`, then (unless `reloadHistory:false`) `clearSenderGroups()` + `loadConversationHistory()` (`js:6355-6361`) | `setFilterMode` persists and emits `store_notifications_changed{changeKind:"filtered"}`; `coldHistoryHydration.ts:106` reloads on it and the renderers repaint | MATCH |
| F6 | The doubled-load trap | init deliberately passes `reloadHistory:false` because an un-awaited second load doubled every count (row `b670b76c`, `js:6311-6313`, `:6415-6418`) | both mounts deliberately only **read** the mode for the same stated reason, citing the same row (`NotificationsHeaderRenderer.ts:195-197`, `JobsPaneRenderer.ts:256-257`) | MATCH — the trap is carried, not re-fallen-into |
| F7 | Badge locations | **two** badges — Notifications header (`#notifications-filter-badge`) and Queues header (`#queues-filter-badge`) — both written by `setFilterMode` (`js:6338-6350`) | **two** badges with the same two ids and the same `filter-mode-badge` class (`NotificationsHeaderRenderer.ts:199-200`, `JobsPaneRenderer.ts:259-261`) | MATCH |
| F8 | Live-event filtering | an arriving event is dropped client-side when the mode says so: `own` drops another user's, `others` drops the current user's (`js:5252-5257`) | `isJobVisibleTo` in `stores/jobViewFilter.ts`, consumed at `JobsPaneRenderer.ts:45` | MATCH (name-level; the predicate's arms were not read line-by-line — flagged rather than asserted) |
| F9 | Owner badge on a card | in a non-`own` mode, an admin sees the job's `user_email` on the card (`js:7506`) | not searched for on the mux side — it belongs to the Jobs pane (A12), which is **Krishna's**. Recorded here so it is not lost | → A12 |

**Note on the shared key (B3b).** `notifications_filter_preference` is the one storage key both clients genuinely share, so far as this seat has read. The A0 note that "the two clients use different keys" holds for section visibility and accordion state; it does **not** hold here. Worth stating in the build, because a build that "gives the multiplexer its own key" would silently break a behaviour that currently works.

**A11 build items**: (1) the Queue Filter Settings section itself — admin-only, hidden for non-admins, header collapse, unpersisted disclosure; (2) its toolbar button `⚙️`; (3) the `Currently viewing: <mode>` status line and the long button labels; (4) clickable filter badges that reveal, save and scroll to the panel (closes A0 B6); (5) refuse a mode change at the setter for a non-admin, not only by hiding the control. Not owed: the mode, its storage key, the `user_filter` mapping, the refresh wiring and the doubled-load guard are all already there.

### A12 — Cosa Jobs Flow (`#section-queues`) vs the multiplexer jobs pane `✓`

Legacy: `notifications.html:1142-1264` (markup: section header, four live queues, Job History), `notifications.js:309-316` (queue state defaults), `:488` (expand-state key), `:541` + `:15461-15488` (restore on load), `:5723-5752` (count badge + empty message), `:6472-6537` (category toggle + lazy load), `:6622-6690` (history load + pagination), `:6869-6925` (delete-all), `:6971-6998` (window change + load more), `:7808-7816` (job-card expansion), `:15429-15445` (expand-state save).
Multiplexer: `multiplexer.html` `#jobs-pane` / `#jobs-buckets-container`, `render/JobsPaneRenderer.ts`, `render/templates/jobBucket.ts`, `render/templates/jobCard.ts`, `stores/JobStore.ts`, `render/templates/sectionToolbar.ts:51`.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure (the section) | open: `#queues-section` carries no `collapsed` class (`html:1149`) (source) | the pane itself is **hidden** by a cold default (`templates/sectionToolbar.ts:80`, `DEFAULT_HIDDEN_SECTION_IDS`); once shown, its body is expanded (`JobsPaneRenderer.ts:288-291`) (source) | **OWED** — already filed as A0 B1; restated here because this is the section it hides |
| B2 | Toggle trigger (the section) | a click anywhere on `.section-header` → `toggleSection('queues-section')` (`html:1143`) (source) | `wireSectionCollapse` on the header bar, exempting real controls and honouring `.toggle-button` (`JobsPaneRenderer.ts:291`, `templates/sectionHeader.ts:181-192`) (source) | MATCH |
| B3 | Persistence (the section) | **none** — `queues-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **none** — no storage call in the renderer (`grep -n "viewState\|localStorage\|storage" render/JobsPaneRenderer.ts` → one unrelated comment hit about `getSessionId`) (source) | MATCH |
| B4 | Toolbar show/hide | `📋 Job Queues` hides `#section-queues` (`html:71`) (source) | `📝 Jobs` hides `jobs-pane` (`templates/sectionToolbar.ts:51`), which holds header and buckets together (`JobsPaneRenderer.ts:288-290`) (source) | MATCH on the mechanism. **Note:** different glyph and label (📋 "Job Queues" vs 📝 "Jobs") |
| B5 | Collapse/expand-all | the toolbar `⊟`/`⊞` does not reach the queues (`notifications.js:14312-14337`) (source) | does not reach the buckets — the bulk event is consumed by the notifications list only (`NotificationsListRenderer.ts:357-363`) (source) | MATCH |
| B6 | Auto-reveal | a filter-mode badge in this header reveals Filter Settings and scrolls to it (`html:1145-1146`, `notifications.js:1840-1845` → `:6364-6384`) (source) | the badge is a passive `<span>` with no listener (`JobsPaneRenderer.ts:257-263`), and Filter Settings does not exist (S11) | **OWED** (same item as A0 B6 / A11) |
| B7 | Live header | static title "Cosa Jobs Flow: Current States" with no section-level count; each queue carries its own badge (`html:1144`, `notifications.js:5723-5728`) (source) | title "📝 Jobs" plus a count chip = the total across the four live buckets (`JobsPaneRenderer.ts:246-250`, `:559-565`) (source) | EXTRA (the chip). **Note:** the title text differs from the lead's |
| B8 | Refresh control | none in this section: live queues repaint from WebSocket queue updates, history reloads on window change / expand (`notifications.js:6519-6535`, `:6971-6990`) (source) | none either: buckets repaint on `store_jobs_changed`, history refetches on window change (`JobsPaneRenderer.ts:515-556`, `:639-658`) (source) | MATCH |
| B9 | Nested accordions | five queue categories — see B9a–B9d below (source) | five buckets — same set (`JobsPaneRenderer.ts:133`) (source) | see below |
| B9a | Nested default | **all five collapsed**: every `.queue-jobs-container` carries `collapsed` in markup and every `queueCategoryState.*.expanded` starts `false` (`html:1163`, `:1180`, `:1197`, `:1214`, `:1253`; `notifications.js:309-315`) (source) | **todo and running expanded**, done / dead / history collapsed (`templates/jobBucket.ts:41-48`) (source) | **OWED** — a conflict, and the lead wins |
| B9b | Nested toggle | a click on `.queue-header` → `toggleQueueCategory(name)`, flipping `collapsed` + `▶`/`▼`, and lazy-loading that queue's cards on first expand (`html:1153`, `notifications.js:6472-6516`) (source) | the bucket header toggles `.collapsed` + `▶`/`▼` with a full WAI-ARIA keyboard contract (Enter/Space, `aria-expanded`, `aria-controls`); controls inside the header are exempted (`templates/jobBucket.ts:104-119`, `:215+`) (source) | MATCH (the keyboard contract is EXTRA) |
| B9c | Nested persistence | persisted: `localStorage["lupin_queue_expand_state"]`, a map of queue → `expanded`, written on every toggle and replayed at init — the replay re-calls `toggleQueueCategory`, so a restored queue also lazy-loads (`notifications.js:488`, `:541`, `:15429-15445`, `:15461-15488`) (source) | **not persisted, and not even kept across a re-render**: `renderAll` rebuilds all five buckets from the template and `replaceChildren`s them (`JobsPaneRenderer.ts:529-553`), and nothing re-applies a prior collapse state — `grep -n "jobs-bucket-cards\|aria-expanded" render/JobsPaneRenderer.ts` returns **zero** hits, against 12 hits in `templates/jobBucket.ts` as the instrument check (source) | **OWED** — twice over: a bucket the user opened closes again on the next job event, and nothing survives a reload |
| B9d | Nested labels | glyph + name + badge: 🟡 TODO · 🔵 Running · ✅ Done · ❌ Dead · 📋 Job History (`html:1152-1235`) (source) | the raw bucket key, lowercase, no glyph: `todo` · `running` · `done` · `dead` · `history` (`templates/jobBucket.ts:116`) (source) | **OWED** |
| B10 | Empty state | per queue, injected when a container holds no card: "No jobs in queue" (`notifications.js:5735-5751`); history has its own "No job history found" (`notifications.js:6675`) (source) | per bucket: "No todo jobs." / "No running jobs." / … / "No history jobs." (`templates/jobBucket.ts:167-173`) (source) | **OWED** — different copy per pane; the lead's wording is "No jobs in queue" and "No job history found" |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "queues-section\|queue-category" notifications.js | grep -i "layout\|left-column"` → no hits) (source) | not referenced (search: `grep -rn "jobs-pane" render/ReadingPaneRenderer.ts` → no hits) (source) | MATCH |
| Q1 | Count badges | per-queue `.queue-count-badge`, set from `queueCounts[queue]` on every WebSocket queue update; history's badge shows the server `total` for the window (`notifications.js:5723-5728`, `:6671`) (source) | per-bucket `(N)` from the rendered bucket length, with history overridden by the server total when known (`templates/jobBucket.ts:117`, `:160-166`) (source) | MATCH |
| Q2 | Delete-all 🗑 | one per queue header. Confirm names the count and the queue — "Remove all N jobs from the todo queue?", the running variant adds "This will interrupt active jobs", history reads "Delete all N history entries from last 30 days?". Then `DELETE /api/queue/{queue}/all` or `DELETE /api/job-history/all?days=N`, then a refetch (`html:1157-1160`, `notifications.js:6869-6925`) (source) | one per bucket header, same two endpoints and the same running warning, but the confirm reads "Delete all {bucket} jobs (N)?" (and an all-users variant for admins), with a 404 treated as success (`templates/jobBucket.ts:125-138`, `JobsPaneRenderer.ts:660-724`) (source) | **OWED** — the confirm must name the queue in the lead's wording and, for history, the window ("last 30 days" / "all time") |
| Q3 | History time window | five options — 1 day / 7 days / 14 days / 30 days / All — defaulting to **30 days** (`selected` in markup); a change resets the offset and reloads if expanded, else marks the pane unloaded (`html:1236-1245`, `notifications.js:6971-6990`) (source) | the same five values with different labels — "Last 24h / Last 7 days / Last 14 days / Last 30 days / All time" — defaulting to 30 days (`DEFAULT_HISTORY_WINDOW_DAYS = 30`, `stores/JobStore.ts:117`, `:204`; `templates/jobBucket.ts:52-59`) (source) | MATCH on values and default. **Note:** the option labels are reworded |
| Q4 | History pagination | a "Load More" button shown while `jobs.length < total`, appending the next 20 rows at the current offset (`html:1256-1260`, `notifications.js:6650-6686`, `:6993-6998`) (source) | a "Load more" button on the same `loaded < total` gate, appending through the store (`templates/jobBucket.ts:194-208`, `JobsPaneRenderer.ts:598-604`) (source) | MATCH (label case differs) |
| Q5 | History de-duplication | history excludes ids currently live in Done/Dead via `exclude_ids` so a job appears in exactly one place (`notifications.js:6642-6653`, and the markup note at `html:1219-1231`) (source) | not found: `grep -rn "exclude_ids" multiplexer --include=*.ts` (non-test) returns nothing; the history fetch sends only `days`, `limit`, `offset` and the user filter (`stores/JobStore.ts:263-268`) (source) | **OWED** — a job can show in both a live bucket and history |
| Q6 | Lazy vs eager load | a queue's cards are fetched on first expand; history likewise (`notifications.js:6499-6506`) (source) | every bucket renders from the store, and history hydrates eagerly on mount (`JobsPaneRenderer.ts:296-298`) (source) | MATCH on what the user sees; the eager hydration is EXTRA |
| Q7 | Job-card expansion | a card header click expands that card's detail, tracked in an in-memory `expandedJobCards` set so a re-render restores it (`notifications.js:317`, `:7808-7816`, `:7135`, `:7181`) (source) | a card header click toggles `.collapsed` on the details element and lazily renders its meta on first expand (`JobsPaneRenderer.ts:612-637`); no set, and `renderAll` replaces the cards (`:529-553`) (source) | **OWED** — an expanded card collapses on the next job event |
| Q8 | Retry | history cards carry a retry that re-submits the job to todo (`notifications.js:6928+` `retryHistoryJob`) (source) | retry is offered on **dead** cards as well as history (`templates/jobBucket.ts:184-191`, `JobsPaneRenderer.ts:606-612`) (source) | EXTRA |
| Q9 | Admin filter control | the header carries only the badge; the three buttons live in Filter Settings (`html:1145-1146`) (source) | badge **and** the Mine / Not Mine / All Users switch, admin-gated, sharing one mode with the notifications header (`JobsPaneRenderer.ts:252-285`) (source) | EXTRA |

**A12 build items**: (1) the jobs pane must start VISIBLE (A0 B1, restated); (2) the filter badge must reveal and scroll to Filter Settings (B6, arrives with A11); (3) all five buckets start COLLAPSED (B9a); (4) bucket expand/collapse must survive a re-render and a reload, under a saved key as legacy's `lupin_queue_expand_state` does (B9c); (5) bucket headers need the lead's glyph + label set — 🟡 TODO, 🔵 Running, ✅ Done, ❌ Dead, 📋 Job History (B9d); (6) empty-pane copy "No jobs in queue" / "No job history found" (B10); (7) the delete-all confirm must name the queue and, for history, the window (Q2); (8) history must exclude ids that are live in Done/Dead, so a job appears in one place only (Q5); (9) an expanded job card must stay expanded across a re-render (Q7).

### A10 — Epic Board `✓`

Legacy: `notifications.html:1079-1110` (markup, including the header's `⊟`/`⊞` at `:1091-1098`), `notifications.js:447-464` (epic constants), `:12436-12441` (the shared tick), `:14340-14360` (the one-fetch-one-clock rule), `:14362-14448` (stories, epic key, title, story text), `:14449-14472` (waits-on-Rick), `:14474-14552` (grouping and ordering), `:14554-14667` (slug, default, state load/save, expanded predicate, toggle), `:14668-14731` (row, group), `:14733-14796` (table and its three sections), `:14798-14856` (dispatch, count, stamp), `:14871-14913` (in-place collapse, toggle handler), `:15069-15095` (the pane's delegated listeners), `:15097-15140` (keys in DOM, collapse-all, expand-all), `:12934` (`_closeDisclosedRowsIn`). Multiplexer: `render/EpicBoardRenderer.ts`, `render/epicBoardModel.ts`, `render/epicBoardCollapse.ts`, `render/templates/epicBoardTable.ts`, `stores/EpicStoriesStore.ts`, `render/templates/sectionToolbar.ts:66`, `boot.ts:719-752`.

The model, the sections, the strings and the persistence contract are a faithful port. Two gaps: the header's bulk collapse pair, and — the serious one — the pane's row controls are rendered but not wired.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure (the SECTION) | **open**: `#epic-board-section` carries no `collapsed` class (`html:1105`) (source) | **open**: `data-collapsed` is set only by a click (`EpicBoardRenderer.ts:152`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('epic-board-section')`; all four header buttons carry `event.stopPropagation()` (`html:1086-1103`) (source) | delegated header click; every header control is a `<button>` so the guard exempts it (`sectionHeader.ts:145-192`) (source) | MATCH |
| B3 | Persistence (the SECTION) | **not persisted**: absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **not persisted**: session-only (`sectionHeader.ts:12-15`) (source) | MATCH |
| B4 | Toolbar show/hide | `🗂️` → `section-epic-board` (`html:50`) (source) | `🗂️` → `epic-board-pane` (`sectionToolbar.ts:66`) (source) | MATCH (different storage keys, as elsewhere) |
| B5 | Collapse/expand-all | **the pane has its own pair in its header**: `⊟` → `collapseAllEpics()` and `⊞` → `expandAllEpics()`, which write an EXPLICIT choice for every rendered key (so it overrides even a group whose default is open) and update each group's DOM in place without a re-render (`html:1091-1098`, `js:15110-15140`) (source) | **absent**: the header carries only `⟳` and the updated stamp (`EpicBoardRenderer.ts:113-129`); the toolbar pair goes to Notifications, never here (`SectionToolbarRenderer.ts:115-122`) (source) | **OWED** — an epic-level bulk collapse, with legacy's explicit-choice semantics |
| B6 | Auto-reveal | none found (source) | none found (source) | MATCH |
| B7 | Live header | `🗂️ Epic Board: <count>` where the **count is EPICS, not rows** — the macro unit — plus `#epic-board-updated` reading `updated HH:MM:SS TZ` (`html:1087-1090`, `js:14849`, `:14858-14869`) (source) | `🗺️ Epic Board · <count>` with the same epics-not-rows rule, stated as the one place this pane disagrees with its siblings, and the same stamp through the same formatter (`EpicBoardRenderer.ts:13-17`, `:240`, `:288-292`) (source) | MATCH (the glyph differs — Phase 1's business) |
| B8 | Refresh control | `⟳` → `refreshTaskList()`, so it refreshes the Task List too; the tooltip says so, because the two panes share one fetch (`html:1099-1102`) (source) | `⟳` → the TASK LIST store's `refresh()`, so it likewise refreshes both (`EpicBoardRenderer.ts:118`) (source) | MATCH. **Note:** the mux button carries no tooltip saying it also refreshes the task list |
| B9 | Nested accordions (epic groups) | one `<tbody class="epic-group" data-epic>` per section; header bar = chevron `▾`/`▸` + label + count, `role="button" tabindex="0" aria-expanded`, `aria-controls` the slug; **all epics COLLAPSED by first-load default**, with ⏳ Waiting-on-Rick the documented exception because a collapsed highlight highlights nothing; choices persisted to `localStorage["lupin.epicBoard.groupState"]` as a MAP of key → isEXPANDED holding only groups the viewer actually toggled, so a newly-minted epic takes the default rather than inheriting a stale set (`js:14554-14666`, `:14693-14731`) (source) | identical on every clause — same key, same `__on_rick__` / `__drift__` sentinels, same slug rule, same map-of-choices shape, same on-Rick-only default (`epicBoardCollapse.ts:27-128`, `templates/epicBoardTable.ts:104-147`) (source). The toggle handler additionally treats the persisted choice as the source of truth rather than reading the class, so a repaint arriving mid-click cannot flip it the wrong way (`EpicBoardRenderer.ts:265-281`) | MATCH, with the read-the-choice-not-the-class discipline as EXTRA |
| B10 | Empty / degraded states | **four**, mirroring the task list so the two panes degrade identically: `auth_required` (count → "0"), `query_unavailable` (count → "0"), `unreachable` → "⚠️ Store unreachable — showing nothing rather than something stale." with **the count deliberately left alone**, and otherwise the table. **No truncation banner**, on purpose, because the pane above already carries one for the same rows (`js:14807-14843`) (source) | the same four, same strings carbon-copied, the same leave-the-count-alone on unreachable with the reasoning spelled out, and the same deliberate absence of a truncation banner (`EpicBoardRenderer.ts:22-23`, `:46-49`, `:207-232`) (source) | MATCH |
| B11 | Layout mode | not referenced by the layout code (search: `grep -n section-epic-board notifications.js` → no match) (source) | a sibling pane in the main column (`multiplexer.html:261-262`) (source) | MATCH |
| E1 | Fetch and clock | **no fetch and no timer of its own**: `renderEpicBoard( composite )` takes the task list's already-fetched composite as an argument, called straight after `renderTaskList` — one fetch, one clock, because two clocks read as a bug the first time they disagree (`js:14340-14352`, `:12436-12441`) (source) | the same rule by subscription: it reads the task list's store off `store_task_list_changed` and never fetches (`EpicBoardRenderer.ts:6-11`, `:156-161`, `boot.ts:719-724`) (source) | MATCH |
| E2 | Open-row filter | the SAME `isTaskOpenStatus` filter the task list applies, so the panes cannot disagree about which rows exist (`js:14845-14847`) (source) | the same `isOpenStatus`, same stated reason (`EpicBoardRenderer.ts:234-236`) (source) | MATCH |
| E3 | Section order and membership | ⏳ Waiting on Rick FIRST (shown only when non-empty, and a **highlight, not a move** — its rows also appear under their own epic), then the per-epic groups, then 🔴 Drift LAST — and **Drift renders even when empty**, as a green "✅ No drift", because a section that vanishes when satisfied cannot be told from one that failed to render (`js:14733-14795`) (source) | the same three sections in the same order, the same on-Rick-only-when-non-empty rule, the same always-render-drift rule, and the four labels plus two story lines carbon-copied (`templates/epicBoardTable.ts:45-62`, `:155-185`) (source) | MATCH |
| E4 | Which rows count as waiting on Rick | any `blocked_by` ref whose id is "rick" (case- and space-insensitive) with kind ∈ {user, persona} — **kind is ignored on purpose**, mirroring the generator, because the same human appears under both and a kind-keyed selector silently drops half the queue (`js:14449-14472`) (source) | the same predicate and the same reasoning (`epicBoardModel.ts`) (source) | MATCH |
| E5 | Group ordering | mirrors the board generator's `sort_key`: `epic:unassigned` sinks LAST among epics, otherwise biggest bucket first, ties broken by key so equal-sized epics never reshuffle between renders. Within a group: **priority first** (Rick's 2026-09-09 ruling), then status rank, then title. On-Rick sorts priority then id (`js:14518-14551`) (source) | the same comparators — this is one of the four copies the legacy comment tracks at `js:14518-14520` (`epicBoardModel.ts`) (source) | MATCH. **Note:** legacy flags "keep all four identical until a parity guard exists — row 507183ff"; that guard is still owed, and it is not this inventory's row |
| E6 | Epic titles and stories | titles and one-line stories come from `GET /api/epic-stories`, fetched **once** and memoized for the page's life — a hand-edited file is not live state; an epic with no entry falls back to its de-slugged key; the story rides INSIDE the group so opening an epic answers "what is this?" in the same gesture (`js:14362-14448`, `:14723-14727`) (source) | the same one-shot memoized load, the same fallback, the same story-inside-the-group (`stores/EpicStoriesStore.ts`, `epicBoardModel.ts`, `templates/epicBoardTable.ts:132-139`). Plus the load's arrival triggers a repaint that deliberately does **not** move the updated stamp, because the stories are a lookup and not this pane's data — measured: without it the board sat on de-slugged names with no story rows for 15 s while the data had already arrived (`boot.ts:743-752`, `EpicBoardRenderer.ts:182-196`) (source) | MATCH, with the arrival repaint as EXTRA |
| E7 | Row shape | rendered through the **same row renderer** as the task list, cell-for-cell, with only the row class differing — it used to be a second implementation with its own column order and no Class field, and killing that was the point (`js:14668-14691`) (source) | the same shared `renderDisclosedRow`, called with the `"epic-board"` scope (`templates/epicBoardTable.ts:143`, `templates/taskRowDisclosed.ts:176-207`) (source) | MATCH |
| E8 | Row CONTROLS reaching a handler | the pane wires its own delegated listeners on the persistent container: a click dispatcher (`_handleEpicBoardClick` → request chips, id copy, detail 📄, row controls) **plus `_wireVerbSelects`** for the verb `change` events (`js:15069-15095`) (source) | **not wired**: the container's only listeners are the accordion click and the accordion keydown (`EpicBoardRenderer.ts:139-149`), and the click handler is `handleAccordionActivate`, which returns immediately unless the target is inside an `.epic-group-header` (`:252-256`). No `change` listener exists on this pane at all, and no other listener reaches it — search `grep -n "addEventListener\|epic-board-pane" multiplexer/boot.ts` shows only the document/window lifecycle listeners and the mount lookup. Yet the rows it paints DO carry the controls: `renderDisclosedRow` emits the actions cell with priority · owner · verb · reason · Submit and the ⋯ / detail / id-copy (`taskRowDisclosed.ts:151-155`) (source) | **OWED, and this is the sharpest row in my four.** Every control in the mux epic board is painted and dead — it is exactly the defect legacy recorded and fixed for the holding area: "THIS PANE HAD NONE AT ALL, AND SO EVERY CONTROL IN IT WAS DEAD … the buttons painted and the clicks landed on no handler" (`js:13927-13939`) |
| E9 | In-place collapse | `_applyEpicGroupCollapseState` flips the class, the header's `aria-expanded` and the chevron without a re-render, **and closes any disclosed rows inside the group** on collapse (`js:14871-14892`, `:12934`) (source) | flips the same three things in place, but does **not** close disclosed rows — search `grep -rn "closeDisclosed" multiplexer --include=*.ts \| grep -v test` → no match (`EpicBoardRenderer.ts:276-281`) (source) | **OWED** — a collapsed group can hide an open controls row that comes back on expand |
| E10 | Keyboard | Enter / Space on a focused `.epic-group-header` toggles it, `preventDefault` so Space does not scroll; both the modern `" "` and the legacy `"Spacebar"` key values are accepted (`js:15084-15091`) (source) | the same, both spellings (`EpicBoardRenderer.ts:143-149`) (source) | MATCH for the header. ⚠️ Legacy's keydown also only covers the header here — the row's own keyboard paths live in its click dispatcher, which is E8's gap |
| E11 | Operator-state survival | `_captureOperatorState` / `_restoreOperatorState` wrap this pane's repaint too, so an open disclosure or a typed reason survives the 60 s tick (`js:14851-14853`) (source) | **absent** — the repaint is an unconditional `replaceChildren` (`EpicBoardRenderer.ts:242-243`); search as in A8 T16 → no match (source) | **OWED** — same build item as A8 T16 and A9 H13 |
| E12 | Count on a torn payload | a non-array `tasks` takes the unreachable branch and the count is left standing rather than zeroed (`js:14840-14843`) (source) | same, with the reasoning written out (`EpicBoardRenderer.ts:225-231`) (source) | MATCH |

**A10 build items**: (1) wire this pane's row controls — a delegated click dispatcher covering the ⋯ disclosure, detail 📄, id-copy and row verbs, plus a `change` listener for the verb / priority / owner selects (E8); (2) the header's `⊟`/`⊞` epic bulk collapse with legacy's explicit-choice semantics (B5); (3) close disclosed rows when a group collapses (E9); (4) operator-state capture and restore across the repaint (E11, shares A8 T16 / A9 H13). Optional: the ⟳ tooltip saying it refreshes the Task List too (B8).

**Carried forward, not this pane's row**: legacy flags four hand-maintained copies of the urgency comparator needing a parity guard (row `507183ff`, `js:14518-14520`).

### A15 — Direct TTS Test `✓`

Legacy: `notifications.html:1366-1381` (markup), `notifications.js:74` (mode constants), `:1771-1774` (Speak Now wiring), `:1789-1800` (the three test buttons), `:4236-4258` (`directTTSTest`), `:4260-4264` (`getCurrentTTSMode`), `:4280-4285` (`testTTS`), `:4287+` (`playTTS`), `:5067-5100+` (`stopAudio`).
Multiplexer: **absent**. Searches of non-test multiplexer source for `direct-tts`, `Direct TTS`, `Speak Now`, `test-instant` and `test-reliable` return **0 hits**, and `grep -n 'direct-tts' html/multiplexer.html` returns nothing — there is no mount point either.

Because the section is absent, every legacy behaviour below is **OWED**.

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | **open and visible**: `#direct-tts-section` carries no `collapsed` (`html:1372`), `#section-direct-tts` no `section-hidden` (`:1367`), and its toolbar button starts `active` (`:74`). Unlike A4 and A11 it carries **no** inline `display:none` | OWED |
| B2 | Toggle trigger | click anywhere on `.section-header` → `toggleSection('direct-tts-section')`; chevron `#direct-tts-toggle` is a `<button>` (`html:1368-1370`) | OWED |
| B3 | Persistence | **none**: `direct-tts-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) and no per-feature key names it (`grep -nF 'direct-tts' notifications.js` → four hits, all DOM lookups, none storage) | OWED (match: no persistence) |
| B4 | Toolbar show/hide | `🔧` button, `data-section="section-direct-tts"`, title "Direct TTS Test" (`html:74`), persisted via `lupin_section_visibility` | OWED |
| B5 | Collapse/expand-all | not affected | — |
| B6 | Auto-reveal | none found (`grep -nF 'section-direct-tts' notifications.js` → 0 hits) | — |
| B7 | Live header | static "🔧 Direct TTS Test (Bypass Q&A)" (`html:1369`) | OWED |
| B8 | Refresh control | none | — |
| B9 | Nested accordions | none | — |
| B10 | Empty state | none — a text input and four buttons, always present | OWED (as markup) |
| B11 | Layout mode | not referenced by the horizontal-layout code | — |
| D1 | Text input | `#direct-tts-input`, placeholder "Type text to speak directly…" (`html:1374`). **No Enter-key handler** — `grep -n "direct-tts-input" notifications.js` finds only the value read at `:4237` | OWED |
| D2 | 🔊 Speak Now | `#direct-tts-button` → `directTTSTest()` (`js:1771-1774`). Empty/whitespace text is refused through `this.error("Please enter text to speak")`, which writes into the **A16 debug log** and the console — there is no inline status element (`js:4239-4243`, `:21020-21023`) | OWED |
| D3 | Mode source | the mode is read from `#tts-mode` — **the select that lives in the Q A section** (`js:4245`). This is the A1 Q6 cross-section dependency seen from the other end: build Q A's mode select and this section reads it | OWED — and see the note |
| D4 | The speak path | `await this.playTTS( text, mode )` — a direct call that deliberately bypasses Q A, job completion and every WebSocket event, with two explicit debug lines saying so (`js:4249-4255`). `playTTS` checks the audio cache first and plays a hit without a server round-trip (`js:4287-4305`) | OWED |
| D5 | Input clearing | the input is cleared **after** the await resolves (`js:4257`) | OWED |
| D6 | Test Instant TTS | `#test-instant-tts` → `testTTS('instant')`, which speaks the fixed string "This is a test of the text-to-speech system in instant mode." (`js:1790-1792`, `:4280-4285`, constant at `:74`) | OWED |
| D7 | Test Reliable TTS | `#test-reliable-tts` → `testTTS('reliable')`, same fixed string with "reliable" (`js:1794-1796`) | OWED |
| D8 | Stop Audio | `#stop-audio` → `stopAudio()`, which stops the pulsing card indicator and then tears down **four** independent audio paths: `currentAudio`, `audioElement` (revoking its object URL), `currentSequentialAudio`, and the Web Audio API sources (`js:1798-1800`, `:5067-5100+`) | OWED |
| D9 | No wiring guard | all four listeners are attached **unguarded** — `document.getElementById(...).addEventListener(...)` with no null check (`js:1772`, `:1790`, `:1794`, `:1798`), so removing any of these four ids from the markup throws during setup and kills every listener registered after it | OWED as a **caution**, not as a behaviour to copy |

**Note on D3.** The TTS-mode select is markup in the Q A section but page-wide state: A1 Q6 already records that other sections read it (`js:15756`, `:20591`, `:20940`), and this section is a fourth reader (`js:4245`, `:4262`). When the build decides where that value lives, this section is one of its dependants. A15 cannot be built before that decision.

**Note on D9.** This row is the reason A15 and A1 are coupled in build order beyond D3: legacy's setup function wires Q A and Direct TTS in the same unguarded run (`js:1771-1800`), so a partial port that creates one section and not the other reproduces the throw.

**A15 build items**: the whole section, specified by B1–B4, B7, B10 and D1–D8 — open and visible by default, unpersisted, toolbar `🔧`; a text input with no Enter handler; **🔊 Speak Now** (refuse empty text via the debug log, read the page-wide TTS mode, call the direct play path bypassing Q A and the queue, clear the input after); **Test Instant TTS** and **Test Reliable TTS** with their fixed strings; and **Stop Audio** tearing down all four audio paths. Guard the listener wiring rather than copying D9.

### A16 — Debug Information `✓`

Legacy: `notifications.html:1383-1394` (markup), `notifications.js:70` (`this.debug = true`), `:21013-21018` (`log`), `:21020-21023` (`error`), `:21025-21028` (`wsDiag`), `:21030-21046` (`addDebugMessage`); styling at `css/notifications.css:339-355`.
Multiplexer: **absent**. Searches of non-test multiplexer source for `debug-log`, `Debug Information`, `addDebugMessage` and `debug-info` return **0 hits** (the two `debug` hits in `transport/AudioTransport.ts:9` and `shared/types.ts:787` are prose about a default binary handler, not this panel), and `grep -n 'debug' html/multiplexer.html` returns nothing — no mount point either.

Because the section is absent, every legacy behaviour below is **OWED**.

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | **open and visible**: `#debug-section` carries no `collapsed` (`html:1389`), `#section-debug` no `section-hidden` (`:1384`), toolbar button starts `active` (`:75`). No inline `display:none` | OWED |
| B2 | Toggle trigger | click anywhere on `.section-header` → `toggleSection('debug-section')`; chevron `#debug-toggle` is a `<button>` (`html:1385-1387`) | OWED |
| B3 | Persistence | **none**: `debug-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`), and `grep -nF 'debug-section' notifications.js` → 0 hits, so nothing else persists it either | OWED (match: no persistence) |
| B4 | Toolbar show/hide | `🐛` button, `data-section="section-debug"`, title "Debug Info" (`html:75`), persisted via `lupin_section_visibility` | OWED |
| B5 | Collapse/expand-all | not affected | — |
| B6 | Auto-reveal | **none** — the panel never reveals itself, not even on an error. `error()` writes a line and nothing more (`js:21020-21023`); no code path touches `section-hidden` or `collapsed` for this section | OWED (match: no auto-reveal) |
| B7 | Live header | static "Debug Information" (`html:1386`) — **the only section header on the page with no leading emoji**. No count, no timestamp | OWED |
| B8 | Refresh control | none. There is also **no Clear button** and no copy/export control: `grep -nF 'debug-log' notifications.js` → two hits, both inside `addDebugMessage` | OWED (match: no controls) |
| B9 | Nested accordions | none | — |
| B10 | Empty state | not an empty state as such — the markup ships **one seeded line**, `<div class="debug-info">System starting up...</div>` (`html:1391`), which is a real child and therefore counts toward the 20-line cap until it is pushed off | OWED |
| B11 | Layout mode | not referenced by the horizontal-layout code | — |
| G1 | What lands in it | three writers, all funnelling into `addDebugMessage`: `log()` (**only when `this.debug`**, `js:21013-21018`), `error()` (**always**, prefixed `ERROR:`, `:21020-21023`) and `wsDiag()` (**always**, prefixed `WS-DIAG:`, `:21025-21028`) | OWED |
| G2 | The debug flag | `this.debug = true` — **hard-coded on** (`js:70`). There is no INI key, no query param and no UI control for it (`grep -n 'this.debug\s*=' notifications.js` → one hit). So in practice every `log()` line lands in the panel | OWED — and see the note |
| G3 | Line format | `[<toLocaleTimeString()>] <message>`, written via `textContent` (so it is XSS-safe and renders no markup), on a `div.debug-info` that also carries the type as a second class — `debug-info info` or `debug-info error` (`js:21030-21036`) | OWED |
| G4 | Order | **newest first** — `insertBefore(debugDiv, debugLog.firstChild)` (`js:21038-21039`). The opposite of a console | OWED |
| G5 | Cap | **20 children**, enforced after every insert by removing `lastChild` — i.e. the oldest — in a `while` loop (`js:21041-21044`) | OWED |
| G6 | Args are dropped | `log( message, ...args )` passes `...args` to `console.log` but hands **only `message`** to `addDebugMessage` (`js:21015-21016`), so a line whose detail lives in the args reads as a bare sentence in the panel while the console has the object. Same in `error()` and `wsDiag()` | OWED as a **caution** — reproduce the behaviour, but know the panel is strictly less informative than the console |
| G7 | Volume | the three writers are called **462 + 162 + 5 = 629 times** across `notifications.js` (`grep -c` per writer). With G2's flag hard-on and G5's 20-line cap, the panel holds roughly the last second of a busy load | OWED (context for sizing, not a behaviour) |
| G8 | Styling | `.debug-log-scrollable` — `max-height: 300px`, `overflow-y: auto`, monospace 12 px, light-grey panel with a border (`css/notifications.css:346-355`); `.debug-info` is monospace 12 px, grey `#6c757d`, 5 px bottom margin (`:339-344`). There is **no `.debug-info.error` rule** — `grep -n '\.debug-info' css/notifications.css` returns one hit — so the `error` class G3 sets is **styled identically to `info`** | OWED. The missing error styling is a legacy defect; flagging rather than copying |

**Note on G2.** Legacy ships with debug logging permanently on and no way to turn it off from the page. The multiplexer has no such panel and no such flag, so the build has a genuine choice to make: reproduce the hard-on flag, or gate the panel behind something. That is a build decision, recorded here so it is not made by accident.

**Note on G8.** `addDebugMessage` distinguishes `info` from `error` in the DOM and the stylesheet never uses the distinction. An error line is visually identical to an ordinary one today, and only the `ERROR:` text prefix separates them. If the build styles the error class, that is a visible change from the lead — worth naming as such rather than shipping it as parity.

**A16 build items**: the whole section, specified by B1–B4, B6–B8, B10 and G1–G5 — open and visible by default, unpersisted, toolbar `🐛`, a plain "Debug Information" header with no controls; a 300 px scrollable monospace panel seeded with "System starting up..."; three writers (`log` gated on the debug flag, `error` prefixed `ERROR:`, `wsDiag` prefixed `WS-DIAG:`) each appending a `[HH:MM:SS] message` line via `textContent` with an `info`/`error` type class; **newest first**; capped at **20** lines by dropping the oldest. Decide G2 (the always-on debug flag) deliberately, and decide G8 (whether an error line looks different) deliberately — legacy does neither.

### A13 — Time Saved Dashboard `✓`

Legacy: `notifications.html:1266-1297` (markup), `:73` (toolbar `⏱️`), `notifications.js:602` (load at init), `:8608-8661` (`refreshTimeSavedStats`), `:8663-8690` (`renderTopSolutions`). Multiplexer: **absent**. A search of non-test multiplexer source and of `multiplexer.html` for `time-saved`, `timeSaved`, `time_saved`, `top-solutions` and `topSolutions` returns **0** hits; the same search shape for `fleet-state` returns 5, so the instrument finds things when they are there.

Because the section is absent, every legacy behaviour below is **OWED**. The rows exist so the build has a complete behaviour spec, not just "add a Time Saved section".

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | open: `#time-saved-section` carries no `collapsed` class (`html:1273`) | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('time-saved-section')` (`html:1268`), flipping `collapsed` and the chevron `▼`/`▶`; the `🔄` calls `event.stopPropagation()` first (`html:1270`) | OWED |
| B3 | Persistence | **none**: `time-saved-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`), so it opens on every load | OWED (match: no persistence) |
| B4 | Toolbar | `⏱️` hides and shows the section (`html:73`; A0) | OWED |
| B5 | Collapse/expand-all | not affected (`notifications.js:14312-14337` acts on Task List owner groups) | — |
| B6 | Auto-reveal | none found: `grep -n "time-saved-section" notifications.js` returns zero hits — the id lives only in the markup (`html:1268`, `:1273`) | — |
| B7 | Live header | static title "⏱️ Time Saved Dashboard" — no count, no updated stamp (`html:1269`) | OWED |
| B8 | Refresh control | `🔄` → `refreshTimeSavedStats()`, which refetches BOTH endpoints and repaints; disclosure untouched, no debounce guard and no auto-poll (`html:1270`, `notifications.js:8608-8661`) | OWED |
| B9 | Nested accordions | none | — |
| B10 | Empty state | the four stat values read `--` until the first fetch (`html:1276-1289`); the leaderboard reads "Loading..." before the first fetch (`html:1294`) and "No replayed solutions yet" when the list comes back empty (`notifications.js:8681-8684`) | OWED |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "time-saved" notifications.js | grep -i "layout\|left-column"` → no hits) | — |
| T1 | First load | fetched once at init, not on expand and not on a timer (`notifications.js:602`) | OWED |
| T2 | Stats fetch | `GET /api/stats/time-saved` with a bearer token; on 2xx it paints four values — `total_time_saved_formatted` → "Time Saved (You)", `time_saved_for_others_formatted` → "Time Saved for Others", `solutions_created` → "Solutions Created", `total_replays_benefited` → "Cache Hits (You)". The two formatted values fall back to `--` and the two counts to `0` when the field is missing. A non-2xx logs and leaves the previous values on screen (`notifications.js:8623-8645`) | OWED |
| T3 | Leaderboard fetch | a SECOND call, `GET /api/stats/time-saved/global`, feeds `renderTopSolutions(globalStats.top_solutions)`. A non-2xx logs and leaves the leaderboard as it was (`notifications.js:8647-8656`) | OWED |
| T4 | Leaderboard rendering | one `.top-solution-item` per solution: `#rank` · the question (HTML-escaped) · "N replays · X saved" from `replays` and `time_saved_formatted`, ranked in the order the server returned (`notifications.js:8687-8690`) | OWED |
| T5 | Error handling | the whole refresh is wrapped in a try/catch that only logs — a throw never clears the dashboard or surfaces a message to the user (`notifications.js:8658-8660`) | OWED |
| T6 | Auth path | both calls use a bare `fetch` with `Authorization: Bearer ${this.accessToken}` — NOT `authedFetch`, so neither one refreshes an expired token (`notifications.js:8624-8626`, `:8648-8650`). Recorded as the lead's behaviour, not endorsed | OWED — see note |

**Note on T6.** These two are among the few legacy calls that skip the token-refreshing wrapper every other panel uses. A build that ports this section should use the multiplexer's `ApiClient` rather than copy the bare `fetch`; that is a superset, not a deviation from what the user sees.

**A13 build items**: the whole section, specified by B1–B4, B7, B8, B10 and T1–T6.

### A14 — System Status `✓`

Legacy: `notifications.html:1300-1364` (markup), `:74` (toolbar `📊`), `notifications.js:1103-1180` (health monitor text), `:1376-1396` (logout), `:1399-1458` (config reload), `:1460-1501` (`refreshAllStatus`), `:1503-1535` (WebSocket status), `:1537-1575` (auth status), `:1577-1583` (session display), `:1585-1612` (copy-to-clipboard), `:21263-21305` (missed count + reset).
Multiplexer: **no System Status section**. `grep -rn "queue-ws-status|audio-ws-status|auth-status|ws-health-status|config-status|/api/init|user-display|queue-session"` over non-test multiplexer source returns no match for any of those ids; `multiplexer.html` declares no status pane. Three of the section's behaviours DO exist in the multiplexer, in other places: logout and the user's email in the nav bar (`render/NavBarRenderer.ts:81-114`, `render/templates/navBar.ts:100`), and the missed-count badge with its Reset (`render/MissedBadgeRenderer.ts:88-106`).

Every row below is therefore either **OWED** (nothing in the multiplexer does it) or **MATCH (elsewhere)** — the behaviour exists but not in this section, which is a Phase 1 placement question, not a missing behaviour.

| # | Behaviour | Legacy (lead) (source) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#status-section` carries no `collapsed` class (`html:1312`) | no section | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('status-section')` (`html:1301`); the `↻` stops propagation (`html:1305`) | — | OWED |
| B3 | Persistence | **none**: `status-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) | — | OWED (match: no persistence) |
| B4 | Toolbar | `📊` hides and shows the section (`html:74`; A0) | the toolbar has no status entry (`templates/sectionToolbar.ts:49-66`) | OWED |
| B5 | Collapse/expand-all | not affected (`notifications.js:14312-14337`) | — | — |
| B6 | Auto-reveal | none found: `grep -c "status-section" notifications.js` → 0; the id appears only in the markup (`html:1301`, `:1312`). Instrument check: `grep -c "queue-ws-status" notifications.js` → 6 | — | — |
| B7 | Live header | static title "System Status" — no count, no stamp (`html:1302`) | — | OWED |
| B8 | Refresh control | `↻` → `refreshAllStatus()`: disables itself and spins, then re-runs the WebSocket, auth, session and health readouts in order, and always re-enables in a `finally` (`html:1304-1308`, `notifications.js:1460-1501`) | — | OWED |
| B9 | Nested accordions | none | — | — |
| B10 | Empty state | not applicable — every row has a seeded value: both sockets read "Disconnected", auth "Not authenticated", user "Loading...", health "Initializing...", the two session codes `-` (`html:1315-1352`) | — | OWED (the seeded values are the spec) |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "status-section\|status-item" notifications.js | grep -i "layout\|left-column"` → no hits) | — | — |
| S1 | Queue WebSocket row | a pill mapping the channel state to text + class: DISCONNECTED "Disconnected"/error · CONNECTING "Connecting..."/warning · AUTHENTICATING "Authenticating..."/warning · CONNECTED "Connected"/good · BACKOFF "Reconnecting..."/warning · OPEN_CIRCUIT "Circuit open"/error; no channel at all reads "Not initialized"/error. Also updated live from socket events, not only on `↻` (`notifications.js:1503-1527`, `:2875-2897`) | no such readout: the mux tracks the same states in `transport/ConnectionStateMachine.ts` but paints no status element (search above) | **OWED** |
| S2 | Audio WebSocket row | the same pill for the audio channel (`notifications.js:1529-1535`, `:3010`) | — | **OWED** |
| S3 | Authentication row | "Not authenticated" with no token; on an expired token it refreshes and, on failure, reads "Token expired"/error; otherwise "Authenticated" plus " (admin)" when the JWT carries the admin role (`notifications.js:1537-1575`) | — | **OWED** |
| S4 | Missed row | "N missed while away" with a Reset button, both shown only while N > 0; Reset POSTs `/api/notifications/undelivered/dismiss` with NO confirm (Rick: a soft dismiss is reversible) and alerts on failure (`html:1326-1328`, `notifications.js:21263-21305`) | the badge and Reset exist with the same show-only-while-positive rule and the same reset call, rendered outside any status section; the failure path keeps the badge for a retry instead of alerting (`render/MissedBadgeRenderer.ts:8-14`, `:88-106`) | MATCH (elsewhere). **Note:** legacy alerts on a failed reset, the mux stays silent |
| S5 | User row | the user's email plus a Logout button that destroys both sockets, marks auth "Logged out" and redirects to `/app/auth/login` (`html:1331-1333`, `notifications.js:1376-1396`) | the nav bar shows the email and a Logout that clears the persisted tokens and redirects to the same login page (`render/templates/navBar.ts:100`, `auth/authGuard.ts:83-90`) | MATCH (elsewhere) |
| S6 | Sessions row | the queue and audio session ids as `<code>`, each with a 📋 copy button that no-ops on an empty value or `-` and shows a brief checkmark (`html:1336-1349`, `notifications.js:1577-1612`) | no session-id readout and no copy control for it (search above) | **OWED** |
| S7 | Health Monitor row | the 90 s WebSocket health monitor's own line: "Monitoring (90s interval)" / "Stopped" / "✓ Healthy (checked HH:MM:SS)" / "Circuit open — click Retry-now" / "Watchdog: queue=… audio=…" (`notifications.js:1103-1180`) | — | **OWED** |
| S8 | Config row | a "↻ Reload" button that calls `GET /api/init`, disabling itself and dimming while in flight, then writes `✓` in green or `✗ <message>` in red beside it, and re-enables in a `finally` (`html:1355-1361`, `notifications.js:1399-1458`) | — | **OWED** |

**A14 build items**: the section itself (B1–B4, B7, B8, B10) plus S1, S2, S3, S6, S7 and S8. S4 and S5 already exist in the multiplexer and are placement questions for Phase 1, not build items — except the one behaviour difference noted at S4 (legacy alerts when a missed-reset fails).

---

## 5. Build list — every OWED row in one place

| # | From | Owed behaviour | Legacy evidence |
|---|---|---|---|
| 1 | A0 B1 | `jobs-pane` starts visible (legacy starts every section visible) | `notifications.html:43-75` |
| 2 | A0 B4a | Showing a section from the toolbar scrolls it into view | `notifications.js:15349` |
| 3 | A0 B6 | A filter-mode badge reveals Filter Settings, saves the choice and scrolls to it (arrives with A11) | `notifications.js:6364-6384` |
| 4 | A1 | The Q A Interface section, per A1: open by default, not persisted, toolbar `❓`, agent select (one-shot), voice-mode badge, submit to `/api/v2/ask` or `/api/v2/submit` with a 2 s debounce, the inline argument interview, voice input, the TTS-mode select (page-wide), TTT/TTFA/RTT metrics | `notifications.html:103-160`; `notifications.js:1777-1935`, `:3099-3300`, `:6118-6150` |
| 5 | A2 | The Submit Agentic Jobs section, per A2: the section accordion, three collapsed-by-default cards plus the always-open TFE card, and four submit pipelines (Claude Code, Research, Test Suite, TFE Resume) with their scheduling controls | `notifications.html:163-408`; `notifications.js:1876-2184`, `:3370-3638`, `:4116-4219`, `:8161-8264` |
| 6 | A7 B3 | Persist the Finished Tasks open/closed state; legacy keeps it in `notifications_finished_tasks_open` and restores it before first paint | `notifications.html:1517-1532`, `:1571-1593` |
| 7 | A3 | Action Required, 14 items: reload persistence for the queue, active id, pause state and half-answered stepper; a toolbar button; auto-reveal on arrival (un-hide, un-collapse, save, scroll); a non-terminal count; defer while TTS plays; the draining progress bar; pause/resume (button and P); cancel (button and Escape); Y/N/C shortcuts; the Neither button with the default highlight; the yes_no comment row; open_ended mic and prefill; multiple_choice Other with mic; and the card chrome (project and persona badges, abstract block, prediction hint) | `io/phase2/A3.md` cites each one |
| 8 | A5 | Notifications, 7 items: the `💬` button must hide the header as well as the pane; TTS playback must expand the sender card and date accordion and scroll to the row; the filter badge must reveal Filter Settings (with A11); the Clear All confirm must name the count and the window; Recent Activity's open state must persist; the sender-card count must read "N new"; the conversation-mode button needs its solo icon pair | `io/phase2/A5.md` cites each one |
| 9 | A4 | TTS: visible if and only if an item exists or the toolbar button is active, evaluated on load, on every queue mutation and on a toolbar toggle (this closes A0 B4b); reload persistence for the queue and focus mode; an active-plus-pending count and a title that follows Paused and focus; separate Pause and Play with a manual pause that blocks advance; and deferring an Action Required prompt while audio plays | `notifications.js:22521`, `:15354-15357`; `io/phase2/A4.md` |
| 10 | A6 | The fleet-size-cap dial: ceiling and value read from the arbiter, readout on input, saved on change with the slider disabled while saving and repainted from the server's answer, refreshed on the same tick as the table | `io/phase2/A6.md` |
| 11 | A8 | Task List, 10 items: the live/parked/total count split; the deploy-defect `query_unavailable` state; legacy's verb order with un-park beside Park; the reason mic; the drop-reason suggestions; the staged priority edit with its Update button; the truncation banner with verbatim server warnings; the press-hold repaint guard; operator-state capture and restore across a repaint; and awaiting an in-flight read before a post-write refresh | `io/phase2/A8.md` cites each one |
| 12 | A9 | Holding Area: the flow-ratio readout and its whole operator cluster (threshold and window sliders, Freeze manager pull, Reset, source line, writes on change only), the truncation banner from this pane's query, and operator-state capture across the repaint | `io/phase2/A9.md` |
| 13 | A11 | Queue Filter Settings: the section and its toolbar button, the "Currently viewing" status line, the badges that reveal and scroll to it, and the admin refusal at the setter. The three-mode switch itself is already inline in two multiplexer headers, on legacy's own `notifications_filter_preference` key | `io/phase2/A11.md` |
| 14 | A12 | Cosa Jobs Flow, 9 items: the pane starts visible; all five buckets start collapsed and their open state survives a re-render and a reload (legacy saves it under `lupin_queue_expand_state`); the lead's bucket glyphs and labels; the empty-pane copy; a delete-all confirm that names the queue and window; history that excludes ids live in Done or Dead; and an expanded job card that stays expanded across a repaint | `io/phase2/A12.md` |
| 15 | A10 | Epic Board: the header's bulk collapse and expand, closing disclosed rows when a group collapses, full row controls with their change listener, and the shared operator-state item | `io/phase2/A10.md` |
| 16 | A15 | The whole Direct TTS Test section: open and visible, unpersisted, toolbar `🔧`, a text input with no Enter handler, Speak Now (empty text refused into the debug log, page-wide TTS mode, direct play bypassing Q A and the queue, input cleared), and the two test buttons | `io/phase2/A15.md` |
| 17 | A16 | The whole Debug Information section: open and visible, unpersisted, toolbar `🐛`, a 300 px scrollable monospace panel seeded with its startup line, and three writers (log gated on the debug flag, error and WS-diagnostic prefixed) | `io/phase2/A16.md` |
| 18 | A13 | The whole Time Saved section, including the fleet-state control the multiplexer has no trace of | `io/phase2/A13.md` |
| 19 | A14 | System Status: both socket pills, the auth and session id readouts, the health monitor and the config reload. Only the logout/email pair and the missed badge with its Reset already exist, in the nav bar | `io/phase2/A14.md` |

**All 17 accordions are inventoried.** The list above is the complete Phase 2 build surface, and the next step is Rick's: he decides what gets built and in what order.

