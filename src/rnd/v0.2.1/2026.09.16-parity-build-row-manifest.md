# Multiplexer parity — store-row manifest (build plan §6 item 6)

**Parent row**: `645a7da5` (Rick, P0). **Drafted by**: Clayton 😎, author #3 for Mr. Radio 🦉, 2026-09-16 ~18:40 EDT.
**Source**: [the build plan](2026.09.15-multiplexer-parity-build-plan.md) at `fa41c222` — §1, §2, §3, §6a, §6b, §7 — and [the spec](2026.09.15-operator-state-preservation-spec.md)'s rulings section.
**State**: **DRAFT — no row is minted.** §6b holds item 6 back until María 🌸 or Rick has reviewed the §6a rulings; this manifest is what gets minted once they have.

**44 rows**: 34 in Phase A, 10 in Phase B. Plus 1 row outside the build that the plan says is owed (§ Adjacent).

## How to read this

- **Row key** is this manifest's handle (`A-2 #2f`, `B-5L`). It is not a store id; put it at the head of the minted title so the depends-on column resolves by eye.
- **Depends-on** carries two kinds of edge, marked:
  - **hard** — a code seam, from §7, §6a or §6b. Landing out of order breaks or duplicates work.
  - **seq** — sequencing only: Rick's *"fixes first, then top to bottom"*, or two rows editing the same file. Out of order is a merge conflict, not a defect.
  §7 warns that edges inferred from scope cells must be re-derived from seams; every **hard** edge below names its seam.
- **Suggested priority** under a P0 parent: **P1** = gates another row, or is a defect Rick reported (the dead buttons, the wiped reason); **P2** = owed parity behaviour; nothing is P3, because the parity rule makes every row owed.
- **Every minted body ends with the footer below, verbatim.** §5 clause 4 says the coverage AC is written into every AC; the bodies here omit it only to keep the manifest readable.

> **FOOTER (append to every body)**
> Finish bar: build plan §5, all seven clauses. Done means every Phase 2 row id this item owns re-reads **MATCH** by that row's own method, and its §5 tier test is green (§6a C0). One test must fail against today's code — Implementer produces the receipt, Reviewer verifies it.
> **Coverage AC: 100% lines AND branches AND functions (`c8 --100`) on every file this item touches.** Implementer produces it; Mr. Radio 🦉 verifies it before the row closes.
> Close with `receipt_refs` scoped `lupin/src/…:NN`.

## Summary table

| Row key | Title | Pri | Depends-on |
|---|---|---|---|
| A-0 | Shared scroll-reveal helper | P1 | — |
| A-1b | Press-hold repaint guard, all three panes | P1 | — |
| A-2 #0 | Shared row controls in `taskRowControls.ts` | P1 | — |
| A-1a | Operator-state capture/restore | P1 | hard A-1b · hard A-2 #0 |
| A-1c1 | Jobs bucket and card disclosure persistence | P2 | — |
| A-1c2 | Action Required reload persistence | P2 | hard A-2 #2f |
| A-1c3 | TTS queue reload persistence + focus mode | P2 | hard A-2 #3c · hard A-2 #3d |
| A-2 #1 | Toolbar: Jobs visible, Filter Settings hidden, 📋 glyph | P1 | hard A-0 |
| A-2 #2a | AR toolbar ⚠️ button | P2 | seq A-2 #1 |
| A-2 #2b | AR auto-reveal | P2 | hard A-0 · hard A-2 #2a |
| A-2 #2c | AR header counts non-terminal only | P2 | — |
| A-2 #2d | AR defers activation while TTS plays | P2 | hard A-2 #3e |
| A-2 #2e | AR draining progress bar | P2 | — |
| A-2 #2f | AR ⏸️ pause/resume | P2 | — |
| A-2 #2g | AR ✕ cancel | P2 | — |
| A-2 #2h | AR keyboard Y/N/C/P/Esc | P2 | hard A-2 #2f · #2g · #2i |
| A-2 #2i | AR ⊘ Neither + default highlight | P2 | — |
| A-2 #2j | AR yes_no comment row | P2 | — |
| A-2 #2k | AR open_ended mic + default prefill | P2 | — |
| A-2 #2l | AR multiple_choice Other, mic, prediction prefill | P2 | — |
| A-2 #2m | AR card chrome | P2 | — |
| A-2 #3a | TTS visibility rule | P2 | — |
| A-2 #3b | TTS separate Pause and Play | P2 | — |
| A-2 #3c | TTS manual pause blocks advance | P2 | hard A-2 #3b |
| A-2 #3d | TTS header count + Paused/focus title | P2 | hard A-2 #3b |
| A-2 #3e | AR→TTS deferral coupling (TTS side) | P2 | — |
| A-2 #4 | Notifications: two-mount toolbar + five behaviours | P2 | hard A-0 · hard A-2 #6 · seq A-2 #1 |
| A-2 #5 | Fleet Status size-cap dial | P2 | — |
| A-2 #6 | Opt-in persisted collapse; Finished Tasks uses it | P1 | — |
| A-2 #7 | Task List counts + truncation banner | P2 | hard A-2 #0 |
| A-2 #8 | Holding Area flow-ratio cluster + banner | P2 | hard A-2 #0 |
| A-2 #9 | Epic Board: wire the painted controls | P1 | hard A-2 #0 |
| A-2 #10 | Jobs: glyphs, empty copy, delete-all confirm, history | P2 | seq A-2 #1 |
| A-2 #11 | Badge wiring and reveal | P2 | hard A-0 · hard B-3 · hard A-2 #1 |
| B-0 | Pre-allocate the seven `boot.ts` mount slots | P1 | hard A-2 #6 · seq all Phase A except A-2 #11 |
| B-1 | Q&A Interface | P2 | hard B-0 |
| B-1b | TTT/TTFA/RTT capture points | P2 | hard B-1 |
| B-2 | Submit Agentic Jobs | P2 | hard B-0 · hard A-2 #1 · seq B-1b |
| B-3 | Queue Filter Settings | P2 | hard B-0 · hard A-0 · hard A-2 #1 · seq B-2 |
| B-4 | Time Saved | P2 | hard B-0 · seq B-3 |
| B-5 | System Status (multiplexer) | P2 | hard B-0 · seq B-4 |
| B-5L | `/api/init` reload gate — legacy half | P2 | — |
| B-6 | Debug Information + `debugSink` funnel | P2 | hard B-0 · seq B-5 |
| B-7 | Direct TTS Test | P2 | hard B-6 · hard B-1 |

Every Phase B row also carries **hard A-2 #6** through B-0 (§7 edge 2).

## Row bodies

### Phase A — shared prerequisites

**A-0 · Shared scroll-reveal helper** — P1 · depends-on: none
Build `(el) => Promise<void>` once, in Phase A. Not a bare `scrollIntoView`: port legacy `notifications.js:25386-25408` — skip when the element is already fully in view, scroll `{behavior:'smooth', block:'start'}`, resolve on a 300 ms timer so callers can sequence after the scroll. Expand-then-scroll pre-work belongs to callers. Zero `scrollIntoView` hits in the multiplexer today. Callers: A-2 #1, #2b, #4, #11, B-3. Source: plan §1 "The scroll-reveal helper", §7 edge 3.

**A-1b · Press-hold repaint guard, all three panes** — P1 · depends-on: none
Defer the repaint while a press is in flight: `mousedown` sets the flag; `mouseup`/`mouseleave`/`blur` release it; the render entry point holds the composite and the release paints the latest one exactly once. Legacy: `:14274-14283`, `:12085-12090`, `:13216-13229`. **Widened to Task List, Holding Area and Epic Board** (spec ruling 4 — all three paint `renderDisclosedRow` and repaint with `replaceChildren`). Lands before or with A-1a, never bundled into capture. Test: unit — `mousedown`, poll tick, assert no repaint; `mouseup`, assert exactly one repaint with the latest composite. Source: plan §1 row 2, spec §7 surface 8.

**A-2 #0 · Shared row controls in `templates/taskRowControls.ts`** — P1 · depends-on: none
Build once, for all three panes: the reason mic, the drop-reason datalist, the staged priority edit with its disabled Update button (carrying `data-original`), and the verb order from the shared module (R4, no change). Includes wiring the Holding Area's row handlers — it paints the shared row but has no `change` listener (`HoldingAreaRenderer.ts:236-240`). Moved out of A-2 #7 by §6a T7; lands before #7, #8, #9 and A-1a. Source: §6a T7, Phase 2 A8/A9 H4.

### Phase A — A-1, operator state (read the spec before opening any of these)

**A-1a · Operator-state capture/restore** — P1 · depends-on: hard A-1b (plan §1), hard A-2 #0 (spec ruling 1: the pending-priority category lands with or after it)
Implement the spec's contract: six categories, two keying schemes, the §3 restore order, the §4 pending-only rule, the §5 absent-node rule, on Task List, Holding Area batch reason, and Epic Board disclosed rows. Rulings to honour: keep the pending-priority category, feature-detected on `data-original` (1); exclude `.task-request-triage` from the value sweep, restore after hydrate (5); move `renderRowError` (`TaskListRenderer.ts:931-960`) into `templates/rowDisclosure.ts` and capture stripes in all three panes (6). Two traps: never interpolate task ids into selectors — select by class, compare the attribute; the text step skips disabled boxes. Test: unit, through the injected `setIntervalFn`, with a real poll tick between action and assertion; verify focus/caret is observable in happy-dom before asserting it, else use `accordionHarness.ts`. AC: *a poll tick landing between an operator's keystroke and their click must not change what that click does.*

**A-1c1 · Jobs bucket and card disclosure persistence** — P2 · depends-on: none
Persist an opened queue bucket and an expanded job card across job events, a collapse-idiom key (legacy `lupin_queue_expand_state`, `:15426-15440`). Q-A2's todo/running default stays (R2). Test: integration — mount, write storage, re-mount. Source: plan §1 row 5.

**A-1c2 · Action Required reload persistence** — P2 · depends-on: hard A-2 #2f (the pause state this persists)
Persist the pending prompt queue, active id, pause state and half-answered stepper (legacy `notifications_action_required`, `:21329-21348`). Backend per spec ruling 3: `StorageService`, injected `| null` option. Expiry per ruling 2: restore-time drop with a 5 s grace, pending entries exempt, `serverExpiryEstimate` display-only, and **persist a local-clock expiry** (`expires_at − clockOffset`, compared to `nowFn()` at restore) because `clockOffset` is 0 after reload. `ActionRequiredStore` persists nothing today. Test: module-boundary unit (write, fresh reader, read) plus integration re-mount. Source: plan §1 row 6, spec §6/§8 and rulings 2–3.

**A-1c3 · TTS queue reload persistence + focus mode** — P2 · depends-on: hard A-2 #3c, hard A-2 #3d (the paused and focus states it persists)
Registered by §6a item 8 as a dropped behaviour. Persist the TTS queue (legacy `notifications_tts_queue`, `js:22919`) with its focus-mode fields; reset the paused state on restore; auto-exit a stale focus. Same backend as A-1c2 (spec §8). Source: Phase 2 A4 build items 2 and 5.

### Phase A — A-2, in page order

**A-2 #1 · Toolbar** — P1 · depends-on: hard A-0
Remove `jobs-pane` from `DEFAULT_HIDDEN_SECTION_IDS` (`sectionToolbar.ts:83`) and rewrite the annotation to name the reversal (R1); delete the vacuous assertion at `section_toolbar_renderer.test.ts:84`, keep `:80`; add `filter-settings-section` to the set (R3); Jobs' glyph becomes `📋` (R6); showing a section scrolls it into view through A-0. Source: A0 B1, B4a.

**A-2 #2a · AR toolbar `⚠️` button** — P2 · depends-on: seq A-2 #1 (same toolbar file) · Phase 2 A3 B4.

**A-2 #2b · AR auto-reveal on arrival** — P2 · depends-on: hard A-0, hard A-2 #2a
Un-hide, un-collapse, save visibility, re-light the button, scroll through A-0. Phase 2 A3 B6.

**A-2 #2c · AR header counts non-terminal items only** — P2 · Phase 2 A3 B7.

**A-2 #2d · AR defers activation while TTS plays** — P2 · depends-on: hard A-2 #3e
Phase 2 A3 R3 / A4 item 6. ⚠️ Shares one seam with #3e — see open question 1.

**A-2 #2e · AR draining progress bar** — P2 · Phase 2 A3 R4.

**A-2 #2f · AR `⏸️` pause/resume** — P2 · Phase 2 A3 R5.

**A-2 #2g · AR `✕` cancel** — P2 · Phase 2 A3 R6.

**A-2 #2h · AR keyboard Y / N / C / P / Esc, suppressed in inputs** — P2 · depends-on: hard A-2 #2f, #2g, #2i · Phase 2 A3 R7.

**A-2 #2i · AR `⊘ Neither` + `response_default` highlight** — P2 · Phase 2 A3 R8.

**A-2 #2j · AR yes_no comment row** — P2 · hint, 300-char input, mic, qualifier pre-expand · Phase 2 A3 R9.

**A-2 #2k · AR open_ended mic + default-as-value prefill** — P2 · Phase 2 A3 R10.

**A-2 #2l · AR multiple_choice "Other", mic, prediction prefill** — P2 · Phase 2 A3 R11.

**A-2 #2m · AR card chrome** — P2 · project badge, persona badge, abstract block, `📋` indicator, prediction hint · Phase 2 A3 R14.

**A-2 #3a · TTS visibility rule** — P2 · visible iff an item exists or the toolbar button is active, evaluated on load, every queue mutation and every toolbar toggle · Phase 2 A4 B1 / item 1.

**A-2 #3b · TTS separate Pause and Play buttons with disabled states** — P2 · Phase 2 A4 T1 / item 4.

**A-2 #3c · TTS manual pause blocks queue advance** — P2 · depends-on: hard A-2 #3b · Phase 2 A4 T1 / item 4.

**A-2 #3d · TTS body header counts active + pending; bar title follows Paused / focus with `.paused` / `.focus-mode`** — P2 · depends-on: hard A-2 #3b · Phase 2 A4 B7 / item 3.

**A-2 #3e · AR→TTS deferral coupling, TTS side** — P2 · Phase 2 A4 item 6 · shared with #2d.

**A-2 #4 · Notifications** — P2 · depends-on: hard A-0 (scroll to row), hard A-2 #6 (the opt-in persist key), seq A-2 #1 (toolbar file)
Toolbar entries gain `sectionIds: string[]`, persisted key = the first id (`notifications-pane`), so `💬` hides the header region and the pane (§6a 9). Plus: TTS playback expands the sender card and its date accordion and scrolls to the row; Clear All's confirm names the count and window; Recent Activity's open state persists through A-2 #6's opt-in key (`commons-recent-activity-body`, §6a 10); card count reads "N new"; the conversation-mode button gets its solo pair. Badge wiring is **not** here — it is A-2 #11. Source: Phase 2 A5.

**A-2 #5 · Fleet Status size-cap dial** — P2
Ceiling and value from the arbiter; readout on input; saved on change with the control disabled while saving; repainted from the server's answer. Source: Phase 2 A6.

**A-2 #6 · Opt-in persisted collapse; Finished Tasks uses it** — P1 · depends-on: none
`wireSectionCollapse` gains an **opt-in** persist key stored through `ViewStateStore`'s accordion flags; only Finished Tasks passes one here. Do **not** extend persistence to the other seven consumers. Update `templates/sectionHeader.ts:12-15` to "session-only unless the caller opts in, mirroring legacy's `LUPIN_ACCORDION_PERSIST_KEYS`". Lands before every Phase B section (§7 edge 2). Source: §6a 10, Phase 2 A7 B3.

**A-2 #7 · Task List counts + truncation banner** — P2 · depends-on: hard A-2 #0
The live/parked/total count split, and the truncation banner with verbatim server warnings. Row controls moved to A-2 #0; `query_unavailable` **struck** (§6a 11); verb order unchanged (R4). Source: Phase 2 A8.

**A-2 #8 · Holding Area flow-ratio cluster + truncation banner** — P2 · depends-on: hard A-2 #0
Flow-ratio readout with threshold and window sliders, Freeze manager pull, Reset, source line, writes on change only; the truncation banner from this pane's query. The `⟳` no-handler defect is legacy's and is not back-ported (§4). Source: Phase 2 A9.

**A-2 #9 · Epic Board: wire the painted controls** — P1 · depends-on: hard A-2 #0
Disclosure, detail, id-copy and verb clicks plus a `change` listener; header bulk collapse and expand; close disclosed rows when a group collapses. Wire into the existing delegated seam (`EpicBoardRenderer.ts:142` click, `:143` keydown) and the shared verb table — no second verb list. Early, in the fixes phase (R5). Source: Phase 2 A10.

**A-2 #10 · Jobs** — P2 · depends-on: seq A-2 #1 (the `📋` glyph change)
The lead's glyphs and labels; empty-pane copy; delete-all confirm naming the queue and window; history excludes ids live in Done or Dead. Buckets-start-collapsed is **struck** (R2). Source: Phase 2 A12.

**A-2 #11 · Badge wiring and reveal** — P2 · depends-on: hard B-3 (the reveal target), hard A-0, hard A-2 #1 (the button to re-light)
Wire the two inert filter badges (`NotificationsHeaderRenderer.ts:198-201`, `JobsPaneRenderer.ts:257-263`): un-hide Filter Settings, re-light its toolbar button, save visibility, smooth-scroll through A-0, `stopPropagation()` so the host section does not collapse. Scheduled immediately after B-3 (§7 edge 1, R7). Source: Phase 2 A11 B6.

### Phase B — top to bottom

**B-0 · Pre-allocate the seven mount slots** — P1 · depends-on: hard A-2 #6; seq all Phase A rows except A-2 #11
One commit, by an implementer: seven ids, seven testids and seven insertion points across `multiplexer.html` and `boot.ts`, in the lead's page order — including whether Q&A sits above `action-required-section`. Seven lanes appending to one slot would conflict. Also allocates the seven toolbar buttons (plan §2 (b)). Source: §6 item 7, §2 (a).

**B-1 · Q&A Interface** — P2 · depends-on: hard B-0
Every row id in plan §2 B-1: B1–B4, B7, B10, Q1–Q6. Plus: the WebSocket writer for `#response-text` (`tts_job_request` via `QueueTransport.ts:270-275`); `AudioStore.ttsMode(): "instant" | "reliable"`, default `"instant"`, not persisted, set by the `#tts-mode` select, read by every playback path including the TTS queue; `ttsMode` written into `JobCompletionCache` metadata and never read back — replay uses the current mode; the new `reliable` branch, which B-7 reuses (§6a 3). `#qa-metrics` renders from B-1b's capture points. The Firefox UA hack is not ported. Mount handshake at `boot.ts:295-320` is this Implementer's.

**B-1b · TTT/TTFA/RTT capture points** — P2 · depends-on: hard B-1
Own the capture points in `AudioStore` and `wireTtsPlayback` that B-1's `#qa-metrics` renders (Q7; legacy `:3133`, `:4040`, `:4365`, `:4422`, `:4765`). Source: §6a 12.

**B-2 · Submit Agentic Jobs** — P2 · depends-on: hard B-0, hard A-2 #1 (frees `📝`, R6), seq B-1b
All 38 behaviours in plan §2 B-2: B1–B4, B7–B10, J1–J30. Rulings: `websocket_id` = `queueSessionId` on every submit (§6a 13 — legacy's CC card sends an unassigned `this.sessionId`; do not back-port); the auto-fix box renders unchecked and stays unchecked if config fails (§6a 14). Work inside the row: extend `ApiClient` with a headers hook for `X-Session-ID` (J14). Wire contract `claude_code_queue.py:24-43`. ⚠️ See open question 2 on splitting.

**B-3 · Queue Filter Settings** — P2 · depends-on: hard B-0, hard A-0, hard A-2 #1 (hidden-by-default set), seq B-2
Plan §2 B-3: B1–B4, B7, F1, F3; B6 is A-2 #11. `NotificationStoreOptions.isAdmin: () => boolean`, required, no default, plumbed through `CreateStoresOptions` (`stores/index.ts:159`) and supplied at `boot.ts:227`; `setFilterMode` on a non-admin refuses — no state change, persist or emit, one `console.warn`, returns `false` (§6a 15). Legacy's long button labels (F1). `jobViewFilter.ts` measured MATCH, not owed (§6a 16).

**B-4 · Time Saved** — P2 · depends-on: hard B-0, seq B-3
Plan §2 B-4: B1–B4, B7, B8, B10, T1–T6. Build both calls on `ApiClient`, not bare `fetch` (T6); a non-2xx leaves prior values; a throw only logs.

**B-5 · System Status (multiplexer)** — P2 · depends-on: hard B-0, seq B-4
Plan §2 B-5: B1–B4, B7, B8, B10, S1–S3, S6–S8. S8's reload is admin-only per R8, disabled and dimmed in flight, green `✓` or red `✗`, re-enabled in a `finally`. **No AC may claim `/api/init` is secured** — this gates the button, not the endpoint. S4 (legacy alerts on a failed missed-reset) is a residual, not owed.

**B-5L · `/api/init` reload gate — legacy half** — P2 · depends-on: none
Rick's R8 amendment: the same admin-only gate in `notifications.js` / `notifications.html`. New scope, not a back-port. Per §6a C5, runs `src/tests/unit/notifications_js/` and the legacy E2E files in its halves **per item**. Split from B-5 because it edits a different client and carries a different test bar.

**B-6 · Debug Information + `debugSink` funnel** — P2 · depends-on: hard B-0, seq B-5
Plan §2 B-6: B1–B4, B6 (never auto-reveals), B7, B8, B10, G1–G8. Add `shared/debugSink.ts` with `log` / `error` / `wsDiag`; every call still writes to the console. Route the console calls in live modules (`boot.ts`, `JobCompletionCache`, `JobsPaneRenderer`, `CommonsActivityRenderer`, `AudioTransport`, `TaskListRenderer`, `NotificationsListRenderer` — 40 at §6a's count; re-measure); `SequentialAudioManager` and `TtsAudioCache` move with B-7; the testkit calls stay (§6a 4). Error lines stay styled like info (G8).

**B-7 · Direct TTS Test** — P2 · depends-on: hard B-6 (D2 refuses into the debug log, §7 edge 4), hard B-1 (`AudioStore.ttsMode()` and the `reliable` branch, D3)
Plan §2 B-7: B1–B4, B7, B10, D1–D9. The direct-play path — cache lookup then blob play, never enqueue — gives `TtsAudioCache` and `SequentialAudioManager` their first production caller; move their console calls onto `debugSink` here. `Stop Audio` tears down all four audio paths. Guard the listener wiring (D9).

## Adjacent — owed by the plan, outside the build

| Row key | Title | Pri | Why it is here |
|---|---|---|---|
| X-1 | `/api/init` is unauthenticated and can swap the DB connection | P1 (suggested) | Plan §2 B-5 and R8: *"The server-side hole is out of scope here and owed its own row."* Server work (`system.py:275-280`). No owner is named in the plan |

Not listed, because they are already filed or conditional: the three legacy defects (`5378dd4e`); the legacy CC card `this.sessionId` defect (§6a 13 — confirmed in `bug-fix-queue.md` under "Legacy Claude Code submit card sends `websocket_id: this.sessionId`"); Firefox instant playback (§6a 3 — a row only if it is still broken).

## Open questions for Mr. Radio 🦉

1. **#2d and #3e describe one coupling from two ends.** §6b gives #2d a hard edge on A-2 #3 and calls #3e "shared with 2d". Minted as written, two Implementers can each build the deferral. Recommendation: keep both rows, #3e exposes the "TTS is playing" signal, #2d consumes it; the edge above already says so.
2. **B-2 carries 38 behaviours in one row**, the same shape §6b split A-2 #2 and #3 for, because §5 binds per item. Recommendation: split by card — B-2 section + CC, B-2R Research (with the `ApiClient` headers hook), B-2T Test Suite, B-2F TFE — which leaves the row count at 47.
3. **B-0's seq edge on "all Phase A"** is Rick's ordering, not a seam. If lanes run in parallel, only the hard A-2 #6 edge binds.
