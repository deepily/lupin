# Guard audit — the sixteen test files added 2026-09-01, run against broken arms

**Rio ⚡ · 2026-09-01 · pinned sha `3500696d` · detached worktree, all three pins**

## Why

Sixteen test files landed on 2026-09-01 carrying 155 tests. Three had ever been run
against a broken arm. A guard that has only been seen passing is a guard nobody has
measured — coverage says a line RAN, never that the test could have noticed it
running wrong.

## Method

Detached worktree at the pinned sha, `LUPIN_ROOT="$PWD"`, `PYTHONPATH="$PWD/src"`,
venv symlinked, primed then purge-and-reconverted (a fresh tree verifies checked-hash
**vacuously** — there is nothing there to judge). The verifier's `scanned roots` line
was read to confirm it described this tree and not the main repo.

**Baseline first: 154 passed, 1 skipped, 0 failed.** Every verdict below is against a
green baseline, so `rc == 1` means a passing test now fails rather than a pre-existing
red being recounted.

Each arm asserts it APPLIED — anchor matched exactly once AND the on-disk sha changed
— before its result is read, and restores by `cp` from a backup, never
`git checkout -- <path>`. `rc` 2/4/5 is could-not-run, never a kill.

## Result: 12 broken, 3 unresolved, 1 not yet attempted

| guard | subject mutated | verdict |
|---|---|---|
| `test_ws_audio_session_id_is_not_shared_by_every_client` | `notifications.js` audio id → queue id | **BROKEN** 1 failed |
| `test_websocket_manager_doc_signature_parity` | renamed `emit_to_user` | **BROKEN** 2 failed |
| `test_masked_invariant_annotations` | typo'd the tag in `coverage_contention.py` | **BROKEN** 3 failed |
| `test_flow_ratio_hidden_attribute_is_honoured` | `[hidden] { display:none }` → `opacity:0` | **BROKEN** 1 failed |
| `test_flow_ratio_mount_invariants` | env var names a path nothing mounts | **BROKEN** 4 failed |
| `test_flow_ratio_gate` | `ratio < allow_below` → `>` | **BROKEN** 19 failed |
| `test_epic_key_guard` | `startswith` → `endswith` on the epic prefix | **BROKEN** 4 failed |
| `test_flow_ratio_settings` | fallback drops `OVERRIDE_SUBDIR` | **BROKEN** 2 failed |
| `test_flow_ratio_settings_endpoint` | `MIN_ALLOW_BELOW` 0.0 → −99.0 | **BROKEN** 1 failed |
| `test_flow_ratio_endpoint` | `get_flow_ratio`'s own verdict, two branches | **BROKEN** 4 and 2 failed |
| `test_closed_vs_new_counts` | `%->done` → `%->dropped` | **BROKEN** 3 failed |
| `test_commit_scope_guard_annotated_paths` | broke `_ANNOTATED_PATH_RE` | **BROKEN** 2 failed |
| `test_commit_scope_guard_ownership_semantics` | `return None, {}` → `return set(), {}` | UNRESOLVED — survived one arm |
| `test_no_literal_route_is_shadowed_by_a_parameterised_sibling` | route motion | UNRESOLVED — see below |
| `test_task_routes_resolve_literal_paths` | route motion | UNRESOLVED — see below |
| `test_notify_idempotency_midconnect_smoke` | — | not yet attempted |

**Zero guards were shown to be unbreakable.** The three unresolved are unresolved
because of the harness, not because the tests held.

## THE FINDING THAT IS ACTUALLY WORTH THE EVENING

**Eight arms survived. Five of those eight were my aim, not their tests.** Sorting
them is the whole job, and reading the test body would have sorted none of them.

| survivor | what it really was |
|---|---|
| `OVERRIDE_SUBDIR` renamed | **equivalent mutant** — the test builds its expected path FROM the constant, so both sides moved together. The guard catches the defect that matters (the fallback dropping the subdirectory) and reddens 2 tests on that arm |
| three arms on `ratio_gate_advisory` | **mis-aimed** — `test_flow_ratio_endpoint` targets `get_flow_ratio`, the read-only report, not the create gate. Nothing I mutated was on its path |
| route motion, first attempt | **broken harness** — I anchored on the first `"/tasks/{task_id}",` in the file, which belongs to a different method's route, and moved flow-ratio from index 7 to index **5**. It went UP. Both guards survived a mutation that did not exist |

⇒ I only caught the last one by probing the mutated app for what it actually
registered instead of trusting that my edit did what I described. **An arm that
does not do what its label says is indistinguishable from a test that cannot see.**

⇒ The second attempt at the route motion produced `rc=2` — a syntactically broken
file, could-not-run. Recorded as UNRESOLVED rather than as either verdict. (Prior
evidence exists and is cited as prior, not as tonight's: commit `e2eb1306` measured
both guards red on this defect once `PYTHONPATH` was pinned.)

## The one genuine gap found

`MAX_ALLOW_BELOW = 1000.0` can be lowered to `10.0` with all 14 endpoint tests still
green. The tested values are 1.4, 1.25, 0.8, 1.9, 1.0, −1 and `"banana"` — **nothing
between 10 and 1000, so the upper clamp is asserted nowhere.** The lower clamp IS
guarded (`MIN_ALLOW_BELOW` reddens a test). Small, real, and cheap to close with one
parametrised case.

## What this does not establish

A pass reports on the mutations someone thought of. Twelve guards broke under ONE
arm each; that is evidence they are not blind, not evidence they are complete. A
second harness aimed at the same files would find different things — that, and not a
matching sha, is the argument for one.
