# PARKED — email_service return_type + the smoke-test swallow

**Parked 2026-09-06 ~19:55 EDT** on Rick's broadcast: 4% of the weekly token allotment
remains, all non-P0 work stops and is documented for tomorrow. This row was **P2**.

**Seat**: Maya 🌻 `597b9c6b` · **Manager**: Mr. Radio 🦉 · **Branch**:
`wt-maya-email-return-type`, tip **`1de3cc82`**, off `wip-v0.2.1-2026.08.29-cjflow-v2-followup`
@ `b6031094`. **Not pushed. Not merged. Tree clean** (only the borrowed `node_modules` link).

## THE WORK IS DONE. Only a confirmation run was outstanding.

| sha | what |
|---|---|
| `612b0010` | `return_type="bool"` → `"boolean"` at `email_service.py:148` and `:207`, + a guard driving the **real** `ConfigurationManager` |
| `e5025f62` | config-shaped errors leave `test_email_configuration` instead of being flattened to `False`, + a guard with a discriminating twin |
| `1de3cc82` | fixes a defect **I** introduced — see below |

## What was actually wrong, and what was NOT

🔴 **The claim that reached me was wrong about the mechanism and must not propagate.** It
said a config type string "may be reporting a security setting OFF, forever, silently".
**It does not.** `"bool"` **RAISES** `ValueError` (`configuration_manager.py:913`); `get()`
calls `_get_typed_value` on **both** branches (`:801` present, `:815` absent), so no path
reaches a fallback quietly. **`smtp_use_tls` was never assigned at all** — the function
aborted before any TLS branch. There was no wrong value; there was no value.

**The real symptom** was sharper: the broad `except` turned the raise into `False`, and
`quick_smoke_test` read that as "SMTP not configured", printed *"This is normal in
development environment"*, and **returned True**. A structural defect passed as a healthy
smoke test with its own error text on screen the whole time.

## Receipts

**Baselines taken FIRST, as SETS** — this tree is not green and `rc==1` means nothing on it:

- unit @ `b6031094` — 3 failed / 22,915 passed. All pre-existing, none mine:
  `test_secret_scan::test_a_detector_change_forces_a_full_rescan` (rotation hold) ·
  `test_secret_scan::test_the_recorded_counts_are_derived_from_the_same_scan` ·
  `test_terraform_invariants::test_terraform_provider_cache_is_present` (worktree artifact)
- cosa @ `b6031094` — 8,800 passed / 26 skipped / **0 failed**, failing set EMPTY

**The unguarded arm** (Mr. Radio's instruction): both `return True` paths in
`quick_smoke_test` inverted, **both tiers run in full** → failing sets **identical** to
baseline, `comm` empty both directions, both tiers. ⇒ **31,715 tests and not one noticed.
PRESENT AND UNWATCHED.** A break list proves *unwatched*, never *absent*. The name search
could not have settled it: **520 files** mention `quick_smoke_test`.

**Mutation arms** — all restores byte-exact by sha, caches purged with `purge-pycache.sh`
between every arm:

| arm | result |
|---|---|
| revert ONE site to `"bool"` | 1 failed; the parametrize id **names the literal**: `...accepts[bool]` |
| empty the extraction | floor control fires — **and 4 passed became 1 SKIPPED**, so an emptied corpus *deletes* the arms rather than reddening them |
| revert the swallow fix (too NARROW) | the two **defect** tests red, the two connection tests **green** |
| make the catch too BROAD | the two **connection** tests red, the two defect tests **green** |
| regress the AST back to a regex | **two independent detectors** fire |

**Opposite arms, opposite kills, no overlap** — without the too-broad arm I could have
shipped a smoke test that never passes.

## 🔴 The defect I introduced, and why neither commit caught it

`612b0010`'s guard extracted its corpus by **regex over raw source**. `e5025f62` then added
a comment to that same file explaining the defect — containing the string
`return_type="bool"`. **The guard failed on its own documentation.**

**I ran guard 1 before commit 2 existed, and guard 2 alone after it. Neither run contained
both changes.** The defect lives in the *interaction*, and only the full tier found it.
**Two green per-file runs are not one green tier.**

`1de3cc82` parses with `ast.walk` over `ast.Call` so prose is excluded **by construction**,
and adds a second detector asserting `"bool"` is absent, naming why.

## ⚠️ OUTSTANDING — pick this up first tomorrow

1. **The final unit tier at `1de3cc82` was still running when I parked.** Expected: the set
   returns to exactly the three pre-existing reds. **Not confirmed. Re-run before merging.**
   `LUPIN_ROOT="$PWD" PYTHONPATH="$PWD/src" .venv/bin/python -m pytest src/tests/unit/ -q`
2. **Not pushed, not merged.** Merge is somebody's call, not mine.

## ⚠️ Recorded, NOT fixed — out of scope, wants its own decision

With the type string correct, `test_email_configuration` now gets past the config and makes
a **real SMTP call** — run outside a network guard it reaches Gmail and returns
`535 Username and Password not accepted`. **That function is collected as a test**: the cosa
suite imports it by name (`test_email_service.py:19-20`) and pytest runs it, emitting
`PytestReturnNotNoneWarning`. The cosa tier sets `network=block` and measured **0 outbound
connections**, so it is contained *there* — and nowhere that guard is absent.

## Also dropped on the same broadcast

- **pocholo 📣's review** of `pocholo-land-orphan-sweeper` (tip `95a5f1aa` when last told).
  Two items he ranked: `expected_state` for delivered (**first** — if wrong, the UPDATE
  matches zero rows forever and the sweeper becomes a silent no-op reporting success), then
  the smoke-test gap. Plus a one-line verify of `notification_expiry_sweeper.py:31-35`.
  He is **not blocked** — sweeper ships disabled, nothing merged.
- **Rio ⚡'s review** — queued, never started.
