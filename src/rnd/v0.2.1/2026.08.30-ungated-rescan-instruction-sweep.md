# The other voices giving the refused instruction — a frozen sweep

**Maya 🌻, 2026-08-30.** Swept at **`39d912ec`**, the merged tip. **Nothing has been changed.**

**Why this is dated and sha-pinned**: Clayton 😎 is running an independent search over the same
question. This list is frozen **before** his lands so the comparison means something — a list
edited after seeing his findings cannot tell anybody whether two searches agree.

---

## THE QUESTION

Rachel 🕊️ found `TODO.md` instructing the exact re-scan the rotation hold refuses — a fourth
voice giving the refused instruction, in prose nobody thought to gate. The hold refuses **recording
a scan**: updating `detector_sha256`, `scanned_ref_sha`, `scan_fingerprint`, the counts, or
`real_findings` while the postgres credential is unrotated.

So: **what else, anywhere in the repo, tells a reader to do that?**

## WHAT I SWEPT

Git-derived throughout (`git grep`), never `find` — `src/cosa/.venv` holds 29,303 vendored `.py`
files and would have inflated every count ~13× with third-party code.

| pass | query |
|---|---|
| field names | `detector_sha256\|scanned_ref_sha\|scan_fingerprint\|real_findings` |
| phrasing | `re-?scan\|clear the red\|record the scan\|last_full_scan\|full scan` |
| instruction verbs | `re-?record\|record the (result\|scan\|triage)\|update the (fixture\|record)\|paste .*(sha\|fingerprint)` |
| by subject | every file matching `secret_scan\|rotation hold\|ROTATION_HOLD\|ROTATION_HELD` — 30 files, read individually |
| render sites | every quoted string in `test_secret_scan.py` carrying an instruction verb |

---

## IN CLASS — GATE THESE

### 1. `src/tests/unit/test_secret_scan.py:504-505` — the red's own lead sentence

The rescan red opens by giving the refused instruction:

> `"…and re-running it here does not match what is on record. `**`Re-scan, TRIAGE the output, and record the result`**` — the fingerprint is measured by this test, so it cannot be filled in without scanning:"`

**It is ungated.** Rendered live at `39d912ec`, it prints **3 lines above** the WITHHELD block and
**11 above** the refusal:

```
E   AssertionError: THE DETECTOR CHANGED SINCE THE LAST RECORDED FULL SCAN, and re-running it
E   here does not match what is on record. Re-scan, TRIAGE the output, and record the result —
E   the fingerprint is measured by this test, so it cannot be filled in without scanning:
E         (THE RECIPE FOR CLEARING THIS RED IS WITHHELD while the rotation hold below
E          is active — it ends by telling you to update the record, which is the one
E          thing that hold refuses. …)
E         detector_sha256   : 8675ec7a…
E         …
E     🔴 DO NOT CLEAR THIS RED BY RECORDING A SCAN.
```

**This is Rachel's finding one sentence higher up.** She caught the recipe *list* being printed
beside the refusal; the fix gated the list. The sentence **introducing** the list was not gated, so
the red still argues with itself — and it argues in the worst order. Mr Radio's reading, and it is
the right one: **a reader acting on the first instruction found never reaches the refusal.**

**Call: GATE.** Route the sentence through the hold the way `_rotation_held_notice()` already is —
one condition, no second flag.

### 2. `src/tests/unit/test_secret_scan.py:270-276` — the withheld block's own pointer

The gate that suppresses the recipe ends by naming where to read it:

> `"read it in the record's `_how_to_clear_the_red`, and it prints here again once rotation lands."`

`_how_to_clear_the_red` in the fixture is **unrefused prose** — six steps, the fourth being
*"update detector_sha256, scanned_ref_sha, scan_fingerprint, the counts and real_findings"*. So the
gate currently **redirects the reader to an ungated copy of the thing it just refused.**

**Call: GATE, at the pointer.** Make the redirect carry the refusal with it, rather than handing
over a clean destination.

🔴 **RULED BY MR RADIO, AND IT CHANGES WHERE I CUT.** My first proposal was a `_hold` key inside
the fixture. He refused it, and he is right: **the fixture is the record of what was scanned and
what the then-owner said to do about it. Rewriting `_how_to_clear_the_red` inside it destroys
evidence to fix a display.** Suppress at the render site; never edit a recorded string.

---

## ⚠️ AMENDED AFTER THE FIXES LANDED — THE RESIDUAL IS CLOSED, NOT DECLARED

**The list above is FROZEN as swept, at `39d912ec`.** This section is appended rather than
folded in, so the comparison with Clayton 😎's independent search still means something.

**Mr Radio ruled the second half of the question I had left open**: adding a **new key** to the
fixture is **annotation, not rewriting the record** — so the direct-reader hole below closes
instead of standing as a stated residual. His original limit is unchanged and is what makes the
distinction work: **never edit a recorded string; adding beside one is fine.**

**Three fixes, all at the render site except the annotation:**

| # | where | what |
|---|---|---|
| 1 | `_rescan_lead_sentence()` | the lead sentence goes through `_rotation_hold_is_active()`. The **fact** ("cannot be filled in without scanning") survives in both states — it is what stops a paste; only the **imperative** is suppressed |
| 2 | the withheld block's pointer | now names `_rotation_hold` at the destination, so the redirect carries the refusal instead of handing over a clean copy |
| 3 | the fixture | a new `_rotation_hold` key **beside** `_how_to_clear_the_red`. **22 keys → 23, zero recorded values changed, `git diff` = 1 insertion / 0 deletions** |

**MEASURED — five arms, each mutation killed by a NAMED test, not by an exit code:**

| arm | failing set | verdict |
|---|---|---|
| baseline | the 2 rotation reds | — |
| `_rescan_lead_sentence` ignores the hold | + `test_while_the_hold_stands_the_red_gives_no_instruction_to_re_scan_or_record` | **KILLED** |
| the pointer stops naming the key | + `test_the_withheld_block_does_not_send_the_reader_to_an_ungated_recipe` | **KILLED** |
| the fixture annotation removed | + that test **and** `test_the_records_own_annotation_refuses_the_recipe_it_sits_beside` | **KILLED** |
| restore control | back to the 2 rotation reds | clean |

`rc == 1` was **not** taken as a kill on its own — the baseline is red by design here, so every arm
is judged on the failing **SET** growing by a named test. That is the trap from §TESTING VENUES
landing in exactly the tree it warns about.

**Both flag positions: 2 failed / 71 passed.** The two are the deliberate rotation reds.

### 🔴 KRISHNA 🦚 CAUGHT MY OWN ITEM 1, ONE SURFACE OVER

He verified the annotation claim independently — 1 insertion / 0 deletions, 22 keys → 23, and
`_how_to_clear_the_red` hashing `ca24c3e87bf3717e` at **both** `39d912ec` and `01516300`, so no
recorded string moved. **I re-derived that hash after the move and got the same value**, which is
what makes it a check rather than a courtesy.

Then he found the placement. **I had put the annotation BESIDE the recipe**, which reads as tidy
and is item 1 wearing the fix's own clothes:

| position | what a reader met, in order |
|---|---|
| line 2 | `_why` — ends *"goes red until somebody re-scans and records the result here"* |
| lines 3–10 | the six recipe steps |
| line 11 | the refusal |

**The reader met the refused action twice before the refusal.** That is the same ordering failure
as the rendered red — *a reader acts on the first instruction they find* — and I reproduced it in
the fix for it, in the same evening, having just written the sentence naming it.

**Moved to first, and the order is now ASSERTED.** Krishna found nothing pinning key order, and
Mr Radio's framing is the reason it needed a test rather than a commit: **a fix nothing defends is
one the next person re-breaks by adding a dict entry in the natural place — at the end.**

| arm | failing set | verdict |
|---|---|---|
| annotation back below the recipe (position 3) | + `test_the_records_own_annotation_leads_it_and_refuses_the_recipe_below` | **KILLED** |
| annotation appended last (position 23 — the natural place) | + the same test | **KILLED** |
| restore control | the 2 rotation reds | clean |

⇒ **Position is load-bearing in a file whose failure mode is somebody acting too early.** Both
flag positions still 2 failed / 71 passed.


## THE RESIDUAL — NAMED, BECAUSE NEITHER GATE REACHES IT

Under the render-site limit, **a human who opens the fixture JSON directly is reached by nothing.**
Two strings there give the refused instruction with no render site to gate:

| path | string |
|---|---|
| `src/tests/unit/fixtures/secret_scan_last_full_scan.json` → `_how_to_clear_the_red[3]` | *"update detector_sha256, scanned_ref_sha, scan_fingerprint, the counts and real_findings"* |
| same file → `_why` | ends *"…goes red until somebody re-scans and records the result here"* |

I am stating this rather than claiming coverage. **Fixing item 2 narrows the path to this file; it
does not close it.** Whether that residual is worth a separate control is Mr Radio's or Rachel's
call, not mine — and the alternative I can see (a `_hold` key) is the one already refused, for a
reason that outranks the residual.

---

## INSPECTED AND RULED OUT — with the reason, so a comparison can disagree with me

### `src/tests/unit/test_secret_scan.py:840-847` — the branch-triage red

**Split verdict, and the second half is a judgement I should not make alone.**

- It **renders `steps`**, so the pointer problem in item 2 reaches it too — **covered by fixing
  item 2**, no separate change.
- Its own inline instruction — *"triage it and add its row to `branch_accepted`"* — is **not** in
  the refused class **as I read the hold**: `branch_accepted` is branch triage, a different field
  from the five the notice names.

⚠️ **That reading could be wrong, and it is a question about the hold's SCOPE, not about this
file.** If the hold covers *any* write to the record, this line is in class. Flagging rather than
deciding.

🔨 **RULED — Mr Radio 🦉, 2026-08-30, and it was not open in the first place: he had ruled it
earlier and the ruling stands.** *Adding a triaged row with a one-line reason is TRIAGE, which the
hold's own notice asks for.* Line 840 is **OUT of the refused class.**

**And the code already said so — the assert is its own confirmation, which is the part worth
keeping.** It ends `+ steps`, appending the gated recipe. So **the assert's recipe half sits behind
the gate and its triage half correctly does not**, in one message. Re-read at `01516300`:

```python
+ "\n\nIf one is real, remove it and read it from the environment or the secret store. "
  "If it is a false positive, triage it and add its row to branch_accepted AS A KEY "
  "WHOSE VALUE IS A ONE-LINE REASON — a bare row is refused:\n"
+ steps                                   # ← the gated half
```

⇒ **The distinction the hold draws is not "writes to the record" versus "reads".** It is
**TRIAGE versus RECORDING A SCAN.** Triage is a human judgement the hold actively wants; recording
a scan is the thing it refuses. I had the boundary in roughly the right place and could not say
why — this is the why, and it is worth more than the verdict.

### Historical records — LEAVE

| path:line | text |
|---|---|
| `src/rnd/v0.2.0/_archive/F/2026.08.17-review-of-the-secret-scanner-precommit-control.md:89` | *"Suggested close: record and re-scan the working branch tip as well as `origin/main`."* |
| `src/rnd/v0.2.1/2026.08.30-two-guards-one-condition-opposite-remedies.md:21` | quotes Tiberius's since-removed *"Re-record them from the SAME scan"* |

Both are **dated records of what was said**, one of them inside the retrospective explaining that
the instruction was wrong. **Gating history is how a record stops being one.** Commit messages fall
under the same call and are immutable regardless.

### `TODO.md:45` — Rachel's, not mine

Already carries her warning at `:36`. Left alone per assignment.

### Clean on inspection

`src/scripts/secret_scan.py` · `src/scripts/pre-commit-secret-scan.py` · `CLAUDE.md` ·
`src/tests/README.md` · `README.md` · `.claude/` · `src/docs/` · `src/workflow/`

`CLAUDE.md:964` and `src/tests/README.md:83` both **name** the rescan test, but discuss guard
placement and local-green respectively. Neither tells anyone to re-scan.

---

## WHAT THIS SWEEP CANNOT TELL YOU

**It reports on the queries I thought of.** That is the same limit a mutation pass has, and it is
the reason Clayton is searching independently rather than reviewing this list — two searches over
one question find different things, and that difference is the finding. A match between us
establishes only that we ran similar queries.

**Send the disagreements, not the confirmations.**
