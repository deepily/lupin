# Sword of Damocles — enforcing it at the request door

**Status**: BUILT 2026-09-15 — steps 1–7 done on `tiberius/sword-of-damocles-enforcement` (rebased onto the working branch at `affd98fe`); switch default ON; awaiting review and the gate. Steps 1–3 by Tiberius, 4–7 by Mr. Radio (skeleton crew, no builder seat). Written 2026-09-14 ~22:40 EDT by Tiberius 👑 (author seat for Mr. Radio 🦉), read at `993be2b6`.
**Row**: `ab8c5728` (owner Mr. Radio). **Trial doc it replaces**: `planning-is-prompting/workflow/sword-of-damocles.md` (TRIAL v0.2).
**Build gate**: Mr. Radio's "go", after Rick answers §4.

## 1. Rick's ruling (2026-09-14 ~22:32 EDT, verbatim)

> I think we promote it and make it make your promotion request provisional upon you supplying a ticket to delete And it better be yours So if you're asking to add 1 the method for requesting 1 of me then requires that you pass in A ticket ID that belongs to you That seems pretty symmetrical Tint for tat I think you should draw up a plan and you should implement it So no I will not retire the trial, And you will make it runtime configurable so I can turn it on or off as I see fit

**As a rule**: an `admit` request must name one deletion ticket, owned by the requester. No ticket, no request. Rick can switch the rule on and off at runtime.

## 2. What exists today (read at `993be2b6`)

| Piece | Where | What it does |
|---|---|---|
| Request door | `src/cosa/rest/routers/tasks.py` → `file_request` (`POST /tasks/{task_id}/request`) | Body `{move, reason, actor}` (`RequestFileIn`, `extra="forbid"`). Checks, in order: blank reason 422 → row 404 → `refusal_for_filing` 422/409 → `manager_refusal` 403 → `refusal_for_refiling` 409 → `repo.apply_request_filing` |
| Verdict door | same file → `record_request_verdict` (`POST /tasks/{task_id}/request-verdict`) | Rick only. `approved` performs the move through `_apply_transition_under_lock`; any refusal rolls the verdict back. `denied` touches nothing |
| Request rules | `src/cosa/rest/task_request_lifecycle.py` | Pure functions: `refusal_for_filing`, `refusal_for_refiling`, `refusal_for_verdict`, badges |
| Request columns | `src/cosa/rest/postgres_models.py` → `request_state`, `request_move`, `request_ts` + two check constraints | Pinned by `src/tests/unit/test_task_request_columns_migration.py` |
| Repository | `src/cosa/rest/db/repositories/task_repository.py` → `apply_request_filing`, `apply_request_verdict` | Writes columns + one audit event; decides nothing |
| Runtime switch pattern | `src/cosa/rest/task_approval_settings.py` → `WRITABLE_KEYS`, `_BOOLEAN_KEYS`, `_VALIDATORS`, `current_settings`, `set_overrides`; `GET`/`PATCH /tasks/approval-settings` | INI default, overridden by `flow-ratio/task-approval-settings.json`. PATCH is operator-only (`caller_is_operator`, keyed on the login account), validates every key before writing, returns the read-back value |
| MCP verb | `src/lupin_mcp/task_store_tools.py` → `task_request_impl`; wired in `src/lupin_mcp/cosa_voice_mcp.py` | Transport only; the server decides |

## 3. Design

### 3.1 The switch

Reuse the approval-settings pattern exactly; no new endpoint.

| Item | Value |
|---|---|
| Setting key | `sword_of_damocles_active` (added to `WRITABLE_KEYS`, `_BOOLEAN_KEYS`, `_VALIDATORS`, `current_settings`) |
| INI key | `task approval sword of damocles active = False` — **held off** (Mr. Radio 22:48) until the container persona measurement passes; flipped to True only in the commit that shows it — in `[Lupin: Baseline]`, beside its sibling approval keys; Production and Development both inherit Baseline |
| Code fallback (key missing) | `False`, matching `FALLBACK_ENFORCEMENT_ACTIVE`. An unparseable override falls through to the INI, as the other booleans do |
| Reader | `get_sword_of_damocles_active()` |
| How Rick flips it | `PATCH /api/tasks/approval-settings {"sword_of_damocles_active": false}`. Lands on the next request, no bounce. `GET` shows value and source |

### 3.2 The request door

`RequestFileIn` gains `deletion_task_id: Optional[uuid.UUID] = None`. The MCP `task_request` verb gains the same optional parameter.

New pure function `task_request_lifecycle.refusal_for_pledge( move, switch_on, pledge, target_id, requester_persona, pledged_elsewhere )`, called after `manager_refusal` (so a non-manager learns nothing about other rows), before `refusal_for_refiling`:

| # | Case | Answer |
|---|---|---|
| 1 | `move=admit`, switch on, no `deletion_task_id` | **422** — names the rule and the setting, and says how to file |
| 2 | `move=demote` with a `deletion_task_id` | **422** — a demote asks for nothing, so there is nothing to pay (see Q3) |
| 3 | pledge id = the target row | **422** — a row cannot pay for itself |
| 4 | pledge row does not exist | **422** naming the id (404 is reserved for the target row) |
| 5 | pledge `owner_persona` ≠ requester's persona | **403** — "it better be yours" |
| 5a | the session bridge resolves **no persona** for the actor's session id | **403** naming it — never falls back to the typed actor string (María, 22:41; that string is row `b8205986`'s hole) |
| 6 | pledge is `done` or `dropped` | **409** — a finished row is a close, not a deletion (v0.2 ruling 1) |
| 7 | pledge already named on another row's **pending** admit request | **409** naming that row — one pledge per admit |
| 8 | switch **off**, admit with a pledge | accepted and honoured, same checks 3–7. Off means "not required", not "ignored" — a field ignored in silence is the failure this module's settings docstrings warn about |

**Requester persona**: resolved by the session bridge from the session id in `actor`, the same source the manager check reads — **never the typed persona name** (María asked, 22:40). ⚠️ Not yet read: which bridge helper returns the persona; step 4 finds it, and if none does, Mr. Radio hears that before anything weaker is used.

**Locking**: lock both rows with `get_by_id_for_update` in UUID order, so two crossing requests cannot deadlock.

### 3.3 Storage

New nullable column `request_deletion_id UUID` on `task_items`, plus a check constraint `request_deletion_id IS NULL OR request_move = 'admit'`, and a migration pinned the way `test_task_request_columns_migration.py` pins the existing three. `apply_request_filing` takes `deletion_id` and names it in the `request_filed` event reason. Serialized item carries it, so Rick's card and `task_get` show it.

Rejected: keeping the pledge only in the event text. The verdict would have to parse an audit string to find the row it drops.

### 3.4 The verdict door — see Q2 for the ruling this needs

## 3.5 Rulings received (Mr. Radio, DM 22:39 EDT)

Q1 drop the peer agreement · Q2 approve admits and drops in one transaction, deny touches nothing, a dead pledge gets 409 plus a re-file · Q3 demote exempt — all three as recommended below. **Admit requests pending before the switch are grandfathered**: approving one admits with nothing to drop (María's question, 22:40; the switch governs filing only).

## 4. Open questions for Rick, each with a recommendation

### Q1. Keep the peer-manager agreement? — **Recommend: drop it**

| | |
|---|---|
| For dropping | "It better be yours" removes the reason it existed: v0.2 let a manager offer a peer's row, so the peer had to agree. Owner-only pledges leave nothing for the peer to protect. It also cannot be checked by a door — a DM thread id is free text — and the friction log shows it was the slowest human step apart from Rick himself |
| Against | Loses a second pair of eyes on whether the pledge is an honest one rather than a freebie |
| Mitigation | Rick still sees the pledged row's title on the card and can deny |

### Q2. What happens to the pledged row on approve and on deny? — **Recommend: approve drops it in the same transaction; deny leaves it alone**

| Verdict | Recommended behaviour |
|---|---|
| **approved** | The admit and the drop happen together, under both row locks, through `_apply_transition_under_lock` for each. The drop's reason names the admitted row and Rick's verdict; its actor is Rick's recorded identity. If either transition is refused, both roll back and the request stays pending |
| **approved, but the pledge is no longer live** (closed or dropped since filing) | Refuse **409**, naming the pledge. The request stays pending. The requester re-files with a new pledge — which means `refusal_for_refiling` must allow a re-file over a pending admit whose pledge is no longer live, by the same requester. Otherwise the request is stuck |
| **denied** | Nothing moves, pledge included. Rick 2026-09-10: "No means take no action whatsoever." The pledge is freed for another request |
| **switch turned off between filing and verdict** | The pledge is still paid. The switch governs what a request must carry, not what a request already offered |

Alternative (not recommended): approve admits and leaves the drop as a second click. That is what the trial did, and it let the promote land without the drop (row `ab8c5728`, amendment 22:51 EDT 2026-09-10: the promote of `1657a852` had landed, the drop of `b52b430b` was "not yet confirmed").

Supersedes v0.2 ruling 2 ("promote stands if the deletion is refused"): with one verdict there is no separate refusal of the deletion. Rick denies the whole request and the manager re-files.

### Q3. Is demote exempt? — **Recommend: yes**

A demote moves a row **off** the board into the holding area; it adds nothing Rick has to fund. Charging for it would discourage exactly the tidying the rule wants. A pledge on a demote is refused 422 rather than ignored (case 2).

### Not asked, stated so it can be vetoed

- **Rick's own admit is untouched.** He admits directly through the transition door; only the manager request door is priced.
- **P0 petitions at create stay exempt** (v0.2 ruling 3) — they never enter the holding area.
- **UI**: Rick's request card should show the pledged row's id and title. That is TypeScript on the holding-area card and is a follow-on row unless Mr. Radio wants it in this build.

## 5. Build order (after go)

Worktree under `.claude/worktrees/`, `LUPIN_ROOT` and `PYTHONPATH` pinned. Tests first; `py_compile` after every `.py` edit.

**Progress (2026-09-14 22:46 EDT, branch `tiberius/sword-of-damocles-enforcement`)**: step 1 ✅ `162805eb` · step 2 ✅ `4e9c187d`. The rules moved into their own module, `src/cosa/rest/task_request_pledge.py`, because `test_nothing_in_this_module_can_touch_a_ticket` bars `task_request_lifecycle` from taking a row. Mutation check: dropping the owner comparison reddens `test_5_…[True/False]`, and dropping the switch branch reddens `test_1_…`. Restore verified by sha.
**Found for steps 3–4**: migrations are Alembic, in `src/migrations/versions/`, and the one to model is `8beada291153_add_task_request_columns.py`. The requester's persona comes from `session_bridge.get_voice_persona( session_id )[ "name" ]` in `lupin_cli/claude_code/hooks/lib/session_bridge.py`, with the session id from `rules.session_id_from_created_by( actor )`. ⚠️ This has not yet been checked server-side: `manager_figure.py` notes the persona-chain env is empty on the server and reads a static bridge field, so confirm `get_voice_persona` resolves inside the container before wiring it in.

| Step | Work | Tests written first |
|---|---|---|
| 1 | Switch in `task_approval_settings.py` + INI keys | reader (INI / override / unparseable / missing), `set_overrides` accepts and rejects `"false"` string, `current_settings` source, PATCH 403 for a seat key |
| 2 | `refusal_for_pledge` pure function | one test per row of §3.2, switch on and off |
| 3 | Column, constraint, migration, serializer | migration test in the existing style; constraint rejects a pledge on a demote |
| 4 | `file_request` wiring + locking + repo `deletion_id` | router tests for every §3.2 code, event reason names the pledge |
| 5 | Verdict: atomic admit + drop, 409 on dead pledge, re-file allowance | approve drops; deny leaves; dead pledge 409 + still pending; injected refusal on the drop rolls back the admit |
| 6 | MCP `task_request` param | transport test that the id is sent and a 422 surfaces verbatim |
| 7 | Docs | `src/docs/rest-api-reference.md` request rows; Mr. Radio updates `sword-of-damocles.md` to v1.0 |

### 5.1 Container persona measurement (2026-09-14 23:01 EDT)

`session_bridge.get_voice_persona( sid )[ "name" ]`, run inside each server container (`docker exec … python`, cwd `/var/lupin/src`), with the host run as a control:

| session id | `lupin-rest-dev` (:7999) | `lupin-rest-test` (:8000) | host |
|---|---|---|---|
| `323067e7` and its full uuid | Tiberius | Tiberius | Tiberius |
| `447b4713` and its full uuid | mr radio | mr radio | mr radio |
| `ee680ab6` | maria | maria | not run |
| `deadbeef` (negative control) | None | None | None |

**It resolves, and the reason is a host bind mount**: both containers mount `/home/rruiz/.claude/sessions` read-write from the host (7,618 files, the same count on both sides). So the server does read host bridge files, which is the dependency Mr. Radio named at 22:46. The manager-close identity in `routers/tasks.py` already relies on the same mount.

**Ruled (Mr. Radio, 23:02)**: the measurement passes. The switch **stays `False`** anyway, because steps 3–7 are not built: requiring a deletion ticket before the approval path can drop it would leave requests stuck. He also asked for a preflight guard on the mount, now added as probe 5c in `src/tests/smoke/test_container_preflight.py` (`test_session_bridge_mount_resolves_a_known_persona`) and `src/scripts/preflight-test-container.sh`. It passes on both containers. Three mutants were each caught by their own assertion: the directory missing, a persona mismatch, and a made-up id resolving. ⚠️ Run from a worktree, the shell preflight fails probe 5b (worktree bind mount) because it derives `HOST_ROOT` from its own location; the main-tree copy passes 5b. That is a worktree artifact that predates this change.

### 5.2 Steps 4–7 (Mr. Radio, 2026-09-15)

- **Step 4** — `file_request` locks target and pledge lower id first (`_lock_row_and_pledge`), resolves the requester via `_requester_persona` (approver account, else `get_voice_persona` on the actor's session id; never the typed name), calls `refusal_for_pledge` after the manager check, and stores `request_deletion_id`. A pending admit whose pledge died may be re-filed (`request_is_stranded_by_its_pledge`).
- **Step 5** — `record_request_verdict` peeks the pledge unlocked, locks both rows in the same order, 409s if the request was re-filed in between, 409s a dead pledge before writing (`refusal_for_consuming_pledge`), and on approval drops the pledge through `_apply_transition_under_lock` in the admit's transaction. Deny touches neither; a no-pledge admit is admitted alone.
- **Step 6** — MCP `task_request( …, deletion_task_id=None )`; the key is sent only when given.
- **Step 7** — route descriptions (`/docs`) and `src/docs/rest-api-reference.md` §26.
- **Mutation arms** (router, each restored by sha): drop skipped → 2 named tests red; stranded re-file ignored → 1; lock order unsorted → 2 (the high-id-target params); re-file race unchecked → 1; requester taken from the typed actor → exactly the 2 bridge-identity tests.
- **Named gap**: the lock-order tests assert the ORDER of `get_by_id_for_update` calls over a fake store; no test drives two real Postgres transactions against each other (AC5's concurrency is argued from the lock order, not measured).

## 6. Acceptance criteria

1. With the switch on, an `admit` request without `deletion_task_id` is refused 422, and the detail names the setting.
2. Every case in §3.2 returns its stated code, each proven by a named test.
3. `PATCH /api/tasks/approval-settings {"sword_of_damocles_active": false}` by the operator turns the requirement off on the next request with no server bounce; a seat key gets 403; `GET` reports value and source.
4. Per the Q2 ruling: approval admits the target and drops the pledge in one transaction, or neither happens.
5. Two concurrent requests pledging the same row: exactly one succeeds (row-lock test).
6. Migration applies and reverses cleanly; existing request rows stay valid.
7. **100% lines, branches and functions** on every touched module (`task_approval_settings.py`, `task_request_lifecycle.py`, `routers/tasks.py`, `task_repository.py`, `task_store_tools.py`), measured by the coverage gate on the build sha.
8. Mutation check on the two load-bearing lines: deleting the owner comparison reddens a named test, and deleting the "admit needs a pledge" branch reddens a named test.
9. Unit, cosa and coverage tiers green on the build sha; sha sent to Mr. Radio for review. No push, no merge by the author.
