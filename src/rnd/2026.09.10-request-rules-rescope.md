# Request rules 3–4 after Rick's "no means no action" ruling — what is left

**Row** c9fafb9d (rules 3 and 4) · **Ruling** decision row 8c83d7ce, amended 2026-09-10 ~17:28 EDT · **Written** 2026-09-10 ~19:20 EDT by Chloé 🗼 for Mr. Radio 🦉
**Read at**: the row and 8c83d7ce via `task_get` · branch `john-202-is-not-a-success` at `45ff6979` (12 commits, UNMERGED, read only — nothing merged, no scratch DB) · working branch `wip-v0.2.1-2026.08.29-cjflow-v2-followup` at `e87d9aff`

## 1. The ruling, verbatim

Asked whether a refused promote/demote auto-files a request or the manager files it separately. Rick chose neither offered option and typed (answered: true, default_used: false):

> "If I refuse a promote it stays where it was in the holding area If I refuse a demote it stays right where it was in the active queue How about that? No means take no action whatsoever"

Mr. Radio's reading on the row: a refusal files nothing and moves nothing. Auto-file (the recommendation) did not carry.

## 2. What rules 3 and 4 still require

| Rule | Requirement in force |
|---|---|
| **3. A manager may REQUEST** a promote or a demote | A way to **file** a request that **persists** on a queue Rick works from a board. No expiry, no `resolves_by`, no resolver, not the promotion ask |
| **4. A request defaults to NO** | An unanswered request authorises nothing. A timeout leaves it pending and writes no denial. Rick's "no" **leaves the ticket exactly where it was** |
| **Acceptance (row)** | A named test on the **real HTTP door** per rule, a positive arm, a mutation arm off a recorded green baseline, a refusal that **says why and names the request path**, 100% lines/branches/functions |

## 3. What already exists

**On the unmerged branch** (counts are `def test_` lines read from git, not a run; pass figures are Rio's, inherited):

| Piece | Where | State |
|---|---|---|
| Rules 1–2: Rick alone promotes and demotes | `2333a752`; `task approval approver personas =` emptied in `lupin-app.ini`; `test_rick_alone_promotes_and_demotes.py` (7) | Built |
| The rule as a function: states `pending/approved/denied`, a pending request grants nothing, silence changes nothing, only the operator answers | `task_request_lifecycle.py` (306 lines); `test_a_request_waits_forever_and_a_denial_closes_it.py` (20) | Built |
| Storage: `request_state`, `request_move`, `request_ts` on `task_items`, all nullable, three CHECKs | migration `8beada291153`; `test_task_request_columns_migration.py` (15) | Built. Postgres **enforcement** proven; the migration's own DDL **installing** it on Postgres has no receipt |
| Which moves are requestable: `admit` and `demote` only; won't-fix and un-park deliberately excluded | `REQUESTABLE_MOVES` in `task_approval_settings.py` | Built |
| `GET /api/tasks/request-badges` — two counts, never summed | `routers/tasks.py` | Built, no caller |
| `POST /api/tasks/{id}/request-verdict` — Rick alone, bound to the account; a denial leaves the ticket untouched | `routers/tasks.py`; `test_the_request_queue_doors_are_operator_only.py` (12) | Built, no caller |

**Live at `e87d9aff`:** none of it. `git grep` for `request_state`, `request-badges`, `request-verdict` and `task_request_lifecycle` under `src/` finds 0 hits. The allowlist still reads `mr radio, cheech, maria`, so **rules 1–2 are not live either**. Live has the promotion ask defaulting to "no" (`e848942a`) and a timed-out ask refusing (`675a1415`).

**No consumer anywhere:** 0 references to either door in `src/lupin_app/static`, `src/lupin_mcp` or `src/lupin_cli` on the branch.

## 4. What is left

1. **The filing door.** Nothing creates a request; `record_request_verdict` answers 409 "none has been filed". Under the ruling a refused direct move files nothing, so filing is its own act, e.g. `POST /api/tasks/{id}/request { move }`. Mr. Radio to settle:
   - who may file: any authenticated seat, or only the row's manager;
   - what a second filing on a pending request returns;
   - that re-filing after a denial overwrites the same three columns and keeps no history of the denial.
2. **The refusal names the door.** The 403 that rules 1–2 return must say why and point at the filing door. That door does not exist yet, so neither can the sentence.
3. **Rick's board.** Show both badges, mark the pending rows, and give Rick approve/deny controls. There is no UI today.
4. **A manager-side verb** (MCP `task_*`) to file and read a request. A door no seat can reach is the same dead end the promotion ask became (Rio: unreachable in the shipped config).
5. **The merge.** A dry-run `git merge-tree` of the branch onto `e87d9aff` shows **16 conflict hunks in 3 files**: `task_promotion_gate.py` (4), `task_approval_settings.py` (2), `test_the_card_and_the_gate_agree_about_silence.py` (10). The live branch landed parallel work on the same code (`7a57a2b7` manager-close, `48e59f13`/`e848942a` card default).
6. **The apply.** `main.py` runs `alembic upgrade head` at startup, so merging applies the migration at the next `:7999` bounce by any seat. The scratch-DB rehearsal must come **before** the merge, and it is already folded into Mr. Radio's single apply-plan ask to Rick.
7. **Tests to the acceptance bar** for the filing door and the board: real HTTP door, positive arm, mutation arm, 100% coverage.

## 5. Questions that are Rick's

**Q1 — Does his answer settle filing, or a different question?** His words describe **his own** refusal: "If I refuse a promote it stays where it was". The fork he was asked about was the **gate's** refusal of a manager's direct move. The two readings build different things:

| Reading | Build |
|---|---|
| A — His "no" leaves the ticket put; a manager files a request as a separate act (Mr. Radio's reading, and what item 1 above assumes) | Filing endpoint + board |
| B — Managers do not file at all; the refused move is the whole of "requesting", and nothing reaches him | No filing door; rule 3 has no mechanism |

Recommend confirming A with the mechanism stated in one line. On this row an answer has already attached to the wrong subject once: the 2026-09-09 carve-out asks were struck twice.

**Q2 — Does approving a request perform the move?** The branch records `approved` and **does not move the ticket**; Rick must then promote or demote it himself through the ordinary door. His ruling says what "no" does to the ticket, not what "yes" does.
- **Option 1 (recommended):** approval performs the move. One act, matching how he phrased "no".
- **Option 2:** approval only records permission. It keeps the move a separate deliberate act, at the cost of two clicks per request.

**Already Rick's, not new here:** the scratch-DB rehearsal and apply plan (Mr. Radio's pending single ask).

**Not Rick's (implementation readings for Mr. Radio):**
- **A denial writes `denied` and closes the request.** "No action whatsoever" is read as about the ticket; if nothing were written, a refused request would stay pending on his board forever.
- **Who counts as the operator:** the verdict door uses the approver allowlist rather than hard-coding `rick` (Rio flagged it; the two are identical until the config is edited).
