# Holding-area consensus — María 🌸 + Mr. Radio 🦉, for Rick

**Written**: 2026-09-10 ~20:00 EDT by María (seat d6ffe515), merging her pass with Mr. Radio's marks.
**Asked for by Rick**: 19:41 to María, *"You get to promote 0 jobs until you and Mr Radio present me with your consensus list"*; 19:44 to Mr. Radio, *"get me a document on your consensus via a doc link"*.
**Population**: 33 `not_approved` rows at ~19:47 EDT (lupin 32 + plan 1). Two finished rows have been closed since (below), leaving 31.
**Not blind**: María's picks reached Mr. Radio before he marked them. Where we disagree, the split is named, not averaged.
**Rick's clicks**: every promote, every drop. Since `adaf7698` went live (merge `7a57a2b7`), managers can **close** finished rows themselves.
**Blockers**: as of ~19:50 EDT, lupin has **0 rows blocked on Rick** (Mr. Radio dropped `1149193f` as answered; María's `35e4ff00` and `5ebd2aff` were cleared at 19:43).

---

## 1. PROMOTE — 4 slots, one per worker

| Slot | Row | Pri | What | For | Against | Agreed |
|---|---|---|---|---|---|---|
| 1 | `cc899c44` | P1 | 15 of 23 notification routes take no credential (for example, anonymous bulk delete) | Highest risk on the list. `e20e249a` went live at 19:21 (anonymous → 401), so the pattern and its tests exist to copy | Every caller (legacy, multiplexer, cosa-voice MCP) must change in the same merge | ✅ both |
| 2 | `bb1a9062` | P2 | PreToolUse denies tool calls as a "voice message" when only a peer DM is queued | Hits every seat, every hour: 29 of 30 interrupts had a peer DM within 5 s (5.4% baseline); Mr. Radio took ~9 more from 19:08 to 19:52 | The hook is shared fleet-wide; a bad fix could swallow real voice input | ✅ both |
| **3** | **SPLIT — you pick** | | see below | | | ❌ split |
| 4 | `2d786391` + `8a784c50` | P2 | Holding-area integrity: a create naming `queued` skips holding; `include_terminal` also un-hides holding rows | Enforces your 09-08 ruling, "Holding is now the only way onto the board, P0 aside". The fix exists on branch `maria-create-door-holding-gate` | Contradicts HEAD test `test_task_create_blocked_at_mint.py:290`, which goes to review as a named question | ✅ both |

### Slot 3 — the split

| Option | Backed by | For | Against |
|---|---|---|---|
| **`6bf8a819`** unit tier red (DOM-assert ratchet + sweeper guard) | Mr. Radio | The unit tier is merge gate 2 and is red on the working branch; tonight's four merges all landed onto a red gate. Both fixes already exist (`0c916ded`, `365cf89a`, neither in HEAD `7dd8b5e3`), so the work is review + merge, the cheapest slot | No user-visible change |
| **`83c3ff74`** multiplexer jobs pane Mine / Not Mine / All Users | María | You ruled its design at 17:40; it finishes the Mine switch you asked for | Needs a server change (`/api/job-history` must learn `user_filter=!self`) |

The loser goes first in NEXT.

🔨 **RICK PICKED `6bf8a819`** (~19:55 EDT, ask_multiple_choice, answered: true, default_used: false). `83c3ff74` goes first in NEXT. He answered on the **legacy** page, so the `4311c503` landing check is still owed, and he named why: the multiplexer's Action Required card does not match legacy (old cards not discarded, no one-card-at-a-time queue, every question rendered at once instead of stepping one question at a time forward and back). Filed as its own row.

---

## 2. NEXT IN LINE — when a slot frees

| Row | Pri | What | Note |
|---|---|---|---|
| loser of slot 3 | | | |
| `1657a852` + `73fdc7ef` | P2/P3 | 28 stale task-list E2E tests; one legacy toolbar E2E always red | Merge gate 9 cannot pass without it; `ts-385c9862` was killed at 87% |
| `6444feeb` | P2 | A build is repo-global, like `git stash` | Bites again with four workers back |
| `b4281428` + `9f6d05a7` | P1/P5 | Seats can close only with a commit | Partly eased by `manager_attestation`; workers still can't |
| `8d043758` | P2 | 97 asserts reach a DOM node through a name; 5 are live OOM hazards | A design row, kept apart from `6bf8a819` |

---

## 3. RULED WORK THAT NEEDS YOUR APPROVE CLICK

| Row | Your ruling | Who builds |
|---|---|---|
| `dbe42964` | 19:44: "Add answer_by (Recommended)", a field beside `resolves_by` | Mr. Radio's lane |
| `47f33bba` | 09-06: option (a), the memento template drops the body Written / Written by / Index block | María, in planning-is-prompting (`workflow/scripts/memento_io.py`); small |

---

## 4. CLOSE / DROP

**Closed tonight by managers — no click needed:**

| Row | Receipt | Checked |
|---|---|---|
| `51b23d36` orphan sweeper comment | commit `b26c32ee` | in HEAD (`git merge-base --is-ancestor`, 19:58) |
| `1d40b477` [PLAN] memento_io worktree root write | commit `b8b063c` | in planning-is-prompting HEAD; Krishna verified 4 cells on 09-06 |

**Drops — your click (a manager drop is refused with 403):**

| Row | Why |
|---|---|
| `5a1b903e` New Ticket card, first filing | Duplicate of `c9895403`, done at `d6ff09d4` |
| `ebde725c` task_create P0 wording | Already landed as `e0e73159` (in HEAD) |

**Managers will close:**

| Row | When |
|---|---|
| `26cd6398` split closing from promoting? | Now — your words on `adaf7698` answered it (Mr. Radio's row) |
| `4311c503` multiplexer answers unreadable | After your landing check: answer one real multiple-choice ask **on the multiplexer card**. Tonight's 19:41 answer came from legacy |

**Park**: `698a5aaf` self_respin reports scheduled but no clear lands. Both our seats' clears landed today, each verified by session id.

---

## 5. DEFER — stay in holding

`966b46ba` promotion gate at 91.7% branch coverage (P2; checked 19:46, not fixed) · `ad7bd01f` stale docstring (P1, cheap) · `80513825` approval-settings file boundary (P2, design) · `158a9865` SSE smokes can't fail (P3, chase 09-15) · `f069fcda` notification origin (P3, chase 09-14) · `b52b430b` c8 pragma on declarations (P3) · `9ab0bddb` doc-viewer normpath (P5) · `0534f9da` orphan commit 5681ca10 (P5) · `95843262` embedding score hazard (P5) · `4163a015` c8 phantom branch (P5) · `b6cfbf8d` topic-confirm slice 2 (P5) · `456f694c` negative assertion fails open (P5) · `7350b450` run-gsm8k import (P5, chase 09-14)

---

**Sources**: María's pass `io/2026.09.10-holding-consensus-maria.md` · Mr. Radio's marks `io/2026.09.10-holding-triage-mr-radio-1945.md` · row bodies read tonight for `83c3ff74`, `cc899c44`, `2d786391`, `26cd6398`, `966b46ba`, `47f33bba`, `adaf7698`, `51b23d36`, `1d40b477`.
