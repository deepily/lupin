# Claude Code Notifications accordion — multiplexer vs legacy client parity

**Date**: 2026-09-10 · **Author**: María 🌸 · **Row**: `5ebd2aff` (P0, Rick by voice ~10:32 EDT)
**Ask, verbatim**: *"compare the … Claude Code Notifications Accordion in both the multiplexer and the Notifications client … bring the multiplexer code up to par with the notifications client … use the Claude code plugin for Chrome such that you can do all the visual comparison and clicking and behavior testing on your own without me"*
**Surfaces**: legacy `/app/notifications` (`notifications.html` + `notifications.js`) · multiplexer `/app/multiplexer` (`multiplexer.html` + `static/js/multiplexer/**`, served as `dist/multiplexer/boot.14c70c51f17a.js`)
**Status**: 🟢 **all four rulings, the sender-card controls and the follow-ups are built, served and checked in Chrome** — last bundle `73a3cb312f35` (19:02 EDT). Ruling 2 (row `98305d96`) closed 19:07 on `5f784c1d`. Open: Rick's own look at the section, and his landing check for Action Required multiple-choice answers (row `4311c503`). See §3 "Where it stands". Rows marked *measured* were driven in Chrome; *code-read* rows are not yet driven.

---

## 0. What Rick sees today, before anything else

🔴 **On a cold load the multiplexer shows "No notifications yet." while the legacy client shows 308.** Measured 10:48–10:55 EDT, one tab open:

| Probe | Result |
|---|---|
| mux store after reload + 10s | `list 0 · senders 0 · cards 0`, header count `0` |
| `GET /api/notifications/senders-visible/<email>` from the mux page | **> 35 s** (no answer inside a 35 s race) |
| `GET /health` from the host | 200 in 0.00 s |
| mux `ApiClient` default timeout | 10 s |
| boot hydration `.catch` | swallows the error — no console line, no UI signal |

⇒ The multiplexer's cold-load history fetch is **abandoned at 10 s and the failure is painted as an empty inbox**. The legacy client has no timeout on the same call and eventually loads. Two separate defects:
1. **Client (this P0)** — a failed hydration must not render as "No notifications yet."; it needs a timeout that tolerates the real endpoint and an honest failure state (the Finished Tasks pane already does this: *"Nothing was measured — this is not 'no finished work'"*).
2. **Server (filed separately, not worked here)** — `senders-visible` taking > 35 s while `/health` is instant.

### Client fix — IN SOURCE, not yet built or driven (11:10 EDT)
| Piece | File | Receipt |
|---|---|---|
| Runner: senders-visible and every per-sender request on a **120 s** timeout; failure **reported**, not swallowed; re-runs on the pane's Retry | `stores/coldHistoryHydration.ts` (new), `boot.ts` | `cold_history_hydration.test.ts` 8/8 |
| Store: `historyHydrationState()` idle → loading → done / failed; **all in-window sender fetches rejected = failed**, not an empty "done" | `stores/NotificationStore.ts`, `shared/types.ts` | `notification_store_hydration_state.test.ts` 8/8; existing `notification_store_hydrate_history.test.ts` 20/20 |
| Pane: "Loading notification history…" / "Couldn't load … Nothing was measured — this is not an empty inbox." + **Retry**; the genuine "No notifications yet." only when done | `render/NotificationsListRenderer.ts` | `render/notifications_list_hydration_state.test.ts` 6/6; existing `notifications_list_renderer.test.ts` 35/35 |

**Arms** (revert the fix, name the red, restore, sha-verify): runner timeout dropped → the two timeout tests red by name, sha `9dac1dd7…` restored identical · all-rejected branch disabled → the two failed/retry tests red by name, sha `001cb69a…` identical. Pane arm **pending** — a second writer is in that file. `tsc -p tsconfig.json` exit 0.

⚠️ Also measured: the multiplexer's notifications **pane was hidden by the section toolbar (💬, persisted in localStorage) while its header stayed visible** — a count of 16 over an invisible list. I turned the pane back on in this browser to compare; **Rick's prior setting was hidden**.

---

## 1. Divergence inventory

Legend — **D** defect to fix · **R** a prior ruling made it deliberate; needs Rick's word to change · **K** deliberate mux-only addition, keep.

### Header bar
| # | Legacy | Multiplexer | Class | Evidence |
|---|---|---|---|---|
| H1 | `Claude Code Notifications: 308` | `[DEVELOPMENT]: 🔔 Notifications 2026-09-10 @ 10:36 EDT 16` | R | Lane 0a uniform section header + H2 env/clock port (`NotificationsHeaderRenderer.ts:129-198`) — measured |
| H2 | `👤 Mine` filter badge | absent | R ✅ | **FIXED — merged `125eee92`, built `78cc06b7250b`, driven in Chrome 18:06; header wrap `5f784c1d` checked 19:08 (row `98305d96`, done).** Admin-only badge plus Mine / Not Mine / All Users switch; the server does the filtering (`exclude_own_jobs` for Not Mine); the direction filter is deleted. See § Ruling 2 |
| H3 | TTS slider inline in the header, label `TTS:` | sibling box below the header, label `TTS preview` | R | split so a header re-render cannot wipe it (`multiplexer.html:175-179`, `393251c5`) — measured |
| H4 | slider reads `notifications_tts_preview_fraction_runtime` = **12.5 %** | slider reads `lupin:tts_preview_fraction` = **0 %** | **D** ✅ in source | two storage keys for one setting ⇒ the two clients disagreed on how much TTS Rick hears — measured (localStorage). **Fix**: `TtsPreviewSliderRenderer.ts` reads the legacy raw key first and writes both on every move. Receipts: `tts_preview_fraction_shared_with_legacy.test.ts` 7/7 · existing slider tests 21/21 · arms: legacy read removed → 2 red by name, legacy write removed → 1 red by name, sha `4376a7af…` restored identical |
| H5 | history-window selector: Today · Last 24 hours · Last 2 days · Last week · Last month · All time | **none** — hard-coded 48 h; `History ▾` is a different feature (expired-notification list) | R→? | "no selector" was the 2026-06-11 design ruling (`boot.ts:480-482`); Rick's P0 asks for parity — measured |
| H6 | `🗑️ Clear All`, red outline | `Clear all`, plain | D | cosmetic — measured |
| H7 | — | `🔄 Bounce server` | K | row `1b4211ac` R2 |

### Sender card
| # | Legacy | Multiplexer | Class | Evidence |
|---|---|---|---|---|
| S1 | `#abe7d752` session id span before 📋 | **absent** | D | header children compared in both DOMs — measured |
| S2 | 📋 copy · ✨ gist · click-to-rename name · × delete-all all wired | rendered, **no handler found** — the only listener returns early on any button in the header | D | `NotificationsListRenderer.ts:507-511`; no other module references these classes — code-read, drive once cards render |
| S3 | per-date `×` soft-delete (`softDeleteByDate`) | **absent** | D | date-accordion header children compared — measured |
| S4 | active indicator ● for the active session, ○ otherwise | always ● | D | `senderCard.ts:176` — code-read |

### Counts — measured after the build, both clients on "Today", one tab at a time
Multiplexer read ~12:08 EDT (after its cold load finished), legacy ~12:15 EDT. The DOM read per card: header `(N)`, each date group's count, and rendered `.sender-message` nodes.

| Sender | Legacy header · date group · rendered | Multiplexer header · date group(s) · rendered |
|---|---|---|
| PLAN `#abe7d752` | 1026 · 2026-09-10 (1020) · 65 | 489 · 2026-09-10 (30) · 30 |
| LUPIN `#d54262de` | 770 · 2026-09-10 (764) · 123 | 374 · 2026-09-10 (57) · 57 |
| heartbeat arbiter | 12 · 2026-09-10 (12) · 12 | 6 · 2026-09-10 (5) + **2026-09-09 (1)** · 6 |
| ctx-tick | 4 · 2026-09-10 (4) · 4 | 2 · 2026-09-10 (2) · 2 |
| **Section total** | **1812** | **878** |

| # | Divergence | Class |
|---|---|---|
| C2 | The multiplexer loads **about half** the rows legacy loads for the same sender and window (489 vs 1026, 374 vs 770, 6 vs 12, 2 vs 4). The legacy total also grew 1600 → 1812 between 11:50 and 12:15, so live arrivals account for part of any gap but not a per-sender halving. | **Legacy is the one that is wrong** Found 12:22 EDT; **confirmed in code 15:18 EDT at `5ec4c146`**. Startup calls `initializeFilterUI()` (`notifications.js:535`), which calls `setFilterMode` (`:6434`). On an admin login that clears the cards and starts `loadConversationHistory()` without awaiting it (`:6379`), and startup then awaits a second load (`:587`). The loader has no in-flight guard (`:19598`), and `addNotificationToSenderGroup` pushes and counts every row with no id check, so every count is doubled. The paired server calls are the 12:22 investigation's measurement, not re-measured. Second, smaller cause: the multiplexer never shows reply bubbles, because its "own" filter drops `direction === "outgoing"` rows (`NotificationStore.ts:87-92`); that fix is merged (`125eee92`): the direction predicate is deleted, and 34 reply bubbles were visible in Chrome at 18:06 (row `98305d96`). **Legacy's double load is FIXED too** (row `b670b76c`, `1b75dcde` + cache token `e4c49fd2`): measured in Chrome, one senders-visible call and rows == distinct ids for every sender. Detail: `io/2026.09.10-mux-count-gap-root-cause.md` (gitignored) |
| C3 | In the multiplexer a date group's count is **far below its own card's header** (30 of 489); in legacy they agree within 6 | **FIXED — merged `f8fa77f7`, live in bundle `3533e3df0b22`.** Cause: the card header counted the full list (`senderCard.ts:198`), but date groups counted the progress-collapsed render list (`senderCard.ts:238-246`, `dateAccordion.ts:56`). Each day now counts the full list and renders the collapsed one. **Measured in Chrome 15:45 EDT: on all 5 cards the header equals the day's count** (775/775, 780/780, 14/14, 5/5, 4/4) |
| C4 | The multiplexer shows a **2026-09-09 row under "Today"**; legacy does not | **Not a divergence.** Both clients send hours = hours since local midnight with anchor = the sender's last activity, and the server keeps [anchor − hours, anchor] (`notification_repository.py:947-955`), so the window always starts before midnight. The two reads were minutes apart, with different anchors |

### Section placement
| # | Legacy | Multiplexer | Class | Evidence |
|---|---|---|---|---|
| P1 | broadcast card and CC session strip sit inside the notifications section body | both are page chrome above the accordions | R | Lane 0c ordering ruling (`multiplexer.html:139-149`) — measured |

---

## 3. Build plan for Rick's four rulings (11:55 EDT, María, seat `56a9b393`) — pre-build check for Mr. Radio

Rick ruled all four "match legacy" at 11:0x (row `5ebd2aff` amendment, `answered: true`, `default_used: false`). Sender-card items S1–S4 and H6 are in source (background agent, reviewed; row amendment 11:50). Order below is the build order. Mr. Radio's pre-build check: *"Plan reads clean — go."*

### Where it stands (after the 11:53:41 EDT build, manifest `built: 2026-09-10T15:53:41Z`)
| Item | State | Receipts |
|---|---|---|
| Ruling 3 — header | built, served, **driven in Chrome** | h3 reads `Claude Code Notifications: N`; `TTS:` slider is the first action inside the bar; controls restyled as legacy's light boxes after measuring the bar at `rgb(233, 236, 239)` with `#333` text. Arms 8/8 + label arm 1/1, each red by name, sha restored |
| Ruling 1 — history window | built, served, driven | the picker read **"Today"** from the key legacy wrote — the shared key works across clients. Store/reload arms 19/19; picker + header-wiring arms 8/8 |
| Ruling 4 — placement | built, served, driven | `#broadcast-card-mount` and `#cc-session-strip` report `parentElement.id === "notifications-pane"`. Arms 2/2 |
| S1–S4, H6 — card controls | built, served | agent arms 30/30; my pane arm 4/4; CSS for the modal, per-date ×, gist pulse and active dot ported |
| Ruling 2 — filter badge + switch | ruled ~15:57; merged `125eee92`, built, **driven in Chrome** | Not Mine sends one senders-visible with `exclude_own_jobs=true`, Mine one without it (both 200, 18:06); badge right after the title; the header overflow it caused is fixed by `5f784c1d` (section 20px inside `main.container`, title 1 line, 19:08). Row `98305d96` done |

**Suite**: 2700/2700 across 152 multiplexer test files · tsc ×3 exit 0 · eslint 0 on every changed source file · Python chrome oracle 23/23.

⚠️ **Served before built.** `multiplexer.html` is served straight from disk, so removing the static `#tts-preview-slider-mount` reached the page before the bundle that stopped looking it up by id. From that edit until the 11:53:41 build, a multiplexer reload could throw at the slider mount. Measured by fetching the served page and bundle, then closed by the build. The lesson for the next page edit: an HTML change and the TS that depends on it ship together, so build in the same breath.

**Still open** — updated 19:15 EDT (María, seat `abe7d752`)
- **Rick's own look** at the section, and **his landing check** for Action Required multiple-choice answers (one real ask answered on the multiplexer card and read back; row `4311c503`). Both are his to give.
- ~~Mine switch~~ — **done** (row `98305d96`, see ruling 2). ~~Legacy double counts~~ — **done** (row `b670b76c`).
- Not in this P0: the jobs pane's Mine / Not Mine / All Users filter (row `83c3ff74`, ruled by Rick ~17:40, in the holding area).
- ~~Count reads `0` while the cold load runs~~ — **fixed**: the multiplexer asks for an hours window (`8f873d99`), and Mr. Radio's server fix (`055dddc4`) took an unwindowed call from 51.9 s to 0.34 s.
- Mux-only `🔄 Bounce server` (row `1b4211ac`, kept) still sits in the bar. The mux-only expired-notifications button is **renamed** `History ▾` → `Expired ▾` (Rick's ruling ~15:57 on row `98305d96`: rename, not remove), so it no longer reads as a second history control beside the window picker.
- ~~The count is a small chip~~ — **done**: bold like legacy's title (`c9c6a065`).
- ~~E2E not run~~ — **run** through `run-e2e-ui-tests.sh` on `:8000`: broadcast card, session strip and parity oracle gave 57 passed, 1 skipped, 1 failed. The failure (focus mode) is fixed by `b0060829` and passed after merge.
- ~~README entry~~ — **done** (`5ec4c146`).
- **Focus mode** — **fixed** (`b0060829`): 250px → 500px like legacy. The old sibling selector stopped matching when ruling 4 moved the strip inside the pane.
- **Action Required answers** (found by Mr. Radio, row `4311c503`): the multiplexer sent `{ response }`, which the server stores with no `value` key, so every answer read back as unanswered. **Step 1** (string answers go bare, `5504e6e2`, merged `f2555c9d`) built as `boot.465e0a51145b.js` at 15:54 and served on `:7999` and `:8000`.
  **Step 2** — in source on branch `maria-action-answer-questions` (16:15 EDT, design approved by Mr. Radio 16:02): the multiplexer copied the `response_options` **object** into `options: string[]`, so a real multiple-choice ask showed no choices and its answer had no header to key by. Now one parser (`stores/responseQuestions.ts`) reads `response_options.questions` for both `ActionRequiredStore` and `NotificationStore`; the card stacks every question (header, question text, radio or checkbox by `multi_select`, option descriptions; batch inputs prefilled from `default_value`); Submit waits until every question is answered; the answer goes as legacy's `JSON.stringify({ answers: { <header>: value } })`, `value` a `string[]` for multi-select (legacy `saveCurrentQuestionAnswer`). Test payloads come from the server's own builders (`fixtures/action_required_questions_payload.json`, pinned by `test_multiplexer_questions_fixture_matches_the_asking_tools.py`), and the Python test feeds the multiplexer's exact wire string through the server's wrap and both askers' readers.
  **Named divergences, not ported in step 2:** legacy offers an "Other" free-text option and a 🎤 mic per question; legacy prefills batch answers from a prediction above the auto-submit floor; legacy shows a `[project]` badge on the question. ~~Legacy pages multi-question asks with Back/Next (the multiplexer stacks them)~~ — **ported by row `360de81b`**, below; Rick rejected that divergence (~19:55).
  **Row `360de81b` (Krishna, branch `krishna-action-required-one-at-a-time`, design OK'd by María 20:16) — the card now matches legacy on three behaviors:**
  - **One card at a time.** The first ask fills an active slot; the rest wait as minimized rows showing `#N`, a type icon (❓ 💬 📋, 📢 otherwise), the prompt cut at 57 chars + `...`, and the timeout (`Nm`/`Ns`); rows have no controls (legacy `renderActionRequiredNotification :22825`, `renderMinimizedNotificationDOM :21498`, `recalculateQueuePositions :22767`). A card's countdown starts when it reaches the slot, as legacy's does (`activateNextNotification :21467`).
  - **Finished cards leave.** Answered here: 600 ms, then removed. Answered in another session: "✓ Responded in another session" for 1500 ms, then removed. Expired, by the local countdown or by the server's `notification_expired` (now subscribed), whatever the countdown shows: 600 ms, then removed. A queued card that finishes leaves at once. The next card then activates. A failed answer stays for retry.
  - **Multiple-choice stepper.** One question at a time with "Question N of X": Back hidden on the first, "Next Question →" validates and saves, "Submit" (one question) or "Submit All ✓" on the last; Back keeps a valid answer and steps back; answers stay checked when a question is shown again, including after a failed submit rebuilds the card (legacy `renderMultipleChoiceUI :23519`, `navigateMultipleChoice :23729`). The wire answer is unchanged; `open_ended_batch` stays stacked, as in legacy.
  **Named divergences of `360de81b`:** no persona badge on a queued row; no minimized→active or genie animation; activation is not held while TTS plays (legacy `:21393`); "Responded in another session" omits the other session's answer. **Legacy defect not copied:** legacy's `handleLocalTimeout` returns early for a card with no DOM element (`:24374-24375`), so a server-expired *queued* card stays in its queue; the multiplexer removes it.

### Ruling 3 — header bar (H1, H3, H6)
- **Title** `Claude Code Notifications:` with the count straight after it, no 🔔. `sectionHeader.ts` learns `icon: ""` (no leading space); every other pane is unchanged.
- **Env label** stays first in the h3. **Clock moves after the count**, so the count reads as the title's value.
- **TTS slider inside the bar**, label `TTS:`. The header renderer creates `#tts-preview-slider-mount` as its first action (legacy order: badge · TTS · history window · Clear All · toggle), and the slot stops click propagation, as legacy's `onclick="event.stopPropagation()"` does.
  - **Wipe safety, measured by reading**: the header calls `replaceChildren` only in `mount` and `unmount`, never on refresh (`NotificationsHeaderRenderer.ts:206, 252`). The 01-B note's "wiped on every header render" does not match the code.
  - **Boot** acquires the slot with a post-mount `querySelector` on the header mount — the commons-activity precedent (`boot.ts:558`) — because the static page no longer declares it. The `boot_mounts_every_pane_the_page_declares` sweep reads only `getElementById`, so it stays consistent.
- **Clear All** gets legacy's red outline, adapted to the dark surface.
- Tests pinning the reversed 01-B sibling layout (`render/notifications_header_region_placement.test.ts`, the `boot_order_mount_integrity` fixture) are rewritten to the new ruling and cite it. The `.notifications-header-region` wrapper stays (the Python chrome oracle keys on it).

### Ruling 1 — history window (H5)
- **Store half done**: `stores/historyWindow.ts`, `NotificationStore.historyWindow / setHistoryWindow / resetHistoryHydration`, `coldHistoryHydration` reload, boot reads the window per run. Receipts: 148/148 across 11 files · tsc ×3 exit 0 · 19/19 revert arms red by name, sha restored.
- **UI**: new `render/historyWindowDropdown.ts` with legacy markup and ids (`#history-window-dropdown.history-window-dropdown` › `button.dropdown-display` + `#history-dropdown-menu.dropdown-menu` › `.dropdown-item[.selected]`), click-outside close, stops propagation so it never collapses the section. Sits after the TTS slot, before Clear All.
- ✅ **D2, ruled ~15:57**: keep the mux-only expired-notifications panel and rename its button `History ▾` → `Expired ▾` (merged `08f60004`, built `3344371af56e`).

### Ruling 4 — placement (P1)
- `#broadcast-card-mount`, then `#cc-session-strip`, move **inside `#notifications-pane`**, before `#sender-cards-container` (legacy `notifications.html:540 → :658 → :676`). `#missed-badge-mount` is mux-only and stays.
- No boot change: every mount is by id. The list renderer resolves only `#sender-cards-container` and never clears its pane (`NotificationsListRenderer.ts:239-266`).
- Collapsing the section now hides the broadcast card — Rick accepted that.
- E2E Python tests reference these ids (`test_multiplexer_broadcast_card.py`, `test_cc_session_strip_and_focus.py`, the parity oracle) — run after the build when the test server is idle.

### Ruling 2 — filter badge + switch (H2) — RULED ~15:57: port legacy's user filter
- **RULED** (Rick ~15:57 EDT, `answered: true`, `default_used: false`, row `98305d96`): port legacy's user filter. Design sanity-checked with Mr. Radio at 17:06 (`io/2026.09.10-mux-mine-switch-design-maria.md`, gitignored); **merged `125eee92`** after his review, built `78cc06b7250b`, driven in Chrome. The switch in the bar pushed the section 141px past the page; Mr. Radio picked a CSS wrap (`5f784c1d`) over a legacy-style panel, measured 20px inside at 19:08.
- **"Mine" and "All Users" send the same request, on purpose.** Legacy adds `exclude_own_jobs=true` to senders-visible only for "Not Mine". Every notification has one recipient, so "All Users" cannot widen it. Ported faithfully rather than treated as a legacy defect; Mr. Radio agreed.
- **The multiplexer's direction predicate is deleted**, so reply bubbles show again (C2's second cause). The mode lives under legacy's raw key `notifications_filter_preference`, shared with legacy.
- **A page load runs one history hydration.** The header only reads the mode at mount; legacy's init-time `setFilterMode` is what doubled its counts (row `b670b76c`). Pinned by `the_mine_switch_loads_history_once_per_mode.test.ts`.
- **The jobs pane is NOT covered** (row `83c3ff74`). `/api/job-history` gives an admin every user's jobs by default and refuses `!self` with 400, so it needs a server decision first.

*The pre-ruling notes follow, kept as history.*
- **Legacy's switch filters by user, not by message.** It is admin-only, lives in the filter-settings panel (`notifications.html:1128-1134`), sends `exclude_own_jobs=true` on senders-visible for "others" and `user_filter=*` / `!self` on the job queues (`notifications.js:6244-6262`, `:19524-19533`). The header badge (👤 Mine · 🚫 Not Mine · 👥 All Users) scrolls to that panel.
- **The mux store's own/others/all keys on message direction** (Mr. Radio's 2026-06-29 axis ruling), and the multiplexer has **no admin flag anywhere** (`grep isAdmin` in `static/js/multiplexer`: 0). Wiring the badge to the existing mechanism would label incoming messages "Mine".
- Recommendation to put to Rick: port the legacy user axis — admin flag from auth, `exclude_own_jobs` on the cold load, reload on change.

### CSS gaps (agent report)
Adapt the legacy rules to the mux dark surface, not byte-copy them (legacy is a light theme): hover-reveal per-date `×` (`notifications.css:2620-2641`), rename modal overlay (`:1811-1921`), gist working pulse (`:1688-1697`), grey `○` / persona-colour `●` and the active card's left border (`:1924-1958`; the mux hard-codes green at `notifications-list.css:76`), history-window dropdown (`:2470-2547`), filter badge (`:61-95`).

---

## 2. Method notes, so the next reader does not repeat my detours

- **Two tabs on one origin distort the measurement.** With both clients open, requests queued behind each other's open streams; the legacy tab froze a 45 s evaluate. Compare one client at a time.
- **The browser is logged in as Rick.** Any click on an Action Required Yes/No would be his keypress. I do not click inside that panel.
- `/app/notifications?classic=1` is only needed when the legacy redirect flag is on; it is off today.
