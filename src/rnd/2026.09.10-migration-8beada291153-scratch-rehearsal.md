# Migration 8beada291153 — scratch-database rehearsal, and proof the scratch database is gone

**Row** c9fafb9d · **Ran** 2026-09-10 23:47:52Z–23:47:57Z by Chloé 🗼 · **Authorised** by Rick, 2026-09-10 ~19:44 EDT, by keypress:

> "Per your recommendation you can rehearse this on a scratch database but I want evidence before we close this ticket that you have removed that scratch database"

## Verdict

The migration installs its three columns and three CHECK constraints on Postgres, the installed constraints refuse all three illegal rows, and the migration downgrades and re-upgrades cleanly. **The scratch database `lupin_scratch_rehearsal_8beada291153` was dropped:** the listing after the drop is identical to the listing before it, and a count of databases with that name returns 0. `lupin_db_dev` was only read and is unchanged.

This closes the gap Rio filed on the row: *"these rules work on Postgres"* had a receipt, *"this migration installs them on Postgres"* did not. It now does.

## What was rehearsed, and on what

| | |
|---|---|
| Migration | `8beada291153` (parent `9a1c4f27bd30`), from branch `john-202-is-not-a-success` at `45ff6979`, run from a detached worktree. A checkout, not a merge; the worktree was removed afterwards |
| Scratch contents | the **full schema** of `lupin_db_dev` plus the **data** of `task_items` (2,604 rows) and `alembic_version`, copied with a read-only `pg_dump` |
| Why not the whole dev database | 5,229 MB on a shared instance, and `task_items` is the only table the migration touches (14 MB, no foreign keys) |
| Chain check | `lupin_db_dev` reads `9a1c4f27bd30`, the migration's parent. No migration was added on the working line since the branch forked, so this revision becomes the single head |
| Credentials | `DATABASE_URL` pointed at the scratch database; the script refused to run unless the URL named it, and never printed the password |

## Results

| Step | Result |
|---|---|
| Upgrade `9a1c4f27bd30 → 8beada291153` | ✅ `alembic_version=8beada291153`; 2,604 rows, 0 with a request (the migration's own pre-check printed `rows carrying a request before the CHECKs are added: 0`) |
| Columns installed | ✅ `request_move varchar NULL`, `request_state varchar NULL`, `request_ts timestamptz NULL` |
| Constraints installed | ✅ `ck_task_items_request_requires_move`, `ck_task_items_request_requires_ts`, `ck_task_items_request_state_is_ruled` |
| Unruled `request_state='bogus'` | ✅ REFUSED by `ck_task_items_request_state_is_ruled` |
| `pending` with no move | ✅ REFUSED by `ck_task_items_request_requires_move` |
| `pending` with no timestamp | ✅ REFUSED by `ck_task_items_request_requires_ts` |
| Complete pending request | ✅ ACCEPTED (`UPDATE 1`), then rolled back; 0 rows carry a request afterwards |
| Downgrade to `9a1c4f27bd30` | ✅ columns gone, 2,604 rows |
| Re-upgrade to `8beada291153` | ✅ same columns and constraints, 2,604 rows |
| **DROP DATABASE** | ✅ `DROP DATABASE` at 23:47:57Z |
| **Listing after the drop** | ✅ identical to the listing before; `rows in pg_database named lupin_scratch_rehearsal_8beada291153: 0` |
| `lupin_db_dev` after | ✅ `alembic_version=9a1c4f27bd30`, 0 request columns |

The other databases in both listings pre-date this run and were not touched: `d1_offline_*` ×3, `lupin_lane_b_test_*` ×2 and `tz_matrix_774323`.

## Removal evidence, verbatim

```
=== S0 listing BEFORE — 2026-09-10T23:47:52Z
d1_offline_1307785
d1_offline_1308442
d1_offline_1309481
lupin_db_dev
lupin_db_test
lupin_db_v1baseline
lupin_lane_b_test_3471482
lupin_lane_b_test_697079
postgres
template0
template1
tz_matrix_774323

=== S1 create scratch — 2026-09-10T23:47:52Z
CREATE DATABASE

=== S7 DROP scratch — 2026-09-10T23:47:57Z
DROP DATABASE

=== S7 listing AFTER — 2026-09-10T23:47:57Z
d1_offline_1307785
d1_offline_1308442
d1_offline_1309481
lupin_db_dev
lupin_db_test
lupin_db_v1baseline
lupin_lane_b_test_3471482
lupin_lane_b_test_697079
postgres
template0
template1
tz_matrix_774323
rows in pg_database named lupin_scratch_rehearsal_8beada291153: 0

=== S8 dev untouched — 2026-09-10T23:47:57Z
lupin_db_dev alembic_version=9a1c4f27bd30
lupin_db_dev request columns=0
```

## Not established

- **Downtime or locking on the live table.** The rehearsal ran on a 2,604-row copy with nobody else connected. Adding three nullable columns and three CHECK constraints takes brief exclusive locks on `task_items`; at the next `:7999` bounce that happens during server start. The lock duration on the live table was not measured.
- **The rest of the schema's data.** Only `task_items` and `alembic_version` carried data. The migration touches nothing else, which was read from its source, not measured against a full copy.
