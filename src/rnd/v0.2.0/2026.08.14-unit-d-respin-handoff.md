# Unit D — re-spin handoff (Tiffany 💍 → Cheech's lineage)

**Date**: 2026-08-14 · **Branch**: `wt-v2-c-executor-2026.08.14` (worktree `lupin-wt-c-executor`)
**Unit**: D — `AskFlow.run()` branch logic + endpoint + Pydantic contract + main.py registration + INI block.
**Plan**: `src/rnd/v0.2.0/2026.08.14-cj-flow-v2-parallel-implementation.md` (as revised by the cascade handoff §3).

## Where D stands

| Piece | State |
|---|---|
| `src/cosa/rest/v2/flow.py` — `AskFlow` | **WRITTEN + compiles + imports.** Four branches (replay / agent / needs_input / receptionist), degradations, TTS reuse, trace, write-back seam. **No unit tests yet.** |
| `routers/v2_ask.py` — endpoint + Pydantic contract | **NOT STARTED** |
| `main.py` registration | **NOT STARTED** |
| INI block (`v2 flow enabled`, `v2 executor`, `v2 snapshot writeback enabled`, `v2 similarity floor`, `v2 trace dir`) | **NOT STARTED** |
| Unit tests for `flow.py` (100% lines+branches) | **NOT STARTED** — the immediate next task |

## Interfaces D was shaped against (verified in the real merged/worktree source, not the doc)

- **A — registry** (`v2/registry.py`, merged): `resolve(command) -> AgentSpec | None`; `AgentSpec.required_args` (tuple), `.factory` (agent CLASS), `.snapshotable`. `resolve` returns None for receptionist/none/deferred/control/unknown.
- **A — router** (`v2/router_client.py`, merged): `RouterClient.route(question) -> (command, raw_args)`; returns `("unknown","")` on router XML parse failure.
- **B — expeditor** (`expeditor.py`, merged): `extract(command, raw_args, original_question, spec) -> ExtractionResult` with `.final_args`, `.missing`, `.fallback_questions`, `.fallback_defaults`, `.special_handlers`. `ArgSpec` (the expeditor's, NOT registry's) + `ArgSpec.from_entry(AGENTIC_AGENTS[command])`.
- **C — executor/trace/pending** (mine, merged): `InlineExecutor().submit(Work, StageTrace) -> Outcome`; `Work(kind, job, user_id, user_email, session_id, snapshotable)` frozen; `StageTrace(trace_dir=...)`; `PendingRequests().put/get/set_status`.
- **C2 — cache** (`v2/cache.py`, in wt `lupin-wt-c2-cache`, **Sam merging shortly**): `V2Cache.lookup(question) -> CacheLookup` (`.is_replay_hit`, `.tier`, `.snapshot`, `.best_candidate`, `.similarity`, `.best_score`, `.question_normalized`, `.t_exact_ms`, `.t_embed_ms`, `.t_ann_ms`, `.embed_cached`).

## Decisions LOCKED with the crew (do not re-litigate)

1. **Weather ArgSpec is synthesized at the call site** (`AskFlow._arg_spec_for`) — do NOT add a weather entry to `AGENTIC_AGENTS` (R-B3, María's line; confirmed by Mr Radio).
2. **Write-back contract with Sam (C2)** — the flow does NOT construct snapshots:
   - `snap = cache.snapshot_from_result( question=q, answer=raw, answer_conversational=conv, routing_command=cmd, agent_class_name=cls )` (call with KWARGS; `agent_class_name` from `spec.factory.__name__`).
   - `sid = cache.write_back( snap, writeback_enabled=self.writeback_enabled ) -> Optional[str]` (None when the flag is off).
   - **The kill-switch lives ONCE, inside `write_back`.** The flow gates only on `snapshotable and status=="done"`, never on the flag.
3. **Fail-loud wiring guard**: `AskFlow.__init__` raises when `writeback_enabled` but the cache lacks `snapshot_from_result`/`write_back` (Mr Radio's ruling, adapted to Sam's co-located builder).
4. **Writeback ships ON in the eval/app config** — a flag switched off "just until X" never comes back and makes the eval measure a system with its write path disabled (Mr Radio). Wire the app with writeback ON once C2 is merged.
5. **Below a perfect (tier-1 exact) match → route, never replay** (R-C1 / §6a). Replay fires ONLY on `lookup.is_replay_hit`.
6. **Never 500** — agent/replay/router/extract failures degrade to the receptionist with distinct `route_reason` (`agent_error`, `replay_error`, `router_error`, `extract_error`).

## Hard constraints for the next driver

- **D must NOT merge before C2** (Sam). C2's cache + `snapshot_from_result` is a merge precondition (store row `41333974`).
- **AskFlow class is ~240 lines WITH DbC docstrings** (logic is lean; the length is docstrings + the `_emit` response dict). Over the 200-line soft target. Judgment call: nothing obviously belongs in A/B/C — every helper is flow-glue. **If you trim, trim docstrings/`_emit`, not the branch logic. Tell Mr Radio/Cheech if you move anything cross-unit.**
- **Never touch a v1 file** without manager sign-off. D only ADDS `flow.py`, `routers/v2_ask.py`, one `main.py` include line, and the INI block.
- **Testing**: unit tests inject FAKES for cache/router/expeditor/executor/pending/notifier — no live Postgres/model/TTS on :7999. 100% lines+branches on `flow.py`. Prove every guard can fail (break→red→restore), per the standing rule.
- **LUPIN_ROOT must be exported to THIS worktree** before running any test, or subprocess/config resolves off the dirty main tree → false red.

## Immediate next steps (in order)

1. Unit tests for `flow.py` (all four paths, every degradation, needs_input park vs terminal, speak=false → no notify, write-back on/off, the construction guard). 100% + red receipts.
2. `routers/v2_ask.py`: `POST /api/v2/ask` `Depends(get_current_user)`; Pydantic request (`question`, `websocket_id`, `speak`, `interactive`) + response (§8 fields). 400 on empty/oversized question; never 500 for agent/replay. A `build_ask_flow(config_mgr)` factory that imports Sam's cache lazily (module stays importable pre-C2).
3. `main.py`: `app.include_router( v2_ask.router )`.
4. INI block in `src/conf/lupin-app.ini` + splainer.
5. Router unit test (`TestClient` + `dependency_overrides`) + integration test (`:8000` scheduled).

## Commit

WIP commit on this branch carries `flow.py` + this handoff. Not green yet (no tests) — it is a continuity checkpoint, not a done claim.
