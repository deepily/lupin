# Memento — Clayton 😎, session `fac98a42`

**Written** 2026-08-24 21:19 EDT, at the 50% context ceiling, on Mr. Radio's instruction.
**Seat** author #1, Mr. Radio's crew (manager session `b98b9c38`).
**Board on re-spin: EMPTY.** Both owed rows closed with receipts. Nothing is in flight that I own.

---

## 1. What I owe when I come back

**One thing, and it is not mine to start yet: the INTEGRATION RE-RUN.**

- `ts-52057478` was **killed** (Rick approved) because it ran 47 min over an
  uncommitted, actively-changing `test_notifications_integration.py`
  (+607/−89, mtime still moving). Its verdict was discarded, never read.
- john has since committed that file as **`957109fa`**; Mr. Radio verified it and
  `routers/notifications.py` byte-clean vs HEAD.
- 🔴 **QUEUE BEHIND RIO.** He waited 45 min for `:8000` and goes first. Do not submit
  until he says he is clear. Submit via `POST /api/test-suite/submit`
  `{"test_types":"integration"}` — never a side door.
- Verify idle first with **`pool-status`**, not `get-queue`: the queue reads are
  **user-filtered** (`is_admin_view: false`), so their "0" means zero of *mine*.
  `pool-status` is unfiltered and is the only one that carries the verdict.

## 2. Rows closed tonight (do not re-open)

| row | receipt | what |
|---|---|---|
| `aa6afbd8` | `f6201ce3` | 4 stale env-contract citations + 3 cache-bust tokens + the non-vacuity anchor that listed 6 assets while the page linked 8 |
| `e7048496` (P1) | `30ca3c42` | preflight PRE bootstrap exemption, scoped to `pre` only; `/proc/1/environ` replaces `docker exec printenv` |
| `1cf6c918` | `bbe1d17b` | `spawn_sessions` refused every project from a worktree; `--git-common-dir` replaces the sibling-path guess |
| `0d7a33db` (P1) | `7921496e` | gate 1 was comparing REGISTRY against itself |
| `ac37dc5a` (P2) | `81b4c09f` | stub-bodied-test detector; the stubs themselves were fixed by john (`957109fa`) and `275c5c52` |

Gate results, all measured: unit **1 failed / 17275** (that 1 pre-existing, no
history, no flaky marker), cosa **8629/0**, websocket **50/50**, bridge guard
**11 passed ×3**, E2E **15 red — all pre-existing** (diffed against the 08-22 junit:
0 new, 1 fixed).

## 3. Open, and explicitly NOT mine

- **Voice ladder → receptionist as a POSITIVE detection?** Left open on `0d7a33db`
  on purpose. Belongs to whoever owns the ladder. Closing it silently would have
  been the easy lie.
- **`EXPECTED_USER_INITIABLE`** — Tiberius's, same shape as my `EXPECTED_COMMANDS`.
  I offered him the template and said I would not touch `test_v2_agents_endpoint.py`
  again. **Keep that promise.**
- The `:70` finding: a 10 ms budget that is really a *behaviour* assertion in a
  timing costume. Loosening the number would preserve the mislabel and destroy the
  signal. The legible form already exists two tests below it.

## 4. How I work — carry this, it is the point

**Verify, do not assert.** Every claim tonight that survived was measured; every one
that nearly went wrong was reasoned. Specifically:

- **A zero needs proving.** `find_stub_bodied_tests` returning 0 is indistinguishable
  from a broken detector — so I planted a stub, watched it fire, removed it. Do that
  every time a check reports "nothing".
- **Diff against a prior artifact rather than eyeballing.** The E2E "15 failures"
  became "0 new" only because the 08-22 junit sat in the same directory.
- **Mutation-test a gate before believing it.** Gate 1 shipped with a docstring
  telling readers how to falsify it; the recipe did not work. Both add and remove
  left it green.
- **Answer unprompted when you hold the fact.** Rick asked which peer wrote the OOM
  explainer; git had it (`27e97fc2`) plus the limitation that the shared `deepily`
  identity cannot name the seat. Mr. Radio called this out as worth keeping.
- **State the limit you could not reach.** `/proc/1/environ` is stronger *by
  construction, not by measurement* — I never reproduced a live divergence, and the
  commit says exactly that.

**And the one I got wrong, which is worth more than the ones I got right:**

> I told the manager "it compiles". True at 21:06, false at 21:10 — a stray
> triple-quote inside a docstring broke every seat's unit collection.
> **A compile check is a statement about a moment, not a property of a file.**
> Re-verify before reporting, not before editing.

Second of the same family: I ranked the `:70` test "fragile" from a small *number*
without checking what it measured. High headroom makes a red **more** informative,
not less. Tiberius corrected me; the inverted ranking would have made me discard the
single most informative failure in the set.

## 5. Fleet facts worth not re-deriving

- **Shared tree, many writers.** Stage by filename, never `git add .`/`-A`. Tonight
  that discipline meant zero reconcile work across four seats.
- **`git stash` is repo-global** — CLAUDE.md gained a section on this today; a
  `stash_guard.py` hook now denies the mutating verbs.
- **"Quiescent box" is retired** (row `5a68c92c`). Do not wait for one; read a red by
  re-running and comparing the *named file*, then resolve its `cc_pid` against
  `/proc` **live** — a roster snapshot goes stale and an unowned-looking file reads
  as contact. Direction discriminates: a dead-owner file that **disappeared** is the
  pruner; one that **appeared or changed** is contact.
- **JS/TS tier stays quarantined.** Analysis only. I read `agent-select.js`; I ran
  nothing.
- Do not pipe a long run through `tail` — I lost a traceback that way and could not
  name the mechanism of the unit tier's one remaining failure.

## 6. Identity

Persona **Clayton 😎** (`fQ9aRKjmL75dgjNakj2u`). Manager **Mr. Radio 🦉** (`b98b9c38`).
Peers: **Tiberius 👑** (deploy tooling, TS analysis), **Rio** (`fc74c1d4`, `c242166d`,
Gate B rig), **john** (`957109fa`). Branch `wip-v0.2.0-2026.08.03-present-and-demo`.
**Nothing pushed — that gate is Rick's.**
