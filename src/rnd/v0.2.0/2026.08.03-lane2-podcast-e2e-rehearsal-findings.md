# Lane 2 podcast E2E rehearsal — stages 4-6, and the two walls behind them (2026-08-03)

**Author:** Tiffany 💍 · **Task:** `a0eaaef3` · **Accountable:** Mr Radio · **Branch:** `wip-v0.2.0-2026.08.03-present-and-demo`
**For:** Rick's Thursday 2026-08-06 voice-driven demo. Lane 2 = the demo path (dropdown "Podcast Generator" → type a vague sentence). Needs no input from Rick.

## The question this rehearsal answered

Nobody had ever observed the Lane 2 pipeline past stage 3 (the confirmation prompt). The six stages: **1** request arrives · **2** reaches the Runtime Argument Expeditor · **3** resolves the document from a description · **4** a job is created · **5** the job runs to completion · **6** the finished podcast is genuinely about the source material. This rehearsal drove all six on `:7999` as the test user, with a **distinctively-named seeded stand-in** so the artifact could be verified by CONTENT, not filename.

## What was seeded

`io/deep-research/interactive.job.tester@lupin.deepily.ai/2026.08.03-verdigris-lighthouse-automation-explainer.md` — a fictional "Verdigris Lighthouse Automation Protocol" with unique, checkable facts (Cordelia Fenwick, 1889; Marrow Point; a clockwork sextant; **62%** keeper-labour cut; catchphrase *"the lamp that minds itself"*). Seeded into the **test user's** dir, never Rick's corpus.

## Wall 1 — the fuzzy matcher overflowed phi-4 (stage 3), now fixed two ways

**First run (config = `/src`):** the matcher threw **before judging anything**:

```
[Expeditor] Fuzzy match error: ... maximum context length is 8192 tokens.
However, you requested 203873 tokens (199777 in the messages...)  → HTTP 400
```

**Root cause:** `expeditor.py:_handle_fuzzy_file_match` builds `docs_map` from the user's deep-research + presentations dirs **plus an `os.walk` of every path in `podcast generator source search paths` = `/src`**, then (`:1104-05`) dumps the **entire** candidate path list into the phi-4 prompt with **no keyword pre-filter**. The router's twin (`podcast_generator.py:match_research_docs`) already had that pre-filter; the expeditor path did not. So *every* description got "Matching failed", regardless of scope or match quality — the overflow precedes the judgment. This **supersedes** the morning catalogue's "out of scope" story: my seed was **in scope** and still failed.

**Candidate count, reconciled (corrected 2026-08-03):** the **expeditor** — the failing path — scans **four** extensions, `.md/.yaml/.yml/.txt` (`expeditor.py:1031`, `source_extensions`), not `.md` alone. Its candidate set under `/var/lupin/src` is **6,676** files (reproduced host and container) — that is what overflowed phi-4, and it matches the logged **199,777** tokens. An earlier **md-only** figure of **1,964** answers a *narrower* question — the **router**'s `match_research_docs`, which filters `.md` only (and already had the pre-filter, so it never overflowed). Both stand, each for its own path: **6,676** (expeditor, four extensions) is the overflow count; **1,964** (router, md-only) is the narrower resolver. Token math settles it — 1,964 md paths ≈ 37k tokens cannot produce 199,777, the four-extension set does. *(Correction on the record: I first reported the md-only 1,964 as authoritative; Rio's ~6,670 was right for the failing path.)*

**The two fixes (both landed):**
- **Config mitigation** (commit `7e29bcfe`, Tiffany): `podcast generator source search paths` `/src` → **empty**, so only the user's always-scanned deep-research + presentations dirs remain — a handful of files, no overflow. Live without a bounce (the expeditor builds a fresh `ConfigurationManager` per call, re-reading the bind-mounted ini). Splainer rewritten so the empty value reads as a deliberate exit condition, not an oversight.
- **Durable code fix** (commit `e651bc76`, Rio): extracted the router's keyword pre-filter into a shared helper `cosa/agents/io_models/utils/fuzzy_file_prefilter.py` and routed **both** paths through it — one behaviour, not two copies that can drift.

**Verified:** re-run with the mitigation, the vague description *"the explainer about automating the old lighthouse so it minds itself"* **resolved to the seeded doc** (confirm summary: `research=/var/lupin/io/deep-research/…/2026.08.03-verdigris-lighthouse-automation-explainer.md`). **Stage 3 passes.**

## Wall 2 — the script parser drops everything on a brace (stages 4-5)

With stage 3 fixed, the job created and ran — then produced a **136-byte, zero-segment "Untitled Podcast"** artifact. **This is not dry-run and not a matcher problem.** On job `pg-c24936c8`: analysis ran, `script_generation` returned **5,533 tokens**, then `parse_script_response` fell to its default fallback (`title="Untitled Podcast"`, `segments=[]`, `0.0 min`) because `lenient_json_loads(response.content)` returned `None`.

**Trigger (Clayton, deterministic three-way control):** a **brace in the model's trailing prose** after the closing ```` ``` ```` fence. Clean fenced JSON → 2 segments; the same JSON followed by an `### Explanation` line containing any `{` → 0 segments; the same trailing prose with no brace → 2 segments. **The job did not error** — it completed and asked a human to approve an empty script. There is no guard between "produced nothing" and "ready for sign-off". Owned by Clayton (P0 `4317efd1`); scope widened — the parser is copied three times and the **presentation generator has the identical bug**, being de-duped onto one helper.

## A third defect, named not chased — undeliverable ask → operator

The post-generation "approve this script" ask **reached Rick's account**, though the job belonged to the test user. Mechanism: the podcast job sets a per-agent module global `cosa_interface.TARGET_USER = self.user_email` (the test user), but the test-user WebSocket had closed before the approval ask fired, so the ask **fell back to the operator**. My harness fix is to hold the WS open through the approval gate — but the fallback itself is a routing defect.

**⚠️ Thursday question worth answering:** if **Rick's** browser drops mid-job, does *his* approval leak to another account? An undeliverable ask silently routing to the operator is a data-routing defect independent of any test harness.

## The pre-filter's own edge, measured against Rick's real pair

Rio's shared pre-filter drops any candidate sharing **zero** keyword tokens with the description — a silent-failure risk if a demo sentence has no word in common with the document's path. **Measured against Rick's real pair** (his sentence about the KISS protocol and saving tokens, vs the real filename `2026.07.25-the-kiss-protocol-how-a-brevity-mandate-got-built.md`): with narrowing forced (201 files → 1), **his doc is the sole survivor** on its `kiss`+`protocol` overlap. The zero-overlap drop **is** real (reproduced: a description with no path-token overlap drops its target), but it does **not** bite Rick — (a) his sentence overlaps his filename, and (b) post-mitigation his candidate map is under the 50-file narrowing threshold, so the pre-filter returns it unchanged. The risk returns only if `/src` is restored to the search key.

## Result — STAGES 4-6 OBSERVED (first time ever), 2026-08-03 ~13:40 EDT

After the coordinated bounce served all three fixes (config mitigation `7e29bcfe` · Rio's shared pre-filter `e651bc76` · Clayton's parser fix `2d5e67df`), a WS-held-open run drove Lane 2 end to end on the real Verdigris seed with a **vague description only**:

- **Stages 1-4:** ✅ vague description resolved to the seeded doc; job created.
- **Stage 5 (script generation):** ✅ produced `2026.08.03-174045-the-lamp-that-minds-itself-…-script.md` (7,899 bytes, **19 dialogue segments**, 10.0 min). ⚠️ But the JOB did **not** reach terminal `done`: after the script was written it hit the **post-generation approval gate** and **died on a voice-gate timeout** (see the delivery correction below). So the *script* is proven; end-to-end *completion* is not.
- **Stage 6 — artifact genuinely ABOUT the source (ham-radio trap DEFEATED by content):** the script names, unprompted, every unique planted fact — **Verdigris Lighthouse Automation Protocol**, **Cordelia Fenwick, 1889**, **Marrow Point off Brasslund**, the **clockwork sextant / self-trimming brass wick / seven sluice-gates of Greywater Cistern**, **"sixty-two percent"** (spelled out), **nine unattended nights**, and the exact signature line **"automate the dull vigil, keep the human for judgement."** It even critiques the invented figures. Not a look-alike — it is the seeded doc. **The content question is answered YES.**

**One transient snag, worth recording:** the *first* post-bounce run failed at script generation on the **phi-4 / Max-plan rate limit** ("try again in a few minutes") — **not** the parse bug. Clayton's fix held perfectly on that empty response: `parse_script_response` **raised loud** ("no recoverable JSON object"), wrote **no empty stub**, and never reached the approval gate. A retry after a short cooldown produced the real artifact above. **Mitigation for the demo: off-peak scheduling** (the parser is now honest either way).

**Parse held:** ✅ — on the rate-limited empty response it failed loud (no silent "Untitled" stub); on the real response it parsed 23 segments cleanly.

**Cleanup (done):** deleted the seed (`io/deep-research/interactive.job.tester@…/2026.08.03-verdigris-…md`) and both my generated outputs (the dry-run stub + the real script) on host AND container (io/ bind-mounted, one delete covers both) — verified gone (container grep = 0). Pre-existing `2026.08.01-*` outputs left untouched. Evidence copy of the real script preserved in the session scratchpad.

**Delivery leak — CORRECTION (it DID recur):** my earlier claim that "the WS was held open, no leak" was wrong. My harness breaks its read-loop the instant the script `.md` lands on disk and closes the WS *there* — but the **post-generation approval gate fires after that**, to an already-closed test-user socket, so it **fell back to the operator (Rick) a second time** (job `pg-b391fa13`) and the job died on a voice-gate timeout. My early socket-close **exposed** the operator-fallback defect; it is not the cause. **This is the top remaining Thursday risk** (row `2c0aaa34`): on stage the script generates fine, then the run stops at an approval prompt whose delivery isn't reliable if the driver's socket isn't live at that instant.

## Evidence / provenance

- Harness: scratchpad `lane2_e2e.py` (measurement) / `lane2_e2e_v2.py` (WS-held-open re-run).
- Overflow: `docker logs lupin-rest-dev`, 12:32:24 EDT. Parser: same, job `pg-c24936c8`.
- Code: `expeditor.py:_handle_fuzzy_file_match:1000-1160`; `podcast_generator/job.py:parse_script_response`; `fuzzy_file_prefilter.py`.
- Commits: `7e29bcfe` (config mitigation + splainer), `e651bc76` (Rio's shared pre-filter).
- Row: `a0eaaef3`.

---

## Rehearsal run 2 — 2026-08-03 (session 2c7311f6, Tiffany 💍) — PASS ✅

**Assignment (Mr Radio):** walk Rick's full Lane 2 path with dry-run OFF and prove the podcast reaches the overlay and plays. Fired after the fail-open gate (`414009c3`), Rio's blob-fetch (`fdfe8c02`), and Rio's X-Frame-Options fix all landed. Row `5e05fc21`.

**Harness:** scratchpad `lane2_rehearsal.py` — fixes the early-WS-close bug (holds through the fail-open approval gate to terminal), answers every ask incl. the Script-Review approval (simulates Rick), and adds an **unconditional framing preflight** that aborts before any LLM/TTS spend if `/app/audio` refuses same-origin framing.

**Result (all 6 byte-level checks green):**
- `terminal_reached` — "All podcasts complete! 1 language, ~9.6 min", 26/26 audio segments stitched.
- `overlay_link_embed` — completion abstract carries `[▶️ Play Here](/app/audio?path=…&embed=1)`.
- `mp3_file_nonempty` — `2026.08.03-201423-three-acts…-t.mp3`.
- `audio_blob_200` / `audio_is_audio` / `audio_bytes>1k` — the overlay's exact call, authed `GET /api/io/file`, returned **200 `audio/mpeg`, 11,469,165 bytes**.
- Preflight passed both runs (`X-Frame-Options: SAMEORIGIN`).

**SCOPE (stated to avoid overclaim):** proves **the pipeline** (real job → playable mp3 → Play Here overlay link → blob-fetch plays), **NOT Rick's content path** — Krishna's Lane 2 run owns "his vague sentence reaches his KISS explainer."

**Findings:**
- **Run 1 failed at INPUT resolution, not the podcast path** — a vague "lighthouse" description matched no document (that seed was cleaned up; test user `interactive.job.tester` has one research doc, empty search paths). Refused to plant a fake seed; re-pointed at the real `2026.04.29-three-act-narrative-structure.md`.
- **Bug `f16c7ce1` (P2):** a "Research document not found" failure (a DATA/INPUT error) dead-letters into the Bug-Fix Expediter, which burns Claude Code cycles trying to code-fix a missing file. Observed to **saturate the agentic pool (3/3)** and SLOW this run (~6 min end-to-end) — it did not kill it. Fix direction: classify research-doc-not-found as user-input (re-ask), not a dead-letter/BFE candidate.
- **Self-correction:** a mid-run "stalled" escalation was premature — the job was slow (pool-contended), not starved.
