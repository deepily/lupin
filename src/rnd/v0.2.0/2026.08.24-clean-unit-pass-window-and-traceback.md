# Clean unit pass — the window, and the one failure in full

**Date** 2026-08-24 · **Author** Clayton 😎 (`d4af6641`) · **Manager** Mr. Radio 🦉
**Why this exists**: the previous unit number was DISCARDED as unattributable — two runs
wrote into one log file and left two conflicting summaries. This is the replacement pass,
run once, with the window recorded at launch instead of reconstructed afterwards.

---

## Result

```
FAILED src/tests/unit/test_user_prompt_submit_hook.py::TestHeartbeatPokeReset::test_reset_skipped_when_prompt_is_heartbeat_poke
1 failed, 17745 passed, 8 skipped, 11 deselected, 9 xfailed, 88 warnings in 786.73s (0:13:06)
```

## The window, verbatim as written at launch and on completion

```
start_wall   : 2026-08-24 22:45:18 EDT
start_head   : 5b19a20ae62c0dffdf7e97e6c4e3cdf570f17779
start_subject: [LUPIN] The pointer is decorative to the resolver, and "backfill the pointer" was the wrong name for the fix
branch       : wip-v0.2.0-2026.08.03-present-and-demo
--- git status --porcelain AT LAUNCH (the whole tree — peers' dirt rides in this run too) ---
 M src/rnd/v0.2.0/_archive/D/2026.08.24-john-memento-vacuity-and-stash-guard.md
?? src/rnd/v0.2.0/2026.08.24-epic-accordion-mini-plan.md
--- end status ---
--- CONCURRENCY AT LAUNCH (checked, not assumed) ---
NOT the only unit run on the box. A peer's pass was already in flight:
3227766 python3 -m pytest src/tests/unit/ -q --timeout=300 --deselect src/tests/unit/test_dm_tutor_flash_lite_routing.py::test_flash_lite_key_ships_and_routes_to_gemini_vertex_client
3467562 python3 -m pytest src/tests/unit -p no:cacheprovider -m not serial_bridge_guard --tb=long -rA -q
Also present: 17 orphaned jstest_survivor processes
  (leftovers of test_the_kill_LEAVES_NO_SURVIVORS, oldest ~1h, all at 0% CPU)
load average at launch: 4.30, 4.02, 4.69
--- finished ---
end_wall  : 2026-08-24 22:58:31 EDT
end_head  : a1ae0ba83222efa740d679d0e91246acf91aabfa
exit_code : 1
log       : /tmp/claude-1001/-mnt-DATA01-include-www-deepily-ai-projects-lupin/d4af6641-f054-45ff-b8bc-b22291ab6ef6/scratchpad/unit-clean-20260824-224518.log
```

Two things that line up matter more than the count: the porcelain at launch is **two peer
R&D documents and no code**, so this number IS attributable to a code tree equal to
`5b19a20a`; and a peer's unit pass was in flight, recorded as an observed fact and NOT as
an explanation for anything.

## The one failure, complete — no tail pipe, no truncation

```
___ TestHeartbeatPokeReset.test_reset_skipped_when_prompt_is_heartbeat_poke ____

self = <test_user_prompt_submit_hook.TestHeartbeatPokeReset object at 0x7fc5e27c9810>

    def test_reset_skipped_when_prompt_is_heartbeat_poke( self ):
        """REGRESSION (c121037b): the heartbeat self-poke is re-submitted as a
        prompt via tmux; UserPromptSubmit must NOT reset the poke-cap for it
        (that reset every turn defeated the cap — poke_count stuck at 1 across
        23 pokes). A prompt opening with POKE_PROMPT_SENTINEL → reset SKIPPED."""
        from lupin_cli.claude_code.hooks.lib.heartbeat_work_owed import POKE_PROMPT_SENTINEL
        with tempfile.TemporaryDirectory() as tmp_dir:
            poke    = POKE_PROMPT_SENTINEL + " (1 in-progress TODO item(s) you own) and no fresh hold. Resume."
            payload = { "session_id": "abc12345-fake-uuid", "prompt": poke }
            with patch( "lupin_cli.claude_code.hooks.user_prompt_submit.reset_poke_count" ) as mock_reset:
>               _run_hook_main( payload, tmp_dir )

POKE_PROMPT_SENTINEL = 'Do not stop yet'
mock_reset = <MagicMock name='reset_poke_count' id='140482057810144'>
payload    = {'prompt': 'Do not stop yet (1 in-progress TODO item(s) you own) and no fresh hold. Resume.', 'session_id': 'abc12345-fake-uuid'}
poke       = 'Do not stop yet (1 in-progress TODO item(s) you own) and no fresh hold. Resume.'
self       = <test_user_prompt_submit_hook.TestHeartbeatPokeReset object at 0x7fc5e27c9810>
tmp_dir    = '/tmp/tmpm5tkk56e'

src/tests/unit/test_user_prompt_submit_hook.py:346: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

payload = {'prompt': 'Do not stop yet (1 in-progress TODO item(s) you own) and no fresh hold. Resume.', 'session_id': 'abc12345-fake-uuid'}
tmp_dir = '/tmp/tmpm5tkk56e', session_id = 'abc12345-fake-uuid'
dm_reconcile_ctx = '', dm_capture = None

    def _run_hook_main( payload, tmp_dir, session_id="abc12345-fake-uuid",
                        dm_reconcile_ctx="", dm_capture=None ):
        """
        Run the UserPromptSubmit hook main() with mocked stdin/stdout.
    
        The store-backed DM inbox reconcile (surface_dm_inbox) is stubbed so the hook
        test stays hermetic (no :7999 network, no HWM file written to the repo root):
        it returns `dm_reconcile_ctx` and, when `dm_capture` is a dict, records the
        call kwargs there.
    
        Returns the emitted JSON dict.
        """
        import io
        from lupin_cli.claude_code.hooks import user_prompt_submit
        from cosa.config.configuration_manager import ConfigurationManager
    
        def _fake_surface( session_id, extra_surfaced_ids=() ):
            if dm_capture is not None:
                dm_capture[ "session_id" ]         = session_id
                dm_capture[ "extra_surfaced_ids" ] = list( extra_surfaced_ids )
            return dm_reconcile_ctx
    
        # Seed the ConfigurationManager singleton BEFORE capturing stdout — at a
        # virgin (hermetic) module boundary the hook's CM instantiation would
        # otherwise print its init banner into the captured stream and corrupt
        # the JSON parse below.
        ConfigurationManager( env_var_name="LUPIN_CONFIG_MGR_CLI_ARGS" )
    
        captured = io.StringIO()
    
        with patch( "lupin_cli.claude_code.hooks.lib.hook_common.SESSION_DIR",
                    Path( tmp_dir ) / ".claude" / "sessions" ), \
             patch( "sys.stdin", io.StringIO( json.dumps( payload ) ) ), \
             patch( "sys.stdout", captured ), \
             patch( "lupin_cli.claude_code.hooks.lib.hook_common.log_payload" ), \
             patch( "lupin_cli.claude_code.hooks.lib.session_bridge.get_claude_session_id",
                    return_value=session_id ), \
             patch( "lupin_cli.claude_code.hooks.user_prompt_submit.resolve_stable_session_id",
                    side_effect=lambda x: x ), \
             patch( "lupin_cli.claude_code.hooks.user_prompt_submit.surface_dm_inbox",
                    side_effect=_fake_surface ):
            try:
                user_prompt_submit.main()
            except SystemExit:
                pass
    
        output = captured.getvalue().strip()
        if output:
>           return json.loads( output )
                   ^^^^^^^^^^^^^^^^^^^^

ConfigurationManager = <function singleton.<locals>.wrapper at 0x7fc7993a5260>
_fake_surface = <function _run_hook_main.<locals>._fake_surface at 0x7fc48775c220>
captured   = <_io.StringIO object at 0x7fc5e35cf4c0>
dm_capture = None
dm_reconcile_ctx = ''
io         = <module 'io' (frozen)>
output     = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
payload    = {'prompt': 'Do not stop yet (1 in-progress TODO item(s) you own) and no fresh hold. Resume.', 'session_id': 'abc12345-fake-uuid'}
session_id = 'abc12345-fake-uuid'
tmp_dir    = '/tmp/tmpm5tkk56e'
user_prompt_submit = <module 'lupin_cli.claude_code.hooks.user_prompt_submit' from '/mnt/DATA01/include/www.deepily.ai/projects/lupin/src/lupin_cli/claude_code/hooks/user_prompt_submit.py'>

src/tests/unit/test_user_prompt_submit_hook.py:96: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

s = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
cls = None, object_hook = None, parse_float = None, parse_int = None
parse_constant = None, object_pairs_hook = None, kw = {}

    def loads(s, *, cls=None, object_hook=None, parse_float=None,
            parse_int=None, parse_constant=None, object_pairs_hook=None, **kw):
        """Deserialize ``s`` (a ``str``, ``bytes`` or ``bytearray`` instance
        containing a JSON document) to a Python object.
    
        ``object_hook`` is an optional function that will be called with the
        result of any object literal decode (a ``dict``). The return value of
        ``object_hook`` will be used instead of the ``dict``. This feature
        can be used to implement custom decoders (e.g. JSON-RPC class hinting).
    
        ``object_pairs_hook`` is an optional function that will be called with the
        result of any object literal decoded with an ordered list of pairs.  The
        return value of ``object_pairs_hook`` will be used instead of the ``dict``.
        This feature can be used to implement custom decoders.  If ``object_hook``
        is also defined, the ``object_pairs_hook`` takes priority.
    
        ``parse_float``, if specified, will be called with the string
        of every JSON float to be decoded. By default this is equivalent to
        float(num_str). This can be used to use another datatype or parser
        for JSON floats (e.g. decimal.Decimal).
    
        ``parse_int``, if specified, will be called with the string
        of every JSON int to be decoded. By default this is equivalent to
        int(num_str). This can be used to use another datatype or parser
        for JSON integers (e.g. float).
    
        ``parse_constant``, if specified, will be called with one of the
        following strings: -Infinity, Infinity, NaN.
        This can be used to raise an exception if invalid JSON numbers
        are encountered.
    
        To use a custom ``JSONDecoder`` subclass, specify it with the ``cls``
        kwarg; otherwise ``JSONDecoder`` is used.
        """
        if isinstance(s, str):
            if s.startswith('\ufeff'):
                raise JSONDecodeError("Unexpected UTF-8 BOM (decode using utf-8-sig)",
                                      s, 0)
        else:
            if not isinstance(s, (bytes, bytearray)):
                raise TypeError(f'the JSON object must be str, bytes or bytearray, '
                                f'not {s.__class__.__name__}')
            s = s.decode(detect_encoding(s), 'surrogatepass')
    
        if (cls is None and object_hook is None and
                parse_int is None and parse_float is None and
                parse_constant is None and object_pairs_hook is None and not kw):
>           return _default_decoder.decode(s)
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^

cls        = None
kw         = {}
object_hook = None
object_pairs_hook = None
parse_constant = None
parse_float = None
parse_int  = None
s          = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'

/home/rruiz/.local/share/uv/python/cpython-3.13.7-linux-x86_64-gnu/lib/python3.13/json/__init__.py:346: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <json.decoder.JSONDecoder object at 0x7fc79c9334d0>
s = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
_w = <built-in method match of re.Pattern object at 0x7fc79c8402b0>

    def decode(self, s, _w=WHITESPACE.match):
        """Return the Python representation of ``s`` (a ``str`` instance
        containing a JSON document).
    
        """
>       obj, end = self.raw_decode(s, idx=_w(s, 0).end())
                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

_w         = <built-in method match of re.Pattern object at 0x7fc79c8402b0>
s          = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
self       = <json.decoder.JSONDecoder object at 0x7fc79c9334d0>

/home/rruiz/.local/share/uv/python/cpython-3.13.7-linux-x86_64-gnu/lib/python3.13/json/decoder.py:345: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <json.decoder.JSONDecoder object at 0x7fc79c9334d0>
s = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
idx = 0

    def raw_decode(self, s, idx=0):
        """Decode a JSON document from ``s`` (a ``str`` beginning with
        a JSON document) and return a 2-tuple of the Python
        representation and the index in ``s`` where the document ended.
    
        This can be used to decode a JSON document from a string that may
        have extraneous data at the end.
    
        """
        try:
            obj, end = self.scan_once(s, idx)
        except StopIteration as err:
>           raise JSONDecodeError("Expecting value", s, err.value) from None
E           json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)

idx        = 0
s          = '\x1b[2m2026-08-25T02:56:56.747229Z\x1b[0m [\x1b[32m\x1b[1minfo     \x1b[0m] \x1b[1mSession ID changed: claude.code@lu...en_ended_batch \\u2014 never AskUserQuestion\\n\\u2022 ack receipt in 1 line before tool calls\\n</system-reminder>"}}'
self       = <json.decoder.JSONDecoder object at 0x7fc79c9334d0>

/home/rruiz/.local/share/uv/python/cpython-3.13.7-linux-x86_64-gnu/lib/python3.13/json/decoder.py:363: JSONDecodeError
------------------------------ Captured log call -------------------------------
INFO     lupin_mcp.cosa_voice_mcp:cosa_voice_mcp.py:520 Session ID changed: claude.code@lupin.deepily.ai#d7335dac -> claude.code@lupin.deepily.ai#b98b9c38 (context clear detected)
```

## What it is, and what I could not establish

The hook emitted **empty stdout**, so the test's `json.loads` raised
`Expecting value: line 1 column 1 (char 0)`.

Run alone it **PASSES**, so it is order-dependent rather than a straight defect. That is one
green in isolation against one red in the suite — **one sample each**, and determinism is the
discriminator, so this is reported as UNCLASSIFIED rather than dismissed as a flake.

⚠️ **Not established: whether this is Tiberius's stdout-poisoning bug**
(`2026.08.24-import-time-watcher-thread-poisons-stdout-tests.md`). The signatures differ —
his import-time watcher thread **ADDS** a line to stdout; here stdout was **EMPTY at column 1**.
Same test shape, different signature. Somebody should settle it; I did not.

## The raw log is deliberately NOT in git

The full `-rA` capture is **20 MB / 304,662 lines** — every passing test line included. A blob
that size lives in the history forever and nobody reads it. The complete traceback is quoted
above, which is the part the no-truncation instruction was protecting.

```
path   : /tmp/claude-1001/-mnt-DATA01-include-www-deepily-ai-projects-lupin/d4af6641-f054-45ff-b8bc-b22291ab6ef6/scratchpad/unit-clean-20260824-224518.log
sha256 : e7747591eb44622afb177ec7e2b1102b4a9c28789cf6208ce464f450eca21ec6
size   : 20M, 304662 lines
```

That path is a session scratchpad and will be swept. If the raw capture is wanted, take it
before then and check it against the hash.
