# Q&A-card presentation path: three fuzzy-file-match features fenced to podcast only

**Date**: 2026-08-05 · **Author**: Mr. Radio 🦉 (session `2c3c8645`) · **Store row**: `ffd46737` (P1, bug)
**Status**: ✅ **P0-1 ANSWERED 2026-08-05 15:15:37Z — the Q&A-card path CLEARS the step that killed it.** Fix still NOT authorized; the crew deliverable remains a *proposal*, on Rick's word only.

> **The measurement** (Tiffany 💍, job `pr-a10a55aa`, `:7999`, current code, test user):
> entered *"Phase 3: Generating slide outline"* at **15:15:15Z** → **"Outline generated: 15 slides (2 opening, 10 body, 3 closing)"** at **15:15:37Z**.
>
> **The contrast that makes it mean something**: `pr-62254a7f` entered the *same phase* at 03:07:54Z last night and raised `ValueError: Outline generation returned no usable entries` at 03:08:31Z. Same phase, same route, opposite outcome — the plan-mode fix (`f67189c3`) covers the Q&A path, which had never been retried on it.
>
> **Ran to completion rather than stopping at Phase 3.** Terminal state: **PPTX 5,462 KB, 15 slides**, full chain — expeditor → Phase 3 outline → 4 elaborate → 5 YAML → 6 Marp → 7 visuals (14) → 8.5 export. **The Q&A-card path runs end to end on current `:7999` code.** Writeup: `src/rnd/v0.2.0/2026.08.05-qa-presentation-path-e2e-verification.md`.
>
> ⚠️ **One claim retracted from that run.** The first report said Phases 4-8 "held **with** the expeditor-supplied `audience_context` — the one leg the card path can't vouch for." They ran clean, but `audience_context` **never reaches the orchestrator** (see §6), so every read was `None`. The run proves those phases don't *crash* on expeditor-supplied args; it does **not** exercise `audience_context`. **A set field is not behaviour observed.** That gap stays open until the fix lands.

---

## 1. The report

Rick, 2026-08-05 ~10:50 EDT:

> "The only path that executes is starting a job from the Generate Presentation from Document card. Calling from the Q&A card and manually choosing presentation fails. Look into that, and once you've ascertained what's going on, spin up an SWE team and propose a fix."

## 2. What the logs actually say — the two paths were never run on the same code

Evidence: `docker logs --timestamps lupin-rest-dev`.

| Time (UTC) | Path | Job | Result |
|---|---|---|---|
| 03:02:36 → 03:08:31 | **Q&A card**, presentation mode (`/api/push`) | `pr-62254a7f` | Expeditor ran fully, resolved the real file, reached Phase 3 → `ValueError: Outline generation returned no usable entries` |
| **03:14:57** | *(bounce — boot deploying `f67189c3`, committed 03:15:41 UTC)* | — | `PRESENTATION_PERMISSION_MODE` `"plan"` → `"default"` |
| 03:20:20 → 03:30:48 | **Card** (`POST /api/presentation-generator/submit`) | `pr-c07dbd3d` | ✅ 15 slides, PPTX 4844KB, `pres-56183c6e` |

⇒ **The failure Rick saw has an already-fixed cause.** The Q&A run died ~7 minutes *before* the plan-mode fix was deployed; the card run succeeded ~5 minutes *after*. No Q&A-card attempt exists on fixed code — the last `[MODE] … presentation` line in the log is 03:02:36.

**This is a "not yet measured", not a "works".** Reproducing the Q&A path on current code is the first owed act.

## 3. Three real defects, live right now

All three are the same shape — a `command == "agent router go to podcast generator"` fence in
`src/cosa/agents/runtime_argument_expeditor/expeditor.py`.

| # | Line | Fence | What presentation loses |
|---|---|---|---|
| 1 | 388–392 | `fuzzy_original = original_question if is_podcast else None` | **Auto-resolve never fires.** Presentation can never resolve `source` from the utterance; it always falls to a blank ask |
| 2 | 388–392 | `use_choice_card=is_podcast` | **The D9 disambiguation card never appears.** The two-candidates picker shipped in `2d6de739` is podcast-only |
| 3 | 422 | `if command == "agent router go to podcast generator":` | **The present-but-unresolvable rescue never runs.** If the extractor pre-fills `source` with a topic phrase, a non-path goes downstream — the same class that crashed podcast on *"make me a podcast on KISS"* (row `bd0ce120`). **Severity is contested — see §3.1** |

### 3.1 Fence 3's severity — two independent traces, one open question

My original wording said "degraded". Both crew members went past it, in opposite directions, and **neither has RUN it**:

- **Rachel 🕊️** (read `expeditor.py:422` + `presentation_generator/job.py:225-231`): a pre-filled `source` bypasses the rescue and reaches `job.py:231` → `raise FileNotFoundError`. **A latent hard crash, not a degrade.** Her own caveat: she has *not* proven the presentation extractor ever pre-fills a topic phrase — on `pr-62254a7f` the `source` *was* resolved.
- **Clayton 😎** (traced with `source="KISS"`): presentation's orchestrator **catches** the `FileNotFoundError` at `orchestrator.py:467` → `_ingest_async` returns `None` → `source_content=None` un-null-checked at `265-266` → Phase 2 `len(None)` → `TypeError` swallowed by `except Exception → return []` at `764-770` → the D6-STRICT guard at `279-280` raises `ValueError` → **job FAILED, loudly and cleanly.** Podcast crashed raw only because it opens the file directly.

**RESOLVED — the two reads were never rivals; they describe different inputs.** For a *topic-phrase* `source`, `job.py:_execute:230-231` pre-validates with `os.path.exists` and raises `FileNotFoundError` **before** the orchestrator is constructed (`:259`). So `orchestrator.py:467` is **unreachable on this input**. Clayton's path covers a `source` that *passes* the `:231` check and then fails during ingest (permission, vanished mid-run) — a genuinely different case, correctly traced, and he conceded the site unprompted.

**Agreed propagation — a clean FAILED job, not a process crash.** `:231` raises → `_execute`'s `try` at `:222` is `finally`-only, no `except` (`:358`) → `do_all`'s `except Exception` at `:175-188` sets `state=FAILED`, `error="Source document not found"`, then **re-raises to the agentic-pool Future** (the designed contract) → dead-lettered. Rachel's own correction of her first wording: *"uncaught at `job.py:231`" was right at the method level and imprecise at the system level — it IS caught, at the queue.* **Her SITE + his OUTCOME are the same path.**

✅ **PROVEN IN PRESENTATION — Rachel RAN it** (2026-08-05, unit level):

```
PresentationGeneratorJob( source_path="KISS", dry_run=False ).do_all()
→ FileNotFoundError( 'Source document not found: KISS' )
→ state == FAILED,  error == 'Source document not found: KISS'
```

So this is no longer "reconciled by two reads" — it is a **measured** outcome matching the reconciliation. Her judgement on cost was also right: the isolated call was cheap after all (the existence check fires before any LLM work), so it cost nothing and **did not spend the owed enqueue-run**.

⏸️ **Still owed, deliberately**: the **full-path proof on `:7999`** — a real presentation enqueued with a topic-phrase `source`, showing what the *user* sees. That stays parked as the fence-3 fix's proof, because the unit receipt proves the mechanism, not the user-facing outcome. Off today's critical path.

**Process note worth keeping**: Rachel volunteered *"I did NOT run it"* before being asked, then volunteered *"I ran the cheap one while your message was in flight"* the moment that changed. Both disclosures were unprompted, and the second one upgraded a parked question to a closed one for free.

### 3.2 The podcast wording leak — CONFIRMED LIVE, not inferred

Tiffany 💍 captured it on job `pr-a10a55aa` (2026-08-05, current code), ask #2 of a **presentation** job:

```
title:   "Missing: research"
message: "Which document should I use for the podcast? Describe it or say the filename."
```

Both the podcast **arg name** (`research`) and the word **"podcast"** reach a presentation user. Source: `expeditor.py:1357-1361`. This is a receipt from the live path, not a read of the source — the prediction was made first and then observed.

**Cosmetic tell that proves the shared handler.** On Rick's presentation job at 03:03:54 the ask read:

```
title:   "Missing: research"
message: "Which document should I use for the podcast? Describe it or say the filename."
```

Hardcoded at `expeditor.py:1357-1361` (and the three fallback asks at 1370–1388), which pass the literal arg name `"research"`. The presentation registry's own question — *"Which document should I convert to a presentation, or which YAML to re-render?"* (`agent_registry.py:150`) — is never used on this path.

## 4. Why the fences exist — they were correct when written

Last night's review of `2d6de739` required the presentation path be untouched **by construction**, not by the author's assertion that it was unaffected. The `is_podcast` fence delivered exactly that, and the reviewer was right to demand it. The follow-through — extending the behaviour to presentation *with its own tests and its own live proof* — was never scheduled.

⇒ **The fix is not "delete the fence."** It is "make the behaviour general, and prove it on the presentation path the same way it was proven on the podcast path."

## 5. Owed, in order

1. **Reproduce the Q&A-card presentation path on current code** — the claim must be measured where it has to hold. Capture the actual failure (or the actual success) with a job id and log lines.
2. ~~**Enumerate every consumer of `fuzzy_file_match`** at HEAD — `research_to_presentation`, TFE's `tfe_checkpoint_match` sibling, any future entry.~~ ✅ **DONE — and my framing was wrong.** Rachel 🕊️ re-derived by grep-at-HEAD: the consumer set is **closed at exactly two** — `research` (`agent_registry.py:75`, podcast) and `source` (`agent_registry.py:163`, presentation). **`tfe_checkpoint_match` is a SEPARATE handler** (`expeditor.py:397`) with its own fence, not a `fuzzy_file_match` consumer; calling it a "sibling" here was my error. No hidden third. ⇒ **The fix must generalize the fence per-consumer, not merely flip podcast→both.**
3. **Propose the fix** — including how the arg name and prompt wording become per-agent rather than hardcoded podcast.
4. **Propose the tests** — unit at the expeditor seam, plus a live proof on the presentation path equivalent to the WebSocket capture that proved the podcast card.

## 6. What the day found that this document was not looking for

The brief was "why does the Q&A path fail." The path turned out to work. What the crew found while proving that is larger than the thing they were sent for.

| Finding | Evidence | State |
|---|---|---|
| **61% of Rick's source never reaches the model** — clip at 30,000 chars in `narrative.py:178` AND `elaboration.py:171`; his outline is **77,621** chars | measured in-container | ✅ **Fix authorized** |
| **The same clip exists in podcast** — `script_generation.py:262` | Rachel, firsthand | ✅ Same commit, shared key |
| **`audience_context` is silently discarded** — `job.py:249-256` copies duration/audience/theme/model and skips it; `config.py` has no such field, so `getattr(…, None)` is always `None` | verified at HEAD | ✅ **Fix authorized** |
| The two prompts contradict each other — narrative *"close to 12"*, outline *"exactly 15"*; **zero test coverage of the formula** | María, `narrative.py:200` / `outline.py:235` | ⏳ Pending Rick |
| **Gate 1's "Revise" is a no-op** — feedback stored at `orchestrator.py:1827`, but `get_narrative_analysis_prompt` has no parameter to receive it. It re-rolls the identical call and burns a revision | María | ⏳ Pending Rick |
| The `max_tokens` ceilings are **phantoms** — `api_client.py:402` says *"unused on the bounded path"*; `ClaudeAgentOptions` never sets it | Clayton, then María independently | ℹ️ Two seats each nearly built a fix against it |

### 6.1 Two same-named files, and the disagreement they caused

`2026.08.03-presentation-outline-lupin-and-planning-is-prompting.md` exists **twice**: `src/rnd/v0.2.0/…` at **48,473** bytes and `io/deep-research/ricardo.felipe.ruiz@gmail.com/…` at **77,621**. Rick fed the `io` copy. Two seats measured different files, reported 38% vs 61% loss, and both were internally consistent. **Cite the full path or measure the wrong document.**

## 7. Ruling — 2026-08-05 ~11:37 EDT

Rick, on a four-option menu with pros/cons per option:

> *"Let's do both context fixes per your recommendation and hold the third for now. I'd love to have this working but I don't think I'm going to demo file matching for presentation jobs. I definitely want to implement it though, so perhaps later make note of it — but not now."*

- ✅ **GO** — the source-clip ceiling (3 sites, one commit, shared INI key) and the `audience_context` copy.
- ⏸️ **HELD, not dropped** — the three fences in this document. Row `5bc22180` carries the proposal, the review, and the owed proofs. **The split was exposure, not risk**: the authorized pair changes what the *model* sees; the fences change what *Rick* sees, the day before he presents.
