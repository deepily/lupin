# 1b — heartbeat-poker factory drift: closed-enumeration proof + disposition

**Author**: Tiberius 👑 (d7fd5dde) · **Worktree HEAD**: 1841186d · **For**: Rachel 🕊️
**Date**: 2026-08-15 · **Method**: Rachel's — prove absence of callers by CLOSED
enumeration of entries into `create_agentic_job`.

## The red
`test_1b` asserts `factory_commands == AGENTIC_COMMANDS` (set-equality both directions).
`agent router go to heartbeat poker` is in the factory (`agentic_job_factory.py:374`,
constructs `HeartbeatPokerJob` at :392) but NOT owned in the v2 registry → `factory − owned`
is non-empty → red.

## Closed enumeration: who passes "heartbeat poker" to create_agentic_job?
Every NON-TEST entry into `create_agentic_job`, and the command each passes:

| Call site | Command argument | Can it be "heartbeat poker"? |
|---|---|---|
| `test_suite_completion_watchdog.py:258` | literal `"agent router go to test fix expediter"` | No — fixed START |
| `dead_queue_watchdog.py:470` | BFE (fixed) | No |
| `agents/test_fix_expediter/orchestrator.py:2132` | validation job (fixed) | No |
| `agents/bug_fix_expediter/job.py:554` | BFE re-dispatch (fixed) | No |
| every agentic router (`routers/bug_fix_expediter:96`, `swe_team:142`, `deep_research:156`, `podcast:511/698`, `presentation:217`, `deep_research_to_*`, `claude_code:181`, `test_suite:157`, `mock_job:332`) | each its OWN fixed command constant | No |
| `todo_fifo_queue.py:1216` | `command` = ROUTER output | No — router is prompt-gated; heartbeat poker is absent from the prompt, so the router substitutes the nearest LISTED command, never emits it |
| `todo_fifo_queue.py:1311` | `routing_command` = ROUTER output (speculative path) | No — same prompt-gating |
| `lupin_app/main.py:1075` | `routing_cmd` from `get_restorable_jobs()` (persisted jobs restored at boot) | No — can only restore a job that was previously CREATED; none can be (branch unreached) |
| `agentic_job_factory.py:462` | `job_info["routing_command"]` (RESUME reconstruction) | No — resumes a previously-created job; none can exist (branch unreached) |

**The set is closed and empty.** The three router/persist/resume paths could only carry
"heartbeat poker" if it were already emitted (impossible — not in the prompt) or already
created (impossible — the only creator is the very branch under test, and nothing reaches
it). No cron, no API endpoint, no dispatcher passes it.

⇒ The `:374` factory branch has NO production entry. Its sole constructor
(`HeartbeatPokerJob(...)` at :392) is reached ONLY by two tests
(`tests/smoke/test_heartbeat_poker_smoke.py:88`, `tests/unit/rest/test_agentic_job_factory_heartbeat.py:47`).

## But it is UNWIRED, not ABANDONED
Evidence it is a deliberate, documented capability — not accidental dead code:
- Design docs: `src/rnd/v0.1.7/2026.05.20-generic-heartbeat-poker-abstraction-design.md`,
  `2026.05.22-heartbeat-poker-d1d4-class-spec.md`.
- `HeartbeatPokerJob` is a full `AgenticJobBase` (commons gateway, RecipientSpec, clock injection).
- The fleet arbiter (`agents/heartbeat_arbiter/arbiter_job.py`) EXTENDS `HeartbeatPokerJob` —
  the class is load-bearing for the arbiter (the arbiter constructs its own SUBCLASS directly,
  not through this factory branch, which is why the factory branch shows no production caller).
- Two dedicated test files.

heartbeat poker is NOT in the router prompt, NOT in the training corpus, NOT in PRODUCT_NAMES
(the card) — confirmed by grep. It is never user-reachable by ANY surface; it is a
system/programmatic agentic job whose factory-dispatch was built but not wired.

## Disposition — I recommend REGISTER, not REMOVE (Rachel's call; touches María's registry)
Rachel's dichotomy was "prove the branch dead ⇒ remove; can't ⇒ registry owns it." The
enumeration proves NO CURRENT CALLER — but the design docs + arbiter dependency + tests prove
it is intentional. That is "unwired," which lands on registry-owns-it.

**Option 2a (recommended) — register + a 3-surface exemption.**
Add `agent router go to heartbeat poker` to the v2 registry as an owned agentic command with
`cli_module=None` (system-invoked, like test suite → 5'/§4 skip it, no RAE `AGENTIC_AGENTS`
dependency), and an `INITIAL_DETECTION_EXEMPTIONS` entry `surfaces=["prompt","training","card"]`
(never a user-routing target on any surface). Then `factory == owned` holds → 1b greens by a
FIX; and the exemption honestly records "owned + factory-only, no user surface." Reason cites
factory:392 + the design docs + the arbiter dependency. Preserves the capability and both tests.
Aligned with the single-source endgame (phase 4/5: prompt + factory DERIVED from the registry —
every factory branch must have a registry entry).

**Option 2b — factory-surface exemption.** Keep it OUT of the registry; add a NEW facility so
test_1b tolerates a factory branch not in `owned` when the command carries a "factory" exemption.
Introduces a brand-new surface concept for one command. More machinery, no reuse of the
existing facility.

**Option 1 — remove.** Delete the `:374` branch + the 2 test files. Cleanest by LoC, but
discards documented, tested, intentional code, and the enumeration proves "no caller now," not
"meant to be gone."

### Why 2a over the others
- Reuses the exemption facility exactly as designed (owned-but-not-user-reachable).
- No new surface concept (2b adds one for a single command).
- Preserves working, documented, arbiter-relevant code + its tests (1 deletes them).
- Matches the design's direction: registry is the single source the factory is derived from,
  so every factory branch SHOULD have a registry entry.

**Blocked on Rachel**: 2a edits María's v2 registry (`registry.py`) — I will not touch it
unilaterally. Greenlight 2a (loop María if she wants) and I land it as a fix, or rule 1/2b.
