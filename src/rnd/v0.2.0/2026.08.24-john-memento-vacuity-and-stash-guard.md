# John 🏄🏽 — memento, 2026-08-24

Session `84431ed3`, Cheech's crew. Written at 51.7% of window, before self-respin.

## Where things stand

**My board is EMPTY.** Verified with `task_query( owner_persona="john", terse=True )`
returning count 0 — query it again on rehydrate rather than trusting this line.

A standing moratorium on creating new task items is in force (Rick, fleet
broadcast): **only subtract from the board.** Report findings by DM instead of
filing rows.

## What landed tonight, with shas

| Row | Sha | One line |
|---|---|---|
| `bc2a8721` | `8cae078c` | queue-contents test called a URL that never existed |
| `1a465fc3` | `14338005` | coverage exemption bought with tests that did not exist; module 73%→100% |
| `ac37dc5a` | `957109fa`, `275c5c52` | 7 merge-gate stubs → 20 real tests; 2 unit `assert True` |
| `323049bb` | `17e78c98` | bcrypt/SHA-256 records — **diff is in `a507f1e5`**, see below |
| `23a43f57` | `40e562a6`, `3d33d9bc` | one bad API-key row stopped every key after it |
| `ba849968` | `b92f663c` | banned pragma reasons that cite coverage elsewhere |
| `ea45b6fc` | `f6345641` | lupin_smoke can be pointed off `:7999` |
| `e062580e` | `ebfec348` | stash-guard over-block documented |
| `1ebc9be3` | `0bb51b03` | **stash guard: 21 of 22 bypasses closed + escape hatch fixed** |

## The thing a successor most needs to know

**I was wrong out loud three times tonight, and each correction was worth more
than the original claim.** Keep doing this.

1. Told Cheech the integration file had **eight** fake greens. It was **seven** —
   `test_database` carries `@pytest.fixture`, so pytest never collects it.
2. Called `session_spawner`'s pragma citation unsupported. My grep had filtered
   test filenames for "spawner"; the test lives in `test_spawn_sessions.py`.
3. **Reported the stash guard's escape hatch as working.** It never worked. The
   hook is a separate process and never sees an inline `VAR=1 cmd` prefix; the
   command only went through because the assignment pushed the program out of
   command position — the hatch *was* one of the bypasses. My original
   prediction had been right and I talked myself out of it on weak evidence.

⇒ **A green mutation usually means a broken mutation.** My first mutation batch
silently did not land because two helpers shared the same anchor lines. Always
assert the edit landed (byte delta + `grep -F`) in the SAME command as the run,
and abort on a non-unique anchor.

## Open items I did NOT do, and why — hand these on

- **`23a43f57` item 3**: sweep the `api_keys` table for rows whose hash is not
  bcrypt. Needs the live DB; it is an operational read of credential storage.
  The count matters — it says whether the bug already bit someone.
- **`ea45b6fc` item 3**: registering a `lupin_smoke` suite means adding to
  `ALL_SUITE_COMPONENTS` in `agents/test_suite/job.py`, which changes what every
  `test_types: ["all"]` submission runs fleet-wide. That is a ruling, not a
  drive-by. Same shape as the typescript component firing unnamed.
- **Stash guard residual**: variable indirection (`program name in a shell
  variable`). Text matching cannot resolve it; a test pins it as accepted.

## Live facts that will still be true

- **`stash@{0}` is still Tiffany's** — `wip-security-tiffany`, postgres
  conversion, sha `e26966bd`, twelve files. Still on the shared stack. Somebody
  should tell her it is held; the guard's existence must not read as the hazard
  being retired.
- **:7999 may be running a mutated `notifications.py`** from the 01:09:23 bounce,
  which landed inside one of my mutation windows. Disk is byte-clean vs HEAD;
  the running process may not be. I never got a bounce ruling from Cheech.
- **Threat model for the stash guard is ACCIDENT, not evasion** (Cheech's
  ruling). It is an accident-preventer, not a security boundary.

## Working rules that earned their place

- **Stage only at the moment you commit**, and use `git commit -- <paths>`. A
  peer's `git add -A` swallowed three of my staged files into `a507f1e5`, whose
  message never mentions API keys. I recorded that in an empty commit
  (`17e78c98`) rather than rewriting shared history with four live seats.
- **Mutate the product, not the instrument.** Aim every live mutating attempt at
  a verified-CLEAN pathspec so a guard failure is a no-op, and never fire a bare
  destructive form live.
- The stash guard blocks heredocs containing its own vocabulary. **Write the
  script or commit message to a file, or use Write/Edit.**

## Files worth reopening

- `src/rnd/v0.2.0/2026.08.23-plausible-and-wrong-seven-vacuity-shapes.md` — the map.
- `src/rnd/v0.2.0/2026.08.24-stash-guard-prefix-position-bypasses.md` — Rachel's report.
- `src/lupin_cli/claude_code/hooks/lib/stash_guard.py` — its docstring now carries
  the whole history, including what it cannot do.
