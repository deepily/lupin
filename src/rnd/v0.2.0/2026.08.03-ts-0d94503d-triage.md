# Triage — test-suite run `ts-0d94503d`

**Triager**: Clayton 😎 (session ee92a5fd) · **Date**: 2026-08-03 · **Branch**: `wip-v0.2.0-2026.08.03-present-and-demo` @ `7a040cd2`

## Headline verdict

**ZERO regressions from this branch.** All 25 smoke + 3 unit failures are either pre-existing on `main`, environmental (container/venue), or full-suite contention. Every failure was reproduced on the `:7999` dev container at the **same commit** the test run used. The two green-but-slow demo-relevant tests (calculator, expeditor) point at one external root cause (Tiffany's phi-4 8k-context blow-up), not our code.

## Proof it can't be our branch

Branch = merge-base `1fa05b16` (confirmed on `origin/main`) + 6 commits. **Entire file delta:**

- `src/lupin_app/static/html/notifications.html` (cache-bust tokens)
- 4 R&D markdown docs
- `src/tests/smoke/test_llm_spec_key_served.py`, `src/tests/unit/test_task_body_overlay_cache_bust.py` (both green)

It touches **none** of the code any failing test exercises (agents, alembic, dm/commons/broadcast/crud/model-server, session_bridge). By construction, no failure here is a code regression from this branch. Every failing test file was last modified at or before the merge-base.

## Run-integrity findings (read these first)

1. **Both failing tiers TIMED OUT, not "failed cleanly."** Attestation `tier-runs.jsonl`: unit killed at **300s** ("9736 passed, 2 failed"), smoke killed at **3600s** ("174 passed, 25 failed"). Counts are **partial, file-level, recovered from progress dots** — the on-disk container logs contain **no FAILURES section and no tracebacks**. Unit timing out at 300s is recurring (same on 2026-07-27 run `ts-1bcfb761`) → harness budget issue, not our branch.
2. **pytest `INTERNALERROR` manufactures phantom failure counts.** `_pytest/unittest.py:382` calls `isinstance(call.excinfo.value, unittest.SkipTest)` where `call.excinfo` is a bare tuple under Python 3.13 → `AttributeError: 'tuple' object has no attribute 'value'`. Any failing `unittest.TestCase` test trips it; it crashes the terminal summary so real failures surface as phantom counts. **Verdict: py3.13 toolchain collision, NOT our code.** It will mislead every future run until the pytest stack is pinned/patched. `swe_team_job` (the visible unit `F`) passes **22/22 in isolation** — it was a phantom.

## Tier status (ts-0d94503d)

| Tier | Result | Notes |
|---|---|---|
| typescript | ✅ 2279 passed | complete log |
| websocket | ✅ 50 passed | complete log |
| integration | ✅ 379 passed, 53 skipped | complete log, `✓ All tests passed` |
| unit | ⏱️ TIMEOUT@300s (2 phantom fails) | see below; all env |
| smoke | ⏱️ TIMEOUT@3600s (25 fails) | see below |
| e2e | ⏳ still running | started 16:15 UTC, no attestation/verdict yet as of 16:45 |

## Smoke — per-file verdicts (reproduced on :7999 dev, same commit)

| Test(s) | Verdict | Evidence | Action |
|---|---|---|---|
| `alembic_...idempotency` ×4 | **Environmental** | `ProgrammingError: type "vector" does not exist` — pgvector ext absent in throwaway DBs; repro on dev | Provision pgvector in throwaway/template DB, or `CREATE EXTENSION vector` in fixture |
| `approach_d_user_messages` | **Environmental (batch)** | **PASSES on dev** (rc=0, 24s) | none — full-suite contention |
| `bfe_phase6_repair_loop` | **Environmental (batch)** | **PASSES on dev** (rc=0, 15s) | none |
| `broadcast_two_session_e2e` | **Pre-existing** | `AssertionError` ack status `completed-with-withheld` mismatch; broadcast code untouched; repro on dev | Update test expectation to current withhold behavior (or fix handler) |
| `calculator_live_pipeline` | **Open → likely external** | Timed out 70s on dev; live model pipeline; matches Tiffany's phi-4 8k-context blow-up | Re-check after Rio's expeditor fix |
| `claude_code_dry_run` | **Environmental (batch)** | **PASSES on dev** (rc=0, 55s) | none |
| `commons_two_session_roundtrip` | **Environmental (batch)** | **PASSES on dev** (3 passed rc=0) | none |
| `conv_mode_three_layer` ×3 | **Pre-existing (stale test)** | `AttributeError: session_bridge has no attribute 'get_conversation_mode'` (mock target removed); untouched | Update mock/test to current `session_bridge` API |
| `crud_live_pipeline` | **Environmental (batch)** | **PASSES on dev** (rc=0, 65s) | none |
| `deep_research_dry_run` | **Environmental (batch)** | **PASSES on dev** (rc=0, 14s) | none |
| `dm_endpoint_smoke` ×3 | **Pre-existing (stale test)** | 400 `sender_project is REQUIRED` (server contract change, row 12b5a766); untouched | Update test to send `sender_project` |
| `expeditor_mock_job_smoke` | **Open → likely external** | `assert False` @41s (live mock job did not reach expected state); matches Tiffany's phi-4 context blow-up | Re-check after Rio's expeditor fix |
| `external_scopes` ×1 (`test_legacy_scope_query_param_is_ignored`) | **Pre-existing (stale test)** | Expects `?scope=` ignored; server returns **400** (aggressive-400 policy flipped 2026-05-21, documented in CLAUDE.md); untouched | Update test to expect 400 |
| `migration_notif_..._roundtrip` | **Pre-existing (stale test)** | `assert '3da5c0d1eee6' == 'f2a3b4c5d6e7'` — new migration moved head; test hardcodes old revision; untouched | Update expected revision id |
| `model_server_smoke` ×4 | **Environmental** | `401 Invalid X-API-Key` on direct calls — test key mismatch with `lupin-model-server` in this env | Fix X-API-Key config for the model server |

## Unit — real failures (from junit XML, immune to the INTERNALERROR)

**These three are standing false-reds, not passive "environmental":** each is unconditionally red in the test container regardless of code, so the full pyramid can never report green there until each is closed at its venue — supply the precondition, or skip with an explicit reason naming it (never bare xfail, never delete the assertion). Owned by Clayton to close.

### ✅ CLOSED (Clayton, 2026-08-03)

All three fixed and verified in BOTH venues. Container (preconditions absent) → `notify_user` **PASSED**, other two **SKIPPED with named reasons**; host (tmux + CLAUDE.md present) → **all 3 PASSED** (assertions actually execute). No bare xfail, no deleted assertions.

- `test_notify_user_connection_error` — **supplied the precondition** (hermetic fix): added the missing `patch('lupin_cli.notifications.notification_models.resolve_target_user', return_value=...)` its sibling success-test already uses, so it no longer depends on ambient `target_user` env and actually reaches the `ConnectionError` branch. Passes in both venues.
- `test_dry_run_exits_before_tmux` — **skip-with-reason** `if shutil.which("tmux") is None` (naming the tmux-binary precondition). The dry-run-exits-0 coverage is not lost when skipped — sibling `test_dry_run_forwards_sourced_roster` asserts it without tmux.
- `test_the_merge_checklist_names_the_serial_runner` — **skip-with-reason** `if not CLAUDE_MD.exists()`. **Where it CAN run:** any full working-tree checkout — host, git worktree, or CI clone. **Why it can't run in the container:** the file-bind mount includes `pytest.ini` but NOT `CLAUDE.md`. To run it in-container, mount `CLAUDE.md` (infra change, not a test change).

| Test | Verdict | Evidence | Action |
|---|---|---|---|
| `test_bridge_dir_guard::test_the_merge_checklist_names_the_serial_runner` | **False-red (env precondition)** | `FileNotFoundError: /var/lupin/CLAUDE.md` (absent at path in container). **Tier-2 contact check named in PR merge requirements** — highest priority. | Make it resolve CLAUDE.md where it exists, or skip-with-reason naming the venue where it CAN run; state that venue plainly |
| `test_fleet_roster_env::test_dry_run_exits_before_tmux` | **False-red (env precondition)** | `FileNotFoundError: 'tmux'` (binary not installed in container) | Install tmux in the image, or skip-with-reason `"requires tmux binary"` |
| `test_notify_user::test_notify_user_connection_error` | **False-red (env precondition)** | `ValueError: Cannot resolve target_user` (env unset) | Supply `target_user` in the fixture, or skip-with-reason naming the missing env |
| _(pytest INTERNALERROR)_ | **Tooling (py3.13)** | see Run-integrity finding #2 | Pin/patch pytest for py3.13 unittest reporting |

## Thursday demo-path flags (2026-08-06)

- **Podcast generation** — ✅ not in the failing set.
- **Notifications / WebSocket** — ✅ websocket 50/50 green; the `test_notify_user` unit failure is env (`target_user` unset), not a delivery defect.
- **Runtime Argument Expeditor** — ⚠️ **the one demo-relevant risk.** `expeditor_mock_job` (`assert False`) + `calculator_live_pipeline` (timeout) are consistent with Tiffany's finding: the expeditor's fuzzy file matcher overruns phi-4's 8k context (199,777 tokens → HTTP 400) when it walks `/src`. Not our branch; **re-check both once Rio's context fix lands.**
- **File resolution** — same candidate root cause as the expeditor; covered above.

## Recommended posture

No merge bypass needed and none proposed. Nothing here is a regression from this branch. The green gate is blocked only by (a) pre-existing stale tests, (b) three standing false-red unit tests (Clayton to close), (c) one external expeditor/phi-4 defect Rio is already fixing, and (d) a py3.13 pytest-reporting collision. Fix those at their source; do not attribute any to `wip-v0.2.0`.
