# V1 Excision + Coverage to 100% — Plan

> ✅ **REVIEWED 2026-08-26 — María: APPROVE WITH FOUR FIXES.** Her doc: [`2026.08.26-maria-review-v1-excision-coverage-plan.md`](2026.08.26-maria-review-v1-excision-coverage-plan.md). All four ratified decisions stand. **Every load-bearing claim in her review I re-verified against the tree myself before amending** — see §6. One finding is a real break this plan would have shipped; two are *right actions with wrong reasons*, which are corrected in place below so the record does not teach the wrong mechanism.
>
> 🔴 **§6.1 MUST LAND BEFORE STEP 3 DELETES ANYTHING.**

**Author**: Mr. Radio 🦉 (`08e47ef3`) · **Date**: 2026-08-26 · **Branch**: `wip-v0.2.0-2026.08.03-present-and-demo`
**Status**: ✅ **APPROVED by Rick 2026-08-26**, all four §5 decisions ratified — ⏸️ **now HELD pending María's sanity check** (see banner). Execution is on this branch.
**Rulings this plan implements**: V2 is a GO; V1 is deprecated and removed (Rick, 2026-08-26 ~12:10 EDT). 100% coverage and V1 excision take precedence over other board items.

---

## 0. The one-paragraph version

`/api/push` is **already** a 410 tombstone, so the v1 *product door* was retired weeks ago. What is left of "V1" is almost entirely the **paired-eval apparatus** built to compare v1 against v2 — ~2,950 lines of scripts, ~3,650 lines of tests, a pinned 15536409 worktree, a dedicated `:7997` server, a compose env pair and a pytest marker. It exists to answer a question Rick has now answered. Separately, the coverage frame sits at **96% (2,337 of 63,240 statements missing)**, and **roughly a quarter of that gap is not our code at all** — ~610 statements of CPython's own `json` module are inside the denominator. The two workstreams interact: excising V1 deletes both code and its tests, so the coverage number must be re-measured after, not predicted.

---

## 1. What "V1" actually is — measured, not assumed

| Layer | State today | Action |
|---|---|---|
| **`/api/push`, `/api/push-agentic`** | **Already 410 tombstones** (`_retired_doors.py:130,165` → `V2_ASK` / `V2_SUBMIT`) | none — done |
| **v1 eval harness** (scripts) | live, 2,953 lines | **DELETE** |
| **v1 eval harness** (tests) | live, ~3,646 lines across 10 files | **DELETE with them** |
| **Pinned worktree** `lupin-v1-baseline-15536409` | on disk, server stopped today | **DELETE** |
| **`:7997` launcher + wedge watchdog** | 322 lines of shell | **DELETE** |
| **Compose env** `LUPIN_V1_ARM_BASE_URL`, `LUPIN_PAIRED_N` | in `docker-compose.yml` | **REMOVE** (needs `--force-recreate`) |
| **`paired_eval_live` pytest marker** | registered + deselected in `pytest.ini` addopts | **REMOVE** |
| **Agent classes** (`TodoListAgent`, `MathAgent`, `CalendaringAgent`, …) | **v2's registry uses these as its `factory`** | 🔴 **KEEP — these are NOT v1** |

### 🔴 The distinction that must not be got wrong

**The agent classes are shared, not v1.** `cosa/rest/v2/registry.py` imports `TodoListAgent`, `CalendaringAgent`, `MathAgent`, `WeatherAgent`, `ReceptionistAgent`, `DateAndTimeAgent`, `CalculatorAgent` and uses them as each `AgentSpec.factory`. Deleting them because they predate v2 would take v2 down with them. **"Remove V1" means remove the v1 *comparison apparatus* and the v1 *entry path*, not the agents that do the work.**

### The files, named

**Scripts (2,953 lines)** — `src/scripts/`: `v1_eval_arm.py` (1,302) · `paired_eval.py` (715) · `render-paired-run-report.py` (298) · `launch-v1-baseline-7997.sh` (263) · `pair-warm-spans.py` (210) · `watch-paired-block-failure-rate.py` (106) · `watch-v1-arm-wedge.sh` (59)

**Tests (~3,646 lines)** — `src/tests/unit/`: `test_v1_eval_arm.py` (906) · `test_paired_eval.py` (577) · `test_v2_paired_bridge_reachability.py` (571) · `test_eval_isolation_guard.py` (375) · `test_v1_ws_recv_events.py` (364) · `test_paired_run_report.py` (331) · `test_pair_warm_spans.py` (259) · `test_v2_paired_allowlist_config.py` (111) · `test_paired_block_failure_rate_watcher.py` (92) · `test_v2_paired_live_precondition.py` (60) — plus `src/tests/integration/test_v2_paired_live.py` (29 KB).

⚠️ **Three of these are NOT purely v1** and need reading before deletion, not a blanket `rm`:
- `eval_isolation_guard.py` / `test_eval_isolation_guard.py` — the store-isolation guard. **v2's own clean-step uses it.** Keep unless proven v1-only.
- `test_v2_eval.py`, `test_v2_terminal_wait.py`, `test_pytest_args_policy.py`, `test_runner_collection_diagnosis.py` — these merely *reference* v1 symbols. They are v2 tests. **Keep; edit the references out.**
- `v2_eval.py` imports `v1_eval_arm` for `make_ws_recv_events` (the terminal-wait wrapper Krishna built). **That WS listener must be relocated into v2's own module before `v1_eval_arm.py` is deleted, or v2's terminal wait dies with it.**

---

## 2. V1 excision — the steps

**Step 1 — Prove the blast radius before deleting anything.**
`grep -rn 'v1_eval_arm\|paired_eval\|V1_PIN\|LUPIN_PAIRED_N\|LUPIN_V1_ARM\|paired_eval_live'` across `src/`, `docker-compose.yml`, `pytest.ini`, `.claude/`, and `src/scripts/*.sh`. Every hit is either **delete**, **edit**, or **keep** — written down per hit, not decided in the moment. Nothing is deleted until this list is complete and reviewed.

**Step 2 — Relocate what v2 still needs.** Move `make_ws_recv_events` + `WsJobEventListener` + `_TERMINAL_STATES` out of `v1_eval_arm.py` into a v2-owned module (proposed: `src/scripts/ws_job_listener.py`). Repoint `v2_eval.py`. Run `test_v2_terminal_wait.py` + `test_v1_ws_recv_events.py` green **before** anything is deleted — the listener tests move with the code.

**Step 3 — Delete, in one commit per layer** so a bisect can land between them: (a) integration test + marker, (b) unit tests, (c) scripts, (d) compose env + launcher shells.

**Step 4 — The worktree.** `git worktree remove` if registered, else `rm -rf`. Confirm `git worktree list` is clean. ~1 GB back.

**Step 5 — Re-measure.** Full frame, isolated `COVERAGE_FILE`, clean tree, recorded sha. **Do not predict the number** — deleting code *and* its tests moves the ratio in both directions and the sign is not knowable in advance.

**Step 6 — Full pyramid**, in the CLAUDE.md order, before this is called done.

### What could go wrong, and the guard for each

| Risk | Guard |
|---|---|
| v2's terminal wait dies with the listener | Step 2 relocates it first, with its tests green, before any delete |
| An agent class gets deleted as "v1" | §1's KEEP row; the registry import list is the authority |
| A shared guard (`eval_isolation_guard`) goes with the harness | Step 1 classifies every hit; nothing blanket-deleted |
| Compose env removed but container not recreated | `--force-recreate` + `docker exec … printenv` to prove absence |
| The `paired_eval_live` marker removed but still in addopts | `--strict-markers` turns a stale marker into a hard error — the suite tells us |

---

## 3. Coverage — where the 2,337 actually are

Measured at `6f75b227`, **tracked-dirty=0**, 21m28s, unit + cosa tiers, isolated `COVERAGE_FILE`:

> **63,240 statements · 2,337 missing · 96% · 27,394 passed, 0 failed · 566 files at 100%**

| bucket | missing | what it is |
|---|---|---|
| 🔴 **CPython stdlib `json/*`** | **~610** | **NOT OUR CODE** — `json/encoder.py` 235 @ 0%, `decoder.py` 206 @ 3%, `scanner.py` 56 @ 0%, `tool.py` 41 @ 0%, `__init__.py` 61 @ 6% |
| `src/lib` | 524 | dead, **unimportable** (`pyaudio` absent) — a decision, not a test-writing task |
| `src/lupin_cli` | 509 | real work |
| `src/lupin_app` | 367 | mostly a measurement-scope artifact — its runtime is the :8000 tiers |
| `src/cosa` | 334 | real work |
| `src/lupin_mcp` | 4 | nearly closed |

**~1,134 of the 2,337 — a shade under half — is not test-writing at all.** It is one config fix and one Rick decision.

### 3a. 🔴 The stdlib is in our denominator

`[tool.coverage.run] source` lists only `src/` directories, so `/home/rruiz/.local/share/uv/python/…/lib/python3.13/json/*` has no business being measured. It is there because **a subprocess recorded it and the data was combined in**: `site-packages/a1_coverage.pth` calls `coverage.process_startup()` whenever `COVERAGE_PROCESS_START` (or `_CONFIG`) is set, and a child started that way with no source config measures everything it imports.

- ~~**Find the setter** — `grep -rn 'COVERAGE_PROCESS_START|COVERAGE_PROCESS_CONFIG' src/` and the runner scripts.~~ ⚠️ **CORRECTED (María §2, re-verified by me):** that grep returns **zero hits repo-wide**. Nothing in this repository sets either variable — the chain is entirely outside the tree, in `.venv/.../a1_coverage.pth` (pytest-cov 7.1.0), which is not ours to edit and not stable across a venv rebuild. **The first step is to CONFIRM the setter is outside the tree, not to find and fix it.**
- ~~**Fix at the source**, then belt-and-braces with an `omit`~~ → **the `omit` for anything outside `src/` IS the fix**, not a belt-and-braces addition. There is no source to fix.
- **Guard it**: a test asserting **every file in the coverage report is under `src/`**. This is the class of defect that returns; a percentage cannot see it. 🔴 **Priority RAISED, not lowered, by the correction above** — with no in-tree source to fix, this guard is the *only* control standing between this repo and a venv-layer change nobody here will see.
- Worth **~1 point** and it is unambiguously correct — nothing of ours is hidden.

### 3b. `src/lib` — Rick's call, not a coverage task

524 statements at 0% because the package **cannot be imported**: `pyaudio` is not installed; `lupin_client_cmd.py` does a bare `import lupin_client`. Last touched 2026-01-28; every in-tree reference is commented out except `run-lupin-gui.sh`, which invokes `python3.10` in a 3.13 repo.

**A. Delete** — removes 524 from the denominator by removing the *code*, worth ~0.84 pts, zero tests written. **B. Revive** — install PortAudio, fix the import, repoint the launcher, then cover it: a project. **C. Leave** — carry a permanent zero no ramp can close.

**Recommendation: A.** This is not the narrow-the-wording option Rick rejected — nothing is hidden; the lines stop existing.

> ✅ **RATIFIED 2026-08-26 — Rick ruled DELETE** (option A), via `/plan-decide`. Recorded in TODO.md's Decisions Log. Not yet executed at time of writing.

### 3c. `src/lupin_app` — scope, and how to settle it

367 missing at ~28% because `main.py` is lifespan + background loops + middleware, exercised by the **:8000** integration/e2e tiers that this baseline does not run. Tiffany's read still holds. Two honest moves, not one: **extract** the testable pure functions (`_managed_bounce_server_label`, `_emit_managed_bounce`, `_log_vram`) into a measured module — the pattern Krishna already used on `main.py`; and for what genuinely only runs under a live server, **measure the :8000 tiers into the same frame** rather than pretending unit tests should reach it.

> ✅ **RATIFIED 2026-08-26 — Rick ruled FOLD THE :8000 TIERS IN, and extract the pure helpers**, via `/plan-decide`. Cost accepted: the full-frame number becomes a deliberate, slower measurement rather than a casual one. Recorded in TODO.md's Decisions Log.

### 3d. The real test-writing work — ~843 statements

`lupin_cli` 509 + `cosa` 334, ranked: `register_session.py` 140 · `notify_user_sync.py` 104 · `session_bridge.py` 86 · `stop.py` 36 · `hook_common.py` 34 · `notification_models.py` 33 · `notify_user_async.py` 20 · `idle_waiter.py` 14 · rest in the tail.

**Method — non-negotiable, and today proved why.** Every module: measure scoped → write → **mutate the source and watch the right test redden**. Two of my own tests today passed against a mutated source and were decorative until rewritten. A test that has never failed is a claim, not a guard.

### 3e. ⚠️ The unexplained one — do not build on it

One full-tier run reported `cosa_voice_mcp.py` at **41% (458 missing)**; the confirmation run and three scoped runs all say **99% (1 missing)**. The delta was **exactly one file** — total missing moved 1,594 → 1,137 and 458 − 1 = 457 accounts for all of it. Not the flag form, not deselection (all 335 tests collected and passed), not a shared `.coverage` (isolated), and there is no `settrace`/`setprofile`/coverage-API call anywhere in the tier.

**Mechanism unknown.** Recorded, not diagnosed. The operational rule until it is understood: **a per-file figure from a single tier run is not evidence — corroborate with a scoped run before acting on it.** It cost me an hour today chasing a 458-statement hole that did not exist.

---

## 4. Sequencing, and why this order

1. **3a stdlib fix** — cheapest, ~1 pt, unblocks a trustworthy denominator. Do it first so every later number is measured against the right frame.
2. **3b `src/lib`** — needs Rick's word; ~0.84 pts the moment he gives it.
3. **V1 excision** (§2) — before the long test-writing grind, ~~because it changes the denominator~~ ⚠️ **REASON WITHDRAWN (§6.3): it does NOT — `src/scripts` is not in the coverage frame.** Still first, for a clean tree and on Rick's ruling. **Expect a small DIP in step 4's re-measure**, from deleting v1 tests that incidentally exercised measured packages.
4. **Re-measure** the full frame. This is the new baseline.
5. **3d test-writing** — biggest-first, mutation-checked, in the ranked order.
6. **3c `lupin_app`** — extract-and-measure; the :8000 scope question may need a ruling.
7. **Raise `fail_under`** as ground is taken, so it can never silently slip back. **The ramp's teeth are this row's chase** — Tiffany wrote that, and the gate sat red for a stretch precisely because a chase date is not a control.
8. **Full pyramid** in the CLAUDE.md order; integration is the final gate.

**On "100%":** the honest arithmetic is that after 3a and 3b, ~843 statements of genuine test-writing remain, plus whatever `lupin_app`'s scope ruling adds. That is reachable. It is not reachable *today*, and I would rather say so now than report a number later that turns out to have been the frame moving rather than the work landing.

---

## 5. What I need from Rick

| # | Decision | Recommendation |
|---|---|---|
| 1 | **Approve this plan** | — |
| 2 | **`src/lib`: delete, revive, or leave?** | **DELETE** (§3b) |
| 3 | **`lupin_app`: fold the :8000 tiers into the coverage frame, or scope the mandate to what unit+cosa can reach?** | Fold them in — otherwise 367 statements are permanently unreachable-by-construction |
| 4 | **`fail_under` as ground is taken** — raise it per milestone, or hold at 96 until 100? | Raise per milestone; a floor that never moves is not a ramp |

> ✅ **ALL THREE RATIFIED 2026-08-26 by Rick via `/plan-decide`** — (2) DELETE `src/lib` · (3) FOLD the :8000 tiers into the frame + extract the pure helpers · (4) RAISE `fail_under` per milestone. Recorded in TODO.md's Decisions Log. None executed at time of writing.

---

## 6. Review response — María's four fixes, and what I re-verified

**Her verdict**: *approve with four fixes before Step 3 deletes anything.* All four ratified decisions
stand. Doc: [`2026.08.26-maria-review-v1-excision-coverage-plan.md`](2026.08.26-maria-review-v1-excision-coverage-plan.md).

⚠️ **I re-verified every load-bearing claim against the tree before amending.** A peer's review is
evidence, not a ruling — and three separate instruments manufactured false failures for me yesterday.
All three of her load-bearing claims check out:

| her claim | my check | result |
|---|---|---|
| a **second** `v1_eval_arm` import exists | `grep -n 'v1_eval_arm' src/scripts/v2_eval.py` | ✅ line **1433**, `load_v1_class_to_command` |
| it is on **every** v2 run's path | `grep -n 'load_mappable_commands'` | ✅ called at **2022**, feeding both `compute_metrics` calls |
| `COVERAGE_PROCESS_START` has no in-tree setter | the grep, excluding rnd | ✅ **zero hits** |
| `src/scripts` is **not** in the coverage frame | `pyproject.toml` `source` list | ✅ seven dirs, `src/scripts` absent |

### 6.1 🔴 The break this plan would have shipped — fix BEFORE Step 3

`v2_eval.py:1433` lazily imports `load_v1_class_to_command` from `v1_eval_arm`, which reads the live
v1 registry **out of the pinned worktree Step 4 deletes**. After excision the import raises, the
`except` at :1441 catches it, and the function returns `None`. The code's own docstring says what
`None` means: routing accuracy is then *"scored over the FULL corpus, which is NOT comparable to the
v1 arm's eligible-only denominator."*

**That is a printed warning on stdout, not a failure.** v2 keeps running and keeps reporting a routing
accuracy — against a wider denominator than every v2 number recorded before the excision. The number
moves and nothing red appears.

🔴 **THE CLASS, in Maria's sharper form.** My first draft called this "instruments that report a number
under a condition where they cannot measure." She widened it - correctly - using her own §6.4 finding as
the proof: **controls that report OK under the condition where they cannot function.** Nothing about
`--strict-markers` looks like a measurement, and it fails identically: it validates markers *applied to
tests*, so it reads as guarding while an unknown name inside a `-m` expression evaluates silently False.

**Five members this week; the last two widen it beyond measurement:**

| member | reports | under the condition |
|---|---|---|
| `aa41fa66` shared `.coverage` | 96.59%, green | ~28,000 statements erased from the denominator |
| the 41% single-file case | a per-file percentage | a mechanism still unexplained |
| today's contended run | 82% | another suite on the box, no warning |
| `load_mappable_commands` -> None | a routing accuracy | the eligible-set registry is gone |
| `--strict-markers` | "markers are strict" | the marker is in `-m`, not on a test |
| the DM condenser | a path, in the slot documented as protected | the path was fabricated from prose (see below) |

⇒ **The common repair is one thing**: each needs a distinguishable "I could not do my job" state. That
is Krishna's ninth pin - observed-succeeded / observed-failed / **not observed** - generalised off the
eval harness onto controls at large.

**A SIXTH MEMBER — and I had it wrong** (Maria; row `cf1587cd`). I first placed the peer-DM condenser
*beside* this table rather than in it: it drops attribution (its prompt asks for a headline, two
supporting statements and a path, never for who said what), but every condensed DM arrives stamped
"condensed in transit", so unlike the five it does announce itself. I argued its defect was therefore
not silence but a warning too generic to act on, and that folding it in flat would make the repair read
as "add a warning" when the warning already exists.

**María then produced the evidence that breaks that reading, and it is worse than dropping.** A DM I sent
her arrived carrying the filename `four-guards-in-dm.py`. **No such file exists in the tree** — checked, not taken on report: neither `git ls-files` nor a
tree-wide `find` matches `four-guards*`. My prose
phrase "four guards in dm.py" had been turned into a path and placed in the PATH SLOT — the field
`a0151611` protects precisely because a reader is expected to trust it. So the condenser does not only
lose content; it **manufactures a citation and files it under the guarantee**. The stamp says "something
was shortened", and a reader who takes that at face value still cannot tell a real path from an invented
one — which is exactly "reports OK under the condition where it cannot do its job", with the OK coming
from the protection promise rather than from a number.

⇒ **It belongs in the table, and it is the nastiest of the six.** The other five hand you a number you
cannot trust. This one hands you a *citation* you cannot trust, and a citation is what a reader chases
instead of re-deriving. The repair is unchanged in kind — a distinguishable "I could not do my job"
state — but here it has to be specific: **name what was dropped**, and never emit a path the source text
did not contain. Announcing that something was shortened is not that state.

**The harm, counted rather than argued**: Maria has spent **three round-trips this week** recovering a
dropped subject, and the clearest instance was the message in which she described this very defect. We
were debugging the channel and the channel dropped the subject of the message describing the bug.

**Fix, taking María's recommendation (a) with (b) as a guard on top:**
- **(a) Freeze the list.** The eligible-command set comes from a pinned sha, so it is a *constant*,
  not a lookup. Write it to a checked-in data file with the pin sha stamped on it and repoint
  `load_mappable_commands` at that. Preserves comparability across the excision.
- **(b) Make the `None` path fatal** *on top of* (a) — if the list cannot be read, v2 refuses to score
  routing rather than scoring it wider. (a) alone leaves the silent-widening path in the code; (b)
  alone takes v2's routing metric offline.

### 6.2 🟠 §3a's first action had no target — corrected in place above

Nothing in this repo sets `COVERAGE_PROCESS_START`. The `omit` **is** the fix, and the guard test is
promoted from belt-and-braces to **the whole control**.

### 6.3 🟠 The excision does NOT move the coverage denominator — my sequencing reason was wrong

`[tool.coverage.run] source` lists seven directories and **`src/scripts` is not among them**. All
2,953 lines of v1 scripts live there, so deleting them removes **zero** statements from the frame.
Deleting their ~3,646 lines of tests can only move the number **down**, where those tests incidentally
exercise measured packages.

⇒ **Excision is neutral-to-negative on coverage, never positive.** §4 sequenced it before the
test-writing grind *"because it changes the denominator"* — **that reason is wrong and is withdrawn.**
The excision is still worth doing first, for a clean tree and on Rick's ruling, but not for that.
**Expect a small dip in the Step-5 re-measure, and say so before the run rather than after.**

### 6.4 🟡 The marker guard does not guard

`--strict-markers` validates markers *applied to tests*; an unknown name inside a `-m` expression is
silently evaluated as False. María executed the check rather than reasoning it: a `-m
"not bogus_marker_xyz_maria"` collect returns 3 tests, clean, no error. **The real control is a grep of
`pytest.ini`'s addopts line at the end of Step 3(a)** — not the suite. Risk-table entry corrected.

### 6.5 🟡 Three more surfaces for the Step-1 list

- `src/tests/unit/test_v2_paired_bridge_reachability.py:434` — patches `v1_eval_arm.load_v1_class_to_command` directly.
- `src/scripts/lib/job_liveness.py:6,28` — references `paired_eval_live` in its usage docstring and `pgrep` example. An **edit**, not a delete.
- `src/conftest.py:203` + `pyproject.toml`'s comment blocks — both cite `src/lib` at 0.0% as the worked example for the shared-`.coverage` guard. Deleting `src/lib` does not break the guard, but leaves the explanation pointing at a package that no longer exists, on the one file whose job is to say why the gate is there. **Update in the same commit as the delete.**

### 6.6 What her review deliberately did not cover

The §3d ranked order (mine, and §3e's corroboration rule already applies), whether `WsJobEventListener`
relocates cleanly (Step 2 runs its tests green first — the right order), and **the runtime cost of
folding the :8000 tiers into every gate run (§3c), which is costed nowhere in this plan.** That last
one is not a blocker but it is a real gap: ratified without a number. Worth measuring before it
becomes the default.

---

## 7. Execution record — what actually happened, 2026-08-26

> ✅ **Rick's go, 2026-08-26**: *"Maria has approval discharges the gate I want you to incorporate the changes and then Implement the plan."* Executed in the ratified order, fixes first.

| # | commit | step |
|---|---|---|
| 1 | `71d5efaa` | §3a — the stdlib left the denominator |
| 2 | `d466a9b9` | §3b — `src/lib` deleted |
| 3 | `1dd574d5` | §6.1 — the routing denominator frozen and fatal |
| 4 | `22ad69b3` | §2 Step 2 — three relocations |
| 5 | `75ad1bd8` | §2 Steps 3–4 — the excision and the worktree |

### 7.1 🔴 The plan's blast radius was short by two, and both misses share a shape

§6.1 found **one** edge from v2 into the delete list. Execution found **three**:

| # | edge | why the plan missed it |
|---|---|---|
| 1 | `v2_eval.load_mappable_commands` → `v1_eval_arm.load_v1_class_to_command` | lazy, inside a function (María found it) |
| 2 | `v2_eval` → `paired_eval.make_provenance` | **module-level** — v2 would have died at *import*, not degraded |
| 3 | `eval_isolation_guard` → `v1_eval_arm.V1_PIN_SHA` | in a **keeper**, not in v2's own file at all |

⇒ **The list was assembled by asking "what is v1 apparatus". All three misses come from the opposite question — "what does v2 still take FROM it".** Two of the three were invisible to a grep of `v2_eval.py`. **A delete list built by naming files is not the same as one built by following imports**, and nothing settles it except running the import with the files gone.

That is now a test rather than a lesson: `src/tests/unit/test_v2_survives_v1_excision.py` blocks the deleted module names with a meta-path finder in a child process and imports v2 anyway — with its own **negative control**, because a blocker that blocks nothing would be green forever.

### 7.2 The §3a mechanism — both published explanations were wrong

The plan said *find what sets `COVERAGE_PROCESS_START`*. §6.2 corrected that to *nothing in the tree sets it; the source is outside the repo*, and I re-verified that grep and agreed. **Both wrong: the variable was never the vector.**

Reproduced instead of argued — `src/tests/unit/test_runner_coverage_blindness.py` spawns child pytests with `--cov=json` (its scope needs a real module) and those children **inherited the parent's `COVERAGE_FILE`**, writing stdlib data into the tier's own file. Running that one file under coverage with an isolated data file recorded **5 measured files, all 5 outside `src/`** — exactly the five in the report. After the fix: **0**.

A property test then found a **second** file with the same shape, `test_db_package_import_hygiene.py` — which was also the single red in the unit tier (row `109b8c4c`). One fix closed both.

### 7.3 The contention guard's first real catch was my own re-measure

Decision 4's guard refused Step 5 because a peer session was mid-coverage-run. That is the run this plan calls *"the new baseline"*. It waited rather than using the escape hatch.

⚠️ **And it nearly cost more than it saved on the day it landed**: five honest tests went red together because a peer was running `pytest src/cosa/tests/`. Children that produce no citable number now engage the hatch, and a test walks `src/tests` so the next such child is caught when written. **A guard that reddens honest tests for reasons unrelated to them is a guard somebody deletes.**

### 7.4 Corrections to this plan's own figures

- **The worktree was 129 MB, not ~1 GB.** Step 4's estimate was off by ~8×. Measured before removal.
- **§6.5's third bullet was done** — `src/conftest.py`'s worked example now says `src/lib` was deleted. `pyproject.toml` needed no edit; its `src/lib` mention was the source-list entry, removed with the package.
- **`src/scripts/run-lupin-gui.sh` was deleted too.** §3b listed it as `src/lib`'s only live caller but did not scope it. Leaving a documented command pointing at deleted files is worse than removing it; recovery is in `CLAUDE.md`.

---

## Appendix — provenance

Every figure here was measured today with an **isolated `COVERAGE_FILE`** on a tree whose dirty-count was recorded. Full-frame: `START_SHA=6f75b227`, tracked-dirty=0. V1 surface: `grep`/`wc` at the same sha. `/api/push` retirement read from `_retired_doors.py`, not remembered. Related rows: `e2099400` (coverage frame), `a2e360f8` (closed — the v1/v2 comparison), `7e2125a7` (v2 warm replay defect, queued, branch work).
