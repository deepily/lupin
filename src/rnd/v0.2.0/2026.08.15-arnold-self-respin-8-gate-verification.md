# Gate verification — self-re-spin WI-1 + WI-2 (rows 33f0e924, b4428470)

**Reviewer**: Arnold 🪨 (session 7889597d), row 50327998, for Cheech 🌿
**Date**: 2026-08-15
**Targets**: `src/lupin_mcp/self_respin_core.py`, `src/lupin_mcp/cosa_voice_mcp.py` (wrapper `self_respin` @2197), `src/cosa/agents/heartbeat_arbiter/self_respin_observer.py`, tests `test_self_respin_core.py` / `test_self_respin_wrapper.py`.
**Task**: verify Krishna's 8 numbered gates; apply the gate-5 correction (false, fixed in `3f8cec99`); flag any gate unverifiable as written; no merge.

## Verdict

**8/8 gates verify as CODE PROPERTIES.** Gate 5 required the correction and its exemption rides the unmerged `3f8cec99`. Three "as-written" citation defects to flag (a stale line number, a wrong field name, two process-gates that aren't code properties). No merge blocker in the code; two merge-order items are the manager's to enforce.

## Per-gate findings

**Gate 1 — observer ships before verb.** PROCESS gate, not a code property. Build order respected: WI-1 observer row (33f0e924) created 02:06, WI-2 verb row (b4428470) 02:28; observer docstring line 5 states "ships BEFORE the self_respin verb." Both are in `review` together now — the "ships before" constraint is a MERGE-ORDER item you enforce at merge, unverifiable from code alone. **VERIFIED as built-order-respected; merge-order is yours.**

**Gate 2 — memento verify defeats partial-write + stale-prior-day.** VERIFIED. `verify_memento_content` (self_respin_core.py:120-163) requires the `SELF-RESPIN-NONCE: <uuid> @ <ts>` line present (a partial write never reaches the trailing nonce), the ts within ±`cycle_window_seconds`, and not future-dated. Atomic temp→rename write (`atomic_write_json`) + read-back (`_readback_ok`), never `exists()`. Marker AND fire-token both read back (fire-token read-back added as Krishna nit 2, lines 331-341).

**Gate 3 — one-shot consumed at the FIRE point.** VERIFIED. `build_guarded_clear_argv` (169-196) puts `rm "$4"` (no `-f`) before `send-keys`, so a second detached fire finds the token gone and short-circuits. Test `test_guarded_argv_consumes_token_before_send_keys`.

**Gate 4 — wrap=False proven at call site.** VERIFIED (property) — CITATION DEFECT. Tests assert `argv[6] == "/clear"` verbatim, no `system-reminder` text in the argv, and `speakerphone_wrap` is NEVER called by the scheduling path (`test_self_respin_core.py:144-149, 417-425`). ⚠️ Krishna's gate cites "confirmed line 1051 gates rider" — line numbers have since moved and 1051 no longer points there; the citation is **unverifiable as written**, but the property is proven by the named tests + the argv builder. Fix the citation to name the test, not a line.

**Gate 5 — confirmation default-YES, offline→PROCEED, proxy can't answer no.** CORRECTED + VERIFIED. `gate_proceed` (82-105): default-used prefix OR "yes" → PROCEED; "no"/"neither"/non-string → ABORT. The original clause **"proxy can't answer no" was FALSE** — a running Responder auto-answered the confirmation "no." Fixed by `human_only=True` (self_respin_core.py:526) threaded through the whole notification pipeline in `3f8cec99` and proven by `TestHumanOnlyExemption`. **Corrected gate**: the confirmation is `human_only`, so the proxy is EXCLUDED from answering — not merely assumed absent. ⚠️ The exemption holds only once `3f8cec99` is on the merge target; it is committed, not yet merged.

**Gate 6 — single INI-fed sensor threshold.** VERIFIED. The budget fraction lives only in `lupin-app.ini:946-948` with splainer entries at `lupin-app-splainer.ini:859-861`; no second numeric threshold constant in the core or observer (both read the arbiter's `status` field derived from that one fraction). No `tick.sh` reintroduces a threshold (none found).

**Gate 7 — observer non-vacuous oracle.** VERIFIED (property) — FIELD-NAME DEFECT. `_is_confirmed_return` (159-188) requires `pre_clear_status == "over_budget"` AND current `status == "within_budget"` AND a turn fresh after `fired_at` — a real high→low transition, not a bare alive-check. ⚠️ Krishna's gate names `consumption_pct_of_window high→low`, but the oracle actually keys on the `status` field, not raw pct. Keying on `status` is BETTER (single-threshold consistency with gate 6), but the gate's wording names the wrong field. Property holds; fix the wording.

**Gate 8 — verb must not self-transition to done; observer owns done-state.** VERIFIED (negative half). No task-store calls in the verb or the observer (grep clean; core docstring line 43-44 + ensure line 285). The verb never marks its own row. NOTE: the observer also writes no `done` — it emits RETURNED / DEAD_NO_RETURN verdicts; a CONSUMER acts on done-state. So "observer OWNS done-state" is a design assignment via signal, not a write in this code — the enforceable property (verb doesn't self-done) is fully verified.

## Unverifiable-as-written (summary for Krishna's spec)

1. **Gate 4** — "line 1051 gates rider": stale line number; re-cite the tests.
2. **Gate 7** — "consumption_pct_of_window": wrong field; the oracle keys on `status`.
3. **Gates 1 & 8 (positive halves)** — "ships before" / "observer owns done" are process/merge-order + consumer-action assignments, not properties of these files; enforce at merge / in the consumer.

## Merge note

Nothing merged. Two items you own at merge: (a) land WI-1 observer no later than WI-2 verb (gate 1); (b) `3f8cec99` (the gate-5 human_only exemption) must be on the merge target or gate 5 regresses.
