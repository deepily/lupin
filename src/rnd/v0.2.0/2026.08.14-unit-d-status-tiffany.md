# Unit D — status (Tiffany 💍, re-spun under Cheech)

**Date**: 2026-08-14 · **Branch**: `wt-v2-c-executor-2026.08.14` · **Commit**: `7b53bf1b`
(merged shared branch `wip-v0.2.0-2026.08.03-present-and-demo` in first — C2 + F now present.)

## Done

| Piece | State |
|---|---|
| `flow.py` unit tests (`test_v2_flow.py`, 21 tests) | ✅ 100% lines + branches |
| `routers/v2_ask.py` — `POST /api/v2/ask` + Pydantic contract + factory | ✅ written, imports clean |
| `test_v2_ask.py` (11 tests) | ✅ 100% lines + branches on v2_ask.py |
| `main.py` registration | ✅ `include_router( v2_ask.router )` |
| INI block + splainer (5 keys) | ✅ dev/testing enabled, baseline OFF, **writeback ON** |
| Full v2 unit suite | ✅ 312 green (280 F + 32 mine), import chain clean |

## Bug found + fixed (red→green receipt)

`flow.py` `_speak` set `sender_id="v2.ask@lupin.deepily.ai"`. The digit in `v2`
violates the `AsyncNotificationRequest.sender_id` pattern `^[a-z]+(\.[a-z]+)+@…`,
so **every spoken v2 response raised `ValidationError`** → uncaught → 500, breaking
the never-500 invariant. Fixed to `ask.flow@lupin.deepily.ai`. The 14 speak-path
tests were red on this until the fix.

## Decisions honored (not re-litigated)

- Weather ArgSpec synthesized at the call site (no `AGENTIC_AGENTS` entry).
- Write-back kill-switch lives once inside `V2Cache.write_back`; flow gates only
  on `snapshotable and status=="done"`. Wired **ON** in dev/eval config (decision 4).
- Fail-loud construction guard: `writeback_enabled` + a cache missing the methods → raise.
- Replay only on tier-1 exact hit; every failure degrades to receptionist, never 500.
- AskFlow docstrings kept (Cheech's 200-line call).

## Design note for Cheech

Empty/oversized `question` → **422** (Pydantic `Field` min/max), not the 400 the
handoff named. This follows the standing Pydantic-native validation mandate
(422 = body shape, 400 = invariants). Flag if you want it forced to 400.

## Still owed

1. **Integration test on `:8000` (scheduled)** — the real stack (live cache/router/model,
   round-trip). Not yet written. This is the remaining substantial piece.
2. **Round-trip cache test (row 41333974)** — ✅ **done**. Sam confirmed the write-back
   seam matches D's calls exactly; his round-trip control (second identical request
   replays) is landed green at `92934eab`, fail-first verified. Writeback ON in config.
