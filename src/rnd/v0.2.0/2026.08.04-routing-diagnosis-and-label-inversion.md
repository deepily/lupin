# Podcast Routing — Step 1 Diagnosis + Label Inversion Fix

**Date**: 2026-08-04
**Author**: Rachel 🕊️ (lane A — routing), Mr. Radio's SWE crew
**Rows**: fc4ccfe6 (router picks the wrong podcast agent), fc8990c6 (labels inverted — FIXED here)
**Demo**: Thursday 2026-08-06, driven by voice
**Venue measured**: `:7999` dev, live `/api/push`, Rick's verbatim demo sentence

---

## Verdict

Rick's demo sentence routes to the podcast agent that **researches the topic from scratch and never opens his file**. The routing itself is only half the story: the two podcast agents' **display labels are swapped against what they actually do**, so the confirmation step a human reads on stage points at the wrong choice. I fixed the labels (row fc8990c6). The routing/classifier fix (row fc4ccfe6) is a separate change, held for review.

There is also a clean escape hatch: if Rick sets the **"Podcast Generator" mode dropdown**, the LLM router is bypassed entirely and the request goes deterministically to the correct file-reading agent — no classification, no confirm.

---

## What I measured (live, deterministic)

Rick's sentence, pushed 3 times to `/api/push` on `:7999`:

> "build me a podcast based on the contents of an explainer document that discusses the KISS protocol and how it saved me a ton of tokens on a daily basis"

| What | Result |
|---|---|
| Cache/snapshot match | Top match 39.7% ("How many miles is 10 kilometers?") → **ignored**, no cache hit |
| LLM router (fine-tuned LoRA `ministral_8b_2410`) | `agent router go to research to podcast` — **3/3 runs, identical** |
| Post-router confirm | "I think you want Doc-to-Pod (convert existing research to podcast). Is that right?" |
| Chosen agent behavior | `DeepResearchToPodcastJob(query=...)` — researches the topic; **no file argument** |
| Expediter | Agent "Research to Podcast", asks 5 questions led by *"What topic would you like me to research and turn into a podcast?"* |

The chosen agent takes `query, budget, languages, audience, audience_context`. **There is no field to point it at a document.** It would produce a plausible KISS podcast that never opened Rick's explainer — the failure that looks like success on stage.

---

## The real story: the labels are inverted

The two podcast agents do the **opposite** of what their user-facing names say. Read from each job's constructor:

| Routing command | Actually does | Input | OLD label (wrong) |
|---|---|---|---|
| `podcast generator` | **Reads an existing document**, makes a podcast from it | `research_path` = "Path to the Deep Research markdown document" | "PodMaker (create a podcast **from a topic**)" |
| `research to podcast` | **Researches a topic from scratch**, then makes a podcast | `query` = "topic to investigate" | "Doc-to-Pod (**convert existing research** to podcast)" |

The name that sounds like "reads my document" (**Doc-to-Pod**) was attached to the agent that **researches from scratch**. The name that sounds like "from a topic" (**PodMaker**) was attached to the agent that **reads a file**. Both backwards.

This is not a cosmetic bug. Within one hour it misled the crew's own coordination — the manager relayed "steer to Doc-to-Pod" to two engineers before the measurement corrected it. A label that fools the people who read this code daily will fool a presenter reading it once. **A third engineer (Clayton) independently confirmed the file-reader is `podcast generator`, via its `fuzzy_file_match` handler.**

**Scope check**: the parallel *presentation* pair (`presentation generator` = `source_path`/document, `research to presentation` = `query`/topic) is **correctly** labeled. The inversion is isolated to the podcast pair.

---

## Step 1 classification (a / b / c)

- **(a) mis-classification — the operative cause.** The router sent a document request to the query/scratch agent.
- **(b) below-floor + confirm — NOT a factor.** The snapshot-cache confirm never fired (39.7%, below its 90% floor and ignored). A *separate* agentic-routing confirm did fire, but it isn't a confidence gate — every agentic command gets it.
- **(c) confirm-step wording — a real, compounding factor.** Because the labels are inverted, even a careful user who picks "Doc-to-Pod (convert existing research)" lands on the scratch agent. The confirm step, which exists to catch a routing miss, actively steers into it.

So the fix is two independent pieces: **correct the labels** (done — row fc8990c6) and **correct the classifier / training** (row fc4ccfe6, held for review).

---

## The label fix (row fc8990c6 — applied)

A swap + a sharpened parenthetical, in `todo_fifo_queue.py`:

| Command | NEW label |
|---|---|
| `podcast generator` (file reader) | **Doc-to-Pod (create a podcast from an existing document)** |
| `research to podcast` (scratch) | **PodMaker (research a topic and create a podcast)** |

Mode-dropdown descriptions (same file, same inversion) corrected to match.

**Consumer enumeration at HEAD** (before touching a shared string):
- Training data (`.jsonl`/`.txt`): **zero** references to the label text — it keys on the *command* strings. So the label swap and the classifier fix are **independent** changes.
- Every test that reads `PRODUCT_NAMES` does so **dynamically** (`PRODUCT_NAMES[cmd]`); the confirm reverse-lookup (line 1085) is dynamic too. **Zero assertions break.**
- Remaining hits are docs/history/rnd — record only.

**Guard test** (`test_podcast_labels_match_agent_behavior`): anchors each label to its constructor behavior. **Control-proven** — re-inverting one label turns it red with the predicted text, restoring turns it green.

---

## Test baseline — with a caveat that matters

Both-roots baseline BEFORE my edit (`pytest src/tests/unit src/cosa/tests/unit`): **21556 passed, 3 failed, 25 skipped, 5 xfailed** (10m42s). The 3 reds:

| Failed test | Status |
|---|---|
| `test_gate_reachability_census` | Genuinely pre-existing |
| `test_tfe_config::test_from_config_loads_all_twelve_keys` | Genuinely pre-existing |
| `test_notify_cc_listener_fallback` (signature intact) | **Intermittent, unexplained, NOT attributed** — appeared in my full-run baseline (10:21), does NOT reproduce (Rio ran it 7/7 green in isolation with the peer edit present) and vanished from my own post-fix full run. Rio investigating; leading hypothesis is test-order module-state pollution (the test's own docstring names `sys.modules['lupin_app.main']` pollution; suspect commit `6a2ed364`, an unrestored `importlib.reload`). Not counted as my baseline either way. |

**Evidence it is pollution, not a diff**: the failing assertion is structural — `inspect.getsource(submit_notification_response)` returned a stray fragment (`print(f"[NOTIFY] Cleaned up pending_responses…")`) instead of the real function body, which only happens when an earlier full-suite test pollutes module state.

**My zero-new claim**: my change touches only `todo_fifo_queue.py` + `test_agentic_disambiguation.py` and cannot move a notifications signature test, so that red is orthogonal to my diff. My post-fix full run showed only the two then-pre-existing reds (`gate_reachability_census`, `tfe_config`), both since **independently closed by Krishna → tree now clean**. I state zero-new precisely as observed; my last full run predates Krishna's close, so I do not claim a clean-tree receipt I did not run.

---

## The deterministic escape hatch (mode-set)

Setting the **Podcast Generator** dropdown posts `mode="podcast"`, which maps via `AGENTIC_MODE_MAP["podcast"]` → `agent router go to podcast generator` and **bypasses the LLM router entirely** — no classification, no confidence floor, no confirm. That is the safe, deterministic path into the file-reading agent for Thursday. (Measured by Tiffany; pg- job fired directly.)

The utterance shortlist (below) only matters on the **pure-voice / no-mode** path, where the LLM router runs.

---

## The natural-language shortlist (Rick's deliverable — DELIVERED)

Rick ruled he will **not** set the dropdown — he speaks a canned line and the router must land on `podcast generator` on its own. So the shortlist is the demo, measured on the no-mode path exactly as he drives it.

### The demo line

> **"make me a podcast on KISS"** — routes 3/3 to `podcast generator` AND resolves 3/3 to Rick's 2026-07-25 KISS file. 25 chars, a verbatim training template.

### The rule (contradiction-tested — a CONJUNCTION of two conditions)

Two looser rules were each **refuted by a targeted probe**:
- "topic good, document bad" — refuted: "podcast **about** my KISS explainer" says "my explainer" yet routes to the file-reader 3/3.
- "the preposition is the whole lever (about wins)" — refuted: "podcast **about my document**" → `research to podcast` 3/3 (about, but no named topic).

The surviving rule needs **both** conditions:

| Utterance | "about/on" frame | Named topic (KISS) | Route |
|---|---|---|---|
| about **the KISS protocol** | ✓ | ✓ | `podcast generator` |
| **from** the KISS protocol | ✗ | ✓ | `research to podcast` |
| about **my document** | ✓ | ✗ | `research to podcast` |
| about **my KISS explainer** | ✓ | ✓ | `podcast generator` |

**`podcast generator` requires BOTH: an "about / on" frame AND a named topic (KISS) as the object.** Miss either — wrong preposition (`from` / `based on the contents of` / `turn … into`) OR no topic (bare "my document") — and it falls to the scratch agent. This also explains Rick's original sentence ("based on the contents of… document that discusses the KISS protocol"): it names KISS but fails the preposition, so it routes to scratch.

**Rehearsal rule for Rick:** *"Say 'a podcast ABOUT KISS' or 'ON KISS' — you must NAME KISS right after about/on. Don't say 'from', and don't say 'my document' without naming KISS."*

### Measured survivors — routes 3/3 AND resolves 3/3 (no wrong-file grabs)

| Utterance | Chars | Routes → podcast generator | Resolves → KISS file |
|---|---|---|---|
| make me a podcast on KISS | 25 | 3/3 | 3/3 |
| podcast about my KISS explainer | 31 | 3/3 | 3/3 |
| make a podcast about the KISS protocol | 38 | 3/3 | 3/3 |
| build me a podcast on the KISS protocol | 39 | 3/3 | 3/3 |
| create a podcast about how KISS saved me tokens | 47 | 3/3 | 3/3 |

Routing measured by me on `:7999`; resolution measured by Tiffany's read-only `fuzzy_file_match` probe against Rick's real directory (10 files, incl. 2 presentation docs). Across all 5 × 3 runs, **zero grabbed a wrong file.**

### Two-turn mechanism (budget it in the demo script)

Resolution does **not** read the routing line. The expediter asks a follow-up — "which document?" — and the matcher reads **Rick's answer**, not the utterance. So the live demo is **two voice turns**: (1) the routing line, (2) an answer containing "KISS" (any KISS-containing answer resolves 3/3). This is why the short line's thin description is a non-issue, and why "resolves to nothing" was never the risk — the risk was a silent wrong-file grab, which did not occur.

---

## Status

| Item | State |
|---|---|
| Step 1 diagnosis | ✅ complete, receipts above |
| Label fix (fc8990c6) | ✅ applied, guard control-proven, post-fix regression zero-new (in review with Rio) |
| Shortlist (Rick's ask) | ✅ DELIVERED — demo line **"make me a podcast on KISS"**, routes 3/3 + resolves 3/3; conjunction rule contradiction-tested |
| Classifier fix (fc4ccfe6) | ⏸ likely unnecessary for the demo — the shortlist carries it; hold for review as a durability improvement |
