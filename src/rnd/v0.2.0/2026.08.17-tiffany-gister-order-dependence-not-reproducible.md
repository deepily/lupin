# Bug `0cfc9b7e` — the gister order dependence is GONE, and I could not reproduce it even at its own sha

**Owner**: Tiffany 💍 (`901ad81b`, Reviewer) · **Manager**: Mr. Radio 🦉 (`e251aa88`) · 2026-08-17
**Row**: `0cfc9b7e` — `test_gister.py::TestGister::test_init_cache_enabled_constructs_cache_table`
reported green alone, red in the full cosa run at `d1dbbecd`.

---

## Verdict

| | |
|---|---|
| **Does the cosa tier fail today?** | **NO.** Green twice, one of those via `run-cosa-tests.sh` in the real working tree. |
| **Did I find the polluting test?** | **NO — and I am not going to claim one.** |
| **Does it reproduce at `d1dbbecd`, the reporting sha?** | **NO.** Clean checkout, full tier, 9116 passed / 0 failed. |
| **Is the green vacuous?** | **NO.** The assertion still fails when the behaviour it guards comes back — mutation-proven below. |
| **Recommendation** | Close on the receipts below, and reopen on sight if it returns. The close rests on measurement, not on absence of evidence. |

## What I measured

| run | sha | invocation | result |
|---|---|---|---|
| full cosa tier | HEAD | `pytest src/cosa/tests/ -q -p no:randomly` | **8686 passed**, 26 skipped, 0 failed (320s) |
| full cosa tier | HEAD | `./src/tests/run-cosa-tests.sh -q` — the runner's own path, in the real tree | **8689 passed**, 26 skipped, 0 failed (319s) |
| full cosa tier | `d1dbbecd` | clean worktree, `pytest src/cosa/tests/` | **9116 passed**, 26 skipped, 0 failed (301s) |
| victim alone | HEAD | single test | 1 passed |
| its directory | HEAD | `src/cosa/tests/unit/memory` | 549 passed |

The row recorded 809 tests in that directory; it is 549 now. The LanceDB deletion lane removed
tests between then and now, so the tree the row describes no longer exists.

## Bisect: nothing upstream pollutes it

Every collection root that pytest reaches BEFORE `unit/memory` was run paired with the victim.
`comparison`, `smoke`, `tools` sort before `unit`; inside `unit`, `agents`, `config`, `core`,
`crud_for_dataframes` and `infrastructure` sort before `memory`.

| paired with | victim |
|---|---|
| `tests/comparison`, `tests/tools` | passes |
| `unit/agents` (4203 tests) | passes |
| `unit/config`, `unit/core`, `unit/crud_for_dataframes`, `unit/infrastructure` | passes |
| `unit/agents/test_token_counter.py`, `.../deep_research/test_cli.py`, `.../swe_team/test_job.py` | passes |

`pytest-randomly` and `pytest-random-order` are **not installed**, so collection order is
deterministic — there is no seed to blame and no seed to reproduce.

## The mechanism the assertion guarded has been deleted

The test asserts the constructor does NOT consult `"solution snapshots lancedb path"`. That config
key existed at `d1dbbecd` and is **gone from `src/conf/lupin-app.ini` at HEAD** — removed by
`8b67ea1e`, "delete the flag seam and the last five LanceDB config keys". Whatever made a cache-layer
module reach for that key during a long run, the key is no longer there to reach for.

## The green is NOT vacuous — I tried to prove it was and failed

My first hypothesis was that with the key deleted the `assertNotIn` had become a check that cannot
fail. **Measured, and it is wrong.** Adding one line back to `src/cosa/memory/gister.py`:

```python
_regress = self.config_mgr.get( "solution snapshots lancedb path", default=None )
```

turns the suite red immediately — `1 failed, 12 passed` — and removing it returns 13 passed. The
assertion still has teeth. (Mutation reverted; `git diff` on that file is empty.)

## What I am NOT claiming

I did not find a polluting test, so I cannot say what fixed this, only that the symptom is gone
under the runner's own invocation and that the config seam it depended on was deleted in between.
The row asked for the leak to be fixed at its source; there is no leak I can demonstrate today.
If it returns, this doc is the baseline: three full-tier runs, the bisect table, and a mutation
proving the assertion still fires.

## Found while bisecting — a separate defect, filed as `905e9345`

`test_job.py::TestExecuteDryRun::test_dry_run_full_path_logs_decisions` is the **mirror image** of
this row: it passes in the full tier and **fails when you open the file and run it** (`0 != 7` —
the mocked repository is never called). It passes paired with almost any sibling in its directory
and fails alone, so it is green in the merge gate and red for the next human who touches it.
Filed unassigned with the full pair table.

## Receipts

- `pytest src/cosa/tests/ -q -p no:randomly` at HEAD → 8686 passed, 0 failed.
- `./src/tests/run-cosa-tests.sh -q` at HEAD → 8689 passed, 0 failed.
- Clean worktree at `d1dbbecd` → 9116 passed, 0 failed.
- Mutation: re-add the lancedb read → `1 failed, 12 passed`; revert → `13 passed`.
- `grep -c "solution snapshots lancedb path" src/conf/lupin-app.ini` → 0 at HEAD, 1 at `d1dbbecd`.
