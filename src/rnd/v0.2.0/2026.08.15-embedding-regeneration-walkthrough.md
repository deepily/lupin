# Embedding regeneration — the beginning-to-end walkthrough

**Date**: 2026-08-15
**Author**: Mr. Radio 🦉 (session `5b7da733`)
**Rows**: `5e848dd8` (the work, blocked on Rick) · `aa0fca0a` (Rick: *"tomorrow morning. First thing."* — that was **today** 09:00)
**Purpose**: Rick's standing constraint is *"no writes to `input_and_output` until you and I discuss the entire process from beginning to end."* This is that walkthrough, written so the discussion is one pass rather than a discovery session.

---

## 1. What is being fixed

Embeddings moved from OpenAI `text-embedding-3-small` to the local nomic models. Both emit 768 dims, so nothing ever failed loudly — the vectors stayed shape-compatible while becoming **scale-** and, more importantly, **space-**incompatible. Cheech measured cosine **0.0079** between the two producers for the *same text*: essentially orthogonal.

⇒ Normalizing the old vectors in place would have produced correct-*looking* numbers that were still meaningless. **Rick's call to regenerate everything from the logged source text is the real fix**, and it is what the script does.

**Nothing is on fire.** Cosine scoring divides by both norms, so live comparisons are already correct. This is consistency work — it is P2 for a reason.

---

## 2. The numbers, re-measured today (read-only `plan`, nothing written)

| spec | table | to regen | missing vector | no text |
|---|---|---|---|---|
| `io_input` | `input_and_output` | 351,283 | 0 | 0 |
| `io_output` | `input_and_output` | 351,283 | 0 | 0 |
| `decisions` | `prediction_decisions` | 905 | 0 | 0 |
| | **TOTAL** | **703,471 embedding calls** | 0 | 0 |

**Every row can be rebuilt from text it already stores — zero rows would be lost.**

**Re-measured 2026-08-17** (read-only `plan` against the live database). The growth curve is the reason this doc keeps needing new numbers:

| Date | Rows in `input_and_output` | Total embedding calls |
|---|---|---|
| 2026-08-02 | 287,207 | 574,414 |
| 2026-08-15 | 326,740 | 654,318 |
| **2026-08-17** | **351,283** | **703,471** |

**+24,543 rows in two days**, ~+12k/day and accelerating. The table is actively written to, which is why the design does not assume a frozen snapshot — and why the run's own duration adds rows that the run will not have covered.

---

## 3. The four steps, in order

```
plan   →   fill (shadow column)   →   verify   →   swap
read-only  writes ONLY the new       read-only    the ONLY
           *_regen columns                        destructive step
```

1. **`plan`** — counts what would be regenerated. Read-only. Run today; table above.
2. **`fill`** — regenerates from each row's own logged text into a **shadow column** (`input_embedding_regen`, `output_final_embedding_regen`, `question_embedding_regen`). The live columns are untouched. Batched, resumable from a checkpoint, `--limit` bounded, `--apply` gated.
3. **`verify`** — read-only: row counts match, every regenerated vector carries a norm inside the **current local model's measured band (~17–24)**, no dimension mismatches. Produces the swap/no-swap verdict.

   > **Corrected 2026-08-17.** This line previously said verify checks for "norm ~1.000". That is backwards and would have made a good run look broken. A norm near 1.0 is the **retired OpenAI** model's fingerprint — an L2-normalized vector. The local nomic models emit **un-normalized** vectors in the 17–24 band, which is exactly what `classify_norm()` accepts and what freshly-generated vectors measure at. Caught when the first real embeddings came back with components like `-3.81`, impossible in a unit vector.

4. **`swap`** — runs `UPDATE <live_column> = <shadow_column>`. The live vectors are **overwritten in place**. **This is the only step that cannot be walked back by re-running.**

   > **Corrected 2026-08-17.** This line previously said swap "promotes the shadow column, then the old column is dropped." The code drops nothing — the shadow column **survives the swap by default**. That inverts the rollback question: *keeping* the shadows is the do-nothing outcome, and discarding them is the step that takes an explicit `DROP COLUMN`. Rick ruled on 2026-08-17 for **no retained shadows and no dump** — so the drop is a deliberate follow-up action, not something the swap does for you.

**Missing step — the shadow columns do not exist.** `fill` references `input_embedding_regen`, `output_final_embedding_regen`, and `question_embedding_regen`, and **nothing in the script creates them**. The first `UPDATE` fails with `column ... does not exist`. Before any fill:

```sql
ALTER TABLE input_and_output     ADD COLUMN input_embedding_regen        vector(768);
ALTER TABLE input_and_output     ADD COLUMN output_final_embedding_regen vector(768);
ALTER TABLE prediction_decisions ADD COLUMN question_embedding_regen     vector(768);
```

A nullable `ADD COLUMN` with no default is metadata-only in Postgres — instant, no table rewrite.

A half-finished or bad `fill` never leaves the table worse off. That is the whole point of shadow-then-swap.

---

## 4. What refuses to start, and why

`should_proceed()` gates every write pass:

| Condition | Result |
|---|---|
| Server busy (jobs in flight) | **REFUSE** — no embedding load on top of someone's run |
| Busy state **unknown** (probe unreachable) | **REFUSE** — the point of the check is not to guess |
| Any hour of the day | **Proceeds** — the clock no longer refuses anyone |
| `--force` | Bypasses the **clock only**, never the busy check |

**The clock is permissive as of 2026-08-17.** `OFF_PEAK_END_HOUR` went 9 → 11 → **24**, on Rick's word: *"You can update the clock to run for as long as we need it to. I am the human at the console. Nothing else will run or interfere. Let's not be too official on this — update the clock/gate so it's permissive and runs at my discretion."*

That costs nothing, because the clock was never the real gate. The busy check still refuses a busy box **and** still refuses when it cannot get an answer — a clock cannot tell you the box is free, only the box can. What the window actually protected was Rick's own interactive hours; when Rick is the one asking for the run, there is nobody left for it to protect. The boundary is still a parameter (`is_off_peak( hour, end_hour=N )`), so a tighter window can be reimposed without editing the module.

**The GPU defect is fixed, not merely recorded** (commit `0310aa05`): a fixed 256-row batch CUDA-OOM'd the shared GPU on the long tail. Batching is now by **character budget** with an adaptive floor/ceiling, a refused batch is **split in half and retried down to a single row**, and a row that fails *alone* is a real reject. 122 unit tests green today; the module was red-proofed by removing the budget bound (6 tests go red, 98 stay green).

---

## 5. What is measured, and what is NOT

> **MEASURED 2026-08-17.** The unmeasured number below is no longer unmeasured. Rick authorized a bounded fill; it ran against the live tables into shadow columns.

| | |
|---|---|
| Rows filled | **2,905** (1,000 `io_input` + 1,000 `io_output` + 905 `decisions`) |
| Wall clock | **9 seconds** |
| Rejects | **0** |
| **Embed + write, combined** | **~3.1 ms/row** |
| `verify` on the filled rows | `bad_norm=0` `bad_dim=0` |

**Extrapolated full run: ~25–36 minutes** for 703,471 calls. The write side roughly doubles the embedding floor rather than dominating it — which is the opposite of what the original text feared, and worth stating plainly since the fear was the reason this number mattered.

The 9 seconds includes process startup (config manager, imports, model-server handshake), so the per-row figure is conservative — a long run amortizes that away. Treat 25 minutes as the optimistic end and 36 as the safe one.

**Historical note, kept because the reasoning was right**: the original text said the write side was unmeasured and refused to assume it away, because measuring it honestly required a write against the real table — exactly what the standing constraint forbade. That was the correct call. The measurement happened the moment the constraint was lifted, and it took nine seconds.

---

## 6. What I need from Rick — two answers, nothing else

1. **Rollback posture.** Keep the shadow columns after the swap · `pg_dump` both tables first · both.
   **My recommendation: keep the shadows, skip the dump** — every row retains its source text, so recovery is *re-derivation* rather than restoration. The honest counter, and it is a real one: **re-derivable is not restorable.** If a bad swap is discovered a week later, the shadow column is the only thing that makes the old state recoverable without regenerating again. That argument is his to weigh, which is why this is his call and not mine.
2. **Go / no-go on the fill**, given the walkthrough above. A `--limit 1000` bounded fill into shadow columns would also measure the write side for real, and is the cheapest way to turn the one unmeasured number into a measured one.

Three prior rulings do not add up to this walkthrough, and I am not treating them as one.

---

## 6a. The offline run — pinned code and exact steps

Rick, 2026-08-17: *"we would want to work from a static plan and a static code set for the actual end-to-end embeddings run."* So the version is named here rather than left as "whatever is on disk":

| | |
|---|---|
| **Pinned commit** | **`cc87e404`** — the state of the tree after the bounded drive, its fixes, and this document's corrections |
| Script | `src/cosa/rest/db/embedding_regeneration.py` |
| Tests at that sha | 133 green, 100% lines + branches |

Confirm before starting: `git rev-parse --short HEAD` reads `cc87e404`. If it does not, you are not running the version this plan describes.

**The run, in order.** Every command needs `PYTHONPATH=src` and, when run from the host rather than inside a container, `LUPIN_MODEL_SERVER_URL=http://localhost:7998` — the compose hostname `lupin-model-server` does not resolve on the host, and without the override every batch fails.

```bash
export PYTHONPATH=src
export LUPIN_MODEL_SERVER_URL=http://localhost:7998   # host-side runs only

python -m cosa.rest.db.embedding_regeneration plan                    # read-only; confirm the counts
python -m cosa.rest.db.embedding_regeneration ensure-columns          # dry-run: prints the DDL
python -m cosa.rest.db.embedding_regeneration ensure-columns --apply  # already applied 2026-08-17; safe to repeat
python -m cosa.rest.db.embedding_regeneration fill --apply            # the long one, ~25-36 min
python -m cosa.rest.db.embedding_regeneration verify                  # must read OK, not BLOCKED
python -m cosa.rest.db.embedding_regeneration swap --apply            # destructive; only on a clean verify
```

**Two things that will stop the run, and what they mean:**

- `ABORTING: the embedding service is not reachable` — the model server is down. Fix it and re-run; completed batches are checkpointed and will not be redone. (Before commit `35c86885` this failure instead split the batch down to single rows and retried each one, burning the whole run — see row `13b35b37`.)
- `verify: BLOCKED ... filled=N` where N is short of `in_scope` — partial coverage. **Do not swap.** Re-run `fill` until `verify` reads OK.

**After the swap, per Rick's 2026-08-17 rollback ruling** (no retained shadows, no dump), drop the three shadow columns explicitly — the swap does not do it:

```sql
ALTER TABLE input_and_output     DROP COLUMN input_embedding_regen;
ALTER TABLE input_and_output     DROP COLUMN output_final_embedding_regen;
ALTER TABLE prediction_decisions DROP COLUMN question_embedding_regen;
```

**One property of this table that the run cannot fix**: it gains ~12,000 rows/day, and `in_scope` moved 351,283 → 351,365 in the fifteen minutes between two commands on 2026-08-17. Rows written *during* the run are not covered by it. They are written by the current model, so they are already in the right space — the residue is rows written between `fill` and `swap`, which a second bounded `fill` before the swap will sweep up.

---

## 7. Status

**Updated 2026-08-17 after the bounded drive.**

- Script: `src/cosa/rest/db/embedding_regeneration.py` — **133 tests green**, 100% lines + branches, pinned at `cc87e404`
- **Live vector columns written: still zero.** The 2,905 rows written went to shadow columns only
- §6 questions: **both answered** — no retained shadows / no dump, and bounded fill first
- Shadow columns: **created** (`ensure-columns`, applied under Rick's explicit yes)
- Write-side timing: **measured** — see §5
- Remaining gate: **`swap`**, and only `swap`

**Landed today**

| Commit | What |
|---|---|
| `35c86885` | A dead embedding server no longer gets split-retried into hundreds of doomed single-row attempts (row `13b35b37`) |
| `5affbc74` | `ensure-columns` — the fill can create the columns it writes into (the DDL that was never written) |
| `cc87e404` | The two paired-eval env keys declared as intended compose divergence (row `a1c56760`) |

## Related

- Row `5e848dd8` — the full build history, including Cheech's era-split measurement and the OOM find
- Row `aa0fca0a` — Rick's 2026-08-14 delay to "first thing tomorrow"
