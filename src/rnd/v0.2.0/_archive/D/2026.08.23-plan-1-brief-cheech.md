# Plan 1 lane brief — Cheech's crew

**From**: María 🌸 · **Date**: 2026-08-23 · **Parent plan**: `src/rnd/v0.2.0/2026.08.23-plan-1-crash-lane-recovery.md`
**State verified**: 2026-08-23, this session, shas below are live — not copied from the plan's morning table.

---

## SPAWN TEXT (paste this into Cheech's brief)

> You are recovering your own crew's lanes from the 2026-08-22 double OOM. Five of your sessions were
> killed mid-flight. Read `src/rnd/v0.2.0/_archive/D/2026.08.23-plan-1-brief-cheech.md` — it is your lane-by-lane
> brief with live shas — then the parent plan for the incident context.
> **Your half is small: three of four lanes need no session at all**, and the only loose file on the box
> in your lanes is one modified file in the vacuity worktree. **Phase 0 (freeze the evidence) comes
> before you resurrect anything.** Two rules govern the whole recovery: bring lanes back **one at a
> time**, and **do not `--resume` any crash-day transcript** — the second OOM landed ~60 seconds after
> yesterday's restore. Seed from the memento or the store row. Set your session topic at boot.

---

## Your roster — 5 sessions

| Session | Row | Where it stopped | Worktree |
|---|---|---|---|
| `6b291856` | **manager (you)** | handing three flaky tests to Tiffany | main repo |
| `462ca379` | `004c94ec` | **merged at `9c654a08`**, then blocked asking you a clarifying question | `.claude/worktrees/tiffany-learnback` |
| `9e0b977d` | `b0570b67` | "Now the regression sweep on affected suites" | `lupin-wt-wake-check-b0570b67` |
| `7d9dc88c` | `ece4d86a` | **negative result** — the four flaky tests did not reproduce | `lupin-wt-flaky-unit-ece4d86a` |
| `31e29a44` | `e3f59977` | "Proving the other arm — that it *runs* rather than skips when the test DB is live" | `lupin-wt-vacuity-e3f59977` |

## Worktree state — re-measured today against `wip-v0.2.0-2026.08.03-present-and-demo`

| Worktree | HEAD | modified | untracked | ahead | Verdict |
|---|---|---|---|---|---|
| `lupin-wt-vacuity-e3f59977` | `3c0491ea` | **1** | 0 | 1 | ⚠️ one dirty file — the only loose work in your lanes |
| `lupin-wt-wake-check-b0570b67` | `3678a1b1` | 0 | 0 | 1 | commits safe |
| `lupin-wt-flaky-unit-ece4d86a` | `73775205` | 0 | 0 | 0 | fully merged |
| `.claude/worktrees/tiffany-learnback` | `4b30a646` | 0 | 0 | 0 | fully merged at `9c654a08` |

## Phase 0 — freeze before you touch anything

1. Copy the one modified file in `lupin-wt-vacuity-e3f59977` out to
   `io/recovery-2026.08.23/loose/vacuity-e3f59977/` **before** any checkout or rebase touches it.
2. Bundle the two non-merged branches:
   `git bundle create io/recovery-2026.08.23/<branch>.bundle <branch> --not wip-v0.2.0-2026.08.03-present-and-demo`
3. Record `git -C <wt> rev-parse HEAD` for all four worktrees into a manifest.

**Gate**: the manifest exists and the bundles restore in a scratch clone.

## Phase 1 — three lanes need no session at all

- **`ece4d86a` (flaky unit tests) — close with the NEGATIVE result recorded. Do not re-run the
  investigation.** The four named tests do not reproduce. The 11 real failures were a gitignored
  config file absent from a fresh worktree — **that is a Plan 3 input, not a defect in the tests.**
- **`004c94ec` (learn-back) — ✅ ALREADY CLOSED with full receipts at 12:23 EDT**, 15 minutes before
  the second OOM. My brief was one step behind on this row; corrected 13:06 on Cheech's report.
  **Do not re-open it.** The clarifying question `462ca379` died waiting on was answered by events:
  the two tests were `test_spoken_char_cap_helper::test_returns_default_on_constructor_exception`
  and the v2 cache lancedb closure, and Pocholo measured both as not reproducing. Recorded in
  `ece4d86a`'s closure. **No seat needs resurrecting for it.**
- **`b0570b67` (wake check) — 1 commit, clean tree.** It needs only the regression sweep it was
  starting when it died. That is a small, bounded job, not a resurrection.

That leaves **`e3f59977` (vacuity sweep)** as your one genuine lane: read the loose modified file from
Phase 0, decide commit-or-discard **from the file contents**, and finish the arm it was proving —
that the test *runs* rather than skips when the test DB is live.

## Phase 3 — resurrect, one lane at a time

1. **One session at a time.** Confirm each is inside its own `ccworker-*.slice`
   (`systemd-cgls --user-unit ccworker.slice`) and let it work a full turn before starting the next.
2. **Watch `memory.current` on the scope**, not the host. Past `MemoryHigh` (18 GiB) it throttles and reclaims.
3. **No `--resume` of any crash-day transcript.** Seed from the memento or the store row.
4. **🔴 CAP EVERY PARALLEL TEST RUNNER AND WRAP IT IN A `timeout`** — `pytest -n`, `node --test`, `tsx --test`, `vitest`, `jest` all fan out to CPU-count workers, and this box has 32 cores. An uncapped `tsx --test` is what killed Mr. Radio's session at 13:02:50 (52 processes, systemd-oomd, user-level pressure 74.67% > 50%). Use `node --test-concurrency=4` / `pytest -n 4`. Original wording named only pytest, which is why it did not prevent this.

## Exit criteria for your half

- Both non-merged branches landed or explicitly dropped with a reason
- The one modified file resolved, with a receipt
- The three Phase 1 rows closed, each carrying its actual result (including the negative one)
- Every row on your board terminal or genuinely blocked with a chase

⛔ **Plan 2 (worktree reclamation) stays blocked until Mr. Radio's half is empty too** — no worktree
dies holding unlanded work.
