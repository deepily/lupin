# 1a ledger (owned-agentic ⊆ router prompt) — for Rachel 🕊️

**Reporter**: Tiberius 👑 (d7fd5dde) · **Worktree HEAD**: df151d7c · **Measured**: 2026-08-15

`test_1a` requires every owned agentic command to be router-prompt-listed UNLESS it
waives "prompt". Current run: **2 failed / 15 passed**. The two reds:

## 1a red — the ONE remaining, awaiting your ruling
| Command | Owned | Prompt | Trained | On card (PRODUCT_NAMES) | Disposition |
|---|---|---|---|---|---|
| `agent router go to test suite` | ✅ | ❌ not listed | ✅ | ✅ | **PENDING your ruling** |

Options for test suite (I do not self-decide):
- **A — prompt exemption** (like BFE): it is user-reachable via the card, not an
  initial router detection. Then `surfaces=["prompt"]`, stays on-card (no card waiver).
  Greens 1a. Reason would cite the card call site + that it's API/card-invoked, not
  emitted. Need: confirm test suite is never a legitimate INITIAL detection.
- **B — completion**: add it to the router prompt template so the router can emit it.
  Only if you want voice users to trigger a test-suite run by initial detection.

My read: A fits the evidence (test suite is in PRODUCT_NAMES, card-selectable-and-
completable, API-dispatched with no fuzzy-human-input initial path — same shape as
BFE). But this is your call.

## 1b red — DIFFERENT facility, also awaiting your ruling
| Command | In factory | Owned (AGENTIC_AGENTS) | Disposition |
|---|---|---|---|
| `agent router go to heartbeat poker` | ✅ | ❌ | **PENDING** — remove the factory branch OR a factory-side exemption (NOT a prompt/training/card exemption; this is factory==owned drift, 1b). |

## Already resolved this run (for the full picture)
| Command | Waives | Landed |
|---|---|---|
| `…test fix expediter` (plain START) | prompt (+ card, pending my facility commit) | 50e71366 / this run |
| `…bug fix expediter` (BFE) | prompt, training | df151d7c |

## Row e5a840c9
Still **queued**, P2, owner tiberius, valid — "drop START training key from
`agent-router-agentic-commands.json` at next retrain." Hygiene, not urgent. No action
needed from you; recorded so it isn't lost.
