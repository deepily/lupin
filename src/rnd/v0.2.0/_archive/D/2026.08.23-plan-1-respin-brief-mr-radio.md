# Re-spin brief — Mr. Radio 🦉's successor

**From**: María 🌸 · **Written**: 2026-08-23 15:35 · **Supersedes nothing** — this is an ADDENDUM to
`src/rnd/v0.2.0/_archive/D/2026.08.23-plan-1-brief-mr-radio.md`, which stays the lane reference.

---

## SPAWN TEXT (paste into the successor's brief)

> You are taking over Mr. Radio 🦉's lanes in the 2026-08-22 double-OOM recovery (Plan 1). The seat
> before you was killed at 13:02:50 today, mid-work. **Nothing was lost** and **you do not start over.**
>
> Read, in order: `src/rnd/v0.2.0/_archive/D/2026.08.23-plan-1-respin-brief-mr-radio.md` (this file — what
> happened and where to pick up), then `…-plan-1-brief-mr-radio.md` (your four lanes), then
> `io/recovery-2026.08.23/MANIFEST-mr-radio.md` (the freeze your predecessor banked).
>
> **You start at PHASE 2, not Phase 0.** The evidence freeze is complete and verified.
>
> 🔴 **The rule that killed your predecessor: CAP EVERY PARALLEL TEST RUNNER AND WRAP IT IN A
> `timeout`.** He ran `./node_modules/.bin/tsx --test` with no timeout on a 32-core box; Node's test
> runner fans out one worker per core and each tsx worker carries an esbuild service. 52 processes,
> and `systemd-oomd` killed the whole scope. Use `node --test-concurrency=4` and always
> `timeout 600 …`. Set your session topic at boot.

---

## What happened to the seat before you

```
13:02:50 systemd-oomd[1106]: Killed …/ccworker-cc_tmux_session_5ed54994.slice/run-r8d9a4fce….scope
  due to memory pressure for /user.slice/user-1001.slice/user@1001.service
  being 74.67% > 50.00% for > 20s with reclaim activity
13:02:50 systemd[1251]: run-r8d9a4fce….scope: systemd-oomd killed 52 process(es)
```

**Three things to carry, not re-derive:**

1. **It was `systemd-oomd`, not the kernel and not the 24 GiB `MemoryMax`.** oomd watches memory
   pressure on the whole `user@1001.service` and kills the worst cgroup under it — so **a seat well
   under its own ceiling still dies, and the seat killed is not necessarily the one that caused the
   pressure.** Do not read "containment already shipped" as "I am covered."
2. **The trigger was one uncapped command**, and it was the red pass on a rescued file:
   `./node_modules/.bin/tsx --test src/tests/unit/shared/arg_interview.test.ts`. An earlier attempt in
   the same lane carried `timeout 180`; the re-run dropped it. **Do not repeat that command as written.**
3. **The containment did work.** Only that one scope died — María, Cheech, Cheech's reviewer and
   Clayton all survived, where yesterday the entire tmux unit was torn down together.

## What your predecessor finished — do NOT redo it

**Phase 0 is COMPLETE and its gate passed.** Manifest: `io/recovery-2026.08.23/MANIFEST-mr-radio.md`.

- All four HEADs recorded and matching the brief — no drift.
- **Bundles verified**: they fetch cleanly into a throwaway clone, tips and ahead-counts matching.
  On disk at `io/recovery-2026.08.23/bundles/` — `wt-qa-card-impl-8407333d`, `wt-qa-card-tester`,
  `wt-qa-card-tester-e328974d`, `wip-receptionist-marker-1568269d` (+ two of Cheech's).
- **All loose files copied out** to `io/recovery-2026.08.23/loose/`, including a `modified.diff`
  per lane.
- **He refined the count and he was right**: the brief said 2 modified + 10 untracked; 3 of those
  were build artifacts (`.venv` ×2, `node_modules`). **Real exposure: 2 modified + 7 untracked
  source files.** The brief has been corrected.

He was **partway into Phase 2 on `8407333d`** — he had read the diff and both new source files, and
was running their test when the kill landed. He committed nothing, so all four HEADs are unchanged.

## Your four lanes (unchanged since the freeze — re-verify anyway, cheaply)

| Worktree | Branch | HEAD | ahead | loose |
|---|---|---|---|---|
| `lupin-wt-qa-card-8407333d` | `wt-qa-card-impl-8407333d` | `a82bb155` | 4 | 2 modified + 2 source |
| `.claude/worktrees/qa-card-tester` | `wt-qa-card-tester-e328974d` | `39e0dc52` | 12 | 5 scratch scripts — **not work** |
| `lupin-wt-qa-card-tester` | `wt-qa-card-tester` | `ec595058` | 13 | clean |
| `lupin-wt-sam-1568269d` | `wip-receptionist-marker-1568269d` | `da526578` | 2 | clean |

**Order — and note that #1 is not merely "low risk," it is what makes #2 cheap** (Cheech's finding,
independently measured 2026-08-23 15:34):

1. **`lupin-wt-qa-card-tester` (13 commits) — LAND THIS FIRST.** Docs-only, 787 lines of
   `src/rnd/*.md`, **zero code, zero risk**. It is **Clayton's reviewer lane for the OTHER tree**, and
   it holds **ten review documents covering phases 1–4, two explicit PASS verdicts, plus findings the
   implementer then fixed.** Landing it first means you inherit the complete review record instead of
   re-reviewing 2,549 lines cold.
2. **`.claude/worktrees/qa-card-tester` (12 commits)** — Tiberius's implementer lane. Because of #1,
   **the job here is not a fresh review**: it is *"did the later commits close the findings in
   Clayton's docs?"*, with those docs as the checklist.
   ⚠️ Its **6 untracked files are SCRATCH, not work** — five shell scripts plus a `.venv` symlink,
   each carrying a self-written *"Scratch, not committed"* header. **Nothing there is at risk of
   loss.** Read them once before the tree is reclaimed: the method behind the gate lives in them
   (measure coverage on the touched files, since the repo floor is `fail_under=96`; compare failures
   **by name, not by count**, per Mr. Radio's 08-22 ruling).
3. **`lupin-wt-sam-1568269d` (2 commits)** — clean tree, needs landing, nothing at risk.
4. **`lupin-wt-qa-card-8407333d`** — the highest-exposure lane and the one that killed the last seat.
   Take it last, with the runner capped.

## Two questions already settled — do not re-investigate

- **The two tester worktrees are two different seats — an implementer and his reviewer working the
  same feature concurrently.** Settled twice, independently (María 12:48, Cheech 15:34): 0 shared
  commit shas, 0 shared patch-ids, 0 shared files touched, common merge-base `9b49f04d`, both forked
  from mainline independently.
  · **A** = `lupin-wt-qa-card-tester` (`ec595058`) → **Clayton** `b36934bf`, the **REVIEWER** — 787
  lines, `src/rnd/*.md` only, zero code.
  · **B** = `.claude/worktrees/qa-card-tester` (`39e0dc52`) → **Tiberius** `e328974d`, the
  **IMPLEMENTER** — 2,549 lines of production code and tests.
  The clincher is not a statistic: a review doc **inside A names B by path in its own header**
  (*"Under review: the phase-3 merge in worktree .claude/worktrees/qa-card-tester"*), and the logs
  show live dialogue — A files *"gate 3's negative control never reads the registry"* at 12:06:43,
  B commits *"Fix my own unfailable negative control — it never read the registry"* at 12:08:47.
  **Two minutes apart.** Both must land; neither supersedes the other.
- **`8407333d` is NOT done.** Its original transcript ends at "Mandatory red pass:" and that pass has
  still never completed — the attempt to run it is what killed the seat. That lane owes a real red
  pass, capped and timed out.

## Standing rules for the rest of the recovery

1. **One lane at a time.** Confirm you are in your own `ccworker-*.slice`
   (`systemd-cgls --user-unit ccworker.slice`) and let each lane finish before starting the next.
2. **No `--resume` of any crash-day transcript.** Seed from this brief and the store row.
3. **Every test run: capped fan-out AND a `timeout`.** Both halves. Which one bites depends on the
   runner — Python lanes here have no `pytest-xdist` installed, so there the hole is the missing
   timeout; Node lanes fan out to 32 by default.
4. **Watch `memory.current` on your own scope**, and remember it is not the binding signal — oomd
   fires on whole-session pressure.

## Exit criteria (unchanged)

- All four worktrees' commits landed on `wip-v0.2.0-2026.08.03-present-and-demo` or dropped with a reason
- The 2 modified + 7 untracked source files resolved, each with a receipt
- Every row on the board terminal or genuinely blocked with a chase
- No further OOM event

⛔ Plan 2 (worktree reclamation) stays blocked until this list is empty. **Cheech's half is already
complete** — his four lanes are done, so these four are the last of the eight.
