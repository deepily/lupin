# Plan 1 lane brief — Mr. Radio 🦉's crew

**From**: María 🌸 · **Date**: 2026-08-23 · **Parent plan**: `src/rnd/v0.2.0/2026.08.23-plan-1-crash-lane-recovery.md`
**State verified**: 2026-08-23, this session, shas below are live — not copied from the plan's morning table.

---

## SPAWN TEXT (paste this into Mr. Radio's brief)

> You are recovering your own crew's lanes from the 2026-08-22 double OOM. Seven of your sessions
> were killed mid-flight. Read `src/rnd/v0.2.0/_archive/D/2026.08.23-plan-1-brief-mr-radio.md` — it is your
> lane-by-lane brief with live shas — then the parent plan for the incident context.
> **Phase 0 (freeze the evidence) comes before you resurrect anything.** Two rules govern the whole
> recovery: bring lanes back **one at a time**, and **do not `--resume` any crash-day transcript** —
> the second OOM landed ~60 seconds after yesterday's restore. Seed from the memento or the store row.
> Set your session topic at boot. You hold the seats; María wrote the plan and does not execute it.

---

## Your roster — 7 sessions

| Session | Role | Where it stopped | Worktree |
|---|---|---|---|
| `dc0137ba` | **manager (you)** | mid-self-re-spin at the context limit — "🫡 Clearing now" | `lupin/src` |
| `8407333d` | Implementer, seat 2 | "19 green at 100/100/100. **Mandatory red pass:**" — blocked on `TaskOutput` | `lupin-wt-qa-card-8407333d` |
| `b36934bf` | Adversarial reviewer | phase 3 cleared; owed the `7451bebe` write-up + verified-red on `e3733953`/`ff13c527` | main repo |
| `e328974d` | Tester, seat 4 | escalated its two riskiest merge calls for adversarial review | `.claude/worktrees/qa-card-tester` |
| `8f780a6e` | Author | "Three regressions, not two — and one is new. Investigating" | main repo |
| `8fdf98ef` | Implementer | claimed row `1568269d`, starting on Clayton's write-up | `lupin-wt-sam-1568269d` |
| `c4098ad9` | Implementer | barely booted — "I'll boot up first" | main repo |

## Worktree state — re-measured today against `wip-v0.2.0-2026.08.03-present-and-demo`

| Worktree | HEAD | modified | untracked | ahead | Verdict |
|---|---|---|---|---|---|
| `lupin-wt-qa-card-8407333d` | `a82bb155` | **2** | **4** | 4 | ⚠️ highest loss exposure |
| `.claude/worktrees/qa-card-tester` | `39e0dc52` | 0 | **6** | 12 | ⚠️ untracked at risk |
| `lupin-wt-qa-card-tester` | `ec595058` | 0 | 0 | 13 | commits safe, needs landing |
| `lupin-wt-sam-1568269d` | `da526578` | 0 | 0 | 2 | commits safe |

**Total loss exposure in your lanes: 2 modified + 7 untracked SOURCE files, in two worktrees.**
Everything else is already in a commit.

> **Corrected 2026-08-23 12:59 by Mr. Radio, verified independently.** My original count said 10
> untracked because `git status --porcelain` collapses an untracked *directory* into one `??` entry —
> 3 of those entries were build artifacts (`.venv` and `node_modules` in `8407333d`, `.venv` in the
> tester worktree), not work. The 7 real ones: `arg-interview.js` + `arg_interview.test.ts` in
> `8407333d`, and five test-harness shell scripts (`.baseline-run.sh`, `.capture-names.sh`,
> `.cov-touched.sh`, `.gate-run.sh`, `.run-scoped.sh`) in `e328974d`. The 2 modified are
> `notifications.html` and `notifications.js`.

## ✅ The parent plan's open question is SETTLED — do not re-investigate it

The two tester worktrees are **two genuinely different seats, not one lane duplicated.** Measured:
neither HEAD is an ancestor of the other, merge-base `9b49f04d`.

- `lupin-wt-qa-card-tester` (branch `wt-qa-card-tester`, 13 commits) is the **reviewer** lane —
  every commit is a review verdict, no product code.
- `.claude/worktrees/qa-card-tester` (branch `wt-qa-card-tester-e328974d`, 12 commits) is the
  **tester/implementer** lane — the actual gates and code (`a0322a77`, `fc5cd050`, `dd72936d`,
  `e3733953`, `ff13c527`, plus three merges).

**Both must land. Neither supersedes the other.**

## Phase 0 — freeze before you touch anything

1. Copy the 2 modified + 7 untracked source files out to `io/recovery-2026.08.23/loose/<worktree>/`
   **before** any checkout, rebase, or worktree operation.
2. Bundle every non-merged branch:
   `git bundle create io/recovery-2026.08.23/<branch>.bundle <branch> --not wip-v0.2.0-2026.08.03-present-and-demo`
3. Record `git -C <wt> rev-parse HEAD` for all four worktrees into a manifest.

**Gate**: the manifest exists and the bundles restore in a scratch clone. No lane resurrects until that passes.

## Phase 2 — the two dirty lanes (this is where the only unrecoverable work is)

Read the loose files copied in Phase 0 and decide commit-or-discard **from the file contents**, not
from the transcript's description of them.

🔴 **`8407333d` is NOT done and must not be landed as if it were.** Its transcript ends at
"Mandatory red pass:" — the red pass was never run. An assertion is not a guard until you delete
what it guards and watch it go red. That lane owes a real red pass before anything of its work counts
as verified.

Also still owed by `b36934bf`: the `7451bebe` write-up, and verified-red on `e3733953` and `ff13c527`.

## Phase 3 — resurrect, one lane at a time

1. **One session at a time.** Confirm each is inside its own `ccworker-*.slice`
   (`systemd-cgls --user-unit ccworker.slice`) and let it work a full turn before starting the next.
2. **Watch `memory.current` on the scope**, not the host. Past `MemoryHigh` (18 GiB) it throttles and
   reclaims — the early warning the old shared slice could not give.
3. **No `--resume` of any crash-day transcript.** Seed from the memento or the store row.
4. **🔴 CAP EVERY PARALLEL TEST RUNNER AND WRAP IT IN A `timeout`** — `pytest -n`, `node --test`, `tsx --test`, `vitest`, `jest` all fan out to CPU-count workers, and this box has 32 cores. An uncapped `tsx --test` is what killed Mr. Radio's session at 13:02:50 (52 processes, systemd-oomd, user-level pressure 74.67% > 50%). Use `node --test-concurrency=4` / `pytest -n 4`. Original wording named only pytest, which is why it did not prevent this.

## Exit criteria for your half

- All four worktrees' commits landed on the integration branch or explicitly dropped with a reason
- The 2 modified + 7 untracked source files resolved, each with a receipt
- Every row on your board terminal or genuinely blocked with a chase
- No third OOM event during the recovery

⛔ **Plan 2 (worktree reclamation) stays blocked until this list is empty** — no worktree dies holding
unlanded work.
