<!-- memento-record: persona=cheech session_id=d7335dac written_at=2026-08-24T22:54:00-04:00 slot=rnd -->
# Cheech — memento #2, session d7335dac, written at the 51.0% ceiling

**Read THIS file. It supersedes `io/mementos/cheech-d7335dac.md` (21:25).**
Do not resolve by persona name: the resolver ranks a HEADERED old file above a
headerless new one **unconditionally** (tier is the first element of the sort
tuple, so freshness is never compared across tiers). Measured tonight.

---

## 🔴 THE ONE THING THAT HAPPENS NEXT

**P0 `f7b57d88` — the `:8000` gate, ~09:30 EDT 2026-08-25.** Blocked on Rick, chase set.
Board is otherwise EMPTY: everything else closed with receipts tonight.

**Two questions are Rick's at 09:30, both put to him and both unanswered:**
1. **The four-door typescript ban.** Condition 1 is RETIRED (source FIXED not fenced —
   276→0). Condition 2 is the suite measurement. ⚠️ I told Rick the 2 GB cap was
   "10× the working set." **THAT WAS NEVER MEASURED** — 196/205 MB is one file run two
   ways. Pocholo measured 8 files at concurrency 4 → **1016 MB, a 2× margin.**
2. **`6577fb2b`'s deferral has lost its premise BOTH ways.** Rick deferred it as a quiet
   P3 explicitly NOT a gate blocker; that file is now failing the final merge gate.
   Parked with his own words in `park_reason`. **His to re-rank, not mine.**

**MY ONE OWED ACTION**: bounce `:8000` once Clayton's `ts-827a54cd` clears, BEFORE 09:30,
then re-run `gate-status.py` and confirm `todo_queue_size` is a NUMBER not None.
A **bounce**, not `--force-recreate` — nothing about mounts or env changed.
⚠️ Until then the venue check verdicts off `run_queue_size` alone, so the QUEUED-in-todo
case — the exact lane this row was filed for — still reads idle.

---

## CREW (4 after the shed) — every one corrected me tonight

| Persona | State |
|---|---|
| **Rachel** 🕊️ | ADVERSARIAL VERIFIER — a standing function, not a lane. Keep her on the gate. |
| **Tiffany** 💍 | `e6b8fe56` closed; built `venue_idle.py`, merged by me at `84643a19`. |
| **pocholo** 📣 | Mid-measurement: the file-count curve that decides the ban. |
| **Krishna** 🦚 | SHED per Rick's `db881492` — gate prep finished with receipts. |

**Mr Radio's** Tiberius gave me **10 findings and 4 corrections**. Not my seat; thank him.

---

## 🔴 THE FINDING OF THE NIGHT — say it as ONE sentence, not twelve

**EVERY INSTRUMENT WE CHECKED ANSWERED A NARROWER QUESTION THAN IT APPEARED TO.**
`monopolize_id` answers identity, not idleness · the jstest watchdog measured the one
process that never grows · a pragma cited coverage living elsewhere · **my own "read the
queues directly" fix is user-filtered** · `/api/busy` was blind to the todo queue · a
worktree guard reports at line 222 of a 279-line log · the gate reporter folds xfails
into skips · **60 test functions across 10 modules can skip silently, 5 module-wide** ·
the wake-check reports what sits at the slot, not what was read.

⇒ **Tiberius's improvement, which is the durable form: AN ALARM SHOULD REPORT WHAT IT
OBSERVED, NOT WHAT IT CONCLUDED.** Each of these was harmless as an observation and
damaging as a verdict.

---

## WHAT I GOT WRONG (four, all retracted to the worker's face)

1. Told the fleet "read the queues directly" was the honest idle check. **It is user-filtered.**
2. Briefed Rio to build a ratchet **that already existed and was green**.
3. Told Rick the memento defect was hygiene after reading the code — **john RAN it and got
   a file 7 days stale.** A measurement beat my reading of the source.
4. Told Pocholo his memory curve was poisoned by 6.5 GB of stray hog. **Measured: the box
   has 207 GB available and the hogs are idle.** Retracted; his 1016 MB stands.

⇒ **The rule that actually worked all night: PROVE IT FROM CONTENT.** Pocholo proved he
read the right memento by quoting a measurement I had never sent him.

---

## OPEN, NOT MINE, DO NOT CHASE

- Survivors are **ORPHANS, not deep descendants** — reparented to `systemd --user`, so no
  tree walk reaches them at any depth. **Fix is `setsid` + kill the process GROUP.**
  Pocholo has it.
- `e062580e` P3→P2 (guard blocks its own documentation; 7 of 22 denials were talking-about).
- Memento class documented at `ada1663f`; pre-write guard exists under row `a18bfec9`
  and reports at the wrong moment — **someone else's row, I stopped two seats on it.**
- `stash@{0}` sha `e26966bd`, 12 files, preserved as a branch ref. **Owner unidentified** —
  "On wip-security-tiffany" names the BRANCH, not the seat. My inference, wrong.

## RICK'S STANDING ORDERS
Moratorium on NEW task-store rows — findings go in the receipt of a row being closed.
Shed one worker (done: Krishna). **Push is his word alone.**

SELF-RESPIN-NONCE: 70e0854a-9185-4bfb-a6d1-94d010ff0e36 @ 2026-08-24T22:55:54.764141-04:00

SELF-RESPIN-NONCE: f8598fb2-7c27-4875-bfe7-71c58fcdf036 @ 2026-08-24T23:04:56.153593-04:00

---

<!-- memento-amendment: by=cheech session_id=d7335dac amended_at=2026-08-24T23:18:00-04:00 -->
# AMENDMENT — 23:18, Pocholo harvested, question 1 now has its number

**Merged at `92e5dc17`** — five commits, docs only. Findings:
`src/rnd/v0.2.0/2026.08.24-typescript-suite-memory-measured.md`.

**Rick's question 1 no longer lacks a measurement — but he ruled on the WRONG one.**
He ruled on **2.4x** (bare runner). **The door runs c8, and c8's margin is 1.77x**
(864 → 1156 MB against a 2048 MB ceiling; +292 MB, one sample). Adequate either way —
this is a provenance correction, not a reversal. **Say it in this order at 09:30:**
the 10x I gave him earlier was 196/205 MB, one file run two ways, and it carried my name.

**Two ways this table gets misread — say both before anyone acts on it:**
1. Every measured run is **GREEN**. The allocator in `32c58572` needs a **FAILING**
   assertion holding a DOM node. **A fix cycle is red by definition** — the state no
   number here covers.
2. Memory is flat 16→117 files but climbs **~145 MB per concurrency slot**. The plateau
   licenses the FILE COUNT and licenses nothing about raising `--test-concurrency`;
   at 8 the same green suite sits near 1.6 GB.
3. 🔴 **A per-file timeout under ~452 s KILLS A PASSING FILE and reports the tier red** —
   `audio_transport.test.ts` passes in 452 s and owns essentially the whole suite clock
   (29 s + 452 s ≈ 481 s reconciles the documented 499 s). `queue_transport` is
   **UNMEASURED**, known only to exceed 344 s. Do not assume it matches.

**Pocholo HELD, not re-spun** — ~25% used, memento current, live for 09:30. I want the
seat that took the measurement in the room when it is ruled on.

**Still my one owed action, unchanged**: bounce `:8000` after `ts-827a54cd` clears, then
re-run `gate-status.py` and confirm `todo_queue_size` is a NUMBER not None. Bounce, NOT
`--force-recreate` — nothing about mounts or env changed.
