<!-- memento-record: persona=cheech session_id=d7335dac written_at=2026-08-24T23:40:00-04:00 slot=rnd -->
# Cheech — memento #3, session d7335dac, written at ~16% after the end-of-session push

**Read THIS file.** It supersedes memento #2 (`…-memento-2.md`, 22:54) and the io-slot
file (21:25). The resolver ranks a HEADERED old file above a headerless new one
unconditionally, so do not resolve by persona name — open this path.

---

## 🔴 THE ONE THING THAT HAPPENS NEXT

**P0 `f7b57d88` — the `:8000` gate, ~09:30 EDT 2026-08-25.** Blocked on Rick, chase set.
It is the ONLY row on my board.

**MY ONE OWED ACTION**: bounce `:8000` once Clayton's `ts-827a54cd` clears, BEFORE 09:30,
then re-run `gate-status.py` and confirm `todo_queue_size` is a NUMBER, not None.
A **bounce**, not `--force-recreate` — nothing about mounts or env changed.

⚠️ Verify idle with `PYTHONPATH=src python3 -m cosa.rest.venue_idle --port 8000` and read
the **EXIT CODE** (0 IDLE / 1 BUSY / 2 UNKNOWN). **UNKNOWN IS NOT IDLE.**
⚠️ Median morning boot is 09:24; eleven of twelve are after 08:52. **Check the box is up.**

## RICK'S TWO QUESTIONS AT 09:30

1. **The four-door TypeScript ban.** Condition 1 (source fixed, 276→0) retired. Condition 2
   is now MEASURED — **and he ruled on the wrong number**: he ruled **2.4×**, which is the
   BARE runner. **The door runs c8 and its margin is 1.77×** (864 → 1156 MB against a
   2048 MB ceiling; +292 MB, one sample). Adequate either way — **provenance, not a reversal**.
   The earlier **10×** was 196/205 MB, one file run two ways, and it carried my name.
   🔴 **Say these as TWO sentences, never one**: the memory condition IS satisfied — and that
   does NOT lift the ban, because every number is from a **GREEN** run and the allocator needs
   a **FAILING** assertion holding a DOM node. A fix cycle is red by definition.
   🔴 **The plateau licenses the FILE COUNT, not the knob** — +145 MB per concurrency slot;
   at 8 the same green suite sits near 1.6 GB.
   🔴 **A per-file timeout under ~452 s kills a PASSING file and reports the tier red** —
   `audio_transport.test.ts` passes in 452 s and owns essentially the whole suite clock.
   `queue_transport` is **UNMEASURED**, known only to exceed 344 s.
2. **`6577fb2b`'s deferral has lost its premise both ways.** His to re-rank, not mine.

---

## DONE TONIGHT, AFTER THE RE-SPIN

**Rick's end-of-session order executed**: **pushed** (`7f85de23`, 0/0 with origin — it was
137 behind when I rehydrated) and **backup run** (`backup.sh --write`, 527 files / 7.41 GB,
SYNC COMPLETE). Backup destination verified as `/mnt/DATA02/…/lupin/` — **the skill doc's
sample output shows a planning-is-prompting path; the script itself is correct.** Do not
panic at that doc.

**Harvested Pocholo across five merges** — every one verified by me before merging, not taken.

## 🔴 WHAT I GOT WRONG, AND IT IS THE SAME SHAPE AS LAST NIGHT'S

**I signed a bad receipt.** I merged, and endorsed twice, "the fatal run burned 388 s of CPU
under a 300 s cap, so the cap never fired." **Invalid** — those are CPU seconds on a 32-core
box where V8's GC and helper threads run in parallel; **CPU seconds cannot bound wall
seconds.** I wrote *"verified myself at lines 43 and 50"* — and what I verified was that the
**numbers sat on those lines**, never that the **inference** was valid.

⇒ **READING A FIGURE IS NOT CHECKING A CLAIM.** A line number proves the text exists. It
proves nothing about the reasoning built on it.

**Pocholo caught it, in his own document.** His fix commit credits the catch to me; it was not
mine to take, and I said so in the merge rather than let the record stand. Struck in the R&D
doc (`c00e264c`) and in row `92e94cb7`'s body (event 9235) — **both were my words, so both
were mine to strike.**

⇒ His durable line, which I am keeping: **a wrong receipt attached to a right conclusion is
worse than no receipt — it invites the conclusion to be thrown out with it.**

## THE OTHER RETRACTION — AND I BRIEFED A CREW ON THE BAD PREMISE

I assigned Pocholo the "surviving orphans / defect five" fix. **He retracted the finding
twenty minutes later and he was right.** I verified rather than take it:
`src/tests/unit/test_jstest_lane.py:481` keys the marker to `os.getpid()`, so the sixteen
orphans (an earlier run's PID) **were never in that test's field of view.** The green was
honest; **the PID scoping is a FEATURE** — a check matching every marker on the box would
false-accuse on a peer's residue, which is the serial bridge guard's problem one directory over.

**What it actually was, and he proved it both ways**: the hog is `persist=True` on purpose,
nothing reaped it, so **every correct FAILURE leaked ~2.1 GB**. Three runs held 6.3 GB for an
hour. Broken kill + fixture → box CLEAN; broken kill without → the original strays reproduce.
**A test that leaks 2 GB every time it works as designed punishes the suite for biting.**

**I stood down 2 of the 3 parts I had briefed** (`setsid`, group kill — undemonstrated defect).
**Kept the aliveness negative**, which stands alone: "no survivors" is satisfied by killing
everything **including the killer**. Proven FORWARD with a discriminating mutation.
⚠️ **The REVERSE is UNPROVEN and merged as such, stated not buried** — three attempts, two
malformed edits that broke collection, so they tested his sed rather than the guard.

⇒ **A brief is a hypothesis. Mine was wrong within the hour, and the worker disproved it.**

---

## CREW — ONE worker, not the four memento #2 claimed

| Persona | State |
|---|---|
| **pocholo** 📣 | 20.9%, ACTIVE, no re-spin owed. Told to wrap, write his memento, and NOT self-respin until the push landed. It has landed. |

🔴 **`list_spawned_sessions` showed 1, my memento claimed 4.** Rachel, Tiffany and Krishna are
gone from my lineage. **Read the roster, never the memento's crew table.**

**Peer managers** (not mine): maria 46.1% — declined my headroom offer, rows all Rick's,
self-clearing. mr radio 24.7%.

## STANDING — UNCHANGED
- **TypeScript tier still BANNED at all four doors** until Rick rules. Never `test_types: ["all"]`.
- **No stashing** (repo-global stack; hook denies the mutating verbs).
- **No unscoped `pkill`** — kill by explicit PID, deepest-first.
- **No new task-store rows** (Rick's moratorium) — findings ride the receipt of a row being closed.
- **Push is Rick's word alone.** He gave it at 23:30 tonight; that word was for tonight.
- María's `src/rnd/v0.2.0/2026.08.24-epic-accordion-mini-plan.md` is untracked in this tree.
  **NOT MINE — do not commit it.**
