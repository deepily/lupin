# E2E Tier Triage — 8 failures + 6 errors (2026-08-03)

**Triager:** Rachel 🕊️ · **For:** Mr Radio 🦉 · **Branch:** `wip-v0.2.0-2026.08.03-present-and-demo`
**Run:** E2E UI tier, junit `e2e-junit-20260803-161518.xml` + `e2e-ui-latest.log` (test container `lupin-rest-test`, `/var/lupin/io/test-suite/artifacts/`).
**Tier totals:** 668 passed, **8 failed, 6 errors**, 2 skipped. (Run totals: 13,286 passed / 35 failed / 8 errors.)

---

## Headline

**None of the 14 is a code regression introduced by this branch, and none breaks the Thursday demo's product path.** All 14 are `src.tests.e2e_ui.*` browser tests. This branch's only rendered-UI change is commit `e1878647`, which **bumped cache-bust `?v=` tokens only** — no pixel or behavior change (and Playwright uses a fresh browser context each run, so the token is moot). The 9 branch commits touched no HTML/JS/CSS *behavior* and no visual component. So the visual-snapshot drift and the click-actionability timeouts were **not introduced here** — they are stale/pending baselines or environmental/timing, pre-existing to the branch cut.

**One item to watch for Thursday: F7 (prediction-vote-down)** — touches the expeditor prediction-hint UI (Lane 1). It is an actionability timeout, not a missing control (the correct button renders), but it's the only failure adjacent to the demo path.

**Caveat I will not overstate:** "pre-existing vs environmental" is settled structurally (branch changed no rendered UI). A confirming run on `main` would nail which, but the branch-diff evidence already rules out "this branch's code broke it." Pixel-truth on the visual diffs is Rick's visual signoff — not automatable.

---

## FAILURES (8) — test body reached its assertion / action

| # | Test | Shape | Verdict | Evidence | Thursday path? |
|---|------|-------|---------|----------|----------------|
| F1 | `test_cc_session_strip_and_focus::test_focused_card_messages_grow_to_80vh_in_focus_mode` | Layout assertion: expected 60vh baseline, got 250px (`182 <= 2` failed) | **Pre-existing / environmental** | Branch changed no multiplexer CSS behavior (only `?v=` tokens in e1878647). Height mismatch is viewport/baseline, not branch code. | No |
| F2 | `test_dm_recent_activity::test_dm_badge_visual_baseline` | `ValueError: unknown file extension:` | **Environmental / test-infra bug** | Not a product failure — the snapshot path has no file extension; harness/tooling defect in the visual-baseline helper. | No |
| F3 | `test_filter_toggle::test_toolbar_show_scrolls_section_into_view` | `TimeoutError` click `notifications-toolbar-debug`; locator **resolved** to the button | **Pre-existing / environmental (actionability)** | Element present and correct; click never satisfied Playwright actionability (covered/animating/stable). No branch JS change to the toolbar. | No |
| F4 | `test_multiplexer_phase6a_visual::test_multiplexer_phase6a_jobs_pane_visual` | Visual snapshot: 2095 mismatched px (960×403) | **Pre-existing baseline drift** (not this branch) | Branch made no visual change; 668 E2E incl. many visual tests passed → targeted stale baseline, not global env. Pixel-truth = Rick's signoff. | No (visual only) |
| F5 | `test_multiplexer_phase6b_visual::test_multiplexer_phase6b_action_required_visual` | Visual snapshot: 2507 mismatched px (960×542) | **Pre-existing baseline drift** (not this branch) | Same as F4. | No (visual only) |
| F6 | `test_multiplexer_task_list::test_mux_body_overlay_dismisses_on_backdrop_click` | `wait_for_selector(attached)` OK, then `.click(pos 5,5)` timed out on `#task-body-overlay` | **Pre-existing / environmental (actionability)** | Overlay attaches; backdrop click at (5,5) fails actionability. Branch didn't change overlay JS (e1878647 = token bump only). | No (task-list UI, not delivery) |
| F7 | `test_prediction_hint_vote_e2e::test_thumbs_down_records_rejected_no_console_error` | `TimeoutError` click `prediction-vote-down`; locator **resolved** to the exact `👎🏼` button | **Pre-existing / environmental (actionability)** | Correct button renders (`data-testid="prediction-vote-down"`); click never actionable. No branch change to prediction-hint JS. | **⚠️ YES — expeditor prediction-hint UI (Lane 1)** |
| F8 | `test_task_list_card::test_body_overlay_dismisses_on_backdrop_click` | Same as F6 — `#task-body-overlay` backdrop click timeout | **Pre-existing / environmental (actionability)** | Duplicate shape of F6; one underlying overlay-backdrop actionability issue hit by two tests. | No |

---

## ERRORS (6) — never reached / passed the assertion; **all raised on TEARDOWN**

Distinct from failures: the test body did not fail on its own assertion — the **teardown** raised a visual-snapshot mismatch. Same underlying cause across all six (visual baseline drift), surfacing as an error because it fires in fixture teardown, not the test body.

| # | Test | Shape | Verdict | Evidence | Thursday path? |
|---|------|-------|---------|----------|----------------|
| E1 | `test_fleet_status_panel::test_populated_panel_visual_baseline` | teardown: `fleet-status-populated.png` mismatch | **Pre-existing baseline drift** | Branch changed no fleet-status UI. Teardown-time visual assert. | No (visual only) |
| E2 | `test_multiplexer_phase5_visual::test_multiplexer_phase5_notifications_pane_visual` | teardown: `multiplexer_phase5_notifications_pane.png` mismatch | **Pre-existing baseline drift** | Same. Notification *pane* is visual-only, not delivery logic. | No (visual only) |
| E3 | `test_multiplexer_phase6c_section_a_visual::test_..._section_a_chip_visual` | teardown: `multiplexer_phase6c_section_a_chip.png` mismatch | **Pre-existing baseline drift** | Same. | No (visual only) |
| E4 | `test_multiplexer_phase6c_section_d_visual::test_..._section_d_baseline_visual` | teardown: `..._section_d_baseline.png` mismatch | **Pre-existing baseline drift** | Same. | No (visual only) |
| E5 | `test_multiplexer_phase6c_section_d_visual::test_..._section_d_pinned_visual` | teardown: `..._section_d_pinned.png` mismatch | **Pre-existing baseline drift** | Same. | No (visual only) |
| E6 | `test_multiplexer_phase6c_section_d_visual::test_..._section_d_pin_moved_visual` | teardown: `..._section_d_pin_moved.png` mismatch | **Pre-existing baseline drift** | Same. | No (visual only) |

---

## Cross-cutting patterns

1. **Visual-baseline drift (F4, F5, E1–E6 = 8 of 14).** All snapshot mismatches on multiplexer/fleet-status/notification-pane chrome. The branch changed no rendered UI, so these are stale/pending baselines from UI work predating the cut — **re-baseline candidates**, not regressions. The pixel judgment is Rick's visual signoff.
2. **Overlay/click actionability (F3, F6, F7, F8 = 4 of 14).** Locator resolves to the correct element; `.click()` never passes Playwright actionability (visible/stable/receives-events). Pattern of "element present but not clickable" — pre-existing/environmental (timing, or an element covering the target). F6 and F8 are the same `#task-body-overlay` backdrop issue hit twice.
3. **Test-infra bug (F2 = 1).** `ValueError: unknown file extension` in the visual-baseline helper — fix the harness, not the product.
4. **Layout assertion (F1 = 1).** 80vh focus-height baseline; viewport/baseline, not branch code.

## Recommended next actions (not taken — triage only)

- **Confirm pre-existing** by running the same E2E tier against `main` (v0.1.9) — expected: the same 14 fail. Cheap and dispositive.
- **Re-baseline** the 8 visual snapshots if the current UI is the intended look (Rick's visual signoff) — `--update-snapshots`.
- **Fix F2** harness extension bug in the DM-badge visual helper.
- **Investigate the overlay-backdrop actionability** (F6/F8 shared) and **F7 prediction-vote** — the only Thursday-adjacent one — for a covering element / timing race.

---

## Provenance

- junit: `/var/lupin/io/test-suite/artifacts/e2e-junit-20260803-161518.xml` (test container).
- log: `/var/lupin/io/test-suite/artifacts/e2e-ui-latest.log` → `e2e-20260803-165518.log`.
- Branch UI change audit: `git log main..HEAD -- src/lupin_app/static` → only `e1878647` (cache-bust token bump). 9 commits total on branch; none touch UI behavior or visual components.
