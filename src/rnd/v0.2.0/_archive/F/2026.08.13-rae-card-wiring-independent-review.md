# Independent Review — RAE facilitator wired into the podcast card (row 7c46fdde)

**Reviewer**: Rachel 🕊️ (session 62476c3e), for Mr. Radio — independent of the builder (Krishna, commit 84d06f29).
**Date**: 2026-08-13
**Question posed**: (Q1) does the live endpoint actually reach the RAE question flow, not a mocked stand-in? (Q2) does the flag default mean the path is reachable in production at all?

## Verdict

**Wired-but-dark.** The RAE card wiring is real code, but it has **never executed live** and is **switched OFF in production**. The 72 green unit tests gate a path production cannot run: every test mocks the expeditor and forces the flag on synthetically. The row can be closed as *wired-but-dark*, **not** as *verified* — and it cannot be verified in production until Rick turns the flag on there.

## Two keys — do not conflate them

| Config key | Where set | Value | Governs | Live evidence |
|---|---|---|---|---|
| `runtime argument expeditor enabled` | `[Lupin: Development]` only | **True** | the **pg- GENERATOR** path: `/api/push` → `push_job` → `_handle_agentic_command` → RAE | **exercised** — today's orphan run `pg-94631198` drove it to done (a NEIGHBOUR of this row) |
| `podcast card uses runtime argument expeditor` | `[Lupin: Baseline]` (line 1512) | **False** | **THIS row's CARD** path: `/api/podcast-generator/submit` → RAE | **never exercised live** |

The card key sits only in Baseline; no section overrides it, so **Development, Production and Testing all inherit False**. It was flipped off deliberately (splainer note, 2026-08-03, "SET FALSE … the new expeditor route is unit-proven but has NEVER run live — expedite() is mocked in every test — so True would put its first live execution on stage").

## Q1 — the live endpoint vs the mock

**The wiring is real.** `src/cosa/rest/routers/podcast_generator.py:597-623`: reads the card key (code default `True` only if the key is absent — it is **not** absent), and when true builds a real `RuntimeArgumentExpeditor` and calls the real `expedite()`. Legacy `match_research_docs` preserved in the `else` branch (`:640-694`).

**But green proves the mock, not the flow.** `src/cosa/tests/unit/rest/test_podcast_generator_router.py`, class `TestSubmitFlowBExpeditor`:
- `:814` — `self.queue.config_mgr.get.return_value = True` — flag forced ON synthetically, never read from config.
- `:825-826` — `exp_inst = MagicMock(); exp_inst.expedite.return_value = <canned dict>` — `expedite()` is a mock.
- `:837` — `patch( f"{_EXP}.RuntimeArgumentExpeditor", return_value=exp_inst )` — the real RAE class is patched out.
- `:864` — `exp_inst.expedite.assert_called_once()` — asserts the **mock** was called.

So the suite proves the router's branch/arg-merge logic (flag-on → calls expedite(), merges dry_run/force_failure_mode, inherits the spec id, skips the legacy resolver). It does **not** prove the live card reaches the real RAE question flow. The live E2E was explicitly still owed and double-gated (row's final amendment: Rio's `e651bc76` landed; Clayton's script-parser fix pending) — it never ran.

## Q2 — reachable in production?

**No.** Baseline `False`, no Production override → `/api/podcast-generator/submit` takes the legacy `match_research_docs` branch in every deployed environment. The RAE card branch is dead in production. Confirmed independently from the INI section inheritance and the single occurrence of the key.

## Consequence & the decision that is not ours

- **72 green unit tests gate a path nobody can execute** in production, and none of them touch the real RAE.
- Closing 7c46fdde as *verified* is not available; *wired-but-dark* is.
- **Turning the flag ON in production is Rick's call, not ours** (Mr Radio takes it to him). Before it flips, the splainer's own precondition stands: the script-parser fix lands AND one live `/api/podcast-generator/submit` → RAE smoke passes on :7999.

## Evidence-to-key map (so no claim floats)

- Orphan run `pg-94631198` (today) → exercises **`runtime argument expeditor enabled`** (generator/push path). NOT this row.
- INI line 1512 + section inheritance → **`podcast card uses runtime argument expeditor`** = False everywhere (this row, Q2).
- Router `:597-623` → the card key's real wiring (this row, Q1 code half).
- Test `:814/:825/:837/:864` → the card key's tests mock the RAE (this row, Q1 mock half).

## Related
- 84d06f29 (Krishna, the wiring), 25fa9083 / 7e29bcfe (flag set False), fc4ccfe6 (routing diagnosis), 68198c9f + 3171c9dd (the pg- generator E2E, neighbour).

---

## Live smoke — the dark path lit for one run (Rachel 🕊️ a4365a7a, 2026-08-13 ~22:5x)

**Update to the verdict**: I ran the live card→facilitator smoke maria authorized. The result upgrades the row from *wired-but-dark* to **wired, lights up, then STALLS** — and it moves the safety answer from "unknown until run" to **not safe to light up**.

**Method (reversible, reverted after)**: forced `podcast card uses runtime argument expeditor = True` in `[Lupin: Development]` only (never Baseline), bounced :7999 via `bounce-dev-server.sh`, then POSTed `/api/podcast-generator/submit` with a vague description (`"the explainer about the KISS protocol…"`) and `dry_run=True` (guards the ~$0.46 render). Reverted the flag and bounced back to off (boot #64). Ground truth read from the :7999 docker log, not the HTTP response.

**Measured, reproducible across TWO runs**:
- **The card path DID reach the real RAE.** The log shows the RAE's own config reads `expeditor default value for podcast generator audience` / `audience_context` (4 lookups) — a fingerprint only `expedite()` produces. So the flag genuinely flips the branch and the endpoint builds a real expeditor, not a mock.
- **Legacy path NOT taken.** `match_research_docs` = **0** occurrences in the same window. The `else` branch is dead when the flag is on, as designed.
- **But `expedite()` then HANGS.** After reading its arg defaults it never surfaces a user question (`sender_id=arg.expeditor` asks = **0**), never creates a job, never completes. The client POST got no response across a **120s** open connection — a hang, not a fast error — on **both** runs.
- Contrast: the **generator** path (`runtime argument expeditor enabled`, neighbour key) completed a full podcast tonight (orphan `pg-94631198`: 21 segs, real mp3, $3.19). So the RAE machinery itself works; the **card entry into it** does not.

**Why this could FAIL and did** (independence check): the falsifiable question was "real RAE or legacy mock/branch?" — a legacy result would have shown `match_research_docs` and no expeditor-default reads. Instead the real RAE ran and then stalled, which the 72 green mocked tests could never have shown.

**Consequence for Rick's decision**: turning the Baseline flag on today would route every card submission into a facilitator that hangs. The row is closeable as *wired*; it is **not** verifiable as *working*. Next step is a debug of where the card-path `expedite()` blocks after computing missing args (candidate: the prediction/ask-building step before the first `notify_user_sync`), not a flag flip.
