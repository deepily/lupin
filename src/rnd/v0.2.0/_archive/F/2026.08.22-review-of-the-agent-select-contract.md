# Review — Tiberius's `agent_select_contract` predicate

**Reviewer**: Clayton 😎 (`b36934bf`) · **Date**: 2026-08-22 · **Under review**: `src/tests/e2e_ui/agent_select_contract.py` + `src/tests/unit/test_agent_select_contract_control.py` (Tiberius 👑, worktree `.claude/worktrees/qa-card-tester`)
**Verdict**: **PASS on falsifiability — every arm verified red by mutation. ONE BLOCKING GAP: the guard goes permanently red the moment phase 3 lands.**

## The falsifiability claim, verified independently

Tiberius reported "seen red by weakening the predicate". I did not take that on faith. I copied both files into my own tree (never mutating his live worktree), disabled each arm of `option_value_drift` in turn, and ran the control:

| mutation | result |
|---|---|
| disable `ORACLE EMPTY` arm | **1 failed**, 9 passed |
| disable `NO OPTIONS` arm | **1 failed**, 9 passed |
| disable `BLANK VALUE` arm | **1 failed**, 9 passed |
| drop the `MISSING` side of the difference | **1 failed**, 9 passed |
| drop the `PHANTOM` side of the difference | **1 failed**, 9 passed |
| restored | **10 passed** |

**Every arm kills exactly one test.** No arm is decorative, and none is covered only by another's assertion. This is the shape the plan asked for and did not have.

The `ORACLE EMPTY` arm in particular earns its place: it is the one thing standing between this gate and the empty-vs-empty trap, where an emptied HTML file and a broken endpoint agree at `set() == set()` and the suite reports green.

## 🔴 The blocking gap — Auto-Route makes the live guard permanently red

`AUTO_ROUTE_SENTINEL = "__auto_route__"` is declared in the module and **used nowhere in the predicate**. It appears only in the control's *clean* case, where the caller happens to put it on **both** sides.

That works synthetically and fails live. After phase 3, `expected` is the registry's `USER_INITIABLE_COMMANDS`, and the sentinel is **deliberately not a registry command** — that was the entire point of choosing a sentinel over an exemption. The rendered DOM must still carry Auto-Route. So the two sides disagree by exactly one element, forever.

Executed, not reasoned:

```
expected = { "agent router go to math", "agent router go to weather" }
rendered = [ "__auto_route__", "agent router go to math", "agent router go to weather" ]

  → PHANTOM: '__auto_route__' was rendered but is not in the expected set
```

**Recommended fix: drop the sentinel inside the predicate, not in each caller.** One stated exception, in the shared function, where the control can exercise it — rather than a `| { AUTO_ROUTE_SENTINEL }` that every future caller must remember to write and one eventually will not.

And it needs **two** control arms, not one:

1. the sentinel present in `rendered` and absent from `expected` is **clean**;
2. a *second* unrecognised value alongside it is still **PHANTOM**.

Without the second arm the fix is a hole the exact width of "anything the render feels like adding", which is the widening we removed the exemption to avoid.

## Two smaller notes, neither blocking

**The predicate cannot police its own oracle's correctness, only its emptiness.** `ORACLE EMPTY` catches a *missing* oracle; it cannot catch a *wrong* one. If phase 3 repoints `expected` at, say, all 20 registry commands instead of the `user_initiable` subset, every arm stays green while the gate checks the wrong set — which is the original gate-3 defect wearing a new coat. That belongs in the caller's own assertion, and it is worth one line in the phase-3 test saying which column it read.

**`rendered_list = [ v for v in rendered ]`** is `list( rendered )`. Harmless; mentioned only so nobody reads intent into it later.

---

# Addendum — the `display_name` gate falsifies backwards

**Ruling being reviewed**: Mr Radio, 2026-08-22 — *"`display_name` gets populated inside `_agentic_spec` from the contract entry, never hand-declared. The falsifiable gate ensures that any change in `JOB_ARG_CONTRACTS` will cause the spec to go red."* **Binding, not advisory.**

**The constraint is right. The stated falsification is backwards, and the two are mutually exclusive.**

Under the ruled derive-shape, `spec.display_name` moves *with* the contract by construction. So changing `JOB_ARG_CONTRACTS` can never turn a `spec.display_name == contract["display_name"]` gate red — the change propagates correctly and nothing is broken. Simulated both worlds:

| | baseline | after mutating `JOB_ARG_CONTRACTS` |
|---|---|---|
| derived (the ruled shape) | green | **green — never red** |
| hand-declared duplicate | green | **red** |

**Why the wording matters more than it looks.** Whoever writes the control will follow the stated falsification: mutate the contract, see green, and conclude the gate is broken. The repair they reach for is to make a contract change go red — and the only way to do that is to stop deriving the value. **The stated falsification, followed literally, un-derives the field and reintroduces exactly the drift the binding constraint forbids.**

**The gate's real subject is the constraint violation, so its must-fail control is:** hand-declare an agentic spec carrying a *differing* `display_name` → red. Same shape as `_card_drift`'s phantom arm — a real command string with one side withheld, never an invented token.

## A second gate, independent of the first

**Every agentic spec has a non-empty `display_name`.** Measured today: all 11 `JOB_ARG_CONTRACTS` entries carry one (`Deep Research`, `Podcast Generator`, `Research to Podcast`, `Claude Code`, …), none blank. So it is green now — and it goes red the day a 12th contract entry omits the key, which is the case where `.get()` returns `None` and the dropdown renders a blank option.

That is the failure the first gate cannot see: an absent key makes **both** sides `None`, and `None == None` passes.
