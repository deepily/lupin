# Pure-Voice Podcast E2E — Run Receipt (rows 3171c9dd + 68198c9f)

**Date**: 2026-08-13 21:48–21:49 EDT
**Runner**: Rachel 🕊️ (session 62476c3e), on Mr. Radio's crew (manager a31a20c5)
**Venue**: :7999 (live product path, via the ready harness)
**Harness**: `src/rnd/v0.2.0/2026.08.04-lane2-e2e-tiffany-harness.py` (`--no-mode --question "make me a podcast"`)

## Verdict

The pure-voice (no-mode) podcast E2E reached **done** on the live :7999 path. The no-mode
utterance routed to the podcast generator (pg-), the Runtime Argument Expediter resolved
the vague request to the seeded KISS doc, and a real ~10-minute audio artifact was produced
whose content is genuinely about the seed — not a hallucinated topic.

## Stage table (observed, not inferred)

| Stage | Result | Evidence |
|---|---|---|
| 1 push accepted | PASS | `POST /api/push` 200 on :7999, no side door |
| 2 routed to podcast generator | PASS | job id `pg-94631198::50c73ba7-…` — `pg-` prefix = podcast generator (doc-resolution path), NOT `rp-`. Confirms fc4ccfe6's measurement that bare "make me a podcast" routes to pg- |
| 3 RAE resolved the vague doc | PASS | finished podcast title `…-say-it-once-say-it-small-inside-the-ki…` carries the seed's invented catchphrase + KISS — only present in the seeded doc |
| 4 job ran | PASS | reached the running queue on :7999 |
| 5 job done, not dead | PASS | done queue, status `completed`; real mp3, 21 segments, ~10 min, 10.3 MB |
| 6 content about the source | PASS | script names 5/6 planted seed facts (Thelonius Quirke, 2019, fifty-eight percent, Project Marble Fountain, "say it once, say it small"); ham-radio decoy count = 0 |

## Artifacts (live at run time; `io/` is gitignored, not a repo scope)

- Script: `io/podcasts/interactive.job.tester@lupin.deepily.ai/2026.08.14-014852-say-it-once-say-it-small-inside-the-ki-script.md`
- Audio:  `io/podcasts/interactive.job.tester@lupin.deepily.ai/2026.08.14-014939-say-it-once-say-it-small-inside-the-ki.mp3`

## Honest caveats

- **Orphaned run.** The harness was killed just after `/api/push` fired (a venue-correction
  from Mr. Radio; the empty harness log was buffering loss, so the push had already gone).
  The job therefore ran with **no live websocket auto-answerer**. It still reached done: the
  interactive asks resolved by timeout-to-default and the fuzzy matcher picked the seed. This
  is a stronger signal for a live demo, not a weaker one — the correct podcast came out even
  with no answers to the asks.
- **Venue.** This is a ~11-min, state-mutating, job-enqueuing E2E — a :8000-class run that was
  hand-run on :7999. That venue mismatch is filed as its own defect (bug `c076245f`): the
  harness is hardcoded to :7999 `/api/push` and cannot be submitted as a gated :8000 suite.
- **Seed hygiene.** The seed KISS doc was written into the test user's corpus by the harness
  and removed after the run (the harness's own cleanup did not run because it was killed;
  removed by hand).

## What each row owed, and how this closes it

- **3171c9dd** (pure-voice no-mode E2E to done): closed — this run is the direct evidence.
- **68198c9f** (voice-driven vague-file demo E2E): closed — dropdown path was proven earlier
  (Tiffany, job `pg-e4f0092d`); this run proves the remaining unverified item, the pure-voice
  leg, to done.

## Related

- fc4ccfe6 — routing diagnosis (bare line → pg-, verbose line → rp-)
- c076245f — make this harness a gated :8000 suite (follow-on)
