# Memento — Rachel 🕊️ (session a4365a7a), under maria 🌸 (9662b5ac)

**Written by hand** 2026-08-13 ~22:45 (write_memento tool is failing silently tonight — bug P1 row 0a36d83d). Verify on disk before trusting.

## Lane
Podcast end-to-end + independent review of it, and the flag decision on **store row 7c46fdde** (card→RAE wiring, "wired but dark").
My store task item: **3e9b28bb** (lupin, owner rachel, accountable maria).

## What is VERIFIED (receipts, not expectations)
- **Generator-path podcast E2E = PASS, independent log receipt.** Tonight's orphan run `pg-94631198` (:7999 dev log, ~21:47–21:49 EDT) drove the *generator* path (`/api/push` → `_handle_agentic_command` → RAE) fully to done: facilitator asked its 3 questions + research + confirm live, script generated (21 segs), script-review gate fired (operator-routed to Rick, 10-min auto-approve), **real mp3 written** (`.../2026.08.14-014939-say-it-once-say-it-small-inside-the-ki.mp3`), Script $0.4675 / Audio $2.7219 / **Total $3.1894**, render 139.2s.
- **Card key is genuinely dark everywhere.** `podcast card uses runtime argument expeditor` = **False**, single occurrence at `src/conf/lupin-app.ini:1512` in **[Lupin: Baseline]**, no section override → Development/Production/Testing all inherit False. Config is read ONCE at startup (cached) → a change needs a :7999 bounce.
- Predecessor review (Rachel 62476c3e) on disk, solid: `src/rnd/v0.2.0/_archive/F/2026.08.13-rae-card-wiring-independent-review.md`.

## The DIVERGENCE (why the generator pass does NOT close the row)
Two different keys. `runtime argument expeditor enabled` (line 324, True in dev) governs the GENERATOR path — that's what pg-94631198 exercised. Row 7c46fdde is the CARD path (`/api/podcast-generator/submit` → RAE), governed by the FALSE card key. **The generator E2E passing tells us nothing about the card path.** The card→facilitator smoke is the decisive missing evidence.

## PENDING — the card→facilitator smoke (authorized by maria, DM 747d5568; GREEN-LIT DM 32bf074d)
Run ONE live card→facilitator smoke on :7999 with the card flag forced on in **[Lupin: Development] ONLY** (not Baseline). Steps:
1. Add `podcast card uses runtime argument expeditor = True` under `[Lupin: Development]` (line 32+).
2. **Bounce via `src/scripts/bounce-dev-server.sh`** (NOT raw restart) — fleet-wide idle check + acks; 4 maria seats + 2 other managers live tonight.
3. POST `/api/podcast-generator/submit` with `research_source=<description>`, `dry_run=True` (skips ~$0.46 render, still runs real `expedite()`).
4. **Ground truth from LOG + mp3, never the HTTP response.** Confirm `arg.expeditor` fires from the submit endpoint (job_id from submit_podcast_job, not a pg-/push id), proving the CARD reached the real RAE — not legacy `match_research_docs`.
5. **Revert the flag, bounce again. DO NOT leave it on** (maria's explicit instruction).

## SMOKE RESULT (DONE 2026-08-13 ~23:00) — flag reverted, dev bounced to off (boot #64)
Ran the card→facilitator smoke ×2, flag forced on in Development only, `dry_run=True`. Ground truth from :7999 log.
- **Card path DID reach the REAL RAE** — log shows `expeditor default value for podcast generator audience/audience_context` reads (only `expedite()` produces these); legacy `match_research_docs` = **0**. The flag genuinely flips the branch.
- **But `expedite()` HANGS** — reads its arg defaults, then NO user ask (`sender_id=arg.expeditor` = 0), no job, no completion; client POST got no response across 120s, both runs. A hang, not an error.
- Row 7c46fdde amended (event 6474); review doc has a "Live smoke" section.

## The flag answer for Rick (FINAL)
- **What turning it on does:** the podcast *card* (Submit Agentic Jobs) stops using its own `match_research_docs` file-resolver and routes through the Runtime Argument Expeditor instead.
- **What's unfinished behind it:** the card path had never run live; when I ran it, it reaches the facilitator and then **hangs** — no question, no podcast. The generator path works; the card path does not.
- **Safe to light up?** **No.** Flipping the Baseline flag today sends every card submission into a facilitator that stalls. Needs a debug of where card-path `expedite()` blocks (candidate: the prediction/ask-building step before the first `notify_user_sync`), not a flag flip. The flag is **Rick's call**.

## NEXT owed
Hand Rick the plain-language answer + doc link — DONE (notify + doc link). Card-path `expedite()` hang: offered maria the bug-file, her disposition (fold into 7c46fdde or spin a debug seat).

## Second lane — peer-DM header de-dup (DONE, committed, held for push)
Maria's task: `build_peer_dm_reminder` (hook_common.py) printed message_id+thread_id in the header AND the footer — cut from the header only, footer copies stay, `PEER_DM_FRAME_PREFIX` intact so `is_injected_peer_dm` + the poke-cap guard (user_prompt_submit.py:93) still match. New test `test_ids_ride_footer_only_and_match_survives` pins emit+match+dedup, falsifiable. Updated 6 sibling tests; 348 passed/2 xfailed across five peer-DM suites. **Committed cde54959** (3 files only, git show --stat verified, INI net-zero), held for Rick's push. No bounce (hook reads from disk).

## Standing rules for me
Address maria via dm_send, 3 sentences + a path. Report measured, not expected. Context monitored — re-spun past 50%. task_transition was classifier-denied at 22:39 (in_progress) — row still visible; don't fight the classifier.
