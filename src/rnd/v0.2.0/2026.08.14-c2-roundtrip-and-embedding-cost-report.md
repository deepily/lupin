# C2 round-trip control + embedding-cost venue — Sam 🎙️ report

**Session** d42f072b · **Manager** Cheech 🌿 · **Date** 2026-08-14
**Branch** wip-v0.2.0-2026.08.03-present-and-demo (main working tree, not a worktree)

## Job 1 — round-trip control (row 41333974) · DONE, green

**The gap was real.** The 25 prior tests drove `write_back` OR `lookup` against a
*fresh* store; none composed them. The fakes only *recorded* calls — the write path
wrote to `added_synonyms` / `upserts`, the read path read from `syn_verbatim` / `rows`.
Different structures, so no test ever closed the loop. 100% line/branch coverage
never saw it because every line already ran; what never ran was the round trip.

**Fix — two parts, both in `src/tests/unit/test_v2_cache.py` only** (product `cache.py`
byte-unchanged, `git diff --stat` clean):

1. Made the three fakes *persist* as well as record — `upsert_snapshot` stores the
   row into `rows`, `add_synonym` populates `syn_verbatim`/`syn_normalized`,
   `add_embedding` seeds `embeddings`, `delete_by_snapshot_id` clears prior keys.
   Backward-compatible: every existing recording assertion still holds.
2. Two new tests:
   - `test_round_trip_write_back_then_identical_lookup_is_tier1_replay` (×2 debug) —
     write_back a snapshot, look up the SAME verbatim question, assert
     `is_replay_hit=True`, `tier="exact_verbatim"`, the v2 tag survived the marshal,
     and zero model calls end to end.
   - `test_round_trip_normalized_variant_also_replays` — a casing/whitespace variant
     replays via `exact_normalized` (tier-1b).

**Fail-first receipt** (the control earns its keep): removed `write_back`'s
`add_synonym` call → all 3 round-trip tests go RED at `assert result.is_replay_hit
is True` (the second identical request no longer replays). Restored → green.

**Result**: `37 passed`; `cosa.rest.v2.cache` **100% lines (161/161) + 100% branches
(44/44)**.

## Job 2 — live embedding-cost number · BLOCKED on a structural decision, not credentials

`src/scripts/v2_embedding_cost.py --live` is a **standalone CLI** needing `get_db()` +
the model server. `POST /api/test-suite/submit` runs **pytest suites** — its
`test_types` field accepts only `integration` / `e2e` (see `test_suite.py:31`). There
is no path to run this script through the sanctioned endpoint as-is, and side-doors
(curl / direct CLI on a box) are explicitly forbidden.

**Decision needed from Cheech**: wrap the live measurement in a pytest test (a new
:8000-marked suite the endpoint can dispatch) — a small code change — or a different
ruling. Until then the live number can't be captured through the allowed venue.
