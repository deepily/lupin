# Phase 2 drift guard — RED prediction (written BEFORE the run)

**Author**: Tiberius 👑 · **Date**: 2026-08-15 · **Manager**: Rachel 🕊️
**Design**: `2026.08.15-agent-registration-single-source.md` §6 / §7 phase 2
**Discipline**: named prediction first, then run, then diff. Anything the run produces
that is NOT below = my guard measures more than I think. Anything below that does NOT
appear = my guard is not reaching.

The guard is EXPECTED RED — the red is the drift inventory, the deliverable. Nothing
here is to be greened by loosening (that is phase 3).

---

## The four sources (measured, not guessed)

| class | commands | | count |
|---|---|---|---|
| CONVERSATIONAL (registry `V2_AGENTS`) | math, calculator, datetime, todo, calendar, weather | | 6 |
| AGENTIC (registry `AGENTIC_COMMANDS`) | deep research, podcast generator, research to podcast, claude code, presentation generator, research to presentation, swe team, **bug fix expediter**, **test fix expediter resume**, **test suite** | | 10 |
| CONTROL | automatic | | 1 |
| NONE | receptionist, none | | 2 |

| source | agentic members | count |
|---|---|---|
| **prompt** template | deep research, podcast generator, research to podcast, claude code, presentation generator, research to presentation, swe team | **7** |
| **training** (`agent-router-agentic-commands.json`) | the 7 above + test suite, **test fix expediter**, test fix expediter resume | **10** |
| **factory** (`create_agentic_job` if/elif) | the 7 above + test suite, bug fix expediter, **test fix expediter**, **heartbeat poker** | **11** |

Scoping note (§5.3): training reads `agent-router-*.json` ONLY. The two `vox-cmd-*.json`
files (23 browser commands) are a separate vocabulary and are excluded; including them
would false-orphan all 23.

---

## Prediction, per assertion (named commands + failure shape)

### Assertion 1 — agentic completeness (registry-agentic vs each consumer)

- **1a  `registry_agentic ⊆ prompt`** → **FAIL**. Missing from prompt:
  **`test suite`, `bug fix expediter`, `test fix expediter resume`** — owned agentic
  commands the router is never told to emit (unreachable by voice).
- **1b  `factory == registry_agentic` (both directions)** → **FAIL**.
  - in factory, not owned: **`test fix expediter`, `heartbeat poker`**
  - owned, no factory branch: **`test fix expediter resume`** (the dead-end: full
    argument interview, then `create_agentic_job` returns None)
- **1c  `training_agentic == registry_agentic` (both directions)** → **FAIL**.
  - in training, not owned: **`test fix expediter`**
  - owned, not in training: **`bug fix expediter`**

### Assertion 2 — conversational (in prompt + training, in NO factory branch) → **PASS**
All 6 conversational commands are in the prompt and the training keys and none has a
factory branch. No conversational drift predicted.

### Assertion 3 — control + none in prompt + training only, BOTH directions → **PASS**
`{automatic, receptionist, none}` are each in prompt and training and in neither the
factory nor the agentic set. No extra, no missing. (arnold's both-directions form.)

### Assertion 4 — class partition (no command in two buckets) → **PASS**
Registry assigns exactly one `cls` per command by construction.

### Assertion 5′ — every `cli_module` runs (`python -m <mod> --help`, returncode 0) → **FAIL**
- **FAIL (predicted, §4)**: `cosa.agents.claude_code`, `cosa.agents.bug_fix_expediter`,
  `cosa.agents.test_fix_expediter` — packages with no `__main__.py`; the returncode is
  non-zero and the check catches it directly (not via the `"No module named"` wording).
- **PASS (predicted)**: `cosa.agents.deep_research.cli` (module with a `__main__` guard),
  `cosa.agents.podcast_generator`, `cosa.agents.presentation_generator`.
- **UNKNOWN — the interesting part**: `cosa.agents.deep_research_to_podcast`,
  `cosa.agents.deep_research_to_presentation`, `cosa.agents.swe_team`. §4 did not flag
  them; I have NOT independently confirmed they are runnable. A FAIL here is a real
  finding I did not predict; a PASS confirms them.
- **SKIP**: `test suite` has `cli_module = None` (API-invoked).

### Falsifiability demos (one per assertion) → **PASS**
Each assertion ships a demo that the predicate fires on a synthetic broken input
(a stand-in that leaks / a mutated source set). These are expected to PASS — they prove
the assertion above can go red.

---

## Predicted failing test methods: 4

`1a`, `1b`, `1c`, `5′`. Assertions 2, 3, 4 and all falsifiability demos PASS.

## The five §3 drifts, and where each surfaces

| §3 drift | surfaced by |
|---|---|
| `bug fix expediter` unreachable by voice | 1a (not in prompt) + 1c (owned, not trained) + 5′ (unrunnable) |
| `heartbeat poker` unreachable by voice | 1b (in factory, not owned) |
| `test fix expediter` → web search | 1b (factory extra) + 1c (training extra) |
| `test fix expediter resume` → dead-end | 1a (not in prompt) + 1b (owned, no factory branch) + 5′ (unrunnable module) |
| `test suite` reachable only by accident | 1a (not in prompt) |

All five surface. If any does NOT, that assertion is not reaching.

---

## OUTCOME (after the run) — diff prediction vs actual

**Result: 4 failed, 7 passed.** Test file: `src/tests/unit/test_v2_registry_drift_guard.py`.
Every named prediction landed exactly. The four categories Rachel asked for, empties stated:

### 1. Predicted-and-confirmed
- **1a** missing from prompt = `bug fix expediter`, `test fix expediter resume`, `test suite` — **exact**.
- **1b** in factory not owned = `heartbeat poker`, `test fix expediter`; owned no factory branch = `test fix expediter resume` — **exact**.
- **1c** in training not owned = `test fix expediter`; owned not trained = `bug fix expediter` — **exact**.
- **5′** unrunnable = `cosa.agents.bug_fix_expediter`, `cosa.agents.claude_code`, `cosa.agents.test_fix_expediter` (all `rc=1`, `No module named …__main__`) — **exact**.
- Assertions **2, 3, 4 PASS**; all **4 falsifiability demos PASS** — as predicted.
- All five §3 drifts surfaced where predicted.

### 2. Predicted-but-absent (guard not reaching) — **EMPTY**
Nothing predicted failed to appear. No assertion is under-reaching.

### 3. Unpredicted (guard reaches further than I thought) — **EMPTY**
No failure appeared that I did not name. The three cli_modules I flagged as UNVERIFIED
(`deep_research_to_podcast`, `deep_research_to_presentation`, `swe_team`) all **PASS** —
they are runnable. My named-uncertainty resolved to green; no surprise, but it was a real
place a surprise could have landed.

### 4. Red that means the TEST is wrong, not the tree — **EMPTY**
Every red is a real §3 drift or a real §4 unrunnable module. The falsifiability demos
confirm each predicate is live, not a no-op. No test-bug red.

**Verdict**: the guard measures exactly what the prediction said — no more, no less. The
red IS the §3/§4 inventory. Phase 3 greens it by fixing the drifts, never by loosening.
