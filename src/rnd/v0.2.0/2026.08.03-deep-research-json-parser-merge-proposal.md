# Proposal: fold deep_research's JSON parser into the shared helper

**Date:** 2026-08-03 · **Author:** Rio ⚡ · **Row:** d923ceb2 · **Status:** PARKED until after the 2026-08-06 demo (Mr. Radio's ruling 2026-08-03)
**Predecessor:** Rachel's consumer sweep `src/rnd/v0.2.0/2026.08.03-json-parser-last-brace-consumer-sweep.md` (commit 52cde456)

> **PARKED, do NOT land before 2026-08-06.** The merge is behaviour-affecting (FIRST-vs-LAST brace, below) and demo week is not when to discover how often that divergence fires. The recommended contract and wrapper stand — they just wait. Whoever resumes this inherits the landing gate in the "Landing precondition" section; do not re-derive it.

## Verdict

Adopt the shared helper's existing contract — **the parser returns `None` and never raises; each consumer owns its failure posture.** Deep research keeps its raise via a 3-line wrapper; BFE folds trivially. **One genuine behaviour change needs scrutiny before landing: deep_research selects the FIRST balanced brace span, the shared helper selects the LAST.** Everything else is structural or a strict improvement.

## Count correction: the sweep undercounted — there are FOUR copies, not three

State this plainly so the next reader trusts the number, not the claim: **Rachel's consumer sweep (52cde456) counted three copies; the real count is four.** The one it missed is BFE `orchestrator.py:526` `_extract_last_json_object`, whose body is byte-identical to the shared extractor. This is not a knock on the sweep — a private static method with a different name does not match a grep for `extract_json_object` — but the operative number for anyone planning this merge is **4**: three last-brace copies (podcast, presentation, BFE — podcast/presentation already folded) plus deep_research's genuinely-different first-brace implementation.

## The copies (four, not three)

Rachel's sweep named three; there is a fourth (BFE). The last-brace extractor body is duplicated in three places; deep_research is a genuinely different algorithm.

| # | Location | Selection | Failure posture | Return |
|---|---|---|---|---|
| Shared | `io_models/utils/json_object_recovery.py` (`recover_json_object`) | **LAST** balanced `{…}` | returns `None`, logs body at ERROR; **caller raises** | parsed `Any` |
| 1 | podcast `script_generation.py` | via shared | caller raises on `None` | — |
| 2 | presentation `json_recovery.py` | via shared | caller raises on `None` | — |
| 3 | deep_research `api_client.py:131` | **FIRST** balanced `{…}` that parses | **RAISES `ValueError`** inside the parser | `dict` |
| 4 | BFE `orchestrator.py:526` (`_extract_last_json_object`) | **LAST** (byte-identical to shared extractor) | returns `None` → caller calls `_fallback_diagnosis` | `str` substring |

## Recommended merged contract

**Parser recovers only; posture lives in the caller** (the shared helper's deliberate design, and the pattern set by the fuzzy-prefilter helper e651bc76). Rationale: policy in the caller means one parser can serve a raiser, a None-propagator, and a fallback-returner without any of them inheriting a posture they did not choose. It is also the anti-silence design — `recover_json_object` logs the full raw body at ERROR on failure, which is exactly the observability whose absence let the podcast copy discard good output for five months.

Concretely:
- **deep_research** — replace its private `extract_json_object` with a thin wrapper that preserves its RAISE boundary:
  ```python
  def extract_json_object( text ) -> dict:
      if text is None or not text.strip():
          raise ValueError( "Cannot extract JSON from empty/blank response" )
      obj = recover_json_object( text )
      if obj is None:
          raise ValueError( "Response did not contain a recoverable JSON object" )
      return obj
  ```
  Its one real caller (`api_client.py:449`) and consumers see the SAME raise-on-failure behaviour.
- **BFE** — replace `_extract_last_json_object` with the shared `extract_json_object` (pure extractor). Bodies are byte-identical → zero behaviour change; caller at `:511` keeps its fence-split + `_fallback_diagnosis` posture.

## Consumers that would move

| Consumer | Move | Behaviour impact |
|---|---|---|
| deep_research `api_client.py:449` (sole real caller) | parser body → shared via wrapper | see flags below |
| deep_research inline smoke test `api_client.py:589-599` | must still pass (recover+raise asserts) | asserts single-brace + fence + blank-raise — all preserved |
| BFE `orchestrator.py:511` caller | private method → shared import | none (byte-identical) |
| podcast, presentation | already on shared helper | none |

No TFE copy exists (checked `test_fix_expediter/` — none).

## Behaviour changes to flag (NOT just structure)

1. **FIRST → LAST brace selection (the one that matters).** deep_research picks the first balanced span that parses; the shared helper picks the last. On a completion with multiple top-level brace spans (a prose example object before the real one, or the real object followed by brace-bearing trailing prose), the two pick **different objects**. The direct-`json.loads` fast path masks this for clean single-object responses (both identical); divergence only bites on the fallback path with multiple spans. **Before landing:** capture 5–10 real deep-research completions and confirm LAST-brace + fence-cut returns the same object FIRST-brace does. If any diverge, that is the risk to weigh.
2. **Fence handling gains the P0 fix.** The shared `recover_json_object` drops everything after the CLOSING ``` fence (P0 4317efd1); deep_research only trims a trailing ``` when the content ends with it. Folding **adds** the trailing-prose-after-fence fix to deep research — an improvement, but a change.
3. **Loud ERROR logging appears.** `recover_json_object` logs the full (bounded) raw body at ERROR on unrecoverable input; deep_research currently raises with no body log. Folding adds that observability — an improvement, but new log output.
4. **Return-type nuance.** deep_research is hinted `-> dict`; `recover_json_object` returns `Any`. A top-level JSON array/scalar would pass through where a dict is expected. If strict dict-ness matters, the wrapper should `assert isinstance( obj, dict )` — decide explicitly.

## Testing owed if landed

- Port deep_research's 4 inline smoke asserts into a real pytest against the wrapper (recover + raise-on-blank + raise-on-unrecoverable).
- A divergence test: a multi-brace input where FIRST ≠ LAST, pinning the chosen semantics deliberately.
- BFE: a byte-identical-swap regression (None → fallback path unchanged).
- 100% lines/branches on the wrapper + any shared-helper change.

## Landing precondition (concrete gate — inherit this, do not re-derive it)

Do NOT land this merge until BOTH hold:

1. **Mr. Radio's word** (the merge is parked until after 2026-08-06).
2. **FIRST-vs-LAST divergence check passes.** Collect **5–10 real deep-research completions** (from `io/` logs or a live `:7999` deep-research run — real model output, not synthetic). For each, run BOTH extractors on the raw completion and compare the parsed object:
   - `old = deep_research.extract_json_object( text )` (FIRST balanced brace)
   - `new = recover_json_object( text )` (shared, LAST balanced brace, with the P0 fence-cut)
   - **PASS** = `old == new` on every sample → the LAST-brace switch changes nothing on real traffic → land the merge.
   - **FAIL** = any sample where `old != new` → the switch would change what deep research extracts. Do NOT land silently. Either (a) keep deep_research on first-brace semantics by parameterizing the shared helper (a `prefer="first"|"last"` arg), or (b) get an explicit ruling that the LAST-brace object is the correct one for those cases. Record which, with the diverging samples, before landing.

   Record the sample set + the comparison result in this doc (or a sibling) so the PASS/FAIL is auditable, not asserted.

## Recommendation

Proceed with **return-None parser + caller-owned posture**, deep_research via the raise-wrapper, BFE via the byte-identical swap — AFTER the two preconditions above clear. The FIRST-vs-LAST check is the only step that can change what deep research actually returns; everything else is structural or a strict improvement. Not urgent (P2, deep-research path is not on the demo).
