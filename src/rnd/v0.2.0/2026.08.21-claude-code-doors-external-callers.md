# Out-of-repo callers of the two Claude Code doors

**Measured** 2026-08-21 by Rio ⚡ (Implementer B, store row `14172091`) from the main checkout at
`/mnt/DATA01/include/www.deepily.ai/projects/lupin`. Read-only grep sweep; nothing executed, nothing
edited outside this repo.

**Why this file exists**: `/api/claude-code/submit` and `/api/claude-code/queue/submit` now answer
**410 Gone** naming `/api/v2/submit`. Everything below is a caller this repo cannot fix, so it is owed
work resident in another repo. Companion to
`2026.08.21-cascade-resume-artifacts/2026.08.21-external-callers-of-non-surviving-doors.md`, which swept
the other sixteen doors and recorded mobile as already migrated **off the alias and onto the canonical
path** — which is exactly why the canonical path had to retire too, and why mobile is affected now when
it was not before.

## Verdict

| | |
|---|---|
| Out-of-repo surfaces sweeping positive | **1** — `src/lupin-mobile` |
| Out-of-repo surfaces sweeping clean | **3** — `src/lupin-plugin-firefox`, `cosa-voice`, `planning-is-prompting` |
| Live out-of-repo call sites | **1** (`claude_code_repository.dart:19`) |
| Out-of-repo test doubles that will go red | **3** registrations across 2 files |
| Cron / shell callers | **0** |

## The one surface that calls it — `src/lupin-mobile` (separately-managed repo)

| kind | file:line | what it is |
|---|---|---|
| **live call** | `lib/features/claude_code/data/claude_code_repository.dart:19` | `_dio.post( "/api/claude-code/submit", … )` — the only real request |
| doc comment | `lib/features/claude_code/data/claude_code_repository.dart:5, :11` | dartdoc naming the door as canonical |
| doc comment | `lib/features/claude_code/data/claude_code_models.dart:8, :11, :45` | request/response model dartdocs |
| doc comment | `lib/features/claude_code/domain/claude_code_bloc.dart:8` | BLoC dartdoc |
| test double | `test/unit/claude_code/claude_code_repository_test.dart:18, :37` | Dio mock handler keyed `POST /api/claude-code/submit` |
| test double | `test/unit/claude_code/claude_code_bloc_test.dart:24, :46` | same |

**What mobile has to do**: post to `/api/v2/submit` with
`{"command": "agent router go to claude code", "args": {prompt, project, task_type, max_turns, dry_run},
"websocket_id": …, "scheduled_at": …, "monopolize": …}`, and read `job_id` off the v2 `AskResponse`.
`queue_position` is **gone** and is not coming back — a place in the queue frozen at submission is stale
the moment it is printed; the job card learns its real place from the queue websocket events.

**Mobile has been here before, on this exact endpoint.** `src/lupin-mobile/history.md` records the
2026-05-11 cutover from the alias to the canonical path: it shipped against a server that had not
renamed yet and took 404s until the parent caught up, deliberately ("obviously disable, don't silently
mask"). The failure mode this time is friendlier — 410 with a body that names the replacement and the
removal date — but the coordination cost is the same shape.

## The three that sweep clean, and the exact commands

Recorded so the next reader can refute this file row by row rather than re-deriving it:

```bash
grep -rn "claude-code/submit\|claude-code/queue/submit" src/lupin-plugin-firefox   # 0 hits
grep -rn "claude-code" src/lupin-plugin-firefox                                    # 0 hits
grep -rln "claude-code/submit\|claude-code/queue/submit" \
     /mnt/DATA01/include/www.deepily.ai/projects/cosa-voice                        # 0 files
grep -rln "claude-code/submit\|claude-code/queue/submit" \
     /mnt/DATA01/include/www.deepily.ai/projects/planning-is-prompting             # 0 files
```

**cosa-voice does not call either door.** The row named it as a likely caller; it is not one. Its MCP
surface never posts to the Lupin queue — sessions submit Claude Code work by hand or through the UI.
Reported as an evidence-backed exclusion, not as an edit.

## Also owed, and NOT out-of-repo — the generated API docs

`src/docs/fastapi/api.json` and `api.md` are stale for **every** retired door, not just these two:
`/api/bug-fix-expediter/submit` and `/api/deep-research/submit` still read as live, and `/api/v2/submit`
is absent entirely. Regenerating needs `src/scripts/generate-api-docs.sh` against a `:7999` serving this
branch's code, which is a whole-branch step after the merge, not a per-door edit — hand-patching a
generated artifact would only make the drift harder to see. Flagged here so it is not mistaken for an
omission in row `14172091`.
