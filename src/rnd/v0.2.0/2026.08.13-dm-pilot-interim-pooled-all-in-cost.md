# DM verbosity pilot — interim read, pooled all-in cost

**Date**: 2026-08-13
**Author**: Cheech 🌿 (session 6794a377)
**Status**: INTERIM, and the collection has since been **stopped**. See "Collection status" below before
citing any number here as final.
**Rows**: `a3666252` (the pull), `35d0a451` (the measurement gap this doc executes the recommendation of)
**Predecessor**: `src/rnd/v0.2.0/2026.08.04-dm-arm-all-in-cost-and-breakeven.md` (Tuesday-only, first statement of the sign flip)

---

## Headline

**Measured on total tokens spent, the rejecting arm does not save tokens — it costs about 7% more.**

The saving everyone has been quoting is real but it is a saving *per message that lands*, and it is
computed over a population that excludes every draft the gate threw away. Put the thrown-away drafts
back in and the sign flips. This was predicted from Tuesday alone on 2026-08-04; it now holds on the
pooled corpus of 1,951 eligible attempts across seven days.

---

## The two numbers, side by side

Both are legitimate. They answer different questions, and the row that filed this gap
(`35d0a451`) recommended reporting both rather than replacing one. That is what this table does.

- **Published** = mean `est_tokens` over **delivered rows only** — *"how long is a message that lands?"*
- **All-in** = total `est_tokens` over **all attempts** ÷ delivered count — *"what did a landed message cost?"*

### Pooled — all seven days (n = 1,951 eligible)

| arm | attempts | delivered | refused | published / delivered | all-in / delivered | tokens burned on refusals |
|---|---:|---:|---:|---:|---:|---:|
| blind | 814 | 814 | 0 | 231.8 | 231.8 | 0 |
| rejecting | 1,137 | 867 | 270 | 161.4 | 248.0 | 75,071 |
| **rejecting vs blind** | | | | **−30.4%** | **+7.0%** | |

### Tue/Wed only — 08-04 + 08-05 (n = 1,462 eligible)

| arm | attempts | delivered | refused | published / delivered | all-in / delivered | tokens burned on refusals |
|---|---:|---:|---:|---:|---:|---:|
| blind | 632 | 632 | 0 | 246.4 | 246.4 | 0 |
| rejecting | 830 | 610 | 220 | 164.5 | 264.6 | 61,043 |
| **rejecting vs blind** | | | | **−33.2%** | **+7.4%** | |

The two slices agree: roughly a third cheaper per landed message, roughly 7% more expensive all-in.
Pooling four extra days did not rescue the arm and did not change the sign.

---

## Why the sign flips

Nothing subtle. The rejecting arm burned **75,071 tokens on 270 drafts that were composed, sent over
the wire, and refused**. Those tokens were genuinely spent by the sender — `est_tokens` is stamped on
the draft before the gate returns 413 (`dm.py:869`). The published average simply never looks at them.

The gate also selects for the **longest** drafts by construction, so every refusal discards an
above-average message and pays again for the rewrite. A refusal can never pay for itself on the
message it refuses: blind costs D, rejecting costs D burned plus R rewritten. The arm can only win by
changing behaviour on **future** messages.

---

## Which is exactly the thing that did not reach significance

From `analyze_arms.py` at the same corpus, 14 matched clock-hour pairs:

| co-primary | what it tests | n pairs | observed | p | crossed? |
|---|---|---:|---:|---:|---|
| A — all attempts | did messages get shorter, retries included | 12 | −32.963 | **0.047** | yes |
| B — first attempts only | **genuine restraint** on the first try | 12 | −32.003 | **0.055** | no |

The pre-registered reading is written into the script's own output: *"Both move → genuine restraint;
only A → retries not restraint; neither → no effect at this dose."* Only A moved.

⚠️ **Read that with the caveat it deserves.** 0.047 and 0.055 straddle the line by a hair; this is a
knife-edge verdict, not a decisive one, and two more days of collection may move either. What is
**not** knife-edge is the cost arithmetic: +7.0% is a direct measurement of tokens spent and needs no
causal assumption at all.

Put together: the mechanism that could have made the arm pay for itself is the one that failed to
cross, and the arm's actual cost is a loss. Those two facts point the same way.

---

## Collection status — the pilot was SUSPENDED partway through the day this was written

⚠️ **Corrected 2026-08-13 later the same morning.** When I first wrote this section it said the
schedule carried **112 slots running to 2026-08-15T14** and that two more days of collection were
coming. That was true of the schedule file and false about what would happen.

**Rick suspended the pilot on 2026-08-13.** `dm experiment suspended = True` landed in
`src/conf/lupin-app.ini` with an `is_suspended()` guard in `src/cosa/rest/dm_experiment.py`, shipped
in commit **`b8d10bd3`** alongside the fleet-wide DM tutor. The wording in the config is explicit that
this is a suspension and not a deletion: *"The schedule JSON and every row already collected are
UNTOUCHED — the blind-vs-rejecting question stays its own analysable study."*

**It did not take effect when it was written.** `:7999` does not auto-reload, so the running server
never saw `is_suspended()` and kept assigning arms and gating live DMs after the ruling — I measured
the corpus taking a new row at 14:09:19Z, well after the change was on disk. The suspension became
real only at the `--force-recreate` that served `b8d10bd3`. Anyone reconciling row counts against the
ruling's timestamp will find rows that postdate it; that gap is the explanation, not a data error.

⇒ **The remaining 08-13 through 08-15 slots never ran.** The corpus is final as of that recreate.

Slots used: 39 distinct, first `2026-08-04T09`, last `2026-08-13T09`. The numbers in this document
were computed at ~14:00Z; a small number of rows landed between then and the recreate, so a final
pull will move the counts slightly. **It will not move the sign** — the all-in gap is 7 points wide
and the last hour of a thin day cannot close it.

Per-day coverage is well under the 7-per-arm-per-day target on every day, and **08-08 through 08-10
produced no eligible rows at all**:

| date | blind slots | rejecting slots |
|---|---:|---:|
| 2026-08-04 | 5 | 5 |
| 2026-08-05 | 3 | 4 |
| 2026-08-06 | 2 | 2 |
| 2026-08-07 | 4 | 2 |
| 2026-08-08 – 08-10 | — | — |
| 2026-08-11 | 4 | 3 |
| 2026-08-12 | 1 | 3 |
| 2026-08-13 | 0 | 1 |

Thin coverage is the standing condition of this pilot, not a new problem — the fleet pause starved
the original window and Rick authorised extension rather than a re-run.

---

## What is NOT being done here, deliberately

`analyze_arms.py` is **unchanged**. It still publishes the delivered-only number. Changing how a
**live** experiment's corpus is scored mid-flight is a ruling for Rick, not a quiet edit by me, and
the experiment has two days left to run. The corpus is append-only and untouched, so both numbers stay
computable at any time — including retroactively, after the window closes.

The reproduction script for the table above is scratch, not committed; it reuses
`analyze_arms.eligible_rows` unchanged so the population predicate is byte-identical to the published
analysis. The only thing it changes is the numerator.

---

## Owed next

1. **Now, not 08-15** — the suspension made the corpus final early, so the final pull is owed
   immediately rather than after a window that will never close. Rows `a3666252` and `8810cb5c` were
   re-parked this morning to 2026-08-16 on the assumption the pilot ran to 08-15; that assumption died
   the same morning and both chases should move **earlier**. Nothing is waiting on a clock any more.
2. **Rick's ruling on `35d0a451`** — publish both numbers permanently, or leave the delivered-only
   number as the headline. This doc executes the recommendation once; the code still does not.
3. Co-primary B at final n. It is the designed test of the causal question and it is currently the
   difference between "this arm works" and "this arm costs 7% extra."
