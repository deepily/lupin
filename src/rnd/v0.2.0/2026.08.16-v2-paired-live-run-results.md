# v2 Paired Live Run — Results & Provisioning Record (row d8d019f6)

**Author**: John 🏄🏽 (session 92ceedc1), on Tiberius 👑's crew · **Date**: 2026-08-16 overnight (box kept up past midnight per Rick via Rachel)
**Row**: d8d019f6 (owner+mgr Tiberius, on john's lane) · **Verdict gate**: closes ONLY on a real median-Δ number (pass or loud refuse), never unit-green.
**Co-verify before close**: Rio + Sam.

---

## Verdict

> **NO median-Δ yet — the closing run is DEFERRED to a morning window (Tiberius, 2026-08-17 ~00:45 EDT).** Three live runs all hit the test-suite's **2000s (33-min) integration timeout** (`job.py:84`) and were SIGTERM'd (exit 143) BEFORE the paired gate could fire. Not a gate refuse, not unit-green — a wall-clock kill. d8d019f6 stays in_progress; it closes only on a real median-Δ number from the full **n=60** run (the design's pre-declared sample).

### First completed run — reached the gate, REFUSED on JWT expiry (ts-d0f50349, n=10, 3600s budget)

The n=10 re-run at a 3600s timeout **completed both arms in 2331s** and reached the integrity gate — the first run to do so. It **refused with reason** (no median-Δ): `guard_run_integrity` (`v2_eval.py:596`) raised `EvalIntegrityError` — `http-all-ok: 4 of 50 requests did not return 200`. The 4 non-200s are all **401 Unauthorized**, clustered at the END (96×200, 4×401). Root cause: the v2 arm's `HttpAskClient` logged in ONCE and held a static bearer; the JWT **expired ~39 min in**. Not a timeout (client timeout 120s; v2 requests measured 34-67s each).

**Fixed — `a4a8470f`:** `HttpAskClient` gains an injectable `relogin_fn`; on a 401 it re-mints a fresh bearer and retries once (stopwatch reset → recorded span is the successful retry). `_default_client_factory` wires it to the same `/auth/login`. RED control `test_http_ask_client_refreshes_bearer_on_401` (proven RED on revert) + a negative control; 6 green. **Prerequisite for the morning n=60** — without it a ~5h run 401s everything past ~30min and can never emit a verdict.

### Why it doesn't fit (measured rates, 2026-08-17)

| Arm | Path | Measured rate | n=10 (2 passes) | n=60 (2 passes) |
|---|---|---|---|---|
| v1 (`:7997`, b0735467) | `/api/push` + async WS collect | **~6.7 s/push** | ~100 pushes ≈ **670 s** | ~600 pushes ≈ **4000 s** |
| v2 (`:8000`, HEAD) | `/api/v2/ask` (synchronous full-inference round-trip) | **~22 s/call** | ~100 calls ≈ **2200 s** | ~600 calls ≈ **13200 s** |
| **Total** | | | **~2870 s (~48 min)** | **~17200 s (~4.8 h)** |

The v2 arm is ~5× slower per unit because each `/api/v2/ask` is a blocking end-to-end Phi-4 inference, vs v1's fast queue-push measured via async WS completion. Run receipts: `ts-cc25ab70` (n=60) SIGTERM'd after ~297 v1 pushes = the v1 **cold pass alone** ≈ 2000 s; `ts-e713237f` (n=10) SIGTERM'd after 59 v2 calls.

### Morning n=60 procedure (the closing run)

1. Fresh boot, **7-9 h runway** (box powers off nightly ~22:53-00:20 & is back ~07:17; run in the morning-awake window off Rick's 9pm-12am peak).
2. Re-stand :7997 pinned v1 at b0735467 → `lupin_db_v1baseline` (mock auth + model-server embeddings): `bash <scratchpad>/launch-v1-7997.sh` (or the compose-container option from the design doc). Verify `/api/code-identity` = b0735467 and `pg_stat_activity` shows :7997 on `lupin_db_v1baseline`.
3. **Raise the integration timeout to ~30000 s (8.3 h)** in `job.py:84` — 10000 s is NOT enough (n=60 ≈ 4.8 h + margin). Currently reverted to 2000 s for overnight hygiene.
4. `LUPIN_PAIRED_N=60` is already in the compose `lupin-rest-test` `environment:` block; `LUPIN_V1_ARM_BASE_URL=http://host.docker.internal:7997` too. `docker compose up -d --force-recreate lupin-rest-test` (env + timeout resolve at CREATE).
5. Clean both stores to 0/0 (`TRUNCATE solution_snapshots` on `lupin_db_v1baseline` AND `lupin_db_test` — the v2 clean-step handles `lupin_db_test`, but truncate `lupin_db_v1baseline` by hand since the v1 arm doesn't).
6. `list-pending` idle → submit via `POST /api/test-suite/submit`, `pytest_args="-m paired_eval_live src/tests/integration/test_v2_paired_live.py -v"`.
7. On landing: median-Δ verdict-first → Rio + Sam co-verify → Tiberius closes d8d019f6. **Revert `job.py:84` back to 2000 s as part of the close.**

---

## What this run required beyond "a-d is built"

The a-d arm-running wiring (`build_paired_verdict`) was committed at `5aade893` before tonight. Standing the LIVE run up surfaced a cascade of real gaps — each fixed, each a genuine defect the unit layer had masked:

### 1. Live-only bug — raw-string TRUNCATE (`v2_eval.py:690`) — FIXED `291fb3fa`
`clean_v2_snapshot_store` passed a raw f-string to SQLAlchemy 2.x `connection.execute()`. A `MagicMock` accepted it (unit-green); a live connection rejects it: `'str' object has no attribute '_execute_on_connection'`. Wrapped in `text()`. Reachability unit test now asserts the arg is a `TextClause` (not bare str) → **RED on revert, proven** (line 163 AssertionError).

### 2. Live-only bug — clean-step TRUNCATE never committed (`v2_eval.py`) — FIXED `9b90ae5d`
`_open_v2_arm_connection` returns a plain SQLAlchemy 2.x `Connection` (commit-as-you-go). Without `connection.commit()`, the TRUNCATE rolled back on `close()`, so `lupin_db_test.solution_snapshots` stayed dirty → the VALIDITY guard refused the run. Added `connection.commit()`; unit control asserts commit was called → **RED on revert, proven** (line 163 AssertionError).

### 3. Infra — `lupin_db_v1baseline` did not exist (design Constraint A, deferred "not made tonight")
Provisioned by cloning `lupin_db_test` (schema + 6 users, faithful measurement DB), then `TRUNCATE solution_snapshots` → clean start. Confirmed absent → present via `pg_stat_activity`.

### 4. Infra — :7997 wiring to the isolated DB + auth + embeddings
The pinned v1 server (bare-host uvicorn at `b0735467`, worktree `lupin-v1-baseline-b0735467`) was re-launched pointed at `lupin_db_v1baseline`, with three env overrides — all orthogonal to routing/timing, so the measured baseline is unaffected:
- `DB_NAME=lupin_db_v1baseline` — isolate the v1 arm's writes (was defaulting to `lupin_db_dev`, the LIVE dev DB — a latent isolation hazard).
- `AUTH_MODE=mock` — the v1 arm authenticates with `mock_token_email_<email>`; the config block is `jwt`, which rejected it.
- `LUPIN_MODEL_SERVER_URL=http://localhost:7998` + `LUPIN_MODEL_SERVER_API_KEY_NAME=model-server-api` (key copied into the worktree `conf/keys`) — the bare host has no in-process GPU embedding engine, so routing embeddings must use the model-server HTTP path — the SAME backend the v2 arm (:8000 container) uses, keeping the paired instrument fair.

**This is precisely the design doc's caveat** (`2026.08.15-v1-baseline-standalone-server-design.md`): "a container from the pinned sha (the boring, faithful option), not a bare host-side uvicorn." The bare host hit DB, auth, and embedding gaps in sequence.

Verified end-to-end: `POST /api/push` → 200, job queued, user resolved, zero embedding-key errors; `/api/code-identity` → `b0735467`; `pg_stat_activity` shows :7997 on `lupin_db_v1baseline`.

### 5. Guard rider (Tiberius) — already satisfied in code
`assert_test_db` allow-list widening (`{lupin_db_test, lupin_db_v1baseline}`, exact-name match) + the RED refusal tests (`test_assert_test_db_refuses_dev_target`, prefix-smuggle, empty, non-string, query/fragment smuggle) already exist and pass — **9/9 green**. Widen, did not weaken.

### Sibling bug (NOT fixed — not in the paired path)
`v1_eval_arm.truncate_snapshots` has the same raw-string + no-commit shape, but `run_v1_baseline` never calls it (the v1 arm is pure HTTP to :7997). Left as a follow-up; to be fixed with a RED control AFTER this run clears (editing it mid-run would poison the run).

---

## Isolation topology (final, fair)

| Arm | Server | Writes to | Auth | Embeddings |
|---|---|---|---|---|
| v1 (baseline `b0735467`) | :7997 bare-host worktree | `lupin_db_v1baseline.solution_snapshots` | mock | model-server :7998 (HTTP) |
| v2 (main tree) | :8000 container | `lupin_db_test.solution_snapshots` | JWT (login) | model-server :7998 (HTTP) |

Stores distinct + clean at start (0/0). VALIDITY guard passed. Same embedding backend both arms.

---

## Run bookkeeping

- Submit: `POST /api/test-suite/submit`, `pytest_args = "-m paired_eval_live src/tests/integration/test_v2_paired_live.py -v"`, sample `n_per_command=60`, seed 1024, corpus `simple`.
- Job: `ts-cc25ab70` (monopolize). A prior doomed run (`ts-0831b664`, hung after I restarted :7997 underneath it) was killed with Rick's approval.
- Commits (scoped by pathspec, no push — Rick's alone): `291fb3fa`, `9b90ae5d`. Verdict/results commit to follow when the number lands.

---

## Follow-ups

- Fill the Verdict section on landing; commit this doc scoped.
- Fix `v1_eval_arm.truncate_snapshots` (raw-string + commit) with a RED control after the run clears.
- The design's container-from-pinned-sha option remains the durable path for future v1 baseline runs (the bare-host provisioning above is reproducible via `launch-v1-7997.sh` but carries the DB/auth/embedding wiring by hand).
