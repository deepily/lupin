# v0.2.0 pgvector Migration — Swap-Chain Execution Log + Flag-Flip Readiness Report

**Date**: 2026-07-07 · **Executor**: Mr. Radio 🦉 (session `17e81460`) · **Authority**: Rick GO (voice, ~10:00 EDT) on operator gate `d93a1edb` (P1)
**Rick's words**: "Let's go ahead and finish the migration from LanceDB to Postgresql. You are the sole instance of Claude code running so the coast is clear." + "This is also explicit permission to take any and all actions necessary as a manager to bring this endeavor to a successful conclusion."
**Parent design**: `2026.06.30-lancedb-to-postgres-pgvector-migration-design.md` · **Backfill utility**: `2026.07.02-lane-d-p4-offline-backfill-utility.md`
**Predecessors**: Lane A `b3b86530` (models+alembic) · Lane B `86467cbd` (repos+flag) · Lane C `e8247902` (call-site swap) · Lane D `ceffb386`/`8841c3a5` (backfill utility) · venv gate `7d50a03a` (pgvector→src/cosa/.venv)

---

## 1. Chain execution record (all times EDT)

| # | Step | Result | Receipt |
|---|------|--------|---------|
| 1 | Quiesce check | ✅ pools 0/0 both servers, consumers alive | `/api/queue/pool-status` 10:02 |
| 2 | Compose tag edit | ✅ `lupin-rest-dev` (L128) **AND** `lupin-rest-test` (L210) → `lupin:1.1.1-pgvector-candidate` (test-server parity = Rick's live ask 10:06) | docker-compose.yml |
| 3 | Recreate dev+test | ✅ both on image `a87d3219`; health 200 both; pgvector imports in both containers. **Bonus fix**: dev had been running stale `1.1.0-prefonts` by image ID (restart-vs-recreate drift) — recreate cured it | `docker ps` 10:04 |
| 4 | Durable-PG verify | ✅ `vector 0.8.4` · alembic head `d0e1f2a3b4c5` · 25 tables incl. all 8 vector-store tables · 4 HNSW dot indexes · 3 targets empty | psql lupin_db_dev |
| 5 | FULL in-container dry-run | ✅ zero writes; source counts: input_and_output **202,004** / solution_snapshots 35 / canonical_synonyms 57. Keystone grew +6,428 since 07-02 (LanceDB still live write path) | dry-run stdout 10:07 |
| 6 | LIVE truncate-then-load | ✅ **input_and_output 202,012 + solution_snapshots 35 + canonical_synonyms 57**, single commit, exit 0, ~8 min (10:08–10:16) | backfill stdout: `source=202012 … inserted=202012 … committed.` |
| 7 | Equivalence spot-check | ✅ **PASS** (after HNSW REINDEX + ef_search=1000 — see §4): count parity (straggler delta only), 8/8 samples × top-5 rank-scores agree w/ identical ids, canonical_synonyms mapping IDENTICAL, solution_snapshots id_hash set IDENTICAL | spot-check stdout 10:35 |

## 2. Scope decisions made during the run

- **Test server folded into the swap** (Rick voice ~10:06): both `:7999` and `:8000` now ride the candidate image. Backfill data targets `lupin_db_dev` ONLY — `lupin_db_test` stays fixture-managed (`clean_test_db` truncates per run).
- **GCP leg is OUT of today's chain** (Rick voice ~10:15): filed task `c845346a` (P2, blocked on Rick's explicit GO — no remote work, no SWE spin-up until then). Scope: terraform follow-through on updated creds + pgvector propagation to the GCP environment.

## 3. Flag-flip readiness report

**VERDICT: READY, with ONE decision owed (HNSW index strategy, §4) before or at the flip.**

- Data: 202,012 + 35 + 57 rows live in `lupin_db_dev`, proven byte-faithful (exact-scan Postgres == LanceDB **exactly** on every probe; the apparent +1.0 score offset is LanceDB's `_distance = 1 − dot` reporting convention — true dot = `1 − _distance`, empirically confirmed across magnitudes 12→503).
- Runtime: both servers on the pgvector image; `vector 0.8.4`; alembic at head; venv gate closed (`7d50a03a`).
- Flag: `vector store backend = lancedb` (lupin-app.ini:541) — flip remains Rick's gate.

## 4. FINDING — HNSW recall pathology on the duplicate-ridden keystone (+ the fix data)

**The keystone is 97.2% duplicate vectors**: 202,012 rows, only **5,728 distinct embeddings** — notification-pipeline texts ("NOTIFICATION: progress/low" etc.) dominate. This is the same data-hygiene class as the arbiter flood-row purge sitting on Rick's open card.

**Consequences measured (query = real user-ish text, top-5 dot):**

| Path | Recall vs exact | Latency (median) |
|---|---|---|
| LanceDB today (flat scan) | perfect (is the baseline) | **~1,293 ms** |
| PG exact scan (`enable_indexscan=off`) | **perfect, always** | **~481 ms (2.7× faster than legacy)** |
| PG HNSW ef_search=40 (default) | **BROKEN** on 2 probed queries (trapped in duplicate plateaus) | ~0.5 ms |
| PG HNSW ef_search=400 | still broken on probed hole | ~few ms |
| PG HNSW ef_search=1000 (pgvector max) | 8/8 samples pass (no guarantee) | ~408 ms |

Timeline: pre-REINDEX the index (built empty, fed 202k incremental inserts) failed even easy queries; post-`REINDEX` (37 s, all 3 HNSW indexes) 7/8 passed at default ef; the last hole needed ef=1000. **REINDEX-after-backfill is now a required chain step.**

**Options for the flip:**
- **(A) RECOMMENDED — exact scan for `input_and_output` knn**: guaranteed LanceDB parity, 2.7× faster than the legacy UX it replaces. Implement = drop the `idx_input_and_output_input_embedding_hnsw` index (or `SET LOCAL enable_indexscan=off` in `dot_topk`); revisit HNSW after a notification-spam purge/dedup collapses the 97.2% duplicate mass (5,728 distinct vectors would index beautifully).
- **(B) ef_search=1000 in `dot_topk`**: all tested queries pass at ~408 ms (barely faster than exact), but recall is probabilistic on this data — a silent-wrong-neighbor risk the L1/L2/L4 match tiers would inherit.
- **(C) leave default**: NOT viable (broken recall on real queries).

The 35-row `solution_snapshots` HNSW indexes are unaffected (tiny table; index irrelevant at that size).

### Straggler-delta caveat (standing)

### Straggler-delta caveat (standing)
LanceDB remains the live write path until the INI flip (`vector store backend = lancedb`, lupin-app.ini:541). Rows written between today's backfill and the flip moment are NOT in Postgres (observed growth: +55 rows in the ~30 min of the chain). **Flip-moment runbook**: quiesce → re-run `--apply --truncate` (idempotent, ~8 min) → **REINDEX the input_and_output HNSW index** (unless option A dropped it) → flip INI → verify. The utility re-run costs the same as today's run.

### Rollback
- Image: compose tags back to `lupin:1.1.0` + recreate (both services).
- Data: Postgres tables are additive-only today; LanceDB store untouched (read-only source). No rollback action needed on data while flag stays `lancedb`.

## 5. CUTOVER EXECUTED (Rick flip-day ruling, ~10:40 EDT ask: "Exact scan" + "Flip now")

| # | Step | Result | Receipt |
|---|------|--------|---------|
| 8 | Straggler re-backfill | ✅ truncate-then-load 202,081 + 35 + 57, committed, exit 0 | backfill-straggler-run.log |
| 9 | Exact-scan ruling implemented | ✅ migration `e1f2a3b4c5d6` (DROP INDEX, reversible downgrade) + model index decl removed + `HNSW_DOT_INDEXES` 4→3 + test contract amended + NEW `test_drop_hnsw_migration.py` (100% L/B/F on the migration) | 24 tests green; db dir 126 passed/1 skipped |
| 10 | INI flip | ✅ `vector store backend = postgres` (`[Lupin: Baseline]` → dev AND test) | lupin-app.ini:543 |
| 11 | Bounce (compose restart, quiesce re-verified 0/0) | ✅ both healthy ~10 s; startup auto-migrate applied `e1f2a3b4c5d6` on BOTH DBs (dev + test); index gone both; 202,081 rows | psql + /health |
| 12 | Live verify | ✅ in-container `get_vector_store_backend() == postgres`; **calculator live-pipeline smoke PASS** (real question through the flipped :7999); **WS smoke ALL PASS** (50) | pytest + smoke logs |
| 13 | Integration gate (:8000, monopolize) | ⏳ submitted `ts-c94c514d`, queue position 1 | /api/test-suite/submit |

**Rollback**: INI key back to `lancedb` + bounce (LanceDB store untouched, still receiving nothing — reads now come from PG; note: post-flip NEW writes land in Postgres only, so a rollback after significant soak time would need a reverse-delta plan).

**Post-flip notes**:
- Boot logs still print legacy "Connecting to LanceDB…" banner lines from `LanceDBSolutionManager` — cosmetic (class retains its name + banner; ground truth verified via `is_postgres_backend()` + live smoke). Candidate cleanup during soak.
- Soak-pass signal (design §7/O2): equivalence harness green across the soak window; keystone knn now serves EXACT scan (~481 ms, 2.7× faster than legacy; guaranteed parity).
- LanceDB teardown (P5 tail: dep removal, INI key removal, GCS util_gcs) stays POST-SOAK — do not rush it.

## 90. Session notes

- `:8000` was verified idle before its recreate (pool 0/0, nothing scheduled) — idle-bounce standing authority + Rick's direct parity ask.
- Monitor row-count probe reads 0 during the load by design: `_run` commits once at the end (all-or-nothing transaction).
