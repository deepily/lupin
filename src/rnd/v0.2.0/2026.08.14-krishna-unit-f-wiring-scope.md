# Unit F wiring — scope reconciliation (Krishna 🦚, 2026-08-14)

**For**: Cheech 🌿 (manager). **Store row**: `43fca908`.
**Verdict**: the harness on disk is a **v2-only cold→warm** instrument; the four review findings you
handed me require a **v1-vs-v2 paired go/no-go** instrument. Wiring the v2 endpoint is done by
construction; the go/no-go harness is a build, and its load-bearing piece (a v1 arm) has no endpoint.

---

## What is already true (verified against the live merged tree)

- **v2 seams match `/api/v2/ask` exactly.** Confirmed against `routers/v2_ask.py` + `flow.py`:
  `path` (replay|agent|receptionist|needs_input), `route_reason` literals
  (`replay_error`/`router_error`/`extract_error`/`agent_error`/`args_incomplete`), `command`,
  `similarity`, `timings_ms["t_first_useful"]`, `trace_id` — all present, all spelled as my constants
  assume. My `ROUTE_REPLAY_ERROR="replay_error"` matches the code (plan prose said `replay_failed`; code wins).
- **`v2 flow enabled = True`** in `[Lupin: Development]` (:7999), `False` in Baseline. Endpoint answers.
- ⇒ The **single-arm v2 cold/warm harness runs against the live endpoint with zero code changes.** It
  produces real v2 numbers: cache-hit rate, cold→warm latency delta, routing accuracy, would-be-wrong (R-C2).

## What the four findings require that does NOT exist

The fixed plan §1.1–§1.5 defines the deliverable as a **decision table** (INVEST/STOP/SPLIT), not a v2 metric dump. That needs:

1. **A v1 arm.** §1.1: "v2 vs v1 over the same corpus in the same session." **v1 has no §8-shaped
   endpoint** — its flow is the queue (`/api/push` → poll `/api/get-queue/done`), which returns none of
   `t_first_useful` / routing `command` / a replay signal. Extracting those from v1 is a **separate
   instrument that does not exist**. This is the load-bearing gap.
2. **Paired median delta** gate + **p95 informational**. My harness has an *unpaired cold/warm* delta only.
3. **The go/no-go table**: veto floors, an **OVERALL** row, a **PENDING HUMAN** replay-correctness row
   with an AI command-match pre-filter that can only *fail*. Absent.
4. **Per-arm table isolation** (§6.4). Depends on a **`v2 snapshot table` INI key that is not defined yet**,
   plus a matching v1-table reset. Neither wired.
5. Separate **AST instruments**: flow size ≤200 stmts (§1.3), mechanism-count = 1. Absent.

No `v1_arm`/`paired`/`OVERALL`/`PENDING HUMAN`/`go_no_go` code exists in `src/scripts` or `src/tests`.

## Decision — Cheech, 2026-08-14 14:45 (thread `4fb7f475`)

**Do both, in this order** (A is a down-payment, B is the true DoD):

1. **v2-only run NOW** — cache-hit rate + v2's own latency distribution. **Labelled in the output as
   NOT the go/no-go table** (no v1 baseline ⇒ no INVEST/STOP verdict). Venue: `:8000` scheduled,
   post-midnight off-peak, via `POST /api/test-suite/submit` — coordinate behind Tiffany's + sam's queued work.
2. **Build the paired v1-vs-v2 harness** — the real verdict instrument (§1.1–§1.5).

### Confirmed: there is no v1 synchronous ask path
v1's entry is `POST /api/push` (`queues.py:237`) → enqueue to the CJ Flow todo queue → answered by
polling `get-queue/done`. **The two arms have different transports, not just different control logic.**

### 🔴 The load-bearing constraint for the paired harness — `t_first_useful` parity
Define `t_first_useful` **identically in both arms, and state what is excluded.** If **queue dwell**
lands in v1's number, v2 wins merely for being **synchronous** — that measures the *transport*, not the
redesign, and flatters v2 in the direction nobody audits (same failure class as Tiberius's baseline
contamination). ⇒ Exclude v1 queue-dwell from `t_first_useful`; measure both arms from the same anchor
(request received / work dispatched) to the same mark (first useful response), and print the exclusion
in the report.

### Still open (folded into B's build, not blocking A)
- The AST **size (≤200 stmts)** + **mechanism-count (=1)** instruments — ownership to confirm with the manager.
- The **`v2 snapshot table`** INI key + a matching v1-table reset for per-arm isolation — to be added under B.

---

## Down-payment (A) — code done, RUN blocked on `:8000` config

### Landed + verified on :7999 (my unit-F files, additive)
- `src/scripts/v2_eval.py` — report now prints `NOT_GONOGO_BANNER` at the top of every report
  (v2-only ≠ go/no-go). Unit suite **34 passed, 100% lines + branches** (288 stmts / 78 br).
- `src/tests/integration/test_v2_eval_live.py` — the sanctioned bridge: `POST /api/test-suite/submit`
  runs only registered `integration`/`e2e` suites, so this `@pytest.mark.integration` wrapper drives
  `v2_eval.main()` against the live server and asserts a real, integrity-checked, non-silent-empty run.
  Compiles + collects clean.

### 🔴 The run is blocked — v2 flow is OFF on :8000
`[Lupin: Testing]` (the `:8000` block) does **not** override `v2 flow enabled`, so it inherits
Baseline's `False`. **`/api/v2/ask` returns 503 on `:8000` today** → every request is a non-200 →
the harness's own integrity guard (http-all-ok) raises. Preconditions for the scheduled run:

| # | Precondition | Where |
|---|---|---|
| 1 | `v2 flow enabled = True` | add to `[Lupin: Testing]` (else 503) |
| 2 | `v2 trace dir = <path>` | add to `[Lupin: Testing]` (else trace-parity has nothing to check → guard raises) |
| 3 | Container **recreated** (`docker compose up -d --force-recreate`), not just restarted | config resolves at CREATE |
| 4 | Coordinate behind Tiffany's + sam's queued `:8000` work | `list-pending` first; schedule post-midnight |
| 5 | Submit `test_types=integration`, `pytest_args="-k v2_eval_two_pass_live"`, `env={LUPIN_TEST_V2_EVAL_LIMIT:"60"}`, `scheduled_at` post-midnight | via `POST /api/test-suite/submit` |

Preconditions 1–3 edit a shared config file and recreate the shared test container while peers have
`:8000` work queued — **manager/ops call, not a unilateral one.**
