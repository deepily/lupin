# Q&A Card Voice Demo — Cue Sheet

**For**: Rick, driving by voice from the Q&A card's free-form input, Agent Mode = **System (Auto-Route)**
**Written**: 2026-08-04 11:10 EDT by Mr. Radio 🦉 · **Revised 11:33 EDT after the bounce landed**
**Server state measured**: `:7999`, **boot #30, started 11:22:14 EDT**

Companion to the [rehearsal card](2026.08.04-thursday-demo-rehearsal-card.md) — that one covers the podcast
card. This one covers the **voice path with the router doing the picking**, which behaves differently in
two places.

> # ✅ NEW WORKING LINE — measured 11:51 EDT
>
> > ## "make me a podcast"
> > ### …then say **KISS** when it asks which document.
>
> **Two turns.** The old one-turn line is dead. This one genuinely opens your file — the job's final
> arguments carry the real path to your KISS explainer, not a topic word.
>
> **THE RULE HAS FLIPPED. Naming the topic up front is what breaks it.** The topic word fills the document
> slot, and the file matcher only runs when that slot is *empty*.
>
> Four phrasings measured identical: *"make me a podcast"* · *"make me a podcast please"* · *"start a
> podcast for me"* · *"I want a podcast"*.
>
> **Boundary — the same shape that burned us at 11:36**: proven as far as *the correct file path lands in
> the job's arguments*. The render was declined, so **nobody has yet watched this line produce finished
> audio from your file.** That check is queued.
>
> <details>
> <summary>Why the old line died (11:36) — the mechanism, worth one read</summary>
>
> `"make me a podcast on KISS"` routed correctly and then crashed:
> `FileNotFoundError: Research document not found: KISS`.
>
> The language model pre-filled the document argument with the literal string `"KISS"`. The file matcher
> only runs for arguments still *missing*, so it never ran, and a topic string reached the job where a file
> path belongs. Confirmed four ways — Tiffany's live run `pg-e1e5e572`, Clayton's log tail, Rio's
> independent read, and source (`expeditor.py:301`, `agentic_job_factory.py:167`, `job.py:222-223`) — then
> live, when the run was carried through the confirm and hit the crash.
>
> **A hazard that was reported and then refuted**: a claim circulated that the "which document?" prompt
> would auto-submit a stale unrelated prediction after 5 seconds. **False.** That prompt is a single
> question; auto-submit and prefill both apply only to multi-question batches, and nothing prefills it.
> The observed submission was a test harness answering itself. Retracted by its author.
> </details>

> ⚠️ **This document was also rewritten after the bounce.** Its first version described the pre-bounce
> server and told you to pick the option whose description sounds *wrong*. **That advice is obsolete and
> inverted.** The label guidance below is correct for boot #30.

---

## Server state — everything is live

`:7999` was bounced at **11:22:14 EDT** (boot #30) and now serves all four fixes. Verified by reading the
labels out of the **running container**, not the working tree:

| Fix | Live on boot #30 | Committed |
|---|---|---|
| Confirm-menu labels un-inverted | ✅ | `572db38b` |
| Auto-resolve — collapses the "which document?" turn | ✅ | `572db38b` |
| Podcast orchestrator changes | ✅ | `572db38b` |
| Dry-run default unchecked | ✅ | `568068bd` |

**The working tree is clean** — nothing demo-critical is left uncommitted, so a checkout can no longer
silently revert the Dry Run flip. That gap was real until 11:25 and Rio caught it.

Receipt: both-roots **21,562 passed / 0 failed**. (An earlier draft of this document cited "473 targeted
tests" — that was a *subset*, not the merge receipt. Corrected.)

---

## The cue sheet — boot #30. Use this one.

**Setup**: Q&A card · Agent Mode left at **System (Auto-Route)** · click 🎤 · your own login · tab stays open.

| # | You do | It does | Watch for |
|---|---|---|---|
| 1 | Say **"make me a podcast"** — nothing after it | Routes to the file-reading agent (`podcast generator`), document slot left empty | Do **not** name KISS yet. That's what broke the old line |
| 2 | Confirm menu appears | *"I think you want …? Is that right?"* | Pick **"Doc-to-Pod (create a podcast from an existing document)"** — the label trap is gone; the option that reads correctly now *is* correct |
| 3 | It asks **"which document?"** | The file matcher is now running — this prompt appearing is the demo *working* | Nothing is pre-filled here; the box is yours |
| 4 | Say **KISS** | Fuzzy-matches to your `2026.07.25` explainer | Final arguments carry the real file path |
| 5 | Script → approval gate → audio renders | ~7 minutes to playable | Click **Play Here** |

**Two spoken turns**, and the "which document?" step is now a *feature of the working path*, not a defect.
That's the reverse of what this card said an hour ago.

> **Take no more than a few seconds on step 4.** Not because anything auto-submits — that was checked and
> refuted — but because it's a live demo and a long silence reads as a hang.

---

## Two things the podcast-card rehearsal card gets differently for this path

1. **The Dry Run checkbox is not your problem here.** It lives on the podcast card. On the Q&A voice path
   the router builds the job's arguments and `dry_run` is simply absent, which parses to **false** — you
   get a real render by default. (Verified in `agentic_job_factory.py:303` + `job.py:64`.)
2. **You are not setting the mode dropdown.** That's your own ruling — the router must land it on its own.
   The escape hatch exists (set Agent Mode → *Podcast Generator* and the LLM router is bypassed entirely,
   deterministically, no confirm) if step 1 ever misses on stage.

---

## The line, and the rule behind it — ⛔ REFUTED 11:36 EDT

> ## ~~"make me a podcast on KISS"~~ — DOES NOT WORK

**The rule below governs ROUTING only, and routing was never the failing half.** All five lines route
correctly, then hand the literal topic string to the job, which fails with `FileNotFoundError`.

<details>
<summary>The routing rule, retained — still true, just not sufficient</summary>

**Say "about" or "on" — and NAME KISS right after it.** Both halves, or it falls to the agent that never
opens your file. Don't say *from*, don't say *based on*, don't say *my document* without naming KISS.

Four backups, each 3/3 on routing — **all now DEAD**, since each names the topic up front:

- "podcast about my KISS explainer"
- "make a podcast about the KISS protocol"
- "build me a podcast on the KISS protocol"
- "create a podcast about how KISS saved me tokens"

</details>

### ✅ The live shortlist — all four verified end-to-end to a resolved file argument

**PRIMARY**: "make me a podcast"
**BACKUPS**: "start a podcast for me" · "I want a podcast" · "make me a podcast please"

Every one is the same two-turn shape: **bare request, then KISS on turn 2.**

### What went wrong with the evidence, stated plainly

The **"resolves to your KISS file 3/3"** claim was measured by feeding the utterance **straight into the
file matcher**. The matcher works. It is simply never called in the real flow, because the argument it
would resolve has already been filled with a topic string.

**Two halves proven separately, and the seam is exactly where it broke.** This card's own earlier version
named that seam as unproven — and it was right to.

**A route-only pass is no longer accepted as evidence for this demo.** Any replacement line must be
measured end-to-end: routes correctly, *and* reaches the document, *and* produces content that could only
have come from your file.

---

## Risks — one of them has materially changed

**1. Script-generation rate limit** — ~3.5 min cooldown, fails loudly. Pause and retry, don't panic.

**2. Spanish translation — CHANGED, and the rehearsal card's advice on this is now out of date.**

> ⚠️ **A separate "Spanish sounds flat" claim was raised at 11:48 and is UNPROVEN — do not plan around it.**
> The report said the Spanish script loses all 91 pause/emphasis cues. Its author has since confirmed that
> figure was **inferred from a count of a metadata list, not measured against the translated text** — and
> the list can read zero while the markers sit in the text untouched. The translated text wasn't saved, so
> the original runs cannot answer it. A clean run is being generated specifically to grep for them.
> **Until that lands, treat "Spanish is flat" as an open question, not a finding.**
>
> Also measured across 4 runs: **zero reproductions of the original Spanish bug.** The two failures seen
> were subprocess crashes with a different signature, likely quota contention. *Could not reproduce* is
> not *fixed*.

The rehearsal card says Spanish "fails silently: you get your English script labelled as Spanish." **That
is no longer true on the code you are running.** Measured by Krishna at 11:30 EDT via a command-line probe,
with the failure *forced* so the detector was proven able to report red before any green run was trusted:

- Console prints `Translation error: … parsed to zero segments`
- Title comes back **"(Mexican Spanish - Translation Failed)"**
- A notification fires

**A Spanish failure now announces itself.** Your Thursday call changes shape accordingly: the risk is a
*visible* failure on stage, not a *silent wrong result* you'd never catch. Whether Spanish stays in is
still your call — but it's a different call than the rehearsal card framed.

*(Whether it fails at all, and how often, is being measured now across 5 real runs.)*

**3. The one seam still unproven** — your sentence → your document → playable audio, in one run, from your
account, with a live browser session. The halves are proven separately; the seam is not. **Only your own
rehearsal closes it**, which is why your run matters more than any of ours.

---

## Revision history

| When | What changed |
|---|---|
| 11:10 | First version — described the **pre-bounce** server; told you to pick the wrong-sounding label |
| 11:33 | Rewritten for **boot #30**: label advice inverted, "which document?" step removed, three turns → one, test receipt corrected from a subset to the both-roots number, Spanish risk updated from *silent* to *visible* |
