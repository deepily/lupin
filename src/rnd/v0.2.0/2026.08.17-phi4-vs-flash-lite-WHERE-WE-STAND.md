# Phi-4-14B vs Gemini 3.1 Flash-Lite — where we stand

**As of**: 2026-08-17, 16:25 EDT · **Crew**: Clayton 😎 implementer · Tiffany 💍 reviewer ·
Sam 🎙️ tester · Mr. Radio 🦉 manager

---

## The verdict in one line

**The DM tutor's guard refuses Flash-Lite's rewrites nearly twice as often as Phi-4's, and
Flash-Lite answers in about half the time.** Two winners, two different questions.

| | phi_4 | flash_lite |
|---|---|---|
| refused for fabrication | **18.8%** (73 of 388) | 33.2% (130 of 392) |
| median latency per row | 3.92 s | **2.06 s** |
| p99 latency | 5.38 s | 23.49 s |
| discordant pairs | **123** (floor: 10) | |
| McNemar exact | **p = 2.7e-07**, Wilson 0.647–0.802 | |

**The latency tiebreaker never fired.** It breaks a *tie*; p = 2.7e-07 is not one.

## The caveat that limits the claim

> ⚠️ **RETIRED 2026-08-18 (row ddf7581e, acceptance item 4).** The line below used to read
> *"76% of Flash-Lite's blocks are capitalised-token novelty alone."* That phrasing implies
> **capitalisation caused the blocks**, which is false — the novelty check has been
> case-insensitive since 13 Aug, and 0 of 146 name-only blocks turned on capitalisation.
> What the number actually described is the share of blocks that were **name-class only**,
> and most of those names were **ordinary English words** (Update, Verify, Ensure) sitting at
> the start of a sentence. Superseded text kept below, corrected.

**76% of Flash-Lite's blocks were name-class only, and most of those tokens were ordinary
English words rather than invented entities.** Drop that one detector class and re-run the
identical test: **p goes from 2.7e-07 to 0.42**. On numbers, hex ids and paths the two arms
are indistinguishable.

**RESOLVED 2026-08-18 — the caveat no longer limits the claim.** V3-strict removes the
ordinary-English tokens from the name class at the source, and the comparison was re-run
against it (run `2026.08.18-mrradio-host-400`, snapshot `6e7979c5`, seed `20260817`, 400
paired bodies, denominator 392 per arm):

| Arm | V0 blocks | V3-strict blocks |
|---|---|---|
| phi_4 | 67 (17.09%) | **30 (7.65%)** |
| flash_lite | 130 (33.16%) | **54 (13.78%)** |

McNemar exact: V0 `p = 1.08e-08` (b=93, c=30); V3-strict `p = 0.0012` (b=38, c=14). Both clear
the pre-registered discordant floor of 10. **The verdict does NOT change** — the guard still
flags Flash-Lite's output significantly more often once the ordinary-English class is gone.
That is a real result, not an absence of one: the earlier `p = 0.42` came from deleting the
whole name class, while V3-strict removes only the part of it that was never about invented
facts. See `2026.08.18-phi4-vs-flash-lite-host-rerun.md` and
`2026.08.18-v3-strict-fabrication-guard.md`.

So the honest claim is: **this guard flags Flash-Lite's output more often.** It is *not*
established that Flash-Lite invents more facts. The result stands **operationally** — that guard
is what ships, and a blocked rewrite never reaches anyone.

Found by Tiffany, by re-deriving every number from the payload and then re-running the test with
one class removed. Nobody asked for the second part.

## What the numbers rest on

- **400 paired bodies**, seeded draw (seed `20260817`), one frozen corpus snapshot
- **Pairing enforced in code, not assumed** — three refusals: matching snapshot checksum,
  matching frozen-index list, per-row body text equality. All three driven by hand and proven to
  raise, **and** a well-formed pair still joins (the control that matters — a guard rejecting
  everything would pass all three red tests)
- **Arm identity proven by name**: Vertex endpoint marker, resolved model id read back as
  `gemini-3.1-flash-lite`, `vertexai=True` + project logged, no API key read
- **Statistics package**: 200 tests, 100% lines and branches
- Latency is a **deployment** comparison, not a model-speed claim — phi_4 is a LAN hop,
  flash_lite crosses the public internet, and both ship exactly as measured

## Your four rulings, all executed

1. **Denominator: NARROW** — `fabrication_blocked / (blocked + rewritten)`
2. **Discordant floor: 10**
3. **Corpus scope: the narrower fleet-to-fleet title** (corpus is 100% agent-to-agent, zero human
   traffic)
4. **Latency is a tiebreaker** — with your two conditions: stated as a deployment comparison, and
   p90 **and** p99 reported per arm

All four landed at 14:55 as real answers, not timeout defaults.

## What is left

| Item | Owner | Note |
|---|---|---|
| **Blind hand-labelled sample** | **needs a person** | The only thing that converts "this guard flags it more" into "this model invents more". Cannot be done by a seat. |
| Merge the study lane | done | 30 commits merged; all shas ancestry-verified on the running branch |
| Push | **yours alone** | 18 commits went up at 15:09; the study lane is unpushed |

Nothing else on this study is blocked on anyone.

---

## Found along the way, not in the study's scope

**Eight guards that could not fire.** Every one was correct-looking code with no test that ever
made it raise:

1. Freeze guard aimed at `/var/lupin` — a container path that never matches on the host
2. A test that misstated its own input to go green; its comment admitted it
3. `client.model_name` asserted against the descriptor we passed in — input compared to itself
4. The reachability census excluded itself inside `.claude/worktrees` — clean exactly where new
   work is written
5. `\bcredentials\b` never matched `application_default_credentials.json` — an underscore is a
   word character
6. The v1 baseline store's clean primitive was a **silent no-op** — 69 stale rows would have
   contaminated this very run and produced a plausible median-Δ
7. A content check whose fixture carried a PEM header, so the one shape that still blocked was
   the one being looked at
8. A review condition written against a classifier that was never built

**Two live defects fixed today, both proven on the running tree:**
- The **doc viewer was serving service-account keys** (`service-account.json`, `sa_key.json`,
  `token.json`, `keyfile.json` and more). Now blocked by a *content* detector keyed on
  `type: service_account` / `private_key` — names alone had already failed twice.
- The **context monitor was blind to live seats** whose bridge file was over 12 hours old — two
  seats alive 15 hours were invisible to the very sensor meant to catch them.
