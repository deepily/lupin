# Final `:8000` gate — receipts

**Row**: `f7b57d88` (epic: brain-integration v2) · **Ran**: 2026-08-25, 10:15–11:38 EDT
**Runner**: Mr. Radio 🦉, solo, on Rick's direct instruction — *"run it yourself, no workers, no assistance, solo. We are running as a skeleton crew this morning. Do not work your board. Focus exclusively on this test set. Full stop."*

---

## Verdict: ✅ GREEN

**Gate sha `7c4964dd`** (branch head at pin time). **362 commits** ride this gate since the last gated head `888754f1`.
Verified ancestors of the pin: `888754f1`, `9b49f04d` (integration→wip merge), `8657cfa9`.

| tier | tests | fail | err | skip | wall | verdict |
|---|---|---|---|---|---|---|
| E2E | 692 | 9 | 8 | 2 | 2505s (41.8 min) | **0 regressions** vs sanctioned baseline |
| Integration | 448 | 1 | 0 | 32 | 2559s (42.6 min) | the 1 failure is self-declared **instrument** |

Artifacts:
- `io/test-suite/artifacts/e2e-junit-20260825-141338.xml`
- `io/test-suite/artifacts/integration-junit-20260825-145532.xml`

### E2E diff, both baselines

| baseline | regressions | pre-existing | fixed | true exit |
|---|---|---|---|---|
| `e2e-junit-20260822-154148.xml` — **the row's sanctioned baseline** | **0** | 16 | 0 | `0` |
| `e2e-junit-20260824-234354.xml` — last night, tighter comparison | 1 | 15 | 0 | `1` |

### The one integration failure is not a product defect

`test_lane2_podcast_e2e_live::test_lane2_voice_driven_podcast_e2e`. The harness's **own** message:

> Lane-2 podcast E2E is INCONCLUSIVE (exit 2) — the harness could not observe some stages, so this is an INSTRUMENT result and not evidence of a product defect.

Its exit contract (row `1cd30181`): `0` = every stage observed and green · `1` = a real red · `2` = unobserved. Same lane-2 red logged 2026-08-24.

---

## The silent-skip hazard did not bite — checked by name, not off the summary line

Five integration modules are known to be able to vanish whole on a module-scoped fixture failure. All five **executed**:

| module | ran | skipped |
|---|---|---|
| `swe_team_pipeline` | 7 | 0 |
| `task_edit_parity_e2e` | 17 | 0 |
| `answers_owed_authlane_dv1` | 4 | 0 |
| `notify_user_sync_integration` | 1 | 0 |
| `task_get_live_roundtrip` | 2 | 1 |

32 skips total across 10 modules, none whole-module-silent. The two E2E skips (`test_commons_activity_filters_and_strip_chrono`) are data-dependent self-skips present identically in **both** baselines.

---

## The single E2E difference is a stale golden, not a break from this build

`test_multiplexer_phase6a_jobs_pane_visual[chromium]`:

| run | result |
|---|---|
| 08-22 | FAIL — `2095 mismatched pixel(s)` in a 960×403 region |
| 08-24 night | **passed** |
| 08-25 (this gate) | FAIL — **`2095 mismatched pixel(s)`** |

**Evidence, not assertion:**

1. The pixel count is **identical to the digit** across runs three days apart. A flaky visual test does not reproduce that. **08-24's pass is the outlier, not today's failure.**
2. Golden PNG `io/test-suite/visual-baselines/test_multiplexer_phase6a_visual/test_multiplexer_phase6a_jobs_pane_visual/multiplexer_phase6a_jobs_pane.png` — **unchanged since Aug 3**.
3. **Zero** commits to `src/lupin_app/static/**` since the 08-24 run — the source did not move.
4. The main checkout's `dist/` was built **2026-08-02** (`hash 0d133fe2891b`) and **19 commits have touched `src/lupin_app/static/js` since**. This gate served a freshly built `59c16a24c0ef`.

⇒ A run served by the stale Aug-2 bundle renders the *old* UI, which matches an Aug-3 golden; a run served by a current bundle does not. That is the failure mode RIG-PROCEDURE §0 warns about — *"the run still looks alive; it is measuring a broken rig."*

⚠️ **Not proven**: the 08-24 container is gone, so what bundle it served cannot be inspected. What **is** proven: the determinism, the unchanged golden, and the unmoved source.

---

## Rig defect found and fixed — host-side, not the branch

`lupin/.claude/worktrees` was `root:root drwxr-xr-x`; the container runs as uid 1001. Verbatim:

```
fatal: could not create leading directories of
'/var/lupin/.claude/worktrees/worktree-preflight-smoke-45054/.git': Permission denied
```

Preflight failed with **1 blocking**. The directory was empty and its parent is `rruiz`-owned, so `rmdir` + `mkdir` fixed it with no sudo; container force-recreated; preflight then **PASSED**.

### This is a sixth entry for RIG-PROCEDURE §0b — and it is not a path, it is a permission

§0b's list (`keys/`, `.terraform`, `cloud-run.env`, `node_modules`, `.venv`) assumes the failure mode is an **absent** git-ignored path. This one was a **present** path with the **wrong owner**, and it produced the same class of result: a blocking red that reads as environment drift and is not. Whoever owns that doc should add the shape, not just the path.

---

## Integration leaves ~227 jobs in `todo` — measured, and it matters to the next gate

The suite enqueues synthetic `Concurrent question N: What is N0?` MathAgent jobs under `concurrent_*@test.com`.

- **Invisible to the submitter's own queue listing** — `/api/get-queue/todo` is user-filtered and this account saw `0` while `/api/busy` reported `216`.
- **Fully visible to `/api/busy`**, so `venue_idle` correctly reported **BUSY for 26 minutes after the suite went terminal**.
- They drain unaided at **~6–8 jobs/min** (227 → 0 in 26 min). Cheap MathAgent work; no meaningful spend.

⇒ **Nobody should read that as a stall, and nobody should recreate the container on top of it.** Directly relevant to row `e6b8fe56`.

---

## Handback complete (§4) — every step verified by true exit code, no pipes

| step | result |
|---|---|
| idle before revert | exit `0`, every lane empty |
| revert recreate | `docker compose -f docker-compose.yml up -d --force-recreate lupin-rest-test` (no gate overlay) |
| mount confirmed | `lupin/src → /var/lupin/src`; served bundle back to main's `0d133fe2891b` |
| preflight | exit `0`, **PASSED** (1 non-blocking warning: no gh auth in container) |
| idle after revert | exit `0` |

**The box is back on main and free.**

---

## Method note — a wrapper's exit status is not the checker's

This bit twice in one run:

```bash
cmd | head                       # $? is head's status
echo "$(basename $B) -> $?"      # $? is basename's status
```

`EXIT=0` was printed beside a **BUSY** verdict, and `TRUE_EXIT=0` beside a **1-regression** diff. Every exit code in this document was re-taken bare. The e2e diff's true statuses are `0` (sanctioned baseline) and `1` (last night's) — the tool flags regressions correctly; the reading was wrong.

---

## Still Rick's, not re-asked

- The `:7999` bounce that makes the 410 tombstones live.
- The push to origin (ruled 2026-08-22: *"We will push when I say that we are ready to push"*).
