# Cascade Stage 3 — Ownership pass: Phi-4 vs Flash-Lite hallucination comparison

**Reviewer**: Rio ⚡ (session `5cba729c`) · **Date**: 2026-08-16
**Target**: `src/rnd/v0.2.0/2026.08.16-phi4-vs-flash-lite-hallucination-comparison.md` at commit **`faf107ee`** (read via `git show`, not the working tree)
**Row**: `c97f1d56` (Stage 3 ownership) · **Convention 6 ACTIVE** (100% lines+branches+functions; a "tests pass" claim with no named coverage-assertion shape is a finding)
**Decomposition**: six sections A–F; deps C→B, D→A, F→A, E independent

## Verdict

**PASS-WITH-FINDINGS — not crew-ready as written.** Four findings, all fixable in the document (no re-architecture). None reopens the section boundaries. The design's own hard gate (Vertex credentials, §5) already blocks any run, so these corrections ride alongside that block rather than adding a new one.

---

## Findings (most-severe first)

### F1 — §2.2 step 1 contradicts §1.2's freeze; the harness would read the LIVE corpus (CONFIRMED)
- **Where**: §2.2 step 1 (line 211): *"Read the replay set from the corpus."* vs §1.2 req 2 (line 99): *"Both arms read the pinned file, never `dm_traffic.jsonl` directly"* + req 4 (line 101): *"the harness should refuse to run against the live path."*
- **Why it matters**: §1.2 is the design's load-bearing precondition — A, D, and F all depend on it. A crew implementing §2.2 literally reads the live append-only log (growing ~5 rows/min), giving the two arms **different populations** — the exact unpaired-comparison defect §1.2 and §3 exist to prevent. Rachel's own decomposition Correction 1 noted this coupling; at HEAD the text is still unreconciled.
- **Fix**: §2.2 step 1 must name the pinned snapshot (§1.2) as the read source and carry the refuse-live-path assertion, not "the corpus."

### F2 — §5 smoke permits a degraded green: "did not fall through to vLLM" has no named assertion shape (CONFIRMED; Rachel flag 1)
- **Where**: §5 smoke row (line 305): *"proves the vendor path resolves — and that it did not silently fall through to vLLM."* §2.2 step 3 calls the **existing** `_apply_dm_tutor` path.
- **Why it matters**: This is the exact failure family I proved live today (`c98bce3f` — a runner silently ran the wrong interpreter and reported a reduced count as a pass). If the new dispatch path has a bug and the `flash_lite` spec key falls back to the default, the harness replays 995 bodies through **Phi-4 again** and publishes it as Flash-Lite's fabrication rate — a green measured in a degraded environment. The plan states the requirement but names **no checkable mechanism** that proves the model that answered was Flash-Lite via Vertex.
- **Fix**: specify the smoke's pass assertion — e.g. assert the resolved client is a genai/Vertex client (not the vLLM client class) and that the response carries a Vertex/genai provenance marker; **fail loud** on a wrong client class rather than counting a pass.

### F3 — Convention 6: no coverage-assertion shape for the new dispatch-path CODE (CONFIRMED)
- **Where**: §5 dispatch row (line 303): *"CODE, not three INI lines… Review it like code."* No test/coverage AC appears anywhere for this code.
- **Why it matters**: §0/§2.1-REVISED establish the dispatch path is new code (the factory has no `google-genai` vendor entry). Under lupin's active 100% mandate, "review it like code" without a named coverage-assertion (100% lines+branches+functions on the new dispatch unit, RED-provable) is a Convention-6 finding.
- **Fix**: add an AC: the dispatch path ships with unit coverage at 100% lines+branches+functions, with a prove-it-can-fail test on the wrong-client-class guard from F2.

### F4 — model id + Vertex-only surface marked SETTLED/DONE on an unconfirmed relay (CONFIRMED; Rachel flag 2)
- **Where**: §2.1a (line 205): *"Model id — SETTLED. Rick supplied it verbatim… `gemini-3.1-flash-lite`."* §5 (line 300): *"✅ DONE."* §0/§2.1-REVISED: *"Vertex is the only surface (Rick's ruling)."*
- **Why it matters**: Rachel flags both the model id and the Vertex-only ruling as an **unconfirmed peer relay**, not a settled fact. A ✅ DONE / SETTLED marker resting on unconfirmed provenance mislabels certainty in the one document a crew treats as authority — and both items gate real work (the id feeds config; the surface ruling removed the API-key fallback, so a wrong ruling strands the arm with no path).
- **Fix**: downgrade both to "relayed, pending direct confirmation from Rick" until confirmed; keep the §5 blocker honest.

---

## Not findings (checked, clean)
- §2.3 fabrication rate `158/(158+798)` = 16.5% matches §1's headline. Consistent.
- E (§2.4) is genuinely independent — it derives ~330/day from its own daily-volume table (lines 235–242), does not borrow A's figure. Rachel's final dependency map (E independent) holds.
- §0/§2.1/§2.1a supersession banners are complete at HEAD — the four staleness findings from Stages 1–2 are closed in the text.
