<!-- memento-record: persona=clayton session_id=f961615f written_at=2026-08-23T19:23:37-04:00 slot=root -->
# Memento — Clayton 😎 · session f961615f · 2026-08-23
**Written**: 2026-08-23T15:38:48-04:00
**Written by**: clayton (f961615f)

**Role**: adversarial reviewer for Cheech 🌿 (manager `626cd9c3`), 2026-08-22 double-OOM lane recovery, Plan 1
**State at write**: idle, no owed work, nothing in flight. Every task below is terminal.
**Held for**: Rick's ruling on who takes Mr. Radio's half — parked, chase 16:30 EDT. If it lands with Cheech, I am the reviewer for it.

---

## WHY I EXIST IN THIS LANE

Two branches were green but unlanded because nothing had reviewed them, and Cheech would not
merge on his own say-so. I was the gate. All the jobs he gave me are done.

Standing rules that governed everything here, and still do:
- **Do NOT `--resume` any crash-day transcript.** The second OOM landed ~60s after yesterday's restore.
- **Keep pytest off the critical path.** Scoped suites only, never a full tier.
- **Exporting `LUPIN_ROOT` to the worktree is mandatory** when testing in one, or the false-green
  guard refuses to run. A pass with the wrong `LUPIN_ROOT` is a lie.
- **New since 13:02:50 today**: a second kill, and it was neither the kernel nor the 24 GiB
  per-session cap — `systemd-oomd` killed a whole scope on user-session memory pressure, 52
  processes, triggered by a runner fanning one worker per core across 32 cores with no timeout.
  Wrap anything long in `timeout`. **oomd kills the WORST cgroup, not the guilty one** — an
  innocent seat can be the one that dies.

---

## TASK 1 — REVIEW OF `wip-respin-wake-check-maya` (row b0570b67) · CLOSED

**Verdict given**: LAND IT WITH THESE CHANGES. **Merged by Cheech at `af9f37d5`.**

713 lines of new production code plus edits to two hot-path files, rescued off a dead session's
/tmp scratchpad, never reviewed. Cheech had already established green (969 passed across 42
files); my job was the question green does not answer.

**Can it break session boot? No.** Fails open three layers deep: `_stamp_respin_boot_receipt`
swallows every exception including ImportError, `write_boot_receipt` catches OSError/TypeError/
ValueError/ImportError, and the whole `_build_memento_block` call already sits inside a
try/except at `register_session.py:2150` that falls back to an empty block. No network, no lock,
no subprocess — so no hang path either. The one expression evaluated OUTSIDE the guard is
`_written_at_of( _header_of( path ) )` at line 1520 (arguments evaluate before the call);
`_header_of` catches OSError and opens with `errors="replace"`, so it cannot raise.

**Do the tests bite? Yes — five mutations, five caught.** Tree restored after each.

| mutation | result |
|---|---|
| `if booted <= fired_at:` to `if False:` (kill predecessor guard) | 3 red, incl. exact-boundary test |
| add `SLOT_MIRROR` to `LIVE_SLOTS` (accept the Krishna failure) | 4 red |
| move receipt write below `if not path: return ""` | 1 red (blank rehydrate) |
| `past_due = now >= deadline` to `past_due = True` | 5 red, all the stays-quiet tests |
| kill blank-query guard in `find_receipt_by_identity` | 1 red |

The suite has **inverse tests** that assert the check stays QUIET — so it cannot be satisfied by
alarming on everything. That is the part usually missing.

**Honest about what it doesn't know? Yes.** PENDING vs DEAD_NO_WAKE is deadline-gated; the
`booted <= fired_at` predecessor guard is the load-bearing piece. Mutations 1 and 4 prove the
tests hold both lines rather than merely describing them.

**Not defects, checked**: `self_respin` is not wired here, but `self_respin_observer` already
covers it from the arbiter app — division of labour, not a hole. No TODOs, stubs, or dead
branches. `respin_wake_check` is not in the arbiter's import graph, so no arbiter-venv exposure.

### THE FINDING THAT MATTERED — and why the green run was creating it

`_stamp_respin_boot_receipt` never passed `base_dir`, so `_resolve_base_dir( None )` called
`fleet_data_root()` with no argument, which reads `cu.get_project_root()` — the **live** fleet
directory, whatever the caller had set up.

Reproduced: `pytest src/tests/unit/test_register_session_memento_block.py` — a **pre-existing
suite, 64 passed, green in Cheech's sweep** — planted 4 real JSON files in
`projects-data/lupin/`. One carried `persona: "Rachel"`, `memento_slot: "repo_io"` (a LIVE slot)
and a `booted_at` of that second: a receipt shaped exactly like a healthy one, sitting in the
directory the wake check reads. False-green vector for a same-id re-spin, planted by the test
suite itself.

**Why 91 passing tests never saw it**: the new suite's `receipts` fixture monkeypatches
`_resolve_base_dir` — the exact seam that was broken. The suite proving the feature was stubbing
out the part that was wrong.

---

## TASK 2 — APPLYING THE FIXES (commit `e58d6844`, merged as `af9f37d5`)

Cheech asked for two changes plus a test that fails without Change A, and a re-run of mutation 1.

**Change A** — pass `base_dir=str( fleet_data_root( repo_root ) )`; `register_session.py:1459`
already held `repo_root` and just wasn't handing it over. `fleet_data_root` accepts one
(`heartbeat_hold.py:202`).

**Change B** — `fired_at` stamped BEFORE the launch, passed into `_arm_respin_wake_watch` rather
than read inside it. The check ignores any receipt older than `fired_at`; read after the launch
it also swallows a HEALTHY successor that boots while later seats are still launching, and the
manager gets a false "it never woke". No module-level `datetime` import existed in that file, so
I kept the local-import style the original used.

**The guard test** — `test_a_unit_run_never_writes_into_the_live_fleet_data_root`. Runs the stamp
with a tmp `repo_root`, asserts no receipt appears at `fleet_data_root()`. Red before (naming the
stray file by full path), green after. It deliberately does **not** use the `receipts` fixture,
for the reason above.

### CHANGE A ALONE WAS NOT ENOUGH — the thing to remember

After Change A I re-ran all ten register_session suites: **252 passed, and 5 receipts still
landed in the live directory.** Three more suites — `bridge_write_contract`,
`persona_failure_alarm`, `preservation` — drive `_build_memento_block` with **no `repo_root` at
all**, so they correctly get the ambient one, which is the real repo. That is the code behaving
exactly as it will in production. Change A can only fix callers that supply a `repo_root`.

**The lesson, generalised**: a fix aimed at one call site does not close a hole whose shape is
"any caller may forget". The durable fix is the one place that cannot be forgotten.

So I added **Change C**, unasked: an autouse fixture in `src/tests/unit/conftest.py` pointing
`DEEPILY_DATA_DIR` at a per-test tmp dir. It is the third sibling of the heartbeat-events and
hook-log isolations already in that file, and serves Rick's ruling on decision `2b20a6d6`
(2026-07-27): no test touches a live data store. Result: **252 passed, 0 planted.**

**Both arms proven independently** — with the conftest guard active, reverting Change A *still*
turns the guard test red. Neither is propping up the other.

**Mutation 1 re-run after all edits**: 3 red, the same three as before. The check that does the
real work was not blunted.

**Ran** (scoped, `LUPIN_ROOT` exported per worktree): 252 register_session + wake-check ·
1411 heartbeat/hold (checking the new isolation broke nothing) · py_compile on both edited
production files.

---

## TASK 3 — REVIEW OF `wt-vacuity-sweep-e3f59977` (row e3f59977) · CLOSED

**Verdict**: LAND IT WITH THESE CHANGES (one false sentence). Fix committed `90392dd6`, **merged
as `a351bb15`**. Cheech's `:8000` gate then proved the RUN arm: **job ts-bf63ebd1, 4 passed, 0
skipped.**

Test-file-only. Replaces three class-level strict xfails with an autouse fixture that SKIPS at
runtime when the engine is not on `lupin_db_test`.

**(a) Call-time read: real, stated reason FALSE.** The docstring said "the hot-swap to
lupin_db_test happens after collection". There is no post-collection hot-swap in the pytest
process. `swap_database` (`database.py:187`) is the only rebinder and runs in the SERVER process
behind `/api/init`. Pytest's engine is a module-level singleton built at import from `LUPIN_ENV`
(`database.py:171`), and the module imports `lupin_app.main` at line 12, so the engine holds its
final value during collection. A module-level check would have read the same value.
**The true reason** — `test_answers_owed_authlane_dv1.py:113`, a sibling in the same directory,
rebinds `db_module.engine` to a throwaway DB mid-run and restores it after its yield **without a
try/finally**.
Cheech's instruction was right: replace the sentence, don't delete it — an unverified replacement
would repeat the defect `3c0491ea` existed to fix.

**(b) Can it skip forever? No, and I proved it without `:8000`.** The gate is `LUPIN_ENV`, which
`run-integration-tests.sh:202` exports as `testing`. Measured on the host, engine URL only, no DB
touched: unset gives `lupin_db_dev` and it skips; `LUPIN_ENV=testing` gives `lupin_db_test` and it
does not skip. I did NOT run the run arm — that lets `clean_test_db` wipe `lupin_db_test`, which
is `:8000` territory. Cheech's gate closed it afterwards.

**(c) The bcrypt and clean_test_db story: both TRUE, measured.** passlib 1.7.4 + bcrypt 4.3.0 —
`module 'bcrypt' has no attribute '__about__'` is raised, trapped by passlib, and carried past;
`hash_password` returns a valid 60-char `$2b$` hash that round-trips, including at 100 bytes. So
the 72-byte story was never the failure. `clean_test_db` does assert `"lupin_db_test" in db_url`
at `src/tests/integration/conftest.py:154`. Good property: the fixture's skip predicate is the
same string test as the assert it is dodging, so the two cannot drift.

**The cost, now in the docstring**: a strict xfail went RED the day the tests started passing —
loud self-expiry. A skip expires quietly; a bare `pytest src/tests/integration/` with no
`LUPIN_ENV` skips all four and still reads green. Cheech put this in the row too.

---

## TASK 4 — THE TWO TESTER WORKTREES (Plan 1's open question) · CLOSED

**Verdict: TWO GENUINELY DIFFERENT SEATS** — an implementer and his reviewer, working the same
feature concurrently. Not a duplicate, not a re-spin. **Cheech re-measured independently and it
held; María closed the plan's open question with it.**

| | tree A | tree B |
|---|---|---|
| path | `lupin-wt-qa-card-tester` | `.claude/worktrees/qa-card-tester` |
| branch / HEAD | `wt-qa-card-tester` / `ec595058` | `wt-qa-card-tester-e328974d` / `39e0dc52` |
| who | **Clayton 😎 `b36934bf`** — REVIEWER | **Tiberius 👑 `e328974d`** — IMPLEMENTER |
| content | 787 lines, all `src/rnd/*.md` | 2549 lines, code + 11 test files |

**Four independent measures, all zero overlap**: shared shas 0 (of 14 vs 15) · shared patch-ids 0
· shared files touched 0 · same merge base `9b49f04d` but independent forks from mainline.

**The clincher is not a statistic.** `src/rnd/v0.2.0/2026.08.22-review-of-tiberius-merge-phase-3.md`
line 3 reads *"Reviewer: Clayton 😎 (b36934bf) · Under review: the phase-3 merge in worktree
.claude/worktrees/qa-card-tester"* — the reviewer naming the implementer's worktree by path. And
the timestamps show a live dialogue: A commits *"gate 3's negative control never reads the
registry"* at 12:06:43; B commits *"Fix my own unfailable negative control — it never read the
registry"* at 12:08:47.

### DISCLOSURE — CARRY THIS FORWARD

**Tree A is a prior seat of my own persona** (Clayton, session `b36934bf`). I am `f961615f`, a
different session with no memory of that work; I found the attribution by reading the doc header,
not from recall. I told Cheech to discount accordingly and he re-measured before passing it on.
**Any successor reviewing tree A is reviewing its own lineage and must say so.**

**Landing consequence (María re-ordered her brief on this)**: B's review already exists inside A —
ten review documents covering phases 1–4, including two PASS verdicts and several findings B then
fixed. Land A first not because docs are low-risk, but because **the successor inherits the
review record** instead of re-reviewing 2549 lines cold. B still needs a landing decision, but a
smaller one: check that B's later commits closed A's findings, using A's docs as the checklist.

**The six untracked files in tree B: all scratch, nothing at risk.** Five shell scripts plus a
`.venv` symlink (55 bytes, points at the main repo's venv), all Tiberius's, 08-22 11:34–12:01.
**Every one carries a self-written "Scratch, not committed" line in its own header.**
`.run-scoped.sh` (scoped pytest with `LUPIN_ROOT` pinned, row a9f87d29) · `.baseline-run.sh` ·
`.capture-names.sh` (failures as a NAME set, so gating is a name-level delta in one venue rather
than a count across venues — Mr Radio's ruling 08-22) · `.gate-run.sh` · `.cov-touched.sh`
(per-file coverage on exactly the touched files, because the repo floor is `fail_under=96` at
`pyproject.toml:233` and a green tier proves nothing about the 100% mandate on two files).
Safe to delete on reclamation, worth reading once first — they encode how B's gate was measured.
**I left all six exactly as found.** This corrected María's brief, which had them down as harness
files needing resolution.

**OOM check — NEGATIVE, and deliberately not overclaimed.** I checked both runners for the
32-core fan-out: `run-unit-tests.sh` and `run-cosa-tests.sh` contain no `-n` and no xdist, and
`pyproject.toml` has no `addopts`. **These scripts are NOT the shape that killed the box at
13:02:50 and I have no evidence pointing at them.** The narrower caution: `.baseline-run.sh` runs
two full tiers back to back with no `timeout`; nobody reclaiming that tree should run it.

---

## WHAT CHEECH NAMED AS WORTH REPEATING

1. **Disclose lineage.** Saying "tree A is my own persona's prior seat, discount it" is what made
   re-measuring obviously necessary rather than optional.
2. **Keep a warning inside its evidence.** I checked both runners, found nothing, and said plainly
   I was NOT claiming the scratch script caused the kill. A warning that overreaches gets
   discounted the first time someone checks it.
3. **Break the test to prove it bites.** The mutation table is the deliverable; an opinion is not.
4. **Say what you did NOT prove.** The run arm, the `:8000` gate, "cannot tell from git alone" —
   name the boundary rather than letting green imply coverage.

---

## OPEN / NOT MINE

- **Rick's ruling on Mr. Radio's half** — parked, chase 16:30 EDT. If it comes to Cheech, this
  seat is the reviewer for it. That is the only reason the seat is held.
- **Receipt-directory cleanup** — row `ffb7310c`, explicitly not mine. I removed only the handful
  my own runs planted.
- **Landing decision on trees A and B** — María's brief, not mine.
- Nothing else owed. No open row, no blocked gate, no un-run verification.

## STATE OF THE BOX AT WRITE

Both my worktrees clean: `lupin-wt-wake-check-b0570b67` at `e58d6844`,
`lupin-wt-vacuity-e3f59977` at `90392dd6`; both merged upstream. Zero stray receipts in
`projects-data/lupin/`. 49 worktrees exist on the box — Plan 2 territory, still blocked until
both halves of Plan 1 are empty. Heartbeat pokes muted by Rick for the duration
(`heartbeat.poke_output_enabled = true` to restore).


---

<!-- memento-amendment: by=clayton session_id=f961615f amended_at=2026-08-23T19:23:37-04:00 -->
**AMENDED** 2026-08-23T19:23:37-04:00 — clayton (f961615f)

## AMENDMENT — 2026-08-23 19:23 EDT · everything after 15:36

The record above stops at 15:36 and is missing the whole evening. **Read this amendment
before acting on anything above it.** Two lanes closed after it was written, and one of
them exists specifically to stop a successor re-deriving a finding that already cost an
hour once.

### WHAT HAPPENED AFTER 15:36, in order

1. **Tree 4 review (`lupin-wt-sam-1568269d`) — STOPPED MID-REVIEW, not mine.** Ownership
   moved to Mr. Radio at 15:39 while I was reading. I touched nothing and ran nothing.
   The one thing I found before stopping, still worth knowing: **tree 4 was ALREADY
   MERGED** (0 ahead / 43 behind, landed at `ced053a1`), so the commit labelled
   `WIP: … trace-spy test still has a monkeypatch-stacking bug, fixed next` (`72eba668`)
   is the LIVE `src/cosa/rest/v2/flow.py`. Unfinished thread I never closed: on the
   `ask()` arm, `command` is the ROUTER's output (flow.py:190), so
   `"user_picked_receptionist"` there is a model prediction, not a click. Nobody has
   confirmed the router never picks that command as a low-confidence fallback.
2. **Row 673f14e8** (five module-level skips) — answered with ZERO test runs; its headline
   finding was false. Now BLOCKED on Rick, chase 10:30.
3. **Row 990934d9** (the orphaned directory) — mine, in progress, see below.

---

### 🔴 THE ONE THING THAT MUST NOT BE RE-DERIVED — THE ALLOWLIST

**Cheech's row 990934d9 claimed `src/tests/e2e/` was "invisible to every detector we have."
THAT CLAIM IS WRONG. He verified my correction himself and withdrew it. Do not re-open it.**

A detector already exists, already runs, and already enumerated both files:

- **`src/tests/unit/gate-reachability-allowlist.json`** — 89 entries, checked in. Both
  "orphaned" files are in it with dated decisions and row references:
  - `test_ask_answer_handback.py` — *UNGATED 2026-08-02 (rows 6f22531f / df698896): boots
    its own uvicorn subprocess on a throwaway Postgres DB and kill+restarts it, needs
    Postgres + ~57s, cannot run in the fast unit gate. **no runner names it**. Run
    directly: `pytest src/tests/e2e/test_ask_answer_handback.py`.*
  - `test_podcast_fail_open_gate_live.py` — *UNGATED 2026-08-04 (row 40fccd91): needs a
    real server AND **real LLM spend**.*
- **`src/cosa/repo/gate_reachability.py`** — `_is_reachable` at **:272** walks each file's
  ancestor directories against the set of runner-named targets. That IS the
  "does any runner select the path it lives in?" question.
- **`src/tests/unit/test_gate_reachability_census.py`** (row d97b024e) rides
  `run-unit-tests.sh` (wired at `run-all-tests.sh:65`), and carries a RED ARM that runs the
  detector against a withdrawn witness on every unit pass — so it is proven able to fire.

I ran the full directory audit anyway before finding the census, and **every** directory I
flagged was already allowlisted: `parity_oracle` 5, `parity_oracle_fullpage` 6,
`ws_channel_browser` 3, `ws_channel_unit` 1, `lupin_smoke` 4, `manual` 4,
`websocket_smoke` 7, `store_owed_cutover_e2e` 1, plus four loose `src/tests/*.py`.

⇒ **The lesson, and it is the generalisable one: an ENUMERATED gap reads exactly like a
SILENT one if you never look for the enumeration.** Before calling anything unmonitored,
grep for an allowlist or a census first — a deliberate dated exemption is the likeliest
explanation for a gap that obvious.

---

### 🔴 WHY THE `e2e` SUITE SLOT CANNOT BE RENAMED

`run-all-tests.sh` lists a suite named `e2e` and maps it to `run-e2e-ui-tests.sh`, which
sweeps **`src/tests/e2e_ui/`** — a different directory, one underscore away. Both "obvious"
fixes are wrong, and I was offered both:

- **Rename the slot** → breaks two things. `e2e` is an API-facing `test_types` value
  (`ALL_SUITE_COMPONENTS`, `cosa/agents/test_suite/job.py:128`, submitted to
  `/api/test-suite/submit`), and `test_typescript_suite_gate.py:189` parses the
  `SUITES=(...)` line out of the shell source and asserts the two lists are identical.
- **Point the slot at both directories** → strictly worse. It would drag a 57-second
  Postgres test and a **real-LLM-spend** test into every `run-all-tests.sh` — exactly what
  those two allowlist entries were written to prevent.

⇒ The mapping is CORRECT; the **label** is the liar. Fixed at both places a person reads —
the header suite inventory and the `[e2e]` mapping — in `4884566f`, **merged as
`20f6b8c2`**. Comment-only: `SUITES=(...)` byte-unchanged, parity gate re-verified.

---

### TONIGHT'S GREEN — `test_ask_answer_handback.py`

**3 passed / 0 failed / 33.41s, exit 0.** First execution in the file's life.
`timeout 300`, `LUPIN_ROOT` pinned to `lupin-wt-e2e-label`, single file.

- `test_stream_death_answer_reaches_asker_via_reattach` PASSED
- `test_orphaned_waiter_answer_travels_via_catchup` PASSED
- `test_live_waiter_wiped_by_restart_answer_travels_via_catchup` PASSED ← the one that matters

The third holds the waiter LIVE, kills and restarts the server for real, and the answer
still reaches the asker via catch-up. An in-memory `pending_responses` dict cannot survive
that by construction; the catch-up fallback demonstrably carries it.

**What the green covers, stated so it is not over-read**: all three assert on what the
**ASKING side RECEIVED** — the re-attach return value and the catch-up `additionalContext` —
**never on what Postgres merely holds**. That is the difference between "the row was
written" and "the answer arrived". Real migrated throwaway DB, genuine process kill+restart.

Box left clean and verified, not assumed: no stray `uvicorn`/`_handback_e2e_server`, no
leftover throwaway databases, worktree 0 dirty.

---

### ⛔ b2 — THE ONLY THING STILL OWED, AND A SUCCESSOR MUST NOT FIRE IT

**`src/tests/e2e/test_podcast_fail_open_gate_live.py` needs REAL LLM SPEND.** That is a
BUDGET decision, not a box clearance. It is **Rick's call, folded into his 10:30 chase**
alongside 673f14e8's write-or-drop question. **Do not run it because it looks like the
obvious next step — it is the one item on this row that is deliberately not started.**

Row `990934d9` is deliberately left **OPEN** on b2. Cheech: one honest open row beats a
tidy count.

---

### HABITS THAT PAID, CARRY THEM FORWARD

- **A brief is a hypothesis, not an instruction.** Cheech's brief was wrong three times in
  one day (item 1, then both proposed label fixes). Each time the right move was to move
  NOTHING and report. He asked for this to be recorded as method.
- **Read the BODY, not the reason.** 673f14e8's headline was false because someone read a
  skip's stated reason; reading the test bodies (3 tests, 3 `NotImplementedError`, 0
  assertions) settled it harder than a run could, and cost no box clearance.
- **Pre-flight before spending a window.** TCP-probe Postgres (`localhost:5432`) before
  taking the box — the file skips wholesale if it is down, and a blind run burns the window
  on "3 skipped".
- **Isolate a check from its harness.** To verify the parity gate without taking the box
  while a peer was mid-run, I re-ran the assertion standalone — same regex from
  `test_typescript_suite_gate.py:183`, same comparison. Same evidence, zero contention.
- **Verify the box is left clean; do not assume it.**
- **Disclose lineage.** Tree A of the qa-card pair (`b36934bf`) is this persona's own prior
  seat. Any successor reviewing it is reviewing its own lineage and must say so.

### STATE AT AMENDMENT

Idle, ~33% context, off the box. Branch `wt-e2e-slot-label-990934d9` (`4884566f`) merged as
`20f6b8c2`. Worktree `lupin-wt-e2e-label` clean. Flagged and handed to Cheech, not mine:
`datetime.utcnow()` deprecation in the auth path at `src/cosa/rest/jwt_service.py:75` and
`:83`. Heartbeat pokes still muted fleet-wide.
