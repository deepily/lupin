# Memento — Mr Radio 🦉, 2026-08-21 ~17:58 EDT (board-down day, re-spin #3 at 50.6%)

SELF-RESPIN-NONCE: a6d33259-5092-4e1d-a081-a6164abe16f1 @ 2026-08-21T21:58:11+00:00

**Seat**: session `e9a672a2-6447-4006-99d4-9b66756f7a28`, persona Mr Radio (Fable 5). Self-re-spin #3; same seat, same board, same lineage. On rehydrate: write wake proof if asked, `heartbeat_hold_io write … --awaiting peer:clayton --looked-in now`, topic "Skeleton crew: board-down with María (managing one implementer)".

## FIRST THING TO DO ON REHYDRATE
**befeba88** (Clayton, mock auth mints uuid5 user_id; tip `e40daca4` on `wt-mock-auth-uuid-befeba88`, worktree `lupin-wt-mock-uuid`, sits on 3ecf5ede — merge no-ff, NO rebase; his 25058-pass sweep is against that sha; my selection 246 + 47 green) is awaiting **María's verdict** (thread 4c8b0898-37d6-4f52-ab51-d6757f55294c). On her approval: `git merge --no-ff wt-mock-auth-uuid-befeba88` into wip, run `src/cosa/tests/unit/rest/test_auth.py src/tests/unit/test_mock_auth_user_id_is_a_uuid.py`, close befeba88 done with the merge sha, then **ONE `:7999` bounce** (`./src/scripts/bounce-dev-server.sh --quiet`; Cheech cleared bounces this afternoon; Rachel's uncommitted probe patch rides along) — that bounce also serves the still-unserved f429e35f + a3d21eb2. Then Clayton's board is empty: DM "prepare for re-spin" → `dismiss_sessions(session_names=["cc-author-mr-radio-1"], write_memento=True, respin_personas=["Clayton"])` → do NOT respawn unless new work (Rick's skeleton-crew order). Note: slot `io/mementos/clayton.md` belongs to an earlier Clayton (4c07338d) — his real memento is `src/rnd/v0.2.0/2026.08.21-clayton-implementer-memento.md` (fecd4be7+); seed from that path if respawning.

## Rick's standing orders today
- ~11:20: María + Mr Radio run both boards to zero, cross-checking; skeleton crew of two + one implementer seat (Clayton, `cc-author-mr-radio-1`, session 7adf6848, thread b40cea7a-8ad8-4e06-874c-c6bdc245109d).
- ~17:01–17:14: DM condenser bare-hash lists — option A chosen, prompt wording approved verbatim, "all dropped paths joined on one line"; **Rick has editorial control over the condenser prompt** — any further prompt change goes back to him via ask_multiple_choice. Code-only changes don't need his okay.
- Push is Rick-gated. Killing a live :8000 job is Rick-gated.

## DONE TODAY via Clayton (all no-ff onto `wip-v0.2.0-2026.08.03-present-and-demo`, María refute-first, my selection on each tip)
`b696b94d` a1420538 reusable document-choice card · `80d33303` 0c280989 describe-ask card_id · `1d49707b` 054207ce tfe `__all__` · `709757bf` adf5c1a1 multi-select validation · `3ecf5ede` a0151611 condenser prompt+restore guard (served boot #98) · `e9830db0` 56a3c48d path-slot guard + disclosure (served boot #99) · `f429e35f` 68601b65 punctuated ids (UNSERVED) · `a3d21eb2` 570e3dda docstring (UNSERVED). My memento commit a5beac5c earlier.

## Other state
- **María** (e5933e98): her open rows — d212f54b eval (blocked on Cheech's 1e597a65, chase 08-22 14:00Z; I reproduced her table from `io/v2-flow/eval-2026-08-21-11-37-48/records.jsonl`, cache_hit denominator is n_ok=181), 647f3733 **ruled**: V1_PIN_SHA → 15536409 (Cheech agreed), plus ~3 blocked rows behind Krishna/Cheech. Cross-check whatever she sends.
- **Me**: 89b27996 blocked on Cheech f7b57d88 (chase 08-22 13:00Z); 3 parked (3e4a4a4a, 9d89afe2, 5c78f7f9). Board otherwise empty.
- Working branch tip a3d21eb2 (+ my memento commit). NOT pushed. Uncommitted in main tree, NOT mine: Rachel's probe patch (running_fifo_queue.py, todo_fifo_queue.py, _unreachability_probe.py) + Cheech's brain-integration docs — leave alone.
- Worktrees still present: lupin-wt-dm-tutor, lupin-wt-slot, lupin-wt-punct, lupin-wt-mock-uuid (Clayton removes his own on reap); branches merged.

## Rules learned today
- Condensed DMs drop items/flip meaning — read the row amendment or ask one question. Send branch TIP SHA, not commit lists (María reviewed one commit behind once).
- One piece per commit, message matches `--stat`; red-before-fix for contract tests; inverse controls for every guard (María's Makefile catch).
- A prompt-template change is live on the running server; `.py` is not — ship the pair with a bounce.
- Old git: `merge-tree $(merge-base) HEAD br | grep -c '^+<<<<<<<'`; doc_path receipts need `lupin/` prefix; worktrees lack gitignored `src/scripts/cloud-run.env` (copy it in before trusting flash-lite tests).
- `io/mementos/<persona>.md` may belong to an earlier holder of the name — never overwrite; seed from the rnd path.
