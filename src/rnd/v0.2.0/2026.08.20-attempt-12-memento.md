# Attempt 12 — memento (Tiberius 👑, session caeee206)

**Row**: `d8d019f6-d8f5-4613-9fe9-2568cca7b241` (in_progress, mine)
**Written**: 2026-08-20 20:50 EDT, while `ts-23613e7d` is still running.

## The one-line state

**The refusal is written and stands: the two arms are not graded on the same
definition of success.** It does not depend on the latency number, which is still
below its sample-size floor.

## The three scorer asymmetries — this is the finding

| # | What | Where | Effect |
|---|---|---|---|
| 1 | **`ok` is a different bar in each arm** | `v2_eval.py:837` = `reply.status_code == 200`, payload never inspected · `v1_eval_arm.py:314` needs a job_id (`:278`), an observed terminal completion, a `completed_ts`, and a computable span | v2 scored on *"did the server answer"*, v1 on *"did the work finish end to end."* Failure rates non-comparable **by construction** |
| 2 | **Mode-switch scored as a v1 failure for correct behaviour** | `v1_eval_arm.py:278` rules no-job_id ⇒ `push_failed` | A fifth of the corpus counted against v1 for answering instantly |
| 3 | **Routing denominator** | `v1_eval_arm.py:440` states *"The v2 arm must exclude the SAME utterances"*; `v2_eval.py:464` divides by `n_ok` with no filter — "eligible" appears **zero** times in that file | v1 excludes 40% of the corpus from its denominator, v2 excludes nothing. A documented contract, unimplemented |

**Declared and NOT a defect** (so nobody counts a fourth): the latency spans
genuinely differ — v1's encloses the full FIFO queue-dwell, v2's is one
synchronous round-trip — but `paired_eval`'s renderer prints exactly that in the
instrument line beside every verdict. An argued choice, not a hidden asymmetry.

## The example that carries the argument

The identical 20 `agent router go to automatic` utterances, same corpus, same
night, **both arms behaving correctly**:

| | v2 | v1 |
|---|---|---|
| This run's data | `ok: {True: 20}`, `status: {200: 20}` | **0/20** surviving |
| What happened | answered, returned 200 | answered instantly, returned `job_id: None` |
| What the scorer says | **success** | **`push_failed`** |

v1 creates no job *because there is no job to create* — it is a mode switch.
One category, two opposite verdicts, zero difference in behaviour.

**Corrected arithmetic**: of v1's 48 failures per 100, 20 are this mis-scoring
⇒ ~28 real out of 80 scoreable ⇒ **v1's honest failure rate ≈ 35%, not 48%**.

## The four defects, and which have rows

| # | Defect | Row |
|---|---|---|
| 1 | Per-test `--timeout` shorter than the suite budget (killed attempt 11) | `64677f38` — **DONE**, commit `d2939fed` |
| 2 | Queue notifications fail validation under `AUTH_MODE=mock` | `befeba88` — **queued**, Mr. Radio said keep it |
| 3 | v1 worktree had 2 of 11 key files (gitignored ⇒ every worktree born credential-less) | no row — **fixed**, commits `ee221b71` + `5ecee90e` |
| 4 | `set_solution_summary_gist` called at `running_fifo_queue.py:1579` and `:1730`, defined only as a mock at `test_running_fifo_queue.py:65` | **no row** — awaiting Mr. Radio's call on the no-new-rows order. Live in the **current** tree; swallowed by a `try/except` printing only under `debug`, so the lazy-backfill path has been silently dead |

⚠️ **All four are the instrument, not the thing measured.**

## Tonight's commits

```
25104997  Hold the warm pairing to the same floor the real gate uses
37ba30b3  Make a zero explain itself, because zero reads as a broken tool
4366749f  Split the passes on the arm's own counter, not on list position
c90c3615  Pair warm against warm, by category, and say how many survived
f86ee2d7  Say when a delta was measured over what little survived
5ecee90e  Prove the baseline arm can embed, instead of asking if a port is up
ee221b71  Refuse to run the v1 baseline arm without the keys it needs
e53f3f4d  Report the delta with the two numbers that make it readable
5dfe0d43  Drop the paired sample to n=20 on Rick's direct call
d2939fed  Refuse a per-test timeout shorter than the suite budget it runs under
a657e88e  Save v1's hours when the v2 arm dies, and notice a run losing a whole category
```

## State of the pairing tool

`src/scripts/pair-warm-spans.py` — 23 tests. Re-derives the delta from spans
already on disk, **no rerun needed**.

- Splits passes on the arm's own `seq` (1..2N, monotonic), **not** list position.
  The positional version silently relabelled warm records as cold once a pass
  contained a failure. v2 had zero failures tonight — the luck that hides a
  defect until the run that matters, while v1 lost 48% in the same run.
- Enforces `paired_eval.MIN_SHARED_PAIRS` (30), **imported not copied**.
- Refuses with a reason: *run not ready* (prints counts) vs *arms measured
  different utterances* (provenance) vs *empty v1 artifact*.
- Every refusal warns against the cold-pass shortcut, with the receipt: that
  pairing gave 49 shared utterances and a meaningless −11.2 s "delta".

**Live at 20:50**: 18 of 100 shared — **below the floor, not yet a number.**
Pooled shows v1 3589 ms vs v2 63675 ms, so the provisional direction is **v1
faster**, but it is not quotable until n ≥ 30.

## Run state

- `ts-23613e7d` running since 19:50:20, credentialed arm (pid 316651, `b0735467`).
- v2 blocks 1–5 all **0%** failure. v1 warm: 52/100 usable.
- Watcher armed (`b9ci7fp3d`) to fire when the pairing clears the floor.

## What the next seat should NOT re-derive

1. **My credential theory was tested and FAILED.** With all 11 keys and zero
   key-not-found lines, v1 posted 48% against the crippled run's 47%, same
   categories lost. The keys were a real defect; they were not the cause.
2. **A container recreate does not kill a persisted job — it rehydrates it**
   (`[CJ-PERSIST] Restored immediate job`). Killing the pytest process inside the
   container is what dead-letters it. A bounce destroys the *queue*; a *running*
   job comes back.
3. **Three things die on a reboot, not one**: `:7997`, the two vLLM endpoints
   (`192.168.1.21:3000/3001`, started from `~/.bash_aliases`, paths in
   `~/.lora_env`), and the container queue.
4. `/api/test-suite/list-pending` **does not exist** on this build. Use
   `GET /api/get-queue/{todo,run}` + `/api/queue/pool-status`.
5. Copying keys is **not** tampering with the pin — `.gitignore:71` excludes
   `src/conf/keys/**`; the worktree stays clean at `b0735467`.

## Next actions, in order

1. Deliver the warm-warm number when n ≥ 30 (pooled **and** per-category **and**
   survivor count — a delta without all three is not readable).
2. Fix the scorers before any rerun: v2's `ok` must require an observed
   completion with a usable payload; the harness must treat a job_id-less
   mode-switch as legitimate; v2 must implement the routing-eligibility exclusion.
3. Rerun on a quiet box only after (2).
