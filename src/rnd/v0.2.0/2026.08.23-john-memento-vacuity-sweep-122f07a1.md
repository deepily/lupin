# Memento — John 🏄🏽 (session 86101fa6), row 122f07a1

**Written** 2026-08-23 ~17:25 EDT at Cheech's instruction. **Manager** Cheech 🌿 (626cd9c3).
**Branch** `wt-sweep-122f07a1`, worktree `../lupin-wt-122f07a1`, off
`wip-v0.2.0-2026.08.03-present-and-demo` @ `a180185a`. **Not pushed.**

---

## The job, and how the brief was wrong

Finish Pocholo's sweep. The brief said **2 non-strict xfail blankets + 24 grep-shaped
tests**. Both numbers were wrong, in opposite directions, and correcting them was the
first deliverable.

| | Brief | Tree | Method |
|---|---|---|---|
| Non-strict xfail blankets | 2 | **0** | AST: every `xfail` call in `src/tests` + `src/cosa/tests` carries `strict=True` |
| Grep-shaped assertions | 24 | **44** | AST: `assert "<literal>" in <var assigned from inspect.getsource>` |

Five of Pocholo's six named blankets were already merged — the four Cheech knew about
(`d5b93b66`, `985cd2be`, `a351bb15`) **plus** the 12-test queue-filtering one, landed as
`9201428f` by Mr Radio's crew, which nobody had told the row. The sixth,
`TestAutoMigrateLive`, is **not a blanket**: `@unittest.skipUnless` on a live
Postgres+pgvector check is a real runtime condition. Cheech ruled: leave it.

⇒ Row `a40e462c`'s own standing rule is what produced this: **an amendment more than a
few hours old is a hypothesis about the tree, not a description of it.**

---

## Shipped — grep half COMPLETE, 44 → 1

| SHA | What | State |
|---|---|---|
| `b2f27112` | SWE-team dozen: 12 greps → 10 behavioural tests | merged |
| `e38abe43` | Deep Research + podcast twelve: 12 → 9 | merged |
| `566cb971` | The last twenty across 10 files, + the detector | **awaiting Cheech** |

The one survivor is `test_notify_cc_listener_fallback.py:446` — a **negative control**
proving `inspect.getsource` returns garbage under a line shift. Source text genuinely IS
the subject; Cheech agreed it stays.

---

## The method that matters more than the row

**Mutate and run in ONE command, with an assertion that the edit landed.**

Two steps, and a *failed edit* produces a GREEN run reading as "the test does not bite" —
when nothing was ever mutated. Worse than no mutation: it teaches you to distrust a test
that was fine.

Receipts, all on this row, all one day:
- Pocholo hit it **five times** on the parent row.
- I hit it once (mutation G).
- **Cheech hit it three times verifying my work** — a `_audio_group_id` typo where the
  real attribute is `_audio_progress_group_id`; an anchor matching two sites; and a check
  run against `swe_team/orchestrator.py` when the test imports
  `test_fix_expediter/orchestrator.py`. Each time the error was his and the finding was
  mine. He reported all three unprompted, on the grounds that *a manager who only reports
  the checks that confirmed him is running the same fake-green risk.*

Scripts: `mutate.py` / `mutate2.py` / `mutate3.py` (session scratchpad). Each asserts its
anchor matches **exactly once** and raises otherwise.

---

## Mutation receipts — 15 total, each red on its own tests, all restored green

**SWE (b2f27112)** · A delegation tag above→inside the loop → 3 tags where 1 belongs ·
B verification tag inside→above → 1 where 3 belong · C coder tag sticky across streams,
**shipped generator line left verbatim** — all three deleted greps returned **True** on
that broken build.

**DR + podcast (e38abe43)** · D DR tag into the subquery loop (all three deleted greps
True again) · E per-language tag hoisted · F milestone tag collapsed onto status tag ·
**G audio-only stale tag → WENT GREEN FIRST TIME.**

**The last twenty (566cb971)** · oracle-aliased-sleep · prefix-unbounded ·
normalizer-create-reimpl · deadqueue-drop-field · flow-rename-parked ·
formatter-wrong-command · tfe-notification-type · shared-helper delegation (both
directions).

### G is the one to remember
The **test** was wrong, not the mutation. It built a fresh agent per run, so
`_audio_progress_group_id` started `None` and even a "keep the existing tag"
implementation opened a new one. **One agent run twice** is what a leak needs.

⇒ **A loop-boundary mutation does not exercise a state-leak.** Different failures need
different mutations. Reporting the green rather than quietly fixing it was, per Cheech,
the best call of the day: a commit whose thesis is *"tests must fail for their stated
reason"* shipping a test that could not, would have been the row refuting itself.

---

## Two comparisons worth carrying forward

**1. A check that could never fail, guarded by the comment documenting its own fix.**
```
assert "notification_type=" not in src or "# Fix 6" in src
```
`# Fix 6` is a comment at line 189 **of that very module**. Re-adding the exact kwarg it
names: left side `False`, right side `True`, assertion `True` — green on its own
regression.

**2. A grep that missed the case its own docstring feared.** The park-reason independence
test worried about "both route through a common helper" and checked only that neither
function's source names the other. Under a shared-helper mutant in **either** direction,
both source checks returned clean while the twins were fully coupled.

---

## Three findings beyond the brief — handed over, none chased

### 1. Five whole files switched off → row `673f14e8`
`pytestmark = pytest.mark.skip(...)` at module level, unconditional. Cheech's own scan
looked only for `pytest.skip( ..., allow_module_level=True )` and returned zero —
**an audit that covers one shape reports a clean tree that is not clean.**
Triage: `test_heartbeat_poker_integration.py` is the genuine defect (its own reason says
the blocker landed); `test_heartbeat_poker_e2e.py` is legitimately gated on task I7; the
other three raise a keep-or-delete decision nobody should take unasked.

### 2. Assertions that cannot fail → row `beffcddd` (P1)
An `assert` inside a `try` whose `except Exception` only logs. `AssertionError` **is** an
`Exception`. **53 test functions, 70 dead assertions, 14 files.** Detector shipped in
`566cb971`.
**Why P1:** these lines EXECUTE, so the 100% gate counts them as covered. *The coverage
number has been reassuring us about tests that prove nothing.*
**The caveat Cheech weighted heaviest:** a green from the detector proves *this one* way
of neutering an assertion is absent — never that a test can fail.

### 3. A third variant the detector does NOT catch
An `or` whose right side is always true (finding 1 above). Not a swallowed assert.
**No clean detector for it.** Named as a gap rather than implying the tool is complete —
an honestly named gap beats a shaky heuristic that produces false positives and gets
ignored.

---

## Recurring shape worth naming

**A weak test sitting next to a justification that does not survive being checked.**

Both TFE dead-queue greps called themselves white-box *"to avoid mocking the full FastAPI
endpoint"*. That endpoint takes its four queues as **ordinary arguments** — nothing to
avoid. Same shape in the TFE smoke: *"inspection rather than a full live push/poll because
in-memory dead queue state is wiped on every uvicorn reload"* — also false, same reason.
The cost was real: green while every field comes back `None`, which is what a user sees.

⇒ **When a test explains why it is weak, check the explanation.**

---

## What is left on row 122f07a1

The grep-shaped half is **done**. Nothing else on this row is owed. Open items belong to
other rows: `673f14e8` (module skips, needs an owner — Cheech is placing it) and
`beffcddd` (dead assertions, P1, detector attached).

---

## Standing constraints for a successor

- **Announce every test run and WAIT for the clear.** I fired run 1 on "firing now unless
  you stop me"; Cheech corrected it — announce-and-wait since. Three seats died in two
  days, two to test runs.
- One run at a time · never a full tier · always `timeout` · single-process pytest ·
  `LUPIN_ROOT` exported to **this worktree** on every run.
- A `quick_smoke_test()` function is not a collected test — run it through its own entry
  point; forcing it through pytest proves nothing.
- **The prohibition governs:** replacing is the work; deleting to move a number is not.
  A grep that is genuinely un-replaceable is a fine outcome **said out loud** — a silently
  kept grep is not.
- **Do not push.** Cheech merges.
