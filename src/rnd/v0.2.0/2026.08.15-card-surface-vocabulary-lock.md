# Card-surface vocabulary lock — Tiberius 👑 (facility) ↔ Tiffany 💍 (card guard)

**Date**: 2026-08-15
**Purpose**: agree the "card" surface vocabulary BEFORE either of us commits to
`src/tests/unit/test_v2_registry_drift_guard.py` (shared file, one branch
`wt-phase2-drift-guard`). Rachel's rulings are settled; this only fixes the split
of who-writes-what and the exact semantics so our edits compose.

## Rachel's rulings (settled — not re-litigated here)
1. "card" is a THIRD value inside the existing `surfaces:[...]` — allowed set
   `{prompt, training, card}`. NO separate card-exemption map.
2. Invariant: `PRODUCT_NAMES ⊆ AGENTIC_COMMANDS` (no phantom card entries), AND
   `AGENTIC_COMMANDS − PRODUCT_NAMES` must be empty EXCEPT commands whose
   exemption names "card".
3. Plain START stays OFF the card; BFE stays ON.
4. BFE is NOT "not voice-reachable" — the card IS a voice path; BFE is simply not
   router-EMITTABLE. Reason text must say that precisely.
5. Every exemption entry cites its dispatch call site; every guard ships a
   COMMITTED must-fail control.

## Semantics — UNIFORM POLARITY (the key agreement)
A value in `surfaces` = **a guard this command WAIVES**, same polarity for all three:
- `prompt`  → waives 1a (owned-must-be-router-listed).
- `training`→ waives 1c owned-arm (owned-must-be-trained).
- `card`    → waives the NEW card guard's owned-arm (owned-must-be-on-PRODUCT_NAMES).

This is why "card" is a `surfaces` value and NOT my earlier `card:"present"/"absent"`
membership field — that field had OPPOSITE polarity (required-membership) and Rachel
overruled it. Everything in `surfaces` reads "guard waived", nothing reads "required".

## The data (measured, worktree HEAD df151d7c)
- 11 owned agentic commands. `PRODUCT_NAMES` has 10 keys.
- `PRODUCT_NAMES − owned = {}` → no phantom. ✅
- `owned − PRODUCT_NAMES = {"agent router go to test fix expediter"}` (plain START) ONLY.

⇒ START is the **only** command that must waive "card". BFE + the other 9 are
on-card and satisfy the card guard UNWAIVED.

## Split of work (proposed — confirm before we edit)
**Tiberius owns (facility):**
- `_VALID_SURFACES += "card"`.
- `_card_commands()` reader over `PRODUCT_NAMES` (I define it; my validator needs it
  first, so I own it and you reuse it — removes the cross-dependency).
- START exemption: `surfaces ["prompt"] → ["prompt","card"]`, reason cites the card
  call site (START absent from PRODUCT_NAMES → never offered by
  `_confirm_agentic_routing:1058`; system-triggered, needs a pasted job hash).
- BFE reason precision per ruling #4 (not router-emittable; card IS its voice path).
- Validator card-staleness arm: waives "card" but IS in PRODUCT_NAMES ⇒ stale
  (parallels the existing prompt arm). `_validate_exemptions` gains a `card` param.
- Update the three facility controls (`test_real_exemptions_are_all_valid`,
  `test_validator_flags_...`, `test_prompt_only_surface_...`) to pass card membership
  and to assert START ∈ `_exempt_on("card")`, BFE ∉ `_exempt_on("card")`.

**Tiffany owns (card guard):**
- New `TestCardSurfaceDriftGuard`:
  - phantom arm: `set(_card_commands()) − set(AGENTIC_COMMANDS) == {}`.
  - owned arm: `set(AGENTIC_COMMANDS) − set(_card_commands()) − _exempt_on("card") == {}`.
- Its COMMITTED falsifiability control (synthetic owned-not-carded + no card waiver
  ⇒ predicate fires red; and a synthetic phantom card entry ⇒ phantom arm fires).

## Reader signature (so our edits agree)
```python
def _card_commands():
    """PRODUCT_NAMES keys — the confirmation-card alternatives a user can pick
    (todo_fifo_queue.py:1026, offered by _confirm_agentic_routing:1058)."""
    from cosa.rest.todo_fifo_queue import TodoFifoQueue
    return set( TodoFifoQueue.PRODUCT_NAMES.keys() )
```
(Class-attribute read, no instantiation. If importing TodoFifoQueue proves heavy at
collection, fallback is a regex over the PRODUCT_NAMES source block — I'll verify the
import cost before committing and tell you which one landed.)

## Order of commits
I land the facility first (self-contained, START waives card), you land the guard on
top. If you'd rather land the guard's reader, say so and I'll consume yours instead —
one of us owns `_card_commands()`, not both.
