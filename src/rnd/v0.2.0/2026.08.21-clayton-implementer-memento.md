# Clayton — implementer memento (2026-08-21)

**Seat**: implementer on Mr Radio's crew (`e9a672a2`), session `63b0a697`, spawned ~15:12 EDT by Rick's authorization.
**Reviewer**: María (`e5933e98`), refute-first, every commit. **Merges**: Mr Radio.

## State at re-spin

| Row | Merge |
|---|---|
| `a1420538` reusable choose-a-file card | `b696b94d` |
| `0c280989` describe-ask keyed on prose | `80d33303` |
| `054207ce` `__all__` unimplemented | `1d49707b` |
| `adf5c1a1` multi-select never validated | `709757bf` |

**All four merged and closed on `wip-v0.2.0-2026.08.03-present-and-demo`. Nothing owed.**
Nothing was pushed — pushing is Rick's gate, merging is Mr Radio's.

All four working worktrees and their branches are **removed** (`lupin-wt-doc-choice`,
`lupin-wt-doc-describe`, `lupin-wt-multiselect`, and `lupin-wt-doc-choice-baseline` —
the last a detached tree at `11343b1d` used for the like-for-like sweep, whose result is
recorded on `a1420538`). Every branch was checked to have zero commits outside the merged
branch before deletion.

## What the three rows actually were

One defect wearing three costumes: **a card or ask was identified by its PROSE**, and the prose is derived per calling agent. That made the wording a join key between code and the proxy answer files, so every new agent needed a byte-identical copy and a wording change silently unanswered the card with no error anywhere.

The remedy each time: give the surface a **stable id** in `response_options` (which the API carries through as an opaque dict — no model field, no endpoint change), match it exactly **before any model call**, and let the entry carry no prose at all. `document_choice` for the multiple-choice card, `document_describe` for the open-ended ask.

Alongside it, three smaller things worth remembering:

- **`file_args` in `JOB_ARG_CONTRACTS`** now declares each file-typed argument once (`kind="file"`, `search_roots`, `search_paths_key`). It killed a derivation where the extra-search-paths config key was built from the display name and **fell back to the podcast's key** — so an agent with no key of its own searched wherever the podcast was configured to look.
- **Strategy-agnostic validation** in `responder.py`: every `multiple_choice` answer is checked against the card's own options, exact-after-stripping, because that is the expeditor's own acceptance rule. Multi-select cards are skipped deliberately.
- **`option_sentinels`** now knows `__all__` as well as `__first_option__` / `__last_option__`.

## Things a successor will get wrong without being told

**Profiles do not inherit.** Each proxy script file carries its own copy of a generic entry. The id buys a VERBATIM copy, not a shared one. I overclaimed this and had to correct it in `861012b9`.

**The straggler guards are scoped by response type, and must stay that way.** The choice-card guard is scoped to `multiple_choice`, the describe guard to `open_ended`. Each would false-accuse the other's entries if widened. Both María and Mr Radio initially read the six open-ended describe entries as choice-card entries; I refuted it with `response_types` read out of the records, and they withdrew. **Read the records, don't grep the prose** — that mistake was made three separate times on these rows by three different people, including me.

**The `agents` tag is a second discriminator, not decoration.** María found that `_entry_for_card_id` returned before `_filter_entries_by_agent` ran and iterated the unfiltered list, so the tag was silently ignored on the id path. Live in the merged card path and harmless only because no `card_id` entry was tagged. Fixed in `b2228d79`; the lookup now searches the filtered list and narrows by `arg_name` too.

**A bare label string does not answer a multi-select card.** It gets wrapped under the header `"response"`, the card's real header reads as ABSENT, and the agent concludes the user selected nothing — silent, and identical to a human declining. `__all__` must submit `{"answers": {"<header>": [labels…]}}`. `", ".join( labels )` would pass review and apply zero fixes. This is why `054207ce` said "pin the encoding first"; it was the load-bearing step.

**A carve-out outlives the reason that justified it.** `unoffered_values` skipped every
multi-select card because the encoding was "pinned nowhere" — true when written, false
eight commits later once `encode_multi_select` pinned it. María caught it; the reason had
expired but still read as sound, and it sat in a different part of the file from the
change that retired it. **When you pin something, go looking for whatever was justified
by it not being pinned.** My own first instinct was to correct the stale docstring, which
would have left the unguarded path in place wearing a fresher excuse.

**Some defects are structurally out of reach of the obvious fix.** Element-wise label
comparison cannot catch `{"answers": {"Wrong": ["a valid label"]}}` — the label is real,
the KEY is wrong. That needed its own function (`unasked_headers`) and its own commit. A
wrong header is not a wrong answer: upstream it reads as NO answer, the reader falls to
its default, and the agent reports the user chose nothing.

## Working practices that earned their keep

- **Red-on-revert, actually run.** Every behaviour claim in every commit message names the arm and the observed failure text. When María asked for the contract test to be run against the unfixed `expeditor.py:1664` before the fix, the output was `None != 'research'` — None rather than a wrong name, because that call site passed *nothing*. That detail only exists because the arm was run rather than reasoned about.
- **One piece per commit, and the message must match `git show --stat`.** I broke this once: `1d638a37` is billed as the empty-map fix and also swept in in-progress work, because I used `git add -A src` mid-piece. Mr Radio ruled no history rewrite; the design note records the wrong boundary rather than hiding it.
- **Do not put a number in a commit message you did not run.** I wrote "619 across the proxy selection" in `fb9faba7`; no selection I ran produced it. María caught it, and the commit was amended (unmerged) to the reproducible 211/42.
- **A test that stays green when you remove the feature is not a test.** `test_the_rejection_names_the_strategy_and_the_value` initially asserted only the strategy name and the value — both of which appear in the SUCCESS line too. It needed `"never offered"` to bite.
- **New production code needs its own falsifier.** The `arg_name` narrowing shipped in `b2228d79` with no producer — nothing sent `arg_name`, so the branch was reachable only from tests. María's catch; fixed by stamping it on both ask surfaces, each with a revert arm.

## Open, deliberately

- Neither `054207ce` nor `adf5c1a1` has been **verified by a live TFE run**. The encoding is pinned from both code ends and the browser's submit path, not from an observed gate.
- The full-suite receipt for `a1420538` is a **set diff, empty in both directions**, against a like-for-like detached worktree at `11343b1d` — branch 30 failed / 24983 passed, baseline 30 failed / 24941 passed. The 30 are environment-shaped (bounce_dev_server, flash-lite/vertex, terraform, hermetic config, gate census, two tail-agent). Do not re-litigate them as regressions.
- My first sweep number was reported from a log captured through a `tail` pipe, holding only 11 of 30 failure lines. The count was right, the evidence was partial, and I said so rather than asserting the rest were pre-existing.

## Pointers

- Design note: `src/rnd/v0.2.0/2026.08.21-reusable-document-choice-card.md` (indexed in `src/rnd/README.md`)
- Sentinels + prose-vs-card entries: `src/conf/notification-proxy-scripts/README.md`
- Contract test: `src/tests/unit/test_file_arg_card_contract.py` — registry-derived, covers every `kind="file"` arg, both card paths
- Pinning tests: `test_proxy_scripts_answer_the_choice_card.py`, `test_proxy_scripts_answer_the_describe_ask.py`
- Answer validation: `option_sentinels.unoffered_values` (labels) + `unasked_headers` (keys), both called from `responder.py` before submit
