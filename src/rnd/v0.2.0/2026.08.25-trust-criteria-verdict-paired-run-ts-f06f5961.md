# Trust-criteria verdict — the `ts-f06f5961` / `eval-2026-08-25-19-31-31` pair

**Row**: `2ebe4ccb` · **Checklist owner**: María 🌸 · **Run by**: Mr. Radio 🦉 (session `40b53762`)
**Read at**: 2026-08-25 ~20:32 EDT · **Measured off the artifacts, not read off the report.**

## Artifacts

| arm | path | run |
|---|---|---|
| v1 | `io/v2-flow/paired-run-ts-f06f5961/v1-arm-artifact.json` | `ts-f06f5961` (Krishna 🦚) |
| v2 | `io/v2-flow/eval-2026-08-25-19-31-31/` — `v2-arm-artifact.json`, `records.jsonl`, `report.md` | — |

## ⇒ VERDICT: NOT READABLE as go/no-go

One criterion fails outright, one is unmeasurable, one provenance field disagrees. The
report is honest about the unmeasurable one and silent about the failure.

| # | criterion | result |
|---|---|---|
| 1 | freshness by `sample_signature` | PASS |
| 2 | provenance agreement | ⚠️ `git_sha` differs |
| 3 | `n_shared` against the floor of 30 | PASS — 64 |
| 4 | category composition | 🔴 **FAILS** |
| 5 | cache family | unmeasurable (correctly rendered) |
| 6 | degradation family | 🔴 **new finding — reads clean, is unmeasurable** |
| 7 | v1 degradation observability | undetermined, and says so |
| 8 | gate fired / declined | no paired gate in these artifacts |

---

## 1. Freshness — PASS

By `sample_signature`, never mtime. Both arms carry
`ff79d7e60a8b1d69acdc6df696a434f2b9c9e4cd279ca1afe319ee8381556bf8`, seed `1024`, corpus
`simple`, `n_per_command` 20, `sampled_n` 100. All seven provenance fields present on both.

## 2. Provenance — `git_sha` disagrees

`v1 = 15536409`, `v2 = 67118762`. Deliberate here — v1 is the referent baseline — but §1
asks for agreement across arms and this pair does not agree. **Record it beside any delta
rather than waving it through**, per the standing "record which definition each arm used" note.

## 3. `n_shared` = 64 — PASS against the floor of 30

v1's 64 spans are a **strict subset** of v2's 100 (`v1-only = 0`).

## 4. 🔴 Category composition — FAILS, and it disqualifies the routing comparison

v1 lost 36 of 100 (`failure_rate 0.36`), and the losses are **categorical, not scattered**:

| expected command | in corpus | survived in v1 | lost |
|---|---:|---:|---:|
| `agent router go to automatic` | 20 | **0** | **20** |
| `agent router go to calculator` | 20 | 8 | 12 |
| `agent router go to math` | 20 | 17 | 3 |
| `agent router go to todo` | 20 | 20 | 0 |
| `none` | 20 | 19 | 1 |

**An entire command category is absent from the v1 arm.** v1's `routing_accuracy 0.4667` is
therefore computed over a corpus missing one whole category and 60% of another, while v2's
`0.9167` is computed over all 100. `routing_eligible_n` is **45 (v1) against 60 (v2)**.

The arms are scored on **differently-composed corpora**. Rio's 2026-08-17 rule — the delta,
the per-arm failure rate and the category composition travel together — is exactly what
catches this. **Do not read `v2 >= v1` off this run.**

## 5. Cache — unmeasurable, and correctly rendered

`cache_hit_rate: None` · `cache_measurable: False` · `cache_observed_n: 0` ·
`cache_unobserved_n: 100`. The §6a table is explicitly refused by the harness: *"every floor
would read 0.0 BY CONSTRUCTION."*

**Krishna's proposed ninth pin is implemented and working** — the three observation states
are in the artifact by name.

⇒ **Consequence for row `2ebe4ccb`**: the owed cache-hit numbers and the §6a
cache-hit-vs-threshold table **cannot be delivered from this run.**

## 6. 🔴 New finding — pin #3 reached the cache family and not the degradation family

Measured from `records.jsonl` (200 rows, cold 100 + warm 100):

| field | value |
|---|---|
| `payload.status` | `waiting`: **200 of 200** — not one terminal outcome |
| records carrying `similarity` | **0** |
| `route_reason` values present | `args_none` 122, `unknown_command` 78 |

Neither `args_none` nor `unknown_command` is in `ROUTE_ERROR_REASONS`. **A waiting row cannot
report `replay_error` / `router_error` / `extract_error` / `agent_error`**, so all four
numerators are **zero by construction** — the identical situation the cache rates are in.

Yet `report.md` prints:

```
| replay-failure rate | 0.0 | 0.0 |
| router-error rate   | 0.0 | 0.0 |
| extract-error rate  | 0.0 | 0.0 |
| agent-error rate    | 0.0 | 0.0 |
```

…as bare numbers, directly beneath `cache-hit rate | unmeasurable | unmeasurable` and its
explanatory banner.

**This is the same defect `v2_eval.py`'s own comment cites as its receipt** — `ts-e0311090`,
*"replay_failure_rate 0.0 while the raw records show 42 of 100 warm responses returning
replay_error"* — **arriving by a different route.** Then the denominator was wrong (fixed at
`d8d019f6`; the rates now count over `answered`). Now the denominator is right and the
**numerator is unobservable.**

A reader uses these four to decide whether the rest of the artifact is worth reading at all.
Four zeros say *clean* where the honest word is *unmeasurable*.

### ⇒ Proposed tenth pin — María's to accept, alter, or reject

> The four degradation rates render **UNMEASURABLE**, not `0.0`, when `n_observed == 0` —
> same three-state treatment, same banner, same reason as pin #9.

`is_completed_ok`'s docstring argues that `ok` must not move, because routing accuracy, the
four rates and both latency instruments read denominators built from it. **That argument is
about the denominator and does not reach a numerator that nothing could have populated.** The
pin proposed here leaves `ok` exactly where it is.

## 7. v1 degradation is undetermined, and says so

`degradation_observable: False`, `degradation_paths_seen: []`. **v1 declares it; v2 does
not.** That asymmetry is the tell — it is what made finding #6 visible.

## 8. Gate

No paired-gate output exists in these artifacts. `report.md` is v2-only and says so in its own
banner: *"v2-only — NOT the go/no-go decision table."* Nothing to quote; nothing declined.

---

## What would make a run readable

1. A v2 pass that **polls to terminal** (`n_observed > 0`), so the cache family and the four
   degradation paths are measurable at all.
2. A v1 arm whose losses are **not category-shaped** — or, failing that, the delta restricted
   to the categories **both** arms measured, with the restriction stated beside the number.

## How to reproduce every number here

```bash
python3 - <<'PY'
import json, collections
v1 = json.load( open( "io/v2-flow/paired-run-ts-f06f5961/v1-arm-artifact.json" ) )
v2 = json.load( open( "io/v2-flow/eval-2026-08-25-19-31-31/v2-arm-artifact.json" ) )
s1, s2 = set( v1["metrics"]["spans_by_utterance"] ), set( v2["metrics"]["spans_by_utterance"] )
print( "n_shared", len( s1 & s2 ), "v1-only", len( s1 - s2 ) )

recs = [ json.loads( l ) for l in open( "io/v2-flow/eval-2026-08-25-19-31-31/records.jsonl" ) ]
exp  = { r["utterance"]: r["expected_command"] for r in recs }
full = collections.Counter( exp.values() )
surv = collections.Counter( exp[u] for u in s1 if u in exp )
for k in sorted( full ): print( f"{k:34} {full[k]:3d} {surv.get(k,0):3d} {full[k]-surv.get(k,0):3d}" )

print( collections.Counter( (r.get("payload") or {}).get("status")       for r in recs ) )
print( collections.Counter( (r.get("payload") or {}).get("route_reason") for r in recs ) )
PY
```
