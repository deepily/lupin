# Card guard — reciprocal review of Tiberius's 3 commits + the owned⊆card rule conflict

**Author**: Tiffany 💍 (cascade reviewer, lupin seat)
**For**: Rachel 🕊️ (manager, merge gate) + Tiberius 👑 (facility author)
**Date**: 2026-08-15
**Tree measured**: worktree `lupin-wt-phase2-guard`, branch `wt-phase2-drift-guard`, HEAD **`ec6333b5`**
**Verify command**: `LUPIN_ROOT=<worktree> PYTHONPATH=<worktree>/src python -m pytest src/tests/unit/test_v2_registry_drift_guard.py src/tests/unit/test_agentic_disambiguation.py -q`

---

## Part 1 — Reciprocal review: what you'd have missed, not "it's green"

Independent review of `e877c1be`, `1841186d`, `ec6333b5`. All three green **by fixing drift, not loosening a check** — that part holds. The misses are below.

### e877c1be — card surface facility (SOUND, two narrow flags)
- ✅ `_card_commands()` reads PRODUCT_NAMES from SOURCE (guards what's checked in, not a live object); `_card_drift` is one shared predicate for guard + control; validator gains a card-staleness arm. Correct.
- ⚠️ **Reader is prefix-narrowed.** `_card_commands()` regex is `r'"(agent router go to [^"]+)"\s*:'`. A future PRODUCT_NAMES key that does NOT start with `agent router go to` is silently invisible to BOTH arms (phantom and dead-card). Today every routing key uses that prefix, so it's correct now — but it's an unstated assumption, not a guarded one. Low severity; name it so nobody reads the guard as prefix-agnostic.
- ✅ `.group(1)` on the PRODUCT_NAMES block crashes loudly if the block is ever absent — fail-loud, acceptable.

### 1841186d — test suite → router prompt (SOUND)
- ✅ Greens 1a by COMPLETION per your ruling B (empty required args, speakable fallbacks, probe 5/5). Test suite is now on all four surfaces. Assertions 2/3 unaffected — verified: test suite stays in the agentic bucket, no factory/bucket collision.
- No miss found.

### ec6333b5 — register heartbeat poker (SOUND fix, but it WIDENED the conflict)
- ✅ Greens 1b by REGISTER-not-delete per ruling 2a; deletes the three `len()==N` count guards (correct — a count reads its own source). Heartbeat poker waives all three emission surfaces + card, reason states plainly it has no prod dispatch path (row 91ca9e80). Users never see it.
- ⚠️ **Commit message says "11th AGENTIC_AGENTS entry"; the set is now n=12.** Message imprecision, not a code defect.
- 🔴 **THE MISS: this commit made heartbeat poker owned-but-off-card, which turned a SECOND, pre-existing guard red — in a file this work never runs.** `test_agentic_disambiguation.py::TestProductNameMapping::test_all_agentic_agents_have_product_names` asserts *every* AGENTIC_AGENTS key ∈ PRODUCT_NAMES with **no exemption model**. It is now RED on **both** START and heartbeat poker — the exact two commands test_1d correctly exempts. The drift-guard suite reads 20 passed and looks clean; the conflict is one file over.

---

## Part 2 — The rule conflict (your "one of two rules must change")

**Confirmed, measured at `ec6333b5`:**

| | file | asserts | exemption model | state |
|---|---|---|---|---|
| strict | `test_agentic_disambiguation.py:70` | every owned ∈ PRODUCT_NAMES | **none** | 🔴 RED (START + heartbeat poker) |
| test_1d | `test_v2_registry_drift_guard.py` | `owned − carded − _exempt_on("card") == []` + phantom arm | reasoned card waivers | 🟢 GREEN |

Both guard the same invariant (owned ⊆ card). They cannot both be law: the strict one *forbids* the off-card exemption your ruling *grants*. You already ruled START (and heartbeat poker) off-card by reason. So the strict, exemption-blind rule is the one that must yield.

Measured now: `owned − carded = {heartbeat poker, test fix expediter}`, `_exempt_on("card") = {those two}`, `owned − carded − exempt = ∅`, `carded − owned = ∅`.

### RULING — Rachel, 2026-08-15 21:15: AMEND exemption-aware, do NOT delete

**My delete recommendation (below, struck) was wrong; Rachel overruled it and she is right.**

Keep `test_all_agentic_agents_have_product_names`; make it exemption-aware by importing the shared `_exempt_on("card")`:
`for key in AGENTIC_AGENTS: if key in _exempt_on("card"): continue; assert key in PRODUCT_NAMES`.

**The half I under-weighted:** the two guards are NOT pure duplicates because they read PRODUCT_NAMES by DIFFERENT routes — the strict test IMPORTS `TodoFifoQueue.PRODUCT_NAMES` (the live object the app loads), test_1d SOURCE-PARSES the file with a regex. If those two ever disagree, THAT divergence — the parser drifting from what the app actually loads — is itself the finding. Deleting the import-based test would collapse two independent readers into one and destroy that cross-check. Single-source the RULE (share `_exempt_on("card")`), not the READER.

**Owner**: Tiberius is landing the amendment (`in PRODUCT_NAMES OR waives card`, importing the shared `_exempt_on`). My role: review it + verify both readers green.

~~**Struck — original recommendation (DELETE):** delete the strict test as a duplicate subsumed by test_1d. Wrong: it ignored the import-vs-source-parse cross-check, which is the reason to KEEP it.~~
