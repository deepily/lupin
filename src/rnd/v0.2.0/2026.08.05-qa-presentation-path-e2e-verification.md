# Q&A-card → presentation path: end-to-end verification on fixed code

**Date**: 2026-08-05 · **Author**: Tiffany 💍 (TESTER, session `2e03e26a`) · **Manager**: Mr. Radio 🦉 (`2c3c8645`)
**Spec**: [Open: podcast-only fences spec](/app/docs?path=lupin/src/rnd/v0.2.0/2026.08.05-qa-card-presentation-path-podcast-only-fences.md) · **Store row**: `ffd46737`
**Venue**: `:7999` (dev), served code = HEAD `9b7abc98` (container up 2026-08-05T14:48:32Z; plan-mode fix `f67189c3` served) · **Job**: `pr-a10a55aa`, run as the test user `interactive.job.tester`

---

## 1. Verdict

**GREEN — the Q&A-card → manually-choose-presentation path runs end to end on current code.** The step that killed Rick's run last night (Phase 3 outline generation) now succeeds. Full flow driven live as the test user, never Rick.

The gap this closes (from the spec): the Q&A path and the card path were never run on the same code, and no Q&A attempt existed on the plan-mode-fixed code. `pr-a10a55aa` **is** that attempt.

## 2. What ran, in order (job `pr-a10a55aa`, HEAD)

| Time (UTC) | Stage | Sender | Result |
|---|---|---|---|
| 15:04:22 | Expeditor ask 1 — 4-question batch | `arg.expeditor` | answered (defaults), routed to **test user** |
| 15:04:23 | Expeditor ask 2 — "which document" | `arg.expeditor` | answered with real filename, routed to **test user** |
| 15:04:24 | Expeditor confirm — yes/no summary | `arg.expeditor` | **source resolved** to a real path; answered "yes" |
| 15:04:24 | Phase 1 — ingest | `presentation.gen` | 2666 words, 13 sections |
| 15:04:25→15:05:14 | Phase 2 — narrative analysis | `presentation.gen` | 10 sections, 12 proposed slides |
| 15:05→15:15 | Gate 1: Narrative Arc Review | `presentation.gen` | auto-proceeded (600s timeout) |
| **15:15:15** | **Phase 3 — generating slide outline** | `presentation.gen` | **enters the step that failed last night** |
| **15:15:37** | **Outline generated** | `presentation.gen` | **15 slides (2 opening, 10 body, 3 closing) — SUCCESS, 22s** |
| 15:15:37 | Gate 2: Slide Outline Review | `presentation.gen` | auto-proceeded (600s) |
| ~15:26 | Phase 4 — elaborate content | `presentation.gen` | 15 slides, 15.0m estimated speaking time |
| 15:33 | Phase 5 — serialize YAML | `presentation.gen` | `2026.08.05-at-15:33-UTC-the-three-act-structure-a-practical.yaml` |
| — | Phase 6 — Marp render | `presentation.gen` | 15 slides |
| — | Phase 7 — render visuals | `presentation.gen` | 14 visuals |
| — | Gate 4: visuals approve | `presentation.gen` | auto-proceeded (600s) |
| — | Phase 8 / 8.5 — export PPTX | `presentation.gen` | **PPTX 5462KB** |
| **terminal** | **Presentation complete** | `presentation.gen` | **15 slides, ~15 minutes — DONE** |

**Terminal state: COMPLETE.** The Q&A path produced a finished 5462KB PowerPoint (15 slides), running Phases 4-8 with expeditor-supplied args.

**Correction (do not overclaim):** `audience_context` was supplied to the **job** but is **dropped before the orchestrator** — `job.py:249-256` never copies it to config and `config.py` has no such field, so every downstream site read `None`. Phases 4-8 therefore ran clean but did **not** exercise `audience_context`; a config field being set is not behaviour observed. This is a separate bug (now authorized for fix). **Still open**: nobody has proven Phases 4-8 with an `audience_context` that actually lands — provable only once the copy-to-config fix is in.

**Source that resolved**: `/var/lupin/io/deep-research/interactive.job.tester@lupin.deepily.ai/2026.04.29-three-act-narrative-structure.md`

## 3. The Phase-3 contrast — same step, opposite outcome

| Job | Code | Entered Phase 3 | Outcome |
|---|---|---|---|
| `pr-62254a7f` (last night) | pre-fix (plan-mode bug) | 03:07:54Z | **died 37s in** — `ValueError: Outline generation returned no usable entries` (03:08:31Z) |
| `pr-a10a55aa` (this run) | HEAD `9b7abc98` (`f67189c3` served) | 15:15:15Z | **15-slide outline in 22s** |

The plan-mode fix (`PRESENTATION_PERMISSION_MODE "plan" → "default"`) holds on the Q&A path, not just the card path. Rick has his second way in.

## 4. Two live receipts for the fix proposal (not blockers to the green)

1. **Podcast wording leaks onto presentation** (`expeditor.py:1357-1361`). The document ask on this presentation job read, verbatim: title **"Missing: research"**, message **"Which document should I use for the podcast? Describe it or say the filename."** Both the podcast arg name and the word "podcast" reach a presentation user. Predicted first, then observed — a receipt, not a read.

2. **Fence #3 severity** — not triggered on this run. Source arrived **missing** (asked via the document ask) and resolved cleanly, so the present-but-unresolvable rescue (podcast-only, `expeditor.py:422`) never needed to fire. The failure fence #3 guards against only bites when the extractor **pre-fills** `source` with a topic phrase; that did not happen on this natural utterance. (Rachel proved that path's severity separately: FileNotFoundError → FAILED.)

## 5. One thing that is by-design, not a bug

The generation-phase gates ("Gate 1/2 … Review") route to the human operator (Rick), not the test-user job owner. I first read this as a misroute from the `target_user` field alone — but the gate's own abstract discloses it: **"Operator routing: originally owned by `interactive.job.tester` (service account) → operator"** (`agent_notification_dispatcher.py:79/251`). A service account's human gates are deliberately routed to the operator. **For Rick's own jobs this is correct** — he owns them. Not filed. (My miscall is recorded in the post-game deposit.)

## 6. How this was driven

Harness (scratchpad, not committed): logs in as the test user, opens a WebSocket on session `tiffany-e2e-pres`, sets mode=presentation, `POST /api/push`, auto-answers only the expeditor's own asks (all test-user-routed), then observes generation over the server log. **Rick-fence**: the WebSocket is authed as the test user, so it only ever receives test-user notifications; the generation gates went to Rick's channel (invisible to the harness) and were never answered by the test — they auto-proceeded on their 600s timeout. No `curl`; `requests` + `websockets` only.

---

## 7. Follow-on fixes — red-bank (2026-08-05, pre-fix), composed green pending

Two fixes were authorized by Rick for the two gaps this run surfaced: the **30k source-truncation ceiling** (now a config key `agent source content max chars`, default 200000, read at FOUR sites — presentation `narrative.py`/`elaboration.py`, podcast `script_generation.py` ×2) and **`audience_context`** (a real `PresentationConfig` field, copied from the job). At the time of writing they are **modified-uncommitted in the working tree** (Clayton's WIP); HEAD is still `9b7abc98`.

**Bind-mount trap (record it):** the container bind-mounts the working tree, so `docker exec … sed <file>` shows the FIXED bytes NOW while the running process (reload off, loaded 14:48Z) still executes the OLD code. A file-read "served" check is therefore incapable of failing for the reason it exists. **The behavioural tail-reached probe IS the served check** — only behaviour distinguishes a loaded module from disk bytes. `StartedAt`-vs-edit-mtime is a cheap *abort* precondition, never a pass. Any bounce right now (even one fired for an unrelated reason) serves Clayton's uncommitted WIP.

**Red banked — name the tree for each half:**

| Evidence | Source tree | Result |
|---|---|---|
| CEILING red, **observed** | HEAD's actual `get_narrative_analysis_prompt` (loaded via `git show HEAD:`, no `max_source_chars` param) fed the 31,472-char seed | 30,855-char prompt; tail "Zephyrine Cascade" (char 30,839) **ABSENT** — HEAD clips the tail |
| CEILING knob works | working-tree builder, param-swept `max_source_chars` 30000 vs 200000 | @30000 tail absent → @200000 tail **present** |
| AUDIENCE red, structural | `git show HEAD:` `config.py` + `orchestrator.py` | no `audience_context` field; **four** `getattr(self.config,"audience_context",None)` sites (696/843/979/1111) silently feed `None` |
| AUDIENCE knob works | working-tree builder | steer text present when passed a value, absent when `None` |

Note: HEAD's builder **already accepts** an `audience_context` param — the drop was never a missing builder arg, it was purely orchestrator-side (no config field + the four getattr defaults). The `getattr`-with-default is the silent-failure mechanism and violates the no-defensive-`getattr` rule; the fix should make those explicit attribute access.

**Scope of the red-bank:** proves the disk/builder mechanism ONLY. The served process still runs 14:48Z bytes (old 30000 ceiling, `getattr→None`). **Only a behavioural tail-reached run on the COMPOSED, freshly-bounced tree earns "served"** — that is row `ee679014`, held until both Clayton's and Cheech's commits are in, Rachel has passed both, and a real bounce has happened. Acceptance there = a finished PPTX **and** the tail-reached probe green. Fallback known-good: `9b7abc98` (made a deck twice: `pr-a10a55aa`, `pr-c07dbd3d`).

---

## 8. Composed-tree green (row `ee679014`) — FULL GREEN, served `e8e9c0e9`

The composed tree (`e8e9c0e9` — both crews' fixes + the census fix, chain `c6f7b45f → 54421d01 → 8de931f8 → f41aa1fe → 934b364b`) was bounced onto `:7999` (boot #39, 17:04:48Z) after a pre-bounce tracked-clean check. Rachel's reviewer gate passed 557 across presentation + both podcast trees. Live run: job **`pr-3e89d1a5`** → **`pres-2eb80e8b`**, deck **6077KB PPTX, 15 slides**, terminal "Presentation complete".

| Acceptance criterion | Result (from real generated content, off served `e8e9c0e9`) |
|---|---|
| Finished PPTX | ✅ 6223690 bytes, 15 slides, `pres-2eb80e8b` (terminal completion, not a phase artifact) |
| **Ceiling lifted** — >30k source tail reaches the deck | ✅ 31,472-char Meridian seed; tail sentinel "Zephyrine Cascade" (char 30,839) in the deck: `zephyrine`×7, `cascade`×13, `last-layer veto`×3 |
| **`audience_context` steers output** | ✅ 6 distinct ocean metaphors (`ocean/reef/tide/current/marine/underwater`) in an ocean-**alien** Meridian deck — attributable to the steer alone, sound on one generation |

**Sha attribution:** `tree_clean=False (doc-only, post-bounce)` — a peer edited a tracked R&D `.md` after boot; reload is off, so the served process runs boot-time `e8e9c0e9` bytes and a markdown doc cannot reach the deck. The sha stands for served **code**.

**Two harness gaps found and fixed on this run (own them):** (1) it green-lit on the `.yaml` deck (written ~Phase 5) and printed "PPTX made" without checking — the finished `.pptx` only appears at Phase 8.5, ~3 min later; fixed to require terminal completion + the real `.pptx`. (2) the `.pptx` carries a fresh export timestamp (`17:11`) distinct from the `.yaml` (`17:08`), so match by content, not exact filename stem. **Merge caveat:** a red in Clayton's in-flight 9k off `c6f7b45f` stops the MERGE, not this live proof.
