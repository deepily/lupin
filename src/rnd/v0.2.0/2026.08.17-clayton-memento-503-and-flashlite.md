# Memento — Clayton 😎 (session b00f8b68), 2026-08-17 ~00:33 EDT

Seat owner: **Mr Radio 🦉** (927fa30a) — monitors my context, only one who can re-spin me. Re-spin at 57.8%/over-budget.
Supersedes the earlier stand-down memento `src/rnd/v0.2.0/_archive/D/2026.08.16-clayton-memento-cascade-ledger-and-section-mint.md`.

## Deliverables tonight — all CLOSED

### 1. Bug 0c3ad4b5 — offline+no-default asks 503 → named signal — CLOSED, PUSHED
- Branch `wt-503-named-offline`, commit **f5de9207**, **PUSHED to origin** (Rick approved with a real keypress).
- Chain: offline+no-default → notifications.py raised HTTPException(503) → notify_user_sync mapped http_error_503 → MCP surfaced bare "error: http_error_503". A 503 reads as "retry", opposite of the offline-user response (block the row with a chase).
- Fix (4 layers): OfflineEvent.response → Optional/None + default_used may be False; notifications.py emits a terminal SSE OfflineEvent(response=null, default_used=false) instead of 503; notify_user_sync maps default_used=False → status "user_not_available"; converse/ask_multiple_choice/ask_open_ended_batch surface the named condition. ask_yes_no untouched (always has a default). Both constraints held: NOT keyed on the 503, NO forged default. Reviewer (Extra 2) mutation-verified the guards.

### 2. Bug 3405f0b2 — Flash-Lite Vertex client — CLOSED, MERGED, **NOT pushed (held for Rick)**
- Merged into `wip-v0.2.0-2026.08.03-present-and-demo`, **HEAD 5a17c975**, no conflicts. All three commits merge-base-verified as ancestors. **PUSH HELD for Rick on 5a17c975.**
- Three commits: **be0f0fbb** (GeminiVertexClient + google-genai vendor entry, ADC no api_key), **9a56b0a9** (vertex:// config-key arm), **643d16da** (dm_tutor/flash_lite INI+splainer + unknown-client_type raise).
- Tiffany 💍 unmocked-verified GREEN (real ConfigurationManager, no config mocks, get_client("dm_tutor/flash_lite") → GeminiVertexClient not Chat/Completion); she re-fires on the working branch as the final receipt.
- Ground truth (Extra 2, proven live): `genai.Client(vertexai=True, project=<LUPIN_GCP_PROJECT_ID>, location="global")`, model gemini-3.1-flash-lite, ADC no api_key, location MUST be "global" (us-central1 404s). Project resolves fail-loud via `cosa.utils.gcp_project.resolve_gcp_project_id`.

#### THE FOUR-LAYER UNREACHABILITY LESSON (carry this forward)
A vendor entry in `VENDOR_CONFIG` is necessary but NOT sufficient. `LlmClientFactory.get_client(key)` checks `config_mgr.exists(key)` FIRST: if the key EXISTS (a config spec like the tutor's dm_tutor/phi_4 pattern), it takes the CONFIG path and NEVER consults VENDOR_CONFIG — so a config-key spec silently built a ChatClient (silent-wrong-client: the study could run believing it was on Vertex). Reachability has four layers: (a) vendor entry — reached ONLY via a raw "google-genai:" descriptor, not a config key; (b) the config-key path — needed the `vertex://` prefix arm (elif BEFORE the :212 ChatClient else; else untouched); (c) the INI key must GENUINELY EXIST — a mocked-config test proves the routing operator, only the REAL ConfigurationManager proves the wiring; (d) the descriptor format `vertex://<location>@<model_id>` — location REQUIRED so the paired study records where each arm ran (missing location fails loud).

#### client_type raise-on-unknown (adjacent finding, fixed in 643d16da)
`_get_vendor_specific_client` had `client_type = config.get("client_type","chat")` with a final `else: → ChatClient` — a typo'd/unknown EXPLICIT client_type silently became a ChatClient (the house defect shape). Fix: explicit `elif client_type=="chat"` + `else: raise ValueError`. An OMITTED client_type still defaults to "chat" (config.get default preserved). Tests: bogus explicit raises; omitted still returns ChatClient.

### 3. Corpus human-vs-fleet split — measured, reported to Rachel
- Finding: dm_traffic.jsonl is **100% fleet, 0 human** — excluding fleet traffic empties the corpus; that's a SCOPE question for Rick, not María's fix. Corpus has NO `sender_persona` field — I measured the real `from` field. Doc: `src/rnd/v0.2.0/2026.08.17-corpus-human-vs-fleet-split.md` (opens with the finding; needs a README link; I left README untouched to avoid collision). Path resolved via `dm._resolve_dm_corpus_dir()`, NEVER hardcoded.

### 4. Earlier: reviewed Pocholo's 91ca9e80 (HBP removal, committed 593cde00); minted the six A–F cascade section rows (owner tiberius, verified clean by Pocholo); walked the Step 8 ledger (found NOT closed, no summary written).

## Constraints in force
NO push except on Rick's DIRECT word (f5de9207 was pushed on his keypress; 5a17c975 is HELD). Merge/commit are standing manager-granted once green+reviewed. Shared checkout — other seats hold uncommitted work; stage by pathspec; verify merges by `git merge-base --is-ancestor`, never trust merge output. Build on isolated worktrees. Set LUPIN_ROOT to the worktree when running tests (conftest false-green guard, row a9f87d29).

## Open threads (NOT mine unless reassigned)
- 5a17c975 push: awaiting Rick.
- 720ce725 (router-miss silent web-search): CANCELLED for me (collision with Rachel on todo_fifo_queue.py); owner sam/tiberius; Rio's full v1 spec is in the row.
- Worktrees `wt-503-named-offline` + `wt-flash-lite-genai-vendor` can be reaped (merged/pushed).
