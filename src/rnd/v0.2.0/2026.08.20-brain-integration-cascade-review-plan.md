# Brain integration — the voice path runs through AskFlow, on the existing CJ Flow queue

**Row**: `10ef4b64` · **Branch**: `wt-brain-integration-10ef4b64` (6 commits held)
**Status**: PROPOSAL — not authorized. Rick: *"I wanna understand what you're proposing before I send you off to implement it."*

---

## ✅ STATE — 2026-08-21. READY TO BUILD. NOTHING IS BUILT YET.

> ## 🔴 IF YOU ARE READING THIS TO IMPLEMENT IT, START AT `## Sequence`.
>
> **ALL FIVE questions are RULED. Every ruling is owned by a step. Nothing below is waiting on
> anybody.** Steps **0 through 13**, in order — the order is load-bearing in three places and each
> says so.
>
> **The section headings below still read like a review in progress, because that is what they were.**
> They are kept as the record of how each decision was reached. **The Sequence is the instruction;
> the prose is the provenance.** Where the two ever disagree, the Sequence wins and the disagreement
> is a defect worth reporting.
>
> **Resumed 2026-08-21** under Mr Radio (Tiberius, Krishna, Chloé); María steward, observer, author.
> Handed to Cheech's SWE crew on Rick's go.

> **Rick, 2026-08-20 23:00**: *"Stop the review, record where we are, pick up where we left off
> tomorrow morning, and build only and all of the parts when everything's been resolved. I'm not in
> a hurry — I prefer to have this done well once instead of fixing it seventeen times thereafter."*

**No code has been written. Eleven commits on top of `9fa4283f`, every one of them plan text.**
Working tree clean. Nothing pushed.

### Where the review got to

Four passes by Cheech's cast. **It had not converged when we stopped** — pass 4 found a *structural*
defect (a live feature about to vanish), not a wording nit. Two of my four original claims survived;
most of the movement was defects in my own sequence.

### ✅ ALL FIVE CLOSED — none of these blocks anything

| # | question | status |
|---|---|---|
| 1 | **User mode** | ✅ **RULED 2026-08-21 — mode STAYS, the queue resolves it first.** The flow does not learn modes. Plus: plug the wrong-agent caching on math / date & time / calculator |
| 2 | A cache guard that survives the 8 exact-hit rows | ✅ **RULED 2026-08-21 — guard BOTH ends**, read at the head, write after the agent runs |
| 3 | The 28 CRUD-backed rows — exclude or drop | ✅ **DISSOLVED 2026-08-21 — the whole cache is dumped** after the refactor. Their measurements are no longer owed |
| 4 | `_process_fast_lane` is dead — does the whole method go, and its seven tests with it? | ✅ **RULED 2026-08-21 — DELETE the method AND all seven tests.** Runtime unreachability check first, then the delete |
| 5 | **NEW** — one canonical entry point | ✅ **RULED 2026-08-21 — TWO doors at v2, `ask` and `submit`.** Every other entry point **refuses with 410**, names `/api/v2/submit`, and carries `REMOVE BY 2026-12-31`. Rick ruled **refuse-now** with the caller count in front of him — **step 11 breaks 14 mobile call sites on purpose** |

### 📏 MEASUREMENTS OWED — **MOSTLY DISCHARGED 2026-08-21, kept for the record**

> ⚠️ **Read the strike-throughs before acting on this table.** Rick's cache-dump ruling removed the
> population these measurements were about, and his mode ruling removed the number that gated the
> build. **This heading said "owed" for hours after the things stopped being owed.**

| what | why it decides something |
|---|---|
| ~~**Routing accuracy**, from the paired eval~~ | **NO LONGER GATING** — mode was ruled KEPT on 2026-08-21, so the build does not wait on this number. Still worth having; no longer a blocker |
| ~~`created_date` / `run_date` on the 27 rows~~ | **MOOT** — step 13 dumps the rows. Nothing needs explaining about data that will not exist |
| ~~For each of the 27: does a plausible utterance reach it?~~ | **MOOT** — same reason. The guard no longer has to beat a population, only to be right about new writes |
| ~~Blank `routing_command` row count~~ | **MOOT** — non-load-bearing since route-first, and gone with the dump |

⇒ **Nothing in this table gates anything. It is kept because it records what stopped being owed and
why — three of these were live questions this morning.**

### ✅ SETTLED — recorded here, and every one now carried by a step

Cache unification · write-back reuses the queue's existing path · the reporting pin removed, one
CRUD-aware resolver · gist tier absorbed into the brain · route-first with a registry-derived CRUD
skip · router failure fails loudly and bails · the receptionist stays and the conversation is her
job · no snapshot written without a valid `user_id`.

### ~~Steps that were CLEARED to build and are NOT being built~~ — SUPERSEDED 2026-08-21

Last night Rick held steps 1, 2, 2b, 3, 4 and the `user_id` fail-loud even though they were clear,
because he wanted the whole thing built once: *"I'm not in a hurry — I prefer to have this done well
once instead of fixing it seventeen times thereafter."*

✅ **That condition is met.** Everything is resolved, and he has handed the plan to Cheech's crew.
**Build the Sequence in order.**

### ✅ THREE RULINGS WERE ORPHANS — all three sequenced 2026-08-21

Each appeared above as decided while having **no step, no Files-table row and no Verification
line.** *A ruling listed as settled and never sequenced is the most expensive kind of gap: every
reader assumes someone else is carrying it.* **They are steps 0, 2c and 2d now** — kept here as the
record of a failure mode worth recognising, not as outstanding work.

| orphan | listed as | missing |
|---|---|---|
| **`user_id` required on write** (fail-loud) | SETTLED **and** cleared-to-build | step · Files row · Verification line |
| **Step 0** — close the trap first | the document argues it must come **first** | absent from the Sequence **and** from the cleared list |
| **`snapshotable=False` travels with the CRUD fork** | ruled in the exclusion-mismatch section | step · Files row · Verification line |

⇒ **The `user_id` one is not cosmetic**: 63 of 64 rows are ownerless, and **the emptied cache refills
ownerless unless that fix is scheduled as its own step.** The wipe does not fix a live write path.

### ~~Resume tomorrow at~~ — ✅ DONE 2026-08-21, both halves

Rick ruled question 1 (mode) **without** the routing number, which is why that measurement stopped
gating anything. And the cast **did** read the revised sequence end to end — Tiberius's cold read is
what caught the front matter still advertising five open questions after all five were ruled.
**Nothing here is pending.**

---

---

## ✅ Rick's ruling on user mode — 2026-08-21 (closes OPEN question 1)

> *"Of course, per your recommendation, we're going to keep it because the queue resolves mode
> first. And of course we're going to have to plug the hole that math, date and time and calculator
> can still cache a wrong agent answer under your question… that exception needs to be plugged or
> remedied by using the same confirmation prompt that I do on other branches. And that is, you ask
> the user: was that answer correct or not? That's called at the end of the execution in the
> running queue, I believe."*

**MODE STAYS.** `push_job` keeps `get_user_mode` and both maps, resolves the mode itself, and hands
`AskFlow` an already-resolved command. **The flow does not learn about modes.**

He ruled it after two questions I had to go read code to answer. Both answers are below, because the
ruling rests on them.

### Q: is mode per-user, or does one user's stickiness become a global flag?

> Rick: *"I may want to keep mode as long as it only tracks one user's mode. Since this is going to
> be a multi-user system, if stickiness of mode for one user turns into a global flag for all users,
> then that is a bad design that we need to comment out and disable."*

**Per-user. Read from source, both ends.**

| leg | what it uses | verdict |
|---|---|---|
| storage | `todo_fifo_queue.py:230` — `self.user_mode_state: Dict[str, Optional[str]] = {}` | keyed by `user_id` |
| write | `/api/mode/current` GET/POST/DELETE all use `current_user["uid"]`; **no `user_id` in the body** | one user cannot set another's mode |
| read | `push_job`'s `user_id` at both live call sites (`queues.py:241`, `:1606`) is `current_user["uid"]` | **same key at both ends** |

⇒ **The bad design does not reproduce.** His condition for keeping mode is met.

### Three multi-user gaps that ARE real — none of them "global flag"

1. **In-memory, one queue object** (`main.py:731`), single uvicorn process. **A second worker gives
   each process its own dict** — mode would then depend on which worker answered. A scaling change
   away, not a code change away.
2. **No persistence** — every restart silently clears everyone's mode, with no notice.
3. **No expiry** — sticky until cleared. The real multi-user failure is a user who forgot they are
   in todo mode.

🔴 **One place a shared entry could form**: `dead_queue_watchdog.py:396` re-pushes with
`getattr( failed_job, "user_id", "" )` — an **empty-string fallback**. An empty `user_id` looks up
mode under `""`, shared by every job that lost its user. It is also the defensive
`getattr`-with-fallback the house style bans. **Own row, not a fold.**

---

## ✅ Rick's ruling on the cache guard — 2026-08-21 (closes question 2, dissolves question 3)

> *"Obviously you want to guard both the read and the write — the read at the head end of the
> process, and the write after the agent executes… Further, we are going to dump all of the cached
> answers and start fresh after this refactor. We don't need to deal with any old bullshit that we
> have to try to figure out why. So let's focus on proper processing now so we can keep all the
> holes closed."*

**BOTH ENDS GUARDED. The legacy rows go — the cache is emptied after the refactor.**

### What that dissolves

**OPEN question 3 is gone.** *Exclude or drop the 28 CRUD-backed rows* stops being a question when
the table is emptied. The measurements owed against those rows — their dates, and which of them a
plausible utterance reaches — **are no longer owed**; nothing is carried forward that needs
explaining. **The 8 exact-hit rows die with them**, and the guard no longer has to survive a
population. It only has to be right about new writes.

⚠️ **Keep the distinction**: the dump is what makes the legacy question moot. **The guards are not
optional because of it** — they are what stops the same rows being rebuilt within a day. A fresh
cache re-poisons itself without them.

### 🔴 The write guard cannot be the correctness prompt — it answers too late

The prompt Rick named is real, at `running_fifo_queue.py:1751` (`_fire_correctness_check_async`,
fired at `:1591`):

```python
msg = f"Was this answer correct? Question: '{truncated_question}' Answer: '{truncated_answer}'"
… ResponseType.YES_NO … → snapshot.answer_is_correct → save_snapshot()
```

But it runs on a **daemon thread, non-blocking**, and on timeout *"leaves `answer_is_correct` as
None (unverified)"* — **the row is already written at `:1565` before the user answers.**

| guard | mechanism that actually works there |
|---|---|
| **READ**, head of the process | refuse any row whose `answer_is_correct` is not a confirmed **True**. Three states, starting at unknown ⇒ **fails CLOSED** — exactly what my `routing_command` guard failed to do |
| **WRITE**, after the agent executes | **the router must have CHOSEN the agent.** A mode-forced answer is not evidence about the question — the user overrode the router — so it is not written. Plus the CRUD exclusion v2 is missing |

⇒ `answer_is_correct` is the **read** guard's field. The **write** guard keys on *who picked the
agent*, not on a verdict that has not arrived. **Both ends are Rick's ruling; only the write-side
mechanism differs from the one he named, and it differs because the prompt is asynchronous.**

### ✅ RULED — the read guard NEVER asks, and an unconfirmed row is never served even once

> **Rick**: *"We want to keep unconfirmed answers from replaying until they are confirmed."*

I put two readings to him rather than paraphrase, because they differ in a way that matters: under
one, an unconfirmed row is **never** served; under the other, its **first** replay still happens and
only a later one is stopped. **His sentence forbids the second.**

- **No new question is ever asked at the head end.** The only question put to the user stays the
  existing end-of-execution *"was this answer correct?"* The head merely **consumes** what the tail
  recorded.
- **Every hit on an unconfirmed row degrades to the spoken memory-problem path** and re-runs, until
  the completion-end confirmation flips it to `True`.

**How to write it** (finding 15, Krishna's serializer trace, on Tiberius's caution): the guard reads
the **hydrated snapshot object** — `snapshot.answer_is_correct is True` — **never the raw column
value**. Both writers serialize through the same shared record builder, so representations are
uniform; **typing the column is optional hardening** against a future writer that skips that
serializer, not a prerequisite. It **fails safe toward over-refusal.**

⚠️ Tiberius's underlying caution is why this note exists at all: **a guard hung on a loosely-typed
nullable column is exactly the mistake I already made once with `routing_command`.** Twice the same
shape. That is worth its own line, not a footnote.

---

## 🔴 CORRECTION — the 27 rows were NOT written by mode. I had this wrong twice.

**What I told Rick**: retiring mode kills the pump that made the bad rows.
**First narrowing**: too wide — todo and calendar carry a `crud_factory`, so under the fork they
resolve to CRUD agents and `running_fifo_queue.py:1563` already refuses to snapshot them.
**Second, and a different kind of wrong**: the pump is **not mode at all.**

| evidence | reading |
|---|---|
| `flow.py:291` `_maybe_write_back` | gated **only** on `snapshotable and status == "done"` |
| registry: `agent router go to todo` | `snapshotable=True`, and `_run_agent` builds from **`spec.factory`** = `TodoListAgent` |
| the v2 path | **no CRUD exclusion anywhere** — v1 has one at `:1563`, v2 has none |

⇒ The rows carry `TodoListAgent` and a todo routing command because **`/api/v2/ask` wrote them
during the eval runs**, where the CRUD fork never applied. Matches the count and the class.
**This is what the write guard has to close.**

**A refutation of mine that itself needed correcting — reconciled with Krishna, both were true about
DIFFERENT classes.** I said the *sibling-class gap* at `running_fifo_queue.py:1563` does not hold,
because `TodoCrudAgent` and `CalendarCrudAgent` are **subclasses** of `CrudForDataFramesAgent`
(`todo_crud_agent.py:14`, `calendar_crud_agent.py:14`) and `isinstance` catches them. True — **and
beside the point**: the 28 rows carry **`TodoListAgent` (27)** and **`CalendaringAgent` (1)**, which
are **siblings**, and those sail straight through that check.

⇒ **v1 is protected by ROUTING, not by the class test.** Under the CRUD fork a todo question builds a
CRUD subclass in the first place, so a `TodoListAgent` never reaches `:1563` to be tested. **That is a
much weaker guarantee than "the exclusion is sound"** — it holds only as long as nothing else
constructs the sibling class. **Do not restate my original sentence; it reads as a guard that is not
there.**

⇒ **What survives of the mode pump**: `math`, `datetime`, `calculator` — not CRUD, still snapshotted
by v1. Rick's mode instruction lands on exactly those. **They are not what made the 27 rows.**

---

## 🔴 SECOND INSTANCE OF THE MODE DEFECT — step 6c also drops the AGENTIC commands

Chloé, via Mr Radio. `todo_fifo_queue.py:798` routes `command in JOB_ARG_CONTRACTS` to
`_handle_agentic_command`; `AskFlow`'s resolve (`flow.py:110`) is **conversational-only** and
`resolve_agentic` is unreachable from the flow. **No plan step preserves that early return.** After
6c, *"run the test suite"* falls through to the receptionist. Covers all 10 agentic commands plus
the automatic control command.

**Same shape as the mode hole: a live feature vanishing with every test still green.**

⇒ **Two instances make it a class.** The sequence assumes `AskFlow`'s entry covers everything
`push_job` covers. It does not. **Anything `push_job` handles BEFORE its LLM-routing block is at
risk by construction.**

### ⇒ REQUIRED: an entry-coverage audit, before any of 6c is built

Enumerate **every** branch `push_job` handles today — conversational, the CRUD fork, mode, agentic
contracts, control commands, receptionist/none, unknown — and show **each one's home after 6c**. A
third instance cannot hide behind a sequence that was never enumerated. *(Mr Radio's framing, taken
verbatim as a requirement. Its mirror image — what enters the queue WITHOUT touching `push_job` — is
the entry-point inventory below.)*

### The mode test that replaces the vacuous one

Chloé's, and it fixes the defect the old one had:

- **Direct**: set todo mode, push a question that routes elsewhere, assert **the router was never
  called** — not merely that the todo agent answered.
- **Agentic**: set `deep_research` mode, push a math question, assert the router was never called
  **and** the agentic handling fired.

**Asserting the router's ABSENCE is what makes it non-vacuous.** The two cases cover both of mode's
jobs: dispatch, and suppressing the disambiguation prompt.

---

## 🔴 FINDING 14 — the replay errors were never the cache's fault

Tiberius, via Mr Radio. All **42** replay errors were **tier-1 exact hits at 100.0** that died
**inside the cached snapshot's own code**:

| cause | count |
|---|---|
| codeless receptionist rows — only `CalculatorAgent` gets the codeless short-circuit | **9** |
| long-form vs short-form command-string mismatch on the todo path | **19** |
| math, unexplained — **because the error text is thrown away** | **14** |

**Two folds come out of it.**

1. **The fail-loud ruling is under-scoped against itself.** `flow.py:102` calls the receptionist with
   **no `primary_error`** — the same gap as the router branch the plan already fixes. Rick's ruling
   covers **all six degrade paths**, so *"what lands"* must extend the `primary_error` wiring and the
   spoken failure to the **replay** branch too. **14 of 42 are unexplained purely because the text
   was discarded** — that is the ruling's own point, arriving as a measurement.
2. **The first two causes are write-side quality defects.** The wipe does not fix them; **the fresh
   cache's write-back must not reproduce them.** A line belongs in the write-back section even under
   the dump ruling.

---

## 📏 One population, stated once — the drift that split two reviewers this morning

STATE AT STOP used three phrasings interchangeably — *28 CRUD-backed*, *"the 27 rows"*, *"27
blank-owner rows"* — and commit `48c28a09` measured 28 against a restated 27. **Two of Mr Radio's
reviewers started on differently-sized populations because of it.**

**Stated once, and it is two separate things:**

- **(a) The CRUD-backed population**: **28 rows = 27 `TodoListAgent` + 1 `CalendaringAgent`**, all
  written **2026-08-20 during the eval**. One night's artifacts — which is why Rick's dump is cheap
  and defensible rather than a loss.
- **(b) Blank ownership is a SEPARATE, LIVE write-path defect** — **63 of 64 rows are ownerless**,
  escalated as foundational. **Not a legacy-cleanup question, and not fixed by the dump**: the write
  path keeps producing them, so **a fresh cache refills ownerless unless the `user_id` fix is
  scheduled as its own step.**

⇒ **The phrase "the 27 blank-owner rows" is dead.** It named neither population correctly.

---

## 🔴 Orphans — cleared to build, absent from the Sequence

**Step 0 is in neither the Sequence section nor the cleared-to-build list**, though the document
argues it must come first. Same family as the `user_id` fail-loud ruling and the `snapshotable`
fix — ruled, then never sequenced. **The observable-commit tally is wrong for the same reason**: 13
steps plus step 0's four commits, with 2b and 6-pre both observable on `/api/v2/ask`, so **at least
five observable commits, not three**. ⇒ **Re-derive the tally from the sequence as written** — a
hand-adjusted count is how the first one went wrong.

---

## Also found while checking mode — not this plan's, flagged rather than sat on

`speech.py:338` calls `todo_queue.push_job( munger.transcription )` with **one of four required
arguments**, under a stale `TODO: get websocket_id from the browser plugin`. That path either is
dead or raises `TypeError` on every use. ✅ **DETERMINED 2026-08-21 — it is BROKEN, not dead**
(`munger.is_agent()` reaches it), it is **door 8**, not an internal caller, and it has a row:
**`ef62749e`**. The "undetermined" here was true when written and stale within the hour.

---

## 🔴 SCOPE EXPANSION — Rick, 2026-08-21: ONE entry point, and it is v2

> *"We also want to add as part of the scope and the context of all of this refactoring work that we
> want to remove the multiple points of entry. That is, I don't want to have a Q&A entry point along
> with an ask entry point along with a specific kind of task queuing mechanism. I want one entry
> point — the new one that you created, the version 2 entry point. I want that to be the canonical
> method for queuing all jobs."*

**This changes what the plan IS.** Until now the plan grafted the v2 flow onto the voice path and
left the other doors alone. The target is now: **`AskFlow` is the single front door, and everything
that queues a job goes through it.**

### 🔴 Every door that exists today — 18, not the 7 I first counted

**My first table was wrong, in the way I have been warned about**: I grepped three router files — the
ones I already had open — and presented the result as an inventory. Chloé measured it repo-wide at
`16c6dc2b`. *An exclusion is only as wide as the path the probe ran.*

| | my table | measured |
|---|---|---|
| **HTTP doors** | 7 | **18** |
| **internal (non-HTTP) callers** | 2 | **7** |
| mechanisms | 3 | 3 — unchanged: `push_job`, `push_job_agentic`, raw `push` |

**The `push_job` and `push_job_agentic` rows were complete.** The entire undercount is raw
`todo_queue.push( job )` — **14 call sites across 12 endpoints**, plus **6 internal callers**.

| # | endpoint | site | mechanism |
|---|---|---|---|
| 1 | `POST /api/push` | `queues.py:241` | `push_job` — **the only question-shaped door** |
| 2 | `POST /api/push-agentic` | `queues.py:332` | `push_job_agentic` |
| 3 | `POST /api/v2/ask` | `v2_ask.py:146` | `AskFlow.run` *(intended survivor)* |
| 4 | `POST /api/v2/resume` | `v2_ask.py:184` | `AskFlow.resume` *(intended survivor)* |
| 5 | `POST /api/job-history/{job_id}/retry` | `queues.py:1606` | `push_job` |
| 6 | `POST /api/jobs/{id_hash}/resume-from-checkpoint` | `queues.py:1829` | **raw `push`** |
| 7 | `POST /api/test-fix-expediter/resume-from` | `queues.py:1932` | **raw `push`** |
| 8 | `POST /api/upload-and-transcribe-mp3` | `speech.py:338` | `push_job` — **BROKEN, determined** |
| 9 | `POST /api/bug-fix-expediter/submit` | `bug_fix_expediter.py:112` | **raw `push`** |
| 10 | `POST /api/claude-code/queue/submit` | `claude_code_queue.py:204` | **raw `push`** |
| 11 | `POST /api/deep-research/submit` | `deep_research.py:183` | **raw `push`** |
| 12 | `POST /api/deep-research-to-podcast/submit` | `deep_research_to_podcast.py:171` | **raw `push`** |
| 13 | `POST /api/deep-research-to-presentation/submit` | `deep_research_to_presentation.py:181` | **raw `push`** |
| 14 | `POST /api/mock-job/submit` | `mock_job.py:192` + `:346` | **raw `push`** |
| 15 | `POST /api/podcast-generator/submit` | `podcast_generator.py:540` + `:729` | **raw `push`** |
| 16 | `POST /api/presentation-generator/submit` | `presentation_generator.py:241` | **raw `push`** |
| 17 | `POST /api/swe-team/submit` | `swe_team.py:164` | **raw `push`** |
| 18 | `POST /api/test-suite/submit` | `test_suite.py:187` | **raw `push`** |

**Internal callers (7)**: `dead_queue_watchdog.py:401` (`push_job`) and `:486` ·
`test_suite_completion_watchdog.py:282` · `job_persistence.py:830` (the catch-up restore) ·
`arbiter_bootstrap.py:184` · `test_fix_expediter/orchestrator.py:2174` ·
`bug_fix_expediter/job.py:586` — all raw `push` but the first.

**`speech.py:338` is determined, not undetermined** (my text said undetermined): `push_job` takes
four required parameters, the call passes one, so it **raises `TypeError` every time it is reached**
— and it is reached whenever `munger.is_agent()` is true. It sits inside a live HTTP handler, so it
is **door 8**, not an internal caller.

### 🔴 What the shape of the list means — it sizes Rick's instruction rather than contradicting it

The three mechanisms are not three styles of one thing:

| mechanism | what the caller supplies | what still needs deciding |
|---|---|---|
| `push_job` | **a question** | everything — route, resolve, expedite |
| `push_job_agentic` | **a named command + args** | nothing about routing; the args are given |
| raw `push` | **an already-built job object** | nothing at all |

⇒ **16 of the 18 doors either name their command outright or hand over a finished job. Exactly ONE
— `/api/push` — is question-shaped**, which is the shape `AskFlow.run` is built around.

**Rick's ruling: a second entry on the flow — `run` for questions, `resume` for jobs.** With the
✅ **CLOSED 2026-08-21 — Rick generalised `submit` to cover it; see step 10.** *The paragraph below
records how the gap was found and is kept for that reason; it is no longer an open question.*

With the measured denominator, that two-way split left a **third arrival kind** unhoused: a **new** job that
already names its command and carries explicit args (door 2 and every `/submit` endpoint). It is not
a question — nothing to route — and it is not a resume — no prior job to continue.

🔴 **Flagged, not decided.** Either `resume` generalises to *"a job whose command is already
decided"* (which covers both new-and-named and continue-existing), or a third entry is needed. **The
ruling was given against a 7-row table; this is the same question with a larger denominator, so it
goes back to Rick rather than being resolved by me.** ✅ **He answered: `submit` generalises to
*"the command is already decided"*, covering both. Step 10.**

⇒ And the guard warning grows with the count: a read guard at the head of `push_job` misses
**doors 6, 7 and 9–18, plus internal callers i2–i7** — twelve endpoints and six callers, not two.

*Source: Chloé's `2026.08.21-queue-entry-point-inventory-corrected.md`, measured repo-wide at
`16c6dc2b`, read-only, each path resolved from its `APIRouter` prefix rather than assumed. Her
disposition column is deliberately EMPTY — that column is Rick's.*

### 🔴 What this immediately tells us about the guards Rick just ruled

**Doors 6 and 7 bypass `push_job` altogether.** A read guard installed "at the head end of the
process" inside `push_job` **does not cover them**. Either the guard lives somewhere both paths
reach, or those two doors must be converted before the guard can be claimed complete.

⇒ **This is the entry-coverage audit, arriving from the other direction.** Chloé's agentic finding
asked *what does `push_job` do that the flow will not*. This asks *what enters the queue without
touching `push_job` at all*. **Both lists are needed and neither is written yet.**

### ✅ Owed before this scope could be sequenced — ALL THREE NOW CARRIED BY STEPS 10–12

1. For each of the 7 doors: does it become a call into `AskFlow`, or does it go away?
2. `push-agentic` explicitly disclaims the expeditor and interactive Q&A — **folding it into a flow
   built around both is a behaviour question, not a refactor.**
3. The two raw-`push` doors reconstruct a job rather than asking a question. **A front door built
   for questions may be the wrong shape for them** — that is a design fork, and it is Rick's.

~~**No step is written for any of this yet.**~~ ✅ **Superseded 2026-08-21 — steps 10, 11 and 12
carry all three.** *(Left visible rather than deleted: this sentence was true when written and stayed
on the page after it stopped being true, which is the defect the fold audits keep finding.)*


---

## Rick's ruling on the entry points — 2026-08-21. TWO doors, `ask` and `submit`.

> *"Submit is a great contrastive pair with the verb ask. Wonderful choice of words. Now I want to
> make sure that this new verb `submit` happens at the V2 level side by side with `ask`, and that
> simultaneously submit on all the other entry points is disabled. We really only want these paired
> entry points."*

**The v2 flow gets a second entry. Every other door is DISABLED — not converted and kept.**

| verb | the caller is saying | head runs? |
|---|---|---|
| **`ask`** | *"here is a question — decide what to do with it"* | **yes** — route, resolve, expedite, cache |
| **`submit`** | *"here is work — the command is already decided"* | **no** — straight to build, run, guarded write-back, notify |

**Why this word and not a coined one**: the product already uses it for exactly this. **Ten of the
eighteen doors have `submit` in their path**, so each becomes `flow.submit( ... )` and the mapping
needs no explanation. `submit` alone is vague; **next to `ask` it is unambiguous, and that contrast
IS the design.**

**One entry, two arrivals**: `submit` covers both a **new** job that names its own command (door 2
and every `/submit` endpoint) and a **saved** job being continued (retry, checkpoint resume,
expediter resume). Rick generalised rather than adding a third verb: *"generalize resume to the
command is already decided."*

### The two behaviour deltas this designs away rather than leaves open

Chloe flagged both against door 2 (`push-agentic`), which deliberately refuses the expeditor and
never asks the user anything. **Under the pair they stop being deltas and become the definition:**

1. **A `submit` path does no LLM routing** — callers keep naming their own command. That is not a
   concession to `push-agentic`; it is what `submit` *means*.
2. **A `submit` path never parks a needs-input question at a service account** — there is nobody
   there to answer. Only `ask` may reach needs-input.

### Cost, named rather than buried

`AskFlow.run()` becomes `AskFlow.ask()`. Churn — but **the endpoint is already `/api/v2/ask`**, so
the method finally matches its own door. **The one rename to refuse is keeping `run` for the decided
path**: `run` currently means the *question* path, so reversing its meaning is the single rename
where an existing caller or test keeps compiling and quietly does the opposite thing.

### ✅ Owed before this could be sequenced — NOW CARRIED BY STEPS 10, 11 AND 12

1. ✅ **RULED 2026-08-21 — GONE PERMANENTLY, AND THE ERROR NAMES THE NEW DOOR.** Rick: *"They should
   be gone permanently with the new door named in the error message. Also we should tag each of those
   disabled doors with an expiration date. By the end of 2026 they should be gone. Say it loud and
   say it proud — dead by the end of the year."*

   - The route **stays registered and refuses** — **410 Gone**, not 404 — and the refusal **names
     `/api/v2/submit`**, so a caller reading the error learns the fix instead of filing a bug.
   - **Every stub carries an expiration date, stated out loud in the code and in the message:
     REMOVE BY 2026-12-31.** Not a quiet comment — the refusal itself says it.
   - It leaves a **tombstone**: a deleted route is invisible, and nothing would stop someone
     re-adding `/api/podcast-generator/submit` next year because the product needs it.

   ⚠️ **Unmeasured and it bears on this: I do not know what EXTERNAL callers these doors have.** The
   six internal ones are known; the browser UI, the extension and any of Rick's scripts are not
   enumerated. **A refusal is a poor way to discover a caller.** Counting them before this is
   sequenced is cheap; guessing is not.
2. **The six internal callers are not HTTP** (`dead_queue_watchdog:486`,
   `test_suite_completion_watchdog:282`, `job_persistence:830`, `arbiter_bootstrap:184`,
   `test_fix_expediter/orchestrator:2174`, `bug_fix_expediter/job:586`). They would call
   `flow.submit()` directly — there is no endpoint to disable.

   ✅ **`job_persistence:830`, the boot-time catch-up restore — RULED 2026-08-21: it goes through
   `submit` like everything else.** It is the caller **least likely to be noticed misbehaving** — it
   runs at startup, unattended, re-enqueueing work an interruption left behind — which is precisely
   why it should not be the one path that skips the guard. **A restored job was interrupted and its
   answer was never confirmed; the first thing the emptied cache learns must not arrive through the
   unguarded door.**

   🔴 **Precondition, not a detail: STARTUP ORDERING.** The flow, the registry and the executor must
   all exist before the restore runs. **I have not read `main.py`'s construction order and will not
   assert that it is fine.** Proving that ordering is part of this step.
3. **`/api/v2/resume` already exists.** Whether it *becomes* `submit`, or stays the flow's narrower
   needs-input resume beside it, is a reconciliation to **read**, not to assume.

---

## FINDING 17 — door-counting is the WRONG FRAME for the read guard, and I used it twice

**Tiberius, and it corrects me.** I told Rick the read guard *"misses twelve endpoints."* Wrong —
and wrong in a way I have now repeated today: **I generalised from a door count instead of reading
the call sites.**

**There are only three live snapshot-lookup sites on the ask path**, verified:

| site | fate |
|---|---|
| `running_fifo_queue.py:302` (`_process_job`) | **7b removes it** |
| `running_fifo_queue.py:946` (`_process_fast_lane`) | **dead** — no production callers |
| `todo_fifo_queue.py:557` (`push_job`'s search) | **7b removes it** |
| `flow.py:94` — the brain's own `cache.lookup()` | **the survivor** |

**After the refactor the read guard has EXACTLY ONE HOME: the brain's lookup.** No door can bypass
it, because no door does its own lookup any more.

**So the eighteen doors size the FRONT-DOOR scope and say nothing about guard placement.** Both are
true; they are not the same argument, and I ran them together.

**The fourth reader, named so nobody thinks it was missed**: `GET /admin/snapshots/search`
(`admin.py:810`, handler at `:757`) is admin-role-gated and returns a **list of matches for a human
to look at**. It never replays a snapshot as an answer and nothing it returns is spoken.

🔴 **AND THE QUALIFICATION THAT TURNS IT INTO AN IMPLEMENTATION CONSTRAINT** (Tiberius): **put the
guard at the REPLAY DECISION, not inside `get_snapshots_by_question`.** A guard placed in the shared
lookup helper would **also filter this endpoint** — hiding unconfirmed rows from the one person whose
job is to look at them, and doing it **silently**. The guard's question is *"may this row be **served
as an answer**"*, and **only the replay path asks it.**

*Verified during the audit: `GET /api/admin/snapshots/search` is `Depends( require_admin )` and
returns `SnapshotSearchResult` — `id_hash`, `question_preview`, `question_gist`, `created_date`,
`score`. **No answer field, no `run_code`, no `run_formatter`, nothing enqueued.** The one-home claim
stands.*

**The frame that closes it** (Mr Radio): **the guard governs REPLAY, not VISIBILITY.** A diagnostic
view is not a replay path, so it **MARKS** unconfirmed rows and never hides them — hiding would blind
the operator to exactly the rows the guard is refusing to serve, and make the cache look healthier
than it is. *Conditional on Tiberius confirming during his audit that `:810` is read-only diagnostic
with no path back into serving an answer. If he finds one, this returns as a real guard question.*

### FINDING 18 — door 8 has no owner

`speech.py:338` is **determined broken**, not undetermined, and belongs to nobody. Store row
**`ef62749e`** minted. **No work during the review**; its disposition rides on the entry-point ruling
above.

---

---

## Rick's ruling on cache scoping — 2026-08-21. NOT user-scoped, and the reason is mechanical.

> *"I am inclined to not make cache lookup user-ID specific. We want person A to benefit from a
> cached answer for person B — that's for sure… questions about the weather or what time it is are
> utterly non-personal by their very nature, and questions about what's on my to-do list are actually
> scoped by the dataframe passed into them, such that the exact same question summons the exact same
> Python code that actually reads a different database. So while the answers are personal, the code
> that executes them is not. That cache exists not just to replay an answer but primarily to replay
> Python code that then recalculates the answer on a per-user basis. So in general we're okay, but we
> should probably document that particular lookup and note it may cause problems in the future."*

**The cache stays shared. Row `85b5c58c` closes on this.**

### I verified the mechanism rather than recording the argument — it holds

`InlineExecutor._replay` (`executor.py:96`):

```python
snap   = work.job.for_current_user( work.user_id, work.session_id )
snap.run_code()
answer = snap.run_formatter()
```

**A cache hit re-executes the stored code as the asking user, then re-formats.** It is not a text
lookup. The snapshot is a **cached computation**, and `for_current_user` rebinds it before it runs —
so the same question replays the same Python against a **different database**. Rick's account of his
own design is correct, and it is the reason the shared cache is safe rather than an argument that it
is.

### 🔴 THE ONE CASE WHERE IT DOES NOT HOLD — codeless snapshots serve stored TEXT

`solution_snapshot.py:848`, in the code's own words:

> *"CalculatorAgent and similar deterministic-dispatch agents have no Python to save… On replay,
> those snapshots' answers come from the cached `self.answer` field, **not from re-execution**."*

⇒ **For a codeless snapshot there is no per-user recalculation.** The stored words are served
verbatim. Rick's safety argument is load-bearing on `run_code()` doing something, and for these rows
it does not.

**Why this is a documented future risk rather than a live one:**

| | |
|---|---|
| today's codeless agent | `CalculatorAgent` — *"15% of 80"*. **Nothing personal to leak** |
| the 9 codeless receptionist rows | finding 14's evidence they exist; **they die on replay**, and the dump removes them |
| what would make it live | **any future agent that is deterministic-dispatch AND answers about a person's own data.** Codeless + personal is the combination to refuse |

⇒ **The rule to carry forward: a snapshot may be shared across users only if replaying it RE-RUNS.**
A codeless snapshot is a stored answer wearing a cached-computation's clothes. **If a future agent is
added with no code to save and a personal answer, it must not be snapshotted** — the same shape as
the existing CRUD exclusion, for the same reason.

*Recorded per Rick's instruction to document the lookup. He is not losing sleep over it tonight and
neither am I; it is written down so the person who adds that future agent does not have to
rediscover it.*

---

## Context

**The problem.** v1's routing grew into a hand-written `if/elif` ladder in `todo_fifo_queue.push_job`,
one branch per agent. v2 (`AskFlow`) was built as a single legible path — cache → router → resolve →
argument expeditor — and it works, but **nothing in the product calls it.** Its only caller is the
HTTP route `POST /api/v2/ask`; the surface people actually speak to never reaches it.

**Rick's framing, which corrects mine.** This is not about proving v2 is *better*. It is a
**replacement**: one comprehensible entry point in place of code that "had evolved into a pile of
spaghetti and was impossible to comprehend." The measurement only has to show it does not regress.
The asynchronous CJ Flow queue **stays** — `AskFlow`'s synchronous executor is an artifact of the
experiment's scope, and the head end was always going to be grafted onto CJ Flow.

**Already landed tonight** (committed, merge held behind attempt 12): the ladder is gone. Six
branches became one `registry.resolve_voice()` lookup, so a seventh conversational command is one
table row and zero edits to the queue. That closed **registration**. This plan closes **routing**.

---

## The shape: ONE integration, not three grafts

`push_job` calls `AskFlow.run()` with a **queue-backed executor** injected. Cache, router, resolve
and the expeditor all run **inside that one call** — they are not grafted separately, because taking
them one at a time would leave a v2 router beside a v1 expeditor and would re-implement `AskFlow`'s
degradation logic inside `push_job` step by step, rebuilding spaghetti somewhere new.

**The seam already exists.** `AskFlow` delegates execution to an injected `Executor` protocol
(`src/cosa/rest/v2/executor.py`); `InlineExecutor` is only the experiment's implementation. `Outcome`
already declares the other one:

```python
status is "done" when an answer was produced, "parked" when a QUEUED EXECUTOR
handed work off (phase 2), "failed" when replay or the agent threw.
    status : Literal[ "done", "parked", "failed" ]
    job_id : Optional[ str ] = None
```

The queued-executor slot, "phase 2" and `job_id` are already in the contract. Nobody built the
executor. **That is the new code: one class.** (The word "parked" there becomes `waiting` — see
finding 5.)

---

## 🔴 What exploration changed — five findings, and the first one is the plan's real subject

### 1. There are already TWO cache stages on the voice path. Adding v2's would make THREE.

This is the finding that matters and my earlier write-up missed it entirely.

| # | where | what it does |
|---|---|---|
| 1 | `todo_fifo_queue.push_job` | similarity search → `ConfirmationDialogue` → `_queue_best_snapshot` |
| 2 | `running_fifo_queue.py` (~line 292-325) | a **second** lookup at execution time, own `threshold_confirmation`, own CRUD exclusion |
| 3 | `AskFlow.run()` step 1 | `cache.lookup()` with `similarity_floor`, replay on exact hit |

Routing `push_job` through `AskFlow` puts all three in one request unless something is removed.
Three thresholds, three definitions of "hit", two of them already disagreeing about CRUD.

**This is the spaghetti, precisely located.** It is also the biggest decision in the work, and it is
Rick's — see Open Decisions.

### 2. `_build_agent` uses `spec.factory`, which bypasses tonight's CRUD ruling

`flow.py::_build_agent` constructs from `spec.factory` — the **v2 pin**, deliberately non-CRUD for
cache-hit reporting. Rick ruled tonight that voice keeps the CRUD fork and the registry carries the
flag (`resolve_voice`). **Routing voice through `AskFlow` as it stands would silently undo that
ruling within a day of making it.**

> 🔴 **SUPERSEDED 2026-08-20 by Rick's ruling on the reporting pin (below).** The fix is no
> longer "the flow resolves the voice binding" — it is **one CRUD-aware resolver**, so there is
> no wrong binding left to reach. Finding 2 is closed by deletion, not by plumbing.

### 3. `_build_agent` is not the queue's agent construction

It hardcodes `push_counter=-1`, `question_gist=agent_question`, `auto_debug=False`,
`inject_bugs=False`. `push_job` passes the real `question_gist` (used by the query log),
the real `push_counter`, and the real debug flags. Agents built by the flow are **not** the agents
the queue builds today.

### 4. Write-back fails SILENTLY, not loudly

`_maybe_write_back` opens with `if not ( snapshotable and outcome.status == "done" ): return None`.
A `parked` outcome writes nothing and says nothing. **The gap would not announce itself** — the
cache would simply stop learning, which is the failure mode nobody notices. And note finding 1:
`running_fifo_queue` may already be writing snapshots at completion, so the answer here may be
"reuse what exists" rather than "build a deferred path."

### 5. Two smaller things, mine to handle either way

- `_run_agent` treats anything but `"done"` as agent failure → receptionist. It must learn **parked
  is success-in-flight**. Small, but it sits in the core degradation path and needs its own control.
- **`parked` already means "waiting on a human answer"** in `flow.py` (`_needs_input`,
  `status="parked"`). `Outcome.status="parked"` means "queued for execution." Two states, one word,
  and the second is about to go live. **Rick's ruling, 2026-08-20**: the queued-for-execution state
  is called **`waiting`**, not parked — *"parked implies that it's temporarily paused, and that is
  not the state we are talking about."* A queued job is not paused; it is waiting its turn and will
  run. `parked` stays with `_needs_input`, where something genuinely IS suspended pending a human.
  Rename first, step 1.

---

## Files

| file | change |
|---|---|
| `src/cosa/rest/v2/executor.py` | rename `Outcome.status` "parked" → **`waiting`**; **new** `QueueExecutor` — `submit()` registers the scoped job, pushes onto the existing FIFO, returns `Outcome(status="waiting", job_id=…)` |
| `src/cosa/rest/v2/flow.py` | **`run()`'s replay branch AND `_run_agent`** accept `waiting` as in-flight; `_build_agent` takes the queue's real construction values (finding 3). 🔴 **The needs-input `parked` is LEFT ALONE** — Rick ruled it keeps its name (*"parked implies temporarily paused, and that is not the state"* applied to the QUEUED state, not this one), and it is the value documented on the live endpoint at `v2_ask.py:57`. **ONLY `Outcome.status` is renamed.** An earlier draft of this row said "rename the needs-input `parked`" — following it breaks the `/api/v2/ask` contract |
| `src/cosa/rest/todo_fifo_queue.py` | `push_job` resolves user mode, then calls the flow with `QueueExecutor`. **BOTH v1 cache stages go** — Rick ruled it; the "whichever stage" phrasing predates the ruling and is dead |
| `src/cosa/rest/running_fifo_queue.py` | `:313` (`_process_job`) — **in scope, not conditional**: remove the accept-above-floor; its safety depends on the upstream ask step 7 removes. `:953` (`_process_fast_lane`) — 🔴 **PENDING open question 4, NOT in scope until ruled.** The two were one row and the document disclaims scope for the second elsewhere |
| `src/tests/unit/`, `src/cosa/tests/unit/rest/` | new pins + the existing 78-test coverage file |

**Reuse, do not rebuild**: the **single CRUD-aware resolver** (step 2b — which **DELETES**
`resolve_voice()`; an earlier draft named it as the reuse target and that is now backwards), the
`Executor`/`Work`/`Outcome` contracts, `register_scoped_job` + `push()` in `todo_fifo_queue`, and
`snapshot_mgr` in `running_fifo_queue` for any completion-time write-back.

🔴 **Step 2b's call sites, absent from the step and from this table**: folding the resolvers changes
`resolve()`'s signature, so **`flow.py:111` and `flow.py:182` both change** and the flow reads the
CRUD flag.

🔴 **Missing row — `src/cosa/rest/fifo_queue.py`**: step 5 deletes `push_blocking_object` /
`pop_blocking_object` (`:83-110`) and `is_accepting_jobs` (`:125-138`) **there**, and reddens its
`quick_smoke_test()` at `:683` (asserts at `:700`, `:743`, `:840-848`).

---

## Sequence

> **Rewritten 2026-08-21** to carry the day's rulings. Until this pass, every ruling landed as prose
> and **no step owned it** — three of them had been sitting SETTLED-and-unsequenced since last night.
> **A ruling nobody sequenced is a ruling nobody builds.**

0. 🔴 **CLOSE THE DEPRECATED-MANAGER TRAP — FIRST, before anything reads a snapshot.** Argued in this
   document since last night and **sequenced nowhere until now**. `grep "def save_snapshot"` returns
   the deprecated `SolutionSnapshotManager` **first**, which is how I came to tell Rick the queue
   writes snapshots to files while the brain reads Postgres — wrong; `main.py:706` RAISES for
   anything but postgres. **Anyone reading this plan and grepping their way into the code hits the
   same trap in the same order.** Four commits. **No behaviour change.** Not observable.

1. **Rename** `Outcome.status` "parked" → **`waiting`** (Rick's ruling: a queued job is waiting its
   turn, not paused). `parked` stays for `_needs_input`, which is a genuine suspension. Pure rename,
   own commit, no behavior change.
2. **`QueueExecutor`** + unit tests, nothing wired. Red-on-revert.
2b. **One CRUD-aware resolver** — fold `resolve_voice()` into `resolve()`, delete the R-A3 pin and
   its rationale comment, move the CRUD exclusion to the report reader. Own commit, pinned by the
   existing `test_registry_voice_binding.py` suite re-pointed at the single resolver.
2c. 🔴 **`user_id` REQUIRED ON WRITE — fail loud.** Rick, 2026-08-20: *"cannot file without providing
   a valid user ID."* **Ruled, listed as settled, and sequenced nowhere until now.**
   `V2Cache.snapshot_from_result` (`cache.py:104`) defaults `user_id=""` and `session_id=""`, and
   `_maybe_write_back` (`flow.py:287`) calls it **passing neither** while holding `ctx` in scope. So
   v2's own write-back manufactures ownerless rows — **the same shape as the 63-of-64 already on
   disk.** Precedent lives in that function: it already raises for an empty question. One field over.
   ⚠️ **The dump does NOT fix this** — a fresh cache refills ownerless without it, which is exactly
   why it cannot stay an orphan. **Test: delete the raise and watch a blank-user write succeed.** A
   test that only asserts a populated write works would have passed before the change. Not observable.

2d. 🔴 **`snapshotable` TRAVELS WITH THE CRUD FORK.** Ruled in the exclusion-mismatch section,
   **sequenced nowhere until now.** The registry marks todo and calendar `snapshotable=True`, and
   under the single CRUD-aware resolver those commands resolve to CRUD agents — which
   `running_fifo_queue:1563` refuses to serialize. **Two sources of truth that disagree by
   construction.** Nothing is broken today (the queue's `isinstance` wins, and `_maybe_write_back`
   never fires for queued work), but **a flag that says "cache this" about a class the writer refuses
   to cache is a trap for the next reader.** Fix: when the resolver applies the fork it returns
   `snapshotable=False`. Not observable.

3. **Both `done`-gates accept `waiting`** as success-in-flight — `_run_agent` at `flow.py:246`
   **and `run()`'s replay branch at `flow.py:99`**, each with a control that fails if a waiting
   outcome degrades to the receptionist.
   ⚠️ **`:99` was my omission, not a reviewer's find**: the original step named only `_run_agent`,
   but the replay branch lives in `run()`. Left unfixed, a **cache hit** routed through the queue
   returns `waiting`, fails the `done` check, and reaches the user as `replay_error` — the
   receptionist apologising for a question the cache could already answer. `_maybe_write_back`
   at `:291` needs **no** change; its guard is correct under Rick's write-back ruling.
4. **Agent-construction parity** (finding 3) — the flow must build the agent the queue builds.
   **All EIGHT differences named explicitly — cascade ruling, pass 3.** My earlier text said
   *"real `question_gist`, real `push_counter`, real debug flags"* and left four of the eight to be
   inferred; a fix that names half its own scope is a fix the implementer will half-apply.

   | kwarg | `flow._build_agent` (`:219`) | `push_job` (`:770`) |
   |---|---|---|
   | `question` | `agent_question` — args folded in by `_compose_question` | `question` — bare |
   | `question_gist` | `agent_question` (the question again) | the real computed gist |
   | `last_question_asked` | `agent_question` | `salutation_plus_question` |
   | `push_counter` | `-1` | `self.push_counter` |
   | `debug` | `self.debug` | `True` |
   | `verbose` | `self.verbose` | `False` |
   | `auto_debug` | `False` | `self.auto_debug` |
   | `inject_bugs` | `False` | `self.inject_bugs` |

   `user_id`, `user_email` and `session_id` are the three that already agree. **`question_gist` is
   the one with teeth** — the query log reads it, so a gist that is really the raw question makes
   every logged row wrong in a way nothing raises about. Pinned by a test asserting the class *and*
   all eight values. The CRUD half of this step is gone — step 2b removed it.
5. **MOVED — see step 7c below.** This slot is kept so the numbering of steps 6 and 7 does not
   shift under anyone reading an older fold. **Nothing is built here.**

   🔴 **THE `:541` CALL GOES IN THE SAME COMMIT — cascade finding, and the original step would have
   broken the live path.** `todo_fifo_queue.py:541` calls `self.pop_blocking_object()` from the
   **`else` branch**, which is NOT inside the dead region: it runs on *every ordinary question*.
   Its comment reads *"make sure to remove a possible blocking object"* — with nothing able to push
   one any more, it pops a `None` and re-sets a flag that is already set. **Inert, but live.**
   Delete the method while leaving that call and every spoken question raises `AttributeError`.
   One commit: the dead block, the two methods, the gate, **and `:541`**.

   ✅ **RULED 2026-08-21 — STEP 5 MOVES AFTER THE SWITCH, and it is now step 7c.** Tiberius's
   position, accepted whole. He named his own falsifier — a reason the dead branch must not survive
   alongside the new flow — and I do not have one, so the say-it-plainly alternative is declined.
   His first reason is decisive:

   1. **Step 5's justification points at code another step deletes.** It argues nothing replaces the
      two-turn dialogue because *"the synchronous yes/no at `todo_fifo_queue.py:636` already IS the
      live confirmation."* **`:636` is inside the block 7b removes.** Moving 5 after 7b **dissolves**
      that dependency instead of documenting around it.
   2. **Step 5 is not observable, so "it lands alone because Rick hears it" was never a reason to
      keep it early.** Zero production callers of `push_blocking_object`; `_accepting_jobs` never
      goes false; the `:541` pop is inert. **Nothing it deletes can be perceived.**
   3. **The reindent gets cheaper.** The ~330 lines unindent either way, but after 6c that block is
      no longer the live path — so a whitespace-dominated diff is reviewed against **dead code**
      rather than against the code every spoken question runs.

   *Safe because the branch cannot be re-entered: `is_accepting_jobs()` never goes false, so `:507`
   is unreachable no matter what calls `push_job`.*

   🔴 **AND IT IS NOT FOUR DELETIONS — it is a ~330-line REINDENT.** The branch removed is
   `if run_previous_best_snapshot:` at `todo_fifo_queue.py:507`; **the LIVE path is its `else:` at
   `:538`, running to `:866`.** Deleting the `if` unindents all ~330 lines of that `else`, so
   `git diff` shows the whole block as changed and the commit is reviewed as a reindent, not a
   delete. **In its new position that diff lands against code the switch has already bypassed.**

   ### Step 7c — the step as it now stands, after 7b

   **DELETE the two-turn confirmation**: the `ConfirmationDialogue` branch, `push_blocking_object` /
   `pop_blocking_object`, and the `is_accepting_jobs()` gate that serves them.

   🔴 **What carries confirmation in the NEW position is 6b's near-match ask — NOT
   `todo_fifo_queue.py:636`.** The earlier justification said `:636` *"already is the live
   confirmation."* **That becomes false the moment this step lands after 7b, because 7b deletes
   `:636`.** A justification that is true in one position and false in another is exactly what moving
   a step is supposed to surface. *(Tiberius, auditing the fold that recorded the move.)*

   Verify unreachability at runtime first — a log line or assertion on the branch — then delete.
   **Rick's instruction: say plainly in the code what was removed and why it was bad.**

6. **SPLIT INTO THREE — cascade ruling, pass 3.** I asked whether step 6 was too wide; it was. The
   three changes have different blast radii and only the last is observable, so they get separate
   commits and the switch lands alone:

   🔴 **Owed inside step 3 — the replay branch still reports the column this document condemns.**
   `flow.py:100-101` reports `command=lookup.snapshot.routing_command`. **After route-first a real
   routed command exists**, so the nullable, blank-defaulted, `or ""`-coerced column condemned at the
   cache-ordering section should not still be what feeds `_finish`. Feed it the command the router
   actually chose.

   - **6-pre — reorder `flow.run()` to router → resolve → cache**, with the registry-derived CRUD
     skip, per Rick's ruling. Observable on `/api/v2/ask` immediately; lands alone, before 6a,
     because 6a is what arms the hole this closes. Pinned by a test that a CRUD command never reads
     the cache, red on revert.
   - **6a — gist tier in `V2Cache`.** Read-side only; `write_back` already computes
     `question_gist` (`cache.py:195`). Not yet reachable from voice. Pinned by a test that a
     reworded-but-same question replays, red on revert.
   - **6b — near-match ask branch in `AskFlow`.** Per Rick's ruling. Also not yet reachable from
     voice. Pinned by a test that a near-match asks before replaying, red on revert.
   - **6c — wire `push_job` to `AskFlow.run()`.** ⚠️ **THE SWITCH.** Everything before it is
     invisible to a speaking user; this is the commit where the voice path changes hands. Lands
     alone, on a quiet box, and it is the first thing to revert if anything is wrong.

7. **SPLIT INTO TWO — cascade ruling, pass 3**, ordered by observability:

   ✅ **RULED 2026-08-21 — 7a IS NOW A FULL DELETION.** Rick: the whole method goes, **and all seven
   tests with it**. Not one branch removed from a method nobody calls — *"tidying a room nobody
   enters"* was the plan's own phrase for that, and it is no longer what happens here.

   ⚠️ **Precondition, same as step 7c's**: the zero-callers finding rests on a **static search**.
   Prove unreachability at runtime — one log line or assertion — **before** the delete lands. The
   plan already demands this of the two-turn branch; the same standard applies to the same kind of
   claim.

   - **7a — `running_fifo_queue:953`** (`_process_fast_lane`). **No runtime behaviour changes.**
     🔴 **The reason is bigger than the review stated: `_process_fast_lane` has NO production
     callers at all.** Verified — the only references anywhere are the seven tests at
     `test_running_fifo_queue.py:1213-1247`, and nothing dynamic reaches it (the one `getattr` by
     name at `:2070` fetches `_process_job`, inside a `__main__` smoke block). Its docstring
     describes it running on the consumer thread, but `_process_job` inlines that logic itself at
     `:300-322`. **A ~40-line dead method with a test suite pinned to it.**
     ⇒ **RULED — the whole method goes, and its seven tests with it.** The "open question for the
     next pass" that stood here is answered; see the ruling above this list.
   - **7b — `running_fifo_queue:313`** (`_process_job`) **and `todo_fifo_queue`'s search-and-ask.**
     Alters the live path. Must land AFTER 6c, because its safety depends on the brain's near-match
     ask already being the thing that asks.
   - **7c — DELETE the two-turn confirmation** (the step that used to be 5; **moved here 2026-08-21**).
     The `ConfirmationDialogue` branch, `push_blocking_object` / `pop_blocking_object`, the
     `is_accepting_jobs()` gate, **and the inert `pop_blocking_object()` call at
     `todo_fifo_queue.py:541`** — that one lives in the `else` branch and runs on *every ordinary
     question*, so deleting the method without it raises `AttributeError` on every spoken question.
     **It is a ~330-line REINDENT, not four deletions**: the removed branch is
     `if run_previous_best_snapshot:` at `:507` and the live path is its `else:` at `:538` running to
     `:866`. **Must land after 7b** — in this position what carries confirmation is **6b's near-match
     ask**, NOT `:636`, which 7b deletes. Prove unreachability at runtime first. **Rick: say plainly
     in the code what was removed and why it was bad.** Not observable.

   *(Full reasoning for the move, and the falsifier Tiberius named, sit in the ruling recorded under
   slot 5 above. **This list is where an implementer reads the step** — a definition that exists only
   in an earlier slot is a step nobody builds.)*

   🔴 **ORDER REVERSED — cascade finding.** The original sequence removed v1's cache stages (then
   step 6) *before* wiring the flow (then step 7). Between those two commits the live voice path
   would have had **no cache at all**: v1's stages deleted, the brain's not yet reachable. Every
   question would have run a full agent, and a bisect landing in that window would show a latency
   regression that belongs to the gap, not to either change. Wire first, remove second — the two
   caches overlap for one commit, which is the safe direction.

8. **Write-back** per Rick's ruling — nothing new built; `running_fifo_queue` already saves at
   completion.

9. 🔴 **THE TWO GUARDS — Rick's ruling of 2026-08-21, and neither had a step until now.** Split by
   end, because they key on different things and only one of them is observable:

   - **9a — the WRITE guard: the router must have CHOSEN the agent.** A mode-forced answer is not
     evidence about the question — the user overrode the router — so it is **not written**. This is
     what stops the mode pump refilling an emptied cache within a day. Also closes the v2 gap: the
     write path has **no CRUD exclusion** (`flow.py:291` gates only on `snapshotable and done`),
     which is what actually produced the 28 rows. **Not observable** — nothing a user perceives.
   - **9b — the READ guard: refuse any row whose `answer_is_correct` is not `True`.**
     🔴 **PLACEMENT IS PART OF THE STEP, not a detail of it: the guard goes at the REPLAY DECISION,
     never inside `get_snapshots_by_question`.** A guard in the shared lookup helper would also
     filter `GET /api/admin/snapshots/search` — blinding the one person whose job is to inspect the
     cache, and doing it silently. The guard's question is *"may this row be served as an answer"*,
     and **only the replay path asks it.**
     **Read the HYDRATED object** — `snapshot.answer_is_correct is True` — **never the raw column.**
     Three states starting at unknown ⇒ **fails CLOSED**, which is precisely what my `routing_command`
     guard failed to do. Typing the column is optional hardening against a future writer that skips
     the shared serializer, **not a prerequisite**.
     ⚠️ **OBSERVABLE, and it is the one nobody will predict**: every hit on an unconfirmed row now
     **re-runs** and speaks the memory-problem line instead of replaying. Until the end-of-execution
     confirmation flips rows to `True`, **the cache appears to stop working.** Rick's ruling is
     explicit that this is correct — *"keep unconfirmed answers from replaying until they are
     confirmed"* — but it lands alone, and the commit message says out loud what a listener will hear.
     **After the dump this is the entire cache**, which is why 13 comes last.
     ⇒ **Test: an unconfirmed row is never served, not even once.** Assert the re-run, not merely
     that the guard function returns false — the guard is only real if the replay does not happen.

10. 🔴 **`submit` BESIDE `ask` — Rick's entry-point ruling, no step until now.**
   `AskFlow.run()` → **`AskFlow.ask()`** (the endpoint has been `/api/v2/ask` all along; the method
   finally matches its own door), plus a new **`AskFlow.submit()`** exposed as **`POST /api/v2/submit`**.
   **`submit` skips the HEAD** — no routing, no expeditor, no cache read — and goes straight to the
   spine: build, run, guarded write-back, notify. It takes **both** a new job that names its own
   command **and** a saved job being continued.
   **Two behaviours fall out of the definition rather than needing rules**: a `submit` path does no
   LLM routing, and it **never parks a needs-input question at a service account** — there is nobody
   there to answer. **Only `ask` may reach needs-input.**
   ⚠️ `/api/v2/resume` **stays as it is and is NOT folded in** — read at `flow.py:131`, it resumes a
   **parked disambiguation question**, not a saved job. It belongs to `ask`. *Checked rather than
   assumed.*
   **Observable on the API** — a new door appears and a method is renamed.

11. 🔴 **DISABLE THE SIXTEEN — Rick: *"gone permanently, with the new door named… dead by the end of
   the year."*** Each route **stays registered and refuses with 410 Gone**, and the refusal
   **names `/api/v2/submit`** so a caller reading the error learns the fix instead of filing a bug.
   **Every stub carries `REMOVE BY 2026-12-31` in the code AND in the message** — *"say it loud and
   say it proud."* Not a quiet comment: the refusal itself says it.
   **410 and not 404, deliberately** — a tombstone. A deleted route is invisible, and nothing would
   stop someone re-adding `/api/podcast-generator/submit` next year because the product needs it.
   ✅ **THE PRECONDITION IS DISCHARGED — Chloé measured it, and Rick ruled on the result.**

   | | |
   |---|---|
   | doors with **at least one production external caller** | **15 of 16** |
   | doors with **none** | **1** — `/api/mock-job/submit` |
   | production external call sites | **21**, across **3** surfaces |
   | cron / shell callers | **0** |

   **The mobile app alone calls 14 of the 16.** `agentic_repository.dart:13` calls itself *"Typed
   wrapper over all 10 agentic job endpoints"* — **it is built around this set**, and carries ~20 test
   doubles that go red with them. **Two of the three surfaces are separately-managed repos** —
   `src/lupin-mobile` and `src/lupin-plugin-firefox` — **which this repo cannot change.**

   ✅ **RULED 2026-08-21 — REFUSE NOW; the other repos catch up.** I recommended sequencing their
   cutover first, citing the one precedent on record: `/api/claude-code/queue/submit` →
   `/api/claude-code/submit` cost a cross-repo round and a **deliberate one-release alias window**
   (`lupin-mobile/history.md:332`, `TODO.md:312`). **Rick chose refuse-now instead.** His call, taken
   with the caller count in front of him.

   ⇒ **What that means, written plainly so the mobile owner is not surprised by it**: on the day this
   lands, **14 mobile call sites and ~20 of its tests fail**, and the Firefox plugin's calls fail with
   them. The 410 body names `/api/v2/submit`, so the fix is discoverable from the failure — that is
   the mitigation, and it is the whole of it.

   🔴 **THIS MAKES CUTOVER WORK IN TWO OTHER REPOS OWED, AND IT HAS NO OWNER.** Neither repo can be
   edited from here. **The work must be handed to a session resident in each repo** — planning it from
   this one and dropping it over the wall is the failure mode. **Named here because a precondition
   that becomes someone else's unassigned work is not discharged, only moved.**

   **Loudly observable** — every one of those doors changes what it returns.

12. **The six internal callers call `flow.submit()` directly** — no endpoint to disable:
   `dead_queue_watchdog:486` · `test_suite_completion_watchdog:282` · `job_persistence:830` ·
   `arbiter_bootstrap:184` · `test_fix_expediter/orchestrator:2174` · `bug_fix_expediter/job:586`.
   🔴 **`job_persistence:830` — the boot-time catch-up restore — is RULED IN** (Rick, 2026-08-21). It
   is the caller **least likely to be noticed misbehaving**: startup, unattended, re-enqueueing work
   an interruption left behind. **A restored job was interrupted and its answer was never confirmed;
   the first thing an emptied cache learns must not arrive through the unguarded door.**
   ⚠️ **PRECONDITION, not a detail: STARTUP ORDERING.** The flow, the registry and the executor must
   all exist before the restore runs. **I have not read `main.py`'s construction order and will not
   assert that it is fine.** Proving it is part of this step. Not observable.

13. 🔴 **DUMP THE CACHE — LAST, and only once 9a and 9b are in.** Rick: *"we are going to dump all of
   the cached answers and start fresh after this refactor. We don't need to deal with any old
   bullshit that we have to try to figure out why."*
   **Order is load-bearing**: dump before the guards and the fresh cache re-poisons itself from the
   same unguarded write path within a day — the wipe cleans the floor, it does not fix the leak.
   **What this dissolves**: the 28 CRUD-backed rows, the 8 exact hits, and every measurement owed
   against them. **What it does NOT fix**: the ownerless-write path (step 2c) and the guards — both
   live defects that outlive their data.
   **Loudly observable** — the cache is empty; every question runs its agent until the store rebuilds.

---

### ⚠️ The observable-commit tally must be RE-DERIVED, not adjusted

The count of six was correct for the sequence **as it stood this morning**. This pass adds steps 0,
2c, 2d and 9 through 13, of which **9b, 10, 11 and 13 are observable**. **I am not publishing a new
number here** — the last two wrong counts in this document both came from adjusting a figure rather
than re-deriving it from the list, and one of them survived a correction *inside the correction
itself*. **Re-derive from the sequence as written, after the reviewers have read it.**

**Observability after the split.** 🔴 **The definition has to change before the count means
anything**: observable = **visible on ANY live surface**, not merely voice-visible. Under the old
voice-only reading a recount returns three again, which is how the wrong number survived two folds.

Under the corrected definition: **6c and 7b** are voice-observable, **and 2b, 6-pre, 6a and 6b are
observable on `/api/v2/ask`.** ⇒ **six observable commits, across 13 steps and 17 commits** (step 0
carries four). **Not "three out of ten."**

🔴 **Residual caught inside the corrected tally itself — the list and the number disagreed.** An
earlier draft named **seven** members and concluded **six**. *Six is the right number; **step 5 was
the wrong member.*** The plan proves elsewhere that the two-turn branch has **zero production
callers**, so deleting it changes nothing a user can perceive — **the prompt Rick actually hears is
the synchronous ask at `todo_fifo_queue.py:636`, which `7b` removes.** Dropping step 5 makes the
membership match the number. *(Tiberius. Worth recording on its own: a corrected count can still
carry the old error inside its list.)*

---

## Verification

- **Per step**: every change proven **RED on revert** — the mutation named in the commit, not just a
  green run afterwards. A test never seen failing is a comment with a green tick.
- ⚠️ **"Stay green" is NOT parity for anything the flow bypasses.** A test that exercises
  `todo_fifo_queue` directly keeps passing when `push_job` stops reaching that code — see the user-mode
  finding above, where the plan's own proof would have certified a deleted feature. Every parity claim
  needs a test that runs THROUGH the new path, or it proves nothing.
- **Behavior parity** — 🔴 **"must stay green UNCHANGED" was unsatisfiable as written, and step 5
  is why**: it deletes the very thing two of those suites exercise.
  `test_todo_fifo_queue_coverage.py:311-316` calls `push_blocking_object` and patches
  `ConfirmationDialogue`; `test_crud_queue_integration.py:410-411` sets `_blocking_object` /
  `_accepting_jobs`. **A suite cannot stay green unchanged against code that no longer exists.**
  ⇒ Each parity claim needs a test that runs **THROUGH** the new path, since a suite exercising
  `todo_fifo_queue` directly keeps passing after `push_job` stops reaching it.

  ✅ **The per-test disposition — Tiberius's measurement, folded as written.** *"Step 5 breaks two
  suites" is true but coarse*: measured, it is **one deletion, two fixture edits, and three rewrites
  that belong to OTHER steps.**

  | test | file:line | disposition | step |
  |---|---|---|---|
  | `test_not_accepting_jobs_confirmation_runs_snapshot` | `test_todo_fifo_queue_coverage.py:311-321` | **DELETE** | 5 |
  | `ConfirmationDialogue` import | `…coverage.py:16` | **EDIT** — the symbol dies with the branch | 5 |
  | `_blocking_object` / `_accepting_jobs` fixture lines | `test_crud_queue_integration.py:410-411` | **EDIT, 2 lines** | 5 |
  | `test_refactor_skips_snapshot_search` | `…coverage.py:323-330` | **REWRITE** | **7b**, not 5 |
  | `resolve_voice` call | `…coverage.py:227` | **REWRITE** | **2b** |
  | `assert "resolve_voice(" in source` | `test_registry_voice_binding.py:214-217` | **REWRITE TWICE** | **2b**, then **6c** |

  - The first is a **true deletion** — it exercises only `push_blocking_object` →
    `ConfirmationDialogue` → `_queue_best_snapshot`, all of which step 5 removes. **There is nothing
    to rewrite it into**: the behaviour is gone by ruling, not relocated. Under the coverage mandate
    that is fine, because the code it covered no longer exists. Its replacement is the plain-language
    comment step 5 already owes.
  - `test_crud_queue_integration.py:410-411` is **fixture scaffolding, not coverage** — the suite
    builds a `RunningFifoQueue` via `__new__` and hand-sets what `__init__` would. **No test intent
    changes**, said plainly so this row is not misread as parity loss.
  - `test_refactor_skips_snapshot_search` pins the `"refactor "` demo-kludge skip, which lives in the
    search-and-ask block **7b** deletes. **Step 5 never touches it.**
  - The **source-text pin is invalidated twice** — 2b changes which resolver `push_job` calls, then
    6c stops it resolving at all. **Better said once here than discovered twice in the build.**
- **Both tiers baselined before touching shared code.** Tonight's lesson: I baselined
  `src/tests/unit` only, and `src/cosa/tests` caught 12 failures I did not know I had.
- **Pre-merge** (worktree, `:7999`): unit + cosa — **and the serial bridge guard**, which the
  project's merge gate requires **before** websocket smoke. 🔴 **The section states the through-path
  rule and then names no through-path test.** Naming one is owed.

### 🔴 VERIFICATION FOR STEPS 9–13 — the half this section did not cover

Everything above verifies steps 0 through 8. **Steps 9 through 13 are the guards, the door pair, the
disablement and the dump — the half that changes what a user and a caller experience — and nothing
here spoke to them.** *(Tiberius's cold read. The gap existed because those steps were written hours
after this section.)*

| step | what proves it, and what a vacuous version would look like |
|---|---|
| **9a** write guard | **A mode-forced answer is NOT written.** Set a mode, ask something the router would send elsewhere, assert **no snapshot row appears**. ⚠️ *Vacuous version*: asserting the guard function returns false. **Assert the absence of the row**, not the opinion of the guard |
| **9b** read guard | **An unconfirmed row is never served, not even once.** Seed a row with `answer_is_correct` unset, ask its exact question, assert **the agent RE-RAN** and the memory-problem line was spoken. ⚠️ *Vacuous version*: asserting the lookup returned nothing. **The guard is only real if the replay does not happen** |
| **9b** placement | **The admin search still sees unconfirmed rows.** Same seeded row, call `GET /api/admin/snapshots/search`, assert **it is listed**. This is the test that fails if someone "helpfully" moves the guard into `get_snapshots_by_question` |
| **10** `ask` / `submit` | **`submit` does not route**: assert the router was **never called**. **`submit` never parks a needs-input question**: assert no pending entry is created for a service account. ⚠️ Both are absence-assertions on purpose — presence-assertions pass whether or not the head was skipped |
| **10** `resume` untouched | `/api/v2/resume` still resumes a **parked disambiguation**, not a job. Its existing suite must stay green **and be REACHED** — the rule from the mode finding applies to itself here |
| **11** the sixteen | Each dying door returns **410**, its body **names `/api/v2/submit`**, and its body **contains `2026-12-31`**. Table-driven over all sixteen — one test per door, because a loop that silently covers fifteen is how door 8 stayed invisible for a day |
| **12** internal callers | Each of the six reaches `flow.submit()`. For **`job_persistence:830`**: a **boot-order test** that fails if the restore runs before the flow exists. ⚠️ **This is the precondition, not a nicety** — `main.py`'s construction order is unread |
| **13** the dump | The store is empty afterwards, **and** the next question writes a row that passes 9a. ⚠️ *Vacuous version*: asserting the table is empty. **Prove the cache still LEARNS** — an emptied cache that never refills looks identical to a working one for exactly one question |

**Every row above is an ABSENCE or a RE-RUN assertion, deliberately.** The defect this whole review
kept finding is a test that passes because the code it guards is no longer reached. *"Stays green"
proves nothing for any path the flow bypasses* — and the same trap is available to every one of these
steps.
- **Post-merge, post-bounce** (Mr Radio's sequencing): websocket smoke via
  `run-websocket-smoke-tests.sh` — **not** bare pytest over that directory — then E2E UI and
  integration on `:8000`.
- **Live check**: speak a todo, a math and a weather request; confirm the job card appears, the
  answer arrives over the websocket, and the CRUD agent handled the todo.

---

## ✅ Rick's ruling on the cache — 2026-08-20

> *"We should simply suck it up and unify caching in the same change, and the [brain's] cache takes
> lead, the other caches get removed. There's no reason that we cannot add the confirmation dialogue
> back — it should be a literal one-line call, correct?"*

**Adopted: one cache. The brain's lookup leads; stages 1 and 2 are removed.**

### On the one-line question — the CALL is one line, the FEATURE is not

The classifier really is one line, near enough:

```python
run_previous_best_snapshot = ConfirmationDialogue( spec, debug=…, verbose=… ).confirmed( question )
```

But confirmation is **a two-turn protocol spread across two separate calls to `push_job`**, not a
synchronous prompt:

| turn | what happens |
|---|---|
| 1 | near-match found → a **blocking object** is stored, `is_accepting_jobs()` goes false, user is asked |
| 2 | the user's next utterance re-enters `push_job`; because the queue is not accepting, it is read as the **answer**; `pop_blocking_object()` retrieves the snapshot and replays it |

So the machinery is the accept-state gate plus the blocking-object push/pop. The one-line call is
only the classifier on turn 2.

### ⇒ And that is good news, because the brain already has that protocol

`AskFlow` already parks a request, asks a question, and resumes on the answer:
`_needs_input` → `PendingRequests` → `AskFlow.resume()` → `POST /api/v2/resume`. That is the same
two-turn shape, built and tested.

**So the confirmation should be expressed as a needs-input park, not re-plumbed onto the blocking
object.** One protocol for "the system asked you something and is waiting", instead of two —
which is the single-source-of-truth goal applied to the ask/resume path as well as to routing.

⚠️ This raises the stakes on the `parked` rename (finding 5): the confirmation dialogue would
literally *be* a needs-input park, sitting beside `Outcome.status="parked"` meaning "queued for
execution." Do the rename **first**, as step 1.

---

### 🔴 THE KLUDGE IS ALREADY DEAD CODE — and the good mechanism is already live

**Rick, 2026-08-20**: *"When we rip out that ugly two-turn confirmation dialogue logic, the one call
to get the confirmation at the head of the processing chain should be using the notifications
synchronous `ask_yes_*` call, don't you think?"*

**Yes — and it already does. That call exists, is live, and is the only confirmation that ever runs.**

`todo_fifo_queue.py:636` already performs a synchronous yes/no through the notification server:

```python
request = NotificationRequest(
    message = msg, response_type = ResponseType.YES_NO, response_default = "no",
    timeout_seconds = 30, priority = "high", suppress_ding = True,
    target_user = user_email, sender_id = f"queue.{…}@lupin.deepily.ai" )
response = notify_user_sync( request, retry_on_timeout=True, max_attempts=3, backoff_multiplier=2.0 )
if response.status == "responded" and response.response_value == "yes": …
```

Self-contained, per-request, timeout with exponential backoff, safe default. **This is the design
Rick is asking for, already shipped.**

**And the two-turn path cannot fire.** Measured, not reasoned:

| check | result |
|---|---|
| production callers of `push_blocking_object` | **zero** — only its definition in `fifo_queue.py` and three test files |
| sites setting `_accepting_jobs = False` | **one** — `fifo_queue.py:108`, inside `push_blocking_object` |

Nothing in the running system ever arms a blocking object ⇒ `is_accepting_jobs()` never goes false
⇒ the `ConfirmationDialogue` branch at the head of `push_job` **never executes in production.**

⇒ **Step 5 is a DELETION, not a port.** Remove the `ConfirmationDialogue` branch, the
blocking-object push/pop, and the accept-state gate. No replacement needs building; the synchronous
ask at line 636 already carries the behaviour. My earlier "port it onto the needs-input park" idea
is withdrawn — it would have rebuilt something that is already there and already better.

**Refutable, and worth one check before deleting**: this rests on a static grep. If anything reaches
`push_blocking_object` dynamically (`getattr`, a plugin, a route not under `src/`), the branch is
live and this is wrong. Confirm with a runtime assertion or a log line before the delete lands.

### Name the kludge, on the way out — Rick's instruction

> *"That is one reason why I wanted to build this better. There should be some kind of comment in the
> code that that is a terrible ugly piece of engineering, a true kludge! So I will not be sad to see
> that piece of ugly plumbing ripped out."*

The replacement carries a note recording **what it replaced and why that was bad**, so nobody
reinvents it:

- the old mechanism spread ONE user interaction across TWO calls to `push_job`, coupling it to a
  queue-wide `is_accepting_jobs()` flag — so a pending confirmation **blocked the whole queue for
  every user**, not just the one being asked;
- it inferred meaning from position rather than identity: your next utterance was read as an answer
  because of the queue's state, not because anything tied it to the question;
- the state lived in a `pop_blocking_object()` side-channel, invisible to anything that did not
  already know to look.

The synchronous ask already in the file has none of those: it blocks only the request that asked,
the answer is bound to that call rather than to the queue's state, and it carries its own timeout
and safe default instead of waiting forever on a flag.

---

## 🔴 STEP 6 SILENTLY DELETES USER MODE — the largest finding of the review

**Cheech found this while checking a rebuttal of mine that was wrong. Both halves stand.**

### First, my error, because it is what led to the find

I told him route-first makes the 27 `TodoListAgent` rows cold, the 8 exact hits included, because todo
forks to CRUD and skips the cache. **That is wrong.** The skip keys on the command the question
ROUTED to. It protects a *todo-intent* question from reading the cache. It does nothing about a
*non-todo* question matching a *todo* snapshot — the lookup still runs, tier 1 still exact-matches at
100.0, and the row still replays. Rachel's 8 hits came from exactly that: non-CRUD utterances
returning todo answers.

I had stated this direction correctly to Cheech an hour earlier — *"the residual is the OPPOSITE
direction, a legitimate READ matching a legacy row"* — and then contradicted myself when it was
convenient. **The exposure is not closed by the reorder. Any mechanism chosen must survive the
8-exact-hits test.**

### Second, the bigger one

**`AskFlow` has no concept of user mode.** Verified:

| check | result |
|---|---|
| `get_user_mode` / `MODE_TO_AGENT` / `AGENTIC_MODE_MAP` in `src/cosa/rest/v2/` | **none** — the only v2 hit is a prose comment at `registry.py:8` |
| `AskFlow.run()` signature | `question, user_id, user_email, session_id, websocket_id, speak, interactive` — **no mode parameter** |
| where the machinery lives | `todo_fifo_queue.py` and `routers/mode.py` only |

`todo_fifo_queue.py:706-707` checks the user's mode **before** LLM routing and dispatches through
`AGENTIC_MODE_MAP` (8 agentic modes) or `MODE_TO_AGENT` (the direct agents), with a documented
disjointness invariant and a test guarding it.

**Step 6 wires `push_job` to `AskFlow.run()`.** A user who has selected todo mode and asks a general
question is sent to the todo agent today, by design. After step 6 they would be routed by content
instead. **A live product feature disappears, and nothing fails loudly when it goes.**

### 🔴 The verification section would have certified the loss

The plan already demands `test_mode_management` and `test_todo_fifo_queue_mode_routing` stay
**green unchanged** — as evidence of behaviour parity. But those tests exercise `todo_fifo_queue`
directly, and the mode branch stays in the file; it is merely never REACHED once `push_job` routes
through the flow. **They would pass while the feature was dead.** Green tests, dead feature — the
plan promised parity on a feature the sequence silently drops, and its own proof would have agreed.

⇒ **This is why "must stay green" is not a control unless something proves the path still runs.**

### 🔗 Mode routing is ALSO the pump — the same feature on both ends

Cheech's connection, verified: the mode branch sets `command = f"agent router go to {user_mode}"`
(`todo_fifo_queue.py:726-729`) and falls straight into the same `resolve( command )` agent build and
run as any other command — which then snapshots. **So a user in todo mode asking *"what's the best
way to paint a room evenly?"* is routed to the todo agent, answers, and the answer is written to the
cache under that non-todo question.**

That is the fingerprint on Rachel's rows: questions that read nothing like todo requests, stored
against `TodoListAgent`. They were never routed by content — mode put them there. **The same feature
is the source of the bad rows AND the capability step 6 drops**, which is why it ranks above the
guard we were arguing about.

⚠️ **One part does NOT add up, and I am flagging it rather than completing the story.** With
`crud for dataframes agents enabled = true` (the only setting in the INI, `:1909`), todo mode should
resolve to `TodoCrudAgent` — which is never snapshotted. So a clean mode-routing pump should have
produced NO todo rows at all. Yet 27 exist under `TodoListAgent`.

Possible: they predate the CRUD fork, or predate `resolve_voice` (built 2026-08-20), or the flag
differed when they were written. **I am not picking one.** The rows carry `created_date` and
`run_date` — read them against the fork's commit date and the answer is a fact rather than a story.
**Owed as a measurement, not an inference.** A tidy explanation that is not checked is how three of
my claims went wrong earlier tonight.

### ✅ What was owed ahead of the cache guard — DISCHARGED 2026-08-21 (mode ruled; steps 9a/9b)

Mode routing changes **which agent answers**, not merely which cached row is served. It ranks above
the argument that surfaced it.

1. **A ruling from Rick**: does the flow learn about modes, or does `push_job` keep resolving mode
   BEFORE calling the flow and pass the resolved command down? Not mine to choose — it decides
   whether the flow becomes mode-aware or stays a content router.
2. Whatever lands, a test that fails when a mode-selected user is routed by content instead.
3. Step 6c does not ship until 1 and 2 exist.

**Nothing in the sequence addresses this today. It is the plan's biggest hole and it was invisible
until someone checked a claim of mine that was wrong.**

---

## ✅ Rick's refinement — the receptionist IS the default, and the failure must be SPOKEN

> *"If we wrap the routing call correctly, perhaps we could give it a good default go-to string that
> explains that it just had a routing error and that for the moment we're going to leave you with
> the default, which is the receptionist — who is always going to be our default."*

**Both premises confirmed against the code.**

1. **The receptionist is already the universal default.** Six call sites in `flow.py`, one per
   failure mode: `replay_error` `:102`, `router_error` `:110`, `unknown_command` `:113` and `:185`,
   `extract_error` `:124`, `agent_error` `:247`. There is no failure path that lands anywhere else.
2. **Falling back to it is the right behaviour**, and it does not contradict *"fail loudly and
   bail."* Loud is about the **user hearing what happened**; bail is about **not routing around it**
   with a cached answer. The receptionist is where you land after saying so, not instead of saying
   so.

### 🔴 And the refinement exposes a gap that is live today

The flow already HAS the mechanism. `_receptionist` takes `primary_error` — *"the FAILURE THAT
CAUSED THE DEGRADE"* — and its own docstring states the principle exactly:

> *"a fallback that hides the fault it was reached by."*

**Two things break that promise:**

| # | gap | evidence |
|---|---|---|
| 1 | `router_error` **never passes `primary_error`** | `flow.py:110` — `self._receptionist( trace, question, ctx, "router_error" )`, no cause. Only `agent_error` at `:247` passes one. |
| 2 | even when passed, the cause is **never spoken** | `_finish` calls `_speak( trace, outcome.answer, … )` — the *receptionist's* answer. `primary_error` goes through `_compose_error` into the emitted `error` field of the response dict. **A JSON field Rick cannot hear.** |

So a routing failure today runs the receptionist, speaks its generic reply, and files the real cause
where only a log reader finds it. **Loud in the payload, silent in the room** — the same shape as
the bare-pytest warning noted earlier in this plan, where the guard did not reach the reader it
existed for.

### What lands

- **Wrap the routing call**; on failure pass a `primary_error` that names it.
- 🔴 **AND the replay branch — `flow.py:102` calls the receptionist with NO `primary_error`.** Rick's
  fail-loud ruling covers **all six** degrade paths, so the `primary_error` wiring and the spoken
  failure extend there too. **14 of the 42 replay errors are unexplained purely because that text was
  thrown away.** This correction sat 630 lines away from the line it corrects for two folds.
- **The receptionist gets a go-to string that says so out loud** — a routing problem happened, and
  you are being handed to the receptionist meanwhile. Rick's words, and it is the part that makes
  "loudly" true rather than nominal.
- **No cache fallback beneath a broken router** — the earlier ruling, unchanged.
- Pinned by a test that a router failure produces a SPOKEN payload naming the routing failure, red
  on revert. A test asserting only the `error` field would pass while the room stayed quiet.

### ✅ CLOSED — Rick confirmed the narrow reading, 2026-08-20

I flagged that *"routing cannot fail"* might also be a verdict on the pre-existing
receptionist-degrades-on-`router_error` path, and refused to implement that on my own inference.
**It is not.** Rick:

> *"Yes, let the receptionist handle that. She is supposed to provide some kind of scintillating —
> or at the very least understanding — conversation after an error happens, besides all the other
> features the receptionist brings to the game later."*

**The receptionist stays, and handling the error conversationally is her JOB, not a consolation
prize.** That settles the design intent behind all six degrade paths, not just this one: the
fallback is meant to talk to the user like a person about what went wrong. It also raises the bar on
the go-to string — it should read as someone explaining a problem, not as an error code with
punctuation.

⇒ **No open questions remain on router failure.** Fail loudly = say it out loud, in her voice;
bail = do not let a cached answer stand in for a broken router.

---

## ✅ Rick's ruling on router failure — 2026-08-20 (closes the item flagged one pass earlier)

> *"If you've got a routing problem you've got problems, so fail loudly and bail. Routing cannot
> fail."*

**Answers the question exactly as it was asked.** After the reorder a router failure precedes any
cache hit, and I asked whether an exact repeat should be allowed to rescue it. **No.** A routing
failure is not something to route around — it fails loudly and bails. Letting a cached answer paper
over a broken router hides the fault behind a correct-looking reply, which is the shape of bug this
whole review keeps finding.

**Applies to**: the `router_error` branch at `flow.py:107`, and any future temptation to add a
cache-fallback beneath it.

✅ **Scope settled.** I recorded the wider reading — that *"routing cannot fail"* might condemn the
existing receptionist degrade path — as an open question rather than acting on my inference. Rick
answered it directly: the receptionist stays and handling the error is her job. See the confirmation
below.

---

## 📊 The legacy-row count came back — and it is not zero

Cheech's reviewers measured what I refused to guess at. **`lupin_db_test` holds 28 CRUD-backed
snapshots — 44.4% of that store.** Written before `running_fifo_queue:291` began excluding
`CrudForDataFramesAgent`; the write side is closed now, so no new ones are being made.

~~**Rachel is checking whether any store beyond dev and test carries them.** That answer is owed
before this is treated as bounded.~~

✅ **MOOT 2026-08-21 — the whole cache is dumped (step 13), so no population needs bounding.** Rachel's
check is no longer owed, in any store. *If she already ran it, the number is worth keeping as a record
of what the dump removed — but nothing waits on it.*

### Cheech's caveat, and it is the right one

> *Existing is not the same as REACHABLE. A CRUD snapshot only hurts if a plausible non-CRUD
> utterance actually scores above the floor against it.*

44.4% is the size of the population, **not** the size of the exposure. The exposure is however many
of those 28 a realistic spoken question would match above the floor — and nobody has run that yet.
**Recorded as a number that is real and not yet actionable**, which is the honest state of it. The
measurement owed is: for each of the 28, does any plausible non-CRUD utterance score above
`similarity threshold confirmation`?

**Also measured, and it supersedes the estimate in the ruling above**: route-first adds roughly
**22 ms** to a path whose cheapest observed end-to-end was over **3 seconds**. Rick accepted the
cost at an assumed ~100 ms; the real figure is smaller, so the ruling holds with more margin than it
was given.

---

## ✅ Rick's ruling on cache-vs-router order — 2026-08-20 (closes the ordering hole)

> *"Currently a router costs approximately 100 milliseconds. I'm happy to pay that cost if it makes
> everything read more easily and doesn't hide a potential bug. So I say route first and then check
> the cache."*

**`AskFlow.run()` reorders: router → resolve → cache.** The flow then knows the command before it
looks anything up, and applies the same skip `running_fifo_queue:291` applies today — **CRUD
commands do not read the cache at all**, because the data behind them is mutable.

### The hole this closes

`flow.py:93` made cache step 1 and the router step 2, so there was no `command` at lookup time and
the flow **structurally could not** do what the queue does. Dormant today (CRUD results are never
snapshotted, and tier 1 needs an exact string match) — **live the moment 6a lands**, because gist
matching is loose about wording by design. *"Put milk on my todo list"* would gist-match an earlier
*"what's on my todo list"*, the read replays, the write never runs, and nothing errors.

### Why Rick's option beat mine, on a point I missed

I recommended keeping cache-first and refusing to replay when the **matched snapshot's**
`routing_command` forks to CRUD. Cheech's reviewers then found what that leans on:

| evidence | reading |
|---|---|
| `vector_store_models.py:279` | the column is `Mapped[Optional[str]]` — **nullable** |
| `solution_snapshot.py:177` | the constructor defaults `routing_command=""` |
| `cache.py:252` | the write path coerces with `or ""` |

So `resolve("")` returns `None`, my guard finds no `crud_factory`, and it **allows the replay** — it
fails OPEN on exactly the rows whose provenance is unknown, which is the worst direction for a guard
to fail. My clever option was load-bearing on a nullable field. **Rick's simple one is not**: it
reads the command of the question being asked *now*, which always exists, so the nullable column
never enters the decision.

⇒ Recorded because the reasoning matters more than the outcome: *the option that needed a caveat
was the one that had a hole in it.*

### What changes

- **`flow.run()`**: router and `resolve()` move above the cache lookup. `_needs_input` resume
  (`:182`) already resolves before doing anything, so it is unaffected.
- **CRUD skip in the flow**, derived from the registry — **NOT a hardcoded "todo or calendar"**
  (Cheech's correction). The condition is `spec.crud_factory is not None and crud_enabled`, so a
  seventh CRUD command later is one `AgentSpec` row and no edit here. Same argument that killed the
  ten-branch ladder in `push_job`.
- **Cost**: ~100ms of routing on every request, including exact repeats, accepted by Rick above.
- **A `router_error` now precedes any cache hit.** ✅ **RULED** — see the router-failure section
  above. Fail loudly and bail; no cache fallback beneath a broken router.

### Sequenced as its own commit, BEFORE 6a

The flow is already live behind `/api/v2/ask`, so this reorder is observable there the moment it
lands — it is not a free refactor and does not belong bundled with the gist tier.

---

## ✅ Rick's ruling on the confirmation prompt — 2026-08-20 (closes cascade findings 6–8)

**The brain gets the near-match ask.** `AskFlow` gains a branch: when the similarity score is high
but not exact, it asks the user the same question the voice path asks today, and replays only on a
yes.

### The contradiction Rachel found — two of my own steps, each defensible alone

| step | what it said | why it looked fine |
|---|---|---|
| **5** | delete the two-turn `ConfirmationDialogue` | *"nothing replaces it — the synchronous yes/no at `todo_fifo_queue.py:636` already IS the live confirmation"* |
| **7** | remove cache stages 1 and 2 | Rick's ruling: the brain's cache leads, the others go |

**`:636` is inside the branch step 7 deletes** — `elif best_score >= threshold_confirmation:` at
`todo_fifo_queue.py:616` **is** cache stage 2. Step 5 justified itself by pointing at something step
7 throws away. Net effect of the two: **no confirmation anywhere on the voice path.** The prompt at
stake — *"Is that the same as: {earlier question}?"*, 30s timeout, defaults to no — is one Rick
hears; live default is on at 90% (`similarity threshold confirmation = 90.0`), off only on the test
server.

### 🔴 THE PART THE REVIEW DID NOT NAME — the two stages are COUPLED, and deleting one silently arms the other

`running_fifo_queue.py` carries its **own** copy of the threshold logic, at **`:313`**
(`_process_job`) and **`:953`** (`_process_fast_lane`), keyed off the same
`self.threshold_confirmation` at `:103`. Above the floor it does **not** ask — it accepts. The
comment at `:314` says why, in as many words:

> `# Above floor — accept (push_job already handled user confirmation)`

**That downstream accept is safe ONLY because the upstream ask happened.** Remove stage 2 from
`todo_fifo_queue` without touching `running_fifo_queue`, and a 90-to-99% match replays a cached
answer to a question nobody confirmed — the exact silent wrong-but-close replay I told Rick we would
never have, arriving as a side effect of a deletion rather than a decision.

⚠️ **This also corrects something I told Rick.** When he ruled on the gist tier an hour earlier, I
argued against threshold-replay on the grounds that a tuned float silently replaying a wrong answer
is worse than a question you can say no to. True — but I implied no such path exists today. **One
does**, at `running_fifo_queue:313` and `:953`. It is confirmed-by-proxy rather than unconfirmed,
and that proxy is what step 7 was about to remove.

### What this changes in the plan

- The near-match ask lands **in `AskFlow`, in step 6**, with the wiring and the gist tier — live
  **before** step 7 removes anything.
- **Step 7 must remove BOTH copies**: `todo_fifo_queue`'s ask AND `running_fifo_queue`'s
  accept-above-floor at `:313`/`:953`. Leaving the second is not "keeping a fallback," it is keeping
  an unconfirmed replay.
- The Files table named `running_fifo_queue.py` as *"only if the cache ruling touches stage 2."*
  **It does touch it, in two places.** Corrected below.

### Exclusion mismatch — same review, and tonight's ruling created it

`running_fifo_queue`'s write-back excludes **three** classes, not two:

```python
serialize_snapshot = ( not isinstance( running_job, ReceptionistAgent ) and
                       not isinstance( running_job, WeatherAgent ) and
                       not isinstance( running_job, CrudForDataFramesAgent ) )
```

My write-back section named only the first two and called it *"the same idea as v2's `snapshotable`
flag."* It is not the same: v2's registry marks todo and calendar **`snapshotable=True`**, and under
tonight's single-CRUD-aware-resolver ruling those commands now resolve to **CRUD agents** — which
the queue refuses to serialize. Two sources of truth that now disagree by construction.

Runtime is protected twice over (the queue's `isinstance` check wins, and `_maybe_write_back` never
fires for queued work), so **nothing is broken today** — but a flag that says "cache this" about a
class the writer refuses to cache is a trap for the next reader. **Fix: snapshotability travels with
the fork.** When the resolver applies the CRUD fork it returns `snapshotable=False`, and the
registry stops claiming otherwise. This is the second-order half of the fork I flagged at the start
of the walkthrough and then dropped when the fork collapsed to one resolver.

---

## ✅ Rick's ruling on the gist tier — 2026-08-20 (closes cascade finding 1)

**The brain absorbs the gist tier.** Port the old Level-3 gist match into `V2Cache` as a middle
tier: **exact match on a normalized gist**, sitting between tier 1 (exact verbatim/normalized) and
tier 2 (vector similarity).

**Why this and not the other two.** It stays **lossless** — a string match on a normalized gist, not
a float compared against a threshold — so there is no number to tune and no near-miss can replay the
wrong answer. Rick's caching ruling still holds unbroken: there is still exactly ONE cache, the
brain's, and the other stages still go. And it preserves what the voice path does today, which was
the whole point of the finding.

**What the finding actually was.** The old voice path had FOUR ways to match; the brain has two.

| | old voice path | brain's cache, before this ruling |
|---|---|---|
| exact, verbatim | ✅ | ✅ tier 1 |
| exact, normalized | ✅ | ✅ tier 1 |
| **exact on the gist** | ✅ Level 3 | ❌ **the gap** |
| fuzzy vector similarity | ✅ Level 4 | ⚠️ tier 2 — recorded, never replayed |

Cheech's reviewers were right that *"removing stages 1 and 2 removes the only thing catching
gist-equal rewordings."* Ask the same question in different words and, without this tier, you wait
for a full agent run instead of an instant replay.

**Rejected: letting tier 2 replay above a threshold.** The machinery is already there — every
request is embedded and its nearest match recorded. It was built record-only for an explicitly
stated reason (`cache.py:21`): *"never a float comparison against an ANN score, which understates
the cache-hit rate the experiment exists to measure."* That reason died with the reporting pin — but
the option still loses on its own merits, because it makes a wrong-but-close replay possible and
puts a tuned number on the live path.

**Implementation note**: write-back already computes `question_gist`
(`cache.py:195` — `snapshot.question_gist = self._gist_normalizer.get_normalized_gist(...)`), so the
data this tier reads is already being written. This is a read-side addition, not a schema change.

**Lands in sequence step 6** — with the wiring, BEFORE the old stages are removed in step 7, so the
gist path is live before its predecessor goes away.

---

## ✅ Rick's ruling on the reporting pin — 2026-08-20 (SUPERSEDES finding 2's fix)

> *"It looks like you're bending over backwards to accommodate the reporting component. Pull that
> shit out of there. It was just an experiment and it was just a test of latency… You're making an
> insane amount of effort to work around that reporting component. Pull it out. Move it somewhere
> else. But do not give it that much of your attention."*

**Adopted, and it collapses finding 2 rather than solving it.**

Everything about the two-resolver split existed to protect one thing: cache-hit *reporting*. R-A3
pinned `spec.factory` to the non-CRUD class so a v2 report would not read 0% cache-hit on todo and
calendar. That pin is what forced `resolve_voice()` into existence, and it is what would have forced
the flow to learn which surface it was serving. **The constraint was the experiment's, and the
experiment does not get to shape the table every request routes through.**

**What changes**

| | before | after |
|---|---|---|
| resolvers | `resolve()` non-CRUD + `resolve_voice()` CRUD-aware | **one** CRUD-aware resolver |
| who applies the fork | one call site, `todo_fifo_queue.py:769` | the resolver itself — unbypassable |
| the R-A3 pin | a rule in `registry.py:126` | **deleted**, with the reason recorded here |
| CRUD exclusion for reports | baked into routing | **moved to the reader** — whatever counts cache hits filters there |

**Why this is the better shape and not just the cheaper one**: with one resolver, a future caller
*cannot* reach the wrong class. The three-option walkthrough I put to Rick was, in his reading,
three ways to keep paying rent on a latency experiment — and he is right that the option I
recommended (thread a surface flag through the flow) was the most expensive of the three.

**Scope check — what is NOT being deleted.** `/api/v2/ask` and `/api/v2/resume` stay; they are the
flow's API surface, not the reporting. *"Do not give it that much of your attention"* is a ruling
about influence, not a delete order. Nothing measured gets thrown away — the exclusion moves to the
report.

**Consequence for the registry comment at `:126`**: it currently argues FOR the pin. It gets
replaced by one line saying the fork is applied at resolve time and reports exclude CRUD at read
time, so the next reader does not reinstate the pin from first principles.

---

## ✅ Rick's ruling on write-back — 2026-08-20

> *"Reuse the path that already exists when it comes to write-back at the end of successful
> execution in the running FIFO queue."*

**Adopted.** `running_fifo_queue._handle_base_agent` already calls `snapshot_mgr.save_snapshot()`
on success, with its own exclusion list (ReceptionistAgent and WeatherAgent are not serialized —
the same idea as v2's `snapshotable` flag). Nothing new gets built; `_maybe_write_back` simply never
fires for a `waiting` outcome, which its existing guard already ensures.

Closes decision row `741670a2` once I am out of plan mode.

### ✅ CHECKED — the write and the read DO touch the same store. Ruling is safe.

I raised an alarm that the queue might write to files while the brain reads Postgres. **It was a
false positive and I am retracting it.** Chain, read at HEAD:

| step | evidence |
|---|---|
| `main.py:706` | **raises `ValueError`** for any manager type but `postgres` — *"the only supported value is 'postgres'"*; the file-based branch is commented out |
| `main.py:712` | builds `snapshot_mgr` from the factory and injects it into the queues |
| `PostgresSolutionManager.save_snapshot()` | upserts via **`SolutionSnapshotRepository`**, and updates canonical synonyms |
| `V2Cache` → `TwoTierQuestionSearch` | reads **`SolutionSnapshotRepository`** (tier 2) and **`CanonicalSynonymRepository`** (tier 1) |

Same repositories, both directions. The queue's completion write is visible to the brain's cache,
including the tier-1 exact-match path. **Rick's reuse ruling holds as given.**

**Why I got it wrong, because the cause is the legibility problem he suspected.** There are THREE
classes carrying a `save_snapshot` method:

- `SolutionSnapshotManager` — **deprecated 2025.09.17**, file-based, lives in the most obvious file
  name (`solution_snapshot_mgr.py`), and its docstring says *"Save snapshot to file-based storage"*;
- `FileBasedSolutionManager` — the factory's file backend, **unreachable in production**;
- `PostgresSolutionManager` — the only one the app can actually build.

A grep for `def save_snapshot` finds the deprecated one first. I read its docstring and believed it.
**The deprecated class is a live trap for exactly this misreading** — and it is dead in production,
since `main.py` refuses to build anything but Postgres.

### STEP 0 — close the trap first (Rick asked for a concrete path)

**It is dead code, and it is broken code.** Verified caller by caller:

| site | state |
|---|---|
| `main.py:706` | **raises** for any manager type but `postgres` — the file backend cannot be built |
| `dependencies/config.py:61` `get_snapshot_manager()` | constructs `SolutionSnapshotManager()` **with no `path`**, which its `__init__` requires ⇒ **`TypeError` if ever called** |
| `routers/admin.py:34` | defines its OWN `get_snapshot_manager()` that returns **main's Postgres singleton** — docstring says "to ensure cache consistency". It **shadows** the broken one. |
| `routers/system.py:22,24` | **imports** the broken dependency and the deprecated class, **never calls either** |
| everything else | tests only |

⇒ **No production path reaches the deprecated manager.** The one production function that would
build it is itself unreachable, and would crash if reached.

**The path, smallest first — each its own commit, each proven red on revert:**

1. **Delete `get_snapshot_manager()` from `dependencies/config.py`** and its `_snapshot_mgr` global.
   It is unreachable and would `TypeError`. Its tests in `test_dependencies_config.py` go with it —
   they currently assert the behaviour of a function that cannot work.
2. **Drop the two unused imports in `routers/system.py`** (lines 22, 24).
3. **Make `SolutionSnapshotManager.__init__` RAISE** instead of emitting a `DeprecationWarning`
   nobody sees — message naming `SolutionSnapshotManagerFactory.create_manager("postgres", …)`.
   ⚠️ This reddens `test_solution_snapshot_mgr.py`, which constructs it ~10 times. **Those tests
   test a class the app forbids**: delete the file with the class, or keep exactly one test that
   asserts construction now raises.
4. **Then decide on the class itself** — deleting it and `FileBasedSolutionManager` is the honest
   end state, but the factory still advertises a `"file_based"` branch, so that is a separate call
   about whether swappable backends are still wanted at all. **Rick's, not mine.**

**Why step 0 and not "later"**: this plan makes the brain's cache the single source of truth for
lookups while the queue writes at completion. Anyone verifying that chain will grep
`def save_snapshot`, land on the deprecated file-based one first, and reach the same wrong
conclusion I did — that reads and writes go to different stores. **The trap fires hardest on
exactly the work this plan schedules next.**

**Nothing gets built against an assumed answer.**
