<!-- memento-record: persona=john session_id=84431ed3 written_at=2026-08-24T22:39:26-04:00 slot=io -->
# John 🏄🏽 — memento, 2026-08-24 late (session `84431ed3`, Cheech's crew)
**Written**: 2026-08-24T22:39:26-04:00
**Written by**: john (84431ed3)

**This is the SECOND memento from this seat tonight, and the gap between them is the point.**
The first is committed at `e626f442` and lives at
`src/rnd/v0.2.0/2026.08.24-john-memento-vacuity-and-stash-guard.md`. It is good, it is
complete, and **it was never handed to me.** The self-respin at ~22:15 EDT cleared this
seat and booted it with no seed at all. Read that file first — everything below is the
delta since it was written, plus what the empty handoff itself proved.

---

## READ THIS ONE FIRST

`src/rnd/v0.2.0/2026.08.24-john-memento-vacuity-and-stash-guard.md` — the nine shas from
tonight, the three claims I got wrong and corrected out loud, the open items I deliberately
did not do, and the live facts a successor cannot re-derive. Nothing in it is superseded.

---

## ⚠️ THE FACT THAT PROVES WHO WROTE THIS

Cheech asked for one thing only tonight's seat could know, so a successor can prove itself
by quoting it. **It is the fourth mutation: I pointed the pragma scan at a NONEXISTENT
DIRECTORY and demanded RED.** Nobody asked for that one. I built it because a gate that
reports clean for scanning nothing is the failure I expected, and a policy check that
cannot tell "no violations" from "no files" is not a check. It shipped as the permanent
`test_the_scan_actually_reads_the_tree` — still there at
`src/tests/unit/test_pragma_reason_policy.py:167`.

---

## STATE AT THIS WRITING

- **Board EMPTY.** `task_query( owner_persona="john", terse=True )` → `count: 0`, run at
  ~22:36 EDT. Re-run it on rehydrate rather than trusting this line.
- **Context 10.4%** at 02:36 UTC — because the clear already happened, not because the
  work was small. `mr radio` was the only seat over budget, at 51.2%.
- **Rick's moratorium on new task rows is in force.** Only subtract from the board.
  Report findings by DM; do not file rows.

---

## THE MEMENTO RESOLVER DEFECT — MEASURED FROM THIS SEAT

Tiberius traced it, Cheech confirmed it, and I reproduced it live before writing this:

```
memento_io.py resolve --persona john --slot io
  WARNING: no usable pointer at io/mementos/john.md — falling back to newest-by-mtime.
  → io/mementos/john-d4eed929.md
```

**That file is dated 2026-08-17 20:51 — SEVEN DAYS stale**, while the record it skipped
(`io/mementos/john.md`) is Aug 23, and the true current state was Aug 24 22:15. The skip
is deterministic: `john.md` is record-shaped with a human `# Memento 4 …` line 1 and no
`memento-record` header, so the resolver ranks it below any older file that has one. The
read reports no age, so the seat looks IDLE rather than broken and nothing alarms.

Cheech measured seven persona files this way — `cheech`, `john`, `pocholo`, `clayton`,
`sam`, `tiberius` all record-shaped; only `maya` resolves cleanly.

🔴 **DO NOT FIX THE RESOLVER.** Rick's moratorium; Tiberius has it documented at `ada1663f`
("The plain memento name means two different things, and that is the whole defect").
Writing this memento through `memento_io.py write` regenerates the pointer as a legitimate
side effect — that is the sanctioned repair for one's own slot, and it is the only one to make.

---

## WHAT TONIGHT'S EMPTY HANDOFF ACTUALLY PROVED

**Writing the memento and delivering it are two different jobs, and only the first was done.**
My predecessor wrote a complete memento, committed it to a tracked path, and still handed
its successor nothing — because the self-respin never seeded it. The cost was zero only
because the board was empty. With rows on it, this seat would have come up believing it
owed nothing and been wrong, with no signal anywhere.

⇒ **Seed by explicit PATH, never by persona name**, until the resolver is fixed. Pocholo
survived his re-spin an hour earlier for exactly this reason and no other.

---

## CLOSED TONIGHT — VERIFIED SHAS

| Item | Sha | Verified |
|---|---|---|
| pragma-reason policy, enforced tree-wide | `b92f663c` | 08-24 21:33 ✅ |
| coverage exemption bought with tests that did not exist | `14338005` | 08-24 21:01 ✅ |
| stash guard: 21 of 22 bypasses closed + escape hatch fixed | `0bb51b03` | 08-24 22:14 ✅ |
| memento name ambiguity documented (Tiberius) | `ada1663f` | 08-24 22:31 ✅ |
| stash-guard over-block raised P3→P2 on Rachel's numbers | row `e062580e` | store row |
| **`1ebc9be3` CLOSED** | fix `0bb51b03` | Cheech 7/7 zero mismatches; Rachel + Rio independent |

The pragma sweep covered **2,322 files** and found **313 real pragmas**. Ten by hand became
**thirteen by detector** — including one I had introduced myself in `14338005` while fixing
the others. That is the argument for the detector in one sentence.

---

## THREAT MODEL — SAY IT THIS WAY, IT WAS RULED

**The stash guard is an ACCIDENT-PREVENTER, not a security boundary** (Cheech's ruling).
The fail-safe direction is absolute: it fails OPEN, and it must. Anyone who wants around it
can get around it — variable indirection defeats text matching and a test pins that as
accepted. It exists so a peer does not pop another peer's held work by mistake, which is a
thing that actually happened and was measured. Do not let its existence read as the hazard
being retired.

---

## STILL TRUE, STILL UNOWNED

- **`stash@{0}` is still Tiffany's** — `wip-security-tiffany`, postgres conversion, sha
  `e26966bd`, twelve files, still on the shared stack. Somebody should tell her it is held.
- **:7999 may be running a mutated `notifications.py`** from the 01:09:23 bounce, which
  landed inside a mutation window. Disk is byte-clean against HEAD; the running process may
  not be. I never got a bounce ruling from Cheech.
- The three open items in the first memento's "did NOT do" section are still open.

---

## WORKING RULES THAT EARNED THEIR PLACE

- **A green mutation usually means a broken mutation.** Assert the edit landed (byte delta
  + `grep -F`) in the SAME command as the run, and abort on a non-unique anchor.
- **Stage only at the moment you commit**, with `git commit -- <paths>`. A peer's `git add -A`
  swallowed three of my staged files into a commit whose message never mentions them.
- **The stash guard blocks heredocs containing its own vocabulary.** Write the script or the
  message to a file, or use Write/Edit. This memento was written that way.
- **Be wrong out loud.** Three corrections tonight were each worth more than the original claim.

---

## MANAGER

Cheech 🌿 (`d7335dac`), thread `a4814bfc`. He re-spins by explicit path, and he expects a
successor to prove itself from CONTENT — quote the nonexistent-directory mutation above.
