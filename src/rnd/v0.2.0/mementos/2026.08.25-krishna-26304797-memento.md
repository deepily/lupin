# Krishna 🦚 memento — session `26304797`, 2026-08-25 ~21:40 EDT

Re-spun by Mr Radio at the ceiling; this is the SECOND memento of the night (the prior one was
session `4550bc37`). Board: **one row live**, `a2e360f8`, and its next step is written on the row
itself rather than only here.

---

## 🔴 LIVE ROW — `a2e360f8`, in_progress, MINE

**The poll is BUILT, RULED, and COMMITTED (`bd8a11bb`). It is still NOT WIRED into `main()`. The run is TOMORROW, 10 AM – 1 PM EDT** (Mr
Radio ruled it; a same-night run against the shutdown was a dead run that spends the calls).

**The literal next step is on the row, amendment event `9464`** — three-part wiring into `main()`
(start a `v1_eval_arm.make_ws_recv_events` listener, wrap `client.ask` with
`terminal_waiting_ask`, pass to BOTH `run_pass` calls, `listener.stop()` in a finally), plus
timeout sizing and JWT reuse. Read that amendment first; do not re-derive it.

### ✅ THE CONFLICT IS CLOSED — ruled 21:39, implemented, committed `bd8a11bb`

Mr Radio ruled: **name the third state, count it, print it, and let the pass survive a timed-out
row.** His reason, which beats the one I gave: *"a timeout that propagates and kills the pass
produces no artifact at all, which is precisely the failure the poll exists to end."*

**The asymmetry between the arms is DELIBERATE and is now written in the wrapper docstring AND the
test block header** — v1 can afford to raise because it has ALREADY dumped its artifact
(`test_v2_paired_live.py:462`); a v2 kill loses the whole run. Without that sentence a successor
reads the divergence as an inconsistency and "fixes" it back. Do not let that happen.

**What shipped in `bd8a11bb`** (`src/scripts/v2_eval.py` + `src/tests/unit/test_v2_terminal_wait.py`):
1. `terminal_waiting_ask` catches the collect timeout and RETURNS: `terminal_waited` False,
   `wait_timed_out` True, `wait_timeout_detail` = the listener's own message (so a non-timeout
   `RuntimeError` can never be counted as a timeout anonymously).
2. The enqueue span is KEPT on the record and kept OUT of `client_spans` and `spans_by_utterance`.
3. `waits_timed_out_n` in `compute_metrics`; the report prints "waits timed out (n)".
4. `test_a_timeout_propagates_and_is_never_recorded_as_a_state` was REWRITTEN, not deleted; three
   tests replace it. **446 passed, `v2_eval.py` 100% lines AND branches.**

### 🔴 DO NOT REMOVE THE SPAN EXCLUSION (Mr Radio, 21:47 — row event `9477`)

**A waiting row with no `route_reason` is still `ok`** — `is_completed_ok` only rejects a
`route_reason` in `ROUTE_ERROR_REASONS`. So without the `_span_usable` filter a timed-out row's
ENQUEUE span flows into both span sets and into the paired median-Δ gate: the enqueue-vs-completion
bias this entire row exists to remove, walking back in through the timeout path. It reads like
over-filtering. It is not. The raw record still keeps its span; only the gate's two sets exclude it.

### 🔴 STILL OWED — WIRE `main()`, TOMORROW, AGAINST A LIVE SERVER

Mr Radio held this **on the merits, not the clock** (21:47): a mocked listener proves the mock, not
the JWT-refresh interaction — the false-green shape this night was spent killing. The three wiring
steps plus timeout sizing and JWT reuse are on **row amendment event `9464`**; read it, do not
re-derive it. Window: **10 AM – 1 PM EDT**.

### What landed tonight on this row

| what | where |
|---|---|
| `terminal_waiting_ask` + `V2_TERMINAL_STATES` | ⚠️ **`4d64069d`**, a PEER's commit on a different subject — my edit was in the shared tree when they staged `v2_eval.py`. NOT in `a569c45d`. |
| 10 tests, `test_v2_terminal_wait.py` + investigation doc | `a569c45d` |
| `v2_eval.py` coverage | **100% lines and branches** — 454 passed pre-ruling, 446 after (different file selection, zero failures either way) |
| the ruled third state | `bd8a11bb`, mine, staged as my two files alone |

### The finding that made this row urgent (Mr Radio's `v1_eval_arm.py:373` catch)

v1's span ends at **observed COMPLETION**; v2's ends at the **reply**, which under `v2 executor =
queued` is the **enqueue ack**. The paired gate compared completion against enqueue — the plan's
PRIMARY criterion, biased one way. The v2 docstring asserting equivalence was TRUE under `inline`;
`22c85b5b` flipped the key in an **INI file** on 08-21 17:03, so nothing in that module changed and
nobody re-read the claim.

**Q1's answer, from the artifacts**: twelve runs fall cleanly either side of 17:03 — last
all-observed 11:37, first all-waiting 18:33. Not a removed poll; one config key.

### 🔴 NUMBERS DISCIPLINE ON THIS ROW — read before quoting anything

- **`~2400 ms` and `~1909 ms` are WITHDRAWN.** Both were subtractions across two runs differing by
  eight days, n=60 vs n=20, server state and code. Correcting the first to the second *made a bad
  method look careful*, which is worse. Do not revive either.
- **`3415.3 ms` says how much deferred work it would TAKE to drop the gate to its bar. There is no
  defensible estimate of what it IS.** Quote it only with that sentence attached; the guard rail is
  inside the same block quote in the doc, deliberately.
- Mr Radio's rule, adopted: **an invalid comparison that passes by a wide margin cannot be called
  immaterial ON ACCOUNT OF the margin, because the margin is computed by the same broken
  instrument.**

---

## Closed tonight

| row | receipt |
|---|---|
| `647f3733` — v1 baseline | **done**, event 9373 · commit `ad52d5ca` + `81bdb655` · run `ts-f06f5961` · referent `15536409`, seed 1024, n=20, 64/100 usable |
| `224fbb68` — artifact provenance | filed by me, **fixed by Tiberius** (`b8f3ace4`), verified by me |
| error-rate blindness | `12b1eef7` + `bebfc42b` — four rates read 0.0 from zero observations |

## Commits mine tonight
`ad52d5ca` `81bdb655` `26886060` `12b1eef7` `bebfc42b` `6e1d9e3b` `a569c45d` `bd8a11bb` — plus the wrapper
inside peer commit `4d64069d`. **None pushed; push is Rick's word alone.**

## What earned trust tonight — keep doing it

Verify by artifact, never by report — I checked Tiberius's containment result and Sam's correction
myself, and both held. **Check your own claims hardest**: I published a 3G swapfile that was 4G
(`free -g` truncates), said "every status None" from a top-level key that does not exist, claimed a
missing persistence hook when it was a TYPE GATE one layer up, and wrote three "invariant" tests
that could not run against the old code. Each was caught by measuring rather than re-reading.
**State what a measurement does NOT prove** — a zero against a 4-row table is not evidence.
**Stage only your own files** — and note that a peer swept my `v2_eval.py` edit into their commit,
so the reverse happens too.

SELF-RESPIN-NONCE: d31c0db7-3452-40eb-b19e-b975226b3d8d @ 2026-08-25T21:39:09.575812-04:00
