# Krishna 🦚 memento — session `4550bc37`, 2026-08-25 ~19:00 EDT

**Re-spun at the context ceiling (50.5%). Mr Radio spawns the successor — NOT a self-respin, so the
fresh seat picks up the 8G default from `480d4e8f`.** Board: **one row live**.

---

## 🔴 LIVE ROW — `647f3733`, in_progress, MINE. Resume here.

**Goal**: run the v1 baseline so v2 has a referent. Every one of Rick's v2 criteria is a comparison
against v1, and that comparison does not exist yet.

### Established tonight — do not re-litigate

**RICK'S RULING, VERBATIM.** Asked via `ask_yes_no`: *"You are the only thing blocking the board.
May I run the v1 baseline on the test server now?"* → **`yes`**. Returned **clean, with no
`[default used]` prefix**, which is what makes it a genuine keypress and not a timeout default.
The `user:rick` block is discharged.

**Mr Radio then ruled**: own the row, transition it out of blocked yourself, run the baseline.
Done — row `in_progress` under Krishna, event 9326.

**The v1 arm IS STANDING.** `:7997` up · `/api/code-identity` → `git_sha 15536409` · embed probe
returned a real **768**-dim vector from the worktree's own environment · credential parity **11/11**.
Cheap re-check: `bash src/scripts/launch-v1-baseline-7997.sh --check` (exit 0 + "identity matches
the pin" = good).

### What `70cbf1bf` established about the worktree — and the disagreement it caused

`70cbf1bf` recorded that **the pinned worktree did not exist on disk**. The 08-21 re-pin moved
`V1_PIN_SHA` to `15536409` and renamed the path `launch-v1-baseline-7997.sh` expects
(`lupin-v1-baseline-15536409`), but **no worktree was ever created at that name**: `git worktree
list` had no entry and the glob `lupin-v1-baseline-*` matched nothing. The launcher **refuses** on a
missing worktree — it does not create one — so the run would have died at its first precondition and
the re-pin would have looked like the cause. Exactly María's flag (*"a rename plus a container
rebuild, not a build-arg flip"*) with the rename half unexecuted.

⚠️ **Mr Radio replied that the artifact refutes this — the worktree exists and is a real checkout.**
Both statements are true and they do not conflict: **I created it at 18:31:57 tonight**, ~25 minutes
before he looked. Evidence, if it comes up again: `stat -c %z` on
`/mnt/DATA01/.../lupin-v1-baseline-15536409` and on `.git/worktrees/lupin-v1-baseline-15536409` both
stamp **18:31:5x today**. It is a real checkout AND it did not exist before tonight. Settled — do not
re-argue it, just cite the timestamps.

### 🔴 EXACT NEXT STEP — where I stopped mid-task

Assembling the submit payload. Established so far:

- `POST /api/test-suite/submit` fields: `test_types` (from `ALL_SUITE_COMPONENTS = unit, cosa,
  typescript, smoke, websocket, integration, e2e`), `pytest_args`, `env_vars`
  (prefix-allowlisted `TFE_`/`BFE_`/`LUPIN_TEST_`), `scheduled_at`, `dry_run`.
- The paired run is **`src/tests/integration/test_v2_paired_live.py`**, selected with
  `pytest_args="-m paired_eval_live"` (pytest.ini addopts deselect it from every default run).
- **n=20 per command** per Rick — `env_vars={"LUPIN_TEST_V2_EVAL_LIMIT": "20"}`. **NOT 60.**
  Five commands × 20 = 100 calls/pass, 200 across cold+warm. Two arms at different n are not a
  comparison.
- ⚠️ **THAT TEST'S DOCSTRING IS STALE — DO NOT TRUST IT.** It calls the bridge a "refusing scaffold"
  whose wiring a–d is unbuilt. **The wiring landed**: `run_v1_baseline` at :282,
  `require_isolated_snapshot_table` at :409, `assert_paired_isolation` at :440, and a real
  `test_v2_paired_go_no_go_live()` at :375. Verified by reading the body — the discipline that paid
  off repeatedly tonight.
- **UNFINISHED, my literal next command**: find how the test learns the v1 arm's base URL (`:7997`) —
  `grep -n "7997\|base_url\|LUPIN_TEST_\|os.environ" src/tests/integration/test_v2_paired_live.py`
- **PREFLIGHT IS MANDATORY BEFORE SUBMITTING**: run `src/scripts/preflight-test-container.sh` on the
  **host** — the endpoint cannot invoke it, since the server runs inside the container.

### Venue — confirm before spending 200 live calls

`venue_idle --port 8000` exits **0 IDLE**, all four lanes empty. But Mr Radio said at 18:32 that
`:8000` is **held for the 09:30 EDT P0 gate**, then at 18:56 said run the baseline. **A held venue is
not an idle one** — confirm which stands. Timing: box powers down ~23:00 EDT; n=20 is ~1h (n=60 was
budgeted ~3h and had to start before 19:30).

---

## Shipped this session — all committed, NONE pushed (push is Rick's word alone; he has ruled it twice — never raise it)

| sha | what |
|---|---|
| `2b1e850d` | v2 eval printed `0.0` for a cache it could not observe → now renders **unmeasurable** (row `2ec6ad9c`, closed) |
| `784c8ff1` | two constants named the pinned v1 baseline; the 410 refusal used the **rejected** one |
| `5a45756b` | **revert of my own `1818105d`** — see the lesson below |
| `98a60884` | two §10a trust-criteria pins compute a flawless run out of one where nothing finished |
| `dfc84ddb` | history.md archived 08-10→08-12, 14.1k→11.4k (row `6c2551f1`, closed) |
| `6e5cb95e` | "ten doors retired" → **twelve**, proven against the entry's own cited sha |
| `a2a08d10`, `5f5ea704` | the sha-stamping convention — branch doc **line 28**, above the first table |
| `70cbf1bf` | branch doc §1 — baseline unblocked + the worktree finding |

## 🔴 THE MISTAKE THAT COST A COMMIT — do not repeat

I measured with `pytest -k "v1_eval_arm" --cov=v1_eval_arm`, saw **77%**, filed row `2d5aa0be`, and
wrote 22 tests to close the "gap". **`-k` filters by test NAME** and silently excluded
`test_v1_ws_recv_events.py`, where that class had been covered since `7df2eadf`. Measured properly the
file was **already at 100%**, and the suite I duplicated was stronger than mine (real in-process
websocket server; a test binding the shipped emitter's frame shape). Reverted at `5a45756b`; row
amended post-terminal so the store does not carry the false number. Correct invocation, now bannered
in `test_v1_eval_arm.py`:

```bash
PYTHONPATH=src:src/scripts python3 -m pytest \
  $( grep -rln v1_eval_arm --include='*.py' src/tests/unit/ ) \
  --cov=v1_eval_arm --cov-branch --cov-report=term-missing
```

## Open, not mine to close

- **`2ebe4ccb`** (P1, María's checklist): finding amended onto the row (event 9299) + doc
  `src/rnd/v0.2.0/2026.08.25-trust-criteria-pins-vs-the-shipped-harness.md`. **María has no active
  session** — a DM bounced `recipient_not_found`, so the row is the durable channel.
- 24+ commits unpushed vs origin `7c4964dd`.

## What earned trust tonight — keep doing it

Re-measure before acting (**three** row premises died on contact today). Before editing a dated
record, establish whether the number was wrong *when written* or merely went stale — they are
different acts. State what a measurement does **not** prove. Run a snippet before committing it to a
doc. Stage only your own files — the commit-scope guard caught a peer's 8 staged files under my name.
