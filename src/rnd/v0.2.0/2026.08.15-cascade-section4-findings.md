# Cascade review — §4/§4.1 findings (Krishna 🦚, 2026-08-15)

**Target**: `2026.08.15-v2-as-the-brain-in-todo-queue.md` (María). Pass: §4, §4.1 (nobody had reviewed).
**Verdict**: María's §8 self-flag on §4.2 is right and **understates** it. §4.2 false on all three axes she named; §4.1's "Exactly as today" false for needs_input row (contradicts §4.3), nearly for agent row; §4/§4.3 split is sound; §4.3 seam under-specified.

## F1 (CONFIRMED) — §4.2 "same design, one tidier" breaks harder than admitted
- **v1 collect** (`expeditor.py:495-624`): batch-asks ALL missing args in ONE prompt (`_batch_collect_args`, 300s) or single ask (180s), then a confirm/edit loop (`_confirm_and_iterate`, `max_iterations=5` at `:708-716`). Failure taxonomy DECLINED/TIMEOUT/UNREACHABLE/INCOMPLETE/MALFORMED (`:57-59`, msgs `:172-214`) → distinct user message + job CANCELLED.
- **v2 `_needs_input`** (`flow.py:200-216`): returns `extraction.missing[0]` only, parks once, returns. No batch, no confirm, no re-ask, no timeout taxonomy (TTL silently evicts to "expired", `pending.py:113-131`).
- **Axis-by-axis**: RE-ASK — v1 5×, v2 none. TIMEOUT — v1 4-way classified, v2 silent + unobserved. PARTIAL — v1 detects `BATCH_INCOMPLETE` and re-asks, v2 returns first-only, no merge.
- **Killer**: NO caller of `pending.get()`/`set_status()` exists in the tree — only `flow.py:210` `put()`; `get`/`set_status`/`__contains__` are self-called inside `pending.py` (`:126`,`:153`,`:182`). The "human round-trip on a background thread" (`flow.py:11-14`, `pending.py:14-19`) is documented, not built. v2's needs_input second half is **absent**, not rougher.
- **Dead-ended twice (Clayton, corroborated)**: even if a resume consumer existed, `POST /api/v2/ask` is the router's ONLY endpoint (`v2_ask.py:136`) — there is no route for the human's answer to re-enter. No reader of the park AND no door to feed it.

## F2 (CONFIRMED) — §4.1 needs_input row "Exactly as today" is false + contradicts §4.3
- Today the expeditor ASKS synchronously itself (batched, blocking); it does not "supply the question so the queue asks/parks." That split is new.
- "keeps the card alive": the card = `spec_id`/tracker machinery (`todo_fifo_queue.py:1157,1169,1197-1198`) that §4.3 says v2 **sheds**; v2 needs_input builds no card. Cannot be "exactly as today" AND shed card lifecycle. §4.1 ↔ §4.3 contradiction.

## F3 (CONFIRMED) — §4.1 agent row "Exactly as today" is NEARLY
- pop→build→push is as today (`todo_fifo_queue.py:1203-1243`), but today the expeditor returns `scheduled_at`/`monopolize` INSIDE `args_dict` (popped `:1203-1204`); §4/§8-gap-1 excludes them from v2's model. The scheduling-arg extraction seam MOVES; §4 leaves who fills them unspecified. "Exactly" glosses it.

## F4 (refinement) — §4.3 seam under-specified
- "No fourth `Work.kind`" is fair. But "tells the queue what to queue" must carry: resolved command + filled arg **VALUES** + user/email/session + snapshotable + (unmodeled) scheduled_at/monopolize. Today `flow.py` returns arg KEY NAMES only (`args_known=sorted(final_args.keys())`, `:216`/`:269`), not the values §4.1 says the caller "receives." Name the payload in §4.3.

## F5 (VERIFY-FOR-MARÍA, not a defect) — §4 DECIDING/PREPARING split is SOUND
- Nothing in DECIDING needs queue state: `flow.py` runs cache/router/resolve/extract on injected fakes, no queue/card/tracker. v1 already separates `analyze` (issues NO prompts, `:329`) from `collect` (asks), so "are they all present" is decidable without asking. Seam is real, not forced.

## Unverified / hedged
- Saw `max_iterations=5` + the "Confirmation loop" call site but did NOT read `_confirm_and_iterate`'s full body — exact edit semantics of the 5 iterations lightly hedged.
- No-resume-consumer finding IS authoritative: the parked entry is an in-process Python dict; only Python can consume it; the targeted Python grep is clean.
