# `push_job` entry-coverage audit — every branch, and its home after step 6c

**Derived by**: Pocholo 📣 (reviewer), 2026-08-21, **before** Lane B's B0 commit.
**Source**: `src/cosa/rest/todo_fifo_queue.py:423-866` at `645e1473` on `wt-brain-integration-10ef4b64`.
**Method**: script-extracted every `if`/`elif`/`else`/`return` at indent ≤ 16 inside `push_job`, then read each block. Not by eye.
**Status**: input to Rachel's B0(i). **Every NONE row is a ruling for Rick, not a decision for the crew.**

The plan found two vanishing features (user mode, agentic commands) and wrote: *"Two instances make it a class… Anything `push_job` handles BEFORE its LLM-routing block is at risk by construction."* This is that enumeration.

| # | branch | file:line | home after 6c |
|---|---|---|---|
| 1 | **unfit question** — empty / >1000 chars / `"invalid"` prefix | `todo_fifo_queue.py:451-461` | 🔴 **NONE** — the flow has no fitness gate. `v2_ask.py` Field-validates ≤ **4000** chars: a different limit, applied at a different layer, with no rejection message |
| 2 | gisting on/off | `:469-471` | ✅ step 4 (agent-construction parity — the `question_gist` kwarg) |
| 3 | `is_accepting_jobs()` gate | `:500` | ✅ step 7c deletes it |
| 4 | confirmed-snapshot replay (`run_previous_best_snapshot`) | `:507-535` | ✅ step 7c deletes it (the two-turn branch) |
| 5 | the `"refactor "` demo-kludge skip | `:543` | ✅ step 7b deletes it |
| 6 | v1 stage-2 search + score bands — out-of-range `:583` · exact ≥100 `:594-614` · ≥threshold+confirmation `:616-668` · below `:690` | `:567-694` | ✅ steps 6c / 7b |
| 7 | user mode → agentic (`AGENTIC_MODE_MAP`) | `:719-725` | ✅ `push_job` resolves mode first, per Rick's ruling |
| 8 | user mode → conversational agent (`MODE_TO_AGENT`) | `:726-733` | ✅ same |
| 9 | LLM routing (no mode set) | `:734-747` | ✅ the flow's router |
| 10 | **`"search and summarize"` prefix** → `_search_and_summarize_safely` | `:748-755` | 🔴 **NONE** — live and tested (`src/cosa/tests/unit/rest/test_todo_fifo_queue_coverage.py:436`). `grep "search and summarize"` over `src/cosa/` returns only this branch, its own TODO comment at `:747`, and that test |
| 11 | resolvable command → build agent | `:756-781` | ✅ the flow's `_run_agent` |
| 12 | `"agent router go to automatic"` control command | `:782-790` | ⚠️ the plan flags it at risk ("plus the automatic control command") but **no step owns it** |
| 13 | `"agent router go to receptionist"` / `"none"` | `:791-796` | ✅ the flow's `_receptionist` |
| 14 | agentic commands in `JOB_ARG_CONTRACTS` → `_handle_agentic_command` | `:797-816` | ⚠️ the plan's own second-instance finding — **still no step** |
| 15 | router returns `"unknown"` | `:817-825` | ✅ the flow's `router_error` |
| 16 | **ROUTER-MISS LOUD FAIL** — router emitted a command nothing wires | `:826-838` | 🔴 **BEHAVIOUR CHANGE, silent.** v1 fires `_notify(...)` at the user and returns `job_id=None`; the flow's `unknown_command` path (`flow.py:113`) routes to the receptionist instead. The loud fail disappears |
| 17 | ding for a new job | `:840-841` | 🔴 none (minor UX) |
| 18 | register scoped id + push | `:842-845` | ✅ `QueueExecutor` |
| 19 | **query logging** — `_log_query_with_results` | five call sites: `:529`, `:609`, `:658`, `:683`, `:860` | 🔴 **NONE** |

## The one to act on first — #19, because the plan already leans on it

`grep -rn 'log_query\|query_log' src/cosa/rest/v2/` returns **zero**. Nothing in the v2 flow logs a query.

After 6c the query log goes dark on **every** path — and the plan's step 4 argues `question_gist` is *"the one with teeth"* precisely **because the query log reads it**:

> *"`question_gist` is the one with teeth — the query log reads it, so a gist that is really the raw question makes every logged row wrong in a way nothing raises about."*

⇒ **A parity fix aimed at a log the same sequence stops writing.** Step 4 and step 6c disagree about whether the query log exists.

## What each NONE row needs

| row | the ruling Rick owes |
|---|---|
| **19** query log | Does the flow write the query log, or is the log retired with v1? |
| **10** search-and-summarize | Keep the prefix feature (give it a home in the flow), or drop it and delete its test? |
| **16** router-miss loud fail | Keep the loud user-facing failure for an unwired command, or accept the receptionist? |
| **1** fitness gate | Is 4000 chars at the API layer the intended replacement for 1000 chars at the queue layer? |
| **12**, **14** | Already named at-risk in the plan; they need steps, not rulings |
