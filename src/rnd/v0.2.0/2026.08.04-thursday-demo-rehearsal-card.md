# Thursday Demo — Rehearsal Card

**For**: Rick, presenting Thursday 2026-08-06, driving by voice
**Written**: 2026-08-04 10:47 EDT by Mr. Radio 🦉
**Reading the evidence**: ✅ = measured on today's tree. ⏳ = built but **not yet merged**.

> ## ✅ UPDATED 11:35 EDT — EVERY ⏳ ON THIS CARD HAS LANDED
>
> Written at 10:47 when three fixes were built-but-not-running. All of them are now **committed and live**:
> `572db38b` (labels, auto-resolve, orchestrator) and `568068bd` (Dry Run flip, auto-submit prefill).
> `:7999` was bounced at **11:22:14** (boot #30) and the working tree is **clean**.
>
> **Where this card and the [Q&A cue sheet](2026.08.04-qa-card-voice-demo-cue-sheet.md) disagree, the cue
> sheet wins** — it was rewritten for boot #30. This card covers the *podcast card*; the cue sheet covers
> the *voice path* you're actually demoing.
>
> Two sections below were **inverted** by the bounce and have been corrected in place: the confirm-menu
> trap and the Spanish risk. The remaining ⏳ marks are stale labels, not open work.

---

## The line — ⛔ REFUTED 11:36 EDT, DO NOT USE

> ## ~~"make me a podcast on KISS"~~

**It routes correctly and then crashes**: `FileNotFoundError: Research document not found: KISS`. Your
explainer is never opened and no podcast is produced.

The language model pre-fills the document argument with the literal string `"KISS"`. The file matcher only
runs for arguments still *missing*, so it never runs — and a topic string reaches the job where a file path
belongs. Confirmed four ways (Tiffany's live run `pg-e1e5e572`, Clayton's log tail, Rio's independent read,
and source at `expeditor.py:301` / `agentic_job_factory.py:167` / `job.py:217-223`), then confirmed **live**
when Tiffany answered YES at the confirm and hit the crash.

**It fails loudly, not silently** — an error on stage, not a plausible wrong podcast.

### The bar every replacement line must clear

Routing to the file reader is **necessary, not sufficient**. A working line must ALSO leave the document
argument **unfilled**, so the matcher fires and resolves your file. The shortlist below was ranked on
routing alone.

⚠️ **There may be no such line.** Rachel's open concern: the word "KISS" is what makes it route to the file
reader *and* what gets captured as the topic — same word, both jobs. If no phrasing threads that needle,
this needs a code fix, not a wording fix. She is measuring end-to-end now.

**Known-working today**: the **Agent Mode → Podcast Generator** dropdown bypasses the router entirely. You
ruled it out; it remains your call.

<details>
<summary>What this section originally claimed, and why the evidence misled</summary>

25 characters. A **verbatim routing training template** (`make me a podcast on RESEARCH_TOPIC`), which is
why it routes so reliably — that part was never in doubt.

✅ Routes to the file-reading agent — **3 of 3** — *this half was and remains true*
❌ "Resolves to your real KISS file — 3 of 3" — **measured by feeding the utterance straight into the file
matcher.** The matcher works. It is never called in the real flow.
❌ "15 read-only runs, zero wrong-file grabs" — true of the matcher, irrelevant to the live path

Two halves proven separately; the seam between them is exactly where it broke. **This card already named
that seam as unproven** — see "What still has no proof, honestly" below. It was right, and the rest of the
card read as more confident than that admission warranted.

</details>

### The rule — TWO parts, and you need BOTH

> ### Say "about" or "on" — and NAME KISS right after it.

This rule was refuted twice before it settled. First version ("topic good, document bad") was too broad.
Second version ("it's the preposition") was still too loose. A deliberate contradiction probe caught it:

| Sentence | Has "about/on"? | Names KISS? | Result |
|---|---|---|---|
| "make me a podcast **on KISS**" | ✅ | ✅ | ✅ **file-reading agent** |
| "make a podcast **about the KISS protocol**" | ✅ | ✅ | ✅ file-reading agent |
| "podcast **about my KISS explainer**" | ✅ | ✅ | ✅ file-reading agent |
| "make a podcast **about my document**" | ✅ | ❌ | ❌ scratch agent |
| "make a podcast **from the KISS protocol**" | ❌ | ✅ | ❌ scratch agent |
| "podcast **based on the contents of** my explainer…" *(your original)* | ❌ | ✅ | ❌ scratch agent |

**Miss either half and it goes to the agent that never opens your file.** "My explainer" is safe *only*
because KISS is named in it — "about my document" alone fails.

Short form to hold in your head: **"a podcast ABOUT KISS."** Don't say *from*, don't say *based on*,
and don't say *my document* without naming KISS.

### Four backups, all 3/3 on both gates

2. "podcast about my KISS explainer" (31) — *richer resolve margin if you want it*
3. "make a podcast about the KISS protocol" (38)
4. "build me a podcast on the KISS protocol" (39)
5. "create a podcast about how KISS saved me tokens" (47)

---

## What actually happens — budget THREE spoken turns

The document matcher **never sees your routing sentence**. The document argument comes back missing, so
the expediter asks a follow-up and matches on **your spoken answer to that question**.

1. **You say**: "make me a podcast on KISS" → routes correctly
2. **It asks**: *"I think you want [label]? Is that right?"* → **you confirm** ⚠️ see the label warning below
3. **It asks**: *"Which document should I use? Describe it or say the filename."*
4. **You answer**: anything containing **"KISS"** → resolves to your file, 3/3
5. It generates the script, gates for your approval, renders audio (~7 minutes to done)

✅ **Clayton's auto-resolve collapses turns 3 and 4 into nothing** — it feeds your routing line straight
to the matcher, which is exactly the input already validated 15 times. **Committed `572db38b`, live on
boot #30.** So the three-turn sequence above is now **one turn**, and steps 3–4 should not happen.

⚠️ **Not yet observed by anyone.** The crew's test accounts stop at the confirm gate and never reach the
document step, so nobody has watched steps 3–4 *fail to appear*. **Your rehearsal is the first real
observation.** If that prompt does appear, say so — it's a finding.

### ✅ THE CONFIRM MENU TRAP IS GONE — this section is RESOLVED (11:22 EDT)

**This warning is retained as history. Do not act on it.** The label fix is committed (`572db38b`) and
`:7999` was bounced at **11:22:14 EDT** (boot #30). Rachel verified the corrected strings on the **live
menu**, not the source.

| What you'll SEE at the confirm | What it is |
|---|---|
| **"Doc-to-Pod (create a podcast from an existing document)"** | ✅ **the file reader — PICK THIS ONE** |
| "PodMaker (research a topic and create a podcast)" | ❌ researches from scratch, never opens your file |

**The option that reads correctly now IS correct.** No trap, no inverted pick.

<details>
<summary>What this section used to say (obsolete — click only if you rehearsed before 11:22)</summary>

Before the bounce the labels were backwards, and this card told you to pick **"PodMaker (create a podcast
from a topic)"** because the correct-sounding option was the wrong one. **If you rehearsed on that advice,
unlearn it** — picking by the old rule now lands you on the scratch agent.
</details>

Rachel's routing measurements are unaffected either way — the router never reads these labels.

---

## Preconditions — the ones that silently decide the day

| | Why |
|---|---|
| **Log in as your own account** | The prompts route to whoever authenticated. A service account has no browser, and the asks go nowhere. |
| **Keep the browser tab live** | The approval prompt needs an active connection to reach you. |
| **Dry Run must be UNCHECKED** | ✅ Default now ships unchecked — committed `568068bd`, guarded by a test so it can't drift back. Left checked you'd reach "🧪 Dry Run Complete" with **no audio and no Play Here button**. *Not a factor at all on the Q&A voice path* — the router never supplies `dry_run`, so it parses false and you get a real render. |

---

## Known risks, and what they look like

**1. Script-generation rate limit** — measured, real, not a code defect.
Looks like: *"the AI service returned an error while writing the podcast script, try again in a few minutes."*
**Measured cooldown: ~3.5 minutes.** A short pause and a retry, not a dead demo. Say "it's rate-limited,
give it a minute" and carry on — it fails loudly rather than producing an empty stub.

**2. Spanish translation** — ✅ **the fail-loud fix is LIVE**, measured 11:30 EDT.
It no longer fails silently. Krishna proved this from the command line, forcing a failure so the detector
was shown able to report red *before* any green run was trusted: the console prints
`Translation error: … parsed to zero segments`, the title comes back **"(Mexican Spanish - Translation
Failed)"**, and a notification fires.

**So the trade the earlier draft described has already landed**: better engineering, more exposed demo.
If Spanish is in Thursday's script you are accepting a chance of a *visible* failure rather than a silent
wrong result. **Still your call whether it stays in** — but you're deciding against different facts than
this card originally gave you.

*How often it fails is being measured across 5 real runs.* The "nondeterministic" claim above is
**inherited, not yet re-confirmed** on this code — treat it as unproven until that report lands.

**3. The confirmation label** — ✅ **RESOLVED**, committed `572db38b`, live on boot #30, verified on the
live menu by Rachel. See the corrected table above. Nothing to work around.

---

## What still has no proof, honestly

- **Your sentence → your document → playable audio in ONE run from your account.** The two halves are
  proven separately; the seam is not. **Only your own rehearsal closes this.**
- **What happens if your browser drops** between script generation and the approval prompt. The test
  harness held its connection open, so the rehearsal's top risk went unexercised.
- **The voice vendor key.** No worker can test it — the sandbox blocks sending a secret to an external
  host. One real render from you proves it.

**All three close with a single rehearsal**: one real podcast, Dry Run unchecked, from your own account,
Play Here clicked.

---

## Receipts

Full 6-stage run to real audio on today's tree — 50 segments, 12.86 MB mp3, and **6 of 6 planted facts**
recovered from the source document with a decoy defeated, proving it read the file rather than riffing on
the topic. Scorer control-proven able to report red. Test tree clean at **21,562 passed / 0 failed**.
