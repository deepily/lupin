# R&D Documentation Index

Research and development documents for the Lupin project, organized by release version.

## Version Directory Index

Documents are archived into the branch/version where they were completed. Date prefixes on filenames determine assignment.

| Version | Period | Files | Subdirs | Focus |
|---------|--------|------:|--------:|-------|
| [v0.5.0](v0.0.5/) | ≤ 2025-07-10 | 7 | 1 | WebSocket TTS streaming, early notification design |
| [v0.6.0](v0.0.6/) | Jul 11 – Aug 1, 2025 | 11 | 0 | WebSocket user routing, progressive TTS, async bridge |
| [v0.7.0](v0.0.7/) | Aug 2 – Aug 15, 2025 | 12 | 0 | Fresh Queue UI, audio caching, smoke testing, job replay |
| [v0.8.0](v0.0.8/) | Aug 16 – Sep 27, 2025 | 5 | 1 | LanceDB migration, three-level question architecture |
| [v0.9.0](v0.0.9/) | Sep 28 – Oct 7, 2025 | 7 | 1 | JWT/OAuth auth system, admin user management |
| [v0.1.0](v0.1.0/) | Oct 8 – Dec 31, 2025 | 28 | 2 | SSE notifications, PostgreSQL, Cloud Run, sender-aware UI |
| [v0.1.1](v0.1.1/) | Jan 1 – Jan 28, 2026 | 18 | 3 | MCP integration, Docker Claude Code, deep research queue |
| [v0.1.3](v0.1.3/) | Jan 29 – Feb 4, 2026 | 11 | 1 | CJ Flow protocol, test remediation, runtime expeditor |
| [v0.1.4](v0.1.4/) | Feb 5 – Feb 16, 2026 | 29 | 2 | PEFT training, calculator agent, proxy design, SWE team |
| [v0.1.5](v0.1.5/) | Feb 17 – Mar 11, 2026 | 15 | 3 | Playwright E2E, trust proxy, voice I/O integration |
| [v0.1.6](v0.1.6/) | Mar 12 – Apr 23, 2026 | 37 | 3 | CJ Flow persistence, timed execution + monopolize + pause, scheduling UI + voice runtime args, presentation generator, test isolation, bug fix expediter, SDK upgrade, Docker non-root user, session-start top-10 briefings |
| [v0.1.7](v0.1.7/) | Apr 24 – May 28, 2026 | 68 | 25 | CJ Flow async multi-lane, per-session voice personas, TTS solo/chorus interaction mode, inter-session commons, notifications UI JS refactor, bounded Claude Code redesign, manager-spawned reviewers |
| [v0.1.8](v0.1.8/) | May 29, 2026 – present | 14 | 1 | GCP deployment prep ([subdir](v0.1.8/2026.05.30-gcp-deployment/): [architecture survey & readiness assessment](v0.1.8/2026.05.30-gcp-deployment/2026.05.30-gcp-deployment-architecture-survey.md) + [provisioning & deployment plan — straw man](v0.1.8/2026.05.30-gcp-deployment/2026.05.30-gcp-deployment-provisioning-plan.md) + [Cloud Run vs GCE GPU + CC hosting research](v0.1.8/2026.05.30-gcp-deployment/2026.05.30-cloud-run-vs-gce-gpu-and-cc-hosting.md) + [M1 VM bring-up plan (mount model, 5 decisions)](v0.1.8/2026.05.30-gcp-deployment/2026.06.03-m1-vm-bringup-plan.md) + [**M2 — arbiter ride-along + VM cutover (execution plan)**](v0.1.8/2026.05.30-gcp-deployment/2026.06.10-m2-arbiter-ride-along-and-vm-cutover.md)); CoSA→Lupin mono-repo fold; scheduled-job bounce survival; CoSA 100% coverage baseline; LanceDB → PostgreSQL/pgvector migration analysis; [notifications UI: focus-mode card height + broadcast Show-more toggle](v0.1.8/2026.05.30-notifications-ui-focus-height-and-broadcast-toggle.md); [master-detail reading pane: centering + iframe embedding/sizing + bust-out](v0.1.8/2026.05.30-master-detail-reading-pane-fixes.md); [broadcast liveness — filter on bridge mtime, not interaction recency](v0.1.8/2026.06.05-broadcast-liveness-mtime-filter.md); [reap event → focus-bar badge drop + broadcast-card refresh (design)](v0.1.8/2026.06.05-reap-event-focus-bar-and-broadcast-refresh.md); [prediction-hook event-loop offload — /health timeout fix (plan)](v0.1.8/2026.06.05-prediction-hook-eventloop-offload.md); [next-session execution plan — heartbeat v2.1 + held-commit resolution](v0.1.8/2026.06.06-next-session-execution-plan.md); [heartbeat arbiter v2.2 closed-loop autonomy (D1–D4) — SWE-team engagement #3](v0.1.8/2026.06.04-heartbeat-hook/07-clayton-v2.2-closed-loop-log.md); [fleet context-pressure assessment utility — proposal](v0.1.8/2026.06.07-managing-context-memory/2026.06.07-fleet-context-pressure-assessment.md) + [context-pressure logic — validation brief for expert](v0.1.8/2026.06.07-managing-context-memory/2026.06.08-context-pressure-logic-validation-brief.md) + [expert review response](v0.1.8/2026.06.07-managing-context-memory/2026.06.08-context-pressure-logic-validation-brief-response.md) + [REVISED plan (post-expert-review)](v0.1.8/2026.06.07-managing-context-memory/2026.06.08-context-pressure-revised-plan.md) + [Phase 2 design (arbiter wiring)](v0.1.8/2026.06.07-managing-context-memory/2026.06.08-context-pressure-phase2-design.md) + [arbiter liveness-gap diagnosis (stale event-sourced roster)](v0.1.8/2026.06.07-managing-context-memory/2026.06.08-arbiter-liveness-gap-diagnosis.md) + [arbiter consumption-gap & operator loop + LOCKED liveness design (4-signal union)](v0.1.8/2026.06.04-heartbeat-hook/2026.06.08-arbiter-consumption-gap-and-operator-loop.md) + [arbiter liveness-fix implementation plan (Phase 1)](v0.1.8/2026.06.04-heartbeat-hook/2026.06.08-arbiter-liveness-fix-implementation-plan.md) + [loop-rename implementation plan (Round 2a, Decision 6)](v0.1.8/2026.06.04-heartbeat-hook/2026.06.08-loop-rename-implementation-plan.md) + [arbiter operator-loop implementation plan (Round 2b — live-push + routing + auto-poke)](v0.1.8/2026.06.04-heartbeat-hook/2026.06.09-arbiter-operator-loop-implementation-plan.md) + [fleet-status table in the notifications client — read-only operator view (design; enrich /api/arbiter/fleet-state with manager+role, group by hierarchy)](v0.1.8/2026.06.09-fleet-status-table-notifications-client/01-design.md) + ["% Window" + "Window size" columns — per-persona context-pressure join (8 cols; client-side)](v0.1.8/2026.06.09-fleet-status-table-notifications-client/02-context-window-columns.md) + [PID-liveness "offline" override — kill-0 fast-death detection (design; ~60s vs ~1h to offline; host-PID-trust gated)](v0.1.8/2026.06.09-fleet-status-table-notifications-client/03-pid-liveness-offline-override-design.md) + [published per-persona context-headroom service — read-only, API-key-gated arbiter JSON (design; headroom to 50%-of-1M / 75%-of-200K budget lines)](v0.1.8/2026.06.07-managing-context-memory/2026.06.09-context-pressure-published-headroom-service-design.md); [**notifications-UI → multiplexer gap-bridge** (analysis + tiered work plan: 12 interim features F1–F12, multiplexer gap analysis, deprecation plan for Sat 2026-06-14)](v0.1.8/2026.06.10-notifications-ui-multiplexer-gap-bridge/README.md); [**MCP worktree project mis-detection fix** — worktree-aware `detect_project()` via `git rev-parse --git-common-dir`; a worktree cwd resolves to the MAIN repo (kills recurring "No credentials for project '<worktree>'" URGENT spam)](v0.1.8/2026.06.10-mcp-worktree-project-detection-fix.md); [**Fleet-Status liveness color-coding + offline-lineage retention** — verdict→color status-dots + %-window heat-tint (client); reaped sessions retain last-known manager lineage until row eviction (arbiter)](v0.1.8/2026.06.10-fleet-status-colors-and-offline-lineage.md); [**broadcast-miss root cause — duplicate listeners + silent skip gates** (plain-English diagnosis: SessionStart double-fire spawns 2 listeners with no singleton guard, PID shadowing orphans one, tmux injection race garbles delivery; 7 silent-skip gates enumerated; F1–F4 fix plan)](v0.1.8/2026.06.10-broadcast-miss-duplicate-listener-root-cause.md); [**multi-manager declaration env var + spawn persona_preference transport fix** (design; root cause: preference echoed in spawn result but never injected into child env at session_spawner.py:298-302 → SessionStart reads None → random allocation; proposes `COSA_VOICE_MANAGERS__<PROJECT>` CSV declaration + strict `COSA_VOICE_REQUESTED_PERSONA` transport)](v0.1.8/2026.06.11-multi-manager-env-var-and-persona-preference-transport-fix.md); [**arbiter missed-poke post-game — test-thoroughness audit + outreach-logging fix design** (2026-06-10 silent-stall forensics; names the missing test class: operational-requirement acceptance/scenario tier vs implemented-contract units; F1 structured outreach + gate-evaluation logging, F2 manager-staleness poke tier `arbiter manager stale poke threshold seconds`=2700, F3 fleet-dark advisory)](v0.1.8/2026.06.11-arbiter-missed-poke-postgame-and-outreach-logging.md); [**arbiter loop-closure journal receipts + self-service tail guide** (same-day organic validation: 45m advisory + 3 bounded pokes + noon fleet-dark during a real 6.5h manager wedge; verbatim journal lines; journalctl cheat sheet for watching `arbiter_outreach` live)](v0.1.8/2026.06.11-arbiter-loop-closure-journal-receipts.md); [**broadcast-miss F1–F4 implementation** — singleton spawn guard (flock+pgrep), SessionEnd reap-all, fanout receipts (`filtered_out[]` in broadcast response), per-tmux injection mutex; built in worktree, regression tests reproduce the duplicate-listener condition](v0.1.8/2026.06.11-broadcast-miss-f1-f4-implementation.md); [**FM-21 test-isolation fixes — hermetic-config module-boundary fixture + event-loop pollution (F2)** (kills the known "3 fails": asyncio.run() swap in 3 decision-proxy victims; module-scoped autouse ConfigurationManager reset + env snapshot/restore in top-level conftest.py; red/green isolation-verify pair; exposed + repaired 2 pre-existing order-dependent tests; files 50 latent BFE get_event_loop sites as debt)](v0.1.8/2026.06.11-fm21-test-isolation-hermetic-config-fixture.md); [**arbiter outreach delivery receipts + human-parsable journal timestamps** (root cause of the 21:28/22:01 UTC silent misses: literal `${LUPIN_DEV_EMAIL}` in the systemd env → swallowed 404 on every live push while `arbiter_outreach` journaled "rick" as reached, + board-only manager DMs with no push; design: `outreach_id` dot-connect via `arbiter_outreach`→`_result`→`_receipt` events, outcome-returning notify contracts, register-question `dm_push` seam, pending-ledger re-announce-on-return, `ts_local` field + `arbiter journal local timezone` INI key)](v0.1.8/2026.06.11-arbiter-outreach-delivery-receipts-and-local-timestamps.md); [**arbiter Fleet-Status lineage persistence + punct-tolerant persona matching** (Rick's "(Unmanaged)" reaped-workers bug: dismiss_sessions kills BOTH lineage sources at reap + the offline-lineage carry is in-memory and :8001 restarted 4× tonight; bonus journal-confirmed "mr radio" role=worker from plain `.lower()` declared-roster compares; design: file-backed lineage carry `io/arbiter/lineage-carry.json` write-on-change + the ONE shared `_normalize_for_match` normalizer at all three compare sites)](v0.1.8/2026.06.11-arbiter-lineage-persistence-and-persona-matching.md); [**fleet-roster env file + reserve-from-random allocation** (single-source `COSA_VOICE_MANAGERS__*` roster: one in-repo env file read by both start-cc-with-tmux.sh and the arbiter systemd drop-in via `EnvironmentFile=`; declared-manager names reserved OUT of random + chain-`*` persona allocation, still claimable by strict request / named chain element; roster travels hook→server as `declared_managers` query param)](v0.1.8/2026.06.11-fleet-roster-env-file-and-reserve-from-random.md); [**external-sender card gap — root cause** (Rick's "no card for outside-system notifications": NOT server-side — the multiplexer has NO notifications-pane history hydration, cards are live-event-only (cold load = 0 cards vs 102 senders with history); JS classic cold-load cards it fine; fix shape = senders-visible + conversation-by-date hydration per JobStore.hydrateHistory precedent; 3 design rulings queued)](v0.1.8/2026.06.11-external-sender-card-gap-root-cause.md); [**mux cold-load notification hydration — design** (fix for the card gap: reuse the single WP9 senders-visible fetch → `SenderStore.hydrate` + `NotificationStore.hydrateHistory` per JobStore precedent, today-anchored window silently, ONE "hydrated" emission per store; dedupe contract = id-keyed both arrival orders, requires the fire-and-forget online path to thread `id=db_notification_id` into `push_notification` — the AR path already does, the omission is the live-frame/DB-row identity split)](v0.1.8/2026.06.11-mux-cold-load-notification-hydration-design.md); [**unified task store — Phase-1 build plan** (Lupin-side build of the PIP-canonical v0.4 design: TaskItem/TaskEvent schema, receipt key-whitelist validation, transition rules, /api/tasks/* debt-clean REST, migration f0a1b2c3d4e5; gate-ruled item_class naming + terminal-state rule)](v0.1.8/2026.06.11-task-store-phase1/01-build-plan.md) + [MCP wrapper spec (task_create/task_transition/task_query — cosa-voice-repo deliverable, spec-only until GO)](v0.1.8/2026.06.11-task-store-phase1/02-mcp-wrapper-spec.md); [**unified task store — Phase-2 write-path hooks build plan** (harness Task* → store mirror via PostToolUse: correlation-keyed writes off `tool_response.task.id` (C1 precedence (a)), completed→review ruling (no receipt-theater), C12 pulled forward — `task_events.reason` migration b2c3d4e5f6a7 + →dropped-requires-reason, C8 spool/replay w/ idempotency probe, F4 manager-figure gate, `/correlate` respawn adoption)](v0.1.8/2026.06.12-task-store-phase2-write-paths/01-build-plan.md); [**cosa-voice token reduction — DM body-in-push (Phase 1 design)** (stop the ~75%-of-budget DM bleed: carry the full DM body inside the push system-reminder instead of an empty-body claim-check that forces a ~3,712-token `commons_read` re-fetch → ~204 tokens/DM, ~18× cut; explicit `kind={human_to_ai\|ai_to_ai}` provenance discriminator branches the formatter envelope `[Voice from Rick]` vs `[DM from <persona>]`; offline-recipient DB-inbox replay deferred to Phase 2 / Monday)](v0.1.8/2026.06.13-cosa-voice-token-reduction/01-dm-body-in-push-phase1-design.md) + [**notification-native AI↔AI messaging (APPROVED — supersedes 01's Layer 2/3)** (carry the DM body inline as an ordinary notification delivered by the existing buffer-drain/inject pipeline; first-class `direction` column `human_to_ai\|ai_to_ai\|ai_to_human` set cross-cutting at every notification call site, NOT `kind` which collides with `type`; persistence = real Postgres columns not the payload dict, so the commons board is dropped; idle-aware delivery active→buffer / idle→tmux closes the `notification.py:55` drain-and-discard landmine; retires the claim-check + `commons_read` re-fetch + watcher reply-poll)](v0.1.8/2026.06.13-cosa-voice-token-reduction/02-notification-native-aixai-design.md); [**unified task store — Phase-2.1 build plan** (Phase-0 plan, UNREVIEWED draft 2026-06-15: 5 deferred/C12 items — `task_correlate` MCP tool, `PATCH /api/tasks/{id}` item-edit, cross-item events query, legal-transition graph, `next_chase_ts` chase consumer + I4 detection; T4 auto-create deferred to 2.2 on the unresolved acked-ledger-scope ruling; 2 open gate calls; flag flipped `task_store.enabled=true`)](v0.1.8/2026.06.15-task-store-phase2.1/01-build-plan.md) + [**task-store daily-use adoption-gaps inventory** (read-only survey after Phase-2.1 green: the gap is ADOPTION not capability; A discoverability — ZERO CLAUDE.md/global pointer to the tools+discipline doc (biggest blocker); E chase consumer flag-OFF + start() unwired → →blocked records next_chase_ts but nothing chases; D query ergonomics — single-status filter, no any-open/owner-OR-accountable/title-search; F write-scope widening rider now arguably Phase-2.1-triggered; receipt-scope-prefix + correlate-vs-receipt_refs owned by María's conventions doc; companion: exhaustive legal-edge matrix for the v1.1 mermaid)](v0.1.8/2026.06.15-task-store-phase2.1/02-adoption-gaps-inventory.md); [**reap tombstone — immediate roster eviction for reaped sessions** (root cause: `dismiss_sessions` deletes the bridge BEFORE the arbiter can read the worker PID, so the fast kill-0 death path structurally can't fire and the reaped row lingers "stale" ~60min until its event ages out; fix = the reaper appends ONE authoritative `kind="reaped"` tombstone on the heartbeat-event rail the arbiter already polls → `build_fleet_view` sets an off-axis `reaped` flag → `build_snapshot` force-offlines → the existing publish-prune evicts the row in ~1 poll; 4 additive edits, 100% L/B/F)](v0.1.8/2026.06.04-heartbeat-hook/2026.06.15-reap-tombstone-roster-eviction-plan.md); [**arbiter tmux-wake — runtime-configurable manager-poke wake surface (Thread C+D)** (root cause: the listener's `_deliver_peer_dm` BUFFERS an arbiter poke whenever the recipient's last heartbeat outcome != `EVENT_IDLE` — true for a stale-stopped-with-owed manager AND, per María's `poke_count=0` probe, an idle-but-`EVENT_IDLE`-never-emitted one — so a dormant pane never wakes while Rick's broadcasts DO via unconditional `_inject_via_tmux`; fix = a host-side `tmux_push_fn` reusing `inject_qualifier_via_tmux` to inject UNCONDITIONALLY, runtime-selected by INI `arbiter poke wake mechanism`=tmux(default)|dm, degrade-safe DM fallback + unconditional durable board write; THE primary fleet liveness path now that the internal self-poke `decision:block` is confirmed broken (filed separately, P1); 100% L/B/F)](v0.1.8/2026.06.16-arbiter-tmux-wake-thread-cd-design.md); [**follow-through accountability — lupin build plan** (lupin-side build of María's PIP-ruled design: kills the manager↔worker mutual-wait deadlock via asymmetric chase + `awaiting:manager` STALL status; KEY INSIGHT — reuse the existing store: `accountable_manager`/`next_chase_ts` exist, `awaiting:manager` maps onto `blocked`+`blocked_by(persona)`, `awaited_since_ts` derives from the blocked-transition event ts; net-new = `follow through escalation tick multiplier` INI key (default 2, live-tick×factor, never hardcoded) + one-shot aged-escalation watcher + §4.5 escalation hygiene (clear on manager-ack OR worker-hold, arbiter reads the hold before "blocking", one-shot-then-cleared); sequence after the liveness builds — shared arbiter surface; captures Rick-ruled bug 82e4eaf0 work-debt-source=store w/ transcript fallback)](v0.1.8/2026.06.16-follow-through-accountability-lupin-build.md); [**task-list UI card — build brief (Tiberius lane / Step 4 of store-canonical task management)** (execute-ready, spawn-gated on Mr. Radio's cascaded-review verdict + locked `task_query` read-contract: clone the fleet-status card's 7-artifact pattern — Renderer/Store/Model/Template/HTML/CSS/boot under `static/js/multiplexer/` — rendering `GET /api/tasks` which ALREADY EXISTS production-ready (near-zero backend), full-fidelity columns incl. blocked/blocked_by/next_chase/owner/accountable, graceful store-down degradation, 100% L/B/F + Playwright)](v0.1.8/2026.06.16-task-list-ui-card-build-brief.md); [**store-canonical task-management — cascade review + cutover log** (3-reviewer unanimous APPROVE-WITH-REVISIONS synthesis = build ACs; the live cutover record: drain → parity 4/4 → flag flip → verified)](v0.1.8/2026.06.16-store-canonical-task-mgmt-cascade-review.md); [**unified task-store — open follow-ups implementation plan** (post-cutover: A poke-cap merge · B arbiter detector done/blocked-on-user awareness · C evidence-gated Step-5 mirror retire · D residue; sequencing + effort)](v0.1.8/2026.06.17-unified-task-store-followups-plan.md); [**fleet availability/bandwidth signaling — design** (Rick voice directive: liveness≠availability — no public signal advertises a held manager's free bandwidth; proposes an availability dimension BUSY/HELD_WITH_CAPACITY/IDLE_AVAILABLE + open_capacity, store-backed publication recommended, consumers = peer managers (load-balancing)/arbiter (suppress false stall)/UI card; §7 open questions for Rick)](v0.1.8/2026.06.17-fleet-availability-signaling-design.md); [**Tiberius top-7 current open items** (Rick co-parse: ranked, memento-filtered backlog from TODO.md current/previous-branch sections — mux migration · cosa-voice token-reduction · bounded-CC migrations · Alembic baseline · arbiter tuning · GCP prod · context-pressure utility)](v0.1.8/2026.06.17-tiberius-top-7-current-open-items.md); [**lupin-nav.js → TypeScript port (standalone esbuild entry)** (first non-mux standalone TS bundle: new `tsconfig.nav.json` + `build-nav.sh` esbuild entry, mux `boot.ts` untouched; DI-seam behaviour-preserving port of the legacy nav IIFE, legacy `.js` kept live until wired; 100% c8 L/B/F, 24 tests, additive)](v0.1.8/2026.06.18-lupin-nav-js-to-ts-standalone-entry-port.md); [**websocket-diagnostic.js → TypeScript port (standalone esbuild entry #2)** (reuses the nav standalone-entry pattern: new `tsconfig.diagnostic.json` + `build-diagnostic.sh`, mux/nav configs untouched; DI-seam port of the 558-line WebSocketDiagnosticTool; APPROVED port-time fix of a latent dead-onopen/onerror reassignment bug via composed handlers preserving the exact connect-promise contract; legacy `.js` kept live until wired; 100% c8 L/B/F 753/133/49, 42 tests, additive)](v0.1.8/2026.06.18-websocket-diagnostic-js-to-ts-standalone-entry-port.md); [**notifications.js decomposition assessment** (read-only structural triage / decision-support for Rick's big-migration scope: RECOMMENDS complete-and-flip the existing mux ~75 `.ts` twins, do NOT decompose the legacy monolith — decomposing throws away flip-ready work; contingency slicing order documented for a forced partial cut)](v0.1.8/2026.06.18-notifications-js-decomposition-assessment.md) + [**fleet WIP state snapshot (2026-06-18)** (post-wind-down handoff: references all 8 open unified-task-store items + the 2 held-unreviewed commits (Clayton write-seam `84f40e5b`, Krishna migration `a7b8c9d0e1f2`) + reaped-crew continuity + cross-manager handoffs)](v0.1.8/2026.06.18-fleet-wip-state-snapshot.md) |
| [v0.1.9](v0.1.9/) | Jun 19, 2026 – present | 0 | 26 | Bug-fix branch (`wip-v0.1.9-2026.06.21`): multiplexer layout-parity + per-worker task-editing, persona-name normalization, task-reassign; [**proactive-manager mechanism + arbiter-staleness + mux-baselines — build plan**](v0.1.9/2026.06.23-proactive-manager-mechanism/01-build-plan.md) (Mr. Radio's lane: Stop-hook debounce + operator-gate queue + 3 urgency tiers + dark-session backstop `fcb5dbc0`; arbiter dead-hold staleness-filter `bc1bc373`; commit mux visual baselines to a tracked dir `6ca79dc2`; `ricks_court`→`operator` rename folded in); [LanceDB 88GB incident remediation runbook + recovery playbook](v0.1.9/2026.06.24-lancedb-88gb-optimize-incident-remediation.md) (6 decisions ruled 2026-06-24, REBUILD-spine impl deferred to off-peak); [**Notifications → Multiplexer migration discrepancies** (holder — #1 priority for this bug-fix branch)](v0.1.9/2026.06.25-notifications-to-multiplexer-migration-discrepancies/00-index.md) — running ledger of every legacy-vs-mux discrepancy; doc 01 = section-layout gap analysis + 8-step comparison methodology (section order wholesale-reordered: broadcast exiled to bottom + Recent-Activity de-nested, focus bar hoisted to top, TTS preview orphaned; per-message pause/stop/ratify dropped); [**arbiter false-'blocked' on live-peer waits + stale-ts `-r2` echo**](v0.1.9/2026.06.30-arbiter-false-blocked-live-peer-and-r2-echo.md) (root cause + surgical `_attention_workers` fix: exclude a non-stuck holder whose awaited peer is itself ALIVE — a legit in-flight dependency is not a stall; KEEP stuck + deadlock-cycle members + dead/absent-peer waits; the `-r2` duplicate is the intentional un-ACK'd one-shot resend re-sending the original stamped body, NOT a listener-layer dup; 100% L/B/F on changed code); [**arbiter mis-routes a stuck MANAGER's escalation to a PEER manager**](v0.1.9/2026.06.30-arbiter-stuck-manager-peer-misroute-bug.md) (bug `ff91cff4`; Rachel investigation → handed off to Tiberius' SWE team; root cause A = un-pruned phantom-dead duplicate manager session `9f133dd9` counted as "1 stuck/dead" alongside the live Tiberius; root cause B = case-7 "owning manager" + case-13 fan-out have no self/peer guard for a manager subject → the tap lands on peer manager Mr. Radio; fix = Rick-only routing for a manager subject + prune corpse-past-ceiling; arbiter-only freeze-safe); [**mux ⇆ legacy parity dossier** (`ffecbcdf` sign-off pack)](v0.1.9/2026.07.02-mux-vs-legacy-parity-dossier.md) — side-by-side status matrix (surface → legacy → mux → receipt → residual gap) over B5 28/28 (`393251c5`), 0a/0c toggle-occlusion (`f2fb18ed`), 0b (`ts-b17b13bf` 19/19 + Clayton flip), P0 bootfix arc (`157da876`/`5d119081`), horizontal frames a/b/c vs 3 legacy goldens (`test_tier_horizontal.py` + `ts-bfe365d4`), TTS-queue-DARK (`c2562b2e`); ends with the ONE subjective visual comparison Rick must make + exact URLs/steps; [**worktree-wipe in-container-prune forensics** (bug `47ac0e50`)](v0.1.9/2026.07.02-worktree-wipe-in-container-prune-forensics.md) — root-caused via a live in-container probe: `preflight-test-container.sh` + its pytest twin run `git worktree prune` inside `lupin-rest-test`, which bind-mounts `./.git` but NOT `lupin-worktrees/` → every host lane's gitdir reads as missing → prune wipes the ENTIRE host `.git/worktrees` registry (physical trees survive → zero data loss); fix = drop the trailing prune from both preflight sites + a reaper in-container guard (`_is_in_container` → skip prune when `project_root==/var/lupin`) + a regression guard test; [**focus bar empty fleet-wide post-pause triage** (bug `863682e3`)](v0.1.9/2026.07.03-focus-bar-empty-triage.md) — two-store divergence (polled `/api/arbiter/fleet-state` roster vs event-sourced senders-visible strip); suspect (b) voice-persona prune ELIMINATED firsthand (all 11 fleet personas intact; prune is PID-death-based not time-based); CONFIRMED = (a) 3h-pause activity-aging (Rick YES on hard-refresh + María self-repopulation ~15-20min), self-healed not a hard defect; follow-on UX = separate P3 decision; [**arbiter poke-storm arc — execution log with receipts** (bugs `92c7ab1d`/`8894e597`/`5a1f17f8`, CLOSED-VERIFIED)](v0.1.9/2026.07.03-arbiter-poke-storm-arc-execution-log.md) — the serialized close-out record: false-STUCK forensics (5 historical `cap_reached` in events file `8a92b253` replayed by offset-reset on BOTH the 21:47Z + 00:21Z `:8001` restarts → three 3-fire poke bursts 22:00–22:02Z / 00:48–00:51Z / 01:43–01:45Z, journal-receipted); three fix layers merged (`59ba12e5` bridge-mtime veto + one-way framing, `20d32319` freshness-gate + durable offsets `io/arbiter/event-offsets.json` + throttle); deploy 03:12:20Z by Rick's hand (harness classifier wall); Cheech firsthand verification — durable offset 94283 = events-file byte size (byte-0 replay structurally impossible), ZERO stuck-pokes 03:12→05:53Z across multiple restarts, the one 05:45Z `manager_stale` correctly LEGIT; [**CC Stop-phase pipe-hold: enforcement guard + fleet unwedge runbook** (bug `73d2b589`)](v0.1.9/2026.07.03-cc-stop-phase-pipe-hold-guard-and-runbook.md) — mechanism proven live on CC 2.1.199 (hook phase waits on stdout EOF, not process exit — a backgrounded grandchild's inherited pipe holds the Stop phase past the 60s timeout; queued msgs + Enter-immune + no drain); audit REFUTED the :842-leak premise (all 4 spawn sites redirect since ≥949cf6e8/v0.1.5) → deliverable pivoted to ENFORCEMENT: 11-test guard suite (AST sweep w/ negative controls + 4 seam pins + live pipe-hold demo pair) + unwedge runbook (kill the pipe-holder, NEVER bare-Enter) + spinner-age detection note; residual = 25c7441c's actual holder (hung in-turn blocking MCP call = ratified next chase)); [**fix design: fire-and-forget notify() must never hold a CC turn** (bug `73d2b589` remediation leg)](v0.1.9/2026.07.03-notify-turn-hold-fix-design.md) — 25c7441c holder PROVEN (transcript: notify tool_use 01:52:53Z → tool_result 02:47:39Z 'MCP error -32000: Connection closed' = 54m47s in-turn hang; :7999 dispatched fine ~01:53Z → stall = MCP server response leg; client HTTP leg is 1-30s-bounded, exonerated); levers: (i-a) MCP_TOOL_TIMEOUT env 660s fleet-wide config-only bound (recommended FIRST, low blast) + (i-b) notify respond-then-work via existing spool (HIGH blast, Rick-gated) + (ii) arbiter turn-age watchdog (detection); interim ops mitigation = kill the session's cosa-voice MCP process, never bare-Enter; DESIGN-ONLY, build awaits Rick's daylight ratification; [**arbiter re-announce notification flood — fix design** (bug `e1bbe011`, P1)](v0.1.9/2026.07.06-arbiter-reannounce-notification-flood-fix.md) — 3000+ duplicate "Operator-gate digest" rows on Rick's return root-caused to the intersection of three by-design behaviors: §3.5 re-announce (`_check_pending_outreach`) retries every 300s via the dedup-BYPASSING `live_retry_fn` → `/api/notify` persists a fresh forensic row UNCONDITIONALLY even on `user_not_available` → no durable idempotency (transport sends no key; endpoint cache is in-memory 60s < 300s cadence); FIX = option (a) `persist` flag (default True, byte-identical for all existing callers) — arbiter re-announce transport sends `persist=false` so retries re-attempt LIVE delivery WITHOUT minting a row (3000+ → exactly 1 forensic row/advisory, minted by the first escalation); options (b) retry-by-id + (c) outreach_id-idempotency rejected (c is FATAL — cached miss short-circuits delivery-on-return); flood-row cleanup Rick-gated + out of scope; [**arbiter spurious-poke fix — design (Phase 0)**](v0.1.9/2026.07.07-arbiter-spurious-poke-fix-design.md) — evidence RESHAPES the bug: of 557 `poked+work_owed:false` oracle rows, 548 (98.4%) are **test-log pollution** (`test_heartbeat_integration.py` synthetic sids `sidC2/C3/C6b` under a monkeypatched `"Mr. Radio 🦉"` persona writing to the PRODUCTION `hook-events.jsonl` via the unpatchable `hook_common.STREAM_LOG` module constant → manufactured the false "Mr-Radio-only, 40ms-burst" signature); the genuine FP is only **9 rows spanning 6 personas** (NOT Mr-Radio-only), each a **stale self-declared `work_owed:True` hold** poking via the DECLARED-owed path (`is_honored`=False + `declared_work_owed`=True) while the oracle sees nothing owed; fix = Lever P (env-overridable hook log dir → test-hermetic, kills Class 1) + Lever A (scoped structural gate in `decide_heartbeat`, kills the 9 real FPs = the ruling's (A)); recommends DE-SCOPE the ruling's (B) per-persona debounce as inert against days-apart real FPs, and PRUNE the never-populated `stale_inbound`; awaiting Tiberius sign-off; [**arbiter MANAGER-STALE false-fire on parked managers — design (Phase 0)**](v0.1.9/2026.07.07-arbiter-manager-stale-false-fire-fix-design.md) — the ARBITER-side analog of the same-day HOOK-side Lever A (`4162e89d`): `_check_manager_staleness` (`arbiter_job.py:3746`) keys on wall-clock last-signal alone, so a legitimately-parked/alive-on-a-cadence manager false-pokes (mr radio 13:08→13:59→14:59→15:59, four benign episodes while Rick-parked); THREE narrow, independently-inert, OBSERVABLE suppressions — Lever A hold-gating (honored `awaiting:user:*` hold ⇒ suppress case-14, hold-side dual of owed_class BLOCKED_ON_USER which the `6929f4ac` open-gate override reclassifies to ACTIVE), Lever B last-signal velocity (N advancing episodes ⇒ alive-on-a-cadence ⇒ suppress, non-advancing resets → real-stall still pokes), Lever C `-r2` ACK-by-activity (fresh recipient bridge-mtime since delivery = implicit ACK ⇒ skip the redundant resend, mechanism preserved per the `2026.06.30-arbiter-false-blocked-live-peer-and-r2-echo` prior art); each with a distinct log/receipt outcome (María's audit); RED-first vs the arbiter test harness; awaiting Tiberius sign-off; [**task_query unscoped-guard — design (Phase 0)**](v0.1.9/2026.07.07-task-query-unscoped-guard-design.md) — Rick-ratified Option A hard-fail on unscoped over-threshold `task_query` pulls + explicit `unscoped_audit=True` escape (item `21e325eb`); grep-audit FIRST surfaced the load-bearing finding — the arbiter + in-process watchers call `TaskRepository.query_tasks()` REPOSITORY-DIRECT (`arbiter_job.py:260/297/324`, `follow_through_escalation_watcher.py:195`), NOT the REST `GET /api/tasks` endpoint, so guard PLACEMENT decides whether the arbiter can break at all: Option A REST-only = arbiter safe by construction (no arbiter touch, no `:8001` restart, but not literal to Rick's "arbiter passes the flag") vs Option B repository-layer = universal + literal to Rick, arbiter-safe via atomic co-deploy (recommended); complete 8-caller inventory (both UI board cards do unscoped `?limit=500` pulls → need the escape), exclude-terminal-by-default (`include_terminal` escape), broadened "scoped" def to protect the arbiter's `gate_class="operator"` sweep; awaiting Tiberius sign-off on the placement fork; [**arbiter "one-time notice" loop-fire fix — design + fix (`58660c64`)**](v0.1.9/2026.07.07-arbiter-one-time-notice-loop-fire-fix-design.md) — the MANAGER-AWAITING-RICK case-16 advisory re-fired every ~60s poll tick (Mr. Radio ate 3 copies in 3 min); root cause = a CROSS-DETECTOR SHARED-FLAG PING-PONG (NOT a thread-id dedup key): `_poll_once` runs `_check_manager_acks` then `_check_manager_staleness` sharing `_manager_blocked_advised`; the acks path DISCARDS that flag on any liveness activity (`:3085`) while the staleness path RE-FIRES whenever the union `freshest_age_s` reads stale — and a parked-but-alive manager is ALIVE (heartbeat self-poke) yet union-stale (the `26dd3afb` bridge-vs-union divergence), so acks-discards→staleness-refires every tick; fix (Tiberius ruling option B) = an in-memory per-`(family, persona)` advisory COOLDOWN (~45m default, INI-promotable) gating all 3 case-16/17 emit sites independent of the flappy flag, cleared only on a GENUINE freshen re-arm (not the acks liveness discard), emitting `arbiter_advisory_suppressed_cooldown` on each suppression; RED-first multi-poll ping-pong test + fixes the existing one-time test's blind spot; `arbiter_job.py` 100% L/B/F, 1456 arbiter/heartbeat green; [**arbiter manager-subject hardening — completing the `ff91cff4` sweep (detection + routing)**](v0.1.9/2026.07.08-arbiter-manager-subject-routing-hardening.md) (bugs `e5e33795` detection-gap + `f48f089d` routing-gap, both P2 — SHIPPED as `3b208f65`/`f325b31d`: extends the `92c7ab1d` bridge-fresh veto to the parallel `_tap_managers` stuck-manager advisory path (mirroring `_auto_poke`), and extends `ff91cff4`'s Rick-only manager-subject routing principle to the sibling advisory cases 15/16/17 — case-17 MANAGER-DONE no longer fans "consider reaping <manager>" to peer managers; arbiter-only freeze-safe; Krishna code-review PASS + Clayton/Krishna/Sam 3-leg verify, 1604 unit/0 fail, `arbiter_job.py`+`arbiter_routing.py` 100% L/B); [**:8000 monopolize deadlock — lineage pass-through fix design**](v0.1.9/2026.07.11-monopolize-agentic-pool-deadlock-lineage-passthrough.md) (bug `3a14292b`, Clayton — the `30398595` Gate A/B monopoly machinery is ALREADY built; the deadlock is that Gate B intake-hold + the `get_non_test_inflight_agentic_jobs` drain classifier treat the monopolizing `test_suite` sweep's OWN spawned `swe_team` children as FOREIGN → 1916 consumer-log "deferring all intake" receipts starve them → 7/7 RED on ts-ad4670ec; recommends Shape A lineage pass-through — explicit `spawned_by_id_hash` field, monopolizer exports its `id_hash` to its pytest subprocess via `LUPIN_TEST_MONOPOLIZE_PARENT_ID`, child submits echo it back as `parent_id_hash`, Gate B admits + drain-classifier exempts jobs whose lineage matches the active monopolizer, foreign writers still blocked; NEVER `job_type` sniffing; Shape B test-suite-outside-pool assessed as a `pool_max=1` hardening follow-up; retires the stale `caf58f71` "no-op placeholder" claim; `:7999`-provable repro-in-miniature unblocks `67473d91`'s deferred E2E confirm; SHIPPED d28b496e→merge 1622b9af, :8000 sweep ts-dfc230a9 GREEN 314/0 — all 7 swe children dispatched mid-window, 1916→0 blanket-deferrals, = 67473d91's deferred E2E confirm satisfied); [**Shape-B hardening — monopolize sweep runs OUTSIDE its pool (design)**](v0.1.9/2026.07.11-monopolize-outside-pool-shape-b-hardening.md) (bug `fe375cf6`, Clayton — the deferred §7 hardening of the landed Shape-A fix: run the monopolizer on a DEDICATED single-worker executor so it stops eating a shared-pool slot → full width N free for children → `pool_max==1` deadlock-safe + `67473d91` budget math exact; crux = preserving the Future-based completion-callback + ghost-sweeper + `get_pool_status` observability invariants, solved by keeping the monopolizer's Future in the SAME `_agentic_futures` dict; `get_pool_status` gets an explicit monopolizer field so the documented `inflight_agentic_jobs` never silently counts a non-pool job; belt KEPT at first landing per prefer-reversible, removal deferred to a follow-up gated on Shape-B's own :8000 green; awaiting Mr. Radio plan-review); [**arbiter blocker→manager cc re-announce dedup**](v0.1.9/2026.07.11-arbiter-blocker-cc-reannounce-dedup.md) (bug `ce13b134`, P3, Clayton — the Part-6 #4 blocker detector throttles the blocker PING on its escalating backoff but re-sends the owning-manager cc "X is blocking worker Y … chase if they stay silent" on EVERY ping fire → manager flooded 3 identical DMs in ~6 min, each a fresh thread; fix = gate the cc on a `(blocker, blocked_item, recipient)` cooldown reusing the `58660c64` advisory-cooldown machinery, blocker ping untouched; the blocked-ITEM leg is REQUIRED — two sequential blocks with the same blocker+worker+recipient but different items (`99399723` then `ca8d6a19`) must each announce once, so a persona-only key over-suppresses [Mr. Radio's positive test case]; new `build_store_blocked_item_index` preserves per-edge item ids the persona-collapsing `build_store_wait_edges` drops; cooldown reuses `manager_advisory_cooldown_seconds` (2700s ≥ chase interval, `0`=legacy opt-out); secondary "arbiter DM-unreachable for chase-acks" wart → RECOMMENDS documenting commons `system-events` as the canonical ack path, NOT building an inbox into a headless observe-only service; RED-first verified [no-gate→3 ccs, persona-key→1 cc]; `arbiter_job.py`+`dependency_graph.py` 100% L/B, 1569 arbiter/heartbeat green; COMMIT-HELD pending APPROVE); [**focus bar keeps reaped workers' M-badged cards until refresh — root-cause + fix fork**](v0.1.9/2026.07.11-focus-bar-reaped-card-stale-until-refresh.md) (bug `0b2783ef`, P3, Clayton — complements Tiffany's LANDED option-2 `ff9aec9c`: the live reap→prune chain (`dismiss_sessions`→`_default_emit_reap`→`session_reaped` WS→`handleReaped`→`sessions.delete`) is verified CORRECT end-to-end incl. sender_id parity; the bug is the MISSED-frame case — owner's WS down at reap OR best-effort emit fails → stale card survives because `hydrate()` is additive-only and the reconnect re-hydrate merges without pruning, while a full refresh rebuilds from empty [implicit reconcile]; secondary "null-persona hydrate" wart REFUTED for the strip store [`hydrate` already skips null-persona `:261`]; fix = a reconcile-on-reconnect pass, RECOMMENDED option 1 = prune only in-snapshot-null-persona sessions [keeps the live-assign additive safety Tiffany preserved], with a released-but-alive icon-prune behavior change + a gated-`hydrate`-invariant touch flagged for Mr. Radio's call; DESIGN-only, build pending approach nod); [**arbiter: distinguish a DESIGNED review-gate hold from a real stall**](v0.1.9/2026.07.12-arbiter-review-gate-hold-vs-stall.md) (bug `cec10ef9`, P3, Krishna — one layer ABOVE Clayton's `ce13b134` dedup [dedup throttles re-announces; this suppresses the announce of a designed hold]; verify-first CONFIRMED live [3 pure repro tests, `test_arbiter_review_hold_fp_repro.py`]: `_attention_workers`' `stuck` short-circuit [`arbiter_job.py:2390`] rosters a worker `blocked_by` its review-gate operator [Rio→Sam, item `ca8d6a19`] because the announce path takes NO owed/store arg → fires `Stuck: Rio` to the manager; fix = thread the EXISTING per-poll authoritative owed read [`:1236`] into the announce path + suppress a worker whose EVERY non-terminal owed item is a store-backed review-gate hold [`status=blocked` + `blocked_by` kind=persona] on a LIVE operator peer; fail-safe every uncertain direction [store hiccup / dead operator / mixed owed work → keep rostering, never hide a real stall]; NO new store schema or worker write-path [the `blocked_by`-persona row workers already write IS the declared hold]; `_item_is_user_gated` + the dedup layer untouched; Mr. Radio's 00:42Z "Blocked: maria" ZERO-store-rows case flagged as a DISTINCT stale-`holding_on`-edge bug to file separately; DESIGN-only, COMMIT-HELD pending APPROVE); [**arbiter: store-corroborate the blocked-edge roster (stale `holding_on` edge, zero store rows)**](v0.1.9/2026.07.12-arbiter-unbacked-blocked-edge-roster.md) (bug `1ff7be20`, P3, Krishna — the split-off from `cec10ef9`; 3 live FP advisories across 2 personas, the last on FULLY-PATCHED code [`ad0f6199` + `cec10ef9` both live], so the path survives both by design; journal ground truth: `build_fleet_view` mints `holding_on` straight off the freshest heartbeat `awaiting` field [`fleet_data_model.py:437`], which is FREE-FORM and STICKY — maria carried `peer:sam-and-reviewers` [a prose label naming NO fleet persona] with `work_owed:false` for ~6h on a 53s-fresh bridge, so the absent "peer" read as not-alive and the `bbce7e2f` dead-peer FAIL-SAFE rostered her → false "Blocked: maria" to her manager against ZERO store rows; the real finding is an ASYMMETRY not a gap — the same poll logged `edges=1, pings_fired=0, taps_fired=1`: the PING leg is ALREADY store-corroborated [`edge_is_store_backed`, `d44b7068`] and fired zero off that edge while the ROSTER leg consulted no store at all; fix = thread the SAME per-poll `store_edges` [already computed, no new query] into `_tap_managers`→`_attention_workers` and drop a blocked-edge holder whose edge has no store `blocked_by` backing, journaling `arbiter_unbacked_edge_suppressed`; FAIL-SAFE to ROSTER in all three uncertain directions [store read failed/unwired → no filtering; deadlock-cycle member always kept; canonical persona keying both sides]; SCOPE FENCE — `_item_is_user_gated`, the `ce13b134` dedup, `cec10ef9` semantics, and the STUCK leg all untouched [the sibling false "Stuck: sam" rides the `_count_stuck_episodes` cap_reached axis = a DISTINCT root, filed separately]; RED-first 11/13 + 2 characterization; `arbiter_job.py` 100% L/B/F; 9041 unit / 0 fail; COMMIT-HELD pending Mr. Radio review); [**arbiter: the stuck advisory must honor the bridge-fresh veto**](v0.1.9/2026.07.12-arbiter-stuck-advisory-bridge-fresh-veto.md) (bug `3287ee1e`, P3, Krishna — sibling of `1ff7be20`, THIRD instance of the same asymmetry family [a correctness gate wired into one consumer of a signal but not its sibling]; verify-first REFUTED both filed premises: `92c7ab1d` is NOT held [merged live 5d4e7baa→59ba12e5] so its `_session_bridge_fresh` primitive is REUSED not rebuilt, and sam's cap_reached records were GENUINE + FRESH [01:00/01:08/01:15Z, two live caps, no intervening `honored`] — the stuck FLAG was computed correctly, the ADVISORY was wrong; smoking gun = ONE poll [07-11 21:12:20 EDT] emitting BOTH `arbiter_stuck_bridge_veto persona=sam bridge_age_s=8.6` ["don't poke — he's alive"] AND a "1 stuck/dead" tap naming him to his manager — the veto was wired into `_auto_poke` [every role] and the manager-SUBJECT advisory [e5e33795] but NEVER the WORKER-subject roster leg; fix = apply the EXISTING veto in `_attention_workers`' stuck branch [bridge_mtimes already threaded via bf8c5cbb — no new plumbing, no store read: a wedged worker often CANNOT write store rows, so store-corroboration is the WRONG shape here]; FAIL-SAFE to ROSTER [mtimes unwired / persona absent / STALE or future-skewed bridge / now=None / deadlock-cycle member → keep announcing; a wedged session stops emitting hook stamps so its bridge goes stale and the true positive survives]; e5e33795's worker-exclusion SCOPE PIN INVERTED + its now-unreachable downstream veto copy REMOVED [coverage proved it dead once subsumed upstream — subtraction, not behavior change; Mr. Radio ratified after archaeology: the exclusion was blast-radius scoping, never a positive assertion]; SPLIT-OFF (B) filed separately = `_count_stuck_episodes` consumes prior caps only on `honored`, so an `idle` recovery consumes nothing → PERMANENT LIVE-STUCK [shared-signal semantics change, own verify-first sweep]; RED-first 5/14; `arbiter_job.py` 100% L/B/F; 9056 unit / 0 fail); [**arbiter: stuck-episode consumption semantics — an `idle` recovery must consume prior caps**](v0.1.9/2026.07.12-arbiter-stuck-episode-consumption-semantics.md) (bug `52b8ed6b`, P3, Krishna — split-off (B) from `3287ee1e`, the AT-SOURCE correctness debt: `_count_stuck_episodes` consumed a prior `cap_reached` ONLY on a later `honored`, so a session recovering via the `idle` beacon consumed NOTHING and stayed flagged `stuck` until the events tail rolled = PERMANENT LIVE-STUCK [sam announced stuck at 21:49 EDT while idle, owing nothing, bridge 0s fresh, from caps recovered 8h earlier]; DESIGN-FIRST per Mr. Radio [shared-signal semantics change]: grep-complete sweep = 1 producer + **10 consumers** [6 arbiter_job · 2 fleet_render · 2 TS UI] each with a per-consumer impact statement; governing property = the change is **MONOTONE** [flag can only flip True→False, never False→True ⇒ no consumer can EVER see a new stuck session, only fewer] — the argument that makes a 10-consumer blast radius tractable; full writer-side outcome vocabulary enumerated with EMPIRICAL fleet-wide counts [honored 3062 · poked 1160 · idle 705 · cap_reached 94 · not_owed **0 = defined but NEVER written** · suppressed_stale_declared_owed 0]; ratified RECOVERY CLASS `{honored, idle}` under Mr. Radio's membership rule — *a recovery outcome is a beacon THE SESSION ITSELF EMITS* [so `suppressed_stale_declared_owed` is excluded on principle as an arbiter-side marker, not merely on its zero count]; load-bearing verify-before-propose: the `idle` record carries `kind:None` ⇒ it IS in `activity_recs` — had it been `idle_prompt`-kind the entire remedy would have been **INERT**; true positive preserved BY CONSTRUCTION [a wedged session owes work ⇒ can never emit `idle`, which requires work_owed=false]; non-redundant with (A) [(A)'s bridge veto is blind to a recovered-but-QUIET session with a stale bridge]; RED-first 5/14 + blast-radius pins per ACTUATING consumer; `fleet_data_model.py` + `arbiter_job.py` both 100% L/B/F; 9070 unit / 0 fail; UI needed NO code change as the sweep predicted); [**LoC roll-up: branch-agnostic window + commit de-duplication**](v0.1.9/2026.07.13-loc-rollup-branch-agnostic-window-and-commit-dedup.md) (bugs `bbff93a3` + `37a8beeb` — the roll-up read stale BRANCH-mode CSVs (`main..<branch>`), so work that landed on `main` was on the baseline side and **structurally uncountable**: skills-distillation under-reported by exactly 1,607 lines, 36% of the repo's existence; the "refresh stale CSVs" step that should have prevented it was **unrunnable** (`--branch` and `--since` are argparse-mutually-exclusive) and Step 4 degraded the failure INTO staleness silently; the aggregator also SUMMED a per-`(date,file_type)` commits column, double-counting any commit touching two file types (86 vs 63). Fix: the roll-up now **computes from git** (date-windowed, `--branches`), CSVs demote to artifact; commit counts are only ever a `len()` over a SHA set; new **coverage guard** reconciles counted SHAs against an independent `git rev-list` oracle and warns LOUDLY. One reported defect **did not survive verification** and was withdrawn with receipts — the "phantom" row was real work. Self-review then caught two bugs *in the fix*: a fresh zero-commit repo aborted the whole roll-up, and colliding repo basenames silently overwrote each other's commit counts. Reconciles to raw git to the line, every repo; 100% L/B); [**worktree orphan census + cleanup — empirical anchor for a lifecycle policy**](v0.1.9/2026.07.14-worktree-orphan-census-and-cleanup.md) (Clayton, for María's policy `c41ba07c` / census `e9435e22` — cleaned the repo 61 branches→16 and 28 worktree dirs→1; the load-bearing finding is a **two-layer census** (`git worktree list` saw 20; 8 more were unregistered filesystem orphans whose admin entries were already pruned → the registry undercounts, the filesystem is truth) and **one near-miss that false-cleaned**: `lane-arnold-backlog` held 9 files of edits present in NO commit, yet `git status` on its dangling-admin `.git` returned empty = "clean", a MAIN-index diff false-DIRTIED on an unrelated session's staged deletion, and a bare isolated-index read false-dirtied on an empty stat cache — only `GIT_INDEX_FILE` + `read-tree` + `update-index --refresh` + `diff-index` told the truth (the edits proved a superseded earlier draft of `task_amend`, which landed in dev by another route — no loss, but the mechanism failed the test); policy recos = guard at `git worktree add` (record owner + reap contract; admin dir dies with the owner, a sidecar manifest survives), janitor verifies with a refreshed isolated index NEVER `git status`, rescue-to-patch before any `--force`, census from the filesystem not the registry; rescue artifacts (45M branch bundle + the near-miss patch, verified to apply) parked in gitignored `io/worktree-cleanup-2026.07.14/`); [**tmux fleet-killer — vertex-taint smoke test isolation-leak fix (PLAN — awaiting cascaded review)**](v0.1.9/2026.07.14-tmux-fleet-killer-vertex-taint-test-isolation-leak-fix-plan.md) (the shared user tmux server died 5× in a day, each death taking every session across all 3 projects; multi-agent Workflow `wf_a71ea905-64f` + author read-only precedence probe root-caused it to `src/tests/smoke/test_vertex_launcher_server_taint.py`'s fixture `kill-server` leaking onto the DEFAULT socket — `_tmux`/`_launch` inherit `$TMUX`, and on tmux 3.2a `$TMUX` BEATS `TMUX_TMPDIR` for socket selection [proven live; the file-history "TMUX_TMPDIR beats $TMUX" note was a non-pane false-green]; proposed fix = strip `$TMUX`/`$TMUX_PANE` in the helpers + a suite-wide `conftest.py` guard [the class fix] + a killtrace execve-argv upgrade to NAME the caller next time; test QUARANTINED to gitignored `src/tmp/` pre-review so it cannot fire during review+impl); [**cascade revision-handoff (Step 9)**](v0.1.9/2026.07.15-cascade-tmux-fleet-killer-revision-handoff.md) (the `cascade-tmux-fleet-killer` close-out artifact: 34 findings closed [A 11 · B 12 · C 11, 0 foundational, 0 votes, 0 escalations, ~55 min], per-section revision summaries, all 6 OSQ dispositions [OSQ-1 Rick-confirmed 03:26Z: execve tracer LAST; OSQ-4 env-strip-sufficient w/ AC5 precedence canary; OSQ-5 no-collision — vertex lane cleared for edit+restore], cross-section dependency map, post-cascade fold bundle [3 store mints incl. vertex-lane continuity], 19 workflow-guidance candidates, and the SWE-team hand-off statement [full /spin-up-swe-team crew; implementer cold-context-briefed on this doc + plan alone]); [**tmux fleet-killer — Tester §7 execution record (third leg GREEN)**](v0.1.9/2026.07.15-tmux-fleet-killer-tester-execution-record.md) (Cheech 🌿: full §7 matrix from inside a fleet pane — py_compile 10/10 · AC5 precedence canary 2/2 · AC2 guard regression 1/1 · killtrace-observation probe 1/1 with the INSTRUMENT receipt verified [self-SIGTERM sender==target==throwaway pid 619838, fleet 387516 untouched] · §10-5 gate pre-capture SUCCEEDED · AC1/AC4 pane-run 4 pass + 1 named env-skip with fleet pid 387516 pre==post · AC3 100% L/B/F on all changed code [conftest changed-hunk proof via coverage-json ∩ diff-hunk]; includes the pytest-cov-can't-measure-conftest instrument lesson); [**Vertex spend guardrails — Cloud Quotas rate-cap request (DRY prep)**](v0.1.9/2026.07.15-vertex-quota-clamp-prep.md) (Krishna 🦚, store 29e8e3e3 for Rick's 17:00 EDT c9dd0cc3 chase: primary-source prices [deepseek-v3.2 $0.56/$1.68 · gpt-oss-120b $0.09/$0.36 · opus-4-8 global $5/$25, us-east5 table has NO 4.8 row — global-only corroborated] + $50/day→TPM worst-case math [defaults permit $72.69/day on the two MaaS models alone; proposed clamps sum $47.55/day] + per-posture A/B/C copy-paste QuotaPreference blocks w/ read-back + rollback + $1k/mo 50/80/90 budget-alert pair + console-only BQ-export click-path + must-429 canary design; ZERO GCP calls — all writes are Mr. Radio's on Rick's word); [**Vertex/Model Garden per-session toggle + web search + request-response logging — DESIGN, REV. 4**](v0.1.9/2026.07.13-vertex-model-garden-toggle-search-and-logging.md) (the vertex-toggle lane's load-bearing design doc — adopted-gap link, REV. 4 fold 2026-07-15: `--vertex` on `start-cc-with-tmux.sh` w/ pane_guard + OSQ-6 both halves LANDED [0d513fc3, df69cee1], SSOT=`global` [us-central1 NOT servable — metadata-200 is the known liar], Monitoring `PublisherModel` as the independent pilot oracle, AC-D0b–D9b, spend guardrails folded [bare-slug ruling settles OSQ D-2], §7 the oracle-calibration lesson canon); [**Vertex logging oracle semantics — AC-D7/AC-D5 + harness record**](v0.1.9/2026.07.15-vertex-logging-oracle-semantics.md) (Krishna 🦚, store cda7bf8b legs 1-5: sink verified EXISTS-but-EMPTY via routed reads [13-field schema, full-payload columns ⇒ 823be9cc "schema captures everything"; numRows=0 vs 2,745 out-TPM live meter = THE TELL]; AC-D7 = inadmissible-until-canary-row w/ measured-not-assumed ingest latency; AC-D5 = INDETERMINATE-not-FAILED when blind; legs 4/5 = clobber guard [21 tests 100% L/B, committed bfe33d7c] + double-write proof script, BUILD-only); [**Orphaned-worktree clear-pass — completion record**](v0.1.9/2026.07.15-orphan-worktree-clear-pass.md) (Krishna 🦚 value-check + rescues, Mr. Radio 🦉 executor on Rick's LIVE GO: 31/31 removed, 5,787 MB reclaimed, post-census 0; ~106k files blob-audited [12 CLEAN / 17 ARTIFACTS-ONLY / 2 WIP]; rescue branches 93f879c8 [Tiffany memento] + 47460d95 [ten-column notnull draft] verified pre-AND-post rm; §4 the authority arc: two seats' classifiers walled the relayed GO, bare-default asks ≠ answers, executed only on in-session consent); [**Freed-persona-name-reuse disambiguation — design proposal**](v0.1.9/2026.07.15-persona-name-reuse-disambiguation-proposal.md) (Krishna 🦚, bug `91067e47`: freed persona names re-granted to next requester with nothing distinguishing new bearer from old — 4 sightings today [cross-fleet Krishna probe, wrong-Sam DM bounce, @Cheech mis-land, Rachel re-grant]; splits into operator-perception vs fleet-addressing modes; compares (a) cooldown-quarantine [source-side window-reducer, can't touch long-lived/cross-fleet] vs (b) session-id-suffix rendering [structural root-fix across all 4, render-not-identity change since strip already keys by session_id] vs (c) both; **recommends (c) phased, (b) leading**; sibling-but-distinct from render-delivery `179fe3ab`); [**`parked` status — board hygiene for the unified task store**](v0.1.9/2026.07.19-parked-status-board-hygiene.md) (store record `954428b3`, gate `522f4815` Rick-approved; **26 rows** (Rachel 🕊️'s row-by-row derivation — see the [companion marker-predicate + triage artifact](v0.1.9/2026.07.19-parked-status-marker-predicate-and-triage.md); the "~18" the crew was briefed with is superseded, and a divergent number is a **finding**, not something to reconcile away) present READY while their own BODY forbids working them — "NOT TO BE WORKED per Rick's direct instruction", "TABLED, NOT CLOSED", "the number IS the deliverable" — so the burn-down is fiction and the owed oracles count un-actionable rows, the root cause of the false pokes flag-silenced in `09c3fcc0`; adds non-terminal `parked` + required `park_reason` column, REUSES `next_chase_ts` as the chase [Rick overruled a review ruling that added a redundant `unpark_when` — §3.2.1 records the correction as the lesson]; **self-expiry computed at READ time** so a passed chase rejoins the owed count with no daemon/sweeper/human; park-suppression is **ADDITIVE** to each reader's existing filter, never a replacement [AC11]; María 🌸's Q1 rationale — no `blocked_by` kind means "a human ruled this not-now", so the hold is unrepresentable in the field the readiness predicate reads + Sam's in-vocabulary/out-of-vocabulary framing; §5 publishes the **marker predicate** as a refutable artifact [imperative + row-level + **attributed**] after two catalogs mis-counted this board from titles; Rachel's sweep additionally isolates **5 BLOCKED-IN-PROSE rows that must NOT park** — they name their own blocker while `blocked_by` sits empty, and parking one would launder a missing dependency into a deadline, so they get their own P1 row); [**spawn-child persona allocation — live receipt**](v0.1.9/2026.07.19-spawn-child-persona-allocation-receipt.md) (closes `179fe3ab` by re-derivation rather than assertion: child booted NAMED and the parent reached it BY NAME — the exact call that returned `recipient_not_found` on actor `<unknown>`; the probe brief **forbade `request_persona`** so a null child could not self-heal into a manufactured green, and the child's own "I cannot verify I am a spawn child" objection was closed from the PARENT side via transcript + session-env window + lineage binding; **scope limit stated in the doc** — spawn-child path only, the top-level SessionStart null remains open under `86aa79ac`); [**post-game — assertions that cannot fail**](v0.1.9/2026.07.22-swe-crew-post-game-assertions-that-cannot-fail.md) (two-seat SWE crew retro, María 🌸 + Mr. Radio 🦉: task-list under-reporting `4bec617b` — 1,171 rows against a 500 cap, 671 dropped SILENTLY, newest-first so the evicted rows were the OPEN ones; and the poke-mute false-idle `b65191ec` — a muted worker's card published "nothing owed" about a board the mute deliberately never read. **The finding**: FOUR checks that day could not observe what they claimed, the last one written immediately AFTER both engineers named the pattern to each other — so naming it does not defend against it; only reverting the guarded thing and watching it go red does. Plus: a guard keyed on COMMITTED state is structurally blind to a pre-commit run. NOT graduated to workflow/ doctrine — pattern-watch pending recurrence); [**VM DM `missing_auth_header` — two stacked defects behind one error string**](v0.1.9/2026.07.25-vm-dm-outbound-key-two-stacked-defects.md) (`bba39eed`; on `lupin-host-test` a mode-600/uid-1001 key file raised PermissionError that a bare `except Exception: return None` erased — and UNDERNEATH it the key VALUE was the dev box's, rsync'd at provisioning and unregistered in that server's DB, so the chmod moved the error from `missing_auth_header` to 401 without restoring service. Loader moved to `outbound_api_key.py` keeping the return contract but adding a diagnosis channel naming path + mode + owner uid; `install-cosa-voice.sh` gains check 5a READABILITY — `os.path.exists()` passes on the exact shape that broke — and 6a server ACCEPTANCE, added because 5a alone sails past defect 2, control-verified 200-vs-401. Nine dev credentials + the dev-key `.bak` then removed from the VM on Rick's instruction, hashed byte-identical against dev first); [**VM bring-up automation — classified fix inventory + `preflight-vm.sh` design**](v0.1.9/2026.07.26-vm-bring-up-automation-preflight-design.md) (Rick's P0 `47c4801b`: five days of lupin-host-test debugging classified into 30 defects — 11 ENVIRONMENT, 7 CONFIG drift, 5 CODE BUG, 5 INSTALLER/TOOLING, 4 FILES-GIT-CANNOT-DELIVER — each routed to repo / provisioning / preflight / static gate, plus a 30-assertion preflight design in 5 layers. The finding that justifies it: `lupin-vm.sh deploy` missing `--no-deps` was written down 07-25 with the words "Filing recommended", was never filed, and took :7999 down on 07-26 in the very first deploy of the next session — prose is not a guard); [**Configuration splintering across dev + test — analysis & recommendation**](v0.1.9/2026.07.26-configuration-splintering-analysis.md) (Rick's precondition to the preflight build: did WE cause part of the config problem? Verdict — yes, about a quarter of it, but the defect is not 'too many files', it is THE SAME FACT ASSERTED IN N PLACES WITH NOTHING COMPARING THEM. Counted authorities per fact: 4 hand-maintained compose mount-lists for one service shape, 5 surfaces + 2 independent validators for 'the notification API key', 3 similarly-named knobs for 'which config block'. Recommends AGAINST a master config file and against compose overlays this week; recommends a compose-parity test, a creds-status verb, and an in-repo env-var contract — generalizing the two patterns already proven right here, the CC-hooks template and the doc-scope parity test); [**backend-blind `db_path` remediation**](v0.1.9/2026.07.27-backend-blind-db-path-remediation.md) (decision `2b20a6d6` ruled both arms — reconcile call sites THEN raise; every offender isolated or removed, no allowlist. Sweep corrections: 3 live call sites → **5** (a pre-construction factory gate + `gister.py`), 6 test offenders → **5** (one was a docstring `Usage:` example, AST-confirmed), and the contaminated store is **`lupin_db_dev`** not `lupin_db_test` — `LUPIN_ENV` is unset for unit runs so `database.py` defaults to development, while BOTH sanctioned runners export `testing`. One fact had **4 authorities** and no comparator. Scope was not 6 tests: **345 unit tests across 9 files** were silently on the shared-store path; unit suite 38→6 failed, 0 new, **30 pre-existing failures fixed** as the same bug wearing `AttributeError`. `_PG_ISOLATION_MODULES` deleted; 14 guard tests control-verified in both directions); [**GCP VM dual-stack `:8000` bring-up — design**](v0.1.9/2026.07.27-vm-dual-stack-8000-bring-up-design.md) (row `f3b5ecf3`, DESIGN ONLY — Rick ruled COEXISTENCE, both stacks bound as on the dev box; the `7998` clash in the row's body is **struck** (cloud-gpu publishes nothing there), so the remap is `:7999`→`8000:7999` only. Two blockers named: preflight's 31 assertions are all written against ONE app on `:7999` and would go **green while blind to half the machine**, and the proxy topology is unresolved — both compose files declare their own `cloudsql-socket-init` with identical `container_name`s, so a second stack's init `rm`s the live proxy's socket, re-arming `70794d58` by topology rather than by a missing flag, where `--no-deps` cannot help. Recommends two proxies + two socket volumes; ruling is the VM seat's); [**GCP VM dual-stack `:8000` — implementation plan**](v0.1.9/2026.07.27-vm-dual-stack-implementation-plan.md) (row `f3b5ecf3`, PLAN ONLY — the ruling's remedy is right and its premise is wrong: the hardcoded `container_name` guards nothing under the invocation this repo actually uses. Measured, two arms: with a DISTINCT compose project docker refuses the duplicate name and the socket survives (the ruling's regime, reproduced); with the SAME project — no top-level `name:` in either file, no `-p` at any call site, `COMPOSE_PROJECT_NAME` nowhere in the tree, both files at one root, so `/var/lupin` -> project `lupin` for both — compose does not conflict, it RECREATES: socket deleted, `rm` ran twice, and stack A's app container destroyed outright. So the deletion is already armed and the gating act is the first `up` of stack 2, not a rename. Pinning a per-file `name:` is the load-bearing half; landed guard `test_compose_dual_stack_project_isolation.py` (11 green, 4 mutants killed as predicted, no-op control survived) turns red on a rename-only diff and green on the correct one-commit shape); [**Vertex OSQ C-1 was answered on 2026-07-13, by accident**](v0.1.9/2026.07.27-vertex-osq-c1-already-answered.md) (row `e919d895`, precondition for gate `31f6d447` — FINDING ONLY, nothing issued, nothing written, nothing spent. The Phase-1.1 probe's own rule is *set the config WITHOUT `dataSharingEnabledProvider`, issue one request, rejection ⇒ required*. That experiment already ran: `fetchPublisherModelConfig` 404 at all three regions (no config object at all ⇒ the field could not have been set) while Rick's `StreamRawPredict` at `global` returned **HTTP 200 ×2**, corroborated by `model_invocation_count` 0→1→2 and, two days later, by a quota refusal reporting **2745 recent usage** against the bare slug. ⇒ **NOT required — Rick gets the OPTIONAL question**, and the paid probe should not run: it would pay a full-object-SET clobber hazard plus a real request to re-learn a settled fact. Limits stated: the success arm is weaker than a rejection would have been, Rick's console EULA ~90s earlier may have recorded consent by another path (unverified, flagged), `global` only, and none of it re-measured — `cloud-run.env` is absent from the tree, so the project cannot be addressed even read-only. **§3a: that read was then TAKEN and it CONFIRMS — config present at `global`, `dataSharingEnabledProvider` ABSENT, and `model_invocation_count` shows 509 anthropic invocations in 14 days with buckets ending TODAY. C-1 = OPTIONAL, on a live self-taken measurement, zero cost and zero write.** Two corrections against the author's own name: the doc's refutation condition (*"a config coming back PRESENT refutes this"*) was the WRONG PREDICATE — the discriminator is whether the FIELD is set, not whether a config exists; and the source doc's *"404 at all three regions — no config anywhere"* row is now STALE (a logging config with a BigQuery sink has been written since), as was the claim that `cloud-run.env` is absent — it is at `src/scripts/`, one directory from where the design doc names it); [**`src/cosa/tests/**` survey — the RUN half**](v0.1.9/2026.07.27-cosa-tests-survey-run-results.md) (row `5bf28e07`, MEASUREMENT ONLY — nothing wired. `41e17369` proved the tree COLLECTS; this is the half that had never run. **8,771 pass / 4 fail / 0 error / 24 skip out of 8,799 run.** Option 1's *"could red the gate for days"* **does not survive measurement**. All four failures attributed: 2 are an unset `HF_HOME` in the runner shell — an ENV hole presenting as broken tests, same shape as the `LUPIN_CONFIG_MGR_CLI_ARGS` gap; 1 has been red since **2026-07-15** (a migration landed on the head it pins, 12 days invisible); 1 went red **2026-07-26** when the `8b93bcf5` P0 fix — *the* lying-zero fix — broke a test asserting a tier reports its error. **Two shipped regressions nobody could see, which is the row's thesis with dates on it.** Also: **11 of 412 files collect ZERO tests** (`class TestAgent( AgentBase )` has an `__init__`, so pytest refuses it) ⇒ the population is 401 contributing files. Count moved 8,798→8,803 under the measurement itself; what rules out lost tests is a control — per-chunk collect sum == whole-tree collect, exactly. ⚠️ The survey EXECUTED a live-dev-Postgres contact (`test_check_schema_parity.py`), and its `skipUnless(_pg_reachable())` gate connects at MODULE IMPORT — so collection alone touches the DB and deleting the test class is insufficient); [**`src/cosa/tests/**` assertion census — do the green tests ASSERT?**](v0.1.9/2026.07.27-cosa-tests-assertion-census.md) (row `f9eb6c3c`, the companion to the run above — MEASUREMENT ONLY, nothing fixed. AST, never grep: a grep for `assert` matches a *description* of an assertion. **8,555 of 8,740 (97.9%) have a proven reachable assertion; 185 have none in their own body or any same-module helper.** 🔴 **14 provably CANNOT FAIL** — they detect failure and report it by `return`, which pytest discards, and `pytest.ini` sets no `filterwarnings` to escalate the warning. Proven with a fixture returning `(False, 0.0, "it failed")` → `1 passed`. That is `5bf28e07`'s *"a suite that cannot go red is indistinguishable from a suite that passes"* in its **running** form. Also 2 tests whose NAME claims a property the body never checks — `test_cli_mode_sets_mode`, `test_output_dir_absolute_already_under_root_unchanged`. ⚠️ The instrument had **two bugs a code read missed and only a control fixture caught** — builtins scored as unresolvable helpers, and a blanket refusal to descend into nested defs that `asyncio.run` plainly invokes; both made the tree look *healthier* than it is); [**Durable tier-run attestation — design**](v0.1.9/2026.07.27-tier-run-attestation-design.md) (row `691d49db` P1, DESIGN ONLY — and it **corrects its own requirement**. The brief said "written somewhere the tests cannot write"; measured, `job.py`'s pytest Popen uses `env={**os.environ, …}`, so the subprocess inherits EVERY credential the orchestrator holds, same uid, same container ⇒ **no store the suite cannot write exists**; the `TFE_/BFE_/LUPIN_TEST_` allowlist filters only caller-supplied vars, not inheritance. Achievable property is **TAMPER-EVIDENCE**: append-only JSONL under the `io/` bind mount (survives the recreate that wipes container `/tmp`, confirmed by `docker inspect`) with a `prev_sha256` chain — anyone may append, nobody rewrites history undetectably. Artifacts move off container `/tmp` in the SAME change, with a static AST guard generalizing Krishna's `fd0cd863` (a fixture is writing the tier's REAL triage log path), because a moved path without a guard re-arms the defect at the new location. Coverage unit-only by construction; the 0→N calibration stays explicitly OWED — unit-green is not calibration, and an empty store must never read as valid); [**aggregate day review — 2026-07-27**](v0.1.9/2026.07.27-aggregate-day-review.md) (Rachel; whole-day review of `da1a5ed8..59038897`, 48 commits / 109 files — nobody had reviewed the day as a whole. ONE real defect: `test_vertex_env.py:181-184` still pins `== 15` after `e9e8087d` re-harvested the tuple to 16, half-landed and RED in the shared tree. 3 commit-message controls re-run and reproduced exactly incl. byte-identical restores; `pytest.ini` `--strict-markers` confirmed to strand no marker at 21745/21752 collected, zero errors; `ac7a4ad5`'s 100% lines+branches verified. Records two reviewer near-misses — a `host_onyl` "typo" that was a synthetic control matched via its own docstring, and a 44% coverage reading from a denominator narrower than the claim's); [**name-vs-predicate audit of Rio's instruments**](v0.1.9/2026.07.27-rio-name-vs-predicate-audit.md) (Rachel, assigned because the author declined to grade his own checks — the A5 test: is the NAME broader than the PREDICATE, and is the check in the right VENUE. 1 finding of 4: `verify-running-code.sh`'s exit-0 "HAS the commit" is sound only where code is BIND-MOUNTED — on `docker-compose.cloud-test.yml` code is BAKED into the image with no `./src` mount, so a container started at T reports HAS for every commit before T regardless of the image's age, and the container name is a free argument. Remedy: return the existing exit 2 CANNOT_DETERMINE where the tree isn't mounted. The other three CLEAR on both axes — `test_timeout_marker_is_armed.py` already carries the positive-control arm row `ed4df64a` asks of every gate); [DM-verbosity-reduction: local vLLM model pick (deep research)](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.07.31-verbosity-reduction-deep-research.md) + [purpose-built install plan for Mistral Small 3.2 24B](v0.1.9/2026.07.31-vllm-mistral-small-24b-install-plan.md) (planning-only — checkpoint choice still open); [HF_HOME disk cleanup plan](v0.1.9/2026.07.31-hf-home-disk-cleanup-plan.md) (DATA01→95% used; ~1.05TB of stale LoRA checkpoints identified for removal, gated on a DATA02 backup of the live `svllmr` checkpoint first); [**Merge-gate :8000 concurrent-writer contamination — design**](v0.1.9/2026.07.07-merge-gate-concurrent-writer-contamination.md) (bug `caf58f71`, split-out from `8bd20375`: while a merge-gate sweep runs on `:8000`, other fleet agentic jobs write the shared `lupin_db_test` concurrently → `refresh_tokens` duplicate-jti flood; NEW root-cause finding — `monopolize=True` is an unenforced no-op placeholder; recommends option (c) preflight guard now — fail loud when a non-test agentic job is inflight at sweep start — + option (a) true-monopoly wiring as the gated durable follow-on); [**Option (a) — true pool-intake monopoly — design**](v0.1.9/2026.07.07-monopolize-true-pool-intake-monopoly-design.md) (follow-on `30398595`: wire the `monopolize` no-op placeholder into a two-gate consumer — drain-before-dispatch + hold-foreign-agentic-intake-during-monopolize, reusing the caf58f71 classifier as oracle; 4 decisions for Tiberius sign-off BEFORE code); Next dev branch: [**LanceDB → PostgreSQL + pgvector migration — design**](v0.1.9/2026.06.30-lancedb-to-postgres-pgvector-migration-design.md) (Rick-ratified 2026-06-26 abandon-LanceDB arc; key finding — Postgres is ALREADY Lupin's relational store, so the migration is additive: enable `pgvector` on the existing DB + port 6 vector tables alongside the existing repository layer; recommends HNSW + cosine, per-table backfill-vs-fresh-start, migration-window backend flag with remove-after-soak; 6 open questions = the cascaded-review agenda); [**pgvector image force-recreate — explainer**](v0.1.9/2026.07.01-pgvector-image-force-recreate-explainer.md) (Rick-facing card for the shared-infra gate: one compose line `postgres:16.3-alpine`→`pgvector/pgvector:pg16`, same data bind-mount so data untouched, ~1 min downtime, trivially reversible pre-migration, co-land rule, greenlight playbook); [**venv pgvector gate execution receipt**](v0.1.9/2026.07.06-venv-pgvector-gate-execution.md) (Rick-authorized gates `7d50a03a`/`c90f24f4` closed: `pgvector==0.4.2` into `src/cosa/.venv` — the hooks + cosa-voice MCP interpreter; import chain + 93 listener unit tests verified green); [**swap-chain execution log + flag-flip readiness report**](v0.1.9/2026.07.07-pgvector-swap-chain-execution.md) (Rick GO on gate `d93a1edb` executed 2026-07-07 AM: dev+test recreated onto `lupin:1.1.1-pgvector-candidate`, durable-PG verify, full dry-run, LIVE ~202k truncate-then-load backfill, dual-engine knn equivalence spot-check; straggler-delta caveat = re-run backfill at flip moment; GCP leg filed `c845346a` blocked on Rick); [**LanceDB teardown PREP — scoping & design**](v0.1.9/2026.07.07-lancedb-teardown-prep-scoping.md) (row `df51e19a`, commit-HELD inert prep; full LanceDB code-surface inventory + 3 grounding corrections — `util_gcs` is a no-op, no "Connecting to LanceDB" print fires under postgres (real artifact = the class-name banner → folds into the rename), and "remove the dep" is the heavy rollback-killing full teardown because top-level `import lancedb` is load-bearing in 8 memory modules across 2 dispatch layers; recommends a safe inert slice now (class rename + banner + `system.py` response-key nit, all rollback-preserving) + deferring the dep/flag/branch removal to a dedicated post-soak task; 3 decisions for Mr. Radio); [**Lane-A P0 — live LanceDB schema inventory**](v0.1.9/2026.07.01-lane-a-p0-live-lancedb-schema-inventory.md) (ground-truth table/column/dtype/row-count inventory read off the LIVE store — the migration's schema oracle); [**Lane-D P4 — offline backfill utility**](v0.1.9/2026.07.02-lane-d-p4-offline-backfill-utility.md) (truncate-then-load backfill design used by the 2026-07-07 swap-chain execution); [**bug `c2d6bcfa` — board_sweep ledgers never expire**](v0.1.9/2026.07.31-bug-c2d6bcfa-board-sweep-ttl-memento.md) (`rearm()` is dead code, so a sweep ledger written once was never refreshed and `reviewed >= live_owed` read satisfied for five days; fixed with a 24h TTL at DISPLAY time rather than a call site — the EXPIRED line replaces only the two arms that compare `reviewed` to `live_owed`, since the live-board arms stay true at any ledger age; age measured as a DISTANCE so a future-dated ledger cannot read as fresh); [**the DM Quality Judge does not discriminate — and three green signals said it did**](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-dm-quality-judge-does-not-discriminate.md) (bug `ca7a2cbf` P1; good and bad DMs get byte-identical grades, and the returned JUSTIFICATION is the prompt example's own note text describing a different message — a grade can coincide, a justification cannot; three explanations killed by measurement incl. the satisfying one; an n=1 claim of mine withdrawn on replication; `2a41e141`'s 150-word containment sits above a boundary the behaviour ignores; why a 100%-covered package with a green unit tier saw none of it); [**a fallback that emits the same shape as a real answer is a forgery**](v0.1.9/2026.08.01-a-fallback-that-forges-an-answer.md) (DRAFT — three same-day defects in unrelated subsystems that turn out to be one bug: `normalize_grade_label` publishing a parse failure as an opinion, `voice_io` publishing silence as consent across six sites, and the ack-dedupe read path; the wrong VALUE is the recoverable half, the unreadable WRONGNESS is the one that keeps getting missed; why three green suites each answered honestly and none could go red; a separate section keeps the sampling family — a control whose inputs cannot produce the failure it guards — deliberately distinct); [**post-game — R4/R5 managed bounce for `:7999`**](v0.1.9/2026.08.01-tiffany-session-0768c103-postgame.md) (Tiffany 💍 session `0768c103`, crew Krishna 🦚 / Rio ⚡ / Rachel 🕊️ — the settle gate counted bridge FILES, which survive a bounce, so it read 7-present at 0.0s and fired before a single socket returned; all-clears got 0 acks while the warnings got 7, same sessions, same minutes, and the warnings are what prove the ack channel was live; rulings carried forward + the open delivery proof); [**the settle deadline: 30 vs 40, and why the answer is neither number**](v0.1.9/2026.08.02-settle-deadline-arithmetic-30-vs-40.md) (Mr. Radio 🦉 — settles the unreconciled backoff arithmetic: the counter increments BEFORE the min(), so wakes are 2/6/14/30/60 from disconnect, confirmed against 63k printed reconnect delays; both live samples fall out exactly at a ~40s downtime catching the t=60 wake; arrival is a SAWTOOTH in downtime, so a FASTER restart can arrive later and two samples cannot bound it; worst case is cap-wide (~30s) so no deadline under 31 is ever safe; the real lever is RECONNECT_MAX_DELAY, and jitter fixes the herd but NOT the worst case a coverage gate must sit through); [**the Mistral 24B cutover postmortem — why it never worked, and why it is parked**](v0.1.9/2026.08.02-mistral-24b-cutover-postmortem.md) (Mr. Radio 🦉 — the 07-31 cutover changed the MODEL and nothing else; 29 references 404'd for two days behind a 5-word fallback that still looks like a gist. TWO defects: prompt_format stayed instruction_completion so the chat template was bypassed (3/3 garbage vs 3/3 valid on json_message), and even fixed, the 30 Alpaca-style XML templates make Mistral emit `< response >` — 0/3 as-is, 0/3 split into system+user, 3/3 chat-native, byte-identical across three decode settings so NOT sampling. ⚠️ removing the xml declaration from the EXAMPLE makes it WORSE (3/3 -> 0/3) — it acts as a formatting anchor. Parked on Rick's ruling; Phi-4 restored, gister verified 3/3); [**DM judge — problem statement for an outside expert**](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-dm-judge-expert-handoff.md) (self-contained handoff for a reader with zero repo context: environment + verbatim symptoms + code citations, four hypotheses already killed by measurement, five ranked options with pros/cons incl. do-nothing, seven questions for the expert, and a copy-pasteable reproducer; leads with the two disclosures — the probe cannot tell a parse-failure fallback (weight 0) from a real `meh` (weight 0), and our own few-shot rewrite dropped the trailing structural example so the 24B now emits an unclosed `<directness>` tag); [**DM Quality Judge v2 — extraction, not judgement (build plan)**](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-dm-judge-v2-plan.md) (synthesis of the two returned expert opinions — [Claude](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-improving-the-dm-quality-judge-claude.md), [GPT](v0.1.9/2026.07.31-dm-verbosity-reduction/2026.08.01-improving-the-dm-quality-judge-gpt.md) — which converge independently on the same fix: stop asking the model to GRADE directness, ask it to LOCATE the verdict and compute the weight in Python, because a grade can never be validated but a quote can; `DmQualityJudgeV2` lands BESIDE v1 behind an INI version key so nothing live changes; three findings confirmed by reading rather than assumed — `CompletionClient` stores `prompt_format` and never applies it, so the judge's Alpaca prompt reaches Phi-4 with NO chat template at all; `PromptFormatter` is unwired and its "phi-4" template is actually the Phi-3 shape; chat mode is one INI word away but v2 would be this repo's first vLLM chat client, so it is spike-gated with a hand-written `<|im_start|>` fallback; Rick's exit bar: probe green → he reads the table → the toggle stays OFF until he rules); [**Late-answer handback — an answered ask must reach the session that asked it**](v0.1.9/2026.08.01-late-answer-handback.md) (bug `7bb0a7df` P1; 🛑 **cascade-review gate before any code**. When a human answers a blocking ask the response IS persisted, but it is handed back only by waking an in-memory dict — the else branch prints *"No SSE stream waiting"* and the answer never travels, which is the duplicate-prompt half of `f433fbae`. **Not bounce-only**: any SSE drop orphans the waiter, so R1 removed the churn and not the hole. Rick's 7 rulings: reuse the socket if it is still open and fall back to the durable channel only if it is not, never expire the record, a returning session pulls what accumulated in its absence, persona-keyed retrieval, 24h replay cap, deliver to a re-spun session **flagged**, deliver both answers after a re-ask. Design: two nullable timestamps rather than a new `state` value (13 server + 5 client sites enumerated as the argument), stamp `sender_persona` on ask rows with **no backfill** — personas get released and reallocated, so re-resolving old rows would write *wrong* personas permanently — and an `answer_catchup.py` sibling of `dm_inbox_reconcile.py` with seed-suppression deliberately **inverted**. Three joints flagged for the reviewers, incl. an auth-lane assumption whose failure mode is a query that returns `[]` and looks correct); [**↳ cascade revision handoff — the review verdict + what changed**](v0.1.9/2026.08.01-cascade-revision-handoff-late-answer.md) (5-section cascaded plan-review, 15 stage-reviews in 43 min, ~70 findings accepted / 0 rejected, **0 escalations, no code**. The review caught the design (a) marking a *send* as a receipt — silently losing answers, the exact bug the plan exists to fix; (b) replaying machine defaults as human answers, the forged-answer defect reborn inside its own fix; (c) making duplicate delivery the steady state with nothing named to collapse it; (d) specifying a dedupe ledger whose writer lives in a different process, so the test would have gone green while production failed. Outputs: `responded_at IS NOT NULL` elevated to a §3 design-level invariant after being discovered independently **three times in three sections**; a three-setter receipt-gated contract for `answer_delivered_at`; a three-term owed predicate; 21 EXECUTOR-tagged red-proofs. Two workflow candidates recorded, deliberately **not** promoted on one run); [**regenerate every stored embedding from logged text**](v0.1.9/2026.08.02-embedding-regeneration-from-logged-text.md) (row `5e848dd8`; the embedding model changed 2026-05-16 with no data migration, leaving two producers that share 768 dimensions and nothing else — cosine ~0.008 on identical text, so nothing ever failed loudly. Rick ruled the WHOLE table, not the norm-1.0 subset: **a norm says whether a vector was normalized, not which model made it**, and is blind to any change inside the local era. Selection is by source text — 578,364 calls. Four gated subcommands: `plan` read-only / `fill` shadow-columns-only / `verify` / `swap` refuses unless verify is clean; `--table-prefix` points the pipeline at a clone. 83 unit tests, 100% L/B, zero live rows written); [**container credential mount — four designs**](v0.1.9/2026.08.02-container-credential-mount-designs.md) (row `c7c60896`; the single-file `.credentials.json` bind resolves by inode at container start while Claude Code refreshes by *replacing* the file, so an 8-hour access token strands both containers ~3x/day; compares dedicated-dir-read-write vs. host-watcher-pure-consumer vs. root-owned-strict-consumer vs. host-symlink, and names the one unmeasured fact that decides between them — whether Anthropic's OAuth rotates the refresh token on use, whose downside lands on Rick's own login); [**agent model version pinning — survey + 3 options**](v0.1.9/2026.08.02-agent-model-version-pinning-options.md) (the config flags already exist — every agent reads its model from `lupin-app.ini`, verified end-to-end to `ClaudeAgentOptions`; 12 keys still on Opus 4.6 while `cc session spawn model *` is already on 4.8; three genuine hardcodes named — `notification_proxy` constant, `cost_tracker`'s tier map silently booking unknown models at Sonnet pricing, and `model_registry.py` as dead code; recommends one `default lead/worker model` indirection over the SDK's floating `opus`/`sonnet` aliases) |
| [v0.2.0](v0.2.0/) | Aug 3, 2026 – present | 41 | 2 | Present-and-demo branch (`wip-v0.2.0-2026.08.03`). **DM verbosity two-arm pilot** — [three-arm rotation plan + design review + pilot plan](v0.2.0/2026.08.04-dm-verbosity-reduction/) → [all-in cost + break-even (Tue only)](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.04-dm-arm-all-in-cost-and-breakeven.md) (the published `est_tokens/delivered` averages over delivered rows only, so refused drafts read as free; a refusal can never pay for itself on the message it refuses, so the arm is an investment with a payback period) → [**midpoint status: units, coverage, and the bound that stopped bracketing zero**](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.05-dm-two-arm-pilot-midpoint-status.md) (effect is in **words**, cost is in **chars÷4 est-tokens** — never the same number; Wednesday 7/14 slots; co-primary B answerable at last, n=5, both co-primaries move but neither is significant; break-even requirement rose 22.4% → 30.8% while the observed gap held ~24%, so **both credit bounds are now losses**) → [DM formatting tutor plan](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.11-dm-formatting-tutor-plan.md) → [tutor-as-agent plan](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.11-dm-tutor-agent-plan.md) → [**tutor agent implementation record**](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.11-dm-tutor-agent-implementation-record.md) (`dm.txt` promoted to `DmTutorAgent` on `AgentBase` with a fail-closed `rewrite_dm()` the DM send path calls; **the promotion moved TWO variables, not one** — `AgentBase` adds the `</stop>` sentinel the proven run never had, AND swaps regex extraction for a strict XML parser that dies on a literal `<file>` in the model's prose, which is what killed the first live call; fixed by instructing CDATA per `dm-compression.txt:34`, flagged as a departure from the plan's no-rewording rule). Presentation/podcast demo lane: [slide-count control](v0.2.0/2026.08.05-presentation-slide-count-control.md), [Q&A-card path + podcast-only fences](v0.2.0/2026.08.05-qa-card-presentation-path-podcast-only-fences.md), [outline truncation](v0.2.0/2026.08.05-outline-truncation-static-estimate.md), [**pure-voice podcast E2E run receipt**](v0.2.0/2026.08.13-pure-voice-podcast-e2e-run-receipt.md) (rows 3171c9dd + 68198c9f — no-mode→pg-, seeded KISS doc resolved, 21-seg mp3, 5/6 planted facts, decoy 0), [**RAE card-wiring independent review**](v0.2.0/2026.08.13-rae-card-wiring-independent-review.md) (row 7c46fdde — wired-but-dark: the card RAE flag is Baseline False so prod takes the legacy path, and every green test mocks the expeditor, so 72 green tests gate a path nobody can execute; two-key evidence map). **[DM tutor — first-day live corpus analysis](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.13-dm-tutor-first-day-live-corpus-analysis.md)** (84 firings, 0 errors, words −57%; the 426→245 claim drop **retired as a fidelity metric** — output lands at 3–5 claims regardless of input and 236 at-shape DMs never fired, so it measures sender bloat; the real defect is that the pointer restore is **line-anchored**, so mid-sentence paths and row ids are still eaten — 13 of 26 on the current sha, including row `e0bb5a94`, which was Krishna's only continuity after his memento failed to write — row `a74f2176`). **[The fabrication guard — how it works](v0.2.0/2026.08.04-dm-verbosity-reduction/2026.08.13-fabrication-guard-how-it-works.md)** (the tutor invented a person who does not exist; the remedy is **no prompt and no judge model** — regex set-difference over four fact classes, refusing the whole rewrite when the rewrite asserts a number/sha/path/name the sender never wrote; names compared against *every* word of the original, ~120-word stopword set, validated on 27 real pairs, 8 refusals on day one; stated limit — an invented **common** noun is invisible, and the content-word alternative was measured and rejected at 23-of-27 false blocks). **[BFE runtime-brake design](v0.2.0/2026.08.13-bfe-runtime-brake-design.md)** (row 5251ecf0 — a runaway repair job cannot be stopped short of killing the process; "cancel the Future" is a myth for a running thread, so the honest brake is three legs: a real timeout at the hung SDK call, a defined dead-transition handler, and a status-guarded idempotent slot reclaim so a zombie thread can't corrupt an already-dead row). **[Self-re-spin — first live fire, wake null result](v0.2.0/2026.08.16-self-respin-live-fire-wake-null-result.md)** (the seat cleared and never woke — but the run fired against an MCP process started 2h28m before the fix commit, so it tested the PRE-fix verb; `src/lupin_mcp/` changes are inert for every already-running seat, and all 10 live processes still hold the old code. Marker shows `pre_clear_status: "unknown"`, which pins the observer to `DEAD_NO_RETURN` for a seat that is demonstrably alive; the mirror-image risk on a fixed build is worse — a peer's DM would have been credited as `RETURNED`, because the oracle cannot tell a self-wake from a poke). **[PPTX text-layer fix — python-pptx deck builder](v0.2.0/2026.08.16-pptx-text-layer-python-pptx.md)** (row f507034e — Marp's `--pptx` rasterized every slide to a PNG, so decks carried zero selectable text; Rick ruled python-pptx. New `PptxDeckRenderer` builds each slide from the `PresentationModel` with real `<a:t>` text runs — text REPLACES the image, not an overlay — embedding genuine raster visuals as pictures; `deck_verdict.py` extended to require a text layer, so today's all-image decks FAIL and a rebuilt deck PASSES. SVG/mermaid diagram fidelity deferred to a v2 follow-up). **[Orchestrator dry_run→offline_mode rename inventory](v0.2.0/2026.08.16-orchestrator-dry-run-rename-inventory.md)** (row ec8ca1ce — the orchestrator's `dry_run` does REAL ingest and writes REAL YAML while the JOB's `dry_run` writes NOTHING; one word, two opposite meanings. Classifies every `dry_run` occurrence JOB vs ORCH so the rename to `offline_mode` was safe, site-by-site; committed 67feafaf). **[Integration suite unrunnable at its 2000s budget](2026.08.16-integration-suite-exceeds-2000s-budget.md)** (the documented FINAL merge gate measures 2046s against a 2000s hard budget → SIGTERM at 99%; a killed run yields file-level results only — no per-test node-ids — so any recent "integration green" was a subset or a lucky-on-load finish; options: raise budget / split the gate / shed the live-LLM tail). **[Nameless-seat live evidence — e071e834](v0.2.0/2026.08.16-nameless-seat-e071e834-live-evidence.md)** (a transient SessionStart HTTP allocate failure left a spawned seat persona-null for 35+ min; the overflow-path hypothesis was triple-refuted via read-only replay — pool of 14, overflow=arnold; the boot retry ladder at `register_session.py:2020` (5/10/15s) covers a reload window, not a server down at boot that returns later; proposed fix = a post-boot re-attempt when the bridge shows `voice_persona==null` and the server is reachable); [**Phi-4 vs Flash-Lite — the host re-run**](v0.2.0/2026.08.18-phi4-vs-flash-lite-host-rerun.md) (2026-08-18, Mr. Radio: run moved to the host shell because neither container can reach Flash-Lite — both die `DefaultCredentialsError`; Flash-Lite refused ~2x as often, answers ~2x faster, and the refusal gap SURVIVES removing the capitalised-word artifact — answers item 3 of row `ddf7581e`). **[Nine assertions that could not fail — lupin_smoke](v0.2.0/2026.08.23-nine-swallowed-assertions-lupin-smoke-9d5fd628.md)** (row `9d5fd628` — nine assertions sat inside a `try` whose `except Exception` only logged, so a failed check was caught and the test passed anyway; the control is the receipt — identical mutations go GREEN on the old shape and RED by name on the new one. Un-swallowing surfaced four things nothing had reported, **none of them product**: `/api/push` is a 410 tombstone three tests were sitting on (already red in the open via an untouched test — filed `a2184fa4`); three "unauthenticated" tests never sent an unauthenticated request because `http_request` re-attaches the token when `headers={}`, under an assertion accepting BOTH answers; a streaming test that fed binary audio frames to `json.loads`; and `test_queue_contents_access`, whose assertions are unreachable because `/api/queue/{type}` is 404 — proven with a planted `assert False` that stayed green. Names two new detector blind spots: an assertion reached through a helper, and an assertion never reached.) **[The poker's first integration test, and a stale .pyc that faked a red](v0.2.0/2026.08.23-heartbeat-poker-integration-tier-673f14e8.md)** (row `673f14e8` — three `NotImplementedError` stubs behind a module skip became seven green tests, skip removed in the same change; **no product defect found** — the poker's integration behaviour had never been exercised at this tier and it holds up. The tier's value is the real `LupinCommonsGateway` over a real `CommonsStore`, which the unit suite's fake gateway structurally cannot reach: slug derivation, envelope round-trip, `read(since=)` filtering (the stale-signal guard itself), and `who()`-derived presence. Nine product-code mutations, all RED. ⚠️ **A stale `.pyc` faked a red in an untouched unit test**: CPython validates cached bytecode on source mtime-seconds + SIZE, and a `400`→`600` mutation is byte-identical, so the interpreter ran the mutant against restored source — same-size mutations are exactly the shape a careful tester reaches for first. Harness now purges bytecode per run. Venue moved `:8000`→`:7999` by the code rather than the stubs' stale docstring.) |


**Repo operations**: [**git branch/worktree cleanup — audit, proposal + execution receipts (2026-07-01)**](2026.07.01-git-branch-worktree-cleanup-proposal.md) — Krishna's read-only two-signal audit (ancestry + patch-id) of 61 local branches; Rick-approved + executed same evening (38 branches deleted, 2 dead TFE worktrees + 4 stale remote refs pruned, 61→23); §7a execution receipts; 6 UNKNOWN squash-rewrite branches held for owner triage. → [**53 worktrees, 90 branches — what is actually strandable (2026-08-23)**](v0.2.0/2026.08.23-stray-worktree-census-and-sweep-plan.md) — the same two-signal method re-run seven weeks later, after the count regrew from 23 branches to 90. Four tiers: **32** merged-and-clean (incl. 7 detached trees all sitting on merged commits), **4** ahead-but-patches-already-upstream, **9** carrying exactly one unmerged commit, and **3** ancient forks (374–549 unmerged, base `53fef419`, Jul 15–25) whose most recent commit subjects were checked one by one and are **all absent** from the integration branch — named work still stranded. Loads on the fact that `git worktree remove` does NOT delete the branch, so tiers 1–2 carry no loss risk; the only two at-risk files (an untracked plan doc, an uncommitted test diff) were banked to `io/worktree-reclamation-2026.08.23/` first. **Sweep NOT executed** — offered as a decision, the ask timed out, and a destructive step does not proceed on a timeout. Records the recurrence itself as the finding: a hand-run sweep is a receipt that the sweep was needed, not a fix.

**Infra ops (GCP VM)**: [**GCP environment readiness review (2026-07-15)**](2026.07.15-gcp-environment-readiness-review.md) — Rick's "everything up and running on GCP" P0: component-by-component readiness table (VM parked-verified · Cloud Run live · networking/pgvector/terraform converged · Vertex 🟡 decision-gated · spend guardrails 🔴 none), blocker #0 = expired gcloud auth (interactive, Rick-only), the 4-decision queue gating Vertex spend, ordered bring-up runbook, cost posture, and the lunch-window board summary (vertex lane re-staffed: Rio launcher review + Cheech C1-C4). · [**CPU VM app-restore runbook (2026-07-08)**](2026.07.08-cpu-vm-app-restore-runbook.md) — post GPU→CPU downgrade of `lupin-host-test` (Option-A recreate rebuilt the boot disk → docker + app wiped). Key finding: the 200G data disk (`lupin-data`, UUID `a84a8a0a-…`) SURVIVED and holds the full app checkout + 125G containerd images + docker volumes + a git bundle → re-deploy is a **RESTORE, not a fresh clone**. Ordered restore path (fstab-mount by UUID → install docker [hoist out of startup.sh's GPU-only branch, the gap] → repoint docker data-root at the disk → drop in `cloud-gpu.yml` + `LUPIN_MODEL_SERVER_URL` → up → healthy-verify) + the precise **Blocker-1 network recipe** (Cloud DNS private zones `run.app`→199.36.153.4 + route; design-owned VPC infra, Rick-auth) + a durable startup.sh fix. **EXECUTION HELD pending Rick's scope ruling.** · [**VM suspended-by-default lane — final report (2026-07-11)**](2026.07.11-vm-suspended-by-default-lane-report.md) — Rick's 24/7-suspended ruling EXECUTED on `lupin-host-test`: config flip `enable_overnight_pause = true → false` (kills the daily-08:00 auto-start policy) + operational `gcloud suspend` (the provider's `desired_status` can't express SUSPENDED). Reconciliation findings incl. the gitignored-`.vm-state`-revert catch (verify the operative apply input, don't trust the git-clean working tree) + audit's "auto-starts daily 08:00" refuted against live describe (policy was dangling-unattached). Apply landed 0-add/1-label/1-destroy exactly as planned; AGY `1.2.1-pgvector-r3-agy` deployed + healthy, final describe **status=SUSPENDED**. Follow-up filed: stale cloudsql-proxy socket recurs on every suspend→resume cycle (store bug `300bc7ca`). · [**VM 24/7-suspended cost recalc (2026-07-11)**](2026.07.11-vm-24-7-suspended-cost-recalc.md) — Rio's decision-grade cost model for the suspended-at-all-times posture: ≈**$52/mo** (band $52–56; the two pd-ssd disks dominate at $51, suspended 32GB RAM ~$1.28, compute $0), corrects the old $10–20 floor; lever: pd-ssd→pd-balanced ≈ **$31/mo**. · [**Bind-mount revert plan (2026-07-12)**](2026.07.12-gcp-bind-mount-revert-plan.md) — Rick's directive: code OUT of the docker image, back onto the `./src` bind-mount (return to M2 "Decision 3", superseded leg-scoped 2026-07-07 for the pgvector cutover). Ordered revert (checkout sync ≥ `c2b02065` FIRST → re-add mount → recreate → health-gate + mount-sha==`.deployed-ref` guard); restores `deploy-cloud-test.sh` AXIS-A validity (currently a false-green landmine); recommends staying on archive+SCP sync over a VM-side GitHub remote; prod (M3) convergence scope held as a pending decision. Store task `aa5d001a`. · [**`lupin-host-test` SSH + tunnel automation (2026-07-22)**](2026.07.22-lupin-host-test-ssh-tunnel-automation.md) — Rick's ask: automate SSH + browser tunnel to the CPU VM from non-dev laptops (gcloud-only, no repo). Deliverable `src/scripts/lupin-vm.sh` — one self-contained wrapper: `vm-start/stop/status`, `shell`/`run`, `svc up|down|restart|status|logs` (docker compose), `firewall status|open` (auto-derives `dev-vpc`, opens IAP→tcp:7999), `tunnel [PORT]` (binds **127.0.0.1**, not localhost). **Proven end-to-end** (Rick reached the app login through the tunnel). §0 is a cold-start playbook + a 6-row gotcha table capturing every error hit live: wrong-compose nvidia (`cloud-test.yml`→`cloud-gpu.yml`), stale cloudsql socket on resume, compose service-vs-container name, IAP firewall gap (4003), macOS `::1` Bad-file-descriptor (IPv4 pin), and remote-DB user-not-seeded (`create_user` one-liner). Commits `46ebc656`→`dacbacbf`.

**Milestone gate (v0.1.9 mux-consolidation)**: [**E2E-gate classification — Gate D env-fail triage (2026-07-03)**](2026.07.03-e2e-gate-classification.md) — Cheech's 3-bucket framework (env-cleared / real-pre-existing / mux-merge-related) + Stage-1/Stage-2 decision tree for Sam's full e2e_ui sweep (32 errors + 16 failures, none touching c0bbd2af); adopts Rachel's 4-part ENV criterion (incl. confirmed-by-green-re-run anchor); firsthand corroboration that e2e_ui's `clean_test_db` relies on `refresh_tokens` `ondelete=CASCADE` (not an explicit reset) so fresh-user tokens self-clear → 32 errors read as cross-suite residue a bounce clears; 4 survivor-risk watch-flags + a contamination/missing-seed/reset-gap sub-cause split; VERDICT (bounce-fresh ts-bcbd0820, 655p/17f/0err; CORRECTED after code-level attribution): all 32 auth ERRORS CLEARED + **0 FUNCTIONAL mux regressions → product PUSH-READY**. The 17 = 9 seed-gap + 6 pre-existing behavioral + **2 MUX-INTENDED-REORDER test/golden-staleness** (phase5 + action_required, ONE root `75a1bad3` — Lane-0a+0c intentionally extracted `#action-required-section` from `#notifications-pane`; product-correct, TEST-SIDE fix = rebaseline golden / broaden-capture + update home-assertion; Cheech+Rio+Tiberius convergent). Strict-100%-green vs documented-test-debt = Rick's call. Follow-ups: seed-fixture `6aa7a7ef`, abstract-%20 `2b0411c7`, AR-reorder cluster `1f4b3992`↔`c7745a76`.

**Unit-test triage (v0.1.9)**: [**cfcbb703 — 24 pre-existing unit-test failures triage (2026-07-11)**](2026.07.11-cfcbb703-unit-test-triage.md) — Rio's triage of the 24 reds (bug `cfcbb703`): all TEST-SIDE drift, **ZERO prod regressions**, collapsing to 4 root causes. **A** (13) presentation-parser D6-STRICT fail-loud (parsers `raise ValueError`, tests asserted old `== []`) — FIXED, 13 assertions flipped, 110 green; **B** (7) LanceDB→Postgres backend flag makes `db_path=tmpdir` a no-op → shared dev-DB state-bleed **+ live pollution vector** — Phase 2, Option-2 ephemeral per-test schema fixture ruled (`clean_test_db` wipe ruled out); **C** (3) Deep-Research ARM `acquire/record_call` gating intentionally dropped (`api_client.py:32/384`) → `TestDeepResearchMigration` deleted (removed contract); **D** (1) cloud-test `./src` mount-shadow removed by `84d81d39` (baked-runtime) → `test_lupin_rest_mount_model_bind` inverted into a reintroduction guard, sibling of `490fe5db`.

**Task-store policy (v0.1.9)**: [**persona-key follow-on policy — design note (2026-07-11)**](v0.1.9/2026.07.11-persona-key-followon-policy.md) — Cheech's design for the two ratified follow-ons to bug `951a22be`: (1) **unknown-persona soft-flag** — off-roster `owner_persona`/`accountable_manager` on create/reassign get a log-warn + a `persona_flag` response advisory (mirrors `title_guard`) + a compact `[persona_flag: … off-roster]` marker folded into the EXISTING `->queued`/`patched` event reason (zero schema, zero new events); NOT a 422 (new/cross-project personas legitimately absent — would have blocked María's founding P1); roster reuses `load_persona_pool_from_config` via a `_get_known_persona_keys()` lazy singleton twinning the `_get_default_scope_roots` precedent (injectable `known_keys=` for 100% unit test). (2) **class-scoped owner guard** — `owner_persona` required for `task`/`bug`/`review_request` but DEFAULTED to the creator's persona (parsed from the contract-stamped `created_by`) on omission (forgiving, never 422); `decision`/`gate` stay ownerless by design.

**Fleet token economy**: [**Quantifying the 2026-08-13 token-burn drop (2026-08-16)**](2026.08.16-token-burn-drop-quantification.md) — Maya's measurement of Rick's anecdotal 80/50/30 MCP-share + 7k-tok/min readings against durable sources. Verdict: the only bounce-surviving instrument is the DM traffic corpus (DM channel only, not total burn); on it, mean DM length fell ~15% (123→104 words) early-week → tutor-era but is noisy and had rebounded to baseline by 08-16. Rick's two headline numbers **cannot be reproduced** — the notify/spoken path writes no durable token log, the aggregator is external, and the corpus began 08-02 (one day *after* KISS) so the largest arm has no measurable "before." Carries the explicit can/cannot-attribute statement, the per-agent normalization caveat, hook-events call-count series (notify volume held steady → brevity shrinks tokens/message, not message count), and the three fixes that would make the numbers measurable.

**Test-harness safety**: [**The cosa tier runs outside the worktree false-green guard's wiring (2026-08-16)**](2026.08.16-cosa-tier-outside-worktree-guard.md) — the guard (row `a9f87d29`, commit `24536cf9`) is wired only into `src/tests/conftest.py`, but the cosa tier runs `pytest src/cosa/tests/`, whose conftest chain (`src/conftest.py` + `src/cosa/tests/conftest.py`) never loads it. So the merge-pyramid tier added 2026-08-13 gets no automatic worktree false-green protection — a worktree run with `LUPIN_ROOT` pointing at the main tree can go false-green, unguarded and unwarned. Found while running condition 3 of `a4d483e0` in a HEAD worktree (confirmed consistency by calling `check_paths` directly). Recommended close: move the collection-time check into `src/conftest.py`, the ROOT conftest both trees already load.

**Cascaded plan review**: [**Phi-4 vs Flash-Lite — cascade revision handoff (2026-08-16/17)**](v0.2.0/2026.08.16-cascade-revision-handoff-phi4-flash-lite.md) — the review record for `2026.08.16-phi4-vs-flash-lite-hallucination-comparison.md`, covering **two full cascade runs in one night**. Run 1 (closed ~16:11, commits `c7d1335b`…`faf107ee`) killed the "config change, not a code change" headline: the factory has no `google-genai` dispatch, so the Flash-Lite arm needs a new client class. Run 2 (03:00–03:30Z, fresh cold cast) reviewed a document that had grown 307 → 604 lines and found **eight more blockers**, three of them one family — *a guard that covers less than it appears to*: §1.2's freeze guard points at `/var/lupin`, a path that can never match on the host, so as specified it passes by default; `check_dead_claims.py` enforced 13 phrases while its own table listed families it never grepped (structurally, since the table sits inside the `--skip-until` exempt region); and the plan forbids the Wald interval while `statsmodels` hands it to an implementer by default. Chloé's ship gate is adopted — **no directional fabrication headline until McNemar's exact test and a Wilson interval land with a *pre-stated* discordant floor** (María computed the arithmetic bound, below 6 discordant pairs nothing can reach significance, and correctly refused to invent the operational number). Also records the manager error worth carrying: **run 2 was cast because a memento claimed the pipeline had never started, while this very file — written by an earlier session of the same manager — described its completion.** The re-run was justified by the document having doubled, not by the reasoning that launched it.

**Merge-gate hygiene**: [**The serial bridge guard asks for a state that never exists (2026-08-24)**](v0.2.0/2026.08.24-serial-bridge-guard-unsatisfiable-precondition.md) — row `5a68c92c`. `CLAUDE.md` told the operator to run the guard "on a quiescent box"; **no such state exists**. Measured with no suite running: **13 entries under `~/.claude/sessions` changed in 60 seconds**, four live seats wrote bridges in ten minutes — **including the seat running the guard**, which writes its own bridge while the check executes, so the precondition is unachievable by any amount of peer coordination. A red therefore told the reader nothing, and the sanctioned response ("re-run") was indistinguishable from weakening a gate; it once accused the manager's own bridge 25 s after he ordered the run. Wording fixed with Rachel's rule — **real contact is deterministic, peer noise is not**: re-run and check whether the SAME file is named, then resolve its `session_id`/`cc_pid` against `/proc`. ⚠️ **Scoping left OPEN for Rick, with numbers**: `fingerprint_dir` globs `*` deliberately (row `877794ed`, to catch `cc-listener-*.stderr` and `.spawn-lock`), and the measured cost is **6,498 entries / 154 MB sha256'd twice per test against 5 Lupin bridges**, with an exclusion list of **2 names against ~4,676 `.log`/`.stderr` files** — a noise surface ~1,300x the signal. Narrowing re-opens the hazard widening found, so both horns are real. **Guard logic untouched.**

**Deploy hygiene**: [**Orphaned bytecode survives deploys — detector, prevention, and two claims that did not hold (2026-08-18)**](v0.2.0/2026.08.18-orphaned-bytecode-survives-deploys.md) — row `70364793`. The VM deploys by `git checkout` and `__pycache__` is gitignored, so a commit that DELETES a `.py` leaves the `.pyc`. Ships the detector (`preflight-vm.sh` check B5) and the prevention (`lupin-vm.sh push-bundle` purges on both moving modes), each proven to fire by hand and by four mutations apiece. **Two of the row's own claims did not survive measurement**: a `__pycache__` orphan is NOT importable (CPython 3.13 raises `ModuleNotFoundError`; only the pre-PEP-3147 sibling `pkg/mod.pyc` imports sourceless), and all three compose files already set `PYTHONPYCACHEPREFIX`, so the container never read the in-tree cache that was cleared — the **restart** fixed the incident, not the clear. Hence the tiering: sibling orphan BLOCKS, `__pycache__` orphan WARNs. Records the false-positive generator caught on first contact with the real tree (pytest's dotted cache tag `cpython-313-pytest-8.4.2` mis-parsed into **1375** findings; 23 after the fix, all real), the measured `PYTHONDONTWRITEBYTECODE` verdict (**+2.5 s per process start, +91%** — recommend NOT setting it, it prevents nothing the prefix does not already prevent), and the scoping that split the renamed-config-key guard to row `3e4a4a4a` (497 `get()` call sites, **81** with no default, across five config blocks; the migration map would not have caught this incident).

**Deploy tooling**: [**`push-bundle` — a silently-swallowed mode arg and a `--dry-run` that touches the VM (2026-08-24)**](v0.2.0/2026.08.24-lupin-vm-push-bundle-arg-and-dryrun-defects.md) — found while probing row `c41ec7e6` on `lupin-host-test`. **Two defects, both with a measured repro.** (1) The mode is a FLAG; `push-bundle <branch> checkout` sends the positional through the arg loop's `*)` arm, finds `BRANCH` already set, and **discards it — exit 0, tree never moves**. A typo'd `--flag` aborts loudly via `die`; a plausible bare word does not, so the two ways to get it wrong have opposite outcomes. The only tell is a missing tail on the dry-run line, i.e. **invisible on the live run where it costs something** — an operator then believes new code is on the box and the stamp never moves, which is the exact wrong answer `c41ec7e6` exists to prevent. (2) `--dry-run … --checkout` **SSHes to the VM**: `do_push_bundle` returns early under `DRY_RUN`, but the post-checkout preflight arm sits OUTSIDE it at subcommand level and ran **59 assertions on the live VM**, logging "the tree moved without a restart" when nothing had — while sibling `deploy` guards the structurally identical call. ✅ **`c41ec7e6` is PROVEN FIXED on hardware** — the moving run was denied twice by this session's harness classifier, so Rick ran it himself; preflight **B6 reports `.deployed-ref` agrees with HEAD (`d04152cf`)**, **60 passed / 5 warn / 0 blocking**, one assertion more than the pre-fix 59. Before: stamp `df611aa7` / 2026-07-13 against VM HEAD `1959ed18`, ~6 weeks apart. **The `dctl_detect_axis` payoff is now demonstrated** — against the same target, the corrected stamp `d04152cf` routes **`code`** while both `df611aa7` and `1959ed18` route `deps`; the earlier "does not flip the routing" reading was true of its state and holds still, since the flip needs the tree moved level AND stamped, not the stamp alone. ⚠️ **`5f1532d1`'s wrong-venue guard stays UNPROVEN** — the run took the cloud-GPU path, not the cloud-test one it guards. Carries a visible retraction: the author's in-flight claim that `reset` mode was unreachable was wrong — `deploy` calls it at `lupin-vm.sh:608`. **Script untouched.**

**TypeScript test tier — 🔴 DANGER, do not run**: [**The TypeScript test runner is killing sessions on this box (2026-08-23)**](v0.2.0/2026.08.23-typescript-test-runner-oom-hazard.md) — María's survey and the tier's **standing ban**. Three `systemd-oomd` kills on 2026-08-22/23, ~2 dozen sessions lost in the first. **Do not run the TypeScript tier by any of its three doors** — `npm test`, `run-all-tests.sh`/`run-typescript-tests.sh`, or `POST /api/test-suite/submit` with `test_types: ["all"]`, which silently expands to include `typescript`. Concurrency caps and timeouts were both tried and both died. → [**Addendum: what the kernel dump says the allocator is NOT (2026-08-24)**](v0.2.0/2026.08.24-ts-oom-addendum-kernel-dump-evidence.md) — row `d84839ae`. The 08-22 kills were **kernel** OOM kills, so they printed a full task table nobody had read. **One `node` process at 218.8 GiB anon-rss — 89% of a 251 GiB box**, next largest live process 6.1 GiB; it is a **single `node:test` worker**, one test file's process, so one worker alone takes the machine and the per-worker ceiling is what matters. **esbuild exonerated** (4.5 MiB siblings in the same dump). **Not the V8 heap** — 218.8 GiB is ~55x the ~4 GiB old-space ceiling and V8 never threw, so the memory is external (`Buffer`/`ArrayBuffer`/native) and **`--max-old-space-size` would not have bounded it; only a cgroup `MemoryMax` does**. Both capped doors send **SIGTERM only** (no `-k`), which a process thrashing in GC never services. **The allocator is still UNIDENTIFIED** and the ban is unchanged; §9 names the three open questions. Carries three of the author's own retractions left visible, incl. the `comm` field showing every kernel-evidenced kill came through the *uncapped hand-run* door — so what `npm test` does is **UNKNOWN, neither cleared nor convicted**. → [**Why `npm test` never completes: 33 transports started, never stopped (2026-08-24)**](v0.2.0/2026.08.24-multiplexer-hang-root-cause.md) — row `f8055be3`. The two hanging multiplexer files are **one defect, not two**: `queue_transport.test.ts` starts 29 transports and stops 3, `audio_transport.test.ts` starts 7 and stops 0. `AudioTransport.ts` contains zero timers but `extends BaseTransportImpl`, which owns them all — **counting timers in a FILE is not counting timers in a CLASS**, which is what made this look like two investigations. `stop()` is correct and simply never called; each leaked transport runs a handshake→backoff→reconnect loop on real timers for **~440 s expected** (20 attempts x 10 s watchdog + 240 s expected backoff), which is why a `timeout 300` yields RC=124 from both invocation shapes. ⚠️ Door 1 caps at **900 s** and `backoffDelayMs` is `Math.random()`-driven with a range to 681 s, so **a completed `npm test` run is NOT evidence the hang is fixed** — it is a coin toss. Analysis only; the fix touches quarantined tier files.

**DM tutor guard**: [**V3-strict on the fabrication guard — implementation record (2026-08-18)**](v0.2.0/2026.08.18-v3-strict-fabrication-guard.md) — row `ddf7581e`, Rick ruled *"V3-strict behind a flag"* live. The guard's invented-name check flagged every novel capitalised token, so ordinary sentence-initial verbs (Update, Verify, Ensure) were refused as fabrications. **Measured: flash_lite 130 blocks → 54, phi_4 67 → 30**, and the Phi-4 vs Flash-Lite **verdict does NOT change** (McNemar exact p=0.0012 under V3-strict, still clearing the pre-registered discordant floor). **Why "strict" is load-bearing**: `/usr/share/dict` holds 20,494 CAPITALISED entries, so `rachel`, `clayton` and `krishna` are all "dictionary words" — a plain dictionary exemption would blind the guard to invented PERSON names, its founding purpose. Only LOWERCASE entries exempt. Word list VENDORED (83,815 entries) because /usr/share/dict exists on the dev box and NOT in the server container, loaded once at import with a test that fails if the read moves onto the send path. Records the cost Rick accepted knowingly (the guard's one proven true positive, "Force-recreated"→"Deployed", now ships) and three build findings: the **curly apostrophe** flipped the verdict on the same word (`Update's` exempt, `Update’s` blocked), adding a config key turned **22 existing tests red** because the send path indexes config directly and a hand-written test literal missed the key (fixed at the cause, NOT with a silent `.get()` fallback), and an unreadable word list degrades to MORE blocking, never less. Retires the *"76% capitalised-token novelty"* line from the reporting — the phrasing implied capitalisation CAUSED the blocks, which is false.

**Timestamp hygiene**: [**Who actually reads these timestamps? An ISO consumer audit (2026-08-23)**](v0.2.0/2026.08.23-iso-timestamp-consumer-audit.md) — rows `3b4002fe` / `63506bd0`. Cheech's ruling: audit the consumers before swapping the remaining `utcnow().isoformat()` sites, because the same one-line change is a **bug fix at five sites, a no-op at three, and a NEW bug at one**. `utcnow().isoformat()` emits no offset and `Date.parse` reads a zone-less string as **LOCAL**, so the notification `timestamp` rendered by `notifications.js` 6870/6987/7081 is already displayed shifted by the operator's offset — the swap FIXES it rather than risking it. The repo had already written the rule down at `notifications.js:9432` and compensated for it by hand at **one field only** (`next_chase_ts`); every other consumer calls `new Date( value )` raw. Sharpest find: `user_service.py:137-138` emits `created_at` **aware** (off a TIMESTAMPTZ column) and `last_login_at` **naive** on the very next line, into the same dict the admin users table renders — one column right, the column beside it wrong. The one site to leave alone is `csv_writer.py:137`, which appends a literal `"Z"`, so a blind sweep produces `+00:00Z`.

**Notification length**: [**The tail moved, the middle did not — notification length before and after the DM tutor (2026-08-18)**](v0.2.0/2026.08.18-notification-length-before-after-dm-tutor.md) — a natural experiment against the live `notifications` table (317,509 rows), cutover 2026-08-13 14:14 EDT (sha `b8d10bd3`). Equal 8-day windows, authored notifications only: **p90 1009 → 738 chars (−27%)** while the **median went 272 → 287 (+6%)** — the distribution did not shift down, its right tail was cut. Replicated per project (lupin p90 1109→802, plan p90 905→554). **The measurement problem was bigger than the effect**: a first cut on `sender_persona IS NOT NULL` returned 416 of 317,509 rows because that column is populated almost only on DMs, and a second cut returned a median of exactly 75 chars on both sides because 52,612 of 75,673 rows were hook-emitted tool pings (`Done: mcp__cosa-voice___dm_send_fn`, 10,038). The load-bearing filter clause is **"the exact message text occurs ≤2 times table-wide"** — a string that recurs is a template, not authorship. Two rivals ruled out with receipts (the 500-char spoken cap is configured `enforce = False` and 808 post-cutover rows exceed it; the "≤3 sentences" turn rider landed 08-03, inside the BEFORE window). **Explicitly refuses causation**: the tutor shipped fleet-wide so there is no untreated control, daily p50 ranges 106–431 and tracks a session count that moved 2 → 81, and no difference-in-differences is possible until an untouched project is found.

**Fleet liveness hygiene**: [**The hold CLI could not set the field its own poke names — stamp flags + receipts (2026-08-20)**](v0.2.0/2026.08.20-hold-cli-stamp-flags-receipts.md) — bug `1dcaf65c`. The Stop-hook poke tells a manager to stamp `last_looked_in_on_workers_ts`; `write_hold` has taken it as a kwarg since A1 and `get_last_looked_in_ts` reads it — but the SANCTIONED CLI exposed no flag, so a manager obeying the poke was pushed toward the hand-written JSON that CLAUDE.md bans and row `011f1f90` counts 33 of. Adds `--looked-in` / `--spinup-check` / `--surfaced-questions` (all three stamps had the same hole). **Cheech ruled against the first cut**: it normalized a zone-less timestamp to UTC, which picks a timezone the caller never gave — a wrong guess is not rejected and not obviously bad, it is off by the host offset, which reads as a plausible timestamp and debounces the wrong way. Zone-less is now REFUSED at exit 2 with nothing written; offsets are preserved, not re-zoned. The 14400 s two-reader divergence (`_parse_iso` assumes UTC, `_iso_age_seconds` resolves naive as LOCAL) is asserted by a DST-safe test rather than only described. Receipts: **39 red on clean HEAD → 108 green**, 100% lines+branches, and a test proving the file lands in `fleet_data_root()` and **not** the repo root. **Left open deliberately**: a write that OMITS the flags still wipes a prior stamp (`write_hold` persists exactly `HOLD_SCHEMA_FIELDS`), pinned with a characterization test rather than changed unasked — carry-forward semantics is a decision, not a pass-through fix.

**Calculator — arithmetic in the calculator**: [**CalculatorAgent gets a real arithmetic operation (2026-08-24)**](v0.2.0/2026.08.24-calculator-arithmetic-operation.md) — row `11af54f8`. The calculator's declared operations were convert / compare_prices / mortgage / unsupported, so every arithmetic question the router sent it was answered by handing the whole thing to MathAgent — the calculator was a waypoint, not a destination. Adds a first-class `arithmetic` operation (n-ary add/multiply; left-folding subtract/divide/modulo/power, so `[100, 20, 5]` subtract is `(100-20)-5`), an `operands` JSON array on `CalcIntent` because the corpus really does ask for "the sum of 98, 134, and 201", and a prompt section that takes arithmetic OFF the `unsupported` list. **`agent.py` is untouched**: all three `_delegate_to_math_agent()` sites survive exactly as the row demanded, because `arithmetic` never reaches one. Five mutations applied to the shipped code, each grep-proven to have landed — all five killed. Records one open thread it deliberately did NOT act on: `router_label_audit.py`'s `capability` rule still says bare arithmetic stays MATH, which the 08-21 "0 lines move" ruling rested on and this change undercuts — that is row `2ebe4ccb`, the labelling half.

**Queues — cancelling before the work starts**: [**Cancel now reaches a job that has not started yet (2026-08-24)**](v0.2.0/2026.08.24-cancel-reaches-queued-jobs.md) — row `4b87fe61`. `POST /api/jobs/{id}/cancel` looked the job up in the RUNNING queue only, so the cheap, no-work-lost moment you most want to cancel at was the one moment it refused — with a 404 reading "Job not found or not running", which a caller reasonably hears as "there is no such job". Now consults running first (graceful stop), then todo (removal + `job_removed` emit). **Settles the row's open question from source rather than by probing**: there is no separate scheduled queue — `fifo_queue.py:155` reads `scheduled_at` off jobs already in todo — so one todo lookup covers every pre-running state. **And corrects the row's own amendment**: it claimed a test-suite job is not an `AgenticJobBase` and therefore had no reachable success path at all; `test_suite/job.py:173` has read `class TestSuiteJob( AgenticJobBase )` since the file was created, so the 400 arm never fired and cancel on a *running* test-suite job worked the whole time. Six mutations, all killed.

**Argument expediter — file disambiguation**: [**The document choice card, made reusable (2026-08-21)**](v0.2.0/2026.08.21-reusable-document-choice-card.md) — row `a1420538`, widened by Rick's ruling. The card is already a thin wrapper over the notification API's single-select ask; what is not reusable is everything around it — the card is identified by its QUESTION PROSE, so the proxy answer files carry one byte-identical entry per agent and a wording change silently unanswers the card. Four pieces: file-typed args declared once in the registry (`kind="file"` + search roots), a stable `card_id` riding in the opaque `response_options` dict so one generic proxy entry replaces the per-agent ones, strategy-agnostic validation of every multiple-choice answer against the card's own options (the boundary is `confidence < floor`, so 0.9 == 0.9 PASSES — the test feeds equal, not above), and a contract test derived from the registry rather than hand-listed. Names the live generality hole it must catch: `expeditor.py:1664`, the post-describe call site, still hardcodes the podcast's arg name and wording — the contract test was run red against it before the fix (`None != 'research'`: the call site passed nothing at all). Shipped in `5e22409f` · `1d638a37` · `7239d107` · `861012b9` · `600b0e28` · `64ff187b`.

**CJ Flow v2 — one entry point**: [**Retiring the Submit Agentic Jobs panel into the Q&A card, and driving the agent list off the registry (2026-08-22)**](v0.2.0/2026.08.22-qa-card-registry-driven-submit-panel-retirement.md) — Rick's morning ask, planned against wip `9b49f04d`. Most of the machinery already exists: the registry holds **20 commands**, both canonical doors are live, and the argument interview already works server-side (`needs_input` → `/api/v2/resume`); what is missing is a read endpoint over the registry and a front end that renders from it instead of a hand-typed `<select>`. Counts the drift across **five hand-maintained lists** and names the one command that is a genuine gap — `test fix expediter resume`, which has its own submit card *because* the dropdown cannot express it. Carries **four rulings taken live with Rick** (`user_initiable` as its own field, one-shot routing, receptionist stays among the agents) and **two corrections to my own findings**, both recorded in place rather than patched over: the two expediters are excluded **by design**, not by drift, and the Broadcast card was never in that section at all — I matched on a shared CSS class and reported about containment.

**Test honesty — the day's synthesis**: [**Plausible and wrong: seven ways a test can pass without testing, and three ways a record can read true without being true (2026-08-23)**](v0.2.0/2026.08.23-plausible-and-wrong-seven-vacuity-shapes.md) — synthesis across rows `122f07a1`, `beffcddd`, `9d5fd628`, `673f14e8` and `990934d9`. Catalogues **seven vacuity shapes** and states plainly that the detector covers **one** of them: swallowed-by-`except` (detected), grep-shaped, non-strict `xfail`, an `or`/`in` whose satisfied branch is always true, tautological `assert True`, an assertion reached through a **helper** (invisible to a statement-counting scan), and — the nastiest — an assertion **never reached**, where the endpoint 404s and every loop iteration hits `continue`, so the test is present, collected, passing, and indistinguishable from healthy coverage. That last one was proven rather than argued: `assert False` planted past the `continue`, edit confirmed on disk, test still passed. Then extends the same shape to **records**, two of the three being the manager's own — an enumerated gap reading as silent (an 89-entry allowlist and a census already covered it), an answer parked **outside HEAD** in an uncommitted docstring one `git checkout` from gone, and a committed snapshot reading as the live record. Ends with the seven checks that catch these, each earned by getting it wrong first — including **a green mutation usually means a broken mutation, not a blunt test**, and **when a test explains why it is weak, check the explanation** (six instances, two rows, not one justification survived contact; the sharpest was a code comment a helper three frames down contradicted).

**CJ Flow v2 — the receptionist routing hole**: [**The receptionist routing hole — measured, and what I recommend (2026-08-22)**](v0.2.0/2026.08.22-receptionist-routing-hole-recommendation.md) — adversarial-review answer to blocker §2 of the Q&A-card plan review, measured at `9cbabf03` by running three shapes through a real `AskFlow`. The review predicted two different behaviours — `ask()` degrading, `submit()` refusing — and there is only **one**, which is worse: a deliberately picked receptionist, a garbage command and an unroutable question come back **identical on every field** (`path=receptionist`, `route_reason=unknown_command`), and `_receptionist()` overwrites the caller's command with its own, so the requested command is gone from the response too. Recommends a new `route_reason` value (`user_picked_receptionist`) with `path` unchanged, over giving the receptionist a factory — that would change what `cls=NONE` means and put it inside the write-back guard's reach. Records why doing nothing is not free: bug `38815328` was 117 receptionist outcomes of which 115 could not say why, and leaving a deliberate pick indistinguishable re-opens that hole in the other direction.

**CJ Flow v2 — routing fidelity**: [**Routing-fidelity test walkthrough — what guards `/api/v2/ask` and `/api/v2/submit` (2026-08-22)**](v0.2.0/2026.08.22-routing-fidelity-test-walkthrough.md) — written for Rick's morning walkthrough. Inventories **~520 test functions across 33 files** as **six layers**, each answering a question the layer below it cannot: the registry as source of truth (incl. the `resolve()` scope guard, the only test here that catches a *correct-looking* refactor rather than an omission); the four-consumer drift guard and the two reachability gaps it deliberately leaves open; the router-prompt pin + training-corpus single-sourcing; the two endpoints; the tombstone/through-path layer that proves the retired doors' work is still possible; and the HTTP tier. Records three things worth knowing before a retrain: **nothing here proves the model was retrained** (out of scope by the drift guard's own docstring), the corpus tests and the v2 submit cluster are on **different branches** until the merge, and the highest-value file is the 5-test successor guard that exists because **63 green tests were consistent with a wrong answer**.

**CJ Flow v2 — Claude Code cutover**: [**Out-of-repo callers of the two Claude Code doors (2026-08-21)**](v0.2.0/2026.08.21-claude-code-doors-external-callers.md) — row `14172091`, Rick's ruling that a Claude Code job be *upgraded* to the v2 front door rather than left to die on the vine. Both `/api/claude-code/*` doors now answer **410** naming `/api/v2/submit`; this file enumerates who that breaks outside this repo, **with the commands that found them** so the next reader can refute it row by row. One live out-of-repo call site (`src/lupin-mobile/.../claude_code_repository.dart:19`) plus three test doubles; the Firefox plugin, cosa-voice and planning-is-prompting all sweep clean — **cosa-voice was named in the row as a likely caller and is not one**. Also records what is owed and is NOT out-of-repo: `src/docs/fastapi/api.json` + `api.md` are stale for *every* retired door (`/api/v2/submit` is absent entirely), which is a post-merge regeneration step, not a per-door edit.

**CJ Flow v2 eval**: [**Trust-criteria checklist — dry run against a records file alone (2026-08-21)**](v0.2.0/2026.08.21-trust-criteria-dry-run-records-only.md) — row `2ebe4ccb`, for María. Runs every item of `planning-is-prompting/src/rnd/2026.08.17-v2-eval-trust-criteria.md` against `io/v2-flow/eval-2026-08-21-11-37-48/records.jsonl` with nothing else in hand: of 30 items **6 are answerable from records alone, 9 partially, 15 not at all** — the 15 need the arm artifact/run log (every provenance field), the v1 artifact (every v2≥v1 comparison), human labels or rulings, or harness behaviour. Ten places two readers would compute two numbers (hit-rate denominator 0.459 over n_ok vs 0.277 over n; routing-accuracy 0.836/0.718 eligible vs 0.497/0.337 all-ok). Side finding: the warm pass lost 117/300 to `replay_error` (todo 59/60, math 36/60).

**Q&A card — registry-driven agent list**: [**Phase 4's observation method, decided before any code is written (2026-08-22)**](v0.2.0/2026.08.22-qa-card-phase-4-observation-method.md) — the plan's phase-4 gate (*"an agentic job completes end to end from the Q&A card alone"*) is **two claims in one sentence**, and the review's "unobservable on `:8000`" worry applies to only one of them. **A** — the card can carry an argument interview to completion — runs on `:7999` in seconds against `agent router go to weather`, which asks for `location` and is served inline by `resolve()` with no queue and no monopolizer. **B** — the queue then drains the job — is CJ Flow's claim, and the review generalised it from `ce29cd20`, an xfail on **`/api/v2/ask`**, a door that has no `parent_id_hash` field. Phase 4's agentic path uses **`/api/v2/submit`**, which does, and the consumer's Gate B admits a monopolizer's own lineage children *through* the intake hold (`queue_consumer.py:95-124`) with the sweep exporting its own id as `LUPIN_TEST_MONOPOLIZE_PARENT_ID` — so "a consumer that is not the test itself" already exists. **Records a discrepancy rather than assuming past it**: row `7451bebe`, which owed the live proof that Gate B ADMITS (as opposed to the tag merely being THREADED — its own history insists on that distinction twice), now reads `done` while its last amendment ends *"STILL UNRUN. No sample taken."* Claim B is therefore authored against María's three-term conjunction (`monopolize_inflight` AND `monopolize_id` names **my** sweep AND `inflight_agentic_jobs >= 1`) but **sequenced behind that row**, and written so a starve reports as a Gate B failure naming `7451bebe` rather than as a phase-4 regression. Also names the trap: the zero-required-args command (`agent router go to test suite`) is the tempting choice for B and the wrong one — a test-suite job inside a test-suite job is a nested monopolizer that Gate B defers *by design*.

**Note**: v0.1.2 was released the same day as v0.1.1 (2026-01-28) — no documents fall in its window.

## How Documents Are Assigned

1. Each document's `YYYY.MM.DD-` date prefix determines its version assignment
2. Version boundaries are derived from git tag dates (release points)
3. Documents that straddle two branches are placed in the branch where they were **finished**
4. Subdirectories (multi-file research topics) move as a unit based on their date prefix

## Adding New Documents

New R&D documents should be placed directly in the **current version directory** (currently `v0.1.8/`). When a new version branch is created, create a new directory for it and place subsequent documents there.

Naming convention: `YYYY.MM.DD-descriptive-kebab-case-name.md`
