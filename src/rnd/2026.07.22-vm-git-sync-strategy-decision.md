# Design — How the GCP VM Should Sync Code from Git (decision aid)

| | |
|---|---|
| **Date** | 2026-07-22 |
| **Author** | Mr. Radio 🦉 (session `2c24d27b`) |
| **For** | Rick — choosing the long-term VM↔git model |
| **Status** | 🟢 Bundle path IMPLEMENTED now (`lupin-vm.sh push-bundle`); this doc frames the durable choice |
| **Context** | `lupin-host-test` has **no GitHub credentials** (no SSH key, no helper, no `gh`). Its `origin` is a local bundle file `/mnt/lupin-data/lupin-wip.bundle`. |

---

## 1. The problem in one line

The VM can't reach private GitHub, so today **you (the dev box) are the only distributor** — every VM update
requires you to ship a bundle. The question: keep that, or make the VM a first-class git peer that anyone can
`pull`/`push` from directly?

## 2. Options, compared

| Option | Creds on VM | Repos reachable | Unattended (cron/systemd)? | Who it acts as | Setup weight | Blast radius if VM compromised |
|---|---|---|---|---|---|---|
| **A. Bundle** (today) | none | n/a (file-fed) | ✅ (file is local) | — | ✅ done | none — no creds to steal |
| **B. Deploy key** | 1 SSH key | **1 repo** | ✅ | the key | light | that one repo (revoke key) |
| **C. Machine user (bot)** | 1 SSH key | **many** (org/team) | ✅ | bot account | medium | every repo the bot can see |
| **D. GitHub App** | short-lived install token | fine-grained per-repo | ✅ | the app install | heavy | scoped + short-lived (smallest) |
| **E. SSH agent forwarding** | **none** | **all yours** | ❌ interactive only | **you** | trivial (`-A`) | your agent, only while connected |

## 3. What each is actually good for

- **A — Bundle** (`push-bundle`): simplest, most locked-down (no creds at rest). Cost: you're forever the
  distributor; the VM can only get what you ship. Great for a low-touch test box; poor for a self-updating deploy.
- **B — Deploy key**: the classic "server needs one repo." Clean and revocable, but **one key per repo** — the
  ceiling you flagged. Fine if the VM only ever tracks `lupin`; annoying once it needs `planning-is-prompting` + more.
- **C — Machine user**: one identity, many repos, unattended. The standard answer for a build/deploy server that
  touches several repos. Cost: a shared bot identity (commits/pushes attributed to it), and a write-enabled bot on a
  shared VM = anyone with VM access can push as it.
- **D — GitHub App**: automation-grade. Short-lived, fine-grained tokens minted per operation → smallest blast
  radius and no long-lived secret on the box. Cost: the most setup (app registration, installation, token minting).
  The right end-state if the VM becomes a real deployment platform.
- **E — Agent forwarding**: best match for "treat the VM like my dev box." Nothing stored; your local key is
  forwarded over the IAP session, so `git pull`/`push` on the VM run **as you** against **all** your repos. Each
  teammate uses their own key. Cost: **interactive only** — no unattended jobs; and the VM root can use your agent
  while you're connected (don't forward onto an untrusted host).

## 4. Recommendation

Two axes decide it: **interactive vs unattended**, and **one repo vs many**.

- **Now (done):** keep **A (bundle)** for shipping controlled snapshots — `push-bundle` makes it one command.
- **For hands-on "it's my dev box" work:** add **E (agent forwarding)** — one-line change to `lupin-vm.sh shell`
  (`--ssh-flag="-A"`). Zero creds at rest, your identity, every repo. This directly answers "not limiting."
- **When the VM needs to self-update unattended (a deploy pipeline):** go **D (GitHub App)** — or **C (machine
  user)** as the lighter interim. Not **B** (single-repo ceiling).

**Suggested path:** A now → add E for interactive use → graduate to D when unattended deploys arrive. Skip B —
its one-repo limit is exactly the constraint you want to avoid.

## 5. What exists today (implemented this session)

- `lupin-vm.sh push-bundle [branch] [--checkout]` — rebuilds the VM bundle from this dev checkout + `git fetch`
  on the VM. **Fetch-only by default** (refs update, working tree unchanged); `--checkout` points the tree at the
  branch. **Code-sync only — does NOT restart the servers.** Verified live: VM fetched `wip-v0.1.9` → `FETCH_HEAD`,
  `origin/wip-v0.1.9` created.
- `lupin-vm.sh push-repo <local-path> [dest-name]` — archive+SCP a local repo as a sibling under `/mnt/lupin-data/`
  (no `.git`; for fresh sibling repos, not for updating lupin).
- **`lupin-vm.sh deploy [branch]` — the full one-shot cycle (added 2026-07-23).** Closes the last mile:
  `push-bundle --checkout` (move the tree) → **restart `:7999` `lupin-rest`** (docker compose force-recreate) →
  **restart `:8001` arbiter** (host `systemd --user` unit) → **verify** (print checked-out `HEAD`, probe `/health`
  on both ports). Both restarts are mandatory because **neither server hot-reloads on the VM**: `:7999` runs with
  `LUPIN_ENV=testing` (uvicorn `--reload` OFF), and the arbiter is `reload=False` by design. `push-bundle` and
  `deploy` share one internal `do_push_bundle` primitive, so the bundle/checkout plumbing never drifts between them.

**Does the branch get checked out on the VM after a bundle push?** **No** — `push-bundle` fetches; the working
tree stays on its current branch until `push-bundle --checkout` (or a manual `git checkout <branch>` on the VM).
This is deliberate: a working-tree move re-points the bind-mounted `./src` the app runs from, so it's opt-in.
**`deploy` always checks out** (that is its whole point) and then restarts, so the running servers actually pick
up the new code.

### 5a. WHERE these commands run — dev box vs the laptops (Rick, 2026-07-23)

`deploy` is a **dev-box operation**: it builds a local `git bundle`, which requires the git checkout *and* the
GitHub-backed history that live on the dev box (and, by extension, any laptop that holds a full lupin checkout).
It is **not** run *from the VM* — the VM is the deploy *target*, not the actor.

The rest of `lupin-vm.sh` is the opposite: the **majority of day-to-day use** is the **two laptops remoting into
the VM** to operate the running Lupin app — `shell`, `tunnel`, `svc status|logs|restart`, `vm-start/stop`. Those
need only `gcloud` + IAP access, no checkout. So the mental model is:

| Command | Run from | Needs a lupin checkout? | Role |
|---|---|---|---|
| `deploy`, `push-bundle`, `push-repo` | **dev box** (distributor) | **yes** (builds the bundle) | ship code to the VM |
| `shell`, `run`, `tunnel`, `svc *`, `vm-*`, `firewall *` | **laptops / any machine** | no | operate / observe the VM |

**No cron (Rick, 2026-07-23):** `deploy` is an **on-demand** operation, invoked by hand when you want the VM on a
new commit — deliberately **not** wired to a timer/cron. If unattended self-update is ever wanted, that is the
separate GitHub-App path (Decision #2), not a cron wrapper around `deploy`.

### 5b. COMMITTED work only — the bundle never ships an in-motion tree (Rick, 2026-07-23)

`git bundle create <file> <branch>` packs the objects reachable from the **branch's committed tip**. That means
what reaches the VM is **committed history only** — three categories are *excluded by construction*:

- **working-tree edits** (modified-but-not-staged),
- **staged-but-not-committed** changes (the index),
- **untracked files.**

Nothing "in motion" can be pushed: the bundle captures a commit object, not the state of your editor. The VM's
`git checkout -B <branch> FETCH_HEAD` then lands *exactly* that committed snapshot — byte-for-byte the tip commit,
never a half-saved file. This is inherent to `git bundle` (it is a transport for commits/refs), not a flag we set.

⚠️ **Consequence — commit before you deploy.** `deploy [branch]` (and `push-bundle`) ship the branch's **last
commit**, not your live working tree. If you have local edits you want on the VM, **commit them first**; an
uncommitted change is silently left behind (correct, safe — but surprising if you expected "deploy = whatever I
see in my editor"). Rule of thumb: `git status` clean on the target branch ⇒ what you see is what deploys.

## 6. Open decisions for Rick

1. Add **E (agent forwarding)** to `lupin-vm.sh shell` now? (trivial, high value for interactive git-as-you)
2. Is an **unattended self-update** on the VM a near-term need? If yes → start the **D (GitHub App)** setup.
3. ~~Should `push-bundle` default to `--checkout`?~~ **RESOLVED 2026-07-23.** `push-bundle` **stays fetch-only**
   (safe, code-sync only); the checkout-and-restart semantic lives in the new dedicated **`deploy`** verb (§5).
   Clean split: plumbing vs deploy intent.

## 7. Full new-instance bring-up runbook (dev-box CLI + Vertex Opus 4.8) — 2026-07-23

The end-to-end sequence to make a fresh VM's **host CLI** identical to the dev box (Claude Code + Agent SDK +
cosa-voice MCP + 8 hooks + shell env + direct Opus 4.8 via Vertex). Every state-changing `gcloud` step runs from
the **dev box** (`!` prefix / your terminal); the Bash classifier blocks the agent from those. Order matters.

| # | Step | Command / action | Runs on | Verify |
|---|------|------------------|---------|--------|
| 1 | Sync code | `lupin-vm.sh deploy` | dev box | both servers 200 |
| 2 | Install CLI (uv → Python 3.13 → Agent SDK → `claude`) | `lupin-vm.sh install-cli` | dev box | prints python 3.13.x, Claude Code ver, `claude-agent-sdk` ver |
| 3 | Push shell env (aliases + `LUPIN_ROOT` + `PLANNING_IS_PROMPTING_ROOT` + `~/.lupin/config`) | `lupin-vm.sh push-env` | dev box | on VM: `env \| grep ROOT`, `cat ~/.lupin/config` |
| 4 | Install cosa-voice MCP **+ the 8 hooks** | `cd $LUPIN_ROOT && src/scripts/install-cosa-voice.sh` | **VM** (from inside the repo) | `claude mcp get cosa-voice` (Scope: User); "Hooks: 8/8" |
| 5 | Grant the **VM service account** Vertex access | `gcloud projects add-iam-policy-binding hello-world-foo-423219 --member="serviceAccount:lupin-host-test-sa@hello-world-foo-423219.iam.gserviceaccount.com" --role="roles/aiplatform.user"` | dev box | wait ~1 min for propagation |
| 6 | Vertex env for Opus 4.8 (global-only) | export `CLAUDE_CODE_USE_VERTEX=1`, `ANTHROPIC_VERTEX_PROJECT_ID=hello-world-foo-423219`, `CLOUD_ML_REGION=global` in VM `~/.bashrc` | VM | a real predict returns 200 at `global` |
| 7 | Claude Code OAuth (interactive, one-time) | run `claude` once, complete login | VM | session starts |
| 8 | **Ship the payloads git cannot deliver** | `lupin-vm.sh push-unversioned` | dev box | driven by `src/conf/vm-unversioned-manifest.tsv`; rows with `local_path` `-` are VM-local and only asserted |
| 9 | **Assert the deployment contract** | `lupin-vm.sh preflight full` | dev box | 31 assertions; `PREFLIGHT PASSED` with 0 blocking |

> **Steps 8-9 are the 2026-07-26 addition (§7d).** `deploy` now runs preflight's `pre` arm before
> it touches anything and the `post` arm after the bounce, so on a re-deploy steps 8-9 are
> automatic. Run them by hand on a FIRST bring-up, where there is nothing to deploy onto yet.

**Gotchas learned this session (each cost a round-trip — encoded so the next spin-up doesn't repeat them):**
- **`install-cli` is idempotent** — `uv venv --clear` wipes a half-built venv from a failed first run (commit `d90415a6`).
- **`LUPIN_ROOT`/`PLANNING_IS_PROMPTING_ROOT` are NOT in the aliases file** — the aliases *reference* them; `push-env`
  now *exports* them with **VM-correct** paths (`/mnt/lupin-data/{lupin,planning-is-prompting}`), never the dev
  `/mnt/DATA01` paths. The env-var NAMES are canonical and unchanged; only the values adapt (commit `5eb54324`).
- **MCP ≠ hooks.** `install-cosa-voice.sh` registers the MCP server AND now installs the 8 hooks from the canonical
  in-repo template `src/conf/claude-code-hooks.json`. Both are `$LUPIN_ROOT`/`$PLANNING_IS_PROMPTING_ROOT`-relative,
  so the SAME template ports verbatim (no path rewriting).
- **Two Vertex principals.** The **dev box** authenticates as your user `admin@rickruiz.altostrat.com`; the **VM**
  authenticates as its attached **service account** `lupin-host-test-sa@…`. Each needs its OWN `roles/aiplatform.user`
  grant — granting the user does nothing for the VM (step 5).
- **Opus 4.8 on Vertex is `global`-only.** A regional `CLOUD_ML_REGION` (e.g. `us-central1`) → model-not-found;
  `us-east5` currently → quota 429. The Claude Code wizard tests a non-global region and mis-reports the failure as
  "permission denied" — SKIP its verification (Save anyway); the env vars in step 6 are the real config.
- **The 8 hook event-types:** SessionStart, SessionEnd, UserPromptSubmit, PreToolUse (×2 matchers), PostToolUse,
  Stop, Notification, PermissionRequest.

## 7a. Addendum — 2026-07-24 fixes (Mr. Radio) : dedicated CC venv, notify auth, env + tooling

A live VM bring-up surfaced several gaps the 2026-07-23 runbook did not cover. All committed on
`wip-v0.1.9-2026.06.21-bug-fix-implementation` (`abd217c7`→`289d793a`). The step table above is amended by the
notes below.

### The single biggest trap — `$LUPIN_ROOT/.venv` is the ARBITER's venv on the VM
On the VM, `$LUPIN_ROOT/.venv` is a **symlink → `.venv-arbiter`** (uid-1001, Python 3.10 — the arbiter service's
venv). The installer + hooks defaulted to `$LUPIN_ROOT/.venv/bin/python`, so the entire host CC integration (MCP
server + 8 hooks) was pointed at another service's venv: **missing deps AND unwritable by the operator**. The dev
box masks this (there `.venv` is the operator-owned venv). Fix (commits `a3afbe7c` + `de9291d4`):
- `install-cosa-voice.sh` now resolves a dedicated **`LUPIN_CC_VENV`** (default `$LUPIN_ROOT/.venv`; per-machine
  override), **provisions** it (`uv venv --python 3.13` + installs the host closure), and **refuses** (loud) to
  register when the resolved venv is a symlink into a foreign-uid service venv.
- The host closure is **`fastmcp==2.14.2 requests pydantic regex pytz`** (`regex`/`pytz` come in transitively via
  `cosa.utils.util`; confirmed complete when MCP connected with 30 tools).
- Hooks resolve their interpreter from `${LUPIN_CC_VENV:-$LUPIN_ROOT/.venv}`.
- **On the VM, set `LUPIN_CC_VENV=$HOME/.venv-lupin-mcp`** (operator-owned). `push-env` now exports it; but a
  `gcloud ssh --command` shell is non-login (does NOT source `~/.bashrc`), so run the installer from an
  **interactive** VM shell, or pass it inline: `LUPIN_CC_VENV=$HOME/.venv-lupin-mcp bash …/install-cosa-voice.sh`.

### `push-env` now exports more (step 3 amended)
`push-env` writes VM `~/.bashrc` (idempotent) with, additionally: `DEEPILY_PROJECTS_DIR=/mnt/lupin-data` (alias
library depends on it), `LUPIN_CC_VENV=$HOME/.venv-lupin-mcp`, and `LUPIN_DEV_EMAIL=ricardo.felipe.ruiz@gmail.com`
(notify() target-user). It does **NOT** ship `LUPIN_API_KEY` — see next. The `~/.lupin/config` seed is
**create-only** (`if [ ! -f ]`), so hand-edited `[lupin]`/`[plan]` sections are never clobbered on re-run.

### Notify auth is a TWO-part credential chain (new — the 401s)
`notify()` needs BOTH:
1. **`LUPIN_DEV_EMAIL`** (target-user) — shipped by `push-env`. Without it: 401 "no target user". `~/.lupin/config`
   `global_notification_recipient` is NOT read in the hook/MCP runtime — the env var is.
2. **`LUPIN_API_KEY`** (the `X-API-Key`, `ck_live_` format) — **per-DATABASE**, validated against a bcrypt hash in
   the server's `api_keys` table. The dev-box key is NOT valid on the VM's separate test DB — it must be **MINTED
   against the VM DB**. Do NOT ship the dev key (a reverted footgun: it can't validate cross-server AND would
   clobber the manual VM key via `~/.bashrc` last-wins — commit `90b56fd4`).

**Mint the VM notify key** — run INSIDE the app container (host venvs lack `sqlalchemy`/`bcrypt`; the container holds
the exact test DB + connection):
```
docker exec -w /var/lupin lupin-rest-cloud-gpu \
  python src/scripts/create_service_account_postgres.py \
  --email=claude.code@lupin.deepily.ai \
  --description="Claude Code cosa-voice notifications on lupin-host-test VM (created YYYY-MM-DD)"
```
- The DB is chosen by the **container's `LUPIN_ENV`** (prints `PostgreSQL Environment: testing`), NOT `--env`
  (which only labels the key file). Do not override it.
- Step 1 is idempotent ("Using existing service account"), so re-runs are safe.
- **Gotcha (fixed `289d793a`):** `/var/lupin` is a **read-only** mount, so the Step-4 write-to-file failed and
  originally aborted the run before Step 5 printed the plaintext — the key was lost (DB keeps only the hash). The
  script now catches that OSError and continues to Step 5, which prints the key "shown only once". **Capture it
  from stdout** and `export LUPIN_API_KEY=<value>` on the host, then restart `claude`.

### WS notification-listener needs per-project creds + the right launch dir (new)
A VM `claude` launched from `/mnt/lupin-data/planning-is-prompting` auto-detects project **`plan`**; the CC
Notification Listener then fails because the VM `~/.lupin/config` has no `[plan]` (or `[lupin]`) section. For a clean
lupin test, launch `claude` from the **lupin** dir and ensure a `[lupin]` section (email/password) exists on the VM
(hand-edited; `push-env` won't overwrite it). This is separate from the `X-API-Key` above.

### `.mcp.json` must never be committed (new — scope-conflict warnings)
A committed `.mcp.json` with absolute dev-box paths pollutes every checkout (on the VM: EACCES + a stale
cosa-voice scope-conflict warning). Removed from **lupin** (`98a1ac7f`) and **planning-is-prompting** (María, PIP
`58bd6b1`), both now `.gitignore`d. cosa-voice is a **per-machine USER-scope** MCP — never a committed project scope.

### Tooling quality-of-life (new)
- `lupin-vm.sh tunnel` takes **`TUNNEL_VERBOSITY`** (default `critical`) to silence benign macOS
  `[Errno 9] Bad file descriptor` IAP noise; `TUNNEL_VERBOSITY=info` restores full logs (`abd217c7`).
- `shell`/`run`/`svc` now send an **SSH keepalive** (`ServerAliveInterval=60`, `CountMax=3`) so idle sessions no
  longer drop with `Broken pipe` / exit 255 (`5369519d`).

### Open follow-ons (filed in the task store)
- `81a24c93` (**done**) — installer CC-venv hardening.
- `c48cd867` (P3) — give the VM real **bidirectional GitHub** push/pull (currently bundle-only); Rick has more repos coming.
- The first mint attempt left an **orphan active key** (hash only, plaintext lost) for `claude.code@lupin.deepily.ai`
  in the VM test DB — harmless (unusable without plaintext); deactivate later for tidiness.

---

## 7b. Addendum — 2026-07-25 (Mr. Radio, session `43ff094e`): persona-404 APPLIED · STT 401 · GitHub push/pull · out-of-band data

Everything below is **applied and verified on `lupin-host-test`**, not proposed. Commits `b7ea000f`, `66206831`, `e1b48afa` (all HELD — push is Rick's).

### The persona-404 fix is LIVE (closes the 7a-era blocker)

Design of record: `src/rnd/v0.1.9/2026.07.24-vm-persona-bridge-mount-uid-divergence.md` **FINAL PLAN v3**.

Root cause: the `claude-creds` **named volume mounts over `/home/rruiz/.claude`**, which SHADOWS the host's
`~/.claude/sessions`. The container saw no bridges, so `find_session_path_by_id()` returned None and
`/allocate` raised 404. Compounded by uid divergence: host OS-Login uid `1721846087` vs container uid `1001`,
bridges written `0600` by `mkstemp`.

Applied, in order:
1. **compose** (`b7ea000f`) — `group_add: ["${LUPIN_BRIDGE_GID:?}"]` + a **long-form** sessions bind with
   `create_host_path: false`. Long form is deliberate: a short `${HOME}` bind silently CREATES an empty dir
   when HOME is wrong, which is indistinguishable from "mounted but empty".
2. **`cloud-gpu.env`** (VM-local, git-ignored) — `LUPIN_HOST_SESSIONS_DIR=/home/admin_rickruiz_altostrat_com/.claude/sessions`
   + `LUPIN_BRIDGE_GID=1721846087`. Backed up first; owner/mode proven unchanged against the backup.
3. **VM** — `chmod 2770` (setgid) on the sessions dir; backfilled the **9** `cc-*.json` from `600` to `660`.
   The other 31 entries (`cc-listener-*`, `cc-stable-*`) were already `664` — only the `mkstemp`-written
   bridges needed it, which matches the root cause exactly.
4. **recreate** — `up -d --force-recreate --no-deps lupin-rest`. **`--no-deps` is not optional**: a bare
   `up -d` re-runs `cloudsql-socket-init`, whose `rm -f` deletes the in-use socket. Verified `lupin-cloudsql-proxy`
   stayed up (13h uptime) across the recreate.

**Bidirectional cross-writer test (the crux) — ALL PASS**, numeric uid/gid/mode at each step:

| step | result |
|---|---|
| host writes bridge | `1721846087 1721846087 660` |
| **container** `set_voice_persona()` | `1001 1721846087 660` |
| host reads + rewrites | `1721846087 1721846087 660` |
| container re-reads | persona intact |

**End-to-end**: `_allocate_voice_persona_via_http()` — the exact function SessionStart runs — allocated
**Rio** with `failure -> None`, and the host read it back.

**Three RED results during verification were the INSTRUMENT, not the fix.** Anyone re-running this will hit
the same three: (a) the allocate route is **`/api/cosa-voice/voice-persona/{sid}/allocate`**, not `/api/...`;
(b) the hook authenticates by **JWT via `/auth/login`**, NOT `X-API-Key`; (c) hook code must run under
**`$LUPIN_CC_VENV`** (`~/.venv-lupin-mcp`), not system python — system python lacks `regex`.

### STT / voice-to-text 401 — Secret Manager was 8 months stale

Symptom: `401 {"detail":"Invalid X-API-Key"}` from the Cloud Run model server; endpoint returned 500.
**Not a cold start** — a cold-starting instance yields a timeout/503, never an application-specific JSON body.

| | sha256 |
|---|---|
| key the client SENDS (`src/conf/keys/notification-api-claude-code-dev`, dev **and** VM container) | `26e3c096d4dfe55a` |
| key the server CHECKS (Secret Manager `lupin-notification-api-key`) | `09baaa60e46382ea` |

The secret had **exactly one version, created 2025-11-12**. The app key was re-minted at some point and Secret
Manager was never updated; nothing compares the two authorities.

**Fix applied**: `gcloud secrets versions add` produced **version 2**. **No revision roll was needed** — the mount
pins `version: latest`, which Cloud Run resolves at INSTANCE START, and with `minScale=0` the old instance had
already scaled to zero. The next call cold-started onto v2 (`/health` 200, `uptime_seconds: 25` on a 32s call).
v1 left **enabled** as a rollback path. Zero Terraform drift — the `--update-labels` force originally proposed
was deliberately skipped.

Proof on Rick's own audio: `TRANSCRIBED in 4.1s -> " Let's see if this thing can actually run an ASR job."`

**CORRECTION for the record**: an earlier claim in this session that a *scheduled-warm* policy could pin a warm
instance holding the old secret is **FALSE for this deployment**. `minScale=0`, `maxScale=1`, and the **Cloud
Scheduler API is not enabled** on the project — the two scale-up/scale-down jobs in the Terraform module were
never provisioned. The module's INTENT was read as live state. Rick caught it. **He is not paying for idle.**

**Guard shipped** (`66206831`): `src/cosa/utils/secret_drift.py` + 28 tests, 100% lines/branches, six mutation
probes. `check_key_drift()` reports match/drift/**unknown**, sha256 only, never plaintext. `unknown` is
deliberately NOT folded into `match`. Validated against real data BOTH ways — `version=1` reports DRIFT with the
remedy, `latest` reports OK.

### The category this runbook was missing: FILES GIT CANNOT DELIVER

Bit us **three times in one day**. `push-bundle` / GitHub sync carry **committed, non-ignored** content only:

| file | why git can't carry it | how it gets there |
|---|---|---|
| `src/conf/keys/**` | gitignored | out-of-band (already established) |
| `cloud-gpu.env` | gitignored, VM-local | edited in place on the VM |
| `src/conf/contact-information.map` | gitignored (`.gitignore:65`, personal data) | **scp** |
| `harvey-labs io/markdown-exports/` | gitignored, 396M | **tar.gz + scp** |

**`contact-information.map`** — `MultiModalMunger.__init__` loads FIVE `.map` files; four are tracked, this one
is not, so on a fresh deploy **every transcription 500'd** for want of an optional personalization file.
Installed at uid 1001 / mode 644 to match its siblings. Code fix in `e1b48afa` makes it optional at load and
**refuses loudly at use** (an empty map would otherwise answer `"N/A"` — a silent wrong answer).

**Recommended follow-on**: a `push-unversioned` verb in `lupin-vm.sh`. This category currently lives in
somebody's head, and a fresh VM rediscovers each member by failing.

### GitHub push/pull on the VM (partially closes `c48cd867`)

**Auth reality**: the dev box does NOT use SSH for GitHub. Remote is HTTPS; auth is `credential.helper = store`
reading `~/.git-credentials` holding a **classic `ghp_` PAT** (40 chars). The `id_ed25519` keys are unused for
GitHub; `gh` CLI is not logged in. **No key pair is required for push/pull.**

**Owner map** — three owners, which is what makes the token question non-trivial:

| repo | owner |
|---|---|
| `lupin`, `planning-is-prompting` | `deepily` (personal) |
| `rickruiz-parallel-search`, `rickruiz-skills-distillation` | **`cloud-ai-fde`** (work org) |
| `harvey-labs` | **`harveyai`** (third party) |

A **fine-grained PAT is OWNER-scoped**: within one owner it can cover "All repositories" or "Only select
repositories", but **it cannot span two owners**. Because Lupin/PIP are one-way dev to test (bundle, no GitHub
credential needed), **one** fine-grained PAT for `cloud-ai-fde` covers everything the VM needs.

**Status**: Rick minted it; it is **PENDING ORG APPROVAL** — the risk called out in advance. Interim measure on
his explicit ruling: the existing **classic PAT** is on the VM (`~/.git-credentials`, mode 600, stored under
username `rickruiz` — harmless, GitHub ignores the username when the password is a PAT). **The classic PAT is
account-wide, so the "credential cannot touch `deepily/*`" guard does NOT hold yet.** The one-way rule is
currently preserved by REMOTE CONFIG instead: Lupin and PIP on the VM point at their bundle files, not GitHub.
**Leave those remotes alone.** Swap to the fine-grained token and revoke this one when approval lands.

**VM git config applied**: `user.name=deepily`, `user.email=42214371+deepily@users.noreply.github.com`
(Rick's ruling: keep pushing as `deepily` until Google policy is known), `credential.helper=store`,
`init.defaultBranch=main`.

**`core.excludesfile` — the line that does NOT copy verbatim.** Dev has `/home/rruiz/.gitignore_global`; the VM
home is `/home/admin_rickruiz_altostrat_com`. Git ignores a missing excludes file **with no warning**, so the
patterns silently stop applying — including `**/.claude/settings.local.json`, which would then be committable
into a work repo. Fixed by shipping the file and setting **`core.excludesfile=~/.gitignore_global`**; git expands
`~`, verified with a negative control, so that form is portable across both machines.

**Verified**: `git ls-remote` reads the private org repo; `wip-2026.07.18-poc-first-draft` checked out.
**PUSH from the VM is NOT yet proven** — only reads.

**Gotcha**: after `git clone`, `git branch` lists LOCAL branches only. A fresh clone creates one local branch
and fetches the rest as remote-tracking refs. Use `git branch -a`, then `git switch <name>` (git 2.34 on the VM).

### `harvey-labs` corpus delivered by bundle + tarball

Third-party (`harveyai`), so GitHub credentials are not the path; bundle is.

- bundle 496M to `/mnt/lupin-data/harvey-labs-wip.bundle`, clone to `/mnt/lupin-data/google/harvey-labs`
- integrity: sha256 `146e327da22ff472`, size `519366603` — **identical both ends**
- `tasks/` corpus: **15,811 files, matches dev exactly**
- `io/markdown-exports/`: gitignored, shipped separately as tar.gz, **395M to 102M (3.9:1)**, extracted and
  verified **5,791 files / 398,065,458 bytes / 1,035 dirs**, and still correctly gitignored afterwards

**A bundle clone only instantiates `refs/heads/*`.** Dev has 167 commits across 15 refs; the VM clone has 25
across 3. The ~13 upstream `harveyai` branches are in the bundle as objects but did not become usable refs.
Fetch a specific ref from the bundle if one is needed — no re-transfer required.

### Tooling defects found (worth fixing before the next bring-up)

- **`lupin-vm.sh deploy` runs `up -d --force-recreate` WITHOUT `--no-deps`** — exactly the hazard named above.
  It was avoided manually here. Filing recommended.
- **`remote_compose()` runs `sudo docker compose`** (`:172`). That is why a mode-600 / uid-1001 `cloud-gpu.env`
  has never blocked it — and it means the FINAL-PLAN note "verify expansion, never via `sudo`" needs re-scoping:
  the real hazard was the `${HOME}`-relative bind source (eliminated in v3), not sudo itself, which the sanctioned
  tooling does unconditionally.
- `safe.directory` appears **twice** in the VM's global config. Harmless (git treats the values as a set), but
  `git config --global --add` never dedupes and **two runbooks** prescribe the same line
  (`2026.07.15-gcp-vm-getting-started-runbook.md:88` and `2026.06.15-vm-connectivity-...:50`). Make the runbook
  line idempotent if it is ever tidied.

### Still open after 2026-07-25

- **Push from the VM unproven** (reads verified only).
- **Branch protection on the two `cloud-ai-fde` repos is unknown** — if `main` requires a review, no PAT gets past
  it and GitHub will not let Rick approve his own PR. The website is NOT a workaround; it enforces the same rule.
- **Fine-grained PAT pending org approval.** If the org has not enabled them, fall back to **SSH deploy keys**
  (per-repo, no org-wide policy change) — **NOT** to a classic PAT.
- The `:7999` **401 on `X-API-Key`** (VM Cloud SQL `api_keys` table) is a DIFFERENT validator from the Secret
  Manager issue above and is **NOT fixed by it**. Undiagnosed.
- `CC 2.1.220` added **`VERTEX_REGION_CLAUDE_5_OPUS`**, which `vertex_env.py` does not guard (calibration
  harvested from 2.1.209). Per that test's own words an unguarded per-model override "silently routes a SINGLE
  model to another region, where it runs, bills, and logs nothing". Opus is the expensive one.
- Staging copies still on the VM: `harvey-labs-wip.bundle` (496M), `harvey-io-markdown-exports.tgz` (102M).

---

## 7c. Addendum — 2026-07-25 evening (Mr. Radio, session `43ff094e`): the doc viewer served nothing on the VM

### The symptom, as Rick saw it

```
lupin-rest-cloud-gpu | [scope_registry] WARNING: external repo 'lupin': path
                       '/var/external-projects/lupin' not found on disk; skipping
   ... the same line, twelve times, one per configured scope ...
lupin-rest-cloud-gpu | [scope_registry] registered 0 external scope(s): []
lupin-rest-cloud-gpu | "GET /api/docs/scopes HTTP/1.1" 200 OK
lupin-rest-cloud-gpu | "GET /api/io/file?path=skills-distillation%2F...md HTTP/1.1" 404 Not Found
```

### Root cause — a compose file that was copied for one purpose and not audited for the others

`docker-compose.cloud-gpu.yml` was written from the **code-mount set** only. `docker-compose.yml` has
carried two doc-viewer binds since 2026-05-12 (`:158-159` for `lupin-rest-dev`, `:244-245` for
`lupin-rest-test`); neither was copied across. The container-side paths are **fixed** by
`src/conf/lupin-app.ini § external repos`, so the compose file is the only place they can ever be
satisfied — and nothing compared the two files.

**The registry was not broken.** It warned once per repo and skipped, which is exactly right. The
failure was that twelve correct warnings scrolled past for weeks and the only consumer-visible
symptom was a 404 on an unrelated-looking URL.

### The fix (commit `43a69c07`) — and why it is four binds, not one

Dev gets away with a single bind because its host directory holds **nothing but repos**:

```yaml
- /mnt/DATA01/include/www.deepily.ai/projects:/var/external-projects:ro
```

**`/mnt/lupin-data` is not that directory.** It is also the **docker data-root**. The equivalent
one-liner on the VM would place inside the doc-viewer's read surface:

- `docker/volumes/lupin_claude-creds/_data` — the persistent **Claude Code OAuth login**
- `lupin-wip.bundle`, `planning-is-prompting-wip.bundle`, `harvey-labs-wip.bundle` — the sync bundles
- `harvey-io-markdown-exports.tgz`, `lost+found`

So the VM binds each repo **explicitly**. Verified present 2026-07-25:

| scope | INI container path | VM host path |
|---|---|---|
| `lupin` | `/var/external-projects/lupin` | `/mnt/lupin-data/lupin` |
| `planning-is-prompting` | `/var/external-projects/planning-is-prompting` | `/mnt/lupin-data/planning-is-prompting` |
| `skills-distillation` | `/var/external-projects/google/skills-distillation` | `/mnt/lupin-data/google/skills-distillation` |
| `parallel-search` | `/var/external-projects/google/parallel-search` | `/mnt/lupin-data/google/parallel-search` |

**The other eight scopes stay absent, and their warnings are CORRECT** — `cosa-voice`, `claude-plans`,
`lookml`, `par-pacific`, `retail-ai-location-strategy`, `google-project-wonderwall`, `scratchpad` are
not on the VM. Do not silence them; they are the registry reporting reality.

`lupin-mobile` is a **third** category worth naming, because it looks like the second: its INI path
`/var/lupin/src/lupin-mobile` is **already inside** the `./src -> /var/lupin/src` code mount, so it is
mount-covered and still missing — the nested repo is absent from the VM's checkout. **No compose edit
fixes it.** A mount gap and a content gap produce the identical warning.

### ⚠️ `create_host_path: false` — the two machines DISAGREE, measured

Both arms run with a negative control, 2026-07-25:

| bind form | dev (compose **v2.19.1**) | VM (compose **v5.3.1**) |
|---|---|---|
| short `- /host/x:/ctr/x:ro` | HOST DIR CREATED | HOST DIR CREATED |
| long, no `create_host_path` | not created | **HOST DIR CREATED** |
| long + `create_host_path: false` | not created | not created |

A probe run on the dev box concludes the key is redundant — **and that conclusion is false for the
machine these files deploy to.** `docker compose config` renders `bind: {}` on both versions and shows
the key on neither, so the rendered config cannot answer the question either. **Test on the VM.**

It matters because auto-create fails in the **worse direction**:

- path **absent** → registry logs a WARNING and skips. **LOUD.**
- path **present but empty** → registry registers the scope and 404s every file under it. **SILENT** —
  no warning is even possible, because from the registry's side the path exists.

An auto-created empty dir converts the first into the second. That is the whole reason the key is there.

### 🔴 MOUNTS ARE NOT PICKED UP BY A RESTART — Rick's question, answered

A container's **mount table is fixed at CREATION**. `docker restart` reuses the existing container,
mount set and all, so a compose edit is **invisible** to it. Only a **recreate** reads the new file:

```bash
sudo docker compose -f docker-compose.cloud-gpu.yml --env-file <env> up -d --force-recreate lupin-rest
# equivalently: docker rm -f lupin-rest-cloud-gpu && docker compose ... up -d
```

`src/scripts/lupin-vm.sh deploy` already does the right thing (`up -d --force-recreate`).

**Do not generalize this to "changes need a restart."** Content *inside* an already-mounted directory
IS live — `./src -> /var/lupin/src` means an edit lands in the container immediately. **The mount
table is not the mounted content**, and only the former needs a recreate. This is the same distinction
`src/tests/smoke/test_container_preflight.py` was written to police, and it has now bitten twice on
this VM: once on the CC sessions bind (07-24), once here.

### The gate that makes this non-recurring

`src/tests/unit/deploy/test_external_scope_mount_parity.py` — 6 tests, no docker required. It compares
`lupin-app.ini § external repos` against each compose file's mount targets and fails on any scope that
is neither mounted nor recorded, **by name and with a reason**, in a `KNOWN_ABSENT` map.

The map is checked in **both directions**: an entry that turns out to be covered fails too, so landing
a mount forces the exemption out. A one-way allow-list would have rotted into a permanent excuse.

- **Instrument proven**: run against `HEAD:docker-compose.cloud-gpu.yml` it names exactly the four
  mountable scopes. A parity test that has never been shown to fail is not evidence.
- **Its reverse arm caught the author**: `lupin-mobile` had been listed as known-absent when it is in
  fact mount-covered — a false exemption that read as a handled case.
- **What it cannot see, by construction**: it is STATIC. A compose file can declare a perfect mount set
  against a host path that does not exist. Only `test_container_preflight.py` (host-side, docker
  required) sees the running container. This tier answers *"was it ever written down"*, never
  *"is it mounted right now"*.

---

## 7d. Addendum — 2026-07-26 (Mr. Radio, session `9a63d597`): the bring-up stops being prose

Rick's P0 (`47c4801b`), verbatim: *"I do not want to go through a weeks worth of debugging just to
get a server up and running ever again. Let's automate the hell out of this!"*

### What this addendum replaces

§7, §7a, §7b and §7c are a **runbook** — a list of things a human must remember. That format has a
measured failure rate. §7b's own "Tooling defects found" section records:

> **`lupin-vm.sh deploy` runs `up -d --force-recreate` WITHOUT `--no-deps`** — exactly the hazard
> named above. It was avoided manually here. **Filing recommended.**

It was never filed. **It took `:7999` down on 2026-07-26 at 14:21, in the next session's first
deploy** — `cloudsql-socket-init`'s `rm -f` deleted the live socket the running proxy owned, and the
proxy went on reporting `healthy` because its probe never touches the socket. One day between
writing the hazard down and being bitten by it.

⇒ **Prose is not a guard. Only an executable assertion is.** Everything below converts runbook
paragraphs into assertions.

### The classified inventory

Five days of debugging (07-22 → 07-26) = **30 distinct defects**. Only **5** were code bugs;
**11** were ENVIRONMENT, **7** CONFIG drift, **5** INSTALLER/TOOLING, **4** FILES-GIT-CANNOT-DELIVER.
Full table + routing: [`v0.1.9/2026.07.26-vm-bring-up-automation-preflight-design.md`](v0.1.9/2026.07.26-vm-bring-up-automation-preflight-design.md).

**This is why a `:8000` end-to-end run would not have helped.** It runs on the same host, where the
host user *is* uid 1001 and the keys are the originals — structurally blind to 25 of the 30.

### The three new surfaces

| Surface | Answers |
|---|---|
| `src/conf/env-contract.tsv` | WHICH env vars must exist, who writes them, what shape — not their values, which are legitimately machine-specific |
| `src/conf/vm-unversioned-manifest.tsv` | WHICH gitignored payloads must be present, with expected owner/mode. **The category §7b named and left homeless** |
| `src/scripts/preflight-vm.sh` | 31 assertions over 5 layers, phase-aware, assert-only with an executable remedy per failure |

Plus `src/tests/unit/deploy/test_compose_service_parity.py` — the comparator for the four
hand-maintained `lupin-rest` definitions, which is the gap §7c's root cause named
(*"nothing compared the two files"*). Analysis:
[`v0.1.9/2026.07.26-configuration-splintering-analysis.md`](v0.1.9/2026.07.26-configuration-splintering-analysis.md).

### Gotchas this addendum adds (each cost a round-trip, as before)

- **`source ~/.bashrc` is a NO-OP in a non-interactive shell.** Debian's stock file opens with
  `case $- in *i*) ;; *) return;; esac`. A `gcloud ssh --command` probe that sources it still sees
  nothing — the first preflight run reported **12 false UNSETs on a healthy VM** for this reason.
  Read the `export NAME=` lines directly, and report which surface supplied each value.
- **`sudo git` needs the inline `-c safe.directory=`.** Root's gitconfig lacks the uid-1001-repo
  exception. `push-bundle` has always passed it inline; anything else reading those repos must too.
- **`cloud-gpu.env` is mode 600 / uid 1001** — a plain `grep` by the SSH user reads *nothing*, with
  no error. Read it with `sudo`, the way `sudo docker compose` does.
- **The VM's repo dirs are uid 1001 and the SSH user cannot write them.** Every git operation on the
  VM runs as root via sudo — which also means `~/.git-credentials` in the SSH user's home is
  invisible on that path.

### What preflight does NOT do

Assert-only (Rick's ruling) — it repairs nothing. It does not silence the eight *correct* absent-scope
warnings. And **a green preflight means those 31 things hold, never that the VM is fine.** The list
growing is the deliverable.
