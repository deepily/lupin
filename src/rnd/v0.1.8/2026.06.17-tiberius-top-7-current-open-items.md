# Tiberius 👑 — Top-7 Current Open Items (for Rick co-parse)

**Date:** 2026-06-17 (eve, post-respin)
**Author:** Tiberius 👑 (session `5d550c64`)
**Branch:** `wip-v0.1.8`
**Purpose:** Rick asked for the 7 most-important *current, still-open* items, drawn from the current-working-branch and previous-branch sections of `TODO.md`, to parse together for the next lane.

## Method

Filtered the ~365 open `- [ ]` checkboxes in `TODO.md` against the eve memento, **dropping everything that has landed**: GCP-M2 cloud-test (shipped+validated+pushed), the CoSA 100%-coverage marathon (complete), the unified task-store cutover (store-only live), the asker→sender DM-path rename (cut over live), and the struck-through 2026-06-11 top-7. What remains below is genuinely current and open, ranked by importance.

> **Caveat:** `TODO.md` is a 273KB bloated historical log (flagged in-doc for size-management). A couple of items may be partially landed; filtered hard but not infallible. Flag any that are actually done.

---

## The Top-7 (descending importance)

### 1. Finish the multiplexer JS→TS migration — cutover flip + contract-test gap
The biggest live engineering lane. Three threads:
- **Cutover flip** — Mr Radio owns the INI flip (`legacy notifications redirect enabled` class); **Rick-gated**. Bundle already rebuilt, `:8000` e2e 75/0, cache-bust held.
- **Contract-test gap** — store **P1 `3d90d3a7`**: a contract test (client URLs ↔ real routes) + a fail-loud e2e so 404s stop getting swallowed.
- **Phase 7b toggle-widget port** — migrate the 📞/🔔 speakerphone toggle + monopoly-pin logic out of legacy `notifications.js` into the multiplexer (`TODO.md` line ~1062).
- Related in-flight (other personas): `f4cf8e46` mux visual/layout quality pass (renders "ugly + wonky" per Rick).

### 2. cosa-voice token-reduction endgame — legacy commons-DM full-removal + polling-tool audit
**Rick's stated goal #1** (token efficiency). After the `dm_send` cutover soaks with zero-hit telemetry:
- **Full-removal pass** — delete the commented-out legacy machinery: `commons_send_to`, `ask_async`/`ask_sync` DM-mode, `register-question` + `CommonsQuestionWatcher` + the main.py lifespan, the 2 legacy listener handlers. KEEP polling-mode + broadcasts + presence (`TODO.md` lines 9, 354). Design: `src/rnd/v0.1.8/2026.06.13-cosa-voice-token-reduction/03-phase4-legacy-commons-dm-retirement-proposal.md`.
- **Polling-tool audit** — now directed DMs use `dm_send`, are `commons_ask_async`/`ask_sync` polling-mode (+ `commons_read` cost) still used at all? Retire if dead (`TODO.md` line 8).

### 3. Bounded-CC agent migrations — ratify D1–D9, then Deep Research → podcast → presentation
Converts metered **firewalled-account** spend into already-paid **Max-plan fixed cost** (cost-shift, not zero-cost).
- **Ratify the D1–D9 decision matrix** first (`TODO.md` line 814).
- **Deep Research** is the largest current firewalled line-item; uses WebSearch + WebFetch natively (already in CC tool surface) (`TODO.md` line 1080).
- **Podcast** script-generation phase (line 1082) and **Presentation** content-generation phase (line 1084) follow.
- All non-interactive runs must default `scheduled_at` to the post-midnight off-peak window.

### 4. Alembic TRUE baseline migration
Make `alembic upgrade head` work from an **empty DB** — encode `schema.sql`'s origin as migration zero, retiring the stamp-then-upgrade workaround (currently the official documented bootstrap). GCP-deploy-relevant durable fix (D7 hybrid ruling; `TODO.md` line 35).

### 5. Arbiter false-positive / whitelist tuning work-order
Cuts the false escalations to Rick (the repeat manager-staleness pokes are **live evidence** — they fired against an interactive manager whose "last signal" was minutes earlier). Suppression classes (D2 ratified):
- (a) human-gated marker w/ TTL, (b) post-ritual standby, (c) zero-workers+`:8000`-run, (d) acking-manager;
- + root-cause **what "last signal" actually measures**;
- + Mr Radio addendum #14 (quota-freeze correlation). Evidence: memento §4 (1 TP vs 15 FP). (`TODO.md` line 36.)

### 6. GCP prod milestone (M3)
M2 cloud-test is shipped+validated+pushed; next is the **prod cut**:
- FCM image-rebuild + SA-JSON; Secure-Boot re-enforce (current OFF is a temporary exception); seed the 12 empty provider-key secret versions; bind data-plane IAM to `vm_sa`; resolve the `LUPIN_ENV == "production"` cloud-trigger tension; prod-vs-test profile decision. (`TODO.md` lines 296–299, 314–319, 396.)

### 7. Fleet context-pressure assessment utility
Rick's "really interesting" parked proposal: a **sensor + recommender** that reads each worker's transcript JSONL occupancy (last assistant message's `input + cache_read + cache_creation` = true context %) and recommends compact vs harvest+respawn-with-memento. Never auto-acts. Ship the **P1-only sensor** first. Proposal: `src/rnd/v0.1.8/2026.06.07-managing-context-memory/2026.06.07-fleet-context-pressure-assessment.md` (`TODO.md` line 196).

---

## Honorable mentions (swap candidates)
- **Task-store adoption gaps E (chase-flag) + F (write-scope)** — Rick decisions; plus the **TODO-vs-store strategic talk** Rick flagged (what does `TODO.md` serve now that the unified task list is live?).
- **Repo-hygiene sweep** — orphan triage (`purge-offline-heartbeat-events.py`, dead-letter `__init__.py`), commit the 06-09 stop-hook brief doc, `.claude-session.md` → `.gitignore` + `git rm --cached` (`TODO.md` line 150).
- **Mic-monopoly sender-card indicator** — multiplexer follow-up (`TODO.md` lines 569, 586–592).
- **E2E smoke-budget proof point** — confirm proxy-test exclusion brings the smoke leg under its 3600s budget (`TODO.md` line 37).

## Already in flight (NOT counted in the 7)
- **DM-as-liveness toggle** — DONE this session: merged into the arbiter (`d0c64ca9`, `--no-ff` of Rachel's `39a11827`) on Mr Radio's fresh-critical APPROVE, post-merge green (1064/0), deployed via `:8001` restart. Push remains Rick's gate.

## State at authoring
- Owned in-progress store items: **0** (EOD sweep `085f773a` closed with receipt `d0c64ca9`).
- Held stack on `wip-v0.1.8` awaiting **Rick's push** (his gate; never auto-pushed).
