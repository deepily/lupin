# Loop Rename — Implementation Plan (Round 2a, Decision 6)

**Date:** 2026-06-08
**Author:** Tiberius 👑 (session d9e65cd8) — manager/serializer
**Status:** DRAFT → María 🌸 pins success criteria → reviewer pass → fresh implementer crew. Held + unpushed.
**Design authority (locked):** `2026.06.08-arbiter-consumption-gap-and-operator-loop.md` § Part 5 Decision 6 ("ONE descriptive name everywhere, no mapping, break the contract — Rick's ruling").
**Sequencing:** Rick chose **rename-FIRST** (2026-06-08) so the Round-2b routing/live-push/actuation work touches the arbiter files once, on already-clean names.

---

## TL;DR

The generic build-lane shorthand `loop_a` / `loop_b` / `loop_b_fleet` / `LoopBRunner` / `HealthWatchLoop` leaked out of the implementation and **into the public `/state` contract**. Round 2a renames the concept to **ONE descriptive name at every layer** — class, runner, file, `/state` section, log event, docs — with **NO alias and NO backward-compat shim** (we own every consumer, so change it globally). This is a pure rename: **zero behavior change**.

⚠️ **This changes the `/state` contract** (`loop_a`→`health_watcher`, `loop_b_fleet`→`fleet_arbiter`). The `:8001` service AND its `:7999` proxy consumer must ship + deploy together (break-before-make; we own both ends). My 2026-06-08 deploy-probe read `loop_b_fleet` — post-rename it's `fleet_arbiter`.

---

## The rename map (Decision 6, authoritative)

| Concept | OLD (generic) | NEW (one name everywhere) |
|---|---|---|
| Health-watch loop — class / runner | `HealthWatchLoop` | `HealthWatcherLoop` |
| Fleet-arbiter loop — class / runner | `LoopBRunner` | `FleetArbiterLoop` |
| `/state` section — health output | `loop_a` | `health_watcher` |
| `/state` section — fleet output | `loop_b_fleet` | `fleet_arbiter` |
| Log events | `loop_a_disabled`, `loop_b_render`, `loop_b_job_start`, `loop_b_recycle`, `loop_b_job_error`, `loop_b_cancel_error` | `health_watcher_disabled`, `fleet_arbiter_render`, `fleet_arbiter_job_start`, `fleet_arbiter_recycle`, `fleet_arbiter_job_error`, `fleet_arbiter_cancel_error` |
| **Module filenames** (doctrine: name everywhere) | `loop_b.py`, `health_watch.py` | `fleet_arbiter_loop.py`, `health_watcher.py` |

*(Micro-choice already locked in Decision 6: the fleet section is `fleet_arbiter`, matching the loop root — not `fleet_view`.)*

---

## Blast radius (grepped 2026-06-08; implementer MUST re-verify each)

**Source (6):** `src/lupin_arbiter_app/loop_b.py` (→ `fleet_arbiter_loop.py`; class `LoopBRunner`→`FleetArbiterLoop`, log events, `ESCALATION_TOPIC` stays), `src/lupin_arbiter_app/health_watch.py` (→ `health_watcher.py`; `HealthWatchLoop`→`HealthWatcherLoop`), `src/lupin_arbiter_app/app.py` (`/state` composite keys `loop_a`/`loop_b_fleet` + per-section `awaiting` placeholders + imports of the renamed modules/classes), `src/lupin_arbiter_app/local_snapshot_store.py` (section keys), `src/cosa/rest/routers/arbiter.py` (the `:7999` proxy reads `loop_a`/`loop_b_fleet` from the pulled composite — **lockstep update**), `src/cosa/rest/arbiter_bootstrap.py` (verify what it references).

**Tests (4 + 1 to verify):** `test_lupin_arbiter_app_app.py`, `test_lupin_arbiter_app_health_watch.py` (→ rename to `test_health_watcher.py`?), `test_lupin_arbiter_app_loop_b.py` (→ `test_fleet_arbiter_loop.py`?), `test_arbiter_router.py` (asserts `body["loop_a"]`/`body["loop_b_fleet"]` — I touched line 125 in Round 1; the rename touches 112). **`test_presentation_veo_renderer.py`** — grep hit on `loop_b`; **almost certainly a FALSE POSITIVE** (unrelated local var) — implementer: confirm and LEAVE if so.

**Docs:** mark Decision 6 "✅ implemented" in the consumption-gap doc; the `2026.06.07-arbiter-and-heartbeat-hook-diagrams.md` references old names (it's another session's uncommitted file — DM/flag, don't edit without coordination).

---

## Steps

> File renames use `git mv` (preserve history). After EVERY edit: py_compile + import chain. **No alias, no shim, no compat layer** (Decision 6). Pure rename — **zero behavior change** is the headline AC.

1. **`git mv` the two modules** → `fleet_arbiter_loop.py`, `health_watcher.py`; update every import (`app.py`, `arbiter_bootstrap.py`, tests).
2. **Rename classes** `LoopBRunner`→`FleetArbiterLoop`, `HealthWatchLoop`→`HealthWatcherLoop` + all references.
3. **`/state` section keys** `loop_a`→`health_watcher`, `loop_b_fleet`→`fleet_arbiter` in `local_snapshot_store.py` + `app.py` (composite + `awaiting` placeholders).
4. **Proxy lockstep** — `routers/arbiter.py` reads the new keys; the proxy passthrough body now carries `health_watcher`/`fleet_arbiter`.
5. **Log events** → the `fleet_arbiter_*` / `health_watcher_*` names.
6. **Tests** — update every asserted key/class/import; rename the two test files to match; re-verify the veo false-positive.
7. **Docs** — Decision 6 → ✅ implemented; flag the diagrams doc owner.

---

## Acceptance criteria (María to pin/extend)

- **Zero behavior change** — only names differ; `/state` shape identical except the two section KEYS. A before/after `/state` diff shows only `loop_a`→`health_watcher`, `loop_b_fleet`→`fleet_arbiter`.
- **Zero residual** generic tokens: grep `loop_a|loop_b|LoopBRunner|HealthWatchLoop|loop_b_fleet|loop_b_render` across `src` (excl. `__pycache__`/worktraees/the veo false-positive/historical rnd archives) → **empty**. No alias/shim introduced.
- **100% L/B** on every touched `lupin_arbiter_app` + arbiter file (rename keeps existing coverage; updated tests still exercise the renamed surface). Bootstrap import-guards may carry the existing `# pragma: no cover`.
- **Proxy + service in lockstep** — `GET /api/arbiter/fleet-state` returns `fleet_arbiter`/`health_watcher` keys; no consumer still reads the old keys.
- **Deploy step:** after merge-to-build, restart `lupin-arbiter-app` :8001 so `/state` serves the new keys; re-verify the liveness smoke (the Round-1 fix still reads workers alive under the new section name).
- Full unit suite green (pre-existing F2 decision_proxy isolation-pollution excepted, filed separately).

### Observer-pinned criteria (María 🌸 — sharpening, 2026-06-08)

**The two judgment calls Tiberius raised — ruled:**
1. **/state contract break → YES, assert no consumer reads old keys, AND make skew *structurally impossible*:** the proxy (`routers/arbiter.py`) change and the service (`app.py` / `local_snapshot_store.py`) change land in **ONE commit** — so a revert reverts both ends together and there is no partial state to skew. (Tightens Risk #2 from "ship together" to "cannot be split.")
2. **Module-filename `git mv` → YES, in-scope** per one-name-everywhere — **and extend it to the two TEST filenames** (`…health_watch.py`→`test_health_watcher.py`, `…loop_b.py`→`test_fleet_arbiter_loop.py`) via `git mv`. The "?" in the Blast-radius/Tests line is resolved to a criterion, not an option.

**Receipts, not assertions (no-confab — every AC closes with a primary artifact, never "I checked"):**
- **Zero-residual** = the *pasted* grep command + literal empty output + the *exact* exclusion set. **⚠️ Grep-token gap caught:** add **`health_watch`** (lowercase module/file token) — the current set has `HealthWatchLoop` but NOT lowercase `health_watch`, so a lingering `import health_watch` or `"health_watch"` string would pass a "clean" grep falsely. Final case-insensitive set: `loop_a|loop_b|loop_b_fleet|LoopBRunner|HealthWatchLoop|health_watch`.
- **Zero-behavior-change** = the *pasted* `/state` before/after diff showing ONLY the two key renames — nothing else moved.
- **Liveness smoke** = the *pasted* post-restart `/state` JSON showing (a) the new `fleet_arbiter`/`health_watcher` keys populated AND (b) a known-live worker reading **ALIVE under the new section key** — an explicit re-proof that the Round-1 `852ab83` union-with-no-stop-event fix survived the rename. "Smoke passed" alone is rejected.

**Add a NEGATIVE test:** flip `test_arbiter_router.py` to assert the new keys AND assert the **old keys are ABSENT** (`"loop_a" not in body and "loop_b_fleet" not in body`) — prove the old contract is *gone*, not merely that a new one exists.

**Residual-grep scope carve-out (so "clean" is reproducible AND we don't falsify history):**
- MUST be clean: all active code, tests, config, and any forward-looking doc.
- LEGITIMATELY excluded (old names are historical fact there): dated R&D docs / postmortems / observer logs; the veo false-positive (`test_presentation_veo_renderer.py` — confirm-and-leave); **and the cross-session uncommitted `2026.06.07-…-diagrams.md`** (another session owns it — DM-flag, do NOT edit) → it is NOT a residual-grep failure at rename-close.

**Coverage (my lane):** CHANGED-SURFACE on rename-touched files = INTERIM, flagged to Rick for explicit confirm at the gate. A pure rename must NOT *drop* coverage — criterion = coverage on the renamed surface ≥ pre-rename (no regression) + 100% L/B on any changed line; attach the coverage excerpt for the touched files as the receipt.

**Low-severity observer note (NOT a 2a blocker):** the new section key `fleet_arbiter` sits one hyphen from the existing `ROSTER_TOPIC = "fleet-arbiter"` (being dropped in 2b). Different namespaces (JSON key vs topic string) → no collision, but the implementer must NOT touch the topic constant during 2a, and 2b's roster-drop should note the proximity so they're never conflated.

**Definition of done** = every Execution-log box checked WITH its receipt attached + the negative test green + the post-deploy liveness smoke pasted.

---

## Out of scope (Round 2b, separate plans)

Routing rework (Part 6 table: drop roster broadcast, blocker+manager cc, crises→all-managers, infra→Rick, poll-error demote) · **live-push to Rick** (`live_notify_fn`) · **active-managers-on-duty resolver** · **auto-poke actuation** (the reach-out-and-poke feature) · **stall-detector calibration**. None of these in 2a — 2a is rename-only.

---

## Risks

1. **Contract break reaches an unseen consumer** → grep ALL of `src` (parent + cosa) for the old keys before declaring done; the `/state` keys are the public surface.
2. **Deploy skew** — if the service restarts on new code but a consumer still reads old keys (or vice-versa), `/state` reads null. Mitigate: ship service + proxy + tests in ONE commit; deploy atomically.
3. **File-rename history loss** → `git mv`, never delete+recreate.
4. **Scope creep into 2b** — resist; any behavior change is out of scope.

---

## Execution log (implementer fills) — Cheech 🌿, 2026-06-09

- [x] modules `git mv` + imports — `loop_b.py`→`fleet_arbiter_loop.py`, `health_watch.py`→`health_watcher.py` (+ 2 test files); history preserved (R status); all imports updated (app.py, fleet_arbiter_loop.py, both test files). py_compile + import-chain green.
- [x] classes renamed — `LoopBRunner`→`FleetArbiterLoop`, `HealthWatchLoop`→`HealthWatcherLoop`; all refs (app.py, tests).
- [x] `/state` section keys — `loop_a`→`health_watcher`, `loop_b_fleet`→`fleet_arbiter` in `local_snapshot_store.py` (docstrings), `health_watcher.py`/`fleet_arbiter_loop.py` (set_section), `app.py` (composite read + awaiting placeholders).
- [x] proxy lockstep — `routers/arbiter.py` reverse-proxy fallback envelope keys + description prose to the new names. **One-commit atomicity preserved** (proxy + service staged together; held — Rick commits).
- [x] log events — `loop_a_disabled`→`health_watcher_disabled`, `loop_b_render`→`fleet_arbiter_render`, `loop_b_job_start/recycle/job_error/cancel_error`→`fleet_arbiter_*`. **Broad-reading extras (Tiberius-ruled in):** `"loop":"A"/"B"` field→`"health_watcher"/"fleet_arbiter"`, thread names, escalation-msg prefixes, prose.
- [x] tests updated + renamed + veo false-positive confirmed — 4 test files updated; 2 renamed via `git mv`. **veo + 4 MORE same-class FPs confirmed** (`loop_attributes`/`loop_agent`/`loop_breaks` substrings in unrelated tests) — all left.
- [x] zero-residual grep clean — precise old-only grep (`health_watch` NOT followed by `er`) returns ONLY the 2 mandated negative-test absence-asserts (expected, not residual). Receipt in final report.
- [x] 100% L/B on touched files — 58 tests, 100% lines+branches on app.py / fleet_arbiter_loop.py / health_watcher.py / local_snapshot_store.py / routers/arbiter.py / __init__.
- [ ] deploy + liveness smoke under new keys — **POST-COMMIT, manager/Rick-owned** (restart `:8001`; NOT the implementer's step). Build-time TestClient /state proof done.

**Negative test (María):** added `assert "loop_a" not in body and "loop_b_fleet" not in body` in BOTH `test_arbiter_router.py` (proxy envelope) and `test_lupin_arbiter_app_app.py` (`/state` cold-start).
**Broad unit run:** 5849 passed / 6 failed. The 6 reds are foreign: 3× `test_decision_proxy_config` (F2 isolation-pollution — passes in isolation) + 3× `test_stop_hook_idle_behavior` (fails standalone too — another session's uncommitted `M src/lupin_cli/claude_code/hooks/stop.py`). NONE touch arbiter code → rename caused **zero** new failures.

---

## Handoff

Fresh **Implementer** + **Reviewer** (no Tester needed — pure rename, unit+smoke suffices). María 🌸 = Workflow Steward/Observer (success criteria pinned BEFORE code; stall-watch with the old-poker stopgap: she detects+flags-with-evidence, Tiberius fires `HeartbeatPokerJob` at the named stuck recipient — targeted, stall≠quiet, ~10-12min threshold on owed work, cap-then-escalate). Selective-stage per manifest; **never `git add -A`**; **never auto-commit/push** — hold for Rick.
