# Arbiter tmux-wake + heartbeat inbound-noise fix — engagement brief

**Date**: 2026-06-16
**Author**: Mr. Radio 🦉 (manager)
**Origin**: Rick voice directive 2026-06-16 (broadcast follow-up to the stop-hook investigation)
**Status**: brief — crew to be spawned. Hold all commits, never push.

## Context

The stop-hook investigation established: the self-poke continuation branch exists, is wired, and is enabled (`heartbeat.enabled=true`, `poke_cap=3`); it emits a `decision:block` silent re-prompt on a work-owed poke. Two separate problems remain, plus Rick's requested enhancement.

## Problems & goals

### (A) Residual probe — Mr. Radio (read-only)
Determine why a real stop didn't self-continue: `poke_cap=3` exhausted vs oracle `not_owed`. Needs `poke_count` (`heartbeat_poke_cap.py:get_poke_count`) + the stop-log for the session.

### (B) Remove the "unanswered inbound questions" component from the heartbeat poke
**Root issue**: the arbiter's OWN manager-stale poke-DMs (qids 925fd895, 43a5e677, ec4393d1, 6ed297c8, d86d0cd7 …) are counted by `_gather_unanswered_inbound_questions` (`stop.py:1112`) → fed as `open_inbound` into `evaluate_work_owed` → self-referential owed-count inflation. **For now**, gate/remove the `open_inbound` feed (Rick's "remove for the moment").
**CAUTION**: `stop.py` is the LIVE enabled Stop hook — edit in a git worktree or with the hook disabled (an enabled hook runs the working tree on every trigger).

### (C) Investigate the arbiter→dormant-session wake gap
Confirm/refute "the arbiter DM can't wake a dormant session." Evidence so far: broadcasts wake via `_inject_via_tmux` (`cc_notification_listener.py:289/404/505`, `hook_common.py:784` `tmux send-keys -l … Enter`), but the arbiter manager-stale poke is delivered as a WS DM that lands in the listener without necessarily tmux-injecting a stopped prompt. Pin down exactly where the arbiter poke is emitted (`src/cosa/agents/heartbeat_arbiter/`, `routers/arbiter.py`) and why it doesn't reach the tmux-inject path when the target is dormant.

### (D) Runtime-configurable arbiter poke surface — **default TMUX**
Add semantically-named INI keys (e.g. `arbiter poke wake mechanism = tmux|dm`) selecting how the arbiter surfaces its poke: direct tmux injection (wakes dormant sessions) vs DM (current, no wake). **Default = tmux.** Wire the arbiter to honor the key, reusing the existing `_inject_via_tmux` mechanism.

### (E) Tests — 100% lines/branches/functions on changed surface (B + D).

### (F) Splainer docs for the new keys in `lupin-app-splainer.ini` (paired with every new `lupin-app.ini` key).

## Key files (from recon)
- `src/lupin_cli/claude_code/hooks/stop.py` — `_run_heartbeat` (1213), `_gather_unanswered_inbound_questions` (1112), main branching (1418-1535)
- `src/lupin_cli/claude_code/hooks/lib/heartbeat_poke_cap.py` — poke_count storage
- `src/lupin_cli/claude_code/hooks/lib/cc_notification_listener.py` — `_inject_via_tmux` (289/404/505)
- `src/lupin_cli/claude_code/hooks/lib/hook_common.py` — tmux send-keys injector (718-784)
- `src/cosa/agents/heartbeat_arbiter/`, `src/cosa/rest/routers/arbiter.py`, `src/cosa/rest/arbiter_bootstrap.py` — arbiter emit path
- `src/conf/lupin-app.ini` + `lupin-app-splainer.ini` — new config keys

## Sequencing
- B is independent → parallelizable immediately (with the worktree caveat).
- C grounds D (investigate before wiring).
- E + Review gate on B + D landing.

## Crew & discipline
- Manager: Mr. Radio. Crew: implementer(s) + fresh-critical reviewer (reproduce-not-trust) + tester.
- Owners stamped to REAL personas at spawn (verify from spawn ack; `persona_preference` ≠ guarantee).
- Fleet-auditable items minted/closed via MCP `task_create`/`task_transition` (the harness mirror is itself under suspicion — see bug 9bf1dc4a).
- Hold all commits as a checkpoint package for Rick; **never push**.
