# Multiplexer Migration — Flip Walkthrough (PREPARED, hold for Rick's GO)

**Date**: 2026-06-17
**Author**: Mr. Radio 🦉 (session `496cdc3d`)
**Status**: PREPARED — present to Rick step-by-step ONLY on his explicit GO. Re-ping every 15 min until he acquiesces.
**Subject**: walking Rick through the JS→TS multiplexer client cutover, which is now FLIP-READY.

---

## 0. One-line summary
The TS multiplexer client is parity-complete and now **flip-ready**: bundle rebuilt + proven on `:8000` (75/0), cache-bust merged held. The flip itself is a **one-line INI change** — Rick's gate.

## 1. Where we are (state)
- **Legacy JS** `notifications.js` (912 KB) is the LIVE client at `/app/notifications`.
- **TS multiplexer** (`src/lupin_app/static/js/multiplexer/**`, ~75 .ts) is BANKED at `/app/multiplexer`, served via `/static/dist/multiplexer/boot.js`.
- **Route seam**: `routers/pages.py:83-105` — `/app/notifications` serves legacy; 302→`/app/multiplexer` when INI `legacy notifications redirect enabled` = True (currently **False**, `lupin-app.ini:815`). `?classic=1` escape hatch preserved.
- Parity verified weeks ago (Lanes A–E, mux c8 100%); task-list card + accordion + blocked_by-array landed `e55fa1d4`.

## 2. What we did today (made it flip-ready)
1. **Rebuilt the stale dist bundle** on the SERVED main checkout (`dist/` is gitignored + bind-mounted, so the served tree is what matters) → new hash `41ad5a63f89f` (was `878f67c41cab`, ~17 h stale); tsc clean; carries the task-list accordion + blocked_by-array.
2. **Cache-bust** `?v=20260617a` on `multiplexer.html:245` → merged HELD `c6f78868`.
3. **`:8000` proof**: bounced `lupin-rest-test`, confirmed it serves the new bundle, ran `e2e -k multiplexer` → `ts-06b3ddd8` = **75 passed / 0 failed / 1 skipped**; page loads + task-list card renders.

## 3. THE FLIP (Rick's one-line gate)
- Set `lupin-app.ini:815` `legacy notifications redirect enabled` **False → True**.
- `:7999` (dev) hot-reloads the INI automatically; `:8000` (test) needs a bounce (`docker restart lupin-rest-test` — ConfigMgr reads INI once at boot). Shared INI = per-restart, not per-server.
- Effect: `/app/notifications` → 302 → `/app/multiplexer`. `?classic=1` still reaches legacy.
- **Cutover ≠ deletion** (Rick's D2 ruling): the JS client + its E2E suites + `?classic=1` stay long-term as a JS-vs-TS research corpus.

## 4. Verify post-flip (~30 s)
- Hit `/app/notifications` → confirm redirect to `/app/multiplexer`.
- Confirm the page loads + notification/task-list cards render.
- Flag-ON path has no standing live E2E (can't test without flipping the shared INI) → the `:8000` 75/0 run + a 30 s flip-day eyeball is the bar.

## 5. Rollback (instant)
- Flip `legacy notifications redirect enabled` back to **False** (one line) → reverts to legacy immediately. Zero-risk, fully reversible.

## 6. Post-flip follow-ups (filed, NOT blocking the flip) — store task `16b5dd0c`
- **Cache-bust hardening**: adopt a runtime manifest-loader (auto-busts on every rebuild; kills the forgot-the-?v drift class). Manual `?v` is fine for flip day.
- **Prod Dockerfile dist-gap**: prod/GCP image 404s on `boot.js` (`.dockerignore:77` excludes `dist/` + no npm step). Fix = multi-stage `node:22 AS mux-build`. **Separate prod-cutover blocker** — the `:7999`/`:8000` flip is unaffected.

## 7. Push
- Everything is HELD on `wip-v0.1.8` (`c6f78868` cache-bust + the rebuilt served bundle). **Push is Rick's gate.**

---

### Decision points for Rick during the walkthrough
1. **Flip now or hold?** (the INI one-liner — recommend: flip on `:7999` first to eyeball, then `:8000`).
2. **Push timing** for the held commits.
3. **Sequence the 2 post-flip follow-ups** (manifest-loader + prod Dockerfile) — now or after soak.
