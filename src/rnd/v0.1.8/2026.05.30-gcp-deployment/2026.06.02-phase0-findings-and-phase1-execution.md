# GCP Milestone 1 — Phase 0 Findings & Phase 1 Execution Log

**Date:** 2026-06-02
**Author:** Mr. Radio 🦉 (session c0aede3d)
**Branch:** wip-v0.1.8-2026.05.29-preparing-for-gcp-deployment
**Parent plan:** [`2026.05.30-gcp-deployment-provisioning-plan.md`](2026.05.30-gcp-deployment-provisioning-plan.md)
**Status:** Phase 0 validation COMPLETE · Phase 1 safe edits DONE (uncommitted) · destructive items HELD for Rick

> This is the **single Phase-0 findings record** required by plan §4.4 — every V1–V8 go/no-go verdict and every straw-man flip, plus the Phase-1 execution log. Run live against the real sandbox project on 2026-06-02 (Rick refreshed `gcloud` creds for `hello-world-foo-423219`).

---

## 1. Phase 0 — Credential-Gated Validation Verdicts

**Environment:** project `hello-world-foo-423219` (num `994324562696`, ACTIVE) · region `us-central1` · zone `us-central1-a` · account `admin@rickruiz.altostrat.com`.

| Step | What it gates | Verdict | Finding |
|---|---|---|---|
| **V1** Project & region | D3 | ✅ **GO** | Project ACTIVE; D3 = **reuse sandbox** confirmed (Rick-locked). Project number `994324562696`. |
| **V2** Enabled APIs | "can we provision" | ⚠️ **GO w/ gap** | ON: `aiplatform`, `artifactregistry`, `cloudbuild`, `compute`, `run`, `secretmanager`. **OFF: `sqladmin`** (blocks V5) and `container` (GKE — Phase-5 only, expected). |
| **V3** GPU quota | D1 VM feasibility / D2 | ✅ **GO** | `nvidia-l4` + `nvidia-a100` available in zone. **`NVIDIA_L4_GPUS` limit = 8, usage = 0** → no quota-increase needed (this was the one potential hard blocker). `NVIDIA_A100_GPUS` limit = 1. |
| **V4** GCS buckets | D4 | ✅ **GO — REUSE** | `gs://lupin-lancedb-test` and `gs://lupin-deep-research-test` **both exist in US-CENTRAL1** (correct region) → D4 test-bucket decision = **reuse, skip provisioning**. `-prod` buckets absent (expected; M1 is test-only). |
| **V5** Cloud SQL | D4 | ⛔ **BLOCKED** | `sqladmin` API disabled → cannot list instances. **Held:** enabling the API is a project mutation; deferred to Rick. PG16 existence/`connectionName` unknown until enabled. |
| **V6** Secrets inventory | D8 / Phase-2 wiring | ✅ **GO** | Only `lupin-notification-api-key` exists (created 2025-11-12). **9 keys to create** in Secret Manager (see §2 count correction). |
| **V7** IAM on default compute SA | least-privilege | ⚠️ **GO (advisory)** | Default compute SA has **broad** roles: `aiplatform.admin`, `ml.admin`, `discoveryengine.admin`, `notebooks.admin`, `datastore.user`, `compute.storageAdmin`, **`storage.admin`**. Missing the three explicit runtime roles (`cloudsql.client`, `secretmanager.secretAccessor`, `artifactregistry.reader`). **Phase-2:** downgrade `storage.admin`→`storage.objectUser` + add the three; do NOT block Phase 0. |
| **V8** Artifact Registry | image-push target | ✅ **GO — provision-new** | No AR repo exists → provision-new `lupin` in `us-central1` (Phase 2). |

### D8 — Secret-hygiene forensic (read-only)

**Verdict: git history is CLEAN — no history rewrite required.**

- Path scan (`git log --all --diff-filter=A -- 'src/conf/keys/**'`): **empty** — keys were never path-committed to the Lupin repo.
- Content scan for real key shapes in tracked text: `sk-ant-` = 0, `sk-proj-` = 0, `AIza` = 0, `gsk_` = 0. The lone `sk-` hit was `"task-1"` test fixtures (false positive). The `hf_` hits are all `hf_downloader.py` / `hf_token` code identifiers — **no token-shaped `hf_<20+ alnum>` material**.
- **CoSA archive** (`/mnt/DATA02/cosa-git-archive-2026.05.29/cosa-dot-git`): no `conf/keys` file adds; `sk-ant-`/`AIza` = 0; `hf_` hits are training-code identifiers, not tokens.

→ D8 **shrinks** to: (a) remove the Dockerfile `COPY src/conf/keys` + `.dockerignore` exclude [DONE], (c) **defensive key rotation** [HELD for Rick], (d) secret-scan build gate [DONE]. The destructive `git filter-repo` + force-push ceremony is **dropped** (nothing to scrub) — consistent with "prefer the simplest reversible mechanism."

---

## 2. Count correction — `src/conf/keys/` holds **10** files, not 11

On-disk listing shows exactly **10** files: `anthropic-api-key-firewalled`, `eleven11`, `gemini`, `google`, `groq`, `huggingface`, `kagi`, `mistral`, `notification-api-claude-code-dev`, `openai`.

The plan's §1.4 reconciliation to "11" appears to **double-count** `notification-api-claude-code-dev`: it lists that file among the 10, then adds "plus the dev notification key" as an 11th — but `cloud-run-setup-secrets.sh:17` reads `notification-api-claude-code-dev`, i.e. the same file. **Authoritative count = 10.** Of these, 1 (`notification-api-claude-code-dev`, seeded as `lupin-notification-api-key`) is already in Secret Manager → **9 keys to create** in Phase 2.

---

## 3. Phase 1 — Pre-Deploy Hygiene Execution Log

All edits are **in the working tree, UNCOMMITTED** (Rick reviews before any commit/push). Tests: **17/17 pass**, run on :7999-eligible unit tier; `bootstrap_helpers.py` at 100% line+branch.

### Fix 1 — `LUPIN_ROOT` path drift ✅ DONE
- `cloud-run-deploy.sh`: `LUPIN_ROOT=/app`→`/var/lupin` (echo + `--set-env-vars`); `CONFIG_MGR_ARGS` `config_path`/`splainer_path` → `/var/lupin/src/conf/...`.
- `cloud-run-build.sh`: local-test `-e LUPIN_ROOT=/app`→`/var/lupin`.
- `docker/lupin/Dockerfile:244`: baked `LUPIN_CONFIG_MGR_CLI_ARGS` → `/var/lupin/src/conf/...`.
- `src/fastapi_app/main.py`: added boot-time `assert_lupin_root_valid()` (after `sys.path` insert) — promotes the weak "is-set" guard to "is-valid"; fails loud naming the missing canary.
- New module `src/fastapi_app/bootstrap_helpers.py` (stdlib-only, unit-testable).
- 🧩 Straw-man kept: `/var/lupin` canonical (4 small script edits vs ~25 Dockerfile edits). NOT flipped.

### Fix 3 — Parameterize project ID + region ✅ DONE
- New `src/scripts/cloud-run-config.sh` — shared resolver sourced by all four scripts; `${LUPIN_GCP_PROJECT_ID:?...}` fail-loud (no sandbox fallback); `REGION`/`REGISTRY`/`AR_REPO` defaults.
- New `src/scripts/cloud-run.env.example` (committed) + `cloud-run.env` added to `.gitignore`.
- All four `cloud-run-*.sh` now `source cloud-run-config.sh`; zero `hello-world-foo-423219` literals remain (the one header-comment example was genericized to `$LUPIN_GCP_PROJECT_ID`).
- `gcr.io/...` → Artifact Registry path shape `$REGISTRY/$PROJECT_ID/$AR_REPO/...` (the AR-now straw-man; deploy's image-existence check switched to `gcloud artifacts docker images describe`).

### Fix 2 — Secret hygiene
- **2a (exclude keys) ✅ DONE:** `.dockerignore` excludes `src/conf/keys/` + `**`; `Dockerfile:267` `COPY src/conf/keys` deleted (replaced with an explanatory NOTE).
- **2d (secret-scan gate) ✅ DONE:** new `src/scripts/secret-scan-gate.sh` (gitleaks; fails loud if gitleaks absent); wired as step `[0/3]` in `cloud-run-build.sh` **before** `docker build`.
- **2b (history rewrite) ⏸ NOT REQUIRED:** forensic (§1) confirms clean history — dropped.
- **2c (key rotation) 🔒 HELD for Rick:** outward-facing + one-way, and rotating live keys would break the running :7999 server. Rotation surfaces per provider are tabulated in plan §5.2 (2c).

### Tests added (all :7999-eligible unit tier, 17 cases, 17 pass)
- `test_startup_lupin_root_assertion.py` (3) — raise + pass + actionable-message branches; 100% on the helper.
- `test_cloud_run_script_paths.py` (4) — LUPIN_ROOT + config-path regression locks.
- `test_secret_hygiene.py` (4) — `.dockerignore` exclude, no-COPY regex lock, gate-before-build ordering, gate fails-loud-without-gitleaks.
- `test_cloud_run_parameterization.py` (6) — no sandbox literal, sources resolver, fail-loud form, `bash -n` × all, resolver abort-on-unset + success-on-set (hermetic tmp copy).

---

## 4. HELD for Rick (outward-facing / one-way / project-mutating)

1. **Enable `sqladmin` API** (`gcloud services enable sqladmin.googleapis.com`) — then re-run V5 to discover any existing PG16 instance + `connectionName`.
2. **Defensive key rotation** (Fix 2c) — 9 provider keys reachable in old image layers. Rotation breaks the live :7999 keys, so AI-unattended is unsafe.
3. **`LUPIN_ENV` tension** (plan §7.1.3) — cloud path gated on literal `"production"`, but M1 selects `[Lupin: Testing-GCS]`. Resolve before cutover.
4. **Commit/push** — nothing committed. Working tree also contains another session's work (`TODO.md`, `2026.06.02-messaging-coordination-plane-design.md`) — stage selectively.

## 5. Remaining Phase-0 AC bookkeeping (Rick-owned decisions)
- D1 (OAuth) / D2 (topology) already locked in the plan's authoritative decisions block — recorded.
- U1/U2 (2026-06-15 Agent-SDK credit ceiling vs measured BFE/TFE volume) — still owed; decisive for the D1 cost model.

---

## 6. Decisions Log (guided walkthrough, 2026-06-02 — ratified by Rick via `ask_multiple_choice`)

- 2026-06-02 — **LUPIN_ENV cloud-trigger** → introduce an explicit **`LUPIN_CLOUD_BACKED` environment variable**, decoupling cloud-backing from the env name. Why: the ~4 gating sites (`database.py` ×2, `main.py`, `system.py`) read raw `os.environ` below the ConfigurationManager layer, so an env var is a ~1-line gate change per site vs a refactor to thread config into the DB-URL builder. **Not yet implemented — next work item.**
- 2026-06-02 — **sqladmin API** → **enabled now** (`gcloud services enable sqladmin.googleapis.com`, exit 0). Why: cheap, reversible, required for Phase 2 regardless. Unblocked V5.
- 2026-06-02 — **V5 (now runnable)** → **no Cloud SQL instance exists** → D4-SQL = **provision-new PostgreSQL 16** in Phase 2 (capture `connectionName` then).
- 2026-06-02 — **Key rotation** → **defer until just before first remote registry push**. Why: zero realized exposure today (clean history, no image pushed remotely, keys only in local layers); rotating now would break the live :7999 keys for a non-existent risk.
- 2026-06-02 — **Registry** → **keep the Artifact Registry migration** (ratify; do not revert to gcr.io). Why: gcr.io deprecated, AR is the forward path, already implemented + tested, repo provisioned fresh in Phase 2 anyway.
- 2026-06-02 — **Commit/push** → **commit my 17 files selectively, hold the push**. Why: snapshot verified Phase-1 work behind a reviewable local commit; push (outward-facing) waits for Rick's explicit word; another session's `TODO.md` + messaging doc excluded from staging.
