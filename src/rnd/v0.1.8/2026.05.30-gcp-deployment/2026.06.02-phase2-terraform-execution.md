# GCP Milestone 1 — Phase 2 (Terraform IaC) Execution Log

**Date:** 2026-06-02
**Author:** Mr. Radio 🦉 (session c0aede3d)
**Branch:** wip-v0.1.8-2026.05.29-preparing-for-gcp-deployment
**Design of record:** [`2026.05.30-gcp-deployment-provisioning-plan.md`](2026.05.30-gcp-deployment-provisioning-plan.md) §6
**Status:** **All 7 modules authored + wired + `terraform validate` green + 14/14 bats-equivalent assertions pass** (UNCOMMITTED)

---

## What landed this pass

**Tooling:** Terraform v1.12.2 (local). `terraform fmt -recursive` clean; `terraform init -backend=false` installs the `hashicorp/google ~> 6.0` provider; **`terraform validate` → "Success! The configuration is valid."**

**Foundation (`src/terraform/`):**
- `envs/test/` — `versions.tf` (TF ≥1.5, google ~>6.0 provider), `backend.tf` (GCS remote state; bucket via `-backend-config`, never hardcoded), `variables.tf` (`project_id` with **no default** → fails loud if unset; `region`; `environment` with a test/prod validation; `create_buckets`), `main.tf` (module wiring), `terraform.tfvars.example`.
- `README.md` — layout, hard rules (no hardcoded project/region, no secrets in tf/tfvars), usage, status.
- `.gitignore` — terraform `.terraform/`, `*.tfstate*`, `terraform.tfvars`, `*.auto.tfvars` (the `.terraform.lock.hcl` IS committed to pin providers).

**Modules authored (✅):**
- `gcs-buckets` — LanceDB + Deep-Research buckets, names **composed** (`lupin-lancedb-${var.environment}`) to match the `lupin-app.ini` URIs (zero code change on the `storage_backend=gcs` flip). Uniform bucket-level access, versioning, public-access-prevention, lifecycle (LanceDB: keep N noncurrent versions; Deep-Research: NEARLINE @ 90d). **Reuse-aware** via `create_buckets` (the test env defaults to `false` — Phase-0 V4 confirmed the `-test` buckets already exist in us-central1; composed names still output for dependents).
- `artifact-registry` — Docker repo `lupin-images`, **immutable tags** (no silent overwrite / no-auto-promote), cleanup policy (keep last N). Phase-0 V8 confirmed none exists → provision-new.

**ACs met so far (plan §6.1):** module skeleton created; `terraform validate` passes; **zero hardcoded project IDs/regions** in any `.tf` (verified by grep — only `var.project_id`/`var.region`).

## Modules — ALL 7 complete (Pass 2, 2026-06-02)

Provisioning DAG (plan §6.5): `secret-manager` + `vpc-vpn` → `iam` → `cloud-sql-pg16` / `gcs` / `artifact-registry` → `gce-gpu-vm`. All wired in `envs/test/main.tf`; Terraform resolves the order via references (+ explicit `depends_on` for the SQL→VPC peering).

- [x] **`iam`** — `build-sa` (AR writer) + `runtime-sa` (cloudsql.client + per-bucket `storage.objectUser` NOT objectAdmin + per-secret `secretAccessor` + AR reader + log/metric writer). No owner/editor; replaces the over-privileged default compute SA (Phase-0 V7).
- [x] **`secret-manager`** — 14-secret inventory, automatic replication, **no versions** (values out-of-band). Accessor bindings live in `iam` (avoids a module cycle).
- [x] **`cloud-sql-pg16`** — private-IP-only PG16 (`db-custom-2-7680`), PITR + 02:00/7-day backup, flags (`max_connections=100`, `log_min_duration_statement=500`), `lupin_app` user (password from a Secret Manager data source — never tfvars), `lupin_db_test` DB, skip-create guard.
- [x] **`vpc-vpn`** — `lupin-vpc` + subnet (10.10.0.0/24), Service-Networking peering for the private-IP SQL, SSH (IAP range) + tight egress (`:3000-3001`) firewalls. **VPN gated on `enable_vpn` (default false)** — classic static-route tunnel authored, waits on the on-prem peer (Phase 0 didn't stand up the on-prem side; BGP/HA-VPN is a future flip).
- [x] **`gce-gpu-vm`** — `g2-standard-8` (L4 bundled with the g2 family — no `guest_accelerator`), `on_host_maintenance=TERMINATE`, 100 GB boot + **separate 200 GB data disk (`prevent_destroy`)**, driver+Toolkit+Docker startup-script, runs as `runtime-sa`. (`docker compose up` stays in the bash deploy wrapper per the §6.1 split.)
- [ ] **`envs/prod`** — clone of `envs/test` (the future GCP→GCP horizontal step) — deferred, not needed for Milestone 1.

### Verification (this pass)
- `terraform fmt -check -recursive` clean; `terraform init -backend=false` + **`terraform validate` → "configuration is valid"** for the full 7-module env.
- **Test layer** (`src/terraform/tests/modules.bats`, the ratified bats wrappers): 14 assertions — validate, no-hardcoded-project, project_id-has-no-default, objectUser-not-objectAdmin, per-resource IAM (project grants only cloudsql/log/metric), no owner/editor, immutable tags, private-IP SQL, PITR, password-from-secret, data-disk-prevent_destroy, GPU TERMINATE, runs-as-runtime-sa, no-secret-versions. **14/14 pass.** (bats isn't installed on this host — assertions executed via a bash mirror; install bats for the CI harness.)

### Still gated on real-apply (credential / out-of-band prerequisites)
- `terraform plan`/`apply` need: ADC (`gcloud auth application-default login`), the remote-state bucket (`<proj>-tfstate`, sandbox per Decision 2), and the `lupin-db-password` secret **version** populated (the cloud-sql data source reads it). Plan-level bats assertions get added once these exist.
- `enable_vpn=true` needs the on-prem peer IP + PSK (out of GCP control).
- D8 key-rotation (deferred to pre-remote-push) before the first AR image push.

## Decisions resolved (guided walkthrough, 2026-06-02)

- **HCL test strategy** → **bats wrappers** around `terraform plan -json` (+ jq assertions). No new language/toolchain; `terraform validate` + `fmt -check` stay in the gate. (terratest deferred to multi-env/prod scale.)
- **Remote-state bucket** → hosted in the **sandbox project** (`hello-world-foo-423219`), `<proj>-tfstate`, versioned + locking. Movable later via `terraform init -migrate-state`.

## Notes
- Build/deploy stays in the `cloud-run-*.sh` wrappers (already parameterized in Phase 1); Terraform owns resources only (plan §6.1 split).
- `cloud-sql-pg16` will consume the `LUPIN_CLOUD_BACKED` work: the VM/app env sets `LUPIN_CLOUD_BACKED=true` + `CLOUD_SQL_CONNECTION_NAME` (the module's output) so `database.py` builds the unix-socket URL.
