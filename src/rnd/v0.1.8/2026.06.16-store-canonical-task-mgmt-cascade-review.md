# Cascade Review Synthesis — María's Store-Canonical Task-Management Plan

**Date**: 2026-06-17 · **Synthesizer**: Mr. Radio 🦉 (manager f6292818) · **For**: María 🌸 (author/Steward), Tiberius 👑 (co-manager / UI-card lane), build crews.
**Plan reviewed**: `planning-is-prompting/src/rnd/2026.06.16-store-canonical-task-management.md`
**Engagement**: Rick GO broadcast `42c3e814` (+ correction `969f2ccd`). DoD = implemented end-to-end, thoroughly tested, PROVEN.

## VERDICT: APPROVE-WITH-REVISIONS (unanimous 3/3)

Short-form cascaded review, 3 lenses, all grounded in real code:
- **Architecture/correctness** — Tiffany 💍: design sound, 5-step order correct; R1+R3 hard pre-build gates.
- **Feasibility/integration** — Krishna 🦚: infra largely EXISTS; UI-card read-contract locked; token-efficiency gate.
- **Risk/migration** — Rachel 🕊️: direction kills `82e4eaf0` by construction; NO-GO on tests as written → GO with 6 test classes.

The single-source decision is correct and genuinely eliminates `82e4eaf0` by construction. The revisions below are required build ACs — land the BLOCKING gates (A, B, J) before/at cutover.

---

## CONSOLIDATED REQUIRED REVISIONS (build ACs)

### A. Sequencing / cutover — **BLOCKING** (Rachel R1, Tiffany R3, Krishna step-2)
The current oracle (`stop.py` work-owed path) reads owed work ONLY from the transcript's native `Task*` calls. If the doctrine "stop using TaskCreate" lands before the store-count seam ships, any obeying session → empty transcript → oracle sees 0 owed → **stops poking → goes dark while owing work** (silent missed poke — the dangerous inverse of a spurious poke).
- Ship step-2 (store-count seam) **behind a heartbeat flag, DEFAULT = old transcript path**, first.
- **DUAL-WRITE interim**: harness `TaskCreate` AND `task_create` until cutover.
- **Cutover order**: (1) ship step-2 behind flag default-old → (2) drain active sessions (§Q(d)) → (3) verify store owed-count == transcript owed-count per session → (4) flip flag → store-source → (5) ONLY THEN doctrine "stop TaskCreate" + retire mirror.
- **Mirror retire (step 5) EVIDENCE-gated, not assumption** (Tiffany R3): keep the mirror as a deprecated, **logged no-op** until its fire-log goes quiet fleet-wide for a defined window, THEN retire. Drop the dead native entries from `TASK_STORE_WRITE_TOOLS` (`post_tool_use.py:46-49`) so the arbiter progress beacon doesn't fire storeless on stray native calls.
- **Rollback**: keep `replay_task_state` + transcript seam behind the flag ≥1 clean fleet cycle; regression mid-flight → flip flag back to transcript-source instantly (no redeploy; native list still written during dual-write). Do NOT delete `replay_task_state` in step 2; deprecate after a clean cycle.

### B. Step-2 scope — preserve THREE signals — **BLOCKING** (Tiffany R1, Rachel R3, Krishna)
`evaluate_work_owed` takes THREE inputs: `owed_items` (stop.py:1281) + `_gather_outstanding_delegations` (1288, manager-owes-review) + `_gather_unanswered_inbound` (1289, worker-owes-reply). Swap ONLY the `owed_items`/`todo_items` source to the store count. **PRESERVE the delegations + inbound gatherers** (Rick's v3 signals, added 2026-06-09 because the oracle was under-populated). A naive "replace the work-owed path with a store count" silently regresses fleet liveness.

### C. Store-as-runtime-dep / fail-safe (Rachel R2, Krishna step-2, Tiffany Qa)
The Stop hook fires **every turn** (stop.py:1278); today the work-owed path is deliberately **`:7999`-FREE / filesystem-only / never-raises**. Step-2 adds the FIRST network dependency on the Stop hot path.
- **Bounded aggressive timeout (≤1-2s, specify ms) + connection reuse** so a slow/hung `:7999` never stalls turn-end.
- **Fail-safe DECISION**: store-down → **don't poke** (don't guess) + a distinct `heartbeat_store_unreachable` log phase (quiet on the hot path).
- **Fail-LOUD OBSERVABILITY at the supervision layer**: the ARBITER (reader 2) escalates to the user when IT also can't reach the store (store-down IS an emergency).
- **Local fallback**: keep transcript replay as a degraded fallback behind the flag (outage degrades to stale-not-blind), or a last-known-owed cache. Document **bounce-windows = no-poke windows**.
- Build on the existing `hooks/lib/task_store_client.py` (urllib `(ok,status,body)` triple, never-raises, transport-None spool trigger) — add a `query_owed(owner, statuses)` read method. (Correct the plan's inaccurate "hook already makes outbound calls, same shape" justification.)

### D. Step-3 arbiter — NET-NEW, not a "repoint" (Tiffany R2, Krishna step-3, Rachel R4)
The arbiter has NO direct owed-work read to "repoint." Its owed signal is ALREADY the stop-hook's emitted `work_owed` verdict, consumed via heartbeat-event records (`fleet_data_model.py:123-126`).
- **CLEANEST (preferred)**: leave the arbiter consuming the stop-hook's emitted verdict — that already IS single-source-by-construction (both flow from ONE store query in the stop-hook). No independent repoint needed.
- **IF** the arbiter instead calls `task_query` independently: extract the owed-count query (owner-scope + status-set) into ONE shared helper both readers import — "same store" ≠ "same answer" unless it's the "same query." Assert param-set equality.
- **KEEP** the `cap_reached`-episode counter on the heartbeat EVENT stream (the task store has no `cap_reached` concept). The `82e4eaf0` "cannot diverge" guarantee applies to the owed-COUNT only.

### E. Step-1 — reuse + one-name (Tiffany R4)
- Reuse `session_bridge._resolve_project_from_bridge_cwd()` (line 385, walks bridge cwd → .git ancestor → correct project; handles cwd-drift-into-`src/cosa`) — do NOT write new resolution.
- `manager_figure.derive_project_name` (40-62) is a DUPLICATE of `hook_credentials._derive_project_name` (per its own docstring 44-48). Converge BOTH on the bridge-cwd helper (one-name rule) OR document why credentials intentionally stays — fixing only one leaves the `LUPIN_ROOT`-basename bug live in the credentials path.
- **Path drift note**: real dir is `src/lupin_cli/claude_code/hooks/`.

### F. Step-4 UI card — read-contract LOCKED (Krishna Q(c)) → Tiberius's lane
- **COMPONENT**: clone the fleet-status pattern — `FleetStatusStore.ts` (poll-driven 60s, in-flight debounce) + `FleetStatusRenderer.ts` + `fleetStatusTable.ts` + `fleetModel.ts`; `FleetApiClient.get<T>` handles JWT/401 → `TaskListStore`/renderer.
- **DATA PATH**: back onto the **EXISTING `GET /api/tasks?owner_persona=&status=`** (`routers/tasks.py:419-477`, full fidelity: status/blocked_by/next_chase_ts/owner_persona/accountable_manager/priority). **NOT** `/api/arbiter/fleet-state` (that's a fleet-composite proxy, wrong shape). No new backing API needed.
- Bonus: once step-3 lands, per-session owed COUNTS can ride the existing fleet card for free (add count to fleet-state payload); the full item-fidelity task-LIST is the separate clone.

### G. Token efficiency — **goal #1 gate** (Krishna Q(b))
- POKE path = **ZERO model tokens** (hook HTTP response consumed by Python, never injected). Add `count_only=true` to `GET /api/tasks` to save server-side serialization on the count query.
- MODEL on-demand query (session-start / next-item-pick) via MCP `task_query` returns FAT rows (~200-400 tok/item, body + audit fields) → 5-15 items = ~1-5k tok/query. **Add a TERSE/PROJECTION mode** (id/title/status/blocked_by/next_chase_ts/priority — drop body + audit) BEFORE making on-demand query the canonical "see your list," or it re-creates the MCP token burn that got cosa-voice turned off.

### H. Doctrine — reconcile F4 (Rachel R5)
F4 "managers-first writes" is silently dropped: `POST /api/tasks` is auth-only (`require_api_key_or_jwt`), NOT manager-gated — F4 lived only in the retired mirror. New mandate = all workers write (workers CAN via MCP `task_create`, so worker-liveness holds). **Reconcile/RETIRE F4 explicitly** (a decision, not a side-effect).

### I. Header (Rachel R6, Tiffany)
Update plan header line 3 "DRAFT v2 … (do NOT implement)" → **APPROVED** (Rick GO `42c3e814`) so the next reader doesn't re-litigate.

### J. Test adequacy — NO-GO as written → GO with 6 classes (Rachel; 100% L/B/F mandate)
1. Unit 100% L/B/F on the new `stop.py` store-count seam — EVERY branch: store-up+rows / store-up-empty / store-unreachable→no-poke / timeout→no-poke / malformed→no-poke.
2. Unit proving delegations + inbound still feed `evaluate_work_owed` after the swap (gate B).
3. Migration drain test: replay→create→idempotent re-run (no dupes via `query_by_correlation_key`); rollback flag flips source with no data loss.
4. Multi-session fleet test (≥2 sessions): assert NONE goes dark across cutover.
5. `82e4eaf0` "identical by construction" across a REAL divergence attempt (not single happy-path equality).
6. Bounded-timeout latency test — the per-Stop query must not stall turn-end.

---

## Q(d) MIGRATION DRAIN (Rachel's design — adopt)
`replay_task_state(transcript_path)` already reconstructs a session's owed native items — it IS the drain tool. Per ACTIVE session: replay → for each owed item `task_create` it (owner = that session's persona; `correlation_key = cc-task:<sid>:g<gen>:<hid>`, idempotent vs any prior mirror row). The mirror was manager-gated → WORKER native items were never in the store → **the drain MUST run per active session, not just managers.** Idempotency via `query_by_correlation_key` (already in `task_store_client`) so re-runs never dupe.

## LANE / SEAM (confirmed with Tiberius)
- **Spine (Mr. Radio's lupin crew)**: Step 1 (E) FIRST, isolated + fresh-critical-reviewed → then Steps 2 (B/C) / 3 (D) / 5 (A step-5). `stop.py` is the LIVE enabled hook → build in a worktree.
- **UI card (Tiberius's crew)**: Step 4 (F/G-count_only). Independent; spins in parallel; consumes the locked `GET /api/tasks` read-contract.
- **Doctrine (María's PIP lane)**: Part-1 mandate + strip old guidance across 6 surfaces + reconcile F4 (H) + header (I), with the "not-live-until-build-ships" caveat; flip live at cutover step (4).

## BUILD GATES
Hard pre-build / pre-cutover gates: **A** (sequencing/dual-write/flag), **B** (preserve 3 signals), **J** (6 test classes @ 100% L/B/F). D/E/F/G are reuse + correctness sharpening. All commits HELD; push is Rick's gate.

---

## Build Progress + Cutover Rulings (Mr. Radio, 2026-06-17)

- **Step-1** MERGED `513b184c` (canonical resolve_project_name, one-name, credentials-path bug fixed). **Step-2** MERGED `8d77ba91` (flag-gated stop-hook store-count seam, default OFF). **Step-4 UI card** MERGED `30848a31` (Tiberius).
- **Step-3 VERIFIED NO-CODE**: the arbiter consumes the stop-hook's emitted `work_owed` via the heartbeat event stream (`fleet_data_model.py:123-126`). Once Step-2's flag flips, the arbiter's owed signal becomes store-derived automatically → single-source by construction. `cap_reached` correctly stays on the event stream (the store has no cap concept). §D cleanest confirmed — no arbiter build needed.
- **O1 RULING (apply — cutover gate):** reorder so delegations + inbound are gathered BEFORE the store read; they are local/store-independent and must NOT go dark during a store outage (§B intent = swap ONLY owed_items). In cutover-prep build.
- **O2 RULING (apply — cutover gate):** `count_only=true` on GET /api/tasks → true COUNT(*), not page-length (saturates at limit=100); query_owed uses it. Also the §G token win. In cutover-prep build.
- **O3:** per-status fresh urllib conn — latency-only, DEFERRED (the 1.0s timeout safety bound already shipped).
- **O4:** owed set excludes claimed/blocked per §B; pending→queued mapping is confirmed at the §A-step-3 count-parity verify.
- **Remaining spine:** cutover-prep (O1 / O2 / §Q(d) drain) → flag flip + per-session count-parity verify → **Step-5 mirror retire** (evidence-gated: convert to deprecated logged no-op now, delete after the fire-log goes quiet fleet-wide — full deletion is post-window).

## Proof Staging (Mr. Radio, 2026-06-17 ~07:08 UTC)

Spine BUILD COMPLETE + merged held: Step-1 `513b184c` · Step-2 `8d77ba91` · Step-3 no-code · Step-4 `30848a31`+`4eeab529` · cutover-prep `2b9c891d`.

**PROOF 1 — drain dry-run (live store, writes nothing):** auto-detected 5 active sessions, per-session report —
| session | persona | owed | skipped | would_create | parity |
|---|---|---|---|---|---|
| 570d9733 | tiberius | 1 | 1 | 0 | OK |
| a7fc6219 | rio | 0 | 0 | 0 | OK |
| 8e095304 | cheech | 0 | 0 | 0 | OK |
| 0ba3e504 | maria | 5 | 0 | 5 | **NO** |
| f6292818 | mr radio | 3 | 3 | 0 | OK |

TOTAL owed=9, created=0 (dry-run), skipped=4, would_create=5, failed=0, parity 4/5. **Validates §Q(d):** correctly identifies María's 5 un-mirrored items (the mirror-gap this engagement fixes) for idempotent back-fill; the 4 already-present sessions skip cleanly (idempotency proven).

**PROOF 2 — changed-surface unit tests on the MERGED tree:** 271 passed (task_store_drain, task_store_client, stop_hook_heartbeat, heartbeat_settings, tasks_router) — clean post-integration.

**STAGED for the supervised one-command cutover (Rick's go):** `drain --apply` (back-fills the 5 → parity 5/5) → verify parity → flip `heartbeat.owed_source_from_store=True`. All reversible. Then Step-5 mirror-retire post-quiet-window.

## ✅ CUTOVER EXECUTED — Rick's GO (broadcast 1b70f1a9, 2026-06-17 ~09:23 EDT)

The unified task-store is now the LIVE fleet-wide source of truth for the heartbeat oracle.

1. **`drain --apply`** — 4 active sessions; created 5 (María's un-mirrored items back-filled), skipped 4, failed 0 → **parity 4/4**. Confirm dry-run: would_create=0, skipped=9, parity 4/4.
2. **Flag flipped** — `heartbeat.owed_source_from_store: true` written to live `~/.claude/settings.json` (the fleet-wide global config every session's stop-hook reads).
3. **Verified** — `load_heartbeat_settings()` returns `owed_source_from_store: True`; the Stop-hook owed-count now sources from `task_store_client.query_owed` (the store), not transcript replay.
4. **Lockstep doctrine** — María flipped the 6 PIP doctrine surfaces §0 not-live→LIVE + the live `~/.claude/CLAUDE.md` strictly AFTER the flag flip (no dark window, per §A).

**Remaining (deferred by design):** Step-5 mirror-retire — evidence-gated; the mirror stays a live bridge until its fire-log goes quiet fleet-wide for a window, THEN delete + drop the dead `TASK_STORE_WRITE_TOOLS` entries. Reversibility retained: flip the flag back to False to revert to transcript-source instantly.
