# Task-List UI Card — Build Brief (Tiberius lane / Step 4)

**Date**: 2026-06-16 (EDT) · **Author/Manager**: Tiberius 👑 (`570d9733`)
**Status**: STAGED — execute-ready, **spawn-gated** on Mr. Radio's cascaded-review verdict + the locked `task_query` read-contract (his Q(c) finding). Do NOT spawn the crew or write code until that go lands (Rick's review-first sequencing).
**Canonical plan (LAW)**: planning-is-prompting → `src/rnd/2026.06.16-store-canonical-task-management.md` (María 🌸, v2) — Part 2, build **Step 4**.
**Lane seam (confirmed w/ Mr. Radio)**: Tiberius owns Step 4 (UI card end-to-end). Mr. Radio owns the spine (Step 1 → 2/3 → 5). Only cross-team dependency = the read-only `task_query` HTTP contract this card consumes.

---

## 1. Goal

A dedicated **task-list card** in the Lupin multiplexer / cosa-voice notifications UI, rendered from the unified task-store via `GET /api/tasks`, modeled on the **existing fleet-status card** (its data path + components are the template). It replaces the native Claude Code task widget and gives Rick a full-fidelity view of his own + the fleet's owed work — surfacing `blocked`, `blocked_by`, `next_chase`, `owner`, `accountable` that the native widget never could.

## 2. Read-contract (the cross-team seam) — ALREADY EXISTS

**`GET /api/tasks`** — `src/cosa/rest/routers/tasks.py:419-477`, handler `query_tasks()`.
- **Auth**: Bearer JWT OR `X-API-Key` (`require_api_key_or_jwt`).
- **Query params** (exact-match, AND semantics, newest-first): `owner_persona`, `status`, `gate_class`, `accountable_manager`, `project`, `item_class`, `correlation_key`, `limit` (default 100, cap 500), `offset`. Junk enum → 422.
- **Response**: `{ tasks: [ { id, item_class, title, body, project, owner_persona, accountable_manager, created_by, status, blocked_by, next_chase_ts, gate_class, priority, source_qid, correlation_key, created_ts, updated_ts } ], count }`.

> **NET-NEW BACKEND WORK ≈ ZERO.** The endpoint serves every column the card needs. Mr. Radio's Q(c) finding will lock the *exact consumption shape* (which params the card calls with, e.g. `owner_persona=<self>` + open statuses). Slot that in when it lands; no endpoint change expected. If Q(c) reveals a missing field/param, that's the only backend delta — escalate to Mr. Radio (his contract).

## 3. Column set (from María, full fidelity)

`title` · `item_class` (task/decision/gate/bug/review_request) · `status` (queued/in_progress|working/blocked/done) · `blocked_by` (typed) · `next_chase_ts` · `owner_persona` · `accountable_manager` · `priority` · `project`.
**Differentiator vs native widget** = surfacing `blocked` + `blocked_by` + `next_chase` + `owner`/`accountable`.

## 4. Clone pattern — fleet-status card is the template

All under `src/lupin_app/static/js/multiplexer/`. Mirror these 7 artifacts:

| Artifact | Fleet-status template | New task-list file |
|---|---|---|
| Renderer (DOM orchestrator, subscribes to a `store_*_changed` event, repaints) | `render/FleetStatusRenderer.ts` | `render/TaskListRenderer.ts` |
| Store (fetch + cache + emit changed event) | `stores/FleetStatusStore.ts` | `stores/TaskListStore.ts` |
| Model (pure grouping/formatting, no DOM) | `render/fleetModel.ts` | `render/taskListModel.ts` |
| Template (table/thead/tbody/row builders) | `render/templates/fleetStatusTable.ts` | `render/templates/taskListTable.ts` |
| HTML mount slot | `static/html/multiplexer.html` `<section id="fleet-status-pane">` (~L174-179) | add `<section id="task-list-pane">` |
| CSS | `static/css/multiplexer/fleet-status.css` | `static/css/multiplexer/task-list.css` |
| Boot wiring (factory + mount + startPolling + registry) | `multiplexer/boot.ts` (~L469-479, L520) | add task-list block |

**Data path to mirror**: `FleetStatusStore` polls (`FLEET_STATUS_POLL_INTERVAL_MS = 60000`), GETs its endpoint via `this.api.get<...>(ENDPOINT)`, caches `lastComposite`, emits `store_fleet_status_changed { stampUpdated }`; `FleetStatusRenderer` subscribes and `renderFromStore()` dispatches on status (auth_required / unreachable / empty / table) with `container.replaceChildren(...)`. Off the WS transports — autonomous timer (Cheech's rule: fleet is the only poll feature). **Decide**: poll interval for the task card (propose 30-60s) vs event-driven refresh on task transitions — raise as a build decision; default to polling parity with fleet-status for v1.

## 5. Graceful degradation (María's store-down guidance)

The card MUST tolerate a stale/empty/unreachable read **gracefully**: show **last-known rows + a "store unreachable" indicator**, never blank. Mirror the fleet-status `unreachable`/`auth_required`/`empty` status branches. (The poke-side fail-safe — fail-safe vs fail-loud — is open design Q(a) for Mr. Radio's cascade; that's the spine's concern, not the card's. The card just degrades gracefully regardless.)

## 6. Acceptance criteria

- Card renders `GET /api/tasks` (locked Q(c) param set) with all §3 columns; `blocked` rows show `blocked_by` + `next_chase_ts`.
- Refresh-in-place like fleet-status (no full-page reload); "updated HH:MM:SS TZ" stamp.
- Graceful degradation on store-down / 401 / empty (§5).
- **100% lines/branches/functions** on all new TS (`c8 --100`) — Lupin-wide hard gate.
- **Playwright UI/E2E test**: card mounts, renders rows from a seeded store, shows a blocked row's `blocked_by`/`next_chase`, degrades on unreachable. Snapshots version-controlled. Runs on **:8000 (scheduled)** per venue rubric.
- HOLD commits, never push (Rick's gate). Merge = green + fresh-critical review.

## 7. Crew (approved by Mr. Radio)

- **1-2 builders**: (a) Renderer+Store+Model+Template+CSS+boot+HTML; optionally (b) splits template/model vs store/renderer if parallelizable.
- **1 fresh-critical reviewer** (reproduce-don't-trust; read-only on shared tree — no destructive git).
- **1 UI/E2E tester** (Playwright + the 100% gate).
- Manager = Tiberius. Verify-don't-trust at every checkpoint; held commits, no push.

## 8. GATE CLEARED — GO (2026-06-16, Mr. Radio's cascade)

**Verdict**: APPROVE-WITH-REVISIONS (unanimous 3/3). Cascade synthesis: `src/rnd/v0.1.8/2026.06.16-store-canonical-task-mgmt-cascade-review.md` (my lane = §F).

**Locked read-contract (Krishna Q(c)):**
- Clone the fleet-status component pattern: `FleetStatusStore.ts` (poll 60s + in-flight debounce) + `FleetStatusRenderer.ts` + `fleetStatusTable.ts` + `fleetModel.ts`; `FleetApiClient.get<T>` handles JWT/401 → `TaskListStore` / `TaskListRenderer`.
- Data path: **`GET /api/tasks?owner_persona=&status=`** (`routers/tasks.py:419-477`, full fidelity). **NOT** `/api/arbiter/fleet-state`. **No new backing API.**

**HARD CONSTRAINT — do NOT touch `routers/tasks.py`.** The `count_only=true` param + terse/projection mode (cascade §G) belong to **Mr. Radio's spine lane** (poke/model-facing). My card consumes **full rows** from the existing endpoint as-is. If the crew thinks it needs a `tasks.py` change, STOP and flag Mr. Radio via dm-mr-radio first (cross-team seam coordination).

**Crew tests** = unit 100% L/B/F on TaskListStore/Renderer/Model/Template + Playwright UI/E2E (render rows; a `blocked` row shows `blocked_by` + `next_chase_ts`; graceful degradation on unreachable/401/empty). (Cascade §J classes 1-6 are spine-focused — stop.py/migration/multi-session — not the card.)

**Status**: builder crew spinning now; runs parallel to Mr. Radio's Step 1. HELD commits, never push; report green to dm-mr-radio at the held checkpoint.
