# Next-Session Execution Plan — Heartbeat v2.1 + Held-Commit Resolution

**Author:** Tiberius 👑 (manager, session e4676369) · **Date:** 2026-06-06 (early AM EDT)
**Status:** DRAFT for Rick's review → on approval, anchors the post-/clear restart (Tiberius + María mementos point here)
**Branch:** `wip-v0.1.8` · **Push state:** tonight's 5 commits committed-but-UNPUSHED (Rick's morning push gate)

---

## 0. Where tonight left us (cold-start context)

**Committed tonight (HELD — not pushed):**

| Hash | Workstream | Owner |
|---|---|---|
| `f3cfabf` | Prediction-hook event-loop offload — /health timeout fix (FM-7) + RLock | Tiffany 💍 |
| `47db123` | Broadcast-liveness: bridge-mtime reachability filter (8h threshold) | Tiberius 👑 |
| `8702cb3` | Reap event → focus-bar badge drop + broadcast refresh (producer+consumer) | Tiberius 👑 + Sam 🎙️ |
| `ba19016` | Arbiter v2.1 §10 design fold + history/README/manifest | Tiberius 👑 + María 🌸 |
| `e6e1214` | Reap-event R&D companion doc | Tiberius 👑 |

**Held in working tree (NOT committed — would break the 100% gate):** the 5 heartbeat-strip files —
`stop.py`, `hook_common.py` (EST tz), `arbiter_gateway.py`, `run-heartbeat-arbiter.py`, `watch-hook-events.py`.
Blocker: the Stop-hook strip red-lined **~14 heartbeat unit tests**; the **flag-vs-skip test decision is PARKED**.

**Design-ready, not implemented:** Arbiter v2.1 "direct-state visibility" — §10 folded into
`src/rnd/v0.1.8/2026.06.04-heartbeat-hook/03-arbiter-design.md`, Cheech-reviewed **APPROVE-WITH-CHANGES (C1–C4)**; no code yet.

**Owed regression guard:** automated `test_reap_event_focus_bar` on :8000 (reap-event feature is live-validated but lacks a committed E2E).

**Paused:** Tiberius's Stop-hook on-behalf reports (deliberately, to stay off the /health path Tiffany was fixing — now landed).

**Live:** heartbeat-arbiter runner ticking every 60s.

---

## 1. Threads (owners · dependencies · acceptance criteria)

### Thread 0 — Morning push *(Rick gate — first thing)*
Push the 5 held commits (`f3cfabf`→`e6e1214`) to origin once Rick clears. **No AI push without explicit go.**
- **AC:** `git push` clean; branch-vs-origin shows the 5 commits landed.

### Thread A — Heartbeat strip resolution → unblock the 5 held files
- **A1 — Decision (Rick-ratified):** flag-vs-skip for the ~14 red tests.
  - **Recommendation:** **feature-flag the strip** behind an INI toggle so BOTH the old idle-waiter/"Anything else?" path AND the new poke+on-behalf-report path stay first-class and tested — per [[feedback_feature_flag_preserves_old_path]]. Skip/delete would lose the old behavior permanently. *Rick ratifies before code.*
- **A2 — Implement A1 → 100%:** wire the toggle (or chosen path), restore the ~14 tests to green + cover the new branches. 100% line+branch+function.
- **A3 — Re-enable on-behalf reports, aligned with v2.1:** sequence AFTER Thread B's direct-state model exists so we wire the clean version once, not the crude version then redo. (Soft-dep on B.)
- **A4 — Commit the 5 held files** (review-gated, selective stage, no `git add -A`).
- **Owner:** a developer (e.g. Mr. Radio 🦉 / Rachel 🕊️) → Tiberius review. **Dep:** A1 ratified.
- **AC:** 14 tests green, new branches 100%, 5 files committed, heartbeat observable end-to-end on a live Stop.

### Thread B — Arbiter v2.1 direct-state-visibility implementation
- **B1 — Fold Cheech's C1–C4 into §10** (María 🌸): C1 metadata-only `os.utime` (strike one-byte-write — bridge-JSON corruption risk, **hard gate**); C2 name the push endpoint + cred; C3 single PostToolUse hook; C4 idle-quiet keys on event-file ts, NOT bridge-mtime.
- **B2 — Develop the 3 build-lanes** (Clayton 😎): server per-MCP-call stamp lane · tool-hook touch lane · render/snapshot/`GET /api/arbiter/fleet-snapshot` endpoint lane.
- **B3 — Review + test to 100%** (Tiberius + an independent reviewer, e.g. Krishna 🦚).
- **B4 — Commit** (review-gated). **Dep:** B1 → B2 → B3 → B4 strictly sequential (PIP-style, per [[feedback_pip_plan_review_is_sequential]]).
- **AC:** workers self-report state via the one-clock bridge-mtime model; fleet-snapshot endpoint live; broadcast-list free-win verified; 100%.

### Thread C — Reap-event regression guard (:8000)
- Author + schedule `test_reap_event_focus_bar` (Krishna 🦚): producer emits `session_reaped` → consumer drops badge + refreshes broadcast card. **:8000 monopolize-mode — submit via `/api/test-suite/submit` with a Rick-confirmed slot** ([[feedback_test_server_monopolize_mode]]).
- **AC:** E2E green on a scheduled :8000 slot; committed as the standing regression guard.

### Thread D — Broadcast-liveness ↔ v2.1 convergence check
- Verify tonight's bridge-mtime broadcast filter (`47db123`) aligns with B's one-clock direct-state model (they should share the same mtime signal). Reconcile the 8h threshold against v2.1's cadence. Small, do during B3.
- **AC:** one documented liveness clock, no parallel/competing signals.

---

## 2. Sequencing & parallelism

```
Rick push (Thread 0) ──┐
                       ├─ (independent of code threads)
A1 decision (Rick) ─→ A2 ─────────────→ A3 (waits on B) ─→ A4 commit
B1 (María) ─→ B2 (Clayton) ─→ B3 (review+D) ─→ B4 commit
C (Krishna, scheduled :8000) ── parallel, slot-gated
```
- **A2 and B1/B2 run in parallel** (different owners, different files).
- **A3 is the one true cross-thread dep** (re-enable reports after B's model lands).
- **C is independent**, gated only on a clean :8000 slot.

## 3. Fleet roster (proposed — fresh spawns after /clear)
Tiberius 👑 manager · María 🌸 (B1 design fold + cross-repo LoC) · Clayton 😎 (B2 dev) · Krishna 🦚 (B3 review + C E2E) · a developer for Thread A (Mr. Radio 🦉 or Rachel 🕊️) · Cheech 🌿 on standby for design re-review. Reap idle workers with mementos per [[feedback_standing_permission_spawn_personas]].

## 4. Global guardrails (carry across the /clear)
- **100% line+branch+function** on all touched code — hard gate ([[feedback_100pct_coverage_multiplexer]]).
- **No commit/push without explicit Rick go** ([[feedback_never_auto_commit_push]]); selective staging only, never `git add -A` ([[feedback_verify_staging_before_commit]]).
- **Review-gated:** every code thread gets an independent reviewer + Tiberius backstop before commit.
- **:7999 for AI-discretionary fast tests; :8000 scheduled-only** ([[feedback_test_server_monopolize_mode]]).
- **Tiffany is NOT the tester** — AI owns the full pyramid.

## 5. Definition of done (this plan)
All of: Thread 0 pushed · Thread A 14 tests green + 5 files committed · Thread B v2.1 implemented + reviewed + committed · Thread C E2E scheduled+green+committed · Thread D one-clock reconciliation documented. Then a fresh session-end (history + LoC delta + backup).

## 6. Open questions for Rick
1. **A1:** confirm feature-flag (recommended) vs skip for the heartbeat strip tests?
2. **Scope for the fresh session:** all four threads, or prioritize A+B (heartbeat completion) and defer C/D?
3. **Push timing:** push tonight's 5 commits at session-start, or after Thread A/B also land (one bigger push)?
