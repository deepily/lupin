# Status Snapshot — Top-5 Pending TODO + GCP Migration Audit

**Date**: 2026-06-16
**Author**: Tiberius 👑 (session 570d9733)
**For**: Rick (TTS session)

---

## Part 1 — Top 5 Pending Items (descending importance)

Source: the freshest pending block in `TODO.md` (2026-06-16, Mr. Radio's session-end) — the **cosa-voice token-reduction engagement**.

| # | Priority | Item | What it means |
|---|----------|------|---------------|
| 1 | 🔴 URGENT | **12 integration test failures** from Phase-4 commons-DM comment-out (`5ce5dba5`) | `test_commons_ask_async_push_integration.py` ×7 + `test_dm_integration_live.py` ×5. Classify each: retired-path assertion → skip/update citing the commit, vs. real break → fix. **Only item violating a hard gate** (100%-pass / test-ownership). Selective-stage those files only. |
| 2 | 🟠 | **Task-store adoption gaps** (Krishna inventory `5b5e5c08`) | Gap A discoverability (recommend close-now); gaps E chase-flag + F write-scope are **Rick decisions**. Fleet-auditability — the work-owed oracle + manager-tick read the store. |
| 3 | 🟡 | **commons polling-tool audit** | Now that directed DMs use `dm_send`, are `commons_ask_async`/`ask_sync` polling-mode (+ `commons_read` cost) still used anywhere? Retire if dead → token win (cosa-voice token efficiency is the named restart priority). |
| 4 | 🟡 | **`/api/notify-peer` → `dm_send`-aligned rename** | One-name doctrine, e.g. `/api/dm-send`. Discuss + execute. Low stakes. |
| 5 | ⚪ | **Full-removal pass** of legacy commons-DM machinery | After cutover soak + zero-hit telemetry. Includes `heartbeat_poker`/`cascade_heartbeat_scheduler` still referencing the now-404 `register-question`. Last because it gates on soak time. |

---

## Part 2 — GCP Migration Audit (worker Rio ⚡, observer; git-verified by Tiberius)

### 🎯 HEADLINE: GCP-M2 cloud-test is **SHIPPED + VALIDATED + PUSHED**. Not pending.

The resume memento this audit was launched against (`io/mementos/tiberius-resume-2026.06.15.md`) was the **06-15 MORNING** state ("4 Rick gates open") — **STALE**. Session 895f0a7b superseded it that evening. **All 4 gates cleared.**

**Git verification (Tiberius, 2026-06-16):**
- HEAD is **0 behind / 6 ahead** of origin/wip.
- GCP "VALIDATED" commit `69893db0` → **on origin**. ✓
- Frontier `7178ab5d` → **on origin** (the origin tip). ✓
- The 6 unpushed HEAD commits are **all arbiter/heartbeat/task_create work — zero GCP** (await Rick's push gate, separate concern).

**What's DONE / deployed** (authoritative commit `69893db0`):
- Keys SCP (hit + documented the "1001-staging" gotcha)
- `lupin-model-server` healthy, 3 models on cuda:0 (the 503 was just empty `src/conf/keys/`)
- `claude /login` OAuth proven (`claude -p` → "CLOUD OAUTH OK"), persisted via `claude-creds` volume (`b524c15d`)
- D1 OAuth-precedence constraint handled (`cloud-test.env` exports no non-empty `CLAUDE_CODE_OAUTH_TOKEN`)
- **Phase G GREEN end-to-end** incl. Cloud-SQL round-trip (17 tables) via the IAP tunnel → `localhost:6999`
- `is_protected` model↔migration drift fixed (migration `d4e5f6a7b8c9`, idempotent)
- Reproducible auto-migrate landed (env.py single-source URL + programmatic `alembic upgrade head` + parity check); 43 tests, 100% cov
- 2 new runbooks under `src/rnd/v0.1.8/2026.05.30-gcp-deployment/2026.06.15-*`

### What's LEFT (none M2-blocking — prod-prep / tech-debt, all Rick-decision)
1. **FCM wake-sends** disabled until image rebuild + service-account JSON.
2. **Secure Boot** re-enforce + signed-driver path (org-admin tech-debt).
3. **History purge** of the rotated OpenAI key via git-filter-repo (hygiene; rotation already moots it).
4. **Sourceless secrets** — `lupin-jwt-secret-key`, `lupin-smtp-username/password` have no file in `src/conf/keys` (needed for prod Secret-Manager path).
5. **VM schedule** — runs 24/7 now (out-of-band gcloud); auto-off **Fri 2026-06-19 23:00 EDT**. Decision: revert to overnight-pause (cost) or stay 24/7?
6. **PRODUCTION deployment** (vs cloud-TEST) — a future milestone, not started.

### Open decision for Rick
After Friday's scheduled stop, does the VM revert to overnight-pause (cost model) or stay 24/7?

---

## Part 3 — Multiplexer Migration Audit (worker Cheech 🌿, observer; read-only)

> **Disambiguation**: this is the **notifications-UI → multiplexer** cutover — a DIFFERENT cutover from the cosa-voice notification-native (crew → `dm_send`) cutover that landed 06-16. Don't conflate.

### 🎯 HEADLINE: Parity is **DONE**; the cutover is **one INI line + a :8000 bounce away — and has simply never been flipped** (waiting on Rick's word since 06-14).

**DONE (verified in code):**
- Parity sprint Lanes A–E / WP0–WP15 ALL landed (2026-06-10 merge-train). Flagless e2e gate 560/3/1/3 green; mux c8 1108/1108.
- Cutover mechanics BUILT + held INACTIVE: INI-keyed 302 redirect in `pages.py:102-104` (gated on flag), `?classic=1` escape hatch, commit `baa7e7e7` (built flag-OFF), pages.py 100% L/B.
- 153 legacy e2e nav sites patched → `?classic=1`, proven inert.
- 6 of 7 `test_multiplexer_*` e2e counterparts built (`ts-2c5e5deb` 64/0/1).
- External-notification cold-load hydration gap CLOSED (`47aa72f3`).
- D2 ruling: cutover ≠ deletion — JS client + suites + `?classic=1` retained LONG-TERM as JS-vs-TS research corpus.

**NOT DONE — never flipped:**
- 🔴 INI `legacy notifications redirect enabled = False` STILL FALSE (`lupin-app.ini:803`); git -S shows only `baa7e7e7` (built-OFF), no flip commit.
- 🔴 `landing.html:116` still → `/app/notifications`; `/app/multiplexer` is NOT the default.
- WHY it slipped Saturday 06-14: Max-plan weekly cap hit 06-12 → delegation offline; fleet pivoted to cosa-voice token-reduction + task-store + GCP-M2. Cutover was GO-READY but deferred; still on Rick's open-gates list.

**WHAT'S LEFT:**
1. **[RICK-GATED]** The GO flip itself: 1-line INI `True` + :8000 bounce. Everything else is GO-READY behind it.
2. **[SELF]** Verify the WP6 STT caret-splice port actually merged (Cheech couldn't locate the commit).
3. **[SELF, at flip]** Docs ride-along (notification-api.md, rest-api-reference.md Pages, websocket-events.md, CLAUDE.md) + `landing.html:116` repoint.
4. **[SELF, post-soak]** `?v=`/hashed-`boot.js` cache-bust hardening (`multiplexer.html:237` loads unhashed boot.js).
5. **[RICK-DECISION, non-blocking]** JS-vs-TS research-corpus study methodology.

**Open question / blocker**: the flag-ON path has no live E2E (can't test without flipping the shared INI = the gated action); mitigated by a TestClient HTTP-layer unit test; flip-day = a 30s probe.

---

## Part 4 — `/api/notify-peer` Rename — DECISION LOGGED (Rick, 2026-06-16)

**Decision**: rename to a **nested verb namespace** — `/api/dm/send`, under a dedicated `APIRouter(prefix="/api/dm", tags=["dm"])`, scaffolded for future siblings `/api/dm/respond | /get | /list` that will mirror future `dm_<verb>` MCP tools 1:1. Hard rename, **no alias/shim** (one-name doctrine; "break everything, do it right"). `build_notify_peer_payload` → `build_dm_send_payload` in the same sweep.

**Rationale (Rick's expansionist instinct)**: a short `dm` base namespace beats a flat single-purpose endpoint — one router with shared auth dep + one `/docs` group, trivial sibling addition, and deepest verb-symmetry across the MCP↔REST boundary.

**Status**: DELEGATED to builder `cc-author-tiberius-1` — hard rename across notifications.py → new `routers/dm.py`, arbiter_live_notify.py, app.py, MCP, all tests (zero `notify-peer`/`notify_peer` survivors), docs. Held + fresh-critical review before land; push is Rick's gate.

---

## Part 5 — Live Crew Status (2026-06-16)

| Item | Worker | State |
|---|---|---|
| 12 integration failures (5ce5dba5) | Rio ⚡ (reaped) | ✅ FIXED — held `731db4d1`; all 12 retired-path, file-skipped + deprecation banner; :8000 receipt `ts-72562f46` (12 skipped, all_passed). **In fresh-critical review** (`cc-reviewer-tiberius-1`). |
| `/api/notify-peer` → `/api/dm/send` rename | `cc-author-tiberius-1` | 🔨 BUILDING (held + review before land) |
| Multiplexer status audit | Cheech 🌿 (reaped) | ✅ DELIVERED (Part 3 above) |

All work HELD on `wip-v0.1.8` — nothing pushed; push is Rick's gate.
