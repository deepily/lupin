# The orphan tick is BUILT and NOT INSTALLED — the record, so the gap is a decision and not a silence

**Author**: Clayton 😎 · **Date**: 2026-09-06 ~16:35 EDT · **Status**: open, awaiting one action
**Standard applied**: Cheech 🌿 — *silence is not consent*. This file exists so that "not installed"
is something a person chose, on a date, with the cost written down — rather than something that
merely never happened.

---

## 1. The state, in one line

Rick ratified **option B** by real keypress at ~16:10 EDT. The tick is written, tested, mutation-proven
and committed. **The crontab line that makes it run does not exist.** Nothing is scheduled.

⇒ **Option B is BUILT AND NOT RUNNING**, and that is the same shape as the defect this whole line of
work exists to close — see §3.

---

## 2. The one action outstanding

```
0,15 10-12 * * * /mnt/DATA01/include/www.deepily.ai/projects/lupin/src/scripts/orphan-sweep-tick.sh >> /home/rruiz/.lupin/logs/orphan-sweep-tick.log 2>&1 # orphan-sweep-tick
```

**Two properties of that line, both measured rather than chosen by taste:**

- **It runs from the MAIN CHECKOUT, not from a worktree.** The tick must survive the seat that wrote
  it being reaped. It resolves `LUPIN_ROOT` and the sweep path explicitly rather than deriving them
  from `$repo_root` — that derivation is cause 2 below, and inheriting it would reproduce the silent
  skip one level down.
- **`10-12`, not `10-13`.** The sweep takes ~4 minutes against ~200 worktrees, and CLAUDE.md's
  off-peak window closes at 13:00. Re-derived rather than quoted: `journalctl --list-boots` gives
  **107 boots, 90 in the morning band, median 09:35:51, earliest 07:17, latest 12:54, 26 of 90 after
  10:00** — which matches the file's own 91-boot figure to seconds.

**Who may install it**: not me on standing authority. A crontab entry is persistent, fleet-visible,
and outlives every session on this box. It is one command once somebody says so.

---

## 3. Why "committed" must not be read as "delivered"

This is the uncomfortable part and it is the reason the file exists.

| artifact | state | consequence |
|---|---|---|
| `orphaned_head_sweep.py` (María, 2026-09-05) | correct, tested, **unscheduled** | never fired in lupin |
| `orphan-sweep-tick.sh` (this work, 2026-09-06) | correct, tested, **uninstalled** | does not fire either |

⇒ **A tool that is correct and unscheduled, and a tool that is correct and uninstalled, are the same
failure.** We were sent to close the first. Leaving the second open would close nothing — it would
move the gap one file to the right and give it a fresher date.

⚠️ **And the honest form of this is not "it will be fine".** Measured on 2026-09-06: category (i) —
the loss-bearing one — went from **0 of 98 at 13:41** to **6 of 97 at 16:15**, six merge commits into
detached HEADs in Tiberius's worktrees. **Inside two hours, on a day somebody was watching.** The
window in which nothing is scheduled is not a quiet window; it is an unobserved one.

---

## 4. What is NOT claimed here

- **Not that installing it is urgent enough to bypass anyone.** It is not. The point is that the
  decision be *made* rather than defaulted into.
- **Not that the tick is the whole fix.** It closes cause 1 (nothing schedules the sweep) and routes
  around cause 2 (session-end resolves the sweep at `$repo_root/workflow/scripts/`, which does not
  exist in lupin, so the ritual takes a silent skip). **Cause 2 is untouched**: a seat running
  session-end in lupin still gets the skip, and that edit lives in the separately-managed
  planning-is-prompting repo.
- **Not that my measurement of six orphans still holds.** It was true at 16:15. Re-derive it; the
  same figure was 0 two hours earlier, and that is the whole lesson of §3.

---

## 5. The three open decisions, and whose they are

| # | decision | owner |
|---|---|---|
| 1 | install the crontab line above | **Cheech**, or Rick |
| 2 | fix cause 2 — a `$PLANNING_IS_PROMPTING_ROOT` fallback in `session-end.md` §8.2, **versus** installing the workflow scripts into lupin. Different blast radii; the second changes what every repo installing the workflow inherits | **the planning-is-prompting owner** |
| 3 | rescue Tiberius's six orphaned heads — one `git branch rescued/…` each, printed by the report | **Tiberius**, or Cheech |

**None of the three is mine**, and none of them is blocked on further work from me.

---

## 6. Provenance

- Option B ruled by **Rick**, real keypress, 2026-09-06 ~16:10 EDT.
- Category (ii) must never be an alert — **María 🌸**, 2026-09-05, having built the alert version,
  measured it (23 of 31 branches fired) and killed it. Guarded by
  `test_the_tick_never_delivers_category_ii`.
- The **silence-is-not-consent** standard that produced this file — **Cheech 🌿**, 2026-09-06.
- Both review defects (the `&&` interpreter guard; the arms living in a DM) — **Cheech 🌿**,
  reproduced independently at baseline sha `ac908df13383`, matching mine.
