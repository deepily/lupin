# Live tickets, triaged and reordered — María 🌸 + Mr. Radio 🦉, 2026-09-16

**Board read**: 2026-09-16 ~14:58 EDT. **Scope**: every live ticket (in progress, queued, blocked, parked) on our two boards plus Tiffany's. Held tickets are further down.
**Everything here is a suggestion.** Rick does the admits, drops and priority changes.
**Sources**: the two blind halves, `2026.09.16-triage-maria.md` and `2026.09.16-triage-mr-radio.md`, compared, with the differences settled by DM.

## Live tickets, in suggested working order

| # | Ticket | Status | Now | Suggest | Goal | Why |
|---|---|---|---|---|---|---|
| 1 | `588c8dc9` Phone sends the user's ID where the server expects an email | **done 15:27** (Tiffany, local commit `a2cde69`, not pushed) | P0 | — | Mobile | The phone now sends the email for the reload and push-token calls. Rio's `d90baf3d` can land once Rick's phone runs a build with `a2cde69` |
| 2 | `645a7da5` Multiplexer parity build | in progress (Mr. Radio) | P0 | **Keep P0** | Multiplexer | Plan decisions reviewed by María and committed (`703525af`); only §6 item 6 is still open |
| 3 | `0f4728bc` Five things needed before Phase A | queued, **offered in a trade** | P1 | **Close as done**, receipt `703525af` | Multiplexer | Every item was decided today inside the build plan. There's no "close into another ticket" feature; this is an ordinary close |
| 4 | `f0e00f01` 19 failing browser tests | in progress (Mr. Radio) | P1 | **Keep P1** | Both | Blocks every merge. Updating the screenshots needs six verdicts from Rick |
| 5 | `8d0c7c83` Three failing browser tests nobody owns | queued | P2 | **Fold under `f0e00f01`** | Both | Same merge gate |
| 6 | `e453a854` Screenshot differs across two loads | blocked | P1 | **P2, fold under `f0e00f01`** | Both | Cause found (a debug log stamps the time); what's left is a screenshot fix |
| 7 | `9b1f7701` Recording-format accuracy check | parked to 09-17 10:00 EDT | P1 | **Keep P1. Ready to unpark on Rick's word** | Mobile | Parked because emulator recording was broken. That's fixed (`4be8fe63` done, Tiffany's `225bc8c`), and the Whisper fix `05ddc8f0` is live |
| 8 | `c51e92da` Phone round-trip test | parked to 09-17 10:00 EDT | P2 | **Keep P2. Ready to unpark on Rick's word** | Mobile | Needs Rick's phone on wifi. The mobile-data half needs an address reachable from outside the house |
| 9 | `edba76c6` Time slot for the phone session | parked to 09-17 10:00 EDT | P2 | **Keep P2. Ready to unpark on Rick's word** | Mobile | The session that runs #7 and #8 |
| 10 | `5ac999f5` 46 failing Flutter tests | queued | P3 | **Raise to P2** | Mobile | A new failure can hide among 46 known ones |
| 11 | `b5035039` Old seat can `/clear` a busy pane | blocked | P1 | **Lower to P2** | Neither | Guard merged; only an outside check remains |
| 12 | `6698d40f` Heartbeat poke tells agents to hand-write hold files | queued | P1 | **Lower to P2** | Neither | The hold tool already works |
| 13 | `fb27b166` Classic page's Retry cuts the question to 100 characters | queued | P1 | **Lower to P3** | Neither | Classic page only, and it goes away when the multiplexer is done; the multiplexer doesn't have the bug |
| 14 | `14761ef1` Worktree guard trial week | queued | P2 | **Lower to P3** | Neither | Planning repo; runs on its own |
| 15 | `158a9865` Five SSE smoke tests that can't fail | queued | P3 | **Keep P3** | Neither | |

**Done today**: `05ddc8f0` Whisper fix (commit `abd47c5f`, live on model server 0.1.1) · `e5ebc815` reload button ruling (closed on Rick's answer).

## Held tickets worth admitting

| Ticket | Suggest | Goal |
|---|---|---|
| `0e5bfa0e` Timezone setting reaches neither client | **Admit at P2** | Multiplexer |
| `d90baf3d` History routes don't check the email is yours | **Admit at P2**, land once Rick's phone runs a build with `a2cde69` (`588c8dc9`, done) | Mobile |
| `1f79c04f` Chevron keyboard test | **Admit at P3** | Multiplexer |
| `12457b56` Restore the heartbeat Stop poke at 17:00 | **Approve** | Rick's order for today |
| `966b46ba` Promotion gate at 97% coverage | **Admit at P2** | Neither |
| `8d043758` DOM-assert lint blind spots | **Admit at P3**, with `9f54712b` merged in | Neither |
| `081dac6d` Shorter holding-area line on the task list | **Admit at P3** | Neither |
| `89be3908`, `b2fb103c`, `587b42a9`, `9ab0bddb` | **Admit at P4** | Neither |

**Closed with receipts (15:30, Mr. Radio)**: `104d62ae`, `1ebb712e`, `6bf8a819`.
**Drop as done, still open**: `e4db0e68` (owned by Tiberius), `168922f9`, `65e7dca4`.

## Trades filed (sword of Damocles, 15:28–15:30), all waiting for Rick's approval

Approving an admit also drops the ticket offered for it. Rule 2 allows a ticket **subsumed by another** as the price; a **finished** one doesn't count.

| Admit | Filed by | Ticket offered |
|---|---|---|
| `081dac6d` shorter holding-area line | María | `73fdc7ef`, covered by `f0e00f01` F4 |
| `9ab0bddb` doc viewer path check | María | `4163a015`, low-value, never reached |
| `1f79c04f`, `89be3908`, `b2fb103c`, `0e5bfa0e`, `d90baf3d`, `966b46ba`, `8d043758` | Mr. Radio | Each pledge is one of `9f54712b`, `0f4728bc`, `8d0c7c83`, `e453a854` or another of his tickets. The four named are covered by other rows; exact pairs are on each request |

⚠️ **One pledge to look at**: `0f4728bc` isn't really covered by another row; its work is finished (all five items decided in `703525af`). Under rule 2 that makes it a close, not a price. **Already moot**: Rick approved `1f79c04f` with it as the pledge, so `0f4728bc` is dropped.

**Status at 15:32** (per Mr. Radio): **approved** `1f79c04f`, `d90baf3d`, `966b46ba`, `8d043758` · **pending** `0e5bfa0e`, `89be3908`, `b2fb103c`, `081dac6d`, `9ab0bddb`.
**Approval keeps the old priority**, so the suggested priorities still need Rick's edit: `1f79c04f` P5 → **P3**, `8d043758` P2 → **P3**. `d90baf3d` and `966b46ba` are already at the suggested P2.

**Not filed**: `587b42a9` (no ticket left worth offering for a P4) and `12457b56` (Mr. Radio restores the poke at 17:00 and closes it).
**Merge**: `3202a3ed` into `89be3908`. `73fdc7ef` folds under `f0e00f01` as a separate failure.
**Leave held, lower to P3**: `6444feeb` (a build is repo-global), `0bcbaead` (manifest backticks mis-name an owner).
**Leave held**: everything else. Full reasons are in the two halves.

**Also done today**: the failing DOM-assert lint check is fixed, commit `1dea0681`.
