# Session Plan — TODO Triage + Option C Docker Non-Root User

**Date**: 2026-04-13
**Session**: 23f409c8
**Prefix**: [LUPIN]
**Status**: Active — serialized from `~/.claude/plans/silly-cooking-quokka.md`
**Related design doc**: `src/rnd/v0.1.6/2026.04.13-docker-non-root-user-plan.md`

---

## Context

Session start on branch `wip-v0.1.6-2026.03.12-cjflow-upe-and-playwrite`. TODO.md is large (462 lines) and mixes priorities. This plan captures the red/yellow/green triage produced at session start so we can work against it, and sets up Option C (Docker non-root user) as the active work item.

**Item #1 status update**: Option B `chown` already run by user — unblocking backups now resolved. Removed from HIGH PRIORITY queue.

---

## 🔴 HIGH PRIORITY (Pending)

| # | Item | Notes |
|---|------|-------|
| ~~1~~ | ~~Run Option B `chown` to unblock backups~~ | ✅ **DONE by user** |
| ~~2~~ | ~~**Integrate Option C (Docker non-root user)**~~ | ✅ **DONE this session** — execution log: `2026.04.13-option-c-execution-log.md` |
| ~~3~~ | ~~Surface per-file rsync errors in `/plan-backup` script~~ | ✅ **DONE this session** — stderr→/tmp log + fail-path line count/path/tail-20 |
| 4 | CoSA submodule commits for Session 9056c113 continuation | USER-RUN from CoSA context |
| 5 | Schedule TFE Resume Live E2E tonight | test-suite submit card, monopolize=true |
| 6 | Phase D follow-ups: file-path resume + CLI flag | Mirror Presentation Generator pattern |
| 7 | Phase E follow-ups: BFE completion report + BFE checkpoint-resume | Apply agentic-voice-workflow v3.0 Phases 11-12 |
| 8 | FastAPI restart + CoSA rebuild to activate checkpoint-resume | Then verify stall → resume flow |
| 9 | FastAPI restart to pick up unified watchdogs + flipped INI defaults | Expect `[Watchdogs] BFE=ENABLED, TFE=ENABLED` |
| 10 | TFE live E2E monopolize run (real SDK + real git) | ~$15/run |
| 11 | PEFT trainer run — TFE voice routing | USER-RUN ONLY (GPU) |
| 12 | BFE Phase 6 LIVE E2E outcome check | Parallel-console state unknown |

## 🟡 TFE follow-ups (non-blocking)

- TFE Phase 0 `llm_refine` real SDK wiring (`src/cosa/agents/test_fix_expediter/cluster.py`)
- TFE `agent_registry.py` entry (currently routed via direct elif)
- Optional `FixContext` Pydantic model refactor (currently `SimpleNamespace`)

## 🟢 Older carry-overs

- Phase 11: Presentation Generator Theme Integration
- Migrate remaining 13 fetch sites to `authedFetch` (Phase 4 cleanup)
- `sdla restart lupin-rest` to propagate new pytest.ini paths into container
- Research 5-6 dead/paused jobs stuck in todo queue
- Clean up debug code in `test_suite/job.py`
- Remove `[DIAG-JR]` diagnostic logging (notifications.js + notifications.py)
- TestSuiteJob: surface stderr when subprocess crashes at startup
- Test-Suite Job — INI-driven timeouts
- Test-Suite Job — Phase 3 (conftest.py addoption consolidation)
- Interrupted job re-enqueue mechanism at startup
- Run Opus through test-suite endpoint (~$2.43)
- TestSuiteJob manual + automated testing remaining items
- Automated E2E testing workflow (standard pattern for scheduled monopolized runs)
- Full E2E re-run to confirm 2 visual regression errors are pre-existing

---

## Active Work: Option C — Docker Non-Root User

**Design doc** (already authored): `src/rnd/v0.1.6/2026.04.13-docker-non-root-user-plan.md`
**Execution log to create on completion**: `src/rnd/v0.1.6/2026.04.13-option-c-execution-log.md` (per BFE doc-pairing convention)

### Reconciliation with in-progress refactor

In-progress compose refactor is purely cosmetic: `lupin-postgres-dev` → `lupin-postgres` container rename across 7 files. **No conflict** with Option C — orthogonal changes. Python code audit is clean: all uses `Path.home()` / `expanduser("~")`, no hardcoded `/root/`.

**One extra hardcoded path found** not in the original design doc:
- `src/scripts/run-fastapi-lupin-test.sh:21` — `[ -f /root/.lupin/test-env.sh ] && source /root/.lupin/test-env.sh` → must become `/home/rruiz/.lupin/test-env.sh`

### Concrete edit list

**A. `docker/lupin/Dockerfile`**
- Insert near top (after base image): `ARG LUPIN_UID=1001` / `ARG LUPIN_GID=1001` / `groupadd` + `useradd -m` / `ENV HOME=/home/rruiz`
- Line 131: `ENV PATH="/root/.local/bin:${PATH}"` → `/home/rruiz/.local/bin`
- Line 228: `RUN mkdir -p /root/.claude` → `/home/rruiz/.claude` + `chown -R rruiz:rruiz /home/rruiz`
- Lines 231-232: `COPY` targets `/root/.claude/*` → `/home/rruiz/.claude/*`, add `--chown=rruiz:rruiz`
- d2 install (line 130 area): ensure runs under `HOME=/home/rruiz` so `.local/bin` lands correctly
- End of file, before `CMD`: `USER rruiz`

**B. `docker-compose.yml`**
- `lupin-rest-dev`: add `user: "1001:1001"`; repoint mounts (lines 43-45):
  - `~/.lora_env:/root/.lora_env:ro` → `/home/rruiz/.lora_env:ro`
  - `~/.lupin:/root/.lupin:ro` → `/home/rruiz/.lupin:ro`
  - `~/.claude/.credentials.json:/root/.claude/.credentials.json:ro` → `/home/rruiz/.claude/.credentials.json:ro`
- `lupin-rest-test`: add `user: "1001:1001"`; repoint `~/.lora_env` mount (line 87)
- Leave `postgres` service alone (uses own `postgres` user)

**C. `src/scripts/run-fastapi-lupin-test.sh`**
- Line 21: `/root/.lupin/test-env.sh` → `/home/rruiz/.lupin/test-env.sh`

### Side-effects to verify (from design doc §"Side effects")

1. `find src/ io/ -user root` → expect empty after Option B ran (user confirmed done)
2. `seed_test_companions.py` writes as UID 1001 OK
3. Claude MCP credentials mount survives rebuild
4. GPU access: `/dev/nvidia*` still usable as UID 1001 (Nvidia runtime usually user-agnostic)
5. `gh` CLI auth dir — only relevant if BFE/TFE calls `gh` from container; defer unless it breaks

### Verification sequence

```bash
# Build + bounce
docker compose build lupin-rest-dev lupin-rest-test
docker compose down && docker compose up -d

# Verify user
docker exec lupin-rest-dev id                        # expect uid=1001(rruiz)
docker exec lupin-rest-test id                       # expect uid=1001(rruiz)

# Verify writes land as rruiz on host
ls -la src/conf/long-term-memory/lupin.lancedb/ | head

# Verify backup succeeds
/plan-backup --write                                 # expect exit 0
find src/ io/ -user root                             # expect empty
```

### Critical files

- `docker/lupin/Dockerfile`
- `docker-compose.yml`
- `src/scripts/run-fastapi-lupin-test.sh` (newly identified)
- `src/rnd/v0.1.6/2026.04.13-docker-non-root-user-plan.md` (design doc)
- `src/rnd/v0.1.6/2026.04.13-option-c-execution-log.md` (execution log, to create)

### Execution order

1. ✅ **Step 0**: Serialize this plan to `src/rnd/v0.1.6/` + update `src/rnd/README.md`
2. **Gate**: `ask_yes_no()` confirm before code edits
3. **Phase 1**: Edit Dockerfile, docker-compose.yml, run-fastapi-lupin-test.sh
4. **Phase 2**: `docker compose build` (user runs)
5. **Phase 3**: Bounce + verify (user runs)
6. **Phase 4**: Create execution log with actual results; close TODO.md item #2

### Risks / open questions

- **d2 CLI install path**: installer respects `$HOME`; fallback is install-as-root-then-chown.
- **Rebuild time**: full image rebuild may be 5-15 min.
- **Scope bundling**: Option C as **separate commit** from ongoing postgres-rename refactor (easier bisect).
