# Container Preflight Smoke — Execution Log

**Session**: be57a252
**Paired design doc**: `2026.04.18-container-preflight-smoke.md`

---

## Step 0 — Session resume + diagnosis (complete)

**2026-04-18T13:27:00** — Session start. Read history.md + TODO.md + prior Bug 9 / 9a R&D docs. Confirmed Bug 9 + Bug 9a are code-landed (commits `5817533`, `aed7c6d`, `2a97532`).

**~13:35:00** — User reported Resume of `tfe-72adc928` died. Pulled failed report from test container:

- `/var/lupin/io/swe-team/reports/interactive.job.tester@lupin.deepily.ai/2026.04.18-at-13:06-EST-ts-d3df4d87-failed-test_fix_expediter-report.md`
- Actual job ID: `tfe-93fb4cf2`
- Failure: `git worktree add /var/lupin/.claude/worktrees/tfe-93fb4cf2 HEAD` → `fatal: not a git repository (or any of the parent directories): .git`
- Duration: 21.2s

**Container state at diagnosis time** (2026-04-18T17:06 UTC):

- `lupin-rest-test` up 31 min; started 16:55:53 UTC.
- `docker inspect` mounts: `pytest.ini`, `src`, `io` only. **No `.git`.**
- `docker exec lupin-rest-test ls /var/lupin/.git` → not found.
- `docker exec lupin-rest-test git rev-parse --is-inside-work-tree` → fatal.

**Conclusion**: running container predates Bug 9a compose edit; a `docker restart` preserved old mount set. This is the precipitating event for the preflight-smoke tier.

## Step 1 — Plan approval (complete)

**~13:40:00** — Plan mode. Wrote `~/.claude/plans/let-s-start-a-new-structured-moonbeam.md`. User via cosa-voice multiple-choice chose "Preflight test + recreate (Recommended)".

## Step 2 — R&D docs (complete)

**~13:50:00** — Design doc + this execution log serialized. Per DOCUMENTATION-FIRST PROTOCOL, docs precede code.

## Step 3 — Preflight script (complete)

**~14:05** — `src/scripts/preflight-test-container.sh` written, `chmod +x`, smoke-tested against currently-drifted container:
- Exit code: 1 (correct)
- Probes 1: OK (container running)
- Probe 2: FAIL (.git not in inspect mount list)
- Probe 3: FAIL (git rev-parse → fatal)
- Probe 4: FAIL (git worktree add → fatal)
- Probe 5: FAIL (credentials file missing)
- Probe 6: WARN (gh auth not logged in)
- Every failure emits its explicit `docker rm -f` + `compose up -d` remedy.

## Step 4 — Preflight pytest smoke (complete)

**~14:10** — `src/tests/smoke/test_container_preflight.py` written; 5 blocking + 1 WARN probe mirroring the shell script 1:1. `py_compile` green. Run against drifted container:
- `test_container_is_running` — PASSED
- `test_git_mount_present_in_inspect` — FAILED (as expected)
- `test_git_sees_work_tree` — FAILED (as expected)
- `test_git_worktree_add_functional` — FAILED (as expected)
- `test_claude_credentials_mount_present` — FAILED (as expected)
- `test_gh_auth_status_warn_only` — SKIPPED (WARN via skip — expected)
- Summary: `4 failed, 1 passed, 1 skipped in 0.46s`

Each failure includes the exact remedy line in the assertion message.

## Step 5 — Workflow wiring (complete)

**~14:15** — One-liners added:
- `CLAUDE.md` → RUNNING/TESTING FASTAPI APPLICATIONS section: "Before clicking Resume on any TFE/BFE stalled job, or before scheduling a live E2E run on `:8000`, run `src/scripts/preflight-test-container.sh`..."
- `.claude/skills/testing-patterns/SKILL.md` → Added Container Preflight row to Test Tiers Overview table.

## Step 6 — User recreates container (complete)

**~17:43:55 UTC** — User ran `docker rm -f lupin-rest-test && docker compose up -d lupin-rest-test`. `/health` returned 200 within seconds.

## Step 7 — Verify preflight green on fresh container (complete)

- `src/scripts/preflight-test-container.sh` → exit 0. 5 `[OK]` + 1 `[WARN]` (gh auth — Phase 5 future-proof marker, non-blocking).
- `pytest src/tests/smoke/test_container_preflight.py -v` → `5 passed, 1 skipped in 0.70s`.

## Step 8 — Deliberate regression test (skipped — intentional)

The initial run against the drifted pre-recreate container (4 blocking fails + 1 WARN, clear remedies) already demonstrated correctness. Cycling the container again purely to re-verify would disrupt the user's Resume flow for no additional signal. Skipping as redundant.

## Step 8a — Phase 5 gh auth (proactive, 2026-04-18T18:15)

User requested fixing the preflight's gh WARN now, knowing the current run would fail at Phase 5 without it. Discovered:

- Host has **no** `~/.config/gh/` directory. `gh` auth on host is via `GH_TOKEN` env var (scope-limited: "missing read:org", but sufficient for personal `gh pr create`).
- Bind-mount approach invalid; pass env var instead.

**Edit**: `docker-compose.yml` — added `GH_TOKEN: ${GH_TOKEN:-}` to `lupin-rest-dev` (line 61) + `lupin-rest-test` (line 117). Pattern: optional (empty default), so compose-up never fails if operator lacks a token.

**No container bounce performed** (user explicit: "NO BOUNCING"). The edit applies only on next recreate. Preflight probe 6 will flip from WARN to OK post-recreate if `GH_TOKEN` is in the shell env at `docker compose up -d` time.

## Step 9 — TFE Resume observation (pending — user action)

- Click Resume on the stalled TFE row (whichever is current post-restart).
- Re-select proposals at voice gate.
- Observe `docker exec lupin-rest-test ls /var/lupin/.claude/worktrees/` populate.
- Confirm Phase 3 applies K > 0 fixes inside the worktree.
- If Phase 5 trips on `gh` auth: escalate to follow-up mount decision.

## Deferred / follow-ups

- `~/.config/gh` mount — only if Phase 5 actually asks for it. Until then, probe 6 stays WARN-only.
- CoSA submodule commits (yours — outside this session's scope per Lupin-only memory).

## Files changed this session

(updated live as work proceeds)

- `.claude-session.md` — manifest section added.
- `src/rnd/v0.1.6/2026.04.18-container-preflight-smoke.md` — design doc.
- `src/rnd/v0.1.6/2026.04.18-container-preflight-smoke-execution.md` — this log.
- `src/scripts/preflight-test-container.sh` — created, chmod +x, verified FAIL against drifted container.
- `src/tests/smoke/test_container_preflight.py` — created, py_compile green, 5 blocking + 1 WARN behave correctly against drifted container.
- `CLAUDE.md` — one-liner added to RUNNING/TESTING section.
- `.claude/skills/testing-patterns/SKILL.md` — Container Preflight row added to tier table.
