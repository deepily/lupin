# Plan: Presentation Generator Phase D Manual Test + E2E Test Suite Status

**Serialized from**: `~/.claude/plans/stateful-splashing-crayon.md`
**Date**: 2026-03-26
**Session**: Planning session (pre-Phase D)

## Context

**Phase D** = live (non-dry-run) Presentation Generator through the CJ Flow queue with real Claude API calls and voice gate interaction. Phase C (dry-run) completed Session 374 with 6/6 smoke tests passing.

**E2E collision question resolved**: The hot-swap collision (Session 372c: 23 `RuntimeError: Server database is NOT lupin_db_test!`) was NOT caused by Phase D (which hasn't been attempted). It was two concurrent `run-e2e-ui-tests.sh` runs overlapping. Session 377 fixed this with `--bg` flag + PID-file overlap guard. E2E suite is now healthy: 297 passed, 0 failed.

---

## Workstream A: Phase D Manual Verification (Browser-Based)

Phase D = live (non-dry-run) Presentation Generator through the CJ Flow queue with real Claude API calls and voice gate interaction.

### Prerequisites
- Server running on port 7999 in normal Development mode
- Logged into Lupin UI in browser
- Server console visible (to watch phase breadcrumbs)

### Manual Checklist

| Step | Action | What to Watch |
|------|--------|---------------|
| 1 | Open browser → `http://localhost:7999/app/notifications` | Page loads |
| 2 | Expand the Presentation Generator agentic card | Card with source path, audience, duration, dry-run fields |
| 3 | Fill: Source = `/src/rnd/2026.03.14-presentation-generator/01-strategy-and-design.md`, Audience = general, Duration = 15 | Fields populated |
| 4 | **UNCHECK** "Dry run" checkbox | Critical — this is what makes it Phase D |
| 5 | Click "Generate Presentation" | Status shows `pr-XXXXXX` job ID + queue position |
| 6 | Phase 1 (Ingest) — automatic | Server console: "Phase 1: Ingesting..." |
| 7 | Phase 2 (Analyze) — **real Claude API call** | Server console: "Phase 2: Analyzing..." (10-30s) |
| 8 | **Gate 1: Narrative Arc Review** — voice prompt fires | Approve via notification UI or voice. Auto-approves on timeout. |
| 9 | Phase 3 (Outline) — real Claude API call | Server console: "Phase 3: Generating outline..." |
| 10 | **Gate 2: Slide Outline Review** — voice prompt fires | Approve via notification UI |
| 11 | Phase 4 (Elaborate) — real Claude API call(s), longest phase | Server console: "Phase 4: Elaborating..." (30-120s) |
| 12 | **Gate 3: Content Review** — voice prompt fires | Approve via notification UI |
| 13 | Phase 5 (Serialize) — local, no API call | Server console: "Phase 5: Serializing..." |
| 14 | Phases 6-7 (Render) — stubs | Pass through quickly |
| 15 | Gate 4 (Render Review) — always auto-approves (stub) | No interaction needed |
| 16 | Phase 8 (Deliver) — stub | Server console: "Presentation generation complete!" |
| 17 | **Verify done queue**: `agent_type=="presentation"`, `cost_summary.total_cost_usd > 0`, `job_id` starts with `pr-` | Job card in queue panel shows done |

**Cost**: ~$0.10-0.30 (real Claude API calls in phases 2-4)
**Duration**: 5-10 minutes total including gate interactions
**Safety net**: All gates auto-approve if voice I/O fails (timeout or MCP disconnect) — pipeline won't get stuck

### Key Files (Read-Only Reference)
- `src/cosa/agents/presentation_generator/orchestrator.py` — gates at lines 1026, 1136, 1250, 1374
- `src/cosa/rest/routers/presentation_generator.py` — submit endpoint
- `src/rnd/2026.03.14-presentation-generator/07-phase-c-d-verification-plan.md` — Phase C-D plan doc

---

## Workstream B: E2E Test Suite Status

**RESOLVED by Session 377**: Added `--bg` flag + nohup background processing + PID-based overlap guard to `run-e2e-ui-tests.sh`. Suite result: 297 passed, 0 failed in 17m12s. The 23 collision errors from Session 372c are confirmed eliminated.

No additional flock fix needed — Session 377's PID-file approach is sufficient.

---

## Coordination Notes

- Phase D does NOT hot-swap the server config (runs against live development server)
- E2E tests DO hot-swap (use `--bg` flag now: `./src/scripts/run-e2e-ui-tests.sh --bg -v`)
- Don't start E2E while Phase D is mid-pipeline (hot-swap would change config under the running job)
- Phase D can safely run while E2E script is being modified (code-only, no server interaction)

---

## Verification

- **Phase D success**: Job in done queue with `cost_summary.total_cost_usd > 0`, all 4 gates exercised (3 via voice, 1 auto-approve)
- **E2E baseline**: Already healthy (297 passed, 0 failed — Session 377)
