# Top-10 Outstanding TODOs — Branch `wip-v0.1.6-2026.03.12-cjflow-upe-and-playwrite`

**Date**: 2026-04-22
**Branch**: `wip-v0.1.6-2026.03.12-cjflow-upe-and-playwrite`
**Source**: `TODO.md` snapshot dated 2026-04-21 22:30 EDT (Session 9934d315 close)
**Session**: `b486e9dc`

This is a point-in-time triage view, not a schedule. Items are ordered by **freshness × blast-radius** — the front of the queue after the massive test-health cleanup of Session 9934d315. All items are `[LUPIN]` scope (parent Lupin repo; CoSA commits are the user's concern per `feedback_lupin_only_never_cosa`).

---

## The top 10

### 🔴 Immediate — unresolved test-health gaps from last session

| # | Item | Source | Triage |
|---|------|--------|--------|
| 1 | **5× `test_lancedb_gcs_integration.py` — CUDA OOM in `ProseEmbeddingEngine`** | Block 1a (4/21) | GPU contention with user workloads; user's GPU 0 has <1 GB VRAM free after server warm-up. Options: force CPU, `skipif` on no-CUDA, or defer to a quieter window. **USER-RUN per `feedback_never_grab_gpu`.** **Update 4/22**: corrected understanding — tests should call the server's `POST /api/embeddings/batch` endpoint (at `src/cosa/rest/routers/embeddings.py:85-110`) rather than instantiating `ProseEmbeddingEngine` in-process. |
| 2 | **`test_conftest_clean_test_db.py::test_clean_test_db_removes_prior_test_users` — `psycopg2 DatatypeMismatch` (uuid vs text)** | Block 1b (4/21) | Pre-existing meta-test fixture bug. Cast `gen_random_uuid()` explicitly or use Python `uuid.uuid4()`. |
| 3 | **`test_cj_flow_pause_schedule.py::test_click_pause_button_updates_card` — pause button click intercepted** | Block 2a (4/21) | Playwright log shows `.queue-category`, `.queue-header`, `.lupin-nav-active` intercepting pointer events. CSS z-index/pointer-events bug — likely regression from parallel-session commit `82243e4` (Bug Fix Mode b802e633, `notifications.js` truncation work). |
| 4 | **`test_job_history_ui.py::test_admin_can_manage_other_users_jobs` — admin retry times out** | Block 2b (4/21) | 30s timeout on `/retry`. Either endpoint broken for admin→other-user, button click doesn't POST, or auth boundary mis-configured. |

### 🟡 High-signal follow-ups from matrix harness (Session d8831785, 4/20)

| # | Item | Touches |
|---|------|---------|
| 5 | **Flip `DEFAULT_MODEL` back to Sonnet 4.6 (Opus is ~5× worse on this workload)** | `src/scripts/tfe_to_cc_phase3_live.py:40` (currently `claude-opus-4-7`) + `notifications.js::renderResumeOverrideControls()` (UI localStorage fallback). Matrix report: `src/rnd/v0.1.6/23-model-effort-matrix-results.md`. Winning arm: **Sonnet 4.6 + effort=high**, 5/11 effective fixes, 17.1 min. |
| 6 | **Why does Opus default to `unclear`?** | Inspect failing Opus-arm stream-json tool-use trace — three Opus arms returned 10–11 unclear verdicts regardless of effort. Highest-signal unanswered question from the matrix. |

### 🟡 E2E / test-infra gaps carried over

| # | Item | Effort |
|---|------|--------|
| 7 | **Visual-regression snapshot drift — 12 `test_visual_page[chromium-*]` failures** | Human page-by-page UI review, then `./src/scripts/run-e2e-ui-tests.sh --bg --update-snapshots -k visual` if intentional |
| 8 | **Auth 401 — `test_tfe_resume_e2e` + `test_cross_container_auth` (8 failures)** | Migrate from mock-token patterns to JWT-from-`/auth/login` per `feedback_mock_tokens_are_legacy` memory. Needs test-side credential-helper design decision. |

### 🟡 Operational hygiene (filed last week, still open)

| # | Item | Notes |
|---|------|-------|
| 9 | **`lupin.lancedb` bloat — `input_and_output_tbl.lance` is 37 GB** | `COPY`'d into every image rebuild. Options: `LANCE.compact_files()`, audit retention, or switch to runtime bind-mount (mirrors postgres-data pattern). HIGH priority if production images hit this. |
| 10 | **Client-side notification dedup by `idempotency_key`** | UI renders the same `Coder: Bash` notification 4× because the server fans one notification to multiple WS subscribers. Sliding-window dedup (~2s) in `notifications.js` before DOM append. |

---

## Not in the top 10 but worth flagging

- **CoSA submodule commits uncommitted** from Sessions 9934d315 + earlier (user commits separately from `src/cosa/` context — not Claude's concern per `feedback_lupin_only_never_cosa`).
- **CJ Flow async + multi-lane (v0.1.7)** — explicitly deferred until v0.1.6 ships per user decision 2026-04-21. Design review at `src/rnd/v0.1.7/2026.04.21-cj-flow-async-multi-lane-design-review.md`. Default concurrency confirmed at `cj flow max concurrent agentic jobs = 3` for first deploy.
- **Coder turn-budget work** — Option B mid-flight check-in + Coder prompt audit. Not blocking, but the next infrastructure lift for the TFE Phase 3 apply path once the visible test health is green.
- **`gh auth setup-git` in Dockerfile** — needed for Phase 5 `git push` inside the container. Deferred until Phase 5 is actually reached in a real TFE Resume run.
- **TFE report rendering bug** — markdown report shows `Duration: 0.0s` and `C1-C8 — 0 failure(s)` even though `state_snapshot` has full cluster data. Not blocking; cluster data is correct in the DB.
- **Worktree maintenance CLI** (Bug 9 follow-up) — `src/scripts/worktree-cleanup.sh` to prune orphaned worktrees, periodic cleanup, disk-usage telemetry. Out of scope for initial Bug 9 landing.

---

## Session choice (2026-04-22)

User selected items **#5** (Bug A: TFE model flip) and **#1** (Bug B: LanceDB CUDA OOM) to tackle this session. See companion design doc: `2026.04.22-tfe-model-flip-and-lancedb-cuda-oom-plan.md`. Execution log: `2026.04.22-90-execution-log.md`.
