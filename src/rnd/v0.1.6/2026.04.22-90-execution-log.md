# Execution Log — Session b486e9dc (2026-04-22)

**Paired design doc**: `2026.04.22-tfe-model-flip-and-lancedb-cuda-oom-plan.md`
**Top-10 briefing**: `2026.04.22-session-start-top-10-briefing.md`

Append-only log. Each phase gets commit hashes, test results, and decision points as it lands.

---

## Phase 0 — Documentation-first

| Step | Status | Timestamp | Artifact / Result |
|---|---|---|---|
| Write top-10 briefing | ✅ done | 2026-04-22T~16:00 | `2026.04.22-session-start-top-10-briefing.md` |
| Write design doc | ✅ done | 2026-04-22T~16:00 | `2026.04.22-tfe-model-flip-and-lancedb-cuda-oom-plan.md` |
| Write this execution log (stub) | ✅ done | 2026-04-22T~16:00 | `2026.04.22-90-execution-log.md` |
| Update `src/rnd/README.md` file count | pending | — | v0.1.6 count `34` → `37` |

---

## Phase 1 — Bug A: TFE model flip

| Step | Status | Timestamp | Result |
|---|---|---|---|
| Edit `tfe_to_cc_phase3_live.py:41` (line was 41, not 40) | ✅ done | 2026-04-22T16:10 | `DEFAULT_MODEL = "claude-sonnet-4-6"`; kept `DEFAULT_EFFORT = "high"` |
| Edit `notifications.js:7279` | ✅ done | 2026-04-22T16:10 | localStorage fallback `|| 'claude-sonnet-4-6'`; dropdown options at :7288-7290 preserved |
| Bump cache-bust in `notifications.html:925` | ✅ done | 2026-04-22T16:10 | `v=20260422a` → `v=20260422b` (bumped past today's earlier Bug Fix session bump to force reload for THIS JS change) |
| `py_compile` verification | ✅ done | 2026-04-22T16:11 | `py_compile: OK` |
| Browser smoke | pending user | — | User verifies on :7999 with localStorage cleared: dropdown defaults to Sonnet 4.6 |
| Parent Lupin commit | ✅ done | 2026-04-22T16:55 | hash: **`9d2b1fd`** — combined Bug A + Bug B + Phase 0 docs (13 files, 654 insertions, 29 deletions) |

---

## Phase 2 — Bug B: LanceDB CUDA OOM via server endpoint

**Design pivot mid-implementation**: user corrected the original plan's complexity ("Why are you complicating this? Why are these tests so fucking opaque?"). The server ALREADY has `POST /api/embeddings/batch` — tests should HTTP-call it instead of instantiating engines in-process. Plan simplified to a class-scoped monkeypatch of `EmbeddingProvider.generate_embedding` and `EmbeddingManager.generate_embedding`.

**Design decision**: No CoSA method addition. Tests don't need `get_snapshots_by_embedding` because the monkeypatch intercepts at the provider level — `SolutionSnapshot.__init__` auto-embeds, `QuestionEmbeddingsTable.get_embedding` queries, and `CanonicalSynonymsTable` all route through HTTP transparently. Every caller of `generate_embedding` benefits from one fix.

| Step | Status | Timestamp | Result |
|---|---|---|---|
| Read `lancedb_solution_manager.py` + `solution_snapshot.py` + `question_embeddings_table.py` to map GPU-touching paths | ✅ done | 2026-04-22T16:20 | Found 20+ `generate_embedding` call sites; all chokepoint at `EmbeddingProvider.generate_embedding` or `EmbeddingManager.generate_embedding` |
| Write `src/tests/smoke/test_embedding_api_smoke.py` (2 tests) | ✅ done | 2026-04-22T16:30 | Both passed against `:7999` in 0.24s; HTTP 200; 768-dim confirmed |
| Add `server_embedder` session-scoped fixture to `conftest.py` | ✅ done | 2026-04-22T16:35 | Logs in once with companion creds; returns `embed(text, content_type)` callable |
| Full rewrite `test_lancedb_gcs_integration.py` with `http_embedding_provider` autouse class-scoped fixture | ✅ done | 2026-04-22T16:35 | Monkeypatches `EmbeddingProvider.generate_embedding` + `EmbeddingManager.generate_embedding` → HTTP |
| Delete `_skip_cuda_gcs_tests()` + `LUPIN_ALLOW_CUDA_TESTS=1` opt-in gate | ✅ done | 2026-04-22T16:35 | Tests now run by default since they no longer touch GPU; torch import removed |
| CoSA edits | ❌ none needed | — | Monkeypatch at provider level obviates `get_snapshots_by_embedding` addition; zero CoSA touch |
| `py_compile` all 3 edited files | ✅ done | 2026-04-22T16:38 | `py_compile: OK (3 files)` |
| Import-probe `EmbeddingProvider` + `EmbeddingManager` | ✅ done | 2026-04-22T16:39 | Both importable; both have `generate_embedding` method |
| Run 5 target integration tests | pending user | — | Requires `:8000` test container; per `feedback_small_ad_hoc_runs_go_to_7999` not Claude's concern. User: `./src/tests/run-integration-tests.sh --bg -v -k test_lancedb_gcs_integration` |
| Run full integration regression | pending user | — | Same — user runs via `--bg` flag |
| Parent Lupin commit | ✅ done | 2026-04-22T16:55 | hash: **`9d2b1fd`** (same commit as Bug A; combined checkpoint) |
| CoSA commit | not needed | — | Zero CoSA edits this session |

### Targeted integration-test validation run (2026-04-22T16:14)

Command: `./src/tests/run-integration-tests.sh --bg -v -k test_lancedb_gcs_integration`
PID: 298099
Log: `/tmp/integration-20260422-161404.log`
Duration: 6.02s

**Summary**: `1 passed, 6 skipped, 290 deselected, 1 xfailed, 60 warnings` — script exits "✓ All tests passed"

| Test | Outcome | Notes |
|---|---|---|
| `test_manager_initialization_with_gcs` | SKIPPED | `gcs_credentials_available` fixture |
| `test_add_snapshot_to_gcs` | SKIPPED | same |
| `test_query_by_question_from_gcs` | SKIPPED | same |
| `test_data_persistence_across_manager_instances` | XFAIL | ran full embedding path (5× auto-embed + 1 query) through HTTP monkeypatch; xfailed on pre-existing normalization-mismatch bug unrelated to Bug B — **this single test validates the fix** |
| `test_multiple_snapshots_and_retrieval` | SKIPPED | `gcs_credentials_available` fixture |
| `test_configuration_block_loading` | PASSED | no embeddings |
| `test_update_existing_snapshot` | SKIPPED | same |
| `test_gcs_backend_handles_special_characters` | SKIPPED | same |

**No CUDA OOM, no traceback, no GPU activity in pytest process.** Follow-up filed to mount host gcloud ADC into `lupin-rest-test` container so the 6 skipped tests actually run.

---

## Decision points encountered

_(append as they arise)_

---

## Regressions / blockers

_(append as they arise)_
