# Test Suite Verification Plan (Development Environment Only)

**Session**: 116 (continued)
**Date**: 2026-02-02
**Goal**: Run full test suite and document results - NO remediation, status report only

---

## Environment Distinction

### Development Environment (FastAPI server running on port 7999)
**THESE TESTS WE WILL RUN:**
- ✅ Unit tests (`src/tests/unit/`)
- ✅ Inline smoke tests (modules with `quick_smoke_test()`)
- ✅ WebSocket smoke tests (`src/scripts/run-websocket-smoke-tests.sh`)
- ✅ Pytest smoke tests (`src/tests/smoke/`)

### Testing Environment (Requires killing dev server)
**THESE TESTS WE WILL NOT RUN:**
- ❌ Integration tests (`src/tests/integration/`)
- ❌ `./src/tests/run-integration-tests.sh`

**Why**: Integration tests start their OWN server instance on port 7999 with `[Lupin: Testing]` config block and test database. Port conflict with dev server.

---

## Test Execution Plan

### Phase 1: Unit Tests
**Command**: `pytest src/tests/unit/ -v`
**Expected**: 14+ tests covering jwt_service, password_service, user_service, rate_limiter
**Dependencies**: None (uses mocks)
**Server Required**: No

### Phase 2: Inline Smoke Tests (Session 116 Migration Files)
Focus on files modified during util_xml.py deprecation:

| Module | Command | Server Required |
|--------|---------|-----------------|
| `gister.py` | `python -m cosa.memory.gister` | No |
| `confirmation_dialog.py` | `python -m cosa.agents.confirmation_dialog` | No |
| `xml_parser_factory.py` | `python -m cosa.agents.io_models.utils.xml_parser_factory` | No |
| `todo_fifo_queue.py` | `python -m cosa.rest.todo_fifo_queue` | No |
| `multimodal_munger.py` | `python -m cosa.rest.multimodal_munger` | No |

### Phase 3: WebSocket Smoke Tests
**Command**: `src/scripts/run-websocket-smoke-tests.sh`
**Expected**: ~50 tests, ~92% pass rate historical
**Server Required**: Yes (dev server on port 7999)

### Phase 4: Pytest Smoke Tests
**Command**: `pytest src/tests/smoke/ -v`
**Tests Available**:
- `test_token_proactive_refresh_smoke.py`
- `test_notifications_sse_smoke.py`
- `test_deep_research_submit_smoke.py`
- `test_mcp_smoke.py`
- `test_deep_research_dry_run_smoke.py`
- `test_podcast_generator_dry_run_smoke.py`
- `test_research_to_podcast_dry_run_smoke.py`

**Server Required**: Yes (dev server on port 7999)

---

## Output Documentation

### Formalized Report File
**Location**: `src/rnd/2026.02.02-test-baseline-report.md`

**Purpose**: Persistent record of test results for remediation reference. During remediation, only re-run tests that failed - passing tests don't need re-verification.

---

## Verification Scope

**What we're verifying**:
- Session 116 util_xml.py deprecation changes haven't broken existing functionality
- Pydantic XML parsing works correctly in all migrated modules
- No regressions in core agent and queue functionality

**What we're NOT doing**:
- ❌ Remediation of any failures
- ❌ Code changes
- ❌ Running integration tests (testing environment)
- ❌ Fixing anything - documentation only

---

## Execution Notes

- All commands require `PYTHONPATH` to include `src/`
- Dev server must be running for WebSocket and pytest smoke tests
- Results will be summarized in tabular form
- Any failures will be documented but NOT fixed
