# Phase 6b Code-Execution Plan — Interactive Widgets (action-required + TTS chrome + delete-button)

**Phase 0 (design)**: 🟢 CLOSED 2026-05-11 PM — design ratified (Q-B1 through Q-B12), REUSE pre-pass closed 2026-05-07 (28 RE + 5 L3), Pass 1 Fitness closed 2026-05-11 (14 findings), **Pass 2 Adversarial closed 2026-05-11** (11 findings: 8 Majors A1-A8 + 3 Minors a1-a3), convergence re-greps clean.
**Authoritative design**: `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/09-phase6b-interactive-widgets-design.md`
**Pass 2 closure record**: `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/95-phase6b-review-findings.md` § "Pass 2 Adversarial — closed 2026-05-11"
**Phase 6a baseline (CAPTURED 2026-05-11)**: `B6a = 31484` bytes — `gzip -9 -c src/fastapi_app/static/dist/multiplexer/boot.65c779ac946b.js | wc -c` (built `2026-05-07T01:40:36Z` per `dist/multiplexer/manifest.json`; this artifact carries the Phase 6a AC11a/AC11b closure code; HEAD `243267b` only changed `TODO.md` so artifact unchanged from current HEAD `b8fbd9f`).
**AC7 ceiling**: `B6a + 8192 = 31484 + 8192 = 39676` bytes (per Pass 1 F-8 — re-baselined from invalid pre-6a `60382`).

**Plan-mode origin**: Pass 2 Adversarial closure cycle (2026-05-11). No `~/.claude/plans/*.md` predecessor — plan synthesized directly from the design doc + Pass 2 ratification record.
**Serialization**: this file = canonical R&D record per CLAUDE.md plan-serialization mandate.
**Implementation gate**: User explicitly opened the code-execution cycle 2026-05-11 ("let's go ahead and get started and begin work on the Code-execution plan") immediately after Pass 2 apply phase closed. Per `feedback_plan_approval_means_go`, user-ratification of THIS plan is the go-ahead for Phase 1.

## Context

Phase 6b lifts inertness from the action-required widgets and the `#tts-pane`, wires interactive handlers, and lands the delete-button click handler that Phase 6a Q-A6 deferred ("Visual only in 6a, handler in 6b"). The Phase 5 read-only widget scaffolds + Phase 4 `ActionRequiredStore` / `AudioStore` public APIs are the consumed inputs. Phase 6c (voice-persona modal, focus tray, audio recorder) is OUT OF SCOPE.

Pass 2 Adversarial surfaced 11 findings clustered around contract-drift to non-existent store APIs (6 Majors) and race conditions (2 Majors). All applied to the design doc. The store-side fixes (`respondAndAwait`, widened `respond()` signature, `AudioStore.stop()`) are new Phase 0 prerequisites #8 / #9 / #10. The race fix for A3 is Path A — an ownership-flag (`dataset.phase6bOwner`) early-return guard added to Phase 5's `NotificationsListRenderer.renderActionRequiredSection()`.

### Scope-of-work delta vs Phase 6a

| Aspect | Phase 6a | Phase 6b |
|---|---|---|
| New TS renderers | 1 (`JobsPaneRenderer`) | 2 (`ActionRequiredRenderer`, `TtsChromeRenderer`) |
| New TS templates | 2 (`jobCard`, `jobBucket`) | 2 (`actionRequiredInteractive`, `ttsChrome`) |
| New CSS files | 1 (`jobs-pane.css` ≤800 LOC) | 2 (`action-required.css` ≤500 LOC + `tts-chrome.css` ≤700 LOC) |
| New FastAPI router | 1 (`multiplexer_config.py`) | 0 (no client-config addition) |
| Store API extensions | 0 (read-only consumer) | **3** (`respondAndAwait`, widen `respond`, `AudioStore.stop`) — Phase 0 prereqs #8/#9/#10 |
| Phase 5 renderer edit | 0 | **1** (early-return guard on `renderActionRequiredSection`) — A3 Path A |
| `boot.ts` ordering invariant | implicit | **explicit** (A8 — renderers first, transports last; AC9b smoke gate) |

### Cross-sub-project handoff

CoSA-side store extensions (#8/#9/#10) live in `src/cosa/stores/` Python equivalents only if the server-side `/api/notify/response` handler currently string-coerces multi-select payloads. The client-side store extensions (`ActionRequiredStore.respondAndAwait`, `AudioStore.stop`) live in `src/fastapi_app/static/js/multiplexer/stores/*.ts` — these are **Lupin-side TypeScript edits** (NOT CoSA submodule per CLAUDE.md project structure; the `multiplexer/stores/` directory is under `src/fastapi_app/static/js/`, parent Lupin). The only CoSA-side work is server-handler payload acceptance for A2 (widened `respond()` shape), and ONLY if the current handler rejects non-string `response_value.response`. Verification at Phase 0.

## Approach summary

- **Per-phase commits with explicit user approval**: each of Phases 0–7 is a distinct commit boundary; user approves every commit individually per `feedback_never_auto_commit_push`.
- **Single in-commit gate per phase**: AC1–AC10e green per phase boundary OR rolled forward into the next phase if dependencies are missing; final cross-phase verification at Phase 6.
- **Separate scheduled gate**: AC11a (Run #1 baseline capture) + AC11b (Run #2 regression check) on `:8000` via `POST /api/test-suite/submit` (handled via `/schedule-tests` skill per `feedback_use_schedule_tests_skill`). HUMAN gate is slot-availability calendar only — NOT tester duty.
- **Build order**: tracking-doc seed (Phase 0) → store API extensions + Phase 5 short-circuit prep (Phase 1 prereqs) → templates (Phase 2) → ActionRequiredRenderer (Phase 3) → TtsChromeRenderer (Phase 4) → delete-button wiring on JobsPaneRenderer with 4A→4B split (Phase 5) → CSS port + page shell + boot wiring (Phase 6) → smoke + cross-phase verification (Phase 7) → scheduled E2E (Phase 8). **9 implementation phases** vs Phase 6a's 8 (extra phase for store-API prereqs).
- **No code edits in Phase 0**. Tracking-doc seed precedes ALL code per `feedback_phase0_serialization_prominence`.

## Pre-flight (before any code edits)

1. **Re-audit at execute time** per `feedback_audit_plans_at_execute_time`: re-grep recent feedback memories for new constraints landed since Pass 2 closed 2026-05-11; surface any conflicts before code edits begin.
2. **Capture Phase 6a baseline `B6a`**: `gzip -9 -c src/fastapi_app/static/dist/multiplexer/boot.<hash>.js | wc -c` at HEAD `243267b`. Record value here + in `90-execution-log.md` Phase 6b section. Update AC7 ceiling to `B6a + 8192`.
3. **Verify Phase 6a 100% c8 backfill still holds**: `c8 --100 ...` on Phase 4+5+6a multiplexer TS files exit 0 (per `feedback_100pct_coverage_multiplexer`).
4. **Verify pre-existing store API signatures**: grep `respond(`, `tick`, `expires_at`, `pause(`, `resume(`, `skip(`, `state(`, `queueLength(` in `src/fastapi_app/static/js/multiplexer/stores/*.ts` — confirm signatures match Pass 2 findings' file:line citations.
5. **Verify Phase 5 `NotificationsListRenderer.renderActionRequiredSection`** still at `:228-243` — A3 Path A's early-return guard edit lands here.
6. **Verify `actionRequiredReadOnly.ts:49`** still sets `data-id-hash` (NOT `data-action-required-id`) — A4 contract-drift fix targets the real attribute name.

## Execution steps

### Step 0 — Tracking-doc seed (Phase 0; NO code edits)

**Output**: a single new section appended to the existing tracking doc, plus this implementation-plan serialization.

**Edits**:
- `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/90-execution-log.md` — append a Phase 6b section after the existing Phase 6a closure. Phase 6b sub-section contents: per-phase progress table (Phase 1 through Phase 8 of THIS implementation cycle, with status columns), AC scorecard (AC1–AC11b, all `pending`), AC10/AC10b/AC10d/AC10e audit table placeholders, side-effect-tasks checklist (3 new Phase 0 prereqs landed, Phase 5 short-circuit applied, INI cap key, `formatDuration` reuse confirmation if any).
- `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/2026.05.11-phase6b-code-execution-plan.md` — this file (the plan serialization).

**Verify at end of Step 0**: both files exist; no code files have been touched.

### Step 1 — Phase 0 prerequisites landing (Phase 1)

The 3 new store API extensions from Pass 2 (#8 / #9 / #10) and the A3 Path A early-return guard MUST land before any Phase 6b renderer code is written. Phase 5's `NotificationsListRenderer` edit is small (one line + a comment); the store extensions are additive (no breaking change to existing optimistic `respond()`).

**1.1 `ActionRequiredStore.respondAndAwait()`** (per Pass 2 A1 — Phase 0 prereq #8):
- `src/fastapi_app/static/js/multiplexer/stores/ActionRequiredStore.ts` — add new public async method per the Phase 0 prereq #8 snippet (design doc L412-427):

  ```ts
  async respondAndAwait(
      idHash: string,
      response: string | ReadonlyArray<string> | Record<string, string>
  ): Promise<void> {
      const item = this.items.get( idHash );
      if ( !item ) throw new Error( `Unknown action_required id: ${idHash}` );
      item.state = "submitting";
      this.emit( "responded-pending", { idHash, response } );
      try {
          await this.api.post( "/api/notify/response", {
              notification_id: idHash,
              response_value: { response },
          } );
          item.state = "responded";
          this.emit( "responded", { idHash, response } );
      } catch ( err ) {
          item.state = "failed";
          this.emit( "failed", { idHash, response, error: err } );
          throw err;
      }
  }
  ```
- Existing `respond()` stays unchanged (Phase 5 backward compat).
- **Unit tests** (`src/tests/unit/multiplexer/stores/action_required_store.test.ts` — EDIT) — add ≥4 cases: success path (`responded-pending` then `responded`), rejection path (`responded-pending` then `failed` + re-throw), unknown idHash throws, double-call serialization.

**1.2 Widen `respond()` (+ `respondAndAwait()`) signature** (per Pass 2 A2 — Phase 0 prereq #9):
- Same file: extend BOTH `respond()` and the new `respondAndAwait()` parameter type from `response: string` to `response: string | ReadonlyArray<string> | Record<string, string>`.
- Verify server-side `/api/notify/response` handler accepts the structured shape. **Reconnaissance step**: `grep -n "response_value" src/cosa/rest/routers/notifications.py` (or equivalent) to confirm wire schema. If the handler currently coerces to string, file a CoSA-context task; otherwise proceed.
- **Unit tests** — extend existing `respond()` test file with ≥3 new cases: array argument (multiSelect), object argument (open_ended_batch), back-compat single-string.

**1.3 `AudioStore.stop()`** (per Pass 2 A6 — Phase 0 prereq #10):
- `src/fastapi_app/static/js/multiplexer/stores/AudioStore.ts` — add new public method:

  ```ts
  stop(): void {
      // Transition to idle, clear remaining queued chunks, do NOT auto-resume on next chunk arrival.
      // Distinct from skip() (advance one chunk) and pause() (suspend without clearing queue).
      this.queue.length = 0;
      this.state_ = "idle";
      this.emit( "store_audio_state_change", { state: "idle" } );
  }
  ```
- (Implementation may differ depending on how `state_` is structured today; the contract is: idle + cleared queue + no auto-resume.)
- **Unit tests** (`src/tests/unit/multiplexer/stores/audio_store.test.ts` — EDIT) — add ≥3 cases: stop-while-playing → idle + queue cleared, stop-while-paused → idle, stop-while-idle → no-op (no event emitted).

**1.4 Phase 5 `NotificationsListRenderer` early-return guard** (per Pass 2 A3 Path A):
- `src/fastapi_app/static/js/multiplexer/render/NotificationsListRenderer.ts:228-243` — extend `renderActionRequiredSection()`:

  ```ts
  renderActionRequiredSection( /* ... */ ): void {
      // Phase 6b ownership flag (per Pass 2 A3 Path A):
      // when ActionRequiredRenderer.mount() has fired, this section is owned by Phase 6b
      // and we must not nuke the interactive widget. See R8 risk row in 09-phase6b-interactive-widgets-design.md.
      if ( this.actionRequiredMount.dataset.phase6bOwner === "true" ) return;
      // ... existing read-only render path unchanged
  }
  ```
- **Unit tests** (`src/tests/unit/multiplexer/render/notifications_list_renderer.test.ts` — EDIT) — add ≥2 cases: guard returns early when flag set, guard does NOT return when flag absent.

**1.5 Cross-store regression**:
- `c8 --100` on `ActionRequiredStore.ts` + `AudioStore.ts` + `NotificationsListRenderer.ts` after the 4 edits → exit 0 (per `feedback_100pct_coverage_multiplexer`).

**Verify at end of Step 1**: `npx tsc --noEmit` clean; `npx eslint src/fastapi_app/static/js/multiplexer/` clean; all 4 store/renderer edits c8 100%; new unit tests green.

**Commit boundary**: Phase 1 commit — "Phase 6b Phase 1: store API prereqs (respondAndAwait + widen respond + AudioStore.stop) + Phase 5 ownership-flag guard".

### Step 2 — Interactive widget templates (Phase 2)

No renderer dependency yet; pure DOM producers. Mirror Phase 5's `actionRequiredReadOnly.ts:71-109` switch-on-`response_type` precedent (REUSE RE-28 ratified).

**2.1 `src/fastapi_app/static/js/multiplexer/render/templates/actionRequiredInteractive.ts`** (NEW):
- Header comment documenting AC2e invariant (per Pass 2 a1): "ALL DOM writes go through `html` tagged template + `.textContent` / `.value` only. NEVER use `.innerHTML =`, `rawHTML(`, or `.outerHTML =`."
- Exports `renderActionRequiredInteractive(item: ActionRequiredItem, handlers: { onSubmit: (response: string | ReadonlyArray<string> | Record<string, string>) => void }): HTMLElement` (per Pass 2 A2 widened type).
- Internal switch dispatches per `response_type` (per Q-B1 + Pass 1 F-3):
  - `yes_no` → `buildYesNo(item, handlers)` — 2 buttons (Yes / No), direct on-click
  - `multiple_choice` + `multiSelect: false` → `buildRadio(item, handlers)` — radio group + Submit
  - `multiple_choice` + `multiSelect: true` → `buildCheckbox(item, handlers)` — checkbox group + Submit
  - `open_ended` → `buildOpenEnded(item, handlers)` — text input + Submit (Enter-to-submit)
  - `open_ended_batch` → `buildOpenEndedBatch(item, handlers)` — per-question inputs + Submit-All
  - `default:` → throw `Error(`Unknown response_type: ${item.response_type}`)`
- Helpers private to the module; tests cover them via the public function.
- `data-id-hash` carried through from the source template (per Pass 2 A4 — real attribute name).

**2.2 `src/fastapi_app/static/js/multiplexer/render/templates/ttsChrome.ts`** (NEW):
- Header comment documenting AC2e invariant (same as 2.1).
- Exports `renderTtsChrome(opts: { state: AudioPlaybackState, queueLength: number, currentTrackName?: string }): HTMLElement`.
- Class structure per design (Q-B6 + Q-B7 + Q-B8):
  - Pane chrome: 4 controls (Pause/Resume single toggle, Stop, Skip) + `<div class="tts-current-track">Playing: ${name}</div>` + queue-length indicator
  - State-driven control enable/disable per `AudioPlaybackState` table
  - Q-B8 `.is-playing-current` / `.is-paused-current` classes ported verbatim from `notifications.css:4692-4712, 4718-4725`
- Click handlers attached HERE in the template (self-contained widget per Phase 6a Q-B6 precedent for `role="button"`).

**2.3 Tests** (NEW):
- `src/tests/unit/multiplexer/render/templates_action_required_interactive.test.ts` — ≥15 PASS:
  - 5 happy-path renders (one per `response_type`)
  - 5 click-dispatch cases (button/radio/checkbox/text-Enter/batch-submit each invokes `onSubmit` with correctly-shaped response)
  - 1 disabled-default state (controls disabled before first interaction is N/A — they enable by default)
  - 1 unknown-response_type throws
  - **AC2e**: grep test verifies the source file contains 0 instances of `.innerHTML =`, `rawHTML(`, `.outerHTML =`
- `src/tests/unit/multiplexer/render/templates_tts_chrome.test.ts` — ≥15 PASS:
  - 6 state-driven render cases (idle / decoding / playing / paused / ended / error)
  - 4 click-dispatch cases (Pause / Resume / Stop / Skip each invokes corresponding handler)
  - 1 currentTrackName rendered when present, hidden when absent
  - 1 queueLength rendered
  - 1 `.is-playing-current` class applied during playing state
  - 1 `.is-paused-current` class applied during paused state
  - **AC2e**: grep test verifies the source file contains 0 instances of `.innerHTML =`, `rawHTML(`, `.outerHTML =`

**2.4 Coverage**:
- `c8 --include='src/fastapi_app/static/js/multiplexer/render/templates/{actionRequiredInteractive,ttsChrome}.ts' --100 npx tsx --test ...` exit 0.
- `c8 ignore next` regions allowed for genuinely-unreachable defensive branches (e.g., `default:` after exhaustive `response_type` switch); MUST include same-line reason comment.

**Verify at end of Step 2**: AC3 + AC4 + Phase 2 portion of AC6 + AC2e all green.

**Commit boundary**: Phase 2 commit — "Phase 6b Phase 2: interactive widget templates (actionRequiredInteractive + ttsChrome) with AC2e safe-write guards".

### Step 3 — `ActionRequiredRenderer` (Phase 3)

Consumes `actionRequiredInteractive` template + `ActionRequiredStore` (now with `respondAndAwait`). Mirrors `NotificationsListRenderer` lifecycle pattern.

**3.1 `src/fastapi_app/static/js/multiplexer/render/ActionRequiredRenderer.ts`** (NEW):
- Factory: `createActionRequiredRenderer(opts: { stores: { actionRequired: ActionRequiredStore }, apiClient: ApiClient }): ActionRequiredRenderer` (NO `container` option per Pass 2 A7).
- Canonical surface: `mount(root: HTMLElement): void`, `unmount(): void`, `forceRenderForTesting(): void`.
- Lifecycle per design § Inertness-lift contract (post-Pass 2 amendments):
  - `mount(root)` SETS `root.dataset.phase6bOwner = "true"` FIRST (per Pass 2 A3 — claims ownership before any DOM write)
  - Queries `[data-id-hash]` widgets (per Pass 2 A4 — real attribute name)
  - For each widget: `getById(idHash)` → atomic `replaceChildren()` with interactive template
  - Subscribes to `store_action_required_changed`; on each non-tick `changeKind` (`added` / `responded` / `expired` / `cancelled` / `offline-frozen` / `offline-resumed`) dispatches to private `applyChangeKind(widget, item, changeKind)` helper using targeted mutations only (per design L101 + R8 risk row)
  - Subscribes to `store_action_required_tick` (1Hz from `ActionRequiredStore.ts:291`) for countdown digit updates via `.textContent` writes (per Pass 2 a2 — NO renderer-side RAF)
  - On `expired` changeKind: transitions widget to `expired_visual` state (controls disabled + `<div class="action-required-expired">Expired — default applied</div>` swap)
  - On `responded` changeKind: transitions to `responded` (or `responded_default` if `responded_by: "server-default"`)
  - Click handler dispatches `respondAndAwait()` (NOT optimistic `respond()` per Pass 2 A1); on resolve → state stays `responded` (driven by store event); on reject → re-enable + inline error stripe (Q-B4) + state to `failed`
- Internal `#mounted: boolean` flag; throw `Error("ActionRequiredRenderer already mounted")` on second mount (mirrors Phase 6a F-26 contract).
- `unsubscribers: Array<() => void>` collected; `unmount()` invokes all + clears flag + clears `dataset.phase6bOwner`.
- **NO `requestAnimationFrame` loop for countdown** — driven solely by store tick events.

**3.2 Tests** (NEW): `src/tests/unit/multiplexer/render/action_required_renderer.test.ts` — ≥21 PASS (per AC5 enumeration sub-table):
- 6 submit happy-path (yes_no Yes / yes_no No / multi single / multi multi / open_ended / open_ended_batch) — each invokes `respondAndAwait` with correctly-shaped response
- 5 error-rollback (each of the above with `respondAndAwait` rejecting → inline error stripe renders + widget re-enables)
- 6 state-machine transitions (pending→submitting, submitting→responded, submitting→failed, failed→submitting retry, pending→expired_visual, expired_visual→responded_default)
- 1 countdown expiry (per Pass 2 a2 — store tick reaches 0 → expired_visual; NO RAF loop asserted via spy on `requestAnimationFrame`)
- 1 mount idempotency (second mount throws)
- 1 inertness-lift atomic strip (MutationObserver count = 1; all 4 markers gone per AC2c)
- 1 inline error stripe render
- **Ownership-flag race test** (per R8 risk row): after `mount()`, simulate `store_action_required_changed{changeKind: "added"}` for a sibling widget via Phase 5's `NotificationsListRenderer.renderActionRequiredSection()` → assert target interactive widget retains its DOM + listeners (early-return guard fires)
- **NO-RAF assertion** (per Pass 2 a2): spy on `globalThis.requestAnimationFrame` during a 5-second simulated countdown → assert spy NOT called

**3.3 Coverage**: `c8 --include='src/fastapi_app/static/js/multiplexer/render/ActionRequiredRenderer.ts' --100 ...` exit 0.

**Verify at end of Step 3**: AC2c + AC5 + Phase 3 portion of AC6 all green.

**Commit boundary**: Phase 3 commit — "Phase 6b Phase 3: ActionRequiredRenderer (Pass 2 A3 ownership flag + A1 respondAndAwait + a2 store-tick countdown)".

### Step 4 — `TtsChromeRenderer` (Phase 4)

Consumes `ttsChrome` template + `AudioStore` (now with `stop()`). Subscribes to both `store_audio_state_change` AND `store_audio_chunk_decoded` (Q-B9 + Pass 1 F-13 — both events RAF-coalesced renderer-side).

**4.1 `src/fastapi_app/static/js/multiplexer/render/TtsChromeRenderer.ts`** (NEW):
- Factory: `createTtsChromeRenderer(opts: { stores: { audio: AudioStore } }): TtsChromeRenderer` (NO `container` option per Pass 2 A7; no `apiClient` needed).
- Canonical surface: `mount(root: HTMLElement): void`, `unmount(): void`, `forceRenderForTesting(): void`.
- Lifecycle:
  - `mount(root)` queries the pane chrome container inside `root`; throws if missing
  - Subscribes to `store_audio_state_change` AND `store_audio_chunk_decoded` — both events scheduled via single shared `pendingRender` flag + `requestAnimationFrame` (per Q-B9 + F-13 symmetric coalescing)
  - On render: reads `AudioStore.state()`, `queueLength()`, optionally `currentNotificationIdHash()` (if Phase 0 prereq #3 lands as the synchronous-getter shape per F-11; otherwise event payload field)
  - Click handlers on chrome controls:
    - Pause/Resume toggle → `audioStore.pause()` or `audioStore.resume()` based on current state
    - Stop → `audioStore.stop()` (per Pass 2 A6 — NEW method)
    - Skip → `audioStore.skip()`
  - Per-notification corner buttons (2 controls per Q-B7): same Pause/Stop semantics, wired via event delegation from the notification list area
- Internal `#mounted: boolean` + `unsubscribers: Array<() => void>` collection (mirror Phase 6a F-26 + Phase 5 lifecycle).

**4.2 Tests** (NEW): `src/tests/unit/multiplexer/render/tts_chrome_renderer.test.ts` — ≥13 PASS (per AC5b enumeration sub-table):
- 7 state transitions (idle→decoding, decoding→playing, playing→paused, paused→playing, playing→ended, ended→idle, any→error)
- 4 control wiring (pause toggle dispatches `pause()`, resume toggle dispatches `resume()`, stop dispatches `stop()` — per Pass 2 A6, skip dispatches `skip()`)
- 2 storm safety (a) chunk_decoded storm — 100 events synchronously → ≤1 DOM mutation cycle; (b) state_change storm — 5 transitions synchronously → ≤1 DOM mutation cycle
- 1 mount idempotency
- **Stop semantics test**: assert `stop()` invocation clears `AudioStore.queue` length to 0 AND transitions state to idle (relies on Phase 1 prereq #10 landing)

**4.3 Coverage**: `c8 --include='src/fastapi_app/static/js/multiplexer/render/TtsChromeRenderer.ts' --100 ...` exit 0.

**Verify at end of Step 4**: AC5b + Phase 4 portion of AC6 all green.

**Commit boundary**: Phase 4 commit — "Phase 6b Phase 4: TtsChromeRenderer (Pass 2 A6 AudioStore.stop wiring + Q-B9 RAF-coalesced subscriptions)".

### Step 5 — Delete-button wiring on JobsPaneRenderer (Phase 5; split 5A → 5B per Pass 1 F-6)

Q-A6 follow-through. Splits into sub-step 5A (extend `JobStore` with `delete()` returning rollback closure) → sub-step 5B (wire click handler in `JobsPaneRenderer`).

**5A — `JobStore.delete()` + AC2d tests**

- `src/fastapi_app/static/js/multiplexer/stores/JobStore.ts` (EDIT) — add `public delete(idHash: string): { restoreState: () => void }` per design § Phase 4 sub-step DOD signature.
- `src/tests/unit/multiplexer/stores/jobstore_delete_api.test.ts` (NEW) — ≥3 cases per Pass 1 F-6 ratification: exposes method, removes entry + emits `store_jobs_changed{changeKind: "removed"}`, no-op on nonexistent idHash; PLUS round-trip `restoreState()` re-inserts identical previous state + emits `changeKind: "added"`, and `restoreState()` is no-op when delete was a no-op.

**DOD table** (per design § Sub-step 4A — renamed to 5A in this plan):

| # | Check | Pass criterion |
|---|---|---|
| 5A-1 | Method exists on `JobStore.prototype` | `typeof store.delete === "function"` |
| 5A-2 | Method removes entry on hit | `getById(idHash)` returns `undefined` post-`delete()` |
| 5A-3 | Method emits `store_jobs_changed` with `changeKind="removed"` | event subscriber receives correct payload |
| 5A-4 | `restoreState()` re-inserts identical previous state | round-trip `getById()` returns deep-equal entry |
| 5A-5 | `restoreState()` emits `changeKind="added"` | event subscriber receives correct payload |
| 5A-6 | Nonexistent idHash → no-op + no-op `restoreState` | no exception, no event emission |
| 5A-7 | c8 100% on `delete()` + closure | `c8 --100` clean on `JobStore.ts` |
| 5A-8 | AC2d test file exists + passes | `vitest jobstore_delete_api.test.ts` green |

**5A is complete when all 8 rows pass.**

**Commit boundary**: Phase 5A commit — "Phase 6b Phase 5A: JobStore.delete() + AC2d tests".

**5B — Wire delete-button click handler on JobsPaneRenderer**

- `src/fastapi_app/static/js/multiplexer/render/JobsPaneRenderer.ts` (EDIT) — extend with delete-button click delegation per Q-B10:
  - On `.job-delete-button` click (event delegation from mount root): capture `idHash` from parent `.job-card[data-id-hash]`; call `JobStore.delete(idHash)` → optimistic local removal + capture `restoreState` closure; POST `DELETE /api/queue/<bucket>/<idHash>` via `apiClient`
  - On 2xx response: discard `restoreState` (no rollback)
  - On 404 response: treat as success per Q-B10 ratification (already gone)
  - On 5xx or network error: invoke `restoreState()` + render inline error stripe on the restored card
  - Strip Q-A6 inertness markers from `.job-delete-button` (4 markers): `data-phase6-pending`, `aria-disabled`, `cursor: not-allowed`, `title="Delete coming in Phase 6b"`
- `src/tests/unit/multiplexer/render/jobs_pane_renderer.test.ts` (EDIT) — extend existing Phase 6a suite with ≥6 new AC5c cases:
  1. 2xx response → closure discarded; no rollback
  2. 404 response → treated as success per Q-B10
  3. 5xx response → `restoreState()` invoked + inline error stripe rendered
  4. Network error → same as 5xx
  5. Idempotency — rapid double-click → only first emits `delete`
  6. Non-delete-button clicks within row → handler is no-op

**DOD table** (per design § Sub-step 4B — renamed to 5B in this plan):

| # | Check | Pass criterion |
|---|---|---|
| 5B-1 | `JobsPaneRenderer` click delegation handles `.job-delete-button` | unit test asserts handler attached |
| 5B-2 | Click calls `JobStore.delete(idHash)` + captures `restoreState` closure | spy on `delete` returns closure |
| 5B-3 | 2xx response → closure discarded; no rollback | assert `restoreState` NOT called |
| 5B-4 | 404 response → treated as success per Q-B10 | same as 2xx |
| 5B-5 | 5xx response → `restoreState()` invoked + inline error stripe rendered | assert `restoreState` called + DOM has stripe |
| 5B-6 | Network error → same as 5xx | same |
| 5B-7 | Idempotency — rapid double-click → only first emits `delete` | spy call count = 1 |
| 5B-8 | Non-delete-button clicks within row → handler is no-op | spy call count = 0 |
| 5B-9 | Q-A6 inertness markers stripped from `.job-delete-button` (4 markers) | querySelector returns null for each marker |
| 5B-10 | c8 100% on `JobsPaneRenderer.ts` delta | `c8 --100` clean on the file |
| 5B-11 | AC5c (≥6 cases) green | `vitest jobs_pane_renderer.test.ts` green |

**5B is complete when all 11 rows pass.**

**Ordering**: 5A → 5B is a natural compile-time gate (5B imports `JobStore.delete` from 5A; `tsc --noEmit` fails 5B without 5A). Split-commit preferred for reviewability per design § Commit chain.

**Commit boundary**: Phase 5B commit — "Phase 6b Phase 5B: delete-button click handler on JobsPaneRenderer (Q-A6 + Q-B10 follow-through)".

**Verify at end of Step 5**: AC2d + AC5c + Phase 5 portion of AC6 all green.

### Step 6 — CSS port + page shell + boot wiring (Phase 6)

**6.1 `src/fastapi_app/static/css/multiplexer/action-required.css`** (NEW):
- Per-pane CSS for action-required interactive widgets (pane-only; widget-internal styling tightly scoped under `.action-required-widget` and descendants)
- AC10b ceiling: ≤500 LOC per Q-B12
- All selectors scoped — NO `*`, `html`, `body`, `:root` (F28 layer-2 stylelint rule)

**6.2 `src/fastapi_app/static/css/multiplexer/tts-chrome.css`** (NEW):
- Per-pane CSS for TTS chrome (pane controls) + per-notification corner buttons + `.is-playing-current` / `.is-paused-current` legacy classes ported verbatim from `notifications.css:4692-4712, 4718-4725`
- AC10b ceiling: ≤700 LOC per Q-B12 (bumped from 500 due to Q-B6 dual-surface scope)
- All selectors scoped — NO global rules

**6.3 `.stylelintrc.json`** (EDIT) — add `overrides` blocks for both new CSS files with `selector-disallowed-list: [["*", "html", "body", ":root"], { "severity": "error" }]`.

**6.4 `src/fastapi_app/static/html/multiplexer.html`** (EDIT):
- Add CSS links near existing notifications-list.css link: `<link rel="stylesheet" href="/static/css/multiplexer/action-required.css">` + `<link rel="stylesheet" href="/static/css/multiplexer/tts-chrome.css">`
- Verify `data-phase6-pending="true"` STAYS on `#tts-pane` BEFORE 6b boot wiring runs; renderer lifts it post-mount

**6.5 `src/fastapi_app/static/js/multiplexer/boot.ts`** (EDIT, multi-touch) — per Pass 2 A7 + A8 ordering invariant:
- Add inline comment marking ordering invariant: `// Order INVARIANT (per Pass 2 A8): renderers FIRST, transports LAST. See AC9b smoke test.`
- After Phase 6a `jobsRenderer.mount(...)`:
  1. Resolve `arRoot = document.getElementById('action-required-pane')` with non-null guard (throw on miss)
  2. Instantiate `createActionRequiredRenderer({ stores, apiClient })` + call `actionRequiredRenderer.mount(arRoot)` + `console.log('[multiplexer] actionRequiredRenderer:mounted')`
  3. Resolve `ttsRoot = document.getElementById('tts-pane')` with non-null guard (throw on miss)
  4. Instantiate `createTtsChromeRenderer({ stores })` + call `ttsChromeRenderer.mount(ttsRoot)` + `console.log('[multiplexer] ttsChromeRenderer:mounted')`
- Extend `BootCompletePayload.handlers` literal: `actionRequiredRenderer: "mounted"` + `ttsChromeRenderer: "mounted"`
- `transports.audio.start(...)` call MUST land AFTER both new mounts (existing call placement; verify ordering)

**6.6 `src/fastapi_app/static/js/multiplexer/render/index.ts`** (EDIT) — add exports for `createActionRequiredRenderer`, `type ActionRequiredRenderer`, `type ActionRequiredRendererOptions`, `createTtsChromeRenderer`, `type TtsChromeRenderer`, `type TtsChromeRendererOptions`.

**6.7 `src/fastapi_app/static/js/multiplexer/shared/types.ts`** (EDIT) — extend `BootCompletePayload.handlers` with `actionRequiredRenderer?: string` + `ttsChromeRenderer?: string` (optional per F12; runtime-unconditional `"mounted"` per F11).

**6.8 `src/fastapi_app/static/html/dev-tools.html`** (EDIT) — line 145 text update: append Phase 6b description (action-required interactive + TTS chrome live).

**Verify at end of Step 6**: build succeeds (`npx esbuild` or whatever the build invocation is); page loads in browser without console errors; AC10b CSS LOC + stylelint pass.

**Commit boundary**: Phase 6 commit — "Phase 6b Phase 6: CSS port + page shell + boot wiring with A7/A8 ordering invariants".

### Step 7 — Smoke + cross-phase verification (Phase 7, `:7999` AI-discretionary)

**7.1 `src/tests/smoke/test_multiplexer_phase6b_smoke.py`** (NEW) — mirrors Phase 6a smoke pattern:
- `BASE_URL = os.environ.get("LUPIN_API_URL", "http://localhost:7999")`
- Use Playwright + `__multiplexerTestHook` for fixture injection
- `test_phase6b_functional_smoke`: 3 fixture action_required prompts (each different `response_type`) + 2 audio chunks → assert widgets render in interactive mode (NO `data-phase6-pending` markers); pane chrome renders with state-matching controls
- `test_phase6b_no_pending_markers_after_mount` (AC2a / AC2b parameterized): after mount, 0 instances of `data-phase6-pending="true"` AND 0 `aria-disabled="true"` on action-required widgets
- `test_phase6b_boot_complete_handshake` (AC9): capture console messages via `page.on("console", ...)`; assert literal canonical order: `notificationsRenderer:mounted` → `jobsRenderer:mounted` → `actionRequiredRenderer:mounted` → `ttsChromeRenderer:mounted` (using `re.search` with line offsets)
- **`test_audio_chunks_arrive_after_mount`** (AC9b — NEW per Pass 2 A8): assert all four `:mounted` console lines appear BEFORE the first `store_audio_chunk_decoded` event in the captured console log
- `test_phase6b_perf_gate` (AC8b): pre-seed 50 prompts; first interactive paint within 200ms of `boot_complete`
- `test_phase6b_css_canary_no_body_drift` (AC10d layer-3): `getComputedStyle(document.body)` before action-required.css + tts-chrome.css load matches after on `["color", "background-color", "font-family", "margin", "padding"]`

**7.2 AC10 cross-phase verification (run by AI)** — single shell sequence:
- (1) `npx tsc --noEmit -p tsconfig.json`
- (2) `npx eslint src/fastapi_app/static/js/multiplexer/`
- (3) Phase 4 unit suite all PASS (cumulative with new respondAndAwait + AudioStore.stop tests)
- (4) Phase 5 unit suite all PASS (cumulative with new NotificationsListRenderer ownership-flag tests)
- (5) Phase 5 smoke all PASS (no regression from short-circuit edit)
- (6) Phase 6a unit suite all PASS (cumulative ≥88)
- (7) Phase 6a smoke 5/5 (no regression from new boot mounts)
- (8) WS smoke 50/50 (`src/scripts/run-websocket-smoke-tests.sh`)
- (9) Phase 6b unit suite ≥40 (templates + renderers + store extensions cumulatively)
- (10) Phase 6b smoke all PASS
- (11) AC2a/AC2b smoke gates green
- (12) AC9 + AC9b smoke gates green
- (13) AC6: `c8 --100` invocation across all new render + store files exit 0
- (14) AC7: `boot.js` gz delta — `gzip -9 -c src/fastapi_app/static/dist/multiplexer/boot.<hash>.js | wc -c` ≤ `B6a + 8192` bytes
- (15) AC10e cross-phase count-cascade: re-run Phase 5 + Phase 6a `pending_count` assertions with floor = 0 (post-6b lift): `pytest src/tests/smoke/test_multiplexer_phase5_smoke.py src/tests/smoke/test_multiplexer_phase6a_smoke.py -k "pending_count"` exit 0

**7.3 AC10b** — `wc -l src/fastapi_app/static/css/multiplexer/action-required.css` ≤ 500; `wc -l src/fastapi_app/static/css/multiplexer/tts-chrome.css` ≤ 700; `npx stylelint src/fastapi_app/static/css/multiplexer/{action-required,tts-chrome}.css` exit 0.

**7.4 AC10d three-layer scope-leak detection** (both new CSS files):
- Layer 1 (grep): `grep -E '^\s*(\*|html|body|:root)\s*\{' src/fastapi_app/static/css/multiplexer/{action-required,tts-chrome}.css` → expect zero matches
- Layer 2 (stylelint with `selector-disallowed-list`): from 6.3 — fail-build on violation
- Layer 3 (canary): `test_phase6b_css_canary_no_body_drift` from 7.1

**Verify at end of Step 7**: AC1, AC2, AC2a, AC2b, AC2c, AC2d, AC2e, AC3, AC4, AC5, AC5b, AC5c, AC6, AC7, AC8a, AC8b, AC9, AC9b, AC10, AC10b, AC10d, AC10e all green; tabular pass/fail report rendered.

**Commit boundary**: Phase 7 commit — "Phase 6b Phase 7: smoke tests + cross-phase regression verification".

### Step 8 — E2E visual baseline + regression on `:8000` (Phase 8, scheduled)

**8.1 `src/tests/e2e_ui/test_multiplexer_phase6b_visual.py`** (NEW) — mirrors Phase 6a visual pattern:
- `BASE_URL` from `LUPIN_API_URL`
- `_INJECT_FIXTURES_JS` — FIXED timestamps for ≥3 action_required prompts (one per `response_type`) + ≥1 audio playback fixture
- `_STABILIZE_DOM_JS` — pin every `.action-required-countdown` text + `.tts-current-track` text to fixed strings before screenshot
- `assert_snapshot(action_required_pane_locator, name="multiplexer_phase6b_action_required.png")` + `assert_snapshot(tts_pane_locator, name="multiplexer_phase6b_tts_chrome.png")`
- Snapshots land under `io/test-suite/visual-baselines/test_multiplexer_phase6b_visual/`

**8.2 AC11a (HUMAN slot-coordination, AI executes)**:
- Before submitting, AI requests `scheduled_at` slot from user via cosa-voice (`ask_yes_no` or `converse`) — user owns `:8000` calendar visibility per `feedback_test_server_monopolize_mode`
- Submit via `/schedule-tests` skill (per `feedback_use_schedule_tests_skill`) with `test_types: "e2e"` + `pytest_args: "-k multiplexer_phase6b --update-snapshots"` + `auto_fix_on_failure: false`. This is **Run #1** (baseline capture).
- Field-name traps remembered: `test_types="e2e"` NOT `"e2e_ui"` (per `feedback_test_types_e2e_not_e2e_ui`); pass-through key is `pytest_args` NOT `args` (per `feedback_test_suite_submit_field_pytest_args`).

**8.3 AC11b**:
- Wait for Run #1 completion via Docker container log inspection (per Phase 5/6a precedent — `/api/test-suite/status/<id>` does not exist; use container logs)
- Submit **Run #2** (regression check, no `--update-snapshots`) at a separate user-confirmed slot
- After Run #2 completes:
  - Assert `find io/test-suite/visual-baselines/test_multiplexer_phase6b_visual/ -type f -name "*.png" | wc -l` > 0
  - Container log contains `Test suite complete` + `e2e: 1 passed, 0 errors` for Run #2

**Verify at end of Step 8**: AC11a + AC11b green; baselines committed (with explicit user approval).

**Commit boundary**: Phase 8 commit — "Phase 6b Phase 8: E2E visual baseline + regression (AC11a + AC11b)".

## Critical files (full inventory)

**NEW (Phase 6b)** — 12 files:
- `src/fastapi_app/static/js/multiplexer/render/ActionRequiredRenderer.ts` (Phase 3)
- `src/fastapi_app/static/js/multiplexer/render/TtsChromeRenderer.ts` (Phase 4)
- `src/fastapi_app/static/js/multiplexer/render/templates/actionRequiredInteractive.ts` (Phase 2)
- `src/fastapi_app/static/js/multiplexer/render/templates/ttsChrome.ts` (Phase 2)
- `src/fastapi_app/static/css/multiplexer/action-required.css` (Phase 6.1)
- `src/fastapi_app/static/css/multiplexer/tts-chrome.css` (Phase 6.2)
- `src/tests/unit/multiplexer/render/templates_action_required_interactive.test.ts` (Phase 2.3)
- `src/tests/unit/multiplexer/render/templates_tts_chrome.test.ts` (Phase 2.3)
- `src/tests/unit/multiplexer/render/action_required_renderer.test.ts` (Phase 3.2)
- `src/tests/unit/multiplexer/render/tts_chrome_renderer.test.ts` (Phase 4.2)
- `src/tests/unit/multiplexer/stores/jobstore_delete_api.test.ts` (Phase 5A)
- `src/tests/smoke/test_multiplexer_phase6b_smoke.py` (Phase 7.1)
- `src/tests/e2e_ui/test_multiplexer_phase6b_visual.py` (Phase 8.1)

**EDITED** — 11 files:
- `src/fastapi_app/static/js/multiplexer/stores/ActionRequiredStore.ts` (Phase 1.1 + 1.2 — `respondAndAwait` + widen `respond()`)
- `src/fastapi_app/static/js/multiplexer/stores/AudioStore.ts` (Phase 1.3 — `stop()`)
- `src/fastapi_app/static/js/multiplexer/stores/JobStore.ts` (Phase 5A — `delete()`)
- `src/fastapi_app/static/js/multiplexer/render/NotificationsListRenderer.ts` (Phase 1.4 — ownership-flag guard)
- `src/fastapi_app/static/js/multiplexer/render/JobsPaneRenderer.ts` (Phase 5B — delete-button click handler)
- `src/fastapi_app/static/js/multiplexer/render/index.ts` (Phase 6.6 — barrel exports)
- `src/fastapi_app/static/js/multiplexer/shared/types.ts` (Phase 6.7 — BootCompletePayload extension)
- `src/fastapi_app/static/js/multiplexer/boot.ts` (Phase 6.5 — A7/A8 ordering + new mounts)
- `.stylelintrc.json` (Phase 6.3 — `overrides` blocks)
- `src/fastapi_app/static/html/multiplexer.html` (Phase 6.4 — CSS links)
- `src/fastapi_app/static/html/dev-tools.html` (Phase 6.8 — line 145 text)
- `src/tests/unit/multiplexer/stores/action_required_store.test.ts` (Phase 1.1 + 1.2 — new test cases)
- `src/tests/unit/multiplexer/stores/audio_store.test.ts` (Phase 1.3 — new test cases)
- `src/tests/unit/multiplexer/render/notifications_list_renderer.test.ts` (Phase 1.4 — guard tests)
- `src/tests/unit/multiplexer/render/jobs_pane_renderer.test.ts` (Phase 5B — AC5c cases)

**TRACKING DOCS** (Phase 0):
- `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/90-execution-log.md` (Phase 0 — Phase 6b section seed)
- `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/2026.05.11-phase6b-code-execution-plan.md` (this file)

## Reused functions / utilities (existing — DO NOT re-implement)

- `ActionRequiredStore.respond()` (existing optimistic; backward-compat retained) — `stores/ActionRequiredStore.ts:182-206`
- `ActionRequiredStore.tick` events at 1Hz — `ActionRequiredStore.ts:291`
- `ActionRequiredItem.expires_at: number` (ms epoch, already on `shared/types.ts:344`)
- `AudioStore.pause()`, `resume()`, `skip()`, `state()`, `queueLength()`, `binaryHandler` — `AudioStore.ts:109-119`
- `AudioStore` events `store_audio_state_change` + `store_audio_chunk_decoded` — `AudioStore.ts:280-323`
- `actionRequiredReadOnly.ts:71-109` switch-on-`response_type` precedent (REUSE RE-28)
- `JobStore.bucket(name)`, `getById(idHash)`, etc. (Phase 6a precedent) — `stores/JobStore.ts`
- `keyedListMerge` keyed by `data-id-hash` — `render/dom.ts:75-118`
- `html` tagged-template + `raw` opt-out — `render/html.ts:241-262`, `:83-85`
- `formatHM`, `formatDateKey`, `formatDuration` (added in Phase 6a) — `render/time.ts:54-71`, `:89-108`, Phase 6a `formatDuration`
- `EventBus` `bus.on(...)` / `bus.emit(...)` — `shared/EventBus`
- Factory shape from Phase 6a `createJobsPaneRenderer` — `render/JobsPaneRenderer.ts`
- Phase 6a `unsubscribers: Array<() => void>` lifecycle pattern
- Phase 6a visual-snapshot test scaffolding — `src/tests/e2e_ui/test_multiplexer_phase6a_visual.py`
- Phase 6a smoke-test scaffolding — `src/tests/smoke/test_multiplexer_phase6a_smoke.py`
- Phase 4/5/6a `c8 --100` invocation

## Verification (end-to-end pass criteria)

Tabular AC scorecard (per CLAUDE.md test-ownership mandate; AI executes; no user manual QA):

| AC | Command | Pass criterion |
|---|---|---|
| AC1 | `npx tsc --noEmit -p tsconfig.json` | exit 0 |
| AC2 | `npx eslint src/fastapi_app/static/js/multiplexer/` | exit 0 |
| AC2a | grep `data-phase6-pending` on action-required widgets post-mount | 0 hits |
| AC2b | grep `aria-disabled="true"` on action-required widgets post-mount | 0 hits |
| AC2c | unit test: 1 MutationObserver childList entry per widget; 4 markers gone | green |
| AC2d | `vitest jobstore_delete_api.test.ts` + tsc | green |
| AC2e (NEW per Pass 2 a1) | `! grep -nE "\.innerHTML\s*=\|rawHTML\(\|\.outerHTML\s*=" actionRequiredInteractive.ts ttsChrome.ts` | exit 0 (no hits) |
| AC3 | `vitest templates_action_required_interactive.test.ts` | ≥15 PASS |
| AC4 | `vitest templates_tts_chrome.test.ts` | ≥15 PASS |
| AC5 | `vitest action_required_renderer.test.ts` | ≥21 PASS (per AC5 enumeration sub-table) |
| AC5b | `vitest tts_chrome_renderer.test.ts` | ≥13 PASS (per AC5b enumeration sub-table) |
| AC5c | `vitest jobs_pane_renderer.test.ts` (AC5c cases) | ≥6 new cases PASS |
| AC6 | `c8 --100` on all new + edited multiplexer TS files | exit 0 |
| AC7 | `gzip -9 -c boot.<hash>.js | wc -c` | ≤ `B6a + 8192` |
| AC8a | smoke `test_phase6b_functional_smoke` + `test_phase6b_no_pending_markers_after_mount` | all PASS |
| AC8b | smoke `test_phase6b_perf_gate` | 1/1 PASS |
| AC9 | smoke `test_phase6b_boot_complete_handshake` (canonical order asserted) | 1/1 PASS |
| AC9b (NEW per Pass 2 A8) | smoke `test_audio_chunks_arrive_after_mount` | 1/1 PASS |
| AC10 | (cross-phase enumerated 7.2 above) | all green |
| AC10b | LOC: action-required.css ≤500, tts-chrome.css ≤700 + stylelint | exit 0 |
| AC10d | grep + stylelint + canary on both new CSS files | all clean |
| AC10e | `pytest ... -k "pending_count"` (Phase 5 + 6a regression with floor=0) | exit 0 |
| AC11a | `/schedule-tests` submission Run #1 (after user slot confirm) | HTTP 200 + `submission_id` |
| AC11b | Run #2 visual-baselines non-empty + container log "1 passed, 0 errors" | both true |

## Pre-exit self-audit (per `feedback_audit_plans_at_execute_time`)

| Memory | Compliance | Status |
|---|---|---|
| `feedback_documentation_step_stops_at_doc` | User explicitly opened the code-execution cycle ("let's go ahead and get started"). Per `feedback_plan_approval_means_go`, ratification of THIS plan is the green light for Phase 1. | ✅ |
| `feedback_phase0_serialization_prominence` | Phase 0 serializes BOTH the execution log seed AND this implementation plan BEFORE any code edit. Step 0 is explicit + named "Phase 0" at the top of the execution sequence. | ✅ |
| `feedback_audit_plans_at_execute_time` | This audit table IS the pre-exit re-audit. | ✅ |
| `feedback_plans_include_tracking_docs` | Design doc 09 + this plan + execution log 90 + findings doc 95 = four artifacts. | ✅ |
| 100% c8 coverage mandate (multiplexer TS) | AC6 + Phase 2.4 + Phase 3.3 + Phase 4.3 + Phase 5A.7 + Phase 5B.10 all gate on `c8 --100`. | ✅ |
| User-is-never-tester | Every AC executor is AI; AC11a HUMAN is calendar-only. | ✅ |
| `feedback_lupin_only_never_cosa` | All client-side TS edits are under parent Lupin paths (`src/fastapi_app/static/js/multiplexer/`); the ONE potential CoSA-side prereq (server-handler shape acceptance for A2) is recon-only + filed as a separate CoSA-context task if needed. NO git ops in `src/cosa/`. | ✅ |
| `feedback_no_defensive_programming` | Renderer + store code uses explicit `=== undefined` / `=== null` checks; no `getattr`-style defensive chains. | ✅ |
| `feedback_never_auto_commit_push` | Per-phase commits ALL gated on explicit user approval. NO commit happens without a yes per commit boundary. | ✅ |
| `feedback_tests_parameterize_base_url` | Smoke + E2E read `LUPIN_API_URL` (default `http://localhost:7999`). | ✅ |
| `feedback_test_server_monopolize_mode` | AC11a/AC11b submit via `/schedule-tests` skill with user-confirmed `scheduled_at`; never side-door. | ✅ |
| `feedback_e2e_two_phase_gate` | Run #1 baseline capture is separate from Run #2 regression check; user confirms slot for each. | ✅ |
| `feedback_test_types_e2e_not_e2e_ui` | AC11a/AC11b explicitly use `test_types: "e2e"` NOT `"e2e_ui"`. | ✅ |
| `feedback_test_suite_submit_field_pytest_args` | AC11a/AC11b explicitly use `pytest_args` NOT `args`. | ✅ |
| `feedback_sweep_for_pattern_offenders` | Pass 2 already swept; convergence re-grep clean. Phase 0 prereq #4 (mount-surface verification) re-checked at execute time. | ✅ |
| `feedback_skip_rnd_doc_for_trivial_fixes` | NOT applicable — Phase 6b is non-trivial. | ✅ N/A |
| `feedback_enumerate_all_activation_paths` | Phase 6b surfaces enumerated: pane chrome + per-notification corner buttons + interactive widget click handlers + delete-button delegation. | ✅ |
| `feedback_naming_underscore_not_abbreviations` | TS file names follow Phase 5/6a camelCase precedent; Python tests use underscore-separated names. | ✅ |
| `feedback_fix_all_failing_tests` | If AC10 cross-phase sweep surfaces ANY failure (incl. pre-existing), fix in same session. | ✅ (commitment) |
| `feedback_comprehensive_automated_testing` | All test layers covered: unit (templates + renderers + stores) + smoke (functional + perf + AC9/AC9b + AC10d canary) + visual E2E. | ✅ |
| `feedback_use_schedule_tests_skill` | AC11a/AC11b explicitly route through `/schedule-tests`. | ✅ |
| `feedback_acknowledge_receipt_before_tool_work` | Conversation mode currently OFF (toggled 2026-05-11); conditional if toggled mid-cycle. | ✅ (conditional) |
| `feedback_recraft_speech_dont_pipe_terminal` | Same conditional. | ✅ (conditional) |
| `feedback_no_duplicate_notify_in_conversation_mode` | Same conditional. | ✅ (conditional) |

**Conclusion**: No memory violations detected; ready to begin Phase 1 after user ratifies this plan and Step 0 (tracking-doc seed) completes.

## Resume pointer

After user-ratification of THIS plan, the next concrete action is:

1. **Phase 0 (Step 0)**: append the Phase 6b section to `90-execution-log.md` with per-phase progress table + AC scorecard placeholders. This file (`2026.05.11-phase6b-code-execution-plan.md`) is already serialized — Phase 0 partially complete.
2. **Capture `B6a`** at HEAD `243267b` (Phase 6a AC11a/AC11b closure). Update AC7 ceiling in this doc.
3. **Begin Phase 1** (Step 1): the four store / renderer prerequisite edits, with the commit boundary "Phase 6b Phase 1: store API prereqs (respondAndAwait + widen respond + AudioStore.stop) + Phase 5 ownership-flag guard".

User gate per `feedback_never_auto_commit_push`: every phase commit requires explicit "commit" authorization. Each commit's diff scope is sized to be reviewable in one sitting (the 5A → 5B split + 1.1/1.2/1.3/1.4 sub-step granularity inside Phase 1 keeps the largest commit at ~5 file edits).
