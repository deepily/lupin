# Phase 5 — Plan Review Pipeline Findings (Pre-Approval Consolidation)

**Date**: 2026-05-05
**Status**: 🟢 **CLOSED 2026-05-05** — all 12 D-tier decisions ratified; Resolution Loop round 1 fixes applied; convergence re-greps clean per PIP §10 termination rule; **user gave final go-ahead 2026-05-05**. Phase 0 deliverable #6 → ✅. Next step: separate plan-mode session plans Phase 5 code execution.

**Convergence (PIP §7 step 5)**:
- Pass 1 re-grep: 1 residual hit (`2026.05.05-phase5-pre-design-exploration.md:365` — F2-resolved inherited Phase 6 marker; verbatim Phase 4 quote, NOT a Phase 5 TBD).
- Pass 2 re-grep: 1 residual hit (`2026.05.05-phase5-pre-design-exploration.md:388` — Q9 narrative "manual removal request"; benign per original Pass 2 analysis).
- Zero new TBD/Manual/`EXECUTOR: HUMAN`-without-justification/bare-checkbox offenders introduced by Resolution Loop fixes.
- 16 `EXECUTOR: AI` tags adopted across the AC table (canonical PIP schema firmly in place — A1 resolved).

Per PIP §10 termination rule: zero new structural findings + every approved fix reflected → loop closes. Awaiting final user go-ahead before `90-execution-log.md` Phase 0 deliverable #6 → ✅ and code-implementation cycle (separate plan-mode session).
**Pipeline run**: REUSE pre-pass + Pass 1 Fitness + Pass 2 Adversarial (all three Agents in parallel; fresh-context per canonical PIP spec)
**Plan docs under review**:
- `06-phase5-renderer-design.md` (~390 lines, drafted 2026-05-05; Q-A through Q-L ratified 2026-05-05)
- `2026.05.05-phase5-pre-design-exploration.md` (~547 lines, Explore-agent citation backing)

This document is the user-facing **batched review summary** — three Agent passes consolidated into one ratification surface. Once user picks per-finding `Apply` / `Reject` / `Defer`, AI applies approved fixes per PIP §7 Resolution Loop, runs convergence re-greps, and updates the design doc's idempotency marker. After all three passes converge cleanly, user gives final go-ahead and `90-execution-log.md` Phase 5 section moves Phase 0 deliverable #6 to ✅. Implementation cycle (separate plan-mode session) follows.

**No code is written until the entire pipeline closes with user approval.**

---

## TL;DR — Decisions ratified 2026-05-05

All twelve D-tier decisions ratified in interactive session 2026-05-05. Recommendation column shows the ratified outcome. Where the user chose the recommended option, the row reads `✅ Ratified — <option>`. Where the user redirected, the row reads `✅ Ratified — <chosen path>` with the redirect noted.

| # | Decision | Outcome |
|---|----------|---------|
| **D-A** | `.ar-*` vs Q-C | ✅ Ratified Option A — rename `.ar-*` → `.action-required-*` matching legacy; reuse legacy CSS for layout/typography. Applied to design doc §Action-required read-only template. |
| **D-B** | Notification interface gaps | ✅ Ratified Option A — extend `shared/types.ts:Notification` with 5 new optional fields (`voice_persona`, `abstract`, `progress_group_id`, `was_expired`, `time_display`); update `NotificationStore.normalize()`; ~5 unit tests; Phase 4 design-doc amendment. Applied to design doc §Critical files Edited list. |
| **D-C+D-D** | AC7 baseline + gzip mode | ✅ Ratified — Phase 4 baseline captured at `gzip -9 -c boot.<hash>.js \| wc -c` = **24,325 bytes** (frozen literal in `90-execution-log.md` Phase 4 closure section); AC7 ceiling = 55,045 bytes. Applied to design doc §`boot.js` size threshold + AC7 row. |
| **D-E** | AC8 fixture mechanism | ✅ Ratified Option A — `page.evaluate(eventBus.emit("notification_queue_update", ...))` direct, bypasses transport. Applied to design doc AC8a. |
| **D-F** | AC11 split + human gate | ✅ Ratified — AC11a (submission) + AC11b (post-run state) + new `Human gate` column with explicit slot-availability justification ("NOT tester duty"). Applied to design doc AC table. |
| **D-G** | Phase 1 smoke selector | ✅ Ratified — Phase 5 cycle bundles `test_multiplexer_phase1_smoke.py` selector update. Applied to design doc §Critical files Edited list + §Page shell D-G note. |
| **D-H** | `formatCountdown` purity | ✅ Ratified — pure formatter; offset stays in `ActionRequiredStore`. Applied to design doc §Module structure (`render/time.ts` row) + §Action-required template (countdown invariant paragraph). |
| **D-I** | singular → plural store keys | ✅ Ratified — `stores: { notifications, senders, actionRequired }` matching `StoreSet`. Applied to design doc §NotificationsListRenderer Lifecycle. |
| **D-J** | 3 citation fixes + `renderMarkdownInline` | ✅ Ratified — DOMPurify `12203-12247` (block) + `12279-12305` (inline); date-key formatter `10068-10098`; progress-group toggle `10398-10412`; `renderMarkdownInline` added to `render/markdown.ts` spec with snapshot test. Applied to design doc §Markdown rendering, §Reused functions, §Q-decisions Q-E row, §Plan-review pipeline scope. |
| **D-K** | Perf budget split + marker assertion | ✅ Ratified — AC8 splits into AC8a (500ms / 3-fixture functional) + AC8b (100ms / 50-fixture perf gate); `data-phase6-pending` count ≥ 3 assertion added to AC8a. Applied to design doc AC table. |
| **D-L** | Action-required mount location | ✅ Ratified — `#action-required-section` (above) + `#sender-cards-container` (below) as direct children of `#notifications-pane`. Applied to design doc §Page shell + §Action-required template (mount-location paragraph). |

**Resolution Loop round 1 status**: complete. Convergence re-greps pending. After re-greps clean, user gives final go-ahead and `90-execution-log.md` Phase 0 deliverable #6 → ✅. Implementation cycle (separate plan-mode session) follows.

---

## Pre-ratification analysis (preserved for traceability)

The original D-A through D-L analysis with options, tradeoffs, and recommendations is preserved below for traceability — these were the surfaces on which the user made their decisions.

| # | Decision | Why it's blocking | Recommendation |
|---|----------|-------------------|----------------|
| **D-A** | **`.ar-*` namespace silently violates Q-C ("keep existing class names")** — Q-H templates introduce `.ar-prompt`, `.ar-response-type-badge`, `.ar-countdown`, `.ar-options-preview`, `.ar-default`, `.ar-pending-notice`, `.ar-option-readonly`, `.ar-input-placeholder` which do NOT exist in legacy `notifications.css`. Legacy uses `.action-required-notification` / `.action-required-header` / `.action-required-title` / `.response-buttons` / `.response-button.yes/no` / `.action-required-timer`. | Q-C ratification body explicitly says "keep existing names verbatim — rename buys nothing functional." Inventing `.ar-*` is a silent override of the just-ratified decision. | **Option A**: Rename `.ar-*` → `.action-required-*` matching legacy + reuse existing CSS rules during the port (full Q-C compliance). **Option B**: Add explicit Q-C carve-out clause: "Q-C applies to all classes EXCEPT the action-required widget; rationale: legacy structure was tightly coupled to interactive handlers and a fresh namespace is preferable for the read-only Phase 5 widget." Recommended: **A** — preserves Q-C's coherence; minimal extra cost. |
| **D-B** | **`Notification` typed shape misses 5 fields the renderer needs** — `voice_persona` (for `--persona-color` CSS var), `abstract` (for indicator), `progress_group_id` (for Q-G lazy-render), `was_expired` (for EXPIRED badge), `time_display` (backend-provided "23:10 EST" string). Phase 4 D-G/Q2 ratified persistence as "unread count only"; `Notification` interface at `shared/types.ts:237-250` deliberately surfaces only 9 fields. `NotificationStore.normalize()` only stores fields named on the interface. | Renderer cannot reach raw envelope via Phase 4 public API. Multiple Phase 5 features assume data the store doesn't surface. | **Option A**: Extend `shared/types.ts:Notification` to surface the 5 renderer-required fields + extend `NotificationStore.normalize()` to copy them through. Phase 4 store interface change initiated by Phase 5; not in scope of "Phase 5 = renderer only" framing — ratify the interface bump explicitly. **Option B**: Add a `getRaw(idHash)` method to `NotificationStore` for lazy access to raw envelope. **Option C**: Descope Phase 5 to drop `--persona-color`/abstract/progress-group/expired/time-display features and accept visual-parity loss. Recommended: **A** — cheapest path to feature-parity; Phase 4 interface stability not yet load-bearing for production use. |
| **D-C** | **AC7 has no captured Phase 4 baseline byte count** — design says "compared to logged Phase 4 figure" but no such figure is in `90-execution-log.md` or `91-phase4-review-findings.md`. Pre-design exploration §3.3 notes `boot.js ~79 KB` minified but does NOT state the gzipped size. AC7 is unexecutable without a literal baseline. | Phase 5 Block: AI cannot run AC7 against an undefined baseline. Compounds with **D-D** below (gzip mode mismatch). | Capture the Phase 4 final `gzip -9 -c dist/multiplexer/boot.js \| wc -c` byte count BEFORE Phase 5 implementation begins; record into `90-execution-log.md` Phase 4 closure section as a frozen byte literal (e.g. `27431 bytes`); AC7 references that line:hash. Action item before code: `EXECUTOR: AI` runs the command + commits the figure into 90-log Phase 4 section as an addendum. Recommended: **execute now, before Phase 5 implementation**. |
| **D-D** | **Gzip mode contradiction in AC7** — AC7 row says `gzip -9 -c`; Phase 4 A5 (referenced via Q-I context) says "30 KB gzipped" without flag. `gzip -9` (max compression) gives different bytes than default `gzip -6`. If Phase 4 baseline captured at one mode and AC7 measures at another, threshold comparison is meaningless. | Same byte literal must be measured by the same compression mode for comparability. | Pin Phase 4 baseline + Phase 5 measurement to identical command: explicitly `gzip -9 -c <path> \| wc -c`; state in AC7 row AND retroactively recapture Phase 4 baseline at `-9` if it was captured at a different level. Recommended: **`gzip -9` (most reproducible, max compression — strictest threshold)**. |
| **D-E** | **AC8 fixture-injection mechanism unspecified** — "three fixture notifications (plain, markdown, action-required) render within 500ms" — but how do they arrive? Server WS replay? `page.evaluate` direct into NotificationStore? Test-only POST endpoint? Phase 4 F10/A2 forced this for AC7 (audio chunks); Phase 5 inherits the gap. Compounds with the Phase 4 D-D pattern. | AC8 unexecutable as written; implementation team will improvise differently across runs. | Pick one and write the assertion shape into AC8: **(a)** `page.evaluate` injects fixture envelopes via `eventBus.emit("notification_queue_update", {...})` direct (matches Phase 4 D-D pattern; bypasses transport — recommended); **(b)** Reuse `/api/notify` real endpoint with auth-token-as-test-user; **(c)** New `/api/test/inject-notification` endpoint. Note: action-required items go via `eventBus.emit("notification_queue_update", {...action_required: true, response_type: "yes_no", expires_at: now+30000})` — same envelope shape, different flag. Recommended: **(a) — matches Phase 4 D-D, no new endpoint, fastest**. |
| **D-F** | **AC11 conflates submission, post-run state, and snapshot-baselines existence** — "First-run E2E baseline capture on `:8000` (scheduled) — captures fresh `__snapshots__/multiplexer-phase5/` images." Two problems: (1) "captures fresh" describes side-effect not assertion; (2) Executor is "AI test-suite submit" but acceptance is the snapshot directory state — split submission from acceptance. | AI must assert files exist + tests pass, not that the run ran. Hand-off ambiguity at human-gate ("`scheduled_at` confirmed by user"). | Split AC11: **AC11a** `EXECUTOR: AI` — submission via `POST /api/test-suite/submit` with user-confirmed `scheduled_at` returns 200 + `submission_id`. **AC11b** `EXECUTOR: AI` (post-run, via test-suite completion watchdog or polling `/api/test-suite/status/<id>` to terminal state) — assert `find src/tests/e2e_ui/__snapshots__/multiplexer-phase5/ -type f -name "*.png" \| wc -l > 0` AND test-suite final state `=== "passed"`. **Human gate** (justified: `external slot-availability calendar — user owns :8000 schedule`): user confirms `scheduled_at` does not collide before AC11a fires. NOT tester duty. Recommended: **adopt the split as written**. |
| **D-G** | **Phase 1 smoke regression risk — placeholder removal silently breaks AC10** — Phase 5 removes `<p data-testid="multiplexer-phase1-placeholder">` from `multiplexer.html:13-15`. Phase 1 smoke at `src/tests/smoke/test_multiplexer_phase1_smoke.py` references this testid. AC10 ("Phase 1/3/4 verification suites still green") wording implies no changes; placeholder removal silently breaks AC10. | AC10 detection happens at AI bash run, not at design review — exactly the surface PIP REUSE pre-pass exists to catch. | Add explicit note: "Phase 5 also updates `test_multiplexer_phase1_smoke.py` to assert `multiplexer-notifications-pane` instead of `multiplexer-phase1-placeholder`." Add to "Edited" file list in the "Critical files" section of the design. Recommended: **Yes — make the test-selector update part of the Phase 5 implementation cycle, not a follow-up**. |
| **D-H** | **`formatCountdown(ms)` clock-offset double-application risk** — design lists `formatCountdown(ms)` as "server-clock-offset aware." But `ActionRequiredStore.ts:157, :302` already does `expires_at - (nowFn() + clockOffset)` and EMITS `countdownMs` already-corrected via `store_action_required_changed { changeKind: "tick", countdownMs }`. If renderer's `formatCountdown(ms)` ALSO applies offset, countdown corrected twice. | Cumulative drift on every tick — undetectable without explicit invariant test. | Design should explicitly state `formatCountdown(ms)` is a PURE formatter (e.g. `5023` ms → `"00:05"`); offset reconciliation owned by ActionRequiredStore. Drop the "server-clock-offset aware" qualifier. Add unit-test invariant: `formatCountdown(5000) === "00:05"` regardless of `Date.now()` or any global offset state. Recommended: **purify the formatter**. |
| **D-I** | **Singular vs plural store keys (TS compile bug)** — design `06-phase5-renderer-design.md:77` writes `mount(root: HTMLElement, deps: { eventBus, stores: { notification, actionRequired, sender } })` — singular `notification`/`sender`. Actual `StoreSet` at `stores/index.ts:36-42` uses plural `notifications`/`senders`. AC1 (`tsc --noEmit`) will fail at compile. | Trivial wording fix; flagged because users approving the design as-written might miss it. | Use `stores: { notifications, senders, actionRequired }` (matching `StoreSet`). Recommended: **trivial accept**. |
| **D-J** | **Three wrong line citations in design + pre-design** — DOMPurify config cited at `notifications.js:12490-12500`; actual is `12203-12247` (block) + `12279-12305` (inline). Date-key formatter cited at `notifications.js:10365-10395`; actual is `10068-10074` (`getDateString`) + `10084-10098` (`extractDateFromTimestamp`). Pre-design `2026.05.05-phase5-pre-design-exploration.md:1.3` cites progress-group toggle at `notifications.js:10678`; actual is `10398-10412`. | If the implementation team uses these as anchors during port, they will hit dead ranges. The cited line `10365-10395` is `createDateAccordion` — entirely unrelated. | Correct three citations. Also: legacy provides BOTH a block markdown renderer AND an inline variant (`renderMarkdownInline` at `12268-12313`); Phase 5 needs INLINE rendering for `.message-text` inside `.sender-message` (chat-bubble case where wrapping `<p>` is wrong). Design proposes only one wrapper; needs both. Recommended: **fix citations + add `renderMarkdownInline` to `render/markdown.ts` spec**. |
| **D-K** | **Three Pass 2 Layer 3 design concerns** — (1) AC11 acceptance shape (handled in **D-F** above); (2) Performance budget conflict — 100ms target for 50-notification fixture (test-pyramid prose) vs 500ms budget for 3-notification fixture (AC8); (3) `data-phase6-pending="true"` marker contract surface but no AC asserts presence after Phase 5 ships. | Performance budgets contradict; future Phase 6 implementer searching for `[data-phase6-pending]` finds nothing if Phase 5 silently drifted. | **(2)**: Drop the 100ms target as informational only OR promote to AC8b with the 50-notification fixture as the canonical perf target — pick one merge gate. Recommended: **promote to AC8b** so both fixture sizes contribute distinct assertions. **(3)**: Add to AC8: `page.locator('[data-phase6-pending="true"]').count() >= 3` (2 hidden panes + each rendered action-required widget). Recommended: **add the marker assertion**. |
| **D-L** | **Action-required widget mount location undefined** — Q-H DOM example shows the widget element but its parent container is undefined. Sender → date → message hierarchy doesn't accommodate items with `prompt` (not `message`) that live in `ActionRequiredStore` (not `NotificationStore`). | Implementation team will improvise mount hierarchy differently across attempts. | Pin mount location explicitly. Recommended: a `#action-required-section` child of `#notifications-pane` rendered ABOVE all sender cards, holding the per-prompt widgets. State in §DOM grouping and §Page shell. Recommended: **adopt above-sender-cards mount**. |

After D-A through D-L are decided, the remaining ~30 findings (Pass 1 ≥ Major + Pass 2 wording ≥ Major) are fixable in the Resolution Loop without further user input.

**Layer 3 design concerns summary**: REUSE flagged 6 (#1-#6 — folded into D-A, D-B, D-I, D-J, D-H, D-G above); Pass 1 flagged 0 (clean); Pass 2 flagged 3 (folded into D-F, D-K above).

---

## REUSE Pre-Pass Findings

**Verdicts**: 3 reuse-as-is + 7 extend-existing + 12 genuinely-new + 5 design-conflict.

| ID | New thing the plan proposes | Existing prior art (file:line) | Verdict | Recommended action |
|---|---|---|---|---|
| RE-1 | `render/html.ts` tagged-template helper (~120 LOC), returns `DocumentFragment` | None — grep `html\``, `TemplateStringsArray`, `function html`, `const html` across `static/js/` returned nothing | genuinely-new | Accept as new. No prior art to reuse. |
| RE-2 | Trusted Types `lupin-html` policy registration (Q-J) | `static/lupin-mobile-test/flutter.js` already registers `trustedTypes.createPolicy("flutter-js", {createScriptURL})` on the same origin; `vendor/purify.min.js:2` (DOMPurify v3.3.1) auto-registers `dompurify` policy when `window.trustedTypes` exists | extend-existing **+ design-conflict** | Confirm `lupin-html` does not collide with existing `dompurify` and `flutter-js` policy names — name-scoped so OK, but Phase 7 CSP-enforcement `trusted-types lupin-html` directive must include `dompurify` (and `flutter-js` if mobile-test is co-served) or the page breaks. Add as Phase 7 prerequisite. |
| RE-3 | `render/markdown.ts` — uses page-loaded `window.marked` + `window.DOMPurify`; cites DOMPurify config from `notifications.js:12490-12500` | Actual config block lives at `notifications.js:12203-12247` (block-level `renderMarkdown`) plus `12279-12305` (inline variant `renderMarkdownInline`). Cited line range is wrong — actually `addActionRequiredNotification`. | extend-existing **+ design-conflict (citation)** | **D-J**. Port DOMPurify config + post-process target/rel rewriting verbatim from `notifications.js:12203-12247` (block). Also port `renderMarkdownInline` (12268-12313) — Phase 5 needs INLINE rendering for `.message-text` inside `.sender-message` (chat-bubble case where wrapping `<p>` is wrong). Design proposes only one wrapper; needs both. |
| RE-4 | `render/time.ts` — `formatHM(ts)`, `formatDateKey(ts)`, `formatCountdown(ms)`; "server-clock-offset aware" | (a) Date-key formatter actually at `notifications.js:10068-10074` (`getDateString`) + `10084-10098` (`extractDateFromTimestamp` — appTimezone-aware). Cited `10365-10395` is wrong — that's `createDateAccordion`. (b) Time formatter (HH:MM 24-hour) at `notifications.js:10107-10121` (`getLocalTimeDisplay`). (c) `clockOffset` arithmetic ALREADY lives inside `ActionRequiredStore.ts:157, :302, :347`; the store EMITS `countdownMs` already-corrected on `tick`. | extend-existing **+ design-conflict** | **D-J** + **D-H**. Fix line citations: `10068-10098` (date) and `10107-10121` (time). `formatCountdown(ms)` should NOT re-do clock-offset math — store already did it. Drop "server-clock-offset aware" qualifier from formatter spec. Decide `appTimezone` handling: legacy uses server-config field via `fetchClientConfig`; Phase 5 either pulls similarly OR accepts browser-local-tz only. |
| RE-5 | `render/dom.ts` — `replaceChildren`, `keyedListMerge` (key by `data-id-hash`) | None — no `keyedListMerge` / DOM-diff prior art in `multiplexer/` or `notifications.js`. Legacy uses imperative DOM manipulation without keyed reconciliation. | genuinely-new | Accept as new. Note: `Element.prototype.replaceChildren` is a native DOM method (Baseline 2020) — wrapper may be unnecessary; `keyedListMerge` is the genuinely-novel piece. |
| RE-6 | `render/NotificationsListRenderer.ts` — lifecycle `mount(root, deps) / unmount()`; subscribes to store-events | EventBus subscription pattern at `multiplexer/shared/EventBus.ts:33-55` (`on()` returns unsubscribe closure) and used throughout Phase 4 stores. `unsubscribers: Array<() => void>` lifecycle pattern at `NotificationStore.ts:141`. | reuse-as-is (pattern) | Apply existing pattern: collect unsubscribe closures returned from `eventBus.on()` into `unsubscribers[]`; `unmount()` runs each. Don't invent new lifecycle primitives. |
| RE-7 | `mount(root, deps: { eventBus, stores: { notification, actionRequired, sender } })` interface | `stores/index.ts:36-42` defines `StoreSet { notifications, senders, actionRequired, audio, jobs }` — note PLURAL `notifications`/`senders` and presence of `audio`+`jobs` | extend-existing **+ design-conflict** | **D-I**. Singular vs plural mismatch will fail TypeScript compile. Either (a) change design to `stores: { notifications, senders, actionRequired }` matching `StoreSet`, or (b) document a destructured subset. AC1 (`tsc --noEmit`) catches at compile, but design will mislead implementers. |
| RE-8 | `render/templates/senderCard.ts` (`renderSenderCard(sender, notifications)`) | Legacy uses `createSenderCard(senderId, insertAtTop)` at `notifications.js:10178-10350` — imperative `innerHTML` with onclick attributes. New helper takes `Notification[]` collection + returns DOM, very different shape. | genuinely-new | Accept as new — verbatim DOM lift was architecturally blocked per design Context. |
| RE-9 | `render/templates/dateAccordion.ts` (`renderDateAccordion(dateKey, notifications)`) | Legacy `createDateAccordion(senderId, dateString)` at `notifications.js:10357-10422` — imperative; uses `style.display = 'none'` + onclick attr | genuinely-new | Accept as new — same architectural reasoning as RE-8. The legacy delegated click handler at `10398-10412` shows the toggle pattern (Q-G lazy-history). |
| RE-10 | `render/templates/notificationItem.ts` (`renderNotificationItem(notification, actionRequiredItem?)`) | Legacy `addMessageToDateAccordion` at `notifications.js:10475-10570` — imperative innerHTML; `updateSenderProgressGroupEntry` at `notifications.js:10620-10750` for in-place updates | genuinely-new | Accept as new. CSS class re-use (`.sender-message`, `.incoming`, `.outgoing`, `.expired-response`, `.progress-group-head`, `.progress-group-history`) per Q-C is the prior-art reuse. |
| RE-11 | `render/templates/actionRequiredReadOnly.ts` — `.action-required-widget`, `.ar-prompt`, `.ar-options-preview`, `.ar-countdown`, `.ar-default`, `.ar-pending-notice` CSS classes | Legacy uses entirely DIFFERENT class hierarchy: `.action-required-notification` / `.action-required-header` / `.action-required-title` / `.response-buttons` / `.response-button.yes/no` / `.action-required-timer` (CSS at `notifications.css:609-770+`; renderer at `notifications.js:13757-13880+`). Per `notifications.js:13782-13832`, legacy widget rendering is response-type-branched (`yes_no` → response-buttons; `open_ended` → input+mic; `multiple_choice` → `renderMultipleChoiceUI`; `open_ended_batch` → `renderOpenEndedBatchUI`) | genuinely-new **+ design-conflict (Q-C scope)** | **D-A**. Q-C ratified "keep existing class names verbatim — rename buys nothing functional." Design then INVENTS `.ar-*` namespace, contradicting Q-C. Rename to `.action-required-*` matching legacy + reuse existing CSS rules in port, OR add explicit Q-C carve-out. |
| RE-12 | `render/index.ts` barrel — `createNotificationsListRenderer({eventBus, stores})` factory | Pattern matches `multiplexer/transport/index.ts:31` (`createTransports`) + `multiplexer/stores/index.ts:67` (`createStores`) | reuse-as-is (pattern) | Apply factory shape exactly — same `create*` naming + `opts: { eventBus, stores, ... }` parameter object idiom. |
| RE-13 | `static/css/multiplexer/notifications-list.css` (port from `static/css/notifications.css` 5,010 LOC → ~800-1,200 LOC) | `static/css/notifications.css` actual line count is **5,040** (verified `wc -l`) — design says 5,010. | extend-existing | Port + strip per design plan. Update line-count citation (5,010 → 5,040) for accuracy. |
| RE-14 | Mount points `<section id="notifications-pane">` + Phase 6 placeholders (`#jobs-pane` + `#tts-pane` hidden) | Current `multiplexer.html` has NO mount points (only `<p data-testid="multiplexer-phase1-placeholder">` at line 13) | genuinely-new | Accept as new. Per Q-L ratification. **NOTE**: removing the placeholder breaks Phase 1 smoke (see RE-23 / D-G). |
| RE-15 | `<link rel="stylesheet">` + `<script src="/static/js/vendor/marked.min.js">` + `<script src="/static/js/vendor/purify.min.js">` page-shell additions | `vendor/marked.min.js` + `vendor/purify.min.js` ALREADY exist (verified `ls`). Legacy `notifications.html:968-969` ALREADY loads them with same paths. | reuse-as-is | Asset paths are correct. No new vendor files needed. |
| RE-16 | `BootCompletePayload.handlers.notificationsRenderer = "mounted"` extension | `shared/types.ts:381-385` already defines `BootCompletePayload { handlers: { audioBinary: string } }` | extend-existing | Add `notificationsRenderer: string` field to the existing interface; Phase 4's `BootCompletePayload` is already typed and emitted at `boot.ts:169-180`. AC9 reads via `page.on("console", ...)` — pattern established in Phase 4 D-C ratification. |
| RE-17 | Renderer needs `voice_persona`, `abstract`, `progress_group_id`, `notification_type`, `priority`, `time_display`, `was_expired` from notifications | `shared/types.ts:232-234` explicitly states these fields are "preserved on the raw envelope but not surfaced" on the typed `Notification` interface. `NotificationStore.normalize()` at `NotificationStore.ts:346-368` only stores fields surfaced on `Notification`. **Renderer cannot reach raw envelope via store API.** | extend-existing **+ design-conflict** | **D-B**. Design's render plans for `--persona-color` CSS var, abstract indicator, progress-group history, expired badges, time-display require NotificationStore extension. Either extend `Notification` interface + `normalize()`, add `getRaw(idHash)`, or descope Phase 5 features. **Currently the design assumes data the store does not surface.** |
| RE-18 | Test directory `src/tests/unit/multiplexer/render/*.test.ts` (subdirectory) | Phase 4 unit tests are FLAT under `src/tests/unit/multiplexer/`; no per-module subdirectories | extend-existing | Either (a) accept the per-module `render/` subdirectory and document divergence, or (b) keep flat (`html.test.ts`, `notifications_list_renderer.test.ts`, `templates_sender_card.test.ts`, etc.) matching Phase 4 precedent. Currently unmotivated divergence. |
| RE-19 | `src/tests/smoke/test_multiplexer_phase5_smoke.py` Playwright pattern | `src/tests/smoke/test_multiplexer_phase{1,3,4}_smoke.py` already exist with established Playwright + `LUPIN_API_URL` parameterization scaffolding | reuse-as-is (pattern) | Apply: copy Phase 4 smoke scaffolding shape (parametrize, console listener, `data-testid` selectors). |
| RE-20 | Progress-group toggle handler "lazy-render history on first toggle-expand click" (Q-G) | Legacy progress-group toggle at `notifications.js:10398-10412` (delegated handler) + `10620-10750` (`updateSenderProgressGroupEntry` history-prepend). Legacy renders ALL history eagerly; does NOT lazy-render. | extend-existing **+ design-conflict** | Q-G specifies lazy-render-on-first-expand which is a NEW behavior. Per **D-B** (RE-17), NotificationStore does NOT preserve `progress_group_id` or per-notification history slice. The store contract gap blocks the lazy-render plan as designed. Couples to D-B resolution. |
| RE-21 | `Notification[]` collection passed to `renderSenderCard(sender, notifications)` — needs grouping by `sender_id` then by `dateKey` | Legacy maintains `senderGroups: Map<senderId, {dateGroups: Map<dateString, Notification[]>}>` at `notifications.js:9991-10060`; SenderStore (Phase 4) maintains a flat `Map<sender_id, SenderRecord>` with `last_active_ts` only — no per-sender `Notification[]` aggregation | genuinely-new | Renderer must compute the sender→date→notifications grouping itself from `notificationStore.list()`. SenderStore alone does not provide it. Document the grouping function as a renderer responsibility. |
| RE-22 | Realistic gzipped budget table sums to "~+11.8 KB"; `+30 KB` AC7 ceiling | Phase 4 sets boot.js delta ceiling (per A5) at "≤ 30 KB gzipped vs Phase 3 baseline"; current `boot.js` is ~79 KB (per pre-design §3.3). Esbuild config at `build-multiplexer.sh` already uses `--minify` + `esm` + `--target=es2022` + `--keep-names`. **No captured baseline byte count.** | reuse-as-is (pattern) **+ design-conflict** | **D-C** + **D-D**. Inherit Phase 4 budget mechanism + AC machinery, but capture concrete baseline byte count + gzip-mode pinning before Phase 5 starts. |
| RE-23 | Removal of `<p data-testid="multiplexer-phase1-placeholder">` from `multiplexer.html:13-15` | Phase 1 smoke test at `src/tests/smoke/test_multiplexer_phase1_smoke.py` likely asserts presence of this exact testid | extend-existing **+ design-conflict (regression risk)** | **D-G**. Phase 1 smoke MUST be re-greened after the placeholder removal — either (a) Phase 5 design notes this and updates the Phase 1 test selector, or (b) AC10 ("Phase 1/3/4 verification suites still green") will fail. |

### Layer 3 Design Concerns from REUSE

Six concerns surfaced — all folded into D-A through D-J in TL;DR. Listed here for completeness; user resolves via D-tier decisions.

| Concern | Maps to | Decision being challenged |
|---|---|---|
| #1 — `.ar-*` namespace silently violates Q-C | **D-A** | Q-C ("keep existing CSS class names verbatim") |
| #2 — `Notification` typed shape misses 5 fields | **D-B** | Phase 4 D-G/Q2 (persistence "unread count only") + interface stability |
| #3 — Singular vs plural store keys (TS compile bug) | **D-I** | None challenged — wording bug |
| #4 — Wrong line citations in 3 places | **D-J** | None challenged — citation bugs |
| #5 — `formatCountdown` clock-offset double-application | **D-H** | None challenged — implementation invariant |
| #6 — Phase 1 smoke regression on placeholder removal | **D-G** | AC10 implicit "no test changes" assumption |

---

## Pass 1 Fitness Findings

**Summary**: 22 findings (5 Block / 12 Major / 5 Minor) clustered around: TESTABILITY (8), COMPLETENESS (5), AMBIGUITY (4), RISK SURFACE (3), EXTERNAL DEPENDENCIES (1), DECISION TRACEABILITY (1). **0 Layer 3 design concerns** — Phase 5 design is internally coherent against Q-A through Q-L + Phase 0 amendments + Phase 4 hooks.

| ID | File / Section | Deficiency type | What's missing | Proposed fix | Severity |
|---|---|---|---|---|---|
| F1 | `06-phase5-renderer-design.md:281` (AC7) | TESTABILITY | "compared to logged Phase 4 figure" — WHERE is Phase 4 baseline gz figure recorded? Searching `90-execution-log.md` and Phase 4 review-findings reveals only "≤ 30 KB gzipped delta" as future contract, not a captured number. | **D-C**. Capture Phase 4 final `gzip -9 -c dist/multiplexer/boot.js \| wc -c` byte count into `90-execution-log.md` Phase 4 closure section before Phase 5 implementation begins; reference that line:hash in AC7 as a frozen byte literal. | Block |
| F2 | `06-phase5-renderer-design.md:281` (AC7) | AMBIGUITY | Gzip mode contradiction: AC7 row says `gzip -9 -c`; Phase 4 A5 says "30 KB gzipped" without flag. `gzip -9` (max) gives different bytes than default `gzip -6`. | **D-D**. Pin Phase 4 baseline + Phase 5 measurement to identical command: explicitly `gzip -9 -c <path> \| wc -c` (no other flags). State in AC7 row AND retroactively recapture Phase 4 baseline at `-9` if needed. | Block |
| F3 | `06-phase5-renderer-design.md:282` (AC8) | COMPLETENESS | Three-fixture page-load smoke spec missing fixture-injection mechanism. Phase 4 F10/A2 forced this; Phase 5 inherits the gap. How do "plain, markdown, action-required" notifications arrive at the renderer in a deterministic test? | **D-E**. Spec one of: (a) `page.evaluate` injects fixture envelopes via `eventBus.emit("notification_queue_update", ...)` direct (matches Phase 4 D-D — recommended); (b) Reuse `/api/notify` real endpoint with auth-token; (c) New `/api/test/inject-notification` endpoint. Pick one and write assertion shape into AC8. | Block |
| F4 | `06-phase5-renderer-design.md:285` (AC11) | COMPLETENESS | "First-run E2E baseline capture on `:8000` (scheduled)" lacks: (a) explicit `--update-snapshots` invocation (mentioned only in CLAUDE.md as separate command); (b) snapshot directory creation precondition; (c) what test-suite-submit JSON body shape carries the "first-run baseline" intent vs regression run. | **D-F**. Spec AC11 execution: `POST /api/test-suite/submit {"test_types": "e2e_ui", "scheduled_at": "<user-confirmed slot>", "args": "--update-snapshots -k multiplexer_phase5"}`; first-run creates `__snapshots__/multiplexer-phase5/` with N expected images; subsequent AC11 runs compare. Add directory-creation step explicitly. | Block |
| F5 | `06-phase5-renderer-design.md:90, 191` (Q-B tick invariant + AC4) | TESTABILITY | "tick must NOT touch parent DOM" invariant test mechanism is prose-only ("mutating a sentinel attribute on the parent and verifying it is unchanged"). Specifics absent: which parent (`#notifications-pane`? `.action-required-widget`? `.sender-card`?); what sentinel; how many tick events constitute a "burst"; what invariant covers. | Pin: parent = `.action-required-widget` (the widget root); sentinel = `data-test-canary="<random-uuid>"` set by test before tick burst; burst = 10 tick events fired sequentially via `eventBus.emit`; assertion = `widget.getAttribute("data-test-canary")` unchanged AND `widget.outerHTML.indexOf("ar-countdown")` unchanged AND only `.ar-countdown` `textContent` differs. Add to AC4 row. | Block |
| F6 | `06-phase5-renderer-design.md:233, 277` (Q-J + AC3) | TESTABILITY | AC3's "TT policy" coverage named but mocking strategy not. How is `window.trustedTypes` mocked in `tsx --test` (no jsdom by default)? How is `KNOWN_TEMPLATES` WeakSet identity-check failure tested without policy actually rejecting? | Add to AC3: TT policy tests use hand-rolled `globalThis.trustedTypes = { createPolicy: (name, hooks) => ({ createHTML: (s, t) => hooks.createHTML(s, t) }) }` shim installed in `beforeEach`; absence test runs in separate sub-describe with `delete globalThis.trustedTypes`. Identity-check failure test calls `policy.createHTML('<x>', NEW_TEMPLATE_STRINGS_ARRAY)` directly and asserts `TypeError`. | Major |
| F7 | `06-phase5-renderer-design.md:279` (AC5) | COMPLETENESS | Per-template floor unspecified. AC5 says "≥ 20 PASS combined" across 7 files. With no per-file floor, a single template can satisfy AC5 alone, leaving others uncovered. Phase 4 F14 set per-store floors. | Pin per-file floors: senderCard ≥3, dateAccordion ≥3, notificationItem ≥4, actionRequiredReadOnly ≥4 (one per response_type), dom ≥3, time ≥4, markdown ≥3 = 24 minimum, satisfying ≥ 20. | Major |
| F8 | `06-phase5-renderer-design.md:280` (AC6) | COMPLETENESS | `c8` flag specifics missing: include pattern stated, no exclude pattern; no rule for inline `c8 ignore` rationale (Phase 4 A1/AC6 nailed this — Phase 5 doesn't restate); no `--all` flag → c8 only counts files exercised by tests. | Add: `c8 --all --include 'src/fastapi_app/static/js/multiplexer/render/**' --exclude '**/*.test.ts' --reporter=text-summary --check-coverage --lines 90 npx tsx --test ...`. Inherit Phase 4 A1 contract: any `c8 ignore` region MUST include same-line comment naming unreachable branch + reason. | Major |
| F9 | `06-phase5-renderer-design.md:284` (AC10) | COMPLETENESS | "enumerated commands from Phase 4 AC10 + Phase 4 unit suite ≥ 88/88 PASS" is forward-reference, not enumeration. Phase 4 F15 forced enumeration; Phase 5 hand-waves. | Enumerate verbatim: (1) `npx tsc --noEmit -p tsconfig.json`; (2) `npx eslint src/fastapi_app/static/js/multiplexer/`; (3) `pytest src/tests/smoke/test_multiplexer_phase1_smoke.py` 7/7 (or post-D-G, updated count); (4) `npx tsx --test src/tests/unit/multiplexer/auth/auth_manager.test.ts ...` (Phase 2 suite); (5) `pytest src/tests/smoke/test_multiplexer_phase3_smoke.py` 1/1; (6) `bash src/scripts/run-websocket-smoke-tests.sh` 4/4; (7) `npx tsx --test src/tests/unit/multiplexer/stores/...` ≥88/88. | Major |
| F10 | `06-phase5-renderer-design.md:271-285` (AC table) | COMPLETENESS | No Verification matrix subsection (Phase 4 had explicit lines 250-256 with executor tags). No Rollback subsection (Phase 4 A7 required step 0: cosa-voice `ask_yes_no` before auto-revert if Phase 1+3+4 verification non-zero AND `git blame` points at Phase 5). | Add Verification matrix mirroring Phase 4 lines 250-256 (with Executor column on every row). Add Rollback subsection with step 0 per Phase 4 A7. | Major |
| F11 | `06-phase5-renderer-design.md:69` (TT policy + `<template>.innerHTML`) | RISK SURFACE | Helper writes to `<template>.innerHTML` which is itself a TT sink. Q-J snippet uses `TT_POLICY.createHTML(combined, strings)` — but return-type ambiguity (TT returns `TrustedHTML` opaque object, not string). TS strict mode will flag. Also `KNOWN_TEMPLATES.add(strings)` happens BEFORE policy check. | Cast policy result via `as unknown as string` for assignment OR use `TrustedTypePolicy.createHTML` typed as returning `TrustedHTML & string`. Add unit test: invoke `html\`<x>${val}\`` twice from same call site; assert `KNOWN_TEMPLATES.has(strings)` true on both calls (same TemplateStringsArray instance). Document spec invariant explicitly. | Major |
| F12 | `06-phase5-renderer-design.md:88, 162-191` (`data-id-hash` invariant) | AMBIGUITY | Action-required widgets keyed by `data-id-hash` per Q-H; notifications (`.sender-message`) keyed by `id="${notification.id \|\| notification.id_hash}"` per exploration §1.1. Q-B says "via `keyedListMerge` (key by `data-id-hash`)" — but `.sender-message` doesn't carry `data-id-hash`, only `id`. Inconsistent keying. | Pin: every notification-pane element participating in keyed merge (`.sender-message`, `.action-required-widget`, `.progress-history-entry`) MUST carry `data-id-hash="${idHash}"`. Legacy `id="..."` attribute NOT used for keyed merge in multiplexer (TTS correlation moves to `data-id-hash` consistently). State in §DOM grouping; add unit test in dom.test.ts. | Major |
| F13 | `06-phase5-renderer-design.md:88, 215-219` (boot.ts wiring + EventBus subscription order) | ORDERING | `createNotificationsListRenderer` invoked AFTER `transports.audio.start(...)` but renderer subscribes to `store_notifications_changed`. If `notification_queue_update` arrives between `createStores` and renderer construction, store mutates and emits, but no listener attached → first render misses early events. | Spec construction order in boot.ts: (1) `createStores`; (2) `createNotificationsListRenderer({eventBus, stores})` — synchronously attaches listeners; (3) `renderer.mount(mountEl)` — calls `notificationStore.list()` to paint initial state; (4) `transports.audio.start(...)`. Mount BEFORE transport start. Add unit test asserting `mount()` calls `list()` before any subscriptions fire. | Major |
| F14 | `06-phase5-renderer-design.md:151-158` (Q-G progress-group history caching) | RISK SURFACE | Lazy-cached fragment is in-memory inside rendered DOM (the `<div class="progress-group-history">`). What happens when `store_notifications_changed { changeKind: "updated" }` fires for that progress group while history expanded? Cached fragment re-rendered? Stale? `keyedListMerge` running over parent — preserves cached children? | Spec: `changeKind: "updated"` for notification with `progress_group_id` re-renders `progress-group-head` only (per existing Q-B pattern) and APPENDS displaced previous head into cached `.progress-group-history` container as new entry. If history never expanded, entry materializes lazily as before. Add unit test: hydrate, expand, simulate updated event, collapse, expand → newest entry visible. | Major |
| F15 | `06-phase5-renderer-design.md:90-92` (action-required widgets in Phase 5) | AMBIGUITY | "Render prompt + countdown + options preview + default" — but where do widgets render? Inside which DOM container? `#notifications-pane` hierarchy is sender → date → message; action-required items NOT messages. Q-H spec shows widget DOM but mount location unspecified — separate pane? Inline within `.sender-card`? Top of `#notifications-pane`? | **D-L**. Pin mount location: `#action-required-section` child of `#notifications-pane` rendered ABOVE all sender cards, holding per-prompt widgets. State in §DOM grouping and §Page shell. | Major |
| F16 | `06-phase5-renderer-design.md:89-92, 282` (action-required + AC8 fixture) | EXTERNAL DEPENDENCIES | Q-B says renderer subscribes to `store_action_required_changed`; `ActionRequiredStore.list()` is source. AC8 says "three fixture notifications (plain, markdown, action-required) render" — but action-required items NOT in `NotificationStore.list()`; in `ActionRequiredStore.list()`. Two separate fixture-injection paths needed. AC8 conflates them. | Spec AC8 fixtures explicitly: (a) plain notification → `eventBus.emit("notification_queue_update", {...non-action-required envelope})`; (b) markdown notification → same with markdown body; (c) action-required item → `eventBus.emit("notification_queue_update", {...action_required: true, response_type: "yes_no", expires_at: now+30000})`. State envelope shapes inline. | Major |
| F17 | `06-phase5-renderer-design.md:282` (AC8 timing) | TESTABILITY | "render within 500ms" + "countdown ticks visibly" — both lack measurement mechanism. From which time origin to which DOM signal? `boot_complete` to `[data-testid="multiplexer-notification"]` first paint? Performance.now() bookends? "Visibly" is unmeasurable in smoke. | Pin: time-origin = `boot_complete` console event ts; time-target = `await page.waitForSelector('[data-testid="multiplexer-notification"]', { timeout: 500 })`; assert wait actually returned within 500ms via `performance.now()` delta. For tick: capture `.ar-countdown` `textContent` at t=0 and t=1500ms; assert string differs. | Major |
| F18 | `06-phase5-renderer-design.md:139-145` (empty state) | COMPLETENESS | Empty-state spec defines DOM and styling but doesn't enumerate transitions: (a) hydrate-with-zero → empty state; (b) hydrate-with-N → no empty state; (c) post-`changeKind: "expired"` reducing list to zero → empty state appears; (d) post-`changeKind: "added"` from zero → empty state removed, sender-card appears. Test coverage implicit. | Add explicit transition list to §Empty-state UI; require unit tests (4 cases) in `notifications_list_renderer.test.ts`. Bump AC4 floor from 14 to 16 (or call out these 4 tests as part of AC4's 14). | Major |
| F19 | `06-phase5-renderer-design.md:303` (Plan-review pipeline scope) | DECISION TRACEABILITY | "TBD slots will surface for Q-A through Q-L" — design doc's own §Plan-review pipeline scope text says TBDs WILL surface. But Q-A through Q-L are all ratified ✅. Prose contradicts table — leftover text from pre-ratification. | Strike "TBD slots will surface for Q-A through Q-L;" from Pass 1 fitness bullet; keep "TT policy registration timing; markdown DOMPurify config drift audit; renderer-vs-store construction-order vs Phase 4 F12 precedent" (legitimate Pass 1 surface). | Minor |
| F20 | `06-phase5-renderer-design.md:92` (markdown wrapper) | EXTERNAL DEPENDENCIES | "match old page's exact DOMPurify config from `notifications.js:12490-12500`" — which config keys? `ALLOWED_TAGS`? `ALLOWED_ATTR`? `FORBID_TAGS`? `USE_PROFILES`? Without verbatim config quoted in design doc (or copied to `render/markdown.ts` source ahead of time), implementer must context-switch. Risk: XSS regression. | Quote verbatim DOMPurify config block from `notifications.js:12203-12247` (per **D-J**) into design doc within `render/markdown.ts` spec section. Add AC: "matches-legacy-config" snapshot test in markdown.test.ts asserting object equality with canonical config. | Major |
| F21 | `06-phase5-renderer-design.md:374` (`hydrateHistory` grep guard) | TESTABILITY | "Grep guard in CI: `grep -r "hydrateHistory" .../render/` returns empty" — where is guard run? Pre-commit hook? `npm run lint`? Standalone CI step? Not bound to any AC. | Bind to AC2 or add AC2a: "ESLint custom rule OR `bash src/scripts/check-no-hydrate-history.sh` (new) returns exit 0 with empty match." | Minor |
| F22 | `06-phase5-renderer-design.md:283` (AC9 console line format) | AMBIGUITY | "`boot_complete` console line includes `handlers.notificationsRenderer === "mounted"`" — Phase 4 D-C established mechanism: `console.log("[multiplexer] boot_complete", JSON)`. Phase 5 extends payload — but assertion text doesn't specify whether literal JSON-stringified ("mounted") or function-name introspection. | Pin: payload field is literal string "mounted" (not function-name). State in design that boot.ts emits `eventBus.emit("boot_complete", {handlers: {audioBinary: ..., notificationsRenderer: "mounted"}})` AFTER `renderer.mount(mountEl)` returns successfully; mirrored to console via `console.log("[multiplexer] boot_complete", JSON.stringify(payload))`. | Minor |

### TBD Resolution

After Q-A through Q-L ratification (just landed), residual TBD markers:

1. **`06-phase5-renderer-design.md:303`** — "TBD slots will surface for Q-A through Q-L" — obsolete (all twelve ratified). **Proposed answer**: strike clause as part of **F19**. Remaining clause ("TT policy registration timing; markdown DOMPurify config drift audit; renderer-vs-store construction-order vs Phase 4 F12 precedent") is actual Pass 1 surface and is satisfied by **F11/F20/F13** above.

2. **`2026.05.05-phase5-pre-design-exploration.md:365`** (verbatim quote of Phase 4 design doc) — "Fetches the canonical history endpoint (server URL TBD at Phase 5 — Phase 4 implementation MUST verify the endpoint exists during impl)". **Proposed answer**: this TBD is **inherited via verbatim quote**, NOT a Phase 5 TBD. Phase 5 does NOT call `hydrateHistory()` (Q7 hook reserved for Phase 6). The TBD remains live for Phase 6, NOT Phase 5. No Phase 5 action; flag as "Phase 6 surface" in pre-design exploration to avoid future confusion.

### Pass 1 Layer 3 Design Concerns

**No Layer 3 design concerns surfaced.** Q-A through Q-L are coherent and trace cleanly to anchor decisions (Q9/Q10/Q11/Q12 + Phase 4 RE-12/F16/Q7). Every Phase 5 design choice traces to either a Q-A through Q-L decision OR a Phase 0 amendment OR a Phase 4 hook. The 22 findings above are all implementation-detail tightenings inside the design surface, not challenges to the ratified Q-decisions.

---

## Pass 2 Adversarial Findings

**Summary**: 14 findings clustered around: zero canonical `EXECUTOR:` tags anywhere (the AC table uses an "Executor" column with `AI bash` / `AI Playwright` / `AI test-suite submit` values but does NOT use the canonical `EXECUTOR: AI` / `EXECUTOR: HUMAN` tag schema); ambiguous-verb / unscoped-judgment leakage (6 findings); AC pass/fail criterion missing or non-executable as written (4 findings); user-as-tester / user-as-gatekeeper language (2 findings); `:8000` slot-confirmation hand-off ambiguity (1 finding).

| ID | File / Line | Problem | Proposed fix |
|---|---|---|---|
| A1 | `06-phase5-renderer-design.md:274-285` (AC1-AC11 table) | "Executor" column uses `AI bash`, `AI Playwright`, `AI test-suite submit` — none match canonical `EXECUTOR: AI` / `EXECUTOR: HUMAN` tag schema Pass 2 audits. A reader scanning for `EXECUTOR: HUMAN` finds zero hits and may assume zero hand-offs, but AC11 effectively defers slot-availability to user. The schema is flat-out absent. | Add explicit `EXECUTOR: AI` to every row that AI executes start-to-finish; reserve a `Human gate` column for AC11's `scheduled_at` slot-coordination (justification: `external slot-availability calendar — user has visibility into other scheduled :8000 runs that AI does not`). Mirror Phase 4's `91-phase4-review-findings.md` D-tier ratification format. |
| A2 | `06-phase5-renderer-design.md:282` (AC8) | "renderer mounts, three fixture notifications (plain, markdown, action-required) render within 500ms, countdown ticks visibly" — `render` is passive; `ticks visibly` is human-perception verb AI cannot execute headlessly without concrete assertion. 500ms is wall-clock budget but timing-source isn't named (server clock? `performance.now()` from `boot_complete`? from page navigation?). | Reword: `EXECUTOR: AI`; "Page loads on `LUPIN_API_URL`; renderer.mount completes within 500ms of `boot_complete` per `performance.now()`; AI Playwright asserts `[data-testid="multiplexer-notification"]` count === 3, `[data-id-hash="<plain>"] .message-text` equals fixture text, `[data-id-hash="<md>"] .message-text` contains rendered markdown HTML, `.ar-countdown` text changes between two samples 1100ms apart." Pass = all four assertions green. |
| A3 | `06-phase5-renderer-design.md:283` (AC9) | "`boot_complete` console line includes `handlers.notificationsRenderer === "mounted"` — `assert in AC8 smoke via page.on("console", ...)`" reuses AC8's runtime; if AC8 fails for unrelated reason (e.g. fixture seed), AC9 silently never runs. No independent pass/fail surface. | Either: (a) split AC9 into own headless run with own `pytest` node id, OR (b) explicitly state "AC9 asserts even on AC8 fixture failure (try/finally around fixture seed) — pass criterion: `console.log` capture buffer contains string `notificationsRenderer:mounted` exactly once." `EXECUTOR: AI`. |
| A4 | `06-phase5-renderer-design.md:284` (AC10) | "Phase 1/3/4 verification suites still green; no regressions" — "still green" descriptive, not executable. "Enumerated commands from Phase 4 AC10" punts to list not enumerated in this doc. AI can't run unenumerated suite. | Inline explicit command list (or canonical reference: `91-phase4-review-findings.md` AC10 row exact bash) — see **F9**. Pass criterion: `pytest exit 0` for each named node + `npx tsc --noEmit` for each named tsconfig + `npm test` exit codes — log each. `EXECUTOR: AI`. |
| A5 | `06-phase5-renderer-design.md:285` (AC11) | "First-run E2E baseline capture on `:8000` (scheduled) — captures fresh `__snapshots__/multiplexer-phase5/` images." Three problems: (1) "captures fresh" describes side-effect, not assertion. (2) Justification "Per locked directive: feature parity not pixel parity" is description, not pass criterion. (3) `EXECUTOR` is `AI test-suite submit` but acceptance is snapshot directory state — split submission from acceptance. | **D-F**. Two ACs: AC11a `EXECUTOR: AI` — submission via `POST /api/test-suite/submit` with `scheduled_at` returns 200 + `submission_id`. AC11b `EXECUTOR: AI` (post-run, polling `/api/test-suite/status/<id>` to terminal state) — assert `find src/tests/e2e_ui/__snapshots__/multiplexer-phase5/ -type f -name "*.png" \| wc -l > 0` AND test-suite final state `=== "passed"`. Human gate (justified: external slot-availability — user owns :8000 schedule): user confirms `scheduled_at` does not collide before AC11a fires. |
| A6 | `06-phase5-renderer-design.md:285` (AC11) | "with `scheduled_at` confirmed by user as non-overlapping" — *user* named explicitly as gate. Per `feedback_test_server_monopolize_mode` correct (slot-availability not budget approval), but only place a human role surfaces in AC table and not flagged with canonical justification. Reader could misread as "user is tester for AC11." | Add inline justification: `Human gate: external slot-availability calendar (user has visibility into in-flight :8000 schedule that AI does not). NOT tester duty. AI executes the submission, polls completion, and asserts AC11a/AC11b.` |
| A7 | `06-phase5-renderer-design.md:367` (Risks table row 1) | "Audit pass after first port + stylelint smoke check; aim ≤ 1,200 residual LOC" — "Audit pass" is verb-noun fossil with no executor; `aim ≤ 1,200` is goal, not pass/fail line. Could read as "user inspects port." | "AI runs `wc -l src/fastapi_app/static/css/multiplexer/notifications-list.css` post-port; pass criterion `≤ 1200`. AI runs `npx stylelint src/fastapi_app/static/css/multiplexer/notifications-list.css` exit 0. Both gated as additional ACs (e.g. AC10b/AC10c) so they actually run." |
| A8 | `06-phase5-renderer-design.md:374` (Risks table row 8) | "Grep guard in CI: `grep -r "hydrateHistory" .../render/` returns empty" — "in CI" aspirational; doc doesn't actually wire grep into any AC. Mitigation IS the AC, but it is NOT an AC. | Promote to AC10d `EXECUTOR: AI`: `! grep -rn "hydrateHistory" src/fastapi_app/static/js/multiplexer/render/` (exit 1 if any match — invert with `!`). |
| A9 | `06-phase5-renderer-design.md:289-292` (Test pyramid) | "Includes Trusted-Types-policy mocking, server-clock-offset edge cases, keyed-merge reorder correctness, `tick` invariant assertion." — listed as description, not testable items. No file:test-name pairs. AI cannot "include edge cases" — AI must run named test. | Replace prose with bullet list mapping each capability to specific test name (e.g. `html.test.ts::"refuses non-tagged-template input under TT mock"`, `time.test.ts::"date-key boundary at midnight with +5h server-clock offset"`). Each is `EXECUTOR: AI`, pass = test exits 0. |
| A10 | `06-phase5-renderer-design.md:292` (Performance row) | "first-render time measured via Playwright `performance.now()` from `boot_complete` to first paint of `[data-testid="multiplexer-notification"]` (target: < 100 ms with 50-notification fixture)" — "measured" but no AC enforces it. Target informational only. | **D-K(2)**. Promote to AC8b `EXECUTOR: AI`: assert `(t_first_paint - t_boot_complete) < 100` per Playwright eval; fail closed. State how 50-notification fixture is seeded (server endpoint? `page.evaluate` injection?) — currently undefined. |
| A11 | `06-phase5-renderer-design.md:285` (AC11) + `:291` (E2E line) | Hardcoded `:8000` in AC11 narrative. Per `feedback_tests_parameterize_base_url`, all Lupin-REST-hitting tests must read `LUPIN_API_URL`. Smoke test row 290 correctly mentions parameterization; E2E row 291 says "(`:8000` scheduled-only via `POST /api/test-suite/submit`...)" — conflates venue with hardcoded port. | Add explicit note in AC11: "E2E test code parameterizes target via `LUPIN_API_URL` env var (default in CI: `http://localhost:8000`; suite submitter sets it). No hardcoded `:8000` literal in `src/tests/e2e_ui/multiplexer_phase5_*.py`." Mention in test pyramid line as well. |
| A12 | `06-phase5-renderer-design.md:420` (Self-audit `feedback_e2e_two_phase_gate` row) | "gate is 'baselines committed + reviewed' before Phase 6 starts" — `reviewed` is human verb here; review-by-whom unspecified. Could imply human review (visual baseline subjective) OR AI re-grep against parity directive. | Either: (a) "AI commits baselines; human reviews captured PNGs for subjective layout sanity (justified: subjective visual judgment of fresh baseline) — AI cannot judge whether visual treatment matches user's mental model of locked-2026-05-05 directive", OR (b) define objective AI-runnable assertion (e.g. snapshot diff < N% vs prior commit's diff). Whichever: name executor and criterion. |
| A13 | `06-phase5-renderer-design.md:191` (action-required tick invariant) | "Unit test (`AC4`) asserts this invariant by mutating a sentinel attribute on parent and verifying it is unchanged after a tick burst." — "verifying" is AI assertion verb but sentinel-attribute trick described in prose only; not pinned to test name or test signature. AC4 (line 278) just says "tick-only-touches-countdown" without sentinel mechanism. | **F5**. Add explicit test name + brief contract in AC4: e.g. `notifications_list_renderer.test.ts::"tick burst does not mutate parent .action-required-widget — sentinel data-test-mutation-guard='unchanged' survives 10 tick events at 100ms intervals"`. `EXECUTOR: AI`. |
| A14 | `06-phase5-renderer-design.md:303-305` (Plan-review pipeline scope) | "Pass 2 adversarial: AC executability (...); ownership tags on each AC; convergence re-grep against Phase 4 baseline patterns." — self-prescription that current doc fails to satisfy (this Pass 2 finding exactly those gaps). Doc *promises* PIP coverage of executability + ownership tags but ships ACs lacking both. | When this `92-phase5-review-findings.md` is ratified, explicitly enumerate which findings from Pass 2 (A1-A14) are incorporated and which (if any) are dismissed with justification — mirror Phase 4's D-tier ratification table format. |

### Pass 2 Layer 3 Design Concerns

Three concerns surfaced — folded into D-F (#1) and D-K (#2 + #3) above.

| Concern | Maps to | Decision being challenged |
|---|---|---|
| #1 — AC11 acceptance is side-effect-shaped, not assertion-shaped; conflates submission/post-run/baselines | **D-F** | AC11 row schema |
| #2 — Performance budget conflict (100ms vs 500ms; two fixture sizes; no canonical merge gate) | **D-K(2)** | AC8 pass criterion + Test pyramid Performance row |
| #3 — `data-phase6-pending="true"` marker is the agreed Phase-5/Phase-6 contract surface but no AC asserts presence | **D-K(3)** | Q-H + Q-L marker invariant |

---

## Mandatory grep output (Pass 1 + Pass 2)

```bash
$ grep -rn "TBD\|confirm during impl\|decide at impl time\|tbd" 06-phase5-renderer-design.md 2026.05.05-phase5-pre-design-exploration.md
2026.05.05-phase5-pre-design-exploration.md:365:> "history bucket starts empty; live `job_removed` events for done/dead jobs append in-session. Phase 5+ renderer calls `hydrateHistory(api)` when the history pane mounts; the call: Fetches the canonical history endpoint (server URL TBD at Phase 5 — Phase 4 implementation MUST verify the endpoint exists during impl), Dedups response entries by `id_hash`..."
06-phase5-renderer-design.md:303:- **Pass 1 fitness**: TBD slots will surface for Q-A through Q-L; TT policy registration timing; markdown DOMPurify config drift audit; renderer-vs-store construction-order vs Phase 4 F12 precedent.

$ grep -rn "Open sub-question" 06-phase5-renderer-design.md 2026.05.05-phase5-pre-design-exploration.md
(no matches)

$ grep -rn "Manual\|manual" 06-phase5-renderer-design.md 2026.05.05-phase5-pre-design-exploration.md
2026.05.05-phase5-pre-design-exploration.md:388:| **Q9** | Old page stays indefinitely (unbounded cutover) | Phase 5 coexists with `/app/notifications` until manual removal request. No forced deletion timeline. |

$ grep -rn "EXECUTOR: HUMAN" 06-phase5-renderer-design.md 2026.05.05-phase5-pre-design-exploration.md
(no matches — AND no `EXECUTOR: AI` matches either; canonical tag schema is absent from both files — see A1)

$ grep -rnE "^\- \[ \] [^E]" 06-phase5-renderer-design.md 2026.05.05-phase5-pre-design-exploration.md
(no matches — both docs use ✅/⏸ status glyphs in numbered/bulleted lists rather than `- [ ]` checkboxes; no bare-checkbox offenders)
```

The single `manual` hit at `2026.05.05-phase5-pre-design-exploration.md:388` is benign — narrative referring to "manual removal request" of the old `/app/notifications` page (a future user-driven decision, not a test-execution hand-off). Not a finding.

---

## Summary

| Pass | Findings | Block | Major | Minor | Layer 3 design concerns |
|---|---|---|---|---|---|
| REUSE | 23 | — | — | — | 6 (folded into D-A, D-B, D-G, D-H, D-I, D-J) |
| Pass 1 Fitness | 22 | 5 | 12 | 5 | 0 |
| Pass 2 Adversarial | 14 | — | — | — | 3 (folded into D-F, D-K) |
| **Total** | **59** | **5** | **12** | **5** | **9 → 12 D-tier decisions** |

**Resolution Loop scope after D-A through D-L are decided**: ~30 wording/coverage/test-spec fixes to apply mechanically. Convergence re-greps (Pass 1 TBD/Open sub-question; Pass 2 Manual/EXECUTOR HUMAN/bare-checkbox) run after fixes land. Expected: 1-2 Resolution Loop rounds before convergence per PIP §10.

**Next step**: User reviews D-A through D-L, picks Apply / Reject / Defer for each, then AI applies approved fixes per PIP §7. Code-implementation cycle (separate plan-mode session) begins only after this `92-phase5-review-findings.md` closes with explicit user go-ahead.
