# Plan — Rename Voice Persona "Domi" → "Rio"

**Date**: 2026-05-11
**Project**: [LUPIN]
**Scope**: Single-persona label rename in the CC-session voice-persona pool. No TTS audio change.
**Trigger**: User reviewed brainstorm of "young & energetic female" alternatives, picked **Rio**.

---

## Phase 0 — Documentation Serialization (this doc)

- ✅ Plan serialized at `src/rnd/v0.1.7/2026.05.11-rename-persona-domi-to-rio.md`
- Per `feedback_documentation_step_stops_at_doc.md`: this plan does NOT auto-progress to implementation. Awaiting explicit user "go" before any code edits.

---

## Context — What "Rename" Means Here

The "Domi" label is **our persona name** layered over an ElevenLabs voice. The rename affects only the display label, INI keys, and test fixtures — the underlying ElevenLabs voice (`AZnzlk1XvdvUeBnXmlld`, ElevenLabs's own catalog name "Domi") is **unchanged**, so allocated sessions sound identical after the rename.

Precedent on file: the same pool already underwent two rename rounds (Nora→maria, Quentin→mr radio, Adam→Tiberius on 2026-04-30/05-01). This rename follows the same pattern.

---

## Proposed Preserved Attributes

| Attribute   | Current               | Proposed (Rio)        | Rationale                                                                 |
|-------------|-----------------------|-----------------------|--------------------------------------------------------------------------|
| `voice_id`  | `AZnzlk1XvdvUeBnXmlld` | `AZnzlk1XvdvUeBnXmlld` | No TTS change — only the label rotates                                  |
| `icon`      | ⚡                    | ⚡ (proposed kept)     | "Energetic" still fits "Rio"; icon-rotation is a separate decision      |
| `color`     | `#880E4F` (pink 900)  | `#880E4F` (kept)       | Preserves the maria-light + Domi-dark contrast pair documented in splainer |
| `profile`   | "Young & energetic female" | "Young & energetic female" | Voice timbre unchanged → profile text accurately describes the same voice |
| Key casing  | `Domi` (PascalCase)   | `Rio` (PascalCase)     | Matches sibling PascalCase keys (Rachel/Tiberius/Arnold); single clean word doesn't need lowercase-no-punctuation treatment |
| `display_name` | (none → falls through to `display_name_for` title-case) | (none — Rio is already proper noun) | No explicit mapping needed in `voice_persona_helpers.display_name_for`; the generic title-case fallback handles "Rio" correctly |

**Decision flags** (defaults proposed; user override welcome):
- 🚩 Icon: Keep ⚡ or rotate to something Rio-evocative (🌊 / 🏖️ / 🎉)?
- 🚩 Color: Keep #880E4F (preserves disambiguation logic) or pick fresh?

---

## File Inventory (Lupin parent repo — runtime + tests)

### Config (2 files)

| File | Lines | Change |
|------|-------|--------|
| `src/conf/lupin-app.ini` | 785, 814-817 | Replace `Domi` → `Rio` in the pool list and the 4 persona-key lines |
| `src/conf/lupin-app-splainer.ini` | 319-322 + rename-history mentions at 297, 301, 326 | Replace `Domi` → `Rio` in the 4 key explanations; add 2026-05-11 rename note to the pool-list splainer entry (preserves audit trail like prior rounds) |

### Tests (2 files)

| File | Lines | Change |
|------|-------|--------|
| `src/tests/unit/test_voice_persona_helpers.py` | 46, 51, 65, 170, 254, 392, 410 | Replace `Domi` → `Rio` in fixture pool, persona dicts, and assertion sets |
| `src/tests/smoke/test_voice_persona_allocation.py` | 129, 149 | Replace `Domi` → `Rio` in pool-name assertions |

### NOT changed (intentional)

- `src/fastapi_app/static/html/test/experimental-tts.html` — the `<option>` label "Domi" here references ElevenLabs's own catalog name for the underlying voice_id, not our persona label. Leave as ElevenLabs's name.
- `src/rnd/v0.1.7/2026.04.28-per-session-voice-personas/01-design.md` + `90-execution.md` — historical R&D docs describing past state at design time. Per project convention, frozen-in-time R&D docs are not retroactively rewritten on every rename.
- `src/rnd/v0.1.7/2026.05.02-voice-persona-clear-preservation/*.md` — same: describes a past session's observed failure case ("Tiberius→Domi"). Historical record.

---

## Sweep Check (per `feedback_sweep_for_pattern_offenders.md`)

Ran `grep -rn "Domi" src/` and classified every hit:

| Location | Class | Action |
|----------|-------|--------|
| `src/conf/lupin-app.ini` (5 hits) | Runtime config | **CHANGE** |
| `src/conf/lupin-app-splainer.ini` (5 hits, 4 keys + 1 disambig mention) | Runtime splainer | **CHANGE** |
| `src/tests/unit/test_voice_persona_helpers.py` (7 hits) | Test fixtures | **CHANGE** |
| `src/tests/smoke/test_voice_persona_allocation.py` (2 hits) | Test fixtures | **CHANGE** |
| `src/fastapi_app/static/html/test/experimental-tts.html` (1 hit) | ElevenLabs catalog label | **KEEP** (not our persona label) |
| `src/rnd/v0.1.7/2026.04.28-per-session-voice-personas/*.md` | Frozen R&D | **KEEP** (historical) |
| `src/rnd/v0.1.7/2026.05.02-voice-persona-clear-preservation/*.md` | Frozen R&D | **KEEP** (historical) |
| `src/lupin-mobile/**` | Sub-repo R&D + history | **HANDOFF** (see below) |
| `src/cosa/history.md`, `src/cosa/.claude-session.md` | Sub-repo history | **KEEP** (frozen log) |
| `src/rnd/v0.0.5/2025.07.07-tts-streaming-comparison-claude.md`, `src/rnd/v0.1.6/2026.04.12-...` | Frozen R&D | **KEEP** (historical) |

Confirmed: no Python/JS/TS source file under `src/` (parent or mobile) references the literal string `"Domi"` in runtime code paths — only INI/test fixtures.

---

## Cross-Project Handoff (per `feedback_cross_project_handoff_doc.md`)

**Mobile sub-repo (`src/lupin-mobile/`)**:
- Runtime impact: **none** — no Dart/JSON/YAML code references "Domi". The mobile client receives persona allocation from the server at session start; whatever label the server sends, mobile renders.
- Doc impact: 4 R&D docs + history.md mention "Domi" by name in cheat-sheets / runbooks (`2026.05.07-automating-rendering-and-behaviors-on-android-emulator.md`, `2026.04.24-on-device-tts-verify-runbook.md`, `2026.05.06-mobile-port-plans/voice-persona/00-index.md`, `2026.05.06-resync-baseline-mobile-vs-lupin.md`, `history.md`). These are frozen historical context — leave alone.
- Action: Seed a one-line note into mobile's TODO.md pointing at this plan doc, so the next mobile session is aware "Domi" in their runbooks/cheat-sheets is now "Rio" on the server. No handoff doc needed (runtime impact is zero).

**CoSA sub-repo (`src/cosa/`)**: no impact — only frozen history.md mentions. No action.

**Firefox plugin**: no impact.

---

## Test Impact

| Layer | Suite | Venue | Expected outcome |
|-------|-------|-------|------------------|
| Unit | `pytest src/tests/unit/test_voice_persona_helpers.py -v` | :7999 (AI-discretionary) | Pass after fixture updates |
| Smoke | `pytest src/tests/smoke/test_voice_persona_allocation.py -v` | :7999 (AI-discretionary) | Pass after fixture updates |
| Inline | `python -m cosa.rest.voice_persona_helpers` quick_smoke_test (if present) | :7999 | Pass (no logic change) |
| WS smoke | `src/scripts/run-websocket-smoke-tests.sh` | :7999 | Unchanged — not persona-dependent |
| E2E UI | `./src/scripts/run-e2e-ui-tests.sh --bg -v` | :8000 (scheduled) | Unchanged — UI renders whatever persona it gets; no hardcoded "Domi" expectations expected (will verify via grep before submission) |
| Integration | `./src/tests/run-integration-tests.sh --bg -v` | :8000 (scheduled) | Unchanged — not persona-dependent |

**Layer routing rationale**: This rename is data-only (INI + fixtures). Unit + smoke at :7999 are the meaningful gates. :8000 suites are recommended for the merge gate per PR MERGE REQUIREMENTS but no regression is expected.

---

## Open Questions for User

1. **Icon**: Keep ⚡, or rotate to something Rio-evocative (🌊 / 🏖️ / 🎉 / other)?
2. **Color**: Keep `#880E4F` (preserves the deliberate maria-light + dark-pink contrast pair), or pick a fresh hue?
3. **Profile text**: Keep "Young & energetic female" verbatim, or rephrase to match the Rio label (e.g., "Bright & energetic female")?
4. **Persona for active session**: This session was allocated "Domi" at start. After the rename lands, do you want this session reallocated to "Rio" (same voice_id, new label) on next /clear, or is per-session continuity not a concern?

---

## Execution Sequence (after approval)

1. Apply config edits (`lupin-app.ini`, `lupin-app-splainer.ini`).
2. Apply test fixture edits (`test_voice_persona_helpers.py`, `test_voice_persona_allocation.py`).
3. Run unit + smoke at :7999, report results in tabular form.
4. Add 2026-05-11 rename note to the splainer pool-list entry (audit trail).
5. Seed mobile sub-repo TODO.md with a one-liner pointing at this plan (in a mobile-context session, NOT from parent).
6. Final tabular pass/fail report; no commit until user says commit.

---

## Out of Scope (explicitly)

- Rebuilding/bouncing :7999 (FastAPI auto-reloads INI? — actually INI is read at startup via ConfigurationManager; the dev server will need a bounce per the `server-lifecycle` skill. Will advise + verify empty queues before SIGKILL per `feedback_dev_server_bounce_courtesy.md`).
- Changing the underlying ElevenLabs voice_id.
- Touching any frozen R&D doc or sub-repo history.
- Editing mobile sub-repo from parent context (TODO seed must happen in a mobile-context session).
- Committing or pushing (no auto-commit/push per `feedback_never_auto_commit_push.md`).
