# Per-Session DND Toggle + Slider Move — Execution Log

**Paired design doc**: `2026.05.14-per-session-dnd-toggle-and-slider-move.md`
**Session**: `a0eaaca1` (Mr. Radio)
**Started**: 2026-05-14 late evening (after Rick's "Other than that the plan looks good Full speed ahead!")

---

## Locked-in design decisions (from the walkthrough)

| # | Choice | Locked value |
|---|---|---|
| 1 | Slider position in header | Right-aligned between `#history-dropdown-container` and Clear All |
| 2 | OFF state icon (chorus) | 🤭 (face with hand over mouth) — Rick's flip |
| 3 | Default for unknown sessions | LOUD |
| 4 | Rewrite anchor | `handleNotificationUpdate` (arrival) |
| 5 | Solo-mode quiet body | Same demotion + monopoly notice |

---

## Phase 0 — R&D doc updates + execution log skeleton

- ✅ Append "Post-Implementation Move" note to `2026.05.14-tts-preview-stop-and-slider.md`
- ✅ Add ⚠️ SUPERSEDED banner to `2026.05.14-speakerphone-toggle-on-sender-card.md` (afternoon's parked plan); historical artifact preserved
- ✅ This execution log created

## Phase 1 — Move TTS slider from strip to accordion header — ✅ LANDED

- ✅ Removed `.cc-tts-fraction-control` block from `#cc-session-strip` (notifications.html ~line 671)
- ✅ Inserted into `.section-header` of `#section-notifications` right after `#history-dropdown-container` and before the `Clear All` button
- ✅ Added `onclick="event.stopPropagation()"` so dragging doesn't toggle the accordion
- ✅ Added `style="margin-right: 10px"` for spacing parity with Clear All
- ✅ Slider now visible whether or not CC sender cards exist

## Phase 2 — Expose tts_interaction_mode in /api/config/client — ✅ LANDED

- ✅ `system.py` `get_client_config()` payload now includes `"tts_interaction_mode": config_mgr.get("tts interaction mode", default="chorus")`
- ✅ Reads from existing INI key — no new keys added
- ✅ `py_compile` clean

## Phase 3 — Per-session DND toggle in createSenderCard — ✅ LANDED

- ✅ `notifications.js:707` — added `this.ttsInteractionMode = config.tts_interaction_mode || 'chorus'` to the config consumer
- ✅ `notifications.js:10493+` — `createSenderCard` icon/title block rewritten with mode-conditional rendering. Default for unknown sessions = LOUD (`sessionId in this.conversationModes ? !!entry : true`)
- ✅ `notifications.js:9999+` — `_setConversationModeLocal` redraw block updated with the same mode-conditional logic
- ✅ `notifications.js:9839` — `toggleConversationMode` POST URL rewired from dead `/api/cosa-voice/conversation-mode/` → canonical `/api/cosa-voice/speakerphone/`
- ✅ `notifications.js:9836` — `current` lookup defaults to LOUD when no entry exists (`sessionId in this.conversationModes`)
- ✅ WS handler `handleConversationModeChanged` already accepts `speakerphone_changed` envelopes via the `case "speakerphone_changed"` routing at line 5504 (re-shapes to `conversation_mode_active` legacy key shape) — no change needed
- ✅ Icons: chorus → 🔊 (loud) / 🤭 (quiet hand-over-mouth) ; solo → 📞 / 🔔 (legacy preserved)

## Phase 4 — Hook rider quiet-mode body — ✅ LANDED

- ✅ `hook_common.py:1133-1140` `_speakerphone_reminder_body` `else` branch (speakerphone_on=False) rewritten. Replaces "No notify required; text-only render" with the quiet-mode directive: "use priority='medium', suppress_ding=False" so notify() still fires for historical record but doesn't engage full TTS
- ✅ Applies in BOTH chorus and solo (the if/else is on `speakerphone_on`, not on `mode`); per the locked-in design decision 5, solo and chorus share the same demotion semantic
- ✅ `py_compile` clean

## Phase 5 — Client-side priority rewrite — ✅ LANDED

- ✅ `notifications.js` `handleNotificationUpdate` near line 5651 — inserted the belt-and-suspenders priority-rewrite block BEFORE the ding plays and BEFORE the priority branch dispatches. Uses `parseSenderId()` to extract `sessionId`, checks `this.conversationModes[sessionId] === false`, rewrites `priority='high'/'urgent'` → `priority='medium'` + `suppress_ding=false`. Logs `[QUIET-MODE]` rewrite event for browser-console debugging.
- ✅ `node -c` syntax check PASS

## Phase 6 — Live MCP verification

🔄 IN PROGRESS — Rick needs to hard-refresh the notifications page (Ctrl+Shift+R) so the new JS/CSS/HTML load. Then I'll fire test notifications to verify all phases live.

---

## Notes

- `notifications.js` is volatile; re-grep each anchor line immediately before editing.
- `node -c` syntax check at every phase boundary.
- Per `feedback_never_auto_commit_push`: wait for explicit "commit" / "push" go after live verification.
