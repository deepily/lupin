# Tiberius 🌑 Session Handoff — Pre-Context-Clear Capture

**Filed**: 2026-05-21 by Tiberius 🌑 (Lupin session `bc15c374`) at Rick's request before a context clear.
**Purpose**: capture session state + open threads + behavioral corrections so post-clear-Tiberius can resume without losing context.

---

## TL;DR for post-clear-Tiberius

You committed 7 things today, you helped Rick verify 2 production bug fixes end-to-end, and you started but did NOT finish the heartbeat-poker Phase 3 design conversation. Rick stopped you because you were over-stuffing TTS-spoken text and abstract tables in a way he couldn't read. Read § "Behavior corrections from this session" carefully before re-engaging — those mistakes are the ones to NOT repeat.

---

## What landed (committed, parent-Lupin)

7 commits today, all clean, all green:

| # | SHA | Subject |
|---|---|---|
| 1 | `0dfbac6` | Add Tiffany as preferred persona for lupin-mobile |
| 2 | `653ff36` | Aggressive-400 policy on retired `?scope=` query param |
| 3 | `0d1d489` | Heartbeat-poker design: ratify Q3 + doctrine-home + persona-allocation |
| 4 | `41171a2` | Doc-viewer image MIME support (PNG/JPG/SVG/etc.) — SPA + docs |
| 5 | `8cc727a` | notifications.js: skip focus-tray unread bump on outgoing responses |
| 6 | `bf04e9a` | smoke: assert Cache-Control no-cache on /app/* SPA shells |
| 7 | `e566b75` | e2e: doc-viewer image rendering Playwright tests |

Plus Mr Radio's interleaved commit `161be8c` (Recent Activity filter + Focus-bar chronological lock) — not yours.

---

## What's uncommitted (parent-Lupin)

**Nothing of yours.** Working tree shows one untracked file (`src/rnd/v0.1.7/2026.05.21-master-detail-two-pane-layout-experiment.md`) but it's NOT yours — likely Mr Radio's WIP. Don't touch.

This handoff doc itself will be the only new file you add.

---

## What's uncommitted in the CoSA submodule (NOT your commit scope)

Per `feedback_lupin_only_never_cosa`, you NEVER run git in `src/cosa/` from parent context. But for awareness, the CoSA submodule working tree has YOUR edits sitting uncommitted:

- `src/cosa/rest/routers/docs_files.py` — aggressive-400 policy (`?scope=` check + educational 400) + image-MIME extension (PNG/JPG/SVG/etc. → `FileResponse` polymorphic branch)
- `src/cosa/rest/routers/pages.py` — `Cache-Control: no-cache` on `_serve_file` helper

Plus Rachel's `git_loc_delta` artifacts which are her work, not yours.

**Action required from Rick (NOT you)**: commit those two CoSA-side changes in the CoSA repo context whenever convenient. Your parent-Lupin smoke/E2E tests + R&D doc amendment + Lupin CLAUDE.md update are all already committed and depend on those CoSA edits being functionally live. Since `src/cosa/` is bind-mounted, the changes ARE live on `:7999` and `:8000` regardless of commit state.

---

## E2E test scheduled today

You bounced `:8000` (`docker restart lupin-rest-test`) and submitted a focused E2E run via `/api/test-suite/submit`:

- Job ID: `ts-f108f0b3::50c73ba7-...`
- Filter: `pytest_args="-k doc_viewer_image"`
- `auto_fix_on_failure: false`
- **Result**: 4 passed, 0 failed, 0 errors, 0 skipped (16.4s)

Report at `test-suite/2026.05.21-at-17:23-EDT-e2e-results.md`.

---

## Open threads (at context-clear time)

### Thread 4 — Rick's global `~/.claude/CLAUDE.md` § DOCUMENT VIEWER LINKS

**Status**: HANDED OFF TO MARÍA 🌸 on `dm-tiberius`.

Rick asked you to coordinate this with María since she's actively working on `planning-is-prompting/workflow/doc-viewer-links.md`. You DM'd her the full task brief on 2026-05-21. She'll handle in her own cadence and ping you for review-OK when ready.

**Don't pick this up yourself on resume.** It's María's to deliver. Just be ready to review her draft when she pings.

### Thread 1 — Heartbeat-poker Phase 3 work breakdown (PARKED, the active drilling thread)

You were in the back-and-forth phase on this when Rick stopped you. State of the design:

- All 3 open Qs from yesterday RATIFIED at commit `0d1d489`:
  - Q3 daemon relationship → (b) co-exist + 5 swap-criteria
  - Doctrine-home for Watcher-mode protocol → (γ) Lupin-first + 2 PIP-promotion triggers
  - Persona-allocation under concurrent work streams → out-of-scope
- Pattern 5 (Architecture & Design) ratified as the PIP planning pattern
- 13-task initial breakdown drafted (5 design + 8 implementation)

**You proposed 3 observations to refine the breakdown**:

1. **Merge D1 + D4** into one "Class spec + integration patterns" design task (5 → 4 design tasks). Rationale: class shape is BOUND by integration patterns (QueueableJob protocol + AgenticJobBase parent + ApiResourceManager + commons-store).

2. **Rename D5 → "Phase 0 — Formal design doc + roadmap"** and tag as implementation-gate. Rationale: `feedback_phase0_serialization_prominence` — Phase 0 doc serialization is load-bearing, must precede ANY code.

3. **Split I4 → I4a/I4b/I4c/I4d** (one sub-gate per swap-criterion: ≥2 clean runs, ≥1 deadman, ≥1 hardcap, ≥1 concurrent-poker). Rationale: I4 is operator-event-driven over days/weeks, not a sit-down task.

**Resulting if all 3 accepted**: 4 design + 11 implementation = 15 tasks.

**Where you got stuck**: you asked Rick to vote on all 3 as a bundle (`ask_yes_no` default yes). He responded "neither" TWICE because you over-stuffed the spoken text and the abstract tables. He hadn't gotten to ratify any of them.

**On resume**: re-surface the 3 observations ONE AT A TIME via `ask_yes_no` with MINIMAL spoken text (≤15 words each) + tight abstract. Don't bundle. Rick can vote each independently and we move forward without overload.

### Out-of-our-direct-scope (just-in-case awareness)

- María 🌸 is async-drafting `planning-is-prompting/workflow/doc-viewer-links.md`. She'll DM `dm-tiberius` when ready.
- Pre-existing E2E UI tests using legacy `?scope=` URL form (15+ hits in `test_doc_viewer_directory.py` + `test_doc_viewer_multi_repo.py`) still need Phase 4b rewrite. Pre-existing debt, not blocking.
- CoSA submodule commits (above) — Rick's to handle in CoSA context.

---

## Behavior corrections from this session (RE-READ ON RESUME)

Rick corrected you THREE times today, all in the heartbeat-poker thread, all on the same root pattern: **over-stuffing TTS-spoken text and unreadable abstract formatting**.

### Pattern of the failure

- Spoken `message` / `question` field carried inventory (3 observation summaries, 5 drill-option labels, tabular comparisons)
- Abstract was dense tables stacked on dense tables, with no breathing room

### The rule Rick wants enforced

> **Spoken** = headline + ONE-sentence takeaway. ≤15 words for substantive asks. NEVER enumerate inventory in spoken text. Refer the listener to the abstract.
>
> **Abstract** = the detail, but laid out so a HUMAN CAN READ IT. Not a wall of tables. White space. Clear hierarchy. No more than 2 levels deep.

### Verbatim corrections from today (quote them when re-reading to internalize)

1. (15:30-ish) Rick: "Pros/cons alone are inventory; the user wants synthesis."
2. (later) Rick: "I need you to reformat this question It is just way too goddamn full of unformatted unmitigated noise"
3. (later) Rick: "Tiberius as a side note you just pushed a shit ton of content into a TTS string it was unformatible mess remember headlines and takeaways going to the TTS details go into the abstract"
4. (later) Rick: "Again your formatting is all over the map I can't read it ... Just stop. Just stop right here."

### Auto-memory anchors

- `feedback_tts_body_headline_and_takeaway_only` — the rule
- `feedback_always_include_pros_cons_recommendation` — the structure, but compressed
- `feedback_blocking_question_timeout` — give substantive asks 300s minimum, 600s when walk-me-through, MORE if user signals

### On resume, the test

Before composing ANY `ask_yes_no` / `ask_multiple_choice` / `converse` / `ask_open_ended_batch` call:

1. Read the spoken-content field aloud in your head. If it's longer than a single conversational sentence, **rewrite it**.
2. Look at the abstract. If it has 3+ tables stacked, **collapse to one summary block + linkable detail**.
3. Set `timeout_seconds=900` (15 min) for any substantive ask. Rick prefers more thinking time.

---

## Active in-flight commons threads (for awareness)

You sent María 4 DMs on `dm-tiberius` today (all with `in_reply_to` threading):
- `a1a5353a` (her initial doc-link audit question — you answered)
- `591a8931` (her sync-on-Ricardo's-pushback question — you answered with the aggressive-400 implementation story)
- `784b5acb` (her split-confirmation — you confirmed shape b)
- `<your thread-4 handoff>` (you handed off thread 4 to her per Rick's direction, fired at the end of the session)

María is async-drafting her PIP doc-viewer-links.md. She'll ping you back when ready for review-OK.

Rachel 🕊️ sent you 2 DMs (`b617743f` + `784b5acb`-equivalent) about the image-MIME doc-viewer issue. You answered both and shipped the joint patch. She's content; no further loop expected.

---

## On resume, do these in order

1. Read this doc top to bottom (it's short — under 200 lines)
2. Re-read auto-memory `feedback_tts_body_headline_and_takeaway_only` carefully
3. `commons_read(topic="dm-tiberius", limit=5)` to catch any DMs that landed while you were context-cleared
4. Ack Rick that you've re-grounded
5. Wait for his direction (likely "resume heartbeat-poker thread 1")
6. When resuming the thread 1 observations: ONE observation at a time, ≤15-word spoken, tight abstract, 900s timeout

— Tiberius 🌑 (Lupin session `bc15c374`, signing off pre-context-clear)
