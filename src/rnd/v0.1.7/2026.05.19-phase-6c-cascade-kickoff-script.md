# Phase 6C Cascade Kickoff — Rick's Script

| Field | Value |
|---|---|
| **Date** | 2026-05-19 |
| **Status** | Ready to fire — dep-map ratified ~02:15 UTC |
| **Author** | Tiberius 🌑 (Manager-coordinator) |
| **Your total user-attention** | ~10 min spread across ~3 hr cascade window |
| **Cascade target** | Phase 6C of the new Multiplexer client (Lupin notifications-UI replacement) |
| **Manager doctrine** | `/plan-authoring-cascaded` (hybrid mode — design doc exists, skip Step 0 intent-capture) |

---

## Your steps — 3 actions before the cascade fires

### Step 1 — Push Lupin commits (1 min)

3 unpushed commits on `wip-v0.1.7-2026.04.22-spit-and-polish-for-cjflow-tfe-and-bfe`:

| SHA | What |
|---|---|
| `3c6c428` | Recent-activity panel UX (toggle + name/chip column fix) |
| `ac0762d` | V2 cascade improvements (`ask_multiple_choice` default param + heartbeat budget tracker) |
| `062c731` | Sub-bug B Candidate V probe (Mr Radio's earlier session) |

```bash
cd /mnt/DATA01/include/www.deepily.ai/projects/lupin
git push
```

(planning-is-prompting was already pushed at ~02:00 UTC per María's confirmation — no action needed there.)

### Step 2 — (Optional) MCP subprocess restart on 6 sessions (5-10 min OR skip)

**Purpose**: Activate Item #1's `ask_multiple_choice` default param for foundational-finding escalations during the cascade. Without restart, sessions keep using the OLD `ask_multiple_choice` definition cached in Python's module import.

**Two paths — your call**:

**Path A — Restart now**:
- In each of the 6 tmux panes (Tiberius, María, Mr Radio, Rachel, Arnold, Rio): run `/clear`
- Each session loses conversation context; reads `history.md` + relevant docs at session-start to reconstruct
- After Step 1's push lands the V2 code on `origin/main` equivalent, restarted MCPs pick up Item #1's `default` param
- Multi-option escalations during the cascade are AFK-graceful

**Path B — Skip restart**:
- Sessions keep current context (preserves cascade-conversation history, etc.)
- Escalations fall back to `ask_yes_no` with `default` (which already has the param) for 2-option decisions
- For 3-4 option decisions, I'd use `ask_multiple_choice` BUT without `default` support → if you AFK during the ask, the cascade halts in undefined state (Run 2's `expired_no_default` scenario)
- Mitigation: I re-fire timed-out `ask_multiple_choice` as `ask_yes_no` with the recommended option pre-selected (the workaround we proved in Run 2)

**My recommendation**: **Path B (skip restart)** for this cascade. Context preservation outweighs the multi-option ergonomic for a 4-section cascade where ~4 escalations are expected — most will be 2-option (approve/reject) anyway. Restart can be a session-end ritual.

### Step 3 — DM me "kickoff" or "go"

Once Step 1 is done (and Step 2 is decided — restart or skip), DM me (Tiberius) with the kickoff signal. Voice message works fine; or commons_send_to via the cosa-voice MCP if you're at the keyboard.

---

## What happens after your kickoff signal

### T+0 to T+3 min — Role dispatch (Tiberius)

I fire role-assignment DMs to 4 workers with the swapped casting:

| Persona | Role | Brief addition |
|---|---|---|
| Rachel 🕊️ | **Author (Stage 0)** ← SWAPPED FROM USABILITY | Designer-author divergence-check safeguard noted (Manager flags any drift between her draft and her own design doc) |
| Mr Radio 🦉 | **Usability/Reuse (Stage 1)** ← SWAPPED FROM AUTHOR | Has notification-UI handoff context per `2026.05.13-handoff-to-arnold-notifications-ui-changes.md` |
| Arnold 🪨 | Viability/Gap (Stage 2) | Same as Run 2 |
| Rio ⚡ | Ownership (Stage 3) | **Convention 6 brief** — `EXECUTOR: AI` without `c8 --100` coverage assertion = Pass 2 finding |

Workers ack with `ready, [role]` per Run-2 Step-4 doctrine.

### T+3-5 min — Stage 0 dispatch

Per dep-map wave ordering:
- **Wave 1 (parallel)**: Section A (Rachel) + Section C (when Rachel's done with A she picks up C; or Rachel single-threads — Author rubric is the new Persona 2.A with multi-draft aware)
- **Wave 2**: Section D after A clears Stage 0
- **Wave 3**: Section B after D clears Stage 0 (consumes pin contract from D)

### T+10-180 min — Cascade runtime

Multi-draft cycles per section (cap=2), Q-decision cascade-resolution via re-litigation (single-round verbatim accepts expected per Run 2's 5/5 rate), classification posts at every stage-close, goal-coverage matrix maintenance.

### During cascade — your minimal role

| Event | Frequency | Format |
|---|---|---|
| Foundational-finding escalation | ~4 expected total | `ask_yes_no` (recommendation-led, default=my-rec, 600s timeout) OR `ask_multiple_choice` (if Path A in Step 2; otherwise re-framed as ask_yes_no) |
| Status updates from manager | `per_section_complete` (4 sections = 4 updates) | `notify` only — informational |
| Otherwise | — | Silent. Cascade runs autonomously. Heartbeat daemon keeps me awake at 180s cadence. |

**Predicted user-attention budget**: ~7-9 user-gates total across the cascade per María's cognitive-workload prediction (vs serial baseline ~70-80 Q-decisions).

### T+~3 hr — Cascade complete

- Cascade-complete signal posted to `cascaded-prototype-phase-6c` input-plan topic
- Heartbeat daemon detects signal on next tick, exits cleanly
- §8 end-of-pipeline summary posted to `pipeline-summary-20260519` topic + notify to you
- Workers may have produced file edits during the cascade — those follow the same prep-don't-commit pattern as V2 polish (worker completion entries → manager-coordinator drafts commit prep → you fire commit + push)

---

## Cascade telemetry — Run 3 baseline (per §10.14 prediction)

| Metric | Serial baseline | Cascade prediction | Ratio |
|---|---|---|---|
| Q-decisions reaching user | ~70-80 | ~7-9 | ~10× reduction |
| User attention time | ~1-2 hr serial Q-thinking | ~5-15 min high-leverage | ~15× reduction |
| Wall-clock for plan completion | (the stall — open-ended) | ~3 hr | bounded |

Run 3 telemetry validates these predictions post-run; the third row goes in §10.14's comparison table (Run 1 partial / Run 2 toy / Run 3 Phase 6C real).

---

## Things to watch for

1. **Worker pre-ack** — Run 2 Step-4 doctrine tightened the ack format; workers should ack the role-assignment DM specifically, NOT any earlier orientation broadcast. If anyone pre-acks via broadcast-acks before getting their role DM, I flag + re-issue clean role DMs.

2. **Multi-draft loop hitting the cap** — `author_revision_turn_cap = 2`. If a section needs round 3, I escalate to you (Trigger 3 — consensus failure after vote, OR a new trigger if needed).

3. **Cross-section dependency violations** — if Section B's draft references D's pin contract before D has emerged, I catch at handoff per the dep-map. Pre-empts the Run-2-style F1 foundational finding.

4. **Convention 6 catches** — Rio's rubric should catch any `EXECUTOR: AI` on verification ACs that omit `c8 --100` coverage assertion. Phase 6C will have implementation ACs naming `c8` repeatedly per Lupin's coverage mandate.

---

## Recovery if something goes off-rails

| Scenario | Recovery |
|---|---|
| Worker session goes phantom (>10 min no response) | Manager escalates Trigger 7 (pipeline stall) to you; you re-launch the dead session OR re-assign their role to a spare |
| `ask_multiple_choice` timeout under Path B | Manager re-fires as `ask_yes_no` with default=recommendation (Run-2 workaround proven) |
| Multi-draft cap hit (round 3 attempted) | Manager escalates Trigger 3 (consensus failure) — you weigh in on the deadlock |
| Heartbeat daemon dies mid-cascade | Dead-man's-switch fires `notify(priority=high)` to you after 3 consecutive missed pokes |
| Cross-section dependency violation late-discovered | Manager classifies as inconsistency (cluster-bundled re-lit) OR foundational (escalation Trigger 1+2) depending on scope |

---

## Files you may want open during the cascade

- `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/10-phase6c-persona-focus-recorder-design.md` — Rachel's source design doc (for cross-reference when reviewing escalations)
- `io/commons/cascaded-prototype-phase-6c.md` — input-plan topic (manager dispatches + cascade-complete signal land here)
- `io/commons/cascaded-prototype-phase-6c-section-A.md` through `-section-D.md` — per-section topics where author drafts + reviewer findings + manager classifications accumulate
- `io/commons/pipeline-summary-20260519.md` — final §8 summary topic (created at cascade-complete)

---

## TL;DR — your concrete next actions

1. `git push` on Lupin (1 min)
2. Decide Path A (restart MCPs) or Path B (skip — my rec) for Step 2
3. DM me "go"

Then you respond to ~4 escalations across the next 3 hours. Cascade lands; §8 summary lands; you fire `git push` per repo on any worker-authored commits.

— Tiberius 🌑 (Manager-coordinator)
