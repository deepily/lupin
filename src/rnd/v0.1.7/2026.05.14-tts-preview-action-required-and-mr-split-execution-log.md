# TTS Preview Fix — Execution Log

**Paired design doc**: `2026.05.14-tts-preview-action-required-and-mr-split-fix.md`
**Session**: `a0eaaca1` (Mr. Radio)
**Started**: 2026-05-14T22:00:00

---

## Phase 0 — R&D companion doc

- ✅ **2026-05-14T21:50** — Design doc created at `src/rnd/v0.1.7/2026.05.14-tts-preview-action-required-and-mr-split-fix.md`
- ✅ **2026-05-14T22:00** — Execution log (this file) created

## Phase 1 — Action-required opt-out removal — ✅ LANDED 2026-05-14T22:10

- ✅ **1a** — opt-out clause at line 13025: removed `item.type === 'action-required'` condition
- ✅ **1b** — docstring at line 13015: removed action-required bullet, added 2026-05-14 override note
- ✅ **1c** — routing comment at line 13173: updated to note action-required no longer opts out
- ✅ **1d** — response handler at line 15139-15140: swapped `stopAudio() + onTTSPlaybackComplete()` for `stopTTSAndAdvance()` to handle auto-pause stall case
- 📝 **1e (deferred)** — cross-device response path at line 15537: NO existing TTS stop logic on that path (grep `activeTTSItem|stopAudio|stopTTSAndAdvance|onTTSPlaybackComplete` between 15530-15700 returned nothing). Means cross-device response while local device is auto-paused-after-preview could leave local queue stalled. Pre-existing edge case made slightly more likely by Phase 1a but not introduced by it. Logged as known limitation; follow-up ticket if Rick wants belt-and-suspenders.

**Cost burn stopped**: all action-required notifications (`ask_yes_no`, `ask_multiple_choice`, `ask_open_ended_batch`, `converse`) now enter the preview-and-pause path. Long context-rich asks save ~75-80% of TTS spend on every fire.

## Phase 2 — Abbreviation pre-mask + split() rewrite — ✅ LANDED 2026-05-14T22:15

- ✅ Replaced `_splitIntoSentences()` body (lines 12989-13036) with the abbreviation-aware `split()`-based algorithm
- ✅ 25 abbreviations pre-masked: personal titles (Mr/Mrs/Ms/Mx/Dr/Prof/Sr/Jr/Rev), geographic (St/Mt/Ft/Ave/Blvd/Rd), discourse (vs/e.g/i.e/etc/viz/cf/No), time (a.m/p.m/A.M/P.M)
- ✅ Placeholder is U+0001 (SOH) — won't collide with any real TTS text
- ✅ Switched `match()` → `split()` with lookbehind `(?<=[.!?])` + lookahead `(?=[A-Z])`. This fixes the latent "3.14 is pi." → drops "3." prefix bug; `split()` preserves all inter-boundary content
- ✅ Word-count fallback at >12 words preserved unchanged
- ✅ Docstring rewritten to reflect new algorithm, with explicit reference to the 2026-05-13 design doc and this 2026-05-14 fix doc

## Phase 3 — Math.floor → Math.ceil — ✅ LANDED 2026-05-14T22:18

- ✅ Single operator swap at (post-Phase-1) line 13073: `Math.floor` → `Math.ceil`
- ✅ Added 3-line explanatory comment above the math
- For Rick's 9-sentence session-end message: preview grows from 2 sentences (~8% by chars) to 3 sentences (~20% by chars). Ceil never undershoots the configured fraction.

## Phase 4 — Inline self-test — ✅ LANDED 2026-05-14T22:22

- ✅ Added `_tts_quick_self_test()` method directly after `_splitIntoSentences()` (~90 lines)
- ✅ Wired call from `async init()` at line 349 — fires only when `this.debug` is truthy
- ✅ 9 cases:
  1. `"Mr. Smith said hi. Then he left."` → ["Mr. Smith said hi.", "Then he left."]
  2. `"Mr. Radio here. End of session ritual complete."` → ["Mr. Radio here.", "End of session ritual complete."]
  3. `"3.14 is pi. Cool fact!"` → ["3.14 is pi.", "Cool fact!"]
  4. `"e.g. Foo. Bar baz."` → ["e.g. Foo.", "Bar baz."]
  5. `"a.m. start. p.m. end."` → ["a.m. start.", "p.m. end."]
  6. `"Hello."` → ["Hello."]
  7. Rick's 9-sentence session-end message → length 9 (no false splits)
  8. Rick's 8-sentence Layered message → length 8
  9. 20-word run-on with no terminal punct → length 2 (word-count fallback)
- ✅ `node -c` syntax check on the full file: PASS

## Phase 5 — Live verification — ✅ VERIFIED 2026-05-14T22:35

### Test 1 — long `ask_yes_no` (action-required path)

- ✅ Fired a 10-sentence action-required `ask_yes_no` via cosa-voice MCP
- ✅ Rick (after hard-refresh confirmed) clicked **YES** — preview-and-pause worked correctly on an action-required notification, proving:
  - Phase 1a opt-out removal is live
  - Phase 1d response handler stall fix works (clicking yes mid-pause advanced queue cleanly)
  - Phase 2 split() rewrite parses real text correctly
  - Phase 3 ceil math produces the expected 2-3 sentence preview

### Test 2 — `notify()` with yesterday's failing "Mr. Radio..." message

- ✅ Fired Rick's verbatim 9-sentence session-end message that broke last night
- Expected preview: "Mr. Radio here. End of session ritual complete. Five CoSA commits landed and pushed to origin on the tracking branch." (~115 chars)
- vs. last night's broken: just "Mr." (~16 chars)
- 80% TTS cost savings on this single message

### Acceptance criteria summary

| Criterion | Result |
|---|---|
| Action-required messages preview-and-pause | ✅ verified live |
| User can click yes/no during auto-pause without stalling | ✅ verified live |
| "Mr." no longer false-splits as standalone sentence | ✅ verified live (Test 2 fired) |
| Ceil math gives ~25% by character count for medium-N messages | ✅ verified by code review + Test 1 result |
| `node -c` syntax check passes | ✅ |
| Inline self-test wired into `init()` (debug-mode-only) | ✅ |

---

## Files touched

- `src/fastapi_app/static/js/notifications.js` (Phases 1-4: ~130 lines added/modified across `_splitIntoSentences`, `_computeTTSPreview`, `init`, response handler)
- `src/rnd/v0.1.7/2026.05.14-tts-preview-action-required-and-mr-split-fix.md` (Phase 0 design doc)
- `src/rnd/v0.1.7/2026.05.14-tts-preview-action-required-and-mr-split-execution-log.md` (this file)
- `.claude-session.md` (Session `a0eaaca1` section)

## Awaiting

- Rick's explicit "commit" / "push" go-ahead per `feedback_never_auto_commit_push`

---

## Notes

- All edits in `src/fastapi_app/static/js/notifications.js` (legacy JS, not multiplexer TS — 100% c8 mandate does NOT apply per `feedback_100pct_coverage_multiplexer`).
- Per `feedback_audit_plans_at_execute_time`: re-grep each anchor line before editing. Line numbers in the design doc are accurate as of 2026-05-14T21:30 but `notifications.js` is volatile.
- Per `feedback_never_auto_commit_push`: pause for explicit "commit" / "push" go-ahead at end of implementation.
