# Notification-List / TTS-Queue Decouple — Execution Log

**Paired design doc**: `2026.05.14-notification-list-tts-decouple.md`
**Session**: `a0eaaca1` (Mr. Radio)
**Started**: 2026-05-14 PM (immediately after Rick approved Option A + pending-CSS path)

---

## Phase 1 — Decouple Path B from TTS-queue advancement — ✅ LANDED

**File**: `src/fastapi_app/static/js/notifications.js`

- ✅ `handleNotificationUpdate` high/urgent branch (line ~5610): added immediate `addNotificationToSenderGroup(notification, false)` + `updateTotalNotificationsCount()` + `.is-tts-pending` class application via `.audio-control-panel[data-notification-id]` → `.closest('li')` selector. Updated docstring comment to explain decouple semantics.

- ✅ `activateNextTTS` fire-and-forget branch (line ~13278): removed the now-duplicate `addNotificationToSenderGroup` + `updateTotalNotificationsCount` calls. Added pending-class clearing via the same selector pattern. Logged `TTS queue: Engaging fire-and-forget audio for ${id}` to mark engagement (vs. the old "Moving to project card" log).

## Phase 2 — Pending-TTS CSS — ✅ LANDED

**File**: `src/fastapi_app/static/css/notifications.css`

- ✅ Added `.is-tts-pending` rule after `.is-paused-current` rules (around line 428). Treatment: 0.78 opacity, 3px amber-tinted left border (matches paused-palette), ⏳ glyph via `::after` pseudo-element in the corner. Clears smoothly via `transition: opacity 0.2s, border-left-color 0.2s`.

## Phase 3 — Verification

- ✅ **`node -c` syntax check** on `notifications.js`: PASS (no parse errors).
- 🔄 **Live MCP verification**: Rick to trigger a long fire-and-forget notify while TTS is paused. Expect: card appears immediately with ⏳ + amber stripe; second notify also visible; engagement clears the pending class.

## Awaiting

- Rick's live test result.
- Commit via `/plan-bug-fix-mode-wrap` once verified.
