# Rio's Top 5 — TODO + Bug Triage

**Filed**: 2026-05-15 by Rio ⚡ (session `c1cbcd11`)
**Scope**: Top 5 open items in descending importance, **excluding**:
- All María Commons-blackboard work (Phase 3 Steps 3-9 close-out, broadcast UI follow-ons, owner_user_id stamper, host-side stale-bridge sweeper daemon)
- Persona-completion 4× duplication bug (filed today by session `c4139ece`)

---

## 1. `doc_scope` registry exposure for cosa-voice consumption

**Filed**: 2026-05-14 by Rachel (session `4926c582`)
**Status**: Design approved by Rick on the planning-is-prompting side (Phases 2 + 3 already landed there). Lupin owes the runtime side.

**Why this is #1**: Cross-repo doc-link routing currently relies on doctrine + per-repo CLAUDE.md pins. The runtime-discovery design (`doc_scope` dict mirroring `voice_persona` in `get_session_info()`) gracefully degrades until Lupin ships its half, but the integration is structurally incomplete without it. Every multi-repo session pays a tax until this lands.

**Lupin's deliverables**:
- Decide registry exposure mechanism — recommended: `GET /api/docs/scopes` (and optionally `/api/docs/scopes/<project_name>`)
- Implement chosen mechanism — endpoint payload shape `{scope, base_url, allowed_prefixes, source}` per R&D doc §4.3
- Update `_scope_registry.py` if needed to include built-ins (`docs`, `io`)
- Coordinate with cosa-voice — the MCP `get_session_info()` extension consumes this
- Add smoke test — verify all 9 entries (2 built-ins + 7 externals) returned

**Primary doc**: `planning-is-prompting/src/rnd/2026.05.14-doc-link-scope-cross-repo.md`

---

## 2. CC-session focus mode + active-toggle do NOT survive page refresh

**Filed**: 2026-05-14 by María (session `f6f865fb`, parked)
**Status**: First-pass fix `0a7da69` did NOT resolve symptom — user verified post-commit with hard refresh. María pivoted to commons; this is queued for any session to claim.

**Why this is #2**: Daily-UX bug on the main notifications page. User-reported, repeatable, and the previous fix attempt missed.

**Critical diagnostic for next claimant**: Before touching code, capture `[FOCUS-RESTORE]` console output from DevTools during a hard refresh. Three plausible root causes hinge on whether the new `_restoreCcUiAfterLoad()` method ran, ran-and-was-overwritten, or ran-but-mis-classified the focused sender.

**Repro**:
1. Open `/app/notifications` while a CC session has rendered a card
2. Click that session's strip icon → confirm "👁 Focus: ON" + only that card visible
3. Hard-refresh (Ctrl-Shift-R)
4. Observe: ALL cards rendered; toggle pill in default off state

**Related docs**:
- Original feature: `src/rnd/v0.1.7/2026.04.30-cc-session-focus-mode/01-design.md`
- Active-toggle: `src/rnd/v0.1.7/2026.05.02-focus-tray-inactive-toggle/01-design.md`
- First-pass remediation: `src/rnd/v0.1.7/2026.05.14-focus-bar-state-persistence-restore.md`
- Bug entry: `bug-fix-queue.md` line 125

---

## 3. Bounded ClaudeCodeJob migration — D1–D9 awaiting ratification

**Filed**: 2026-05-13 by Tiberius (session `66d534ab`)
**Status**: Audit + migration plan ready; awaiting Rick's pass over the D1-D9 decision matrix. No code touches until then.

**Why this is #3**: Empirically confirmed (2026-05-12, 10-job probe) that bounded ClaudeCodeJobs are zero per-token cost — $2.05 in `cost_usd` telemetry while console balance moved $0.00. Three migration candidates currently call `AsyncAnthropic` via `ANTHROPIC_API_KEY_FIREWALLED` and are structural fits for bounded CC:

- Deep Research agent — largest current firewalled-account line-item
- Podcast script-generation phase (script only; audio TTS stays as-is)
- Presentation content-generation phase (content/YAML only; pptx-assembly stays as-is)

**Decision matrix at a glance**:
- D1: Phase order (Podcast first vs Deep Research first) — recommend Podcast first
- D2: `scheduled_at` default (post-midnight) — recommend YES
- D3: Deep Research progress events — recommend preserve via tool-use
- D4: OpenAI sites — recommend defer (out of scope)
- D5: Runtime Argument Expeditor — recommend defer with revisit trigger
- D6: Output parser — recommend per-migration (lenient Podcast, strict Presentation+DR)
- D7: Pool concurrency limit — recommend keep current values
- D8: `cost_usd` telemetry — recommend keep with "telemetry only" UI disclaimer
- D9: Migration marker convention — recommend `__init__.py` banner pattern

**Primary doc**: `src/rnd/v0.1.7/2026.05.13-bounded-cc-migration-audit-and-plan.md`
**Canonical policy**: `CLAUDE.md` § "COST MODEL — BOUNDED CC vs FIREWALLED SDK"

---

## 4. Multiplexer Phase 6c Q-decisions queue

**Filed**: 2026-05-13 PM by Rio ⚡ (session `9fae8c74`)
**Status**: 15 questions queued across Clusters B, C, D. Cluster A already 5/5 ratified on 2026-05-12. Pull-on-demand cadence — Rick says "next" / "ready" / "fire" and I deliver one at a time.

**Why this is #4**: Multiplexer Phase 6c is the active design-phase frontier. Phase 6b is closed; Phase 6c implementation is gated on these Q-decisions plus REUSE / Pass 1 / Pass 2.

**Queue contents**:

| Cluster | Topic | Q count |
|---------|-------|---------|
| B | Focus tray + focus-mode toggle | 5 (Q-B1..Q-B5) |
| C | Sender-card audio recorder | 6 (Q-C1..Q-C6) |
| D | Conversation-mode UI pin | 4 (Q-D1..Q-D4) |

**Each delivered question carries**: proposed answer + alternatives walked + per-option pros/cons + recommendation with flip-condition (per `feedback_always_include_pros_cons_recommendation`). TTS body = headline + takeaway only; pros/cons + flip-condition go in `abstract` (per `feedback_tts_body_headline_and_takeaway_only`).

**Primary doc**: `src/rnd/v0.1.7/2026.05.02-notifications-ui-js-refactor/10-phase6c-persona-focus-recorder-design.md`

---

## 5. Voice-routing classifier mis-routes "research something and present it"

**Filed**: 2026-05-07 by session `6825e6af`, surfaced during Phase 4b cascade-elimination run on `:8000`
**Status**: Open in bug-fix-queue. Pre-existing classifier issue surfaced by new pytest-path coverage.

**Why this is #5**: Scenario 15 (`EXP_RTPRES_MISSING`) in `test_proxy_integration` fails — voice command "research something and present it" routes to `deep_research` instead of `research_to_presentation`. 14 of 15 scenarios passed; this is the lone failure. **NOT** a regression of the 503-cascade fix.

**Reproducer**:
```
Voice command: "research something and present it"
Expected:      agent router go to research to presentation
Actual:        agent router go to deep research
```

**Likely fix shape**: Additional PEFT training examples via XML prompt generator per `feedback_voice_routing_training_data`. Files: voice-router classifier model + training data, possibly `src/cosa/agents/agent_router/`.

**Cross-ref**: Phase 4b evidence in `src/rnd/v0.1.7/2026.05.05-503-cascade-real-root-cause/90-execution-log.md` §Phase 4b
**Bug entry**: `bug-fix-queue.md` line 139

---

## Items considered but ranked below the cut

- **Phase 7b — Multiplexer toggle widget migration** (~3 hours engineering, non-blocking, separate ticket from speakerphone solo/chorus close)
- **TFE/BFE post-resume proposal-review UX** (filed 2026-04-27 — cluster + propose-fixes presentation needs end-user decision context; user cancelled a resumed job for lack of clarity)
- **Test_suite mode HTTP 500 — actual NoneType.split source** (filed 2026-05-01, smaller blast radius, needs `--auto-proxy` live repro)
- **`claude-agent-sdk` install state in test container** (filed 2026-05-01, ~12 skips at `test_sdk_validation.py`, low impact)
- **Server-side preflight surrogate for `/api/test-suite/submit`** (filed 2026-05-01, architectural follow-up)
- **Smoke harness label improvement: distinguish http_error_503 from "User cancelled"** (filed 2026-05-01, optional)
- **`POST /api/notify/response` ERR_CONNECTION_RESET** (filed 2026-04-22, diagnosed as SSH-tunnel EMFILE; superseded by WebSocket-reconnect circuit-breaker work)
- **Receptionist agent: multiple issues** (filed 2026-04-24, context-length error + bundled issues)
