---
authorized_by: task:3a2f726b-caa5-469c-94aa-67d95f0c3936
doc_kind: plan
---

# Isolating deletion candidates for June, July and August — plan and methodology

**Author** Mr. Radio 🦉 (seat `8fa24215`) · **Date** 2026-09-22 · **Ticket** `3a2f726b`
**Ordered by** Rick, by voice, 2026-09-22: *"create a plan and methodology for isolating candidates for this deletion for June July and August."*
**Status** PLAN. Nothing below has been executed. No file has been deleted for these three months.

> This document is the first written under the new `src/rnd` policy and carries the
> `authorized_by:` frontmatter that policy requires. It passed María 🌸's guard on the way in.
> That is deliberate — a policy nobody has walked through once is a policy with an unmeasured
> door.

---

## 1. Why these three months are not September again

September was cut with a discriminator I built in an afternoon and then had to correct twice.
Both corrections matter here, because June–August is **five times the population** and the
same errors would be five times as expensive.

| what September taught | what it costs here |
|---|---|
| `src/rnd/README.md` is not authorization — 75 of 106 were "cited" only there | counting it would pass ~70% of this set as sanctioned |
| 26 of 106 were **load-bearing** — named by path inside a test | `git rm` on a load-bearing file reddens the merge gate |
| I published six signals having run five | the missed signal found 12 keepers, one of them a live P0's deliverable |
| a killed run prints like a finished one | a partial verification over 341 deletions certifies nothing |

**⇒ The methodology below is the September one with the holes named and closed, not a fresh idea.**

---

## 2. The population, measured 2026-09-22

Derived from the 555 pre-September candidates (see
`src/docs/2026.09.22-pre-september-rnd-deletion-candidates.md`), sliced to these three months.

### 🔴 The date convention is not universal, and a filename sweep silently under-counts

| definition | count |
|---|---|
| **A** — filename carries a `2026.06/07/08` date | **281** |
| **B** — file lives under a `2026.06/07/08` initiative *directory* | 103 |
| **A ∪ B — the real population** | **341** |
| **B − A** — dated-folder sub-docs a filename sweep **misses** | **60** |

Those 60 are the numbered sub-documents inside initiative folders — `00-functional-change-summary.md`,
`01-gap-analysis.md`, `05-arbiter-consumer-implementation.md`, `90-p1-context-headroom-log.md`.
They carry an **ordinal** prefix instead of a date, because inside a dated folder the folder
already carries the date.

⚠️ **A sweep keyed on the filename date would have reported 281 and looked complete.** The
convention in `CLAUDE.md` ("documents should begin with the date") describes top-level documents
and was never true of initiative sub-documents. *Write the predicate the enumeration approximates:*
the question is "does this artifact belong to a June–August initiative," and a filename date is one
imperfect proxy for it.

### Shape of the 341

| month (by filename, where dated) | n |
|---|---|
| 2026.06 | 67 |
| 2026.07 | 86 |
| 2026.08 | 128 |
| *(ordinal-prefixed sub-docs)* | *60* |

| property | n | why it matters |
|---|---|---|
| already under an `_archive/` path | **58** | someone already judged these cold |
| receipt- / run-artifact-shaped names (`log`, `results`, `sweep-`, `probe`, `baseline`, `rehearsal`) | **41** | the class the policy says never belonged in `src/rnd` |
| **names no store row id** | **46** | ⬅ **the only subset that needs a human to read it** |
| names at least one store row id | 295 | the finding is already minted; the file is a duplicate of a row |

**Heaviest directories**: `v0.1.9` (109) · `v0.2.0` (32) · `v0.2.0/_archive/F` (28) · `v0.1.8` (25) ·
`v0.2.1` (19) · `v0.1.8/2026.06.04-heartbeat-hook` (15) · `v0.2.0/2026.08.04-dm-verbosity-reduction` (14).

---

## 3. The methodology — five gates, in this order

Order is load-bearing. Running the cheap tests first means the expensive one (reading files)
is paid on 46 documents instead of 341.

### Gate 0 — LOAD-BEARING. Runs first, overrides every later verdict.

Does any code or test name this path? If yes the file is **never bare-deleted**. Two exits only:

- **PROMOTE** — grant it authorization, add frontmatter, keep it.
- **RETARGET** — move the content, re-point the citation, *then* delete. One commit, in that order.

This is María's Rule 0. My September finding is what put it there: her first draft said non-`.md`
deletes by rule, no judgment, and that rule would have reddened the suite.

**Instrument**: fixed-string search of each basename across `git ls-files 'src'` filtered to
code and test extensions. **Never a regex** — a character class under-reports silently.

### Gate 1 — DURABLE CITATION.

Cited from `CLAUDE.md`, `CLAUDE.local.md`, `TODO.md`, `history.md`, `history/`, `src/docs/`,
or `src/workflow/`? Keep.

🔴 **`src/rnd/README.md` is excluded from the haystack.** It is the index entry every worker is
instructed to add. An index that lists its own directory makes every file in it "cited," and the
signal reads green while measuring nothing.

### Gate 2 — OPERATOR AUTHORIZATION. *(the gate I skipped in September)*

Does the document's own text name Rick as the ordering authority — `(Rick, P0)`, *asked for by
Rick*, *by Rick's voice ruling*, *Rick's broadcast*? Keep.

⚠️ **I published this as part of a six-signal discriminator and then did not run it.** Adding it
afterwards found 12 keepers in September and 20 in the pre-September set, including the deliverable
of row `645a7da5` — a P0 Rick ordered, still in progress. *A signal reported as run and not run is
worse than a signal never claimed*, because it spends the trust the other five earned.

### Gate 3 — CLASS.

Is this a research document at all? Logs, probe rigs, receipts, screenshots, `.failset`/`.meta`
files and shell scripts are **transient by class** and never belonged here. **41 of the 341** carry
receipt-shaped names.

⚠️ Gate 0 still wins: `harvest.sh` is a shell script *and* test-cited.

### Gate 4 — UNMINTED FINDING. The only gate that needs a human.

Of what survives Gates 0–3, does the document name **any store row**? A document that cites a row
is a narrative duplicate of durable work — the finding outlives the file. A document citing **no**
row may hold a finding that exists nowhere else.

**Only 46 of 341 name no row.** Those 46 get read. For each: mint a row for any real finding, then
delete.

In September this test was exact — **1 of 71** named no row (`2026.09.06-the-orphan-tick-is-built-and-not-installed.md`),
it did hold a genuine unminted finding, and it is now row `18ec288d`.

⚠️ **Naming a row is not proof the finding is minted.** A doc can cite the row it was written *for*
while containing a *second*, never-filed finding. The 46-file read catches the clear cases; it does
not make the other 295 provably safe. **Say which you mean:** this is "I looked and found one,"
not "there can only be one."

---

## 4. Execution plan

| phase | action | output | gate |
|---|---|---|---|
| **P1** | Run Gates 0–3 mechanically over all 341 | a candidate list with a per-file reason | none — read-only |
| **P2** | Positive control: plant a known-cited doc, confirm Gate 0 catches it; restore | a receipt | none |
| **P3** | Read the ~46 Gate-4 files; mint rows for real findings | new store rows | none |
| **P4** | **Present the cut to Rick** with per-file reasons and the keep list | one decision ask | 🔴 **Rick rules** |
| **P5** | `git rm` the ruled set; strip `README.md` index entries; annotate dead cross-links | a commit | — |
| **P6** | Full merge pyramid before the commit is pushed | green tiers | 🔴 **Rick pushes** |

**P4 is the only place a file becomes deletable.** Everything before it is measurement.

### Controls that must actually run

- **Positive control before trusting any zero.** A negative result is worth nothing until the same
  search has been watched returning a positive.
- **Delinking is not line deletion.** In September, a naive `grep -vF` over `README.md` would have
  removed the *only* index entry for a **surviving** doc, because one line linked both a deleted and
  a kept file. Check removed-lines-against-survivors *before* applying.
- **Read the exit code with the summary line.** My first September tier printed `25683 passed` and
  `EXIT=124` — a `SIGTERM` at 99%. The passing count looked exactly like success.
- **Never `git add .`.** `src/conf/lupin-app.ini` was dirty from another session throughout this work.

---

## 4a. AMENDMENT, 2026-09-22 ~11:20 EDT — Gate 2 does not mechanize, and P1–P3 were run

I executed P1, P2 and P3 immediately after writing this plan, because they are read-only and need
no ruling. Two results change what §3 and §4 can promise.

### P2 passed — the instrument is now proven, not assumed

Planted a fake test citing `2026.06.02-phase0-findings-and-phase1-execution.md`, a candidate the
sweep had scored as uncited. **0 → 1.** Removed the plant. **1 → 0.**

⇒ A zero from this search now means *no citation*, not *the search is broken*. That is the control
§4 asked for and it is paid.

⚠️ **But Gate 0 re-run over the 341 returned 0 load-bearing, and that number proves nothing.** The
341 were *derived* by excluding everything cited in the same corpora. Re-running the same search is
the same route agreeing with itself — *two sides that derive one value by different routes are
coinciding, not agreeing*, and here it is not even two routes. **A genuinely independent Gate 0
would have to enter from the tests' side**: enumerate every doc path named inside `src/tests/` and
intersect. That has not been done.

### 🔴 P3 found the real blocker: authorization is not recoverable from prose

Reading the 46 turned up documents that are plainly Rick-ordered and that **Gate 2 did not catch**:

- `v0.1.8/2026.06.17-fleet-availability-signaling-design.md` — *"Origin: **Rick voice directive**
  2026-06-17 — 'While you're in a wait state… If not, write a design doc.'"*
- `v0.1.9/2026.06.19-multiplexer-layout-parity-methodology/00-feasibility-report.md` — *"**Question
  owner:** Rick"*
- `v0.1.8/2026.06.04-heartbeat-hook/2026.06.10-poke-abstract-receipts.md` — *"**Rick-directed**"*

None matches the §3 Gate-2 pattern. The phrasings in use include *Rick voice directive*, *Question
owner: Rick*, *Rick-directed*, *Rick-ratified*, *for Rick*, *on Rick's word*, *Rick's explicit word*
— and that list is itself an enumeration, which is the defect it is trying to fix.

**Measured over all 341:**

| predicate | matches |
|---|---|
| my narrow ordering-authority regex | **0** *(by construction — the 341 already excluded its 20 hits)* |
| Rick named in the **header block** (first 10 lines, where provenance lives) | **161** |
| Rick mentioned **anywhere** in the body | **260** |
| **carry any frontmatter at all** | **0** |

⇒ **The three predicates disagree by 260 files out of 341, and none of them is right.** The narrow
one demonstrably misses real authorizations. The broad one is meaningless — Rick is the operator, he
is mentioned everywhere. The header-block one is a better proxy and still cannot tell *"Rick ordered
this"* from *"written for Rick to read"* from *"Rick is cc'd."*

🔴 **This is why the new policy's `authorized_by:` frontmatter exists.** Authorization was never
recorded in a structured field, so it is not mechanically recoverable from documents written before
the field existed. **0 of 341 carry frontmatter.** Going forward this gate is a one-line check.
Retroactively it is a reading problem, and no regex fixes that.

### What that does to §4

**P4 cannot present a mechanically-derived cut.** The honest options, for Rick:

| option | cost | risk |
|---|---|---|
| **A** — read all 341 header blocks and rule per file | real hours | lowest; the only one that can be called accurate |
| **B** — treat the 161 header-mentions as keep, delete the other 180 | cheap | deletes anything whose authorization sits below line 10 |
| **C** — delete only the clearly-safe intersection: receipt-shaped **or** already `_archive/`, and no Rick anywhere | cheapest | smallest cut, highest confidence |

**I recommend C as a first pass and A for the remainder**, because C removes the class that never
belonged here (logs, receipts, superseded archive copies) without needing an authorization judgment
at all — and it leaves the ambiguous middle for a reading rather than a guess.

⚠️ **I am not running any of them.** §4's P4 gate stands: nothing is deletable until Rick rules.

---

## 5. What this plan does not claim

- **Nothing has been read.** All 341 were classified by citation, not by opening them.
- **Gate 0 has not been re-proven on this population.** It passed on three known-cited documents;
  that is a check, not a planted-and-detected control.
- **The 555 figure is a census with a timestamp**, not a closed question. `git ls-files` at
  `2f6d2337`, 2026-09-22. Peers commit; re-derive before acting.
- **This is not a recommendation to delete 341 files.** It is the population and the method.

---

## 6. The finding Rick should see before ruling

| month | new R&D artifacts |
|---|---|
| 2026.06 | 67 |
| 2026.07 | 86 |
| **2026.08** | **128** |
| 2026.09 | 59 *(month cut short by this ticket)* |

**The rate was accelerating, not steady.** September is the smallest recent month only because it
was stopped. ⇒ **A one-time cleanup of 341 files buys about three weeks at the August rate.** The
guard is the durable half of this ticket; the deletion is the arrears.
