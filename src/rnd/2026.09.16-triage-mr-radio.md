# Triage — Mr. Radio 🦉 (blind half), 2026-09-16

**Order**: Rick's broadcast `dcd9edcd` — triage the tickets of Chloé, Maya, Pocholo, Rio, Sam and Tiberius, and suggest priority changes that finish the multiplexer work and get Tiffany 💍 what she needs for the mobile client.
**Written blind**: before reading María 🌸's half (`2026.09.16-triage-maria.md`), per skeleton-crew rule 4. The comparison of the two sets comes after.
**Measured 2026-09-16 ~14:35 EDT**: the six seats own **zero** live rows, because their rows moved to the managers' boards at last night's reaps. Four held rows still carry their names (sam 1, tiberius 3).
**Split agreed with María (DM, 14:32–14:34)**: she takes those 4 rows plus the 9 held rows on my board the six seats filed. I take my live queue, my own held rows, and the mobile path.

**Recommendation basis**: Rick's two goals, (1) finish the multiplexer and (2) unblock mobile. Anything else steps down one level unless it is a live security or merge-gate problem.

## My live queue (accountable: Mr. Radio)

| Row | Now | Suggest | Why |
|---|---|---|---|
| `645a7da5` multiplexer parity build | P0 | **P0, keep** | Goal 1. §6 is ruled except item 6 (rulings uncommitted, waiting on María's confirmation). |
| `05ddc8f0` Whisper cuts transcript at a pause | P0 | **P0, keep, and do it first** | Goal 2. A small fix (`return_timestamps=True` at two model-server calls and two local paths) that has to land **before** the Opus accuracy check, or that check measures this bug instead of the codec. Under skeleton crew a manager builds it. |
| `9b1f7701` Opus/AAC accuracy check | P1 | **P1, keep** | Goal 2. Blocked on Rick: the ten voice memos are not on disk (Tiffany checked at 09:50). Asked at 14:36. |
| `f0e00f01` 19 E2E reds | P1 | **P1, keep** | The merge gate for everything, multiplexer included. 12 snapshots open; the rebaseline needs six verdicts from Rick. |
| `0f4728bc` five preconditions before Phase A | P1 | **Close into `645a7da5`** once §6a is committed | Every precondition it names is a §6 item, now ruled there. Two rows tracking one gate go stale apart. |
| `e453a854` V1 snapshot self-inconsistent | P1 | **P2, fold under `f0e00f01`** | Commit `0deed068` explains the inconsistency (a debug log stamps the wall clock). What's left is a snapshot fix inside the E2E lane. |
| `6698d40f` poke tells agents to hand-write holds | P1 | **P2** | Fleet tooling, neither goal. The hold verb already exists and works (used today at 13:47). |
| `fb27b166` legacy Retry re-asks a 100-char question | P1 | **P3** | A legacy-client defect. Legacy is deleted once the multiplexer is done (Rick, 2026-09-10). Checked from source at 14:45: the multiplexer doesn't share the cut. Legacy truncates into a `data-question` attribute (`notifications.js:6752`), while the multiplexer's retry reads `job.meta["question_text"]` whole (`JobsPaneRenderer.ts:743`). Not run live. |
| `8d0c7c83` three E2E reds nobody owns | P2 | **P2, fold under `f0e00f01`** | Same merge gate, same lane. |
| `5ac999f5` mobile Flutter suite: 46 failing tests | P3 | **P2** | Goal 2. Tiffany's full-suite runs carry 46 known reds (44 legacy_quarantine + 2 wire-contract), so a new red can hide among them. |
| `c51e92da` phone round-trip probe | P2 | **P2, keep** | Goal 2. Built and merged (mobile wip `77b86b7`); needs Rick's handset on wifi. The LTE half needs an address reachable from outside the house. |

## My own held rows (not filed by the six seats)

| Row | Now | Suggest | Why |
|---|---|---|---|
| `12457b56` restore Stop poke at 17:00 | P1 | **Approve** | Rick's order for today. |
| `966b46ba` `task_promotion_gate.py` at 97% | P2 | **P2, approve** | Breaks the 100% coverage mandate on main. |
| `6bf8a819` DOM-assert ratchet red on an unowned file | P2 | **P2, approve** | A red in the unit tier's lint. |
| `6444feeb` a build is repo-global | P2 | **P3** | Already controlled by skeleton-crew rule 7 (announce builds). The row is the tooling fix. |
| `0bcbaead` manifest backticks mis-name an owner | P2 | **P3** | Commit-guard cosmetics, neither goal. |
| `9f54712b` `dom_assert_lint` order dependence | P3 | **P3, approve** | Neither goal. |

## Tiffany's path, in order

1. `05ddc8f0` Whisper fix (P0). Needs a model-server restart, which is gated while jobs run.
2. Rick's ten recordings copied to `lupin/io/opus-accuracy/recordings/`.
3. `9b1f7701` accuracy run on `:8000`, submitted through `/api/test-suite/submit`.
4. `c51e92da` handset probe on wifi. LTE only if an outside address exists.
5. `5ac999f5` Flutter reds, so the mobile suite can see new failures.
