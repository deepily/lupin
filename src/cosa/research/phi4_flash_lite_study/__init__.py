"""
Phi-4 14B vs Gemini gemini-3.1-flash-lite on the DM Tutor's rewrite task.

Same prompt, same production fabrication guard, same replayed DM bodies, same
counters — the model is the only thing that differs between the arms.

An IMPORTABLE package rather than a dashed script directory, so coverage can be
measured normally (`--cov=cosa.research.phi4_flash_lite_study`) and `main()` can
be tested rather than excluded.
"""
