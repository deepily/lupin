"""Durable CoSA test-tooling (reusable triage/measurement harnesses).

Distinct from `cosa/tests/unit/tools/` (which holds the *tests* for these
tools) and from `cosa/tools/` (product/integration tools). This package is the
home for durable, reusable TEST tooling promoted out of ad-hoc scratchpad
harnesses — e.g. `measure_visual_flake` (visual-flake triage over Sam's
tolerant comparators).
"""
