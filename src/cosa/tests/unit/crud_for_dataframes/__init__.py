"""
DataFrame CRUD Unit Tests

Unit tests for the cosa.crud_for_dataframes package — the voice-driven CRUD
surface over per-user pandas DataFrames (schemas, XML intent model, parquet
storage, stateless CRUD operations, dispatch/voice-formatting, Claude Code
intent-extraction fallback, and the AgentBase-subclass agents).

Created 2026-06-03 (CoSA coverage campaign, utils+config+tools+crud lane —
Cheech 🌿). New package.
"""
