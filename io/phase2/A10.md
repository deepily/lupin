### A10 — Epic Board `✓`

Legacy: `notifications.html:1079-1110` (markup, including the header's `⊟`/`⊞` at `:1091-1098`), `notifications.js:447-464` (epic constants), `:12436-12441` (the shared tick), `:14340-14360` (the one-fetch-one-clock rule), `:14362-14448` (stories, epic key, title, story text), `:14449-14472` (waits-on-Rick), `:14474-14552` (grouping and ordering), `:14554-14667` (slug, default, state load/save, expanded predicate, toggle), `:14668-14731` (row, group), `:14733-14796` (table and its three sections), `:14798-14856` (dispatch, count, stamp), `:14871-14913` (in-place collapse, toggle handler), `:15069-15095` (the pane's delegated listeners), `:15097-15140` (keys in DOM, collapse-all, expand-all), `:12934` (`_closeDisclosedRowsIn`). Multiplexer: `render/EpicBoardRenderer.ts`, `render/epicBoardModel.ts`, `render/epicBoardCollapse.ts`, `render/templates/epicBoardTable.ts`, `stores/EpicStoriesStore.ts`, `render/templates/sectionToolbar.ts:66`, `boot.ts:719-752`.

The model, the sections, the strings and the persistence contract are a faithful port. Two gaps: the header's bulk collapse pair, and — the serious one — the pane's row controls are rendered but not wired.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure (the SECTION) | **open**: `#epic-board-section` carries no `collapsed` class (`html:1105`) (source) | **open**: `data-collapsed` is set only by a click (`EpicBoardRenderer.ts:152`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('epic-board-section')`; all four header buttons carry `event.stopPropagation()` (`html:1086-1103`) (source) | delegated header click; every header control is a `<button>` so the guard exempts it (`sectionHeader.ts:145-192`) (source) | MATCH |
| B3 | Persistence (the SECTION) | **not persisted**: absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **not persisted**: session-only (`sectionHeader.ts:12-15`) (source) | MATCH |
| B4 | Toolbar show/hide | `🗂️` → `section-epic-board` (`html:50`) (source) | `🗂️` → `epic-board-pane` (`sectionToolbar.ts:66`) (source) | MATCH (different storage keys, as elsewhere) |
| B5 | Collapse/expand-all | **the pane has its own pair in its header**: `⊟` → `collapseAllEpics()` and `⊞` → `expandAllEpics()`, which write an EXPLICIT choice for every rendered key (so it overrides even a group whose default is open) and update each group's DOM in place without a re-render (`html:1091-1098`, `js:15110-15140`) (source) | **absent**: the header carries only `⟳` and the updated stamp (`EpicBoardRenderer.ts:113-129`); the toolbar pair goes to Notifications, never here (`SectionToolbarRenderer.ts:115-122`) (source) | **OWED** — an epic-level bulk collapse, with legacy's explicit-choice semantics |
| B6 | Auto-reveal | none found (source) | none found (source) | MATCH |
| B7 | Live header | `🗂️ Epic Board: <count>` where the **count is EPICS, not rows** — the macro unit — plus `#epic-board-updated` reading `updated HH:MM:SS TZ` (`html:1087-1090`, `js:14849`, `:14858-14869`) (source) | `🗺️ Epic Board · <count>` with the same epics-not-rows rule, stated as the one place this pane disagrees with its siblings, and the same stamp through the same formatter (`EpicBoardRenderer.ts:13-17`, `:240`, `:288-292`) (source) | MATCH (the glyph differs — Phase 1's business) |
| B8 | Refresh control | `⟳` → `refreshTaskList()`, so it refreshes the Task List too; the tooltip says so, because the two panes share one fetch (`html:1099-1102`) (source) | `⟳` → the TASK LIST store's `refresh()`, so it likewise refreshes both (`EpicBoardRenderer.ts:118`) (source) | MATCH. **Note:** the mux button carries no tooltip saying it also refreshes the task list |
| B9 | Nested accordions (epic groups) | one `<tbody class="epic-group" data-epic>` per section; header bar = chevron `▾`/`▸` + label + count, `role="button" tabindex="0" aria-expanded`, `aria-controls` the slug; **all epics COLLAPSED by first-load default**, with ⏳ Waiting-on-Rick the documented exception because a collapsed highlight highlights nothing; choices persisted to `localStorage["lupin.epicBoard.groupState"]` as a MAP of key → isEXPANDED holding only groups the viewer actually toggled, so a newly-minted epic takes the default rather than inheriting a stale set (`js:14554-14666`, `:14693-14731`) (source) | identical on every clause — same key, same `__on_rick__` / `__drift__` sentinels, same slug rule, same map-of-choices shape, same on-Rick-only default (`epicBoardCollapse.ts:27-128`, `templates/epicBoardTable.ts:104-147`) (source). The toggle handler additionally treats the persisted choice as the source of truth rather than reading the class, so a repaint arriving mid-click cannot flip it the wrong way (`EpicBoardRenderer.ts:265-281`) | MATCH, with the read-the-choice-not-the-class discipline as EXTRA |
| B10 | Empty / degraded states | **four**, mirroring the task list so the two panes degrade identically: `auth_required` (count → "0"), `query_unavailable` (count → "0"), `unreachable` → "⚠️ Store unreachable — showing nothing rather than something stale." with **the count deliberately left alone**, and otherwise the table. **No truncation banner**, on purpose, because the pane above already carries one for the same rows (`js:14807-14843`) (source) | the same four, same strings carbon-copied, the same leave-the-count-alone on unreachable with the reasoning spelled out, and the same deliberate absence of a truncation banner (`EpicBoardRenderer.ts:22-23`, `:46-49`, `:207-232`) (source) | MATCH |
| B11 | Layout mode | not referenced by the layout code (search: `grep -n section-epic-board notifications.js` → no match) (source) | a sibling pane in the main column (`multiplexer.html:261-262`) (source) | MATCH |
| E1 | Fetch and clock | **no fetch and no timer of its own**: `renderEpicBoard( composite )` takes the task list's already-fetched composite as an argument, called straight after `renderTaskList` — one fetch, one clock, because two clocks read as a bug the first time they disagree (`js:14340-14352`, `:12436-12441`) (source) | the same rule by subscription: it reads the task list's store off `store_task_list_changed` and never fetches (`EpicBoardRenderer.ts:6-11`, `:156-161`, `boot.ts:719-724`) (source) | MATCH |
| E2 | Open-row filter | the SAME `isTaskOpenStatus` filter the task list applies, so the panes cannot disagree about which rows exist (`js:14845-14847`) (source) | the same `isOpenStatus`, same stated reason (`EpicBoardRenderer.ts:234-236`) (source) | MATCH |
| E3 | Section order and membership | ⏳ Waiting on Rick FIRST (shown only when non-empty, and a **highlight, not a move** — its rows also appear under their own epic), then the per-epic groups, then 🔴 Drift LAST — and **Drift renders even when empty**, as a green "✅ No drift", because a section that vanishes when satisfied cannot be told from one that failed to render (`js:14733-14795`) (source) | the same three sections in the same order, the same on-Rick-only-when-non-empty rule, the same always-render-drift rule, and the four labels plus two story lines carbon-copied (`templates/epicBoardTable.ts:45-62`, `:155-185`) (source) | MATCH |
| E4 | Which rows count as waiting on Rick | any `blocked_by` ref whose id is "rick" (case- and space-insensitive) with kind ∈ {user, persona} — **kind is ignored on purpose**, mirroring the generator, because the same human appears under both and a kind-keyed selector silently drops half the queue (`js:14449-14472`) (source) | the same predicate and the same reasoning (`epicBoardModel.ts`) (source) | MATCH |
| E5 | Group ordering | mirrors the board generator's `sort_key`: `epic:unassigned` sinks LAST among epics, otherwise biggest bucket first, ties broken by key so equal-sized epics never reshuffle between renders. Within a group: **priority first** (Rick's 2026-09-09 ruling), then status rank, then title. On-Rick sorts priority then id (`js:14518-14551`) (source) | the same comparators — this is one of the four copies the legacy comment tracks at `js:14518-14520` (`epicBoardModel.ts`) (source) | MATCH. **Note:** legacy flags "keep all four identical until a parity guard exists — row 507183ff"; that guard is still owed, and it is not this inventory's row |
| E6 | Epic titles and stories | titles and one-line stories come from `GET /api/epic-stories`, fetched **once** and memoized for the page's life — a hand-edited file is not live state; an epic with no entry falls back to its de-slugged key; the story rides INSIDE the group so opening an epic answers "what is this?" in the same gesture (`js:14362-14448`, `:14723-14727`) (source) | the same one-shot memoized load, the same fallback, the same story-inside-the-group (`stores/EpicStoriesStore.ts`, `epicBoardModel.ts`, `templates/epicBoardTable.ts:132-139`). Plus the load's arrival triggers a repaint that deliberately does **not** move the updated stamp, because the stories are a lookup and not this pane's data — measured: without it the board sat on de-slugged names with no story rows for 15 s while the data had already arrived (`boot.ts:743-752`, `EpicBoardRenderer.ts:182-196`) (source) | MATCH, with the arrival repaint as EXTRA |
| E7 | Row shape | rendered through the **same row renderer** as the task list, cell-for-cell, with only the row class differing — it used to be a second implementation with its own column order and no Class field, and killing that was the point (`js:14668-14691`) (source) | the same shared `renderDisclosedRow`, called with the `"epic-board"` scope (`templates/epicBoardTable.ts:143`, `templates/taskRowDisclosed.ts:176-207`) (source) | MATCH |
| E8 | Row CONTROLS reaching a handler | the pane wires its own delegated listeners on the persistent container: a click dispatcher (`_handleEpicBoardClick` → request chips, id copy, detail 📄, row controls) **plus `_wireVerbSelects`** for the verb `change` events (`js:15069-15095`) (source) | **not wired**: the container's only listeners are the accordion click and the accordion keydown (`EpicBoardRenderer.ts:139-149`), and the click handler is `handleAccordionActivate`, which returns immediately unless the target is inside an `.epic-group-header` (`:252-256`). No `change` listener exists on this pane at all, and no other listener reaches it — search `grep -n "addEventListener\|epic-board-pane" multiplexer/boot.ts` shows only the document/window lifecycle listeners and the mount lookup. Yet the rows it paints DO carry the controls: `renderDisclosedRow` emits the actions cell with priority · owner · verb · reason · Submit and the ⋯ / detail / id-copy (`taskRowDisclosed.ts:151-155`) (source) | **OWED, and this is the sharpest row in my four.** Every control in the mux epic board is painted and dead — it is exactly the defect legacy recorded and fixed for the holding area: "THIS PANE HAD NONE AT ALL, AND SO EVERY CONTROL IN IT WAS DEAD … the buttons painted and the clicks landed on no handler" (`js:13927-13939`) |
| E9 | In-place collapse | `_applyEpicGroupCollapseState` flips the class, the header's `aria-expanded` and the chevron without a re-render, **and closes any disclosed rows inside the group** on collapse (`js:14871-14892`, `:12934`) (source) | flips the same three things in place, but does **not** close disclosed rows — search `grep -rn "closeDisclosed" multiplexer --include=*.ts \| grep -v test` → no match (`EpicBoardRenderer.ts:276-281`) (source) | **OWED** — a collapsed group can hide an open controls row that comes back on expand |
| E10 | Keyboard | Enter / Space on a focused `.epic-group-header` toggles it, `preventDefault` so Space does not scroll; both the modern `" "` and the legacy `"Spacebar"` key values are accepted (`js:15084-15091`) (source) | the same, both spellings (`EpicBoardRenderer.ts:143-149`) (source) | MATCH for the header. ⚠️ Legacy's keydown also only covers the header here — the row's own keyboard paths live in its click dispatcher, which is E8's gap |
| E11 | Operator-state survival | `_captureOperatorState` / `_restoreOperatorState` wrap this pane's repaint too, so an open disclosure or a typed reason survives the 60 s tick (`js:14851-14853`) (source) | **absent** — the repaint is an unconditional `replaceChildren` (`EpicBoardRenderer.ts:242-243`); search as in A8 T16 → no match (source) | **OWED** — same build item as A8 T16 and A9 H13 |
| E12 | Count on a torn payload | a non-array `tasks` takes the unreachable branch and the count is left standing rather than zeroed (`js:14840-14843`) (source) | same, with the reasoning written out (`EpicBoardRenderer.ts:225-231`) (source) | MATCH |

**A10 build items**: (1) wire this pane's row controls — a delegated click dispatcher covering the ⋯ disclosure, detail 📄, id-copy and row verbs, plus a `change` listener for the verb / priority / owner selects (E8); (2) the header's `⊟`/`⊞` epic bulk collapse with legacy's explicit-choice semantics (B5); (3) close disclosed rows when a group collapses (E9); (4) operator-state capture and restore across the repaint (E11, shares A8 T16 / A9 H13). Optional: the ⟳ tooltip saying it refreshes the Task List too (B8).

**Carried forward, not this pane's row**: legacy flags four hand-maintained copies of the urgency comparator needing a parity guard (row `507183ff`, `js:14518-14520`).
