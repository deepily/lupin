### A15 — Direct TTS Test `✓`

Legacy: `notifications.html:1366-1381` (markup), `notifications.js:74` (mode constants), `:1771-1774` (Speak Now wiring), `:1789-1800` (the three test buttons), `:4236-4258` (`directTTSTest`), `:4260-4264` (`getCurrentTTSMode`), `:4280-4285` (`testTTS`), `:4287+` (`playTTS`), `:5067-5100+` (`stopAudio`).
Multiplexer: **absent**. Searches of non-test multiplexer source for `direct-tts`, `Direct TTS`, `Speak Now`, `test-instant` and `test-reliable` return **0 hits**, and `grep -n 'direct-tts' html/multiplexer.html` returns nothing — there is no mount point either.

Because the section is absent, every legacy behaviour below is **OWED**.

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | **open and visible**: `#direct-tts-section` carries no `collapsed` (`html:1372`), `#section-direct-tts` no `section-hidden` (`:1367`), and its toolbar button starts `active` (`:74`). Unlike A4 and A11 it carries **no** inline `display:none` | OWED |
| B2 | Toggle trigger | click anywhere on `.section-header` → `toggleSection('direct-tts-section')`; chevron `#direct-tts-toggle` is a `<button>` (`html:1368-1370`) | OWED |
| B3 | Persistence | **none**: `direct-tts-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) and no per-feature key names it (`grep -nF 'direct-tts' notifications.js` → four hits, all DOM lookups, none storage) | OWED (match: no persistence) |
| B4 | Toolbar show/hide | `🔧` button, `data-section="section-direct-tts"`, title "Direct TTS Test" (`html:74`), persisted via `lupin_section_visibility` | OWED |
| B5 | Collapse/expand-all | not affected | — |
| B6 | Auto-reveal | none found (`grep -nF 'section-direct-tts' notifications.js` → 0 hits) | — |
| B7 | Live header | static "🔧 Direct TTS Test (Bypass Q&A)" (`html:1369`) | OWED |
| B8 | Refresh control | none | — |
| B9 | Nested accordions | none | — |
| B10 | Empty state | none — a text input and four buttons, always present | OWED (as markup) |
| B11 | Layout mode | not referenced by the horizontal-layout code | — |
| D1 | Text input | `#direct-tts-input`, placeholder "Type text to speak directly…" (`html:1374`). **No Enter-key handler** — `grep -n "direct-tts-input" notifications.js` finds only the value read at `:4237` | OWED |
| D2 | 🔊 Speak Now | `#direct-tts-button` → `directTTSTest()` (`js:1771-1774`). Empty/whitespace text is refused through `this.error("Please enter text to speak")`, which writes into the **A16 debug log** and the console — there is no inline status element (`js:4239-4243`, `:21020-21023`) | OWED |
| D3 | Mode source | the mode is read from `#tts-mode` — **the select that lives in the Q A section** (`js:4245`). This is the A1 Q6 cross-section dependency seen from the other end: build Q A's mode select and this section reads it | OWED — and see the note |
| D4 | The speak path | `await this.playTTS( text, mode )` — a direct call that deliberately bypasses Q A, job completion and every WebSocket event, with two explicit debug lines saying so (`js:4249-4255`). `playTTS` checks the audio cache first and plays a hit without a server round-trip (`js:4287-4305`) | OWED |
| D5 | Input clearing | the input is cleared **after** the await resolves (`js:4257`) | OWED |
| D6 | Test Instant TTS | `#test-instant-tts` → `testTTS('instant')`, which speaks the fixed string "This is a test of the text-to-speech system in instant mode." (`js:1790-1792`, `:4280-4285`, constant at `:74`) | OWED |
| D7 | Test Reliable TTS | `#test-reliable-tts` → `testTTS('reliable')`, same fixed string with "reliable" (`js:1794-1796`) | OWED |
| D8 | Stop Audio | `#stop-audio` → `stopAudio()`, which stops the pulsing card indicator and then tears down **four** independent audio paths: `currentAudio`, `audioElement` (revoking its object URL), `currentSequentialAudio`, and the Web Audio API sources (`js:1798-1800`, `:5067-5100+`) | OWED |
| D9 | No wiring guard | all four listeners are attached **unguarded** — `document.getElementById(...).addEventListener(...)` with no null check (`js:1772`, `:1790`, `:1794`, `:1798`), so removing any of these four ids from the markup throws during setup and kills every listener registered after it | OWED as a **caution**, not as a behaviour to copy |

**Note on D3.** The TTS-mode select is markup in the Q A section but page-wide state: A1 Q6 already records that other sections read it (`js:15756`, `:20591`, `:20940`), and this section is a fourth reader (`js:4245`, `:4262`). When the build decides where that value lives, this section is one of its dependants. A15 cannot be built before that decision.

**Note on D9.** This row is the reason A15 and A1 are coupled in build order beyond D3: legacy's setup function wires Q A and Direct TTS in the same unguarded run (`js:1771-1800`), so a partial port that creates one section and not the other reproduces the throw.

**A15 build items**: the whole section, specified by B1–B4, B7, B10 and D1–D8 — open and visible by default, unpersisted, toolbar `🔧`; a text input with no Enter handler; **🔊 Speak Now** (refuse empty text via the debug log, read the page-wide TTS mode, call the direct play path bypassing Q A and the queue, clear the input after); **Test Instant TTS** and **Test Reliable TTS** with their fixed strings; and **Stop Audio** tearing down all four audio paths. Guard the listener wiring rather than copying D9.
