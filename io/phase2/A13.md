### A13 — Time Saved Dashboard `✓`

Legacy: `notifications.html:1266-1297` (markup), `:73` (toolbar `⏱️`), `notifications.js:602` (load at init), `:8608-8661` (`refreshTimeSavedStats`), `:8663-8690` (`renderTopSolutions`). Multiplexer: **absent**. A search of non-test multiplexer source and of `multiplexer.html` for `time-saved`, `timeSaved`, `time_saved`, `top-solutions` and `topSolutions` returns **0** hits; the same search shape for `fleet-state` returns 5, so the instrument finds things when they are there.

Because the section is absent, every legacy behaviour below is **OWED**. The rows exist so the build has a complete behaviour spec, not just "add a Time Saved section".

| # | Behaviour | Legacy (lead) (source) | Verdict |
|---|---|---|---|
| B1 | Default disclosure | open: `#time-saved-section` carries no `collapsed` class (`html:1273`) | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('time-saved-section')` (`html:1268`), flipping `collapsed` and the chevron `▼`/`▶`; the `🔄` calls `event.stopPropagation()` first (`html:1270`) | OWED |
| B3 | Persistence | **none**: `time-saved-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`), so it opens on every load | OWED (match: no persistence) |
| B4 | Toolbar | `⏱️` hides and shows the section (`html:73`; A0) | OWED |
| B5 | Collapse/expand-all | not affected (`notifications.js:14312-14337` acts on Task List owner groups) | — |
| B6 | Auto-reveal | none found: `grep -n "time-saved-section" notifications.js` returns zero hits — the id lives only in the markup (`html:1268`, `:1273`) | — |
| B7 | Live header | static title "⏱️ Time Saved Dashboard" — no count, no updated stamp (`html:1269`) | OWED |
| B8 | Refresh control | `🔄` → `refreshTimeSavedStats()`, which refetches BOTH endpoints and repaints; disclosure untouched, no debounce guard and no auto-poll (`html:1270`, `notifications.js:8608-8661`) | OWED |
| B9 | Nested accordions | none | — |
| B10 | Empty state | the four stat values read `--` until the first fetch (`html:1276-1289`); the leaderboard reads "Loading..." before the first fetch (`html:1294`) and "No replayed solutions yet" when the list comes back empty (`notifications.js:8681-8684`) | OWED |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "time-saved" notifications.js | grep -i "layout\|left-column"` → no hits) | — |
| T1 | First load | fetched once at init, not on expand and not on a timer (`notifications.js:602`) | OWED |
| T2 | Stats fetch | `GET /api/stats/time-saved` with a bearer token; on 2xx it paints four values — `total_time_saved_formatted` → "Time Saved (You)", `time_saved_for_others_formatted` → "Time Saved for Others", `solutions_created` → "Solutions Created", `total_replays_benefited` → "Cache Hits (You)". The two formatted values fall back to `--` and the two counts to `0` when the field is missing. A non-2xx logs and leaves the previous values on screen (`notifications.js:8623-8645`) | OWED |
| T3 | Leaderboard fetch | a SECOND call, `GET /api/stats/time-saved/global`, feeds `renderTopSolutions(globalStats.top_solutions)`. A non-2xx logs and leaves the leaderboard as it was (`notifications.js:8647-8656`) | OWED |
| T4 | Leaderboard rendering | one `.top-solution-item` per solution: `#rank` · the question (HTML-escaped) · "N replays · X saved" from `replays` and `time_saved_formatted`, ranked in the order the server returned (`notifications.js:8687-8690`) | OWED |
| T5 | Error handling | the whole refresh is wrapped in a try/catch that only logs — a throw never clears the dashboard or surfaces a message to the user (`notifications.js:8658-8660`) | OWED |
| T6 | Auth path | both calls use a bare `fetch` with `Authorization: Bearer ${this.accessToken}` — NOT `authedFetch`, so neither one refreshes an expired token (`notifications.js:8624-8626`, `:8648-8650`). Recorded as the lead's behaviour, not endorsed | OWED — see note |

**Note on T6.** These two are among the few legacy calls that skip the token-refreshing wrapper every other panel uses. A build that ports this section should use the multiplexer's `ApiClient` rather than copy the bare `fetch`; that is a superset, not a deviation from what the user sees.

**A13 build items**: the whole section, specified by B1–B4, B7, B8, B10 and T1–T6.
