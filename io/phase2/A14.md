### A14 — System Status `✓`

Legacy: `notifications.html:1300-1364` (markup), `:74` (toolbar `📊`), `notifications.js:1103-1180` (health monitor text), `:1376-1396` (logout), `:1399-1458` (config reload), `:1460-1501` (`refreshAllStatus`), `:1503-1535` (WebSocket status), `:1537-1575` (auth status), `:1577-1583` (session display), `:1585-1612` (copy-to-clipboard), `:21263-21305` (missed count + reset).
Multiplexer: **no System Status section**. `grep -rn "queue-ws-status|audio-ws-status|auth-status|ws-health-status|config-status|/api/init|user-display|queue-session"` over non-test multiplexer source returns no match for any of those ids; `multiplexer.html` declares no status pane. Three of the section's behaviours DO exist in the multiplexer, in other places: logout and the user's email in the nav bar (`render/NavBarRenderer.ts:81-114`, `render/templates/navBar.ts:100`), and the missed-count badge with its Reset (`render/MissedBadgeRenderer.ts:88-106`).

Every row below is therefore either **OWED** (nothing in the multiplexer does it) or **MATCH (elsewhere)** — the behaviour exists but not in this section, which is a Phase 1 placement question, not a missing behaviour.

| # | Behaviour | Legacy (lead) (source) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | open: `#status-section` carries no `collapsed` class (`html:1312`) | no section | OWED |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('status-section')` (`html:1301`); the `↻` stops propagation (`html:1305`) | — | OWED |
| B3 | Persistence | **none**: `status-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) | — | OWED (match: no persistence) |
| B4 | Toolbar | `📊` hides and shows the section (`html:74`; A0) | the toolbar has no status entry (`templates/sectionToolbar.ts:49-66`) | OWED |
| B5 | Collapse/expand-all | not affected (`notifications.js:14312-14337`) | — | — |
| B6 | Auto-reveal | none found: `grep -c "status-section" notifications.js` → 0; the id appears only in the markup (`html:1301`, `:1312`). Instrument check: `grep -c "queue-ws-status" notifications.js` → 6 | — | — |
| B7 | Live header | static title "System Status" — no count, no stamp (`html:1302`) | — | OWED |
| B8 | Refresh control | `↻` → `refreshAllStatus()`: disables itself and spins, then re-runs the WebSocket, auth, session and health readouts in order, and always re-enables in a `finally` (`html:1304-1308`, `notifications.js:1460-1501`) | — | OWED |
| B9 | Nested accordions | none | — | — |
| B10 | Empty state | not applicable — every row has a seeded value: both sockets read "Disconnected", auth "Not authenticated", user "Loading...", health "Initializing...", the two session codes `-` (`html:1315-1352`) | — | OWED (the seeded values are the spec) |
| B11 | Layout mode | not referenced by the horizontal-layout code (search: `grep -n "status-section\|status-item" notifications.js | grep -i "layout\|left-column"` → no hits) | — | — |
| S1 | Queue WebSocket row | a pill mapping the channel state to text + class: DISCONNECTED "Disconnected"/error · CONNECTING "Connecting..."/warning · AUTHENTICATING "Authenticating..."/warning · CONNECTED "Connected"/good · BACKOFF "Reconnecting..."/warning · OPEN_CIRCUIT "Circuit open"/error; no channel at all reads "Not initialized"/error. Also updated live from socket events, not only on `↻` (`notifications.js:1503-1527`, `:2875-2897`) | no such readout: the mux tracks the same states in `transport/ConnectionStateMachine.ts` but paints no status element (search above) | **OWED** |
| S2 | Audio WebSocket row | the same pill for the audio channel (`notifications.js:1529-1535`, `:3010`) | — | **OWED** |
| S3 | Authentication row | "Not authenticated" with no token; on an expired token it refreshes and, on failure, reads "Token expired"/error; otherwise "Authenticated" plus " (admin)" when the JWT carries the admin role (`notifications.js:1537-1575`) | — | **OWED** |
| S4 | Missed row | "N missed while away" with a Reset button, both shown only while N > 0; Reset POSTs `/api/notifications/undelivered/dismiss` with NO confirm (Rick: a soft dismiss is reversible) and alerts on failure (`html:1326-1328`, `notifications.js:21263-21305`) | the badge and Reset exist with the same show-only-while-positive rule and the same reset call, rendered outside any status section; the failure path keeps the badge for a retry instead of alerting (`render/MissedBadgeRenderer.ts:8-14`, `:88-106`) | MATCH (elsewhere). **Note:** legacy alerts on a failed reset, the mux stays silent |
| S5 | User row | the user's email plus a Logout button that destroys both sockets, marks auth "Logged out" and redirects to `/app/auth/login` (`html:1331-1333`, `notifications.js:1376-1396`) | the nav bar shows the email and a Logout that clears the persisted tokens and redirects to the same login page (`render/templates/navBar.ts:100`, `auth/authGuard.ts:83-90`) | MATCH (elsewhere) |
| S6 | Sessions row | the queue and audio session ids as `<code>`, each with a 📋 copy button that no-ops on an empty value or `-` and shows a brief checkmark (`html:1336-1349`, `notifications.js:1577-1612`) | no session-id readout and no copy control for it (search above) | **OWED** |
| S7 | Health Monitor row | the 90 s WebSocket health monitor's own line: "Monitoring (90s interval)" / "Stopped" / "✓ Healthy (checked HH:MM:SS)" / "Circuit open — click Retry-now" / "Watchdog: queue=… audio=…" (`notifications.js:1103-1180`) | — | **OWED** |
| S8 | Config row | a "↻ Reload" button that calls `GET /api/init`, disabling itself and dimming while in flight, then writes `✓` in green or `✗ <message>` in red beside it, and re-enables in a `finally` (`html:1355-1361`, `notifications.js:1399-1458`) | — | **OWED** |

**A14 build items**: the section itself (B1–B4, B7, B8, B10) plus S1, S2, S3, S6, S7 and S8. S4 and S5 already exist in the multiplexer and are placement questions for Phase 1, not build items — except the one behaviour difference noted at S4 (legacy alerts when a missed-reset fails).
