### A9 — Holding Area `✓`

Legacy: `notifications.html:942-1077` (markup, including the flow-ratio cluster at `:993-1067`), `notifications.js:9693-9769` (fetch + filer grouping), `:11210-11442` (ratio formatting), `:11443-11562` (ratio fetch + paint), `:11563-11798` (settings fetch/save, manager-pull, reset), `:11799-11893` (control bind + retrying paint), `:12436-12452` (the shared tick), `:13804-13867` (render), `:13869-13921` (one group), `:13923-13966` (the pane's only click listener), `:13968-14042` (row ids, group status, batch-button lockout), `:14043-14166` (the batch loop and the two batch verbs), `:14167-14183` (refresh). Multiplexer: `render/HoldingAreaRenderer.ts`, `render/holdingAreaModel.ts`, `render/holdingAreaBatch.ts`, `render/templates/holdingAreaTable.ts`, `stores/HoldingAreaStore.ts`, `render/templates/sectionToolbar.ts:65`, `boot.ts:700-717`.

The batch machinery is a close, deliberate port — in two places the multiplexer fixed a legacy defect on the way (rows T5 and T6). The gap is the operator control cluster, which the multiplexer does not have at all.

| # | Behaviour | Legacy (lead) | Multiplexer | Verdict |
|---|---|---|---|---|
| B1 | Default disclosure | **open**: `#holding-area-section` carries no `collapsed` class (`html:966`) (source) | **open**: `data-collapsed` is set only by a click (`HoldingAreaRenderer.ts:249`, `sectionHeader.ts:157-164`) (source) | MATCH |
| B2 | Toggle trigger | a click anywhere on `.section-header` → `toggleSection('holding-area-section')` (`html:954`) (source) | delegated header click, controls exempted (`sectionHeader.ts:145-192`) (source) | MATCH |
| B3 | Persistence | **not persisted**: `holding-area-section` is absent from `LUPIN_ACCORDION_PERSIST_KEYS` (`html:1517-1532`) (source) | **not persisted**: session-only (`sectionHeader.ts:12-15`) (source) | MATCH |
| B4 | Toolbar show/hide | `🗃️` → `section-holding-area`, added on Rick's 2026-09-02 voice ruling, persisted to `lupin_section_visibility` (`html:51-63`) (source) | `🗃️` → `holding-area-pane`, persisted to `view_state_section_visibility` (`sectionToolbar.ts:60-65`) (source) | MATCH (different keys, as elsewhere) |
| B5 | Collapse/expand-all | not affected — the toolbar pair acts on Task List owner groups (`js:14312-14337`) (source) | not affected — the toolbar pair acts on Notifications (`SectionToolbarRenderer.ts:115-122`) (source) | MATCH |
| B6 | Auto-reveal | none found (search: the only writers of this id's `collapsed` are `toggleSection` and `applyPersistedAccordions`) (source) | none found (source) | MATCH |
| B7 | Live header | `🗂️ Holding Area · <count>` — the count is the number of held rows, or **`—` under any sentinel**; plus the PROMOTE-request badge; plus **the flow-ratio readout `#task-list-flow-ratio`**, which states the created-vs-closed ratio across every project and priority (`html:955-960`, `js:13829-13844`, `:11476-11562`) (source) | `🛑 Holding Area · <count>` with the same count rule including the em dash under a sentinel, plus the promote badge. **No ratio readout** — search `grep -rln "flow-ratio\|flowRatio" multiplexer --include=*.ts \| grep -v test` returns only `templates/finishedTasksTable.ts`, and that is the `flow-ratio-field` CSS class borrowed for the window slider, not the readout (`HoldingAreaRenderer.ts:206-215`, `:546-548`) (source) | **OWED** — the flow-ratio readout. Count and badge MATCH; the header glyph differs (🗂️ vs 🛑) and that is Phase 1's business |
| B8 | Refresh control | `#holding-area-refresh` renders in the header with **no `onclick` and no listener anywhere** — search `grep -rn -F holding-area-refresh static/` hits only the markup at `html:961-963` and the multiplexer's own button; the positive control `task-list-refresh` carries its handler inline on the element at `html:869`. So the legacy ⟳ does nothing except collapse the section, since the header's toggle is what its click reaches (source) | ⟳ → `store.refresh()`, and being a `<button>` the header guard exempts it, so it refreshes without collapsing (`HoldingAreaRenderer.ts:195-200`) (source) | **EXTRA** — the multiplexer's button works. ⚠️ Do not "restore parity" here: legacy's is a dead control, and this is a legacy defect worth its own row |
| B9 | Nested accordions | **none, deliberately**: rows are grouped by FILER into `.holding-area-group` divs with a header bar, and the legacy source says in as many words that this pane has no accordion, so a non-control click is ignored rather than collapsing anything (`js:13869-13921`, `:13941-13943`) (source) | the same filer grouping and the same explicit no-accordion note (`templates/holdingAreaTable.ts:13-15`) (source) | MATCH |
| B10 | Empty / degraded states | **four**: `auth_required`, `query_unavailable` ("a deploy defect, not an outage"), `unreachable` ("last known state not shown" — and it deliberately does **not** replay stale rows the way the task list does), and the genuinely-empty "Nothing waiting on triage." (`js:13830-13861`) (source) | the same four, same strings, carbon-copied — plus a **pre-first-poll null composite takes the unreachable sentinel** rather than claiming the queue is empty, and a status-less composite with a non-array `tasks` is treated as malformed rather than empty (`HoldingAreaRenderer.ts:85-95`, `:288-320`) (source) | MATCH, with two multiplexer refinements that are EXTRA |
| B11 | Layout mode | not referenced by the layout code (search: `grep -n section-holding-area notifications.js` → no match) (source) | a sibling pane in the main column (`multiplexer.html:254-255`) (source) | MATCH |
| H1 | Poll cadence | **no timer of its own**: `refreshHoldingArea()` rides the task list's 60 s tick — "one clock, two fetches", because the held query is a second question the composite cannot answer (`js:12446-12449`) (source) | **its own** 60 s `setInterval`, started after mount, for the same stated reason — a second FETCH rather than a second view (`HoldingAreaStore.ts:130`, `:258-261`, `boot.ts:700-715`) (source) | MATCH in cadence. **Note:** one clock vs two. Two timers can drift apart, which is the failure legacy's shared tick exists to avoid — worth María's eye, but it is not a behaviour the operator can see today |
| H2 | Grouping | by FILER, not owner, because triage asks "what did this person file" and the two fields disagree on roughly a quarter of live rows; groups alphabetical (`html:942-952`, `js:9731-9769`) (source) | the same, in `groupHeldRowsByFiler` (`holdingAreaModel.ts`) (source) | MATCH |
| H3 | Group header | filer label · count · **Approve all** · **Won't fix all** · one batch-reason input · a status span, every control carrying `data-filer` (`js:13903-13920`) (source) | the same six elements with the same two tooltips, carbon-copied verbatim and pinned against the legacy strings (`templates/holdingAreaTable.ts:36-100`) (source) | MATCH |
| H4 | Rows | rendered through the **same row renderer** as the task list, so the ⋯ disclosure, the verbs, the detail 📄 and the id-copy all behave identically (`js:13901`, `:13954-13962`) (source) | the same shared row templates (`templates/holdingAreaTable.ts` → `taskRowControls` / `taskRowDisclosed`) (source) | MATCH — and A8's row-level OWED items (the mic, the datalist, the priority Update button, the verb order) apply here too, since both panes draw the same row |
| H5 | Which rows a batch acts on | read off the **rendered DOM at press time**, keyed on `.task-verb-select[data-task-id]` (a control every row has) and filtered to rows where the `approve` option is **enabled** — never a cached list, which would go stale the moment a peer approved something (`js:13968-14003`) (source) | the same rule, and the same two traps named (`HoldingAreaRenderer.ts:389-416`) (source) | MATCH |
| H6 | Batch approve | no reason, no confirm — the non-destructive direction, since an over-approved row is demoted straight back (`js:14136-14147`) (source) | same (`holdingAreaBatch.ts`, `HoldingAreaRenderer.ts:361-369`) (source) | MATCH |
| H7 | Batch won't-fix | terminal, and refuses **before any request leaves the browser** when the one shared reason is blank; every row in the group gets that same reason, and the tooltip says so (`js:14149-14166`) (source) | same, via `HOLDING_BATCH_BLANK_REASON` (`HoldingAreaRenderer.ts:497-501`) (source) | MATCH |
| H8 | Batch loop | **sequential, never `Promise.all`** — eight parallel 403s against one allowlist race in an unreproducible order. Counts successes and refusals separately, keeps the FIRST server message, repaints the status line **after every row** (the in-flight number counts ATTEMPTS), and both batch buttons are dead for the whole batch (`js:14043-14134`) (source) | the same loop, the same attempt-counting, the same both-buttons lockout — plus the lockout is backed by a `Set` keyed on filer, because `disabled` lives on an element the 60 s poll repaints (`HoldingAreaRenderer.ts:163-172`, `:490-537`) (source) | MATCH, with the set-based guard as EXTRA |
| H9 | Where the final report lands | painted, then `refreshHoldingArea()` is awaited on the **next line** — and that refresh rebuilds every group including the status span, so the partial-failure report the method exists to produce is **erased before it can be read** (`js:14126-14132`) (source) | the refresh is awaited **first** and the final line painted after it, onto the group that survived — the group holding the refused rows; and remembered per filer so a later poll cannot erase it either (`HoldingAreaRenderer.ts:326-341`, `:465-473`, `:533-536`) (source) | **EXTRA** — the multiplexer fixed a legacy defect. Keep it; do not port legacy's ordering back. Worth its own legacy bug row |
| H10 | Read-after-write | the batch calls `refreshHoldingArea()`, whose in-flight guard means it can **join** a fetch that began before the transitions landed (`js:14167-14183`) (source) | a separate `refreshAfterWrite()` that guarantees a read which STARTED later, and it is on the store interface on purpose so a fake cannot blur the two (`HoldingAreaRenderer.ts:100-109`, `:533-535`, `HoldingAreaStore.ts:253`) (source) | **EXTRA** — same class of fix as H9 |
| H11 | Flow-ratio operator cluster | inside the section body (never the header, which would collapse on every drag): **Gate opens below** threshold slider 0–200 % → `PATCH /api/tasks/flow-ratio/settings`; **Window** slider 1–14 days (days on screen, hours on the wire); **Freeze manager pull** checkbox, `checked` in the markup so an unreachable server cannot read as "pull is allowed", writing `PATCH /api/tasks/manager-pull`; **Reset** to configured defaults; a status line naming the SOURCE ("saved override" / "from config"). Writes on `change`, never `input`. The cluster stays HIDDEN until a settings fetch succeeds, and a failed paint is retried on the next tick while the bind happens exactly once (`html:993-1067`, `js:11563-11893`) (source) | **entirely absent** — search `grep -rln "flow-ratio\|flowRatio\|manager-pull\|managerPull" multiplexer --include=*.ts \| grep -v test` returns one file, `templates/finishedTasksTable.ts`, and only for the borrowed `flow-ratio-field` class. Legacy's own comment already records the absence: "No-op when the controls are absent (the multiplexer page has no cluster; this file serves both)" (`js:11828-11829`) (source) | **OWED** — five controls plus their hidden-until-readable rule and their retrying paint |
| H12 | Truncation warning | the same banner the task list uses, fed this pane's own query so its own `limit` is read rather than the board's — measured: a status-filtered call to the same endpoint returned exactly 500 of 1,912 rows, silently (`js:13846-13856`) (source) | **absent** (same search as A8 T14: no `has_more` / `truncated` anywhere in non-test multiplexer source) (source) | **OWED** — and it is the same build item as A8 T14, needing the per-pane query |
| H13 | Operator-state survival | `_captureOperatorState` / `_restoreOperatorState` wrap the repaint here too, so a typed batch reason or an open disclosure survives the 60 s tick (`js:13863-13865`) (source) | **absent** for the row/disclosure state; only the batch REPORT is remembered (`HoldingAreaRenderer.ts:326-339`) (source) | **OWED** — same build item as A8 T16, and the batch-reason box is the sharpest case: it is typed slowly and cleared by the next poll |
| H14 | Request chips | the promote-request badge beside the count and Approve/Deny behind each row's pending chip, hydrated after every paint (`html:955-957`, `js:11107`, `:13866`) (source) | the same through `wireRequestPane` with `BADGE_HOLDING_AREA` (`HoldingAreaRenderer.ts:221-230`, `:344-347`) (source) | MATCH |
| H15 | No search box | **deliberately none** — Rick removed it on 2026-09-11 after a day's use, because the Task List's Find box already reaches held rows through the visibility-free endpoint (`html:1068-1072`) (source) | none, and boot records the same ruling as the reason for passing no `lookupFetch` (`boot.ts:707-708`) (source) | MATCH |

**A9 build items**: (1) the flow-ratio readout in the header (B7); (2) the flow-ratio operator cluster — threshold slider, window slider, Freeze-manager-pull, Reset, source line, hidden-until-readable, retrying paint, writes on `change` only (H11); (3) the truncation banner fed by THIS pane's query (H12, shares A8 T14); (4) operator-state capture and restore across the repaint, the batch-reason box especially (H13, shares A8 T16). Plus A8's shared row items, which this pane inherits.

**Legacy defects found while inventorying, for a separate row — do NOT back-port**: the `#holding-area-refresh` button is unwired (B8); the batch's final report is erased by the refresh on the next line (H9); the post-batch read can join a fetch that predates the writes (H10).
